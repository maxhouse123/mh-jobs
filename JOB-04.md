# JOB-04 · 学生抽屉档案补全 + 材料时间精确到分

**结果：全闸通过**

## 做了什么（一句白话）
学生填资料时往库里写了 26 项，管理端抽屉「档案」以前只显示 7 项，护照号、出生日期、中文名、家庭住址、宗教、CSCA、资助方式、个人陈述等根本没读出来。这次把它们全读出来，并把档案分成**四组**（身份 / 联系 / 学业 / 意向），一眼能找到想看的那项；空的项显示破折号，不再是空白。材料上传时间以前只到「日」，现在精确到**分**，能对上「今天上午重传过」。

## 业主要看什么（打开哪里、点哪个、应该看到什么）
1. 管理端 → 左边「学生」→ 点任意学生 → 抽屉里原来的「📇 档案」现在变成四段带标题的小块：
   - **🪪 身份**：申请号 / 学号 / 姓名 / 中文名 / 护照号 / 出生日期 / 性别 / 国籍 / 宗教
   - **📞 联系**：电话 / WhatsApp / 微信 / 邮箱 / 家庭住址
   - **📚 学业**：HSK / CSCA / GPA / GPA 档位
   - **🎯 意向**：目标学校 / 意向专业 / 入学学期 / 资助方式 / 在境内还是境外 / 个人陈述 / 申请日期 / 下一步
2. 哪一项库里没值，就显示「—」，不会是空白或 undefined。
3. 抽屉往下「📎 材料（云端）」区，每条材料的时间现在显示到分钟（如 `2026-09-15 09:45`）。

**注意**：护照号、出生日期只在抽屉里显示，**不进学生列表、不进任何导出**（按队列边界）。

## 十道闸（逐条 + 实测数字）
| 闸 | 结果 | 实测 |
|---|---|---|
| 1 基底md5 | PASS | `376a9068391aa88248963c3a5ae79794`（=JOB-05 产物）|
| 2 锚点唯一 | PASS | 6 处锚点各出现 1 次 |
| 3 补丁自检 | PASS | select 含 10 个新列=1、旧 23 列 select 归 0；映射 passportNo/birthDate/religion/csca 真值各=1；`_v174Profile`/`_v174InChina` 定义与调用各 1；材料 `substring(0,16)`=1。反向样本拿 v173 跑，3/3 失败 → 有分辨力 |
| 4 产物md5 | 记录 | `dc54b4324472cd5bf6246d71493cd76e` |
| 5 语法 | PASS | 脚本块 0 报错 |
| 6 功能清单 | PASS | 函数 548→550（新增 2：`_v174Profile`/`_v174InChina`）；id/window/onclick 全等；**消失 0** |
| 7 四语 | PASS | 管理端中/英双语（无俄法）；新增文案中英齐全、无裸键 |
| 8 真点击 | PASS | 真浏览器渲染 `_v174Profile`：档案字段 **26 个**（要求≥17）、四组标题齐、护照号/出生日期/中文名/家庭住址/宗教/CSCA/资助/个人陈述全部上屏、空值（宗教）显破折号、在境内/境外映射正确、材料时间显示到 `09:45`、**390px 不横向溢出**（截图 `~/mh-verify/gate8_job04.png`）|
| 9 视觉一致 | PASS | 新写死颜色 **0**、新 CSS 类 **0**、新字号 **0**（四组复用现成 `dd-sec`/`dd-grid2`）|
| 10 线上版本 | 跳过 | 本轮不推送 |

## 基底 / 产物 md5
- 基底：`376a9068391aa88248963c3a5ae79794`
- 产物：`dc54b4324472cd5bf6246d71493cd76e`

## index.html 改了几处
3 处，v173→v174，无残留。

## 提交哈希
主仓库 `8e536cc`（已 commit，未推送）。

## 偏离或疑点
1. **档案分组用了 4 个小节标题**（身份/联系/学业/意向），把原来单个「档案」小节替换掉了——这是队列「分四组」的要求，复用现成小节样式，没有新增任何样式。原有的 7 项一个不少地放进了对应组里（申请号/学号在身份组、申请日期/下一步在意向组），没丢信息。
2. **「在境内/境外」字段**：库里这列（`in_china`）具体存的是什么值我没有实测（true/false 还是别的），所以写了个兼容映射——认得 true/1/yes/境内 → 显「境内」，false/0/no/境外 → 显「境外」，其它没见过的值就原样显示，不会显错。等你验收时如果看到某个学生这项显示的是原始值（不是「境内/境外」），告诉我实际存的是什么，我再精确适配。
3. 同 JOB-05：新函数名写进了头注导致「调用次数」自检最初多算 1 次，已把自检改成精确匹配调用整句，已通过。
