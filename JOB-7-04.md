# JOB-7-04 · 「Start a new round」卡 390px 断词 — 回执

**结果：做完，学生端 v386。390px 走查 4/4 过。一处卡片 CSS。**

## 大白话
巡检第 1 张图：手机窄屏（390px）下那张「Start a new round / 开启新一轮」卡，左栏被右边那个「In progress · 26 day(s) until it can be ended」药丸挤到只剩 100px 宽，英文单词被硬折成「Availabl-e」「accoun-t」，药丸自己又不换行、把整张卡撑破。

修：**就那一张卡的布局 CSS，三处微调**——
1. 卡片容器加 `flex-wrap:wrap`：放不下时药丸整块换到下一行，而不是硬挤。
2. 左栏加 `flex:1 1 200px`（本就有 `min-width:0`）：能伸能缩、优先占一行、文字不再被压到断词。
3. 药丸加 `white-space:normal`：真放不下时药丸内文字可换行，不撑破。

没动任何逻辑、没加新样式类、没碰其它卡。

## 版本
学生端 v385 → v386（index 指针同步）。

## 闸 8（390px 走查）
`tests/job7-04.mjs`（视口 390px，注入该卡真实 markup + 最长英文药丸文案测量）：
- 卡片真实布局宽 356px（= 390 − 内边距），**内部零横向溢出** ✅
- 页面 390px **零横向溢出** ✅
- 药丸真实渲染宽 302px（说明它换到自己一行、没被裁切）✅
- 「Start a new round」标题**高度 20px = 单行**（不再断词换行）✅
- **4/4 全过**；JS 解析干净。

## 动过的文件
- 改：`student-portal/maxhouse_student_portal_v386.html` + `student-portal/index.html`；新增 `tests/job7-04.mjs`

**主仓库未推。本回执已推日志仓库。**
