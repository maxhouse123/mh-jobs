# JOB-6-09 回执 · 收拾仓库根目录（2026-09-16）

> 大白话版。把仓库根目录堆着的 78 个 `patch_*.py` 挪进了归档文件夹，**是搬走不是删掉**，随时能翻回来。仓库本身没任何变化（这些文件本来就没进 git）。

## 做了什么
1. 数清楚：根目录有 **78 个** `patch_*.py`。
2. 确认它们都没被 git 跟踪（`git ls-files patch_*.py` 为空，`.gitignore` 一直挡着）。
3. `mv patch_*.py ~/maxhouse-patch-archive/` —— **搬走，不是删**（归档夹沿用 9 月 3 日的先例，已存在）。
4. 搬完根目录 0 个残留；归档夹现有 **226 个**（原 148 + 这次 78）。

## 验证
- 搬前搬后 `git status` **完全一样**（都只有 `?? jobs/`）—— 仓库零变化，符合「零提交」边界。
- 没动任何被跟踪文件、没动任何非 `patch_*.py` 文件。

**边界守住**：只动根目录的 `patch_*.py`；是 mv 不是 rm；零提交、零推送。
