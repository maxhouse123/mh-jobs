# JOB-23-01 回执 · 管理端 v220：摘掉 3 个假入口 + 左下角填真邮箱 + 五处「导出」改成真 CSV

做这一步的人：CC（无人值守）。这一包动了管理端一端的代码，版本从 v219 升到 v220（旧版保留可回滚）。

## 一、做了什么（大白话）

1. **侧栏摘掉 3 个假/占位入口**（点进去全是编造数字、误导你判断）：
   - 「推荐归因」——里面 15 条种子学生 + 假合作伙伴 + 一个假佣金合计 ¥4,800；
   - 「数据洞察」——整页写死的假图表；
   - 「材料范例库」——读的是本机浏览器缓存，不是云端。
   三个入口从左栏拿掉了。**但**：它们的页面本体、渲染代码、词典文字**一个都没删**，只是不在左栏露面了（这是既定的「演示车道·销债不销码」做法，将来要真做时代码还在）。
2. **左下角「账户设置（敬请期待）」占位去掉**：以前点它只弹一句「敬请期待」。现在登录成功后，左下角会自动显示你的**真实登录邮箱**、头像变成邮箱前两个字母大写、角色显示「管理员」。取不到邮箱时保持原来的 Yan Liu / Super Admin / YL。
3. **五处假「导出」全部改成真下载 CSV**：顶栏的下载图标、驾驶舱的「导出报告」、学生页 / 申请记录页 / 付款流水页的「导出」，以前点了只弹一句「已生成 xx.csv」，其实什么都不下载。现在：
   - 在**学生 / 申请 / 付款**这三个列表页点导出 = **所见即所导**：你搜索、筛选、隐藏测试之后屏幕上是哪些行，导出的就是哪些行。
   - 在**其它页**点导出 = 弹一个三选一的框（学生 / 申请 / 付款），框上的条数是**真实条数**（读首屏缓存），选完导出那一整套。
   - 导出的文件是标准 CSV（带 BOM 头，Excel/Numbers 打开中文不乱码），文件名形如 `maxhouse-学生-20260925-1530.csv`；导出前后各有一句提示。
   - 删掉了写死的「47 / 82 / 38」假条数和「已生成」假提示。

版本四件套已同步：新文件名 v220、头注、[VER] 控制台探针、index.html 三处重定向，外加新版横幅用的 `MH_PORTAL_VER=220`。

## 二、结果是什么（都过了机器验）

### 先红 → 后绿（这一包对应第 23-00 的 R1、R2）
- R1（推荐归因假数据）：入口已摘，左栏点不到 → 绿。
- R2（假导出）：学生页点导出，**真下载**了一个 CSV，文件头是 UTF-8 BOM（EF BB BF），行数 = 桩里非测试学生数（7 行），表头含「申请号 / Application No」→ 绿。

### 闸3 判据（逐条）
| 判据 | 结果 |
|---|---|
| ① 左栏 归因/洞察/范例库 nav 项 = 0；其余 nav 项 = 28；三个 #view-* 容器仍在 | ✅ 0 / 28 / 3 |
| ② 无 `showToast(…敬请期待…)`；`doExport` 函数体无「已生成」 | ✅ 0 / 0 |
| ③ 学生页真下载：3s 内到、首 3 字节=BOM、行数=非测试学生数 7、表头含 申请号/Application | ✅ 中英各一遍 |
| ④ 驾驶舱弹框三项、各含真实条数（学生 10/申请 6/付款 15）、选付款→下载、行数=15 | ✅ 中英各一遍 |
| ⑤ `user.roleAdmin` 中英各 1；登录桩后左下角=邮箱/头像 AD/角色 管理员；点左下角不弹 toast | ✅ |
| ⑥ N1 差集：无任何函数/id/handler 消失 | ✅（新增 7 个 `_v220*` 函数） |
| ⑦ 零 pageerror；中英切换零裸键 | ✅ 报错 0 / 裸键 0 |

### 三层自验
- **P3 静态断言**（随卡入仓 `tests/job23-01-admin-v220.mjs`）：**28/28 全过**。
- **闸8 无头真渲染**（`jobs/JOB-23-01/repro/green.mjs`，中英各一遍）：**8/8 全绿**，pageerror = 0。
- **闸9 零新增样式**：v219 与 v220 的 `<style>` 全部代码块**逐字节相同**（长度 67422 = 67422）→ 没加任何新 CSS / 颜色 / 字号。
- **回归**：23-00 的七处旧病重跑仍**全红**（改的是行为，尺子没坏）。

### 有意移除清单（对照 QUEUE-23 §0.3 授权）
| 移除项 | §0.3 授权 | 说明 |
|---|---|---|
| 侧栏 `data-view="attribution"` nav-item（含 1 个 inline onclick） | 第 1 项 | 容器/renderAttribution/注册/词典键保留 |
| 侧栏 `data-view="analytics"` nav-item（含 1 个 inline onclick） | 第 1 项 | 同上（renderAnalytics） |
| 侧栏 `data-view="samples"` nav-item（含 1 个 inline onclick） | 第 1 项 | 同上（renderSamples） |
| `.sidebar-user` 的 `onclick=showToast(…敬请期待…)` | 第 2 项 | 换成登录后填真邮箱 |
| `doExport` 只弹 toast 的假路径（函数体改写，函数名保留） | 第 3 项 | 不算移除，属改写 |
> N1 机器差集：按**函数/元素名**比对，**零消失**（被删的 3 个 `switchView(...)` 与 1 个 `showToast(...)` 都是调用实例，函数名本身在别处仍在用，故名集不减）。

## 三、业主要做什么

代码层面这一包机器已验全绿，无需你现在操作。真正要你**肉眼验收**的（留到收官统一给）：
1. 打开管理端，左栏确认没有「推荐归因 / 数据洞察 / 材料范例库」三项；
2. 登录后看左下角显示的是你的真实邮箱，点它不再弹「敬请期待」；
3. 进学生页点顶栏下载图标，真下载一个 csv，用 Numbers 打开，列名和页面表头对得上。

## 附：闸账
- secret-scan：JOB-23-01 + 测试脚本 = CLEAN ✅。
- 主仓库：v220 + index + changelog + 测试 + 桩工具已 commit（33bfd45，**不 push**）；主仓库现领先远端 2 笔（均为本轮 JOB-23-xx，正常）。
- 本回执在 ~/mh-jobs commit 后立即 push。

下一包 = 23-02
