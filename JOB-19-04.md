# JOB-19-04 回执 · 学生端 v398 —— 材料名引号按语言

## 一句话
`_v706HumanizeIssue`（把 AI 判词里带引号的内部代码换成材料名）此前包裹的引号恒为中文「」（四语都「」）；本包按当前界面语言 `currentLang` 选引号 —— **zh「」/ en ""/ ru «»/ fr « »**（fr 内含空格、ru 无）。**只改这一处替换用的开/闭引号**，替换范围、判词、判定一律不动、不重判。**student v397 → v398**，四件套齐；真页面（stuA 真登）红绿四语全过 + N1 差集空 + P3 回归 22/22。CC 只 commit（主仓库一笔）、**未推主仓库、无迁移、无 SQL、无部署**。

## 做了什么
### 唯一改动（`_v706HumanizeIssue`，v397 第 10103 行起）
替换前（v397，恒中文引号）：
```js
out = out.replace(re, '「' + map[code] + '」');
```
替换后（v398，引号随语言）：
```js
var _hl = (typeof currentLang !== 'undefined' && currentLang) ? currentLang : 'en';   // 当前界面语言
var _qo = _hl === 'zh' ? '「' : _hl === 'ru' ? '«' : _hl === 'fr' ? '« ' : '"';   // 开引号
var _qc = _hl === 'zh' ? '」' : _hl === 'ru' ? '»' : _hl === 'fr' ? ' »' : '"';   // 闭引号
...
out = out.replace(re, _qo + map[code] + _qc);   // 原恒 「」
```
- 替换范围正则 `["'‘’“”] + code + ["'‘’“”]`（仅【带引号】的纯英文代码 `/^[a-zA-Z_]+$/`）两版**逐字一致**——不动判词、不重判、不扩缩替换面。
- 新增仅 `_hl/_qo/_qc` 三个函数内局部 `var`（不进 N1 函数集）。

### 四件套（版本 bump）
1. 文件名 `student-portal/maxhouse_student_portal_v398.html`（v397 留盘可回滚）。
2. `[VER] student portal v398 (JOB-19-04: …)` 探针。
3. `student-portal/index.html` 三处重定向（script/meta/canonical + 可见链接）→ v398。
4. `docs/changelog/student-portal-versions.md` 追加 v398 条目（`[VER] v397→v398`）。

### 外科手术式 diff（v397 → v398，全文仅 4 段）
`diff` 证实全文改动 = ①[VER] 行 ②新增 `_hl/_qo/_qc` 三行 ③`replace` 那一行。其余 200 万字节**逐字节不变**——`_aiHintBandHtml`（承载判词的琥珀提示条）等一切未动。

## 结果（三层自验 + 新闸）
### 真页面红绿（stuA 真登，只读，零写库）—— `jobs/JOB-19-04/redgreen.mjs` → `redgreen-results.json`
直接调用真页面 `window._v706HumanizeIssue`（真 DOCS 数组 / 真 `t()` / 真 `currentLang`），判词样本 `Please re-upload a clear full page of '<criminal>'.`（无犯罪证明栏那条判词）。每语切 `setLang` 后**等 `currentLang` 真切**（v351 起 ru/fr 词典包异步加载，须 await）再量包裹引号 + 元素级截图。
```
RED (v397):  zh/en/ru/fr 四语包裹皆 「」（bug：语言被忽略）
GREEN(v398): zh「无犯罪记录证明」 · en "Police clearance certificate" · ru «Справка об отсутствии судимости» · fr « Extrait de casier judiciaire »
VERDICT: red 恒「」=true / green 逐语随语言=true / zh 仍「」=true / OVERALL PASS=true
```
截图（元素级、真实文字、无 PII，账号无邮箱出现）：`~/mh-jobs/shots/19/19-04/{red,green}-{zh,en,ru,fr}.png` + `INDEX.md`；完整版 `~/mh-verify/shots/JOB-19/19-04/`。

### P3 随卡回归（入仓）—— `tests/static-check-v398-student.mjs`
**22/22 绿**：①静态（四件套 + v398 引号分支 + v397 红对照硬编码「」 + 替换范围正则两版一致）②N1 差集（ids/handlers/functions removed 0 / added 0）③逻辑模型（逐字复现 `_qo/_qc` 选择，四语断言随语言 + v397 恒「」红对照）。随本包入主仓库 tests/。

### 闸1（零裸 i18n / 逐键）
本包**未新增任何 t() 键**（只换包裹字符），不存在裸键陷阱。材料名标签沿用现役 `doc.*` 词典，真页面实测四语标签均真实且随语言（无犯罪记录证明 / Police clearance certificate / Справка… / Extrait…），非回落英文。

### N1 差集空
同一提取器 v397 vs v398：ids 368→368、handlers 188→188、functions 1116→1116，**0 lost / 0 added**（`jobs/JOB-19-04/inv/`）。内联 JS 该函数块 `node` 解析 0 错。

## 新闸证据 N1–N7
- **N1**：v397→v398 差集空（见上）；改动全为 diff 可证的外科手术式增改。
- **N2**：旧病＝四语判词引号恒「」——本包真页面成对坐实：RED(v397) 四语皆「」（红）↔ GREEN(v398) 英/俄/法随语言、中文仍「」（绿），逐语 ok=true。
- **N3**：真身份 = stuA 真登录学生端（真 `signInWithPassword`，非注入会话），**只读、零写库、零上传**；脚本不打印 token/密码/key。
- **N4**：不适用云函数分支矩阵（本包纯前端字符串函数，不连 EF/DB）。语言分支矩阵（zh/en/ru/fr × 开/闭引号）由真页面 + 逻辑模型双证。
- **N5**：截图为元素级、真实文字、账号邮箱无出现（判词样本仅材料类型名，无 PII）；调试期**不推送**，最后一次干净后过 secret-scan 再推；脚本一律不打印敏感值。
- **N6**：只改学生端一个文件（+ index 重定向 + changelog + 一支 tests + 本包 jobs 工装）；共用查看器 `assets/mh-viewer-v2-1-1.js`、`kit2/`、学校端、管理端、审核端、precheck-ai、admin-doc、watermark-doc、send-invite-email **一行未动**。
- **N7**：改动=既有琥珀提示条（`_aiHintBandHtml`，**持久条、非 toast、无自动消失计时**）内材料名的包裹引号；diff 证实该条渲染逻辑与配色**逐字节未变**，不新增/不改提示件生命周期，不被后续 toast 顶掉的行为与 v397 一致。属字符级替换，无新计时/可见性风险，N7 无回归。

## 测试写库收尾
本包**零 DB 写入、零上传**；唯一真身份动作＝ stuA 真登录（读会话，登录本身不写业务库）。无测试件需清理；无带身份探针复核之必要。

## 业主要做什么
- 无需操作。主仓库本包 commit 一笔待收官卡（1145）统一推；无迁移、无 SQL、无部署。
- 收官后可亲眼验收（进 SUMMARY「要业主亲眼看的」）：学生端切英文/俄文/法文界面，AI 判词里材料名的引号随语言（英 "…"、俄 «…»、法 « … »），中文仍「…」。

## 给 19-05 的交接
- 学生端进到 v398。三端本轮版本：partner v167（19-01）/ reviewer v45（19-03，watermark-doc v34 待部署）/ student v398（19-04）。
- **迁移**：19-04 无迁移。下一空号仍 0213。
- 19-05 = 历史存储孤儿：按 19-00 ⑥ 结果**只查证、出清单（不删）**，写 `jobs/JOB-19-05/orphans.md` + 回执 + 一条「怎么确认删对了」只读 SQL；SUMMARY-19 单列「要业主手删的存储路径」。
- 下一包 = 19-05
