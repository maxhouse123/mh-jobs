# JOB-13-05 · 存量 PDF 瘦身工具（CC 只造 + 自测，业主跑）

## 做了什么
- 造了 4 个文件（都在 `jobs/JOB-13-05/`）：
  - `slim.html`：无头驱动页，装 pdf.js + pdf-lib + 13-04 的共享核心 `mh-pdf-slim-v1.js`。
  - `slim-stock.mjs`：Node 用 ~/mh-verify 的 Chromium 打开 slim.html，对一份 PDF 做本地瘦身（存量口径 keepPdf=true：单页也保持 PDF 不转 JPG；多页 2200/0.82；新 ≤ 原 70% 才采用）。
  - `run.sh`：三个模式 `--selftest`（CC 唯一跑）/`--list`（业主）/`--apply`（业主）。
  - `list.sql`：只读清单（供 db-run.sh 跑）。
- **CC 只跑 `--selftest`**（不读 .env、不碰网络/库）；`--list`/`--apply` 留给业主在原生终端挂代理跑。

## 结果是什么（命令带出处）
- `bash -n jobs/JOB-13-05/run.sh` → **SYNTAX OK**；`node --check slim-stock.mjs` → OK。
- `--apply` 分支四关键点 grep（`grep -cF`）：**备份路径 3 / x-upsert 3 / HEAD 确认 3 / DELETE 4**，全有。
- `bash jobs/JOB-13-05/run.sh --selftest`（对 13-04 夹具跑核心）：
  - f1.pdf（3 页）IN=1694KB → OUT=886KB **ADOPT** ratio 0.52 · PDF
  - f2.pdf（单页，keepPdf）IN=1607KB → OUT=690KB **ADOPT** ratio 0.43 · PDF（存量口径：单页也保持 PDF，与 13-04 把单页转 JPG 的差异是**有意的**，见规格）
  - f3.pdf（1KB 文字）**KEEP**（原样）
  - 与 13-04 决策一致：F1 采用、F3 原样；F2 同样采用（只是存量保持 PDF）。
- `bash jobs/db-run.sh jobs/JOB-13-05/list.sql`（只读清单）有输出：
  - **大 PDF 24 件**（未软删/pdf/size_kb>600），最大 4052KB（criminal），其中 8 件已缓存、16 件未缓存。
  - **孤儿对象 1 个**：`43668ae4-96b6-4518-9725-0b1ade089e92/bank/1789571102495_4vtz_mh8-test-3mb.jpg`，3,145,728 字节（3MB），`referenced=false`（无任何 documents 行引用它 → 可安全删）。
- 列名核实：documents 表只有 `size_kb` 一列尺寸（无其它大小列，13-00 schema-probe 已证），故 --apply 只更新 size_kb。

## 业主要做什么
本工具是一次性存量清理，上线后不再用。要清存量（**先恢复 Supabase 服务，见 JOB-13-00 的 402**）：
1. 卡1092 看清单：原生终端（挂代理）跑
   `cd /Volumes/Dev/MAXHOUSE && bash jobs/JOB-13-05/run.sh --list`
   会打印大 PDF + 孤儿对象并写 `jobs/JOB-13-05/list.json`。
2. 卡1093 执行：确认 `/Volumes/Data/MAXHOUSE-backups` 已挂载后跑
   `bash jobs/JOB-13-05/run.sh --apply`
   每件：下载→备份到 `pdf-originals-20260917/`→本地瘦身→采用则 x-upsert 覆盖上传→HEAD 确认→更新 size_kb→删该件 wm-cache 缓存→索引置 pending 让 cron 重灌；孤儿对象最后删。逐件结果写 `jobs/JOB-13-05/apply.log`。
- CC 全程没跑 --list/--apply（红线：碰不到 .env 和 Storage 字节）。
