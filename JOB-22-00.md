# JOB-22-00 回执 · 开工检查 + 探针 + 旧病复现（先红）+ 旧版按钮清单

CC 执行。第二十二轮第一包。只读侦查 + 造探针脚本，未改任何端 HTML、未写库、未 commit 主仓库代码文件、未 push。

## 一、开工检查（§0.2）—— 全部对上，无「必须停」
- 五端 md5 全等基线：admin v218 `73b36f0a…` / reviewer v45 `c2cf1f26…` / partner v169 `7bbddf58…` / student v399 `dd3a5bcc…` / school v234 `a2d2e593…` / viewer `ee351485…`。
- 云函数 md5 全等：watermark-doc `194b8503…` / precheck-ai `50a3df3c…` / admin-doc `ac5f227a…`。
- 迁移账本到 0213（已应用），0214 无（空）。
- `git fetch` 后本地 HEAD = origin/main = `4f5fa56`（第二十一轮 7 提交业主已推）；`git status --porcelain --untracked-files=no` 为空。
- `~/mh-verify/accounts.json` 在（version 1，四账号 stuA/partner1/reviewer1/schX 均齐）；**四账号本地真页面真登全 OK**（`probes/login-smoke.mjs`：partner1/reviewer1/stuA/schX 逐个 LOGIN OK，无打印密码）。
- `git rev-parse HEAD` → `jobs/JOB-22-00/base-head.txt` = `4f5fa56ff86def406db11a7f8e99008972c6b703`。

## 二、探针（只读）

### ① `_headers` 逐块表（`probes/p1-headers-table.md` 全文）
- 根病坐实：Link 全挂在 `/<portal>/*`（第 61–70 行），命中该目录**所有**资源（含 `vendor/supabase.js`）→ 每取一个同目录文件就再来一轮 preload。本地实测 Chromium **确实处理子资源响应上的 Link preload**（`diag-subresource.mjs`：子资源 Link 预加载未用资源→告警），这就是业主「3→4→7→8 递增」的机理（线上再叠 CF 把 Link 转 103 Early Hints）。
- 五端 `<script src=.../vendor/supabase.js>` 均为**普通同步脚本、无 crossorigin/defer/module/查询串**（grep 行号在表内）→ Link 的 `as=script` 不带 crossorigin，属性一致，无需改属性。
- 字体路径 `/assets/fonts/*`（第 51 行）**无 Link 规则**，字体响应本身不带 Link（正确）。

### ②本地重现预加载警告（`probes/serve-with-headers.mjs` + `probes/probe-preload.mjs`；日志 `repro/preload-before.log`）
- 造了按 `_headers` 规则回放 Link/Cache 头的本地静态服务器（解析 `_headers`，Cloudflare 语义「靠后同名头覆盖」，`! 名`摘除）——**留给 22-01 做前后对比**。
- **检测器已验真**（`diag-detector.mjs`）：对 200 响应上 Link 预加载一个真没用到的资源，Chromium 必报「preloaded using link preload but not used」→ 检测器有效。
- **console 告警本地实测 = 0 条（五端全 0）**：本地服务器把 Link 放在 200 响应上，Chromium 首屏把预加载的 supabase.js + 字体 400/700 **全用上了**（全 0 unused 告警），故无告警。业主线上看到的告警来自 **CF 边缘 Early Hints(103) 早发 + 转 103** —— 本地 HTTP/1.1 服务器发不了 103，**这条 console 告警本地不可复现**（与第二十一轮 21-04 结论一致）。按 N2：两条思路（Link 于 200 / 子资源 Link）均未在本地触出该 console 告警 → 标**未复现·未放行·等业主线上核**；但根病有**确定的、可量的替代红信号**（见下）。
- **结构红信号（确定红→22-01 转绿）**：量「响应头 Link 出现在哪些响应上」。**改前：五端各 1 条子资源泄漏**（`vendor/supabase.js` 也带 Link）→ TOTAL Link-leaks=5 → **STRUCTURAL: RED**。22-01 把 Link 只挂网页文档后应为 0。这正是 22-05 线上走查要断言的（「响应头 Link 只出现在网页文档上」）。

### ③中介端卡片重叠（子代理跑 `probes/p-card-overlap.mjs`；`repro/card-overlap-before.{log,json}`）
partner1 真登 → StuB 详情弹窗(760px 居中) → `_r2SoftToast` 出护照琥珀卡 → 滚到提交区 → 三档视口量：

| 视口 | 卡片矩形 x..right | 弹窗面板 x..right | 卡×面板右下角交集 | 判 |
|---|---|---|---|---|
| 1220 | 844..1204 | 230..990 | **3066 px²** | **红** |
| 1280 | 904..1264 | 260..1020 | **2436 px²** | **红** |
| 1440 | 1064..1424 | 340..1100 | **756 px²** | **红** |

- **三档全红**（固定右下角 360px 卡片压住 760px 弹窗面板右下角；越窄越重，与业主 ~1220 视口现象一致）。卡片全程在屏内。
- **诚实说明**：本测试生 StuB 十一类材料均已提交/在审，plain 详情弹窗里**没有**「提交新文件」按钮、没有「我已看到…」确认勾选行（该行只在护照**上传暂存**态出现），故 7 项具名元素中只有 titleBar/closeBtn/thumbs 在场、且都不与卡相交；**真红信号是卡压弹窗面板右下角**（正是业主说的「盖住提交/确认控件所在角落」）。22-02 把卡搬进弹窗后此重叠归 0——届时无论提交按钮/确认行在不在场都不再相交。

### ④缩略图过期（子代理复现；签发清单见下）
- 机理坐实：`_hydrateCloudStudent`（v169 L8519–8546）`createSignedUrls(..., 3600)` 一次签进 `f.previewUrl`，**不记签发时刻、不续签**；详情弹窗直接 `<img src="${f.previewUrl}">`（L7660/7949）**无 onerror**。
- **真页面复现红**：StuB 有 16 个已签 previewUrl 的已上传件；把每个 previewUrl 改成同源过期/404 地址后重开弹窗（`_hydrated` 已 true 不再 hydrate，坏链存活）→ 监听到 **2 个图片请求 404** = 断图、无兜底 → **红**。（未上传任何真件，纯改内存态。）
- **同类「签发存起来不续签」清单**（供 22-03）：
  - **学生端 v399**：`createSignedUrl(...,3600)` 多处（L11944/16877/16899/20607/24136），存 `f.previewUrl`；渲染 L9531/9534 直接 `<img>/<video src=previewUrl>` 无 onerror；L9180 缩略图 onerror 仅「小图→原图」回退（原图也过期则仍断），**非续签** → 同族病，22-03 候选。无 `previewSignedAt`。
  - **学校端 v234**：签发都是**短时、按需**（`createSignedUrl(...,300/600/60/300)`，开一次签一次），**无**存 1 小时 previewUrl 的持久 hydrate → 大概率 **22-03 只加横幅**（22-03 现量确认）。

### ⑤旧版本提示可行性（`probes/`：index 格式 + §0.4 约束）
- 五端 `index.html` 指向格式（grep）：student `maxhouse_student_portal_v399`（下划线，4 处）；school/partner `maxhouse-...-v234/v169`（连字符，4 处）；admin/reviewer `maxhouse-...-v218/v45`（连字符，3 处）。
- 22-02 正则 `maxhouse[_-][a-z_-]*portal[_-]v(\d+)` **五端全命中**（下划线/连字符都吃）。
- **CF Access 后的 fetch 测试**：§0.4 既定事实——审核端/管理端线上在 Cloudflare Access(邮箱 OTP) 后，**无头过不去**，故 `fetch('./index.html')` 的线上行为**无法用 reviewer1 无头实测**。结论按读代码判：22-04 采**防御式静默**——登录态下同源 `fetch('./index.html',{cache:'no-store'})` 会带 CF Access cookie、正常应返回真跳转页（含版本串）；若返回的是 CF Access 页/任何不含 `maxhouse..portal_v<digits>` 的内容 → 正则不命中 → **静默不提示**（天然安全）。取不到/解析不出一律静默。

### ⑥老积压（REJ-1 / 配额云化）
- 按 QUEUE-22 自身拆分「22-06 交付图纸、本轮不写代码」，⑥ 的需求/现状/涉及端/改动量图纸**在 22-06 落**（`jobs/JOB-22-06/backlog-REJ-1.md` + `backlog-quota-cloud.md`）。本包只记账，不提前写。

## 三、旧病复现先红（N2）汇总
- ② 结构红（Link 子资源泄漏 5 条）✓；console 告警本地未复现（CF 边缘，标未放行）。
- ③ 卡片重叠 1220/1280/1440 三档全红 ✓。
- ④ 缩略图过期断图（2 个 404）红 ✓。

## 四、五端旧版按钮清单（N1 基线，`jobs/JOB-22-00/inventory/`）
静态清单（inv.mjs static）：student-v399(ids368/handlers188/fns1116) · school-v234(170/225/856) · partner-v169(205/120/754) · admin-v218(200/172/808) · reviewer-v45(74/31/157)。升版时对新版求差集。

## 五、⚠ 图纸冲突（铁律14 P2·停工报告原文，不自修）
**QUEUE-22 ①「审核端 @font-face 只有 300/400/500/600」与源码不符**：reviewer v45 第 166–171 行明确声明 manrope-700(L170)、800(L801)。因此 22-01「审核端去掉 700」的**前提是错的**（700 已声明）。且本地实测 reviewer 首屏 400/700 **都在用**（0 条 unused 告警）→ 盲删 700 会把首屏在用字体降级、与提速相反。
- **本包只报原文冲突、不自修**。22-01 处置建议：**主改点=把 Link 从 `/<portal>/*` 收到只挂网页文档（根病、无争议、结构红→绿）**；字体预加载**载荷保持不变**（含 700，因五端首屏 0 unused 告警=都在用），只搬 Link 归属路径；「去掉 700」不执行，留业主/Claude 定。这样 22-01 gate3 的结构信号（Link 子资源泄漏 5→0）照样过，且不引入无证据的回退。

## 六、闸账
- **N1**：五端静态清单基线已存（升版时求差集）。
- **N2**：③④ 真页面先红；② 结构红 + console 告警未复现按 N2 标未放行。
- **N5 secret-scan**：mh-jobs push 前扫 `~/mh-jobs`（见下节命令输出）CLEAN。
- **shell 卫生**：本包无新 `.sh`；收尾仍跑 `jobs/JOB-20-04/shell-lint.sh`（结果附下）。
- 主仓库：`jobs/QUEUE-22.md` + `jobs/JOB-22-00/`（base-head/probes/inventory/repro）点名 add + commit，**未 push**（§0.9）。

## 状态
- 无「必须停」触发（基线齐、四账号真登过、无删桶/改真实数据/动共用查看器/云函数）。
- 本回执 push 到 mh-jobs。

下一包 = 22-01
