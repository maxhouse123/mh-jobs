# JOB-19-06 回执 · 收官 —— 线上走查加两项 + SUMMARY-19（含 PUSH_OK）

## 一句话
CC 从 JOB-18-06 复制出 `jobs/JOB-19-06/online-walk.{sh,mjs}`（旧文件一字不动）并按 QUEUE-19 §19-06 改：①五端 `[VER]` 目标改第十九轮版本、砍掉 18-06 的学生端合成件真上传写库（本包不需要）；②新增 **schX 真登线上中介端 → 期望被拒/页面无机构内容/会话为空** 的拒绝测试；③新增 **partner1 若有现成 Q1 提醒场景则量卡片样式（有才判）**。本机对**现役线上**单探一遍，如实记录：现役=第十八轮（partner v166 / student v397 / school v234；reviewer/admin 在 CF Access 后），round-19 未推送前所有「指向新版」项为红/挡**属预期**。收官写 `SUMMARY-19.md`，顶格单行 `PUSH_OK=yes`。CC 只推 mh-jobs（回执/截图仓），**未推主仓库、未部署、无迁移、无手贴 SQL**。

## 做了什么

### 1) 走查脚本（从 18-06 复制后改，旧文件不动）
- `jobs/JOB-19-06/online-walk.sh`（启动器，跑完一律 exit 0）
- `jobs/JOB-19-06/online-walk.mjs`（走查主体）
- 相对 18-06 的改点：
  - `PKG='19-06'`、回执 `JOB-19-06-online.md`、公开截图 `shots/19/walk/`、自推 commit 文案改本包。
  - `TARGETS` 五端目标改第十九轮：**student v398 / partner v167 / reviewer v45**（三端升版）、**admin v218 / school v234**（不动）。
  - 删掉 18-06 的 `pickSafeSlot / verifyClean / studentWalk（合成件真上传）/ makeSyntheticTranscript / expandAncestors`——**本包不写库**、不需要学生端上传写测。
  - 新增 `schXRejectWalk(browser)`（②）与改写 `partnerWalk(browser)`（③，只读、不主动触发上传）。

### 2) ② schX 拒绝测试（新增）
`schXRejectWalk`：schX 真填账号密码登线上中介端，**只登录+观察，绝不填写/提交注册表（不写库）**；固定观察窗（9s，不等 unlocked——v167 会被登出永不解锁会挂）后取三态：
- `body.locked`（是否仍锁在门上）、`hasSession()`（localStorage 有无 `sb-*-auth-token`）、`instShown`（页面有无中介端登入后才见的「我的推荐/Sent invites/添加推荐」等机构内容文字）。
- **v167-正确（绿）判据** = `locked===true && session===false && instShown===false`（拒绝文案四语命中更佳）。
- 另记 `partner_bootstrap/hydratePartner` 请求次数（v167 应 0）。

### 3) ③ partner1 走查（改写，只读）
`partnerWalk`：partner1 真登 → `[VER]=v167` console 检查；**Q1 提醒卡片「有才判」**——只在页面已存在告警卡（`.v167-soft-card/#r2SoftToast/…`）或红 error toast 时量其计算后背景色，**不主动触发上传**（避免写库/造件）；无现成场景记 N/A。

## 结果（本机对现役线上单探，全部如实）

小结：**绿 3 / 红 4 / 灰(N/A) 4**——**4 条红全部是「round-19 未推送」的预期红**，无一是新病。

| 步骤 | 实际 | 判 | 说明 |
|---|---|---|---|
| ① [VER] student→v398 | 现役 v397 | 红 | 未推送前预期 |
| ① [VER] partner→v167 | 现役 v166 | 红 | 未推送前预期 |
| ① [VER] school→v234 | 已指向 v234 | 绿 | 本轮不动，已在线 |
| ① [VER] admin→v218 | http 302 | N/A | CF Access 挡 |
| ① [VER] reviewer→v45 | http 302 | N/A | CF Access 挡 |
| ② schX 登中介端被拒（v167 判据） | locked=false, session=false, 机构内容=false | 红 | 现役 v166 无角色门，schX 登入即进**空白注册表**（无 partners 行）→ 预期红；v167 上线后 body 应仍锁转绿 |
| ② schX 登入后无 partner_bootstrap | 见 1 次 | N/A | v166 无门会 hydrate，预期 |
| ② schX 走查 0 未捕获报错 | pageerror ×0 | 绿 | |
| ③ [VER]=v167 (console) | 未见（现役 v166） | 红 | 未推送前预期 |
| ③ Q1 提醒卡片（有才判） | 登入后无现成 Q1 告警卡/条 | N/A | 有才判，不主动触发上传 |
| ③ 中介端仪表盘公开截图 | 不产出 | N/A | 见下「PII 红线」 |
| ③ 中介端 0 未捕获报错 | pageerror ×0 | 绿 | |

### 截图（公开一张，元素级/文本脱敏）
- `shots/19/walk/schX-partner-login.png` —— schX 登线上中介端后**只见空白机构注册表（占位符 you@agency.com 等，非真实数据）**，正是 QUEUE-19 所述「学校账号进去因无 partners 行只看到空白注册表」的**预期红铁证**；v167 上线后此页应被角色门挡在门外。
- **PII 红线**：partner1 仪表盘含其本人机构身份（如机构名）+ 被推荐学生姓名/邮箱等真实 PII，散布多处卡片、选择器打码难保全 → 按零信任红线**不产出该端公开截图**，其证据全在上表红绿。本机完整版仅存 `~/mh-verify/shots/JOB-19/19-06/`（不推送）。

## 业主要做什么
- 本包无需操作。**收官推送由业主用卡1145 做**（认 `SUMMARY-19.md` 里 `PUSH_OK=yes`）；watermark-doc v34 部署走卡1146；线上真·30 份计时表在部署后跑 `jobs/JOB-19-03/wm-timing-probe.mjs` 产出。
- 业主推送后可重跑 `bash jobs/JOB-19-06/online-walk.sh`（先关代理，`export SUPABASE_ANON_KEY=<公钥>`）——届时五端 [VER] 与 schX 被拒项应转绿。

## 新闸证据 N1–N7
- **N1**：本包只在主仓库新增 `jobs/JOB-19-06/*` 走查工装（非任一端产品代码），无端 UI 改动 → 无按钮/id/函数差集适用。
- **N2**：走查对**现役线上**先跑，4 条红均为「round-19 未推送」的预期红（先红），业主推送后再跑转绿（后绿）——红绿闭环由本脚本自证。
- **N3**：schX/partner1 均**真表单登录**（无会话注入）；schX 拒绝测试只登录+观察、不提交注册表、不写库；`hasSession` 读真页面 localStorage。
- **N4**：不适用（本包不改云函数/无分支矩阵；watermark-doc v34 的 N4 表在 JOB-19-03）。
- **N5**：公开只推一张 `schX-partner-login.png`（空注册表、占位符、无真实 PII）；partner1 仪表盘按 PII 红线不公开；回执/截图/脚本推送前过 secret-scan。
- **N6**：只新增 `jobs/JOB-19-06/{online-walk.sh,online-walk.mjs}`、`JOB-19-06-online.md`、`JOB-19-06.md`、`SUMMARY-19.md`、`shots/19/walk/*`；共用查看器、学校端、管理端、precheck-ai、admin-doc、watermark-doc、任一端产品代码**一行/一字节未动**。
- **N7**：③ Q1 卡片按 N7 量（第 1/4 秒可见、计算后琥珀底、不被 toast 顶、× 能关）——但**有现成场景才量**；本次现役线上登入后无现成 Q1 告警件 → 记 N/A（不主动触发上传写库）。partner v167 卡片各调用点的 N7 全套已在 **JOB-19-01** 真页面量过并全绿。

## 给收官的交接
- SUMMARY-19 已写（§0.8 全项 + 顶格单行 `PUSH_OK=yes`）。
- **迁移**：本轮全程无迁移（19-00② 证 0213 不需写）；下一空号仍 0213。
- 本包是第十九轮**最后一包**，收官完成。
