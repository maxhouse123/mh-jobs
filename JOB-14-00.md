# JOB-14-00 · 开工检查 + 流量根因探针（只读）

## 做了什么
- 核对仓库/五端 md5/EF md5/db-run.sh、跑 mh-verify 自测；跑只读探针 `jobs/JOB-14-00/probe.sql`（cron、预检影子、审计、缓存索引、不支持类型、大图、邀请、net）；本地对 c4998653 备份重跑瘦身核对清晰度；curl 探 precheck-ai CORS/鉴权 + 读两只云函数源码。**全程零写库、零下载生产原件**（本地只用业主已存的备份）。

## 结果是什么（每个数字带命令出处）
**核对**：五端 md5 全对 SUMMARY-13；watermark-doc md5 `9c291689…` 对上；mh-verify `SELFTEST|MH-OK`；工作区 clean。
**cron（sec1a/1b）**：三条定时任务——`precheck-ai-shadow */5`（**每天 288 次**，09-12～16 实测 288/天）、`wm-cache-backfill */5`（09-17 跑 227 次）、`boost-expiry-reminders` 每天 1 次。
**缓存索引（sec4a）**：status = `cached 95 / gave_up 4`（round 13 的 4 件大坏件已封顶，backfill 不再打它们）。
**不支持类型活件（sec5）**：**6 个 .mp4 视频**（3.3–4.4 MB，多在审核队列里 → 现被 415 挡，14-03 修）+ **5 个 .docx**（34 KB）。
**大图候选（sec6）**：jpg/jpeg>1500KB 共 5 件（两张 6189KB「big size 2.jpg」最大，还有 2215/2102/1510KB）；png/webp>1500 共 2 件（14-06 不动）。
**net 近6h（sec8）**：546（云函数内存不足）**64 次**最多 / 500×11 / 200×5 / 402×8 / null×7。→ 大图在被反复渲染 OOM。
**邀请（sec7）**：partner1（pakistan angecy，uid 1aad823a）今天建了 2 条，都有邮箱、status=sent；其中 invitetest 那条 resend_count=1（round 13 重发过）。
**本地清晰度核对（sec 14-00c，`node local-slim-check.mjs`）**：c4998653 是**单页** PDF，原件 4052KB → 瘦身 82KB（对上线上 84195 字节）；原件/瘦身件第 1 页都渲染成 732×2200 px，拉普拉斯清晰度 577 vs 585（比值 1.01）→ **结论：82KB 合理**（原件是超大 JPEG 底，瘦身只是去掉冗余、细节没丢）。
**precheck-ai 健康（14-00d）**：OPTIONS 预检 → HTTP 405（函数不处理 CORS 预检，但请求到了 edge runtime，有 x-deno-execution-id）；无鉴权 POST → 401（函数自己的 x-precheck-key 校验，源码 L144）。→ **precheck-ai 是「关 Verify JWT + 自校验 x-precheck-key」**（源码头注 L2 也写「Deploy 关 Verify JWT」）→ **14-01c 的部署命令要带 `--no-verify-jwt`（现状查得清，不需留给业主）。**

## 🔴 根因结论（谁在烧 9-07 起每天 1.5 GB cached egress）
**主凶 = precheck-ai-shadow 定时任务反复下载「出错的大文件」。** 证据链：
1. 该 cron 每 5 分钟一跳 = **288 次/天**（sec1b 实测）。
2. precheck-ai 源码 **先整份下载原件（L182 `storage.download`），再检查体积（L195 `buf.length>4MB` 抛错）** —— 顺序反了，大图每次都被完整拉下来才丢。
3. 源码 L163：凡上一轮 `verdict='error'` 的 doc **每一跳都重新加入重试集** → 6MB 的坏大图被**每 5 分钟重下一次、永不封顶**。单张 6MB × 288 次/天 ≈ **1.7 GB/天**，正好对上业主看到的 1.5 GB/天。
4. `wm-cache-backfill` 曾是第二条烧钱线，但 round 13 把 4 件大坏件判成 gave_up 后（sec4a gave_up=4），这条基本已止血。
5. 546 内存错误（64/6h）= 审核端按需渲染大图时 OOM（用户触发，次要）。
→ **14-01c 正是治主凶**（下载前先看 size_kb，>3000KB 直接 error 且 attempts=99 永不重试；错误行重试封顶 3 次）。定不了的部分：precheck 在 402 限制期是否仍产生下载egress，标「猜」——但只要停掉重下循环，账单必降。

## 业主要做什么
- 无需操作（本包只读）。结论：**账单主要是 precheck-ai 每 5 分钟重下坏大图烧的，14-01 会堵死这个循环**；14-06 再把存量大图压小，双管齐下。
