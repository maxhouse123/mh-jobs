# JOB-7-01 · 学生端做成真正可安装的 PWA — 回执

**结果：代码全部做完，十道闸里能本地跑的全过（9/9）。只有一件卡住——你的新图标压缩包不在电脑里（见下方「要你补的」），我先放了临时占位图标让功能能装能测。**

## 大白话说我做了啥
以前学生用手机 Chrome 打开学生端，只能「创建快捷方式」，装出来的图标带个 Chrome 小角标、名字被截成「Maxhouse In…」、点开还带地址栏——因为学生端**根本没有 PWA 说明文件（manifest）**，Chrome 只能拿网页标题当名字。

这次我给学生端补齐了成为「真 App」的三样东西：
1. **说明文件** `student-portal/manifest.webmanifest`：App 名「Maxhouse · 留学中国」、桌面短名就叫 **Maxhouse**、打开方式 standalone（无地址栏）、主题色沿用你现有的 #3F3494。
2. **图标目录** `student-portal/icons/`：512/192/180/32/16 五个尺寸。
3. **一个很克制的 service worker** `student-portal/sw.js`：**只缓存图标和说明文件，绝对不碰 HTML 和接口**——这条是你特别交代的第 3 点。理由：缓存了 HTML 会把学生卡在旧版本，永远看不到新功能。所以我写成「除了图标，一律走网络、一律不存」。
4. 主网页 `<head>` 里挂上 manifest、苹果图标、application-name=Maxhouse、32/16 favicon；网页底部加了一段 try 包住的 sw 注册（失败也不影响打开）。
5. 学生**第一次访问其实是 index 跳转页**，所以那个 index.html 我也挂了 manifest 和图标。

**版本：学生端 v382 → v383**（旧版保留）。index 跳转指针已同步指到 v383。

## 十道闸
- **闸 3（静态）**：manifest 是合法 JSON、含 4 条 icons ✅；主文件 rel="manifest" 正好 1 处 ✅；sw 注册正好 1 处 ✅（顺手把一段 2021 年的死注释清理了，不然它也被数进去）；application-name=Maxhouse ✅。
- **闸 8（真浏览器走查）**：本地起了个 127.0.0.1 小服务器（service worker 必须在 localhost 才能装，file:// 装不了），用你电脑里的 Chrome 无头跑 `tests/job7-01.mjs`：
  - service worker 成功 ready ✅
  - manifest 能 fetch、能解析、正好 4 个图标 ✅
  - display=standalone、short_name=Maxhouse ✅
  - 页面 DOM 里有 rel=manifest、apple-touch-icon 指向 icon-180、application-name=Maxhouse ✅
  - icon-512.png 能以 png 正常取到 ✅
  - **9/9 全过**
- JS 全部 5 段内联脚本解析干净、sw.js `node --check` 通过。

## ⚠️ 要你补的一件事（唯一未尽）
**你说图标包在 `~/Downloads/maxhouse-pwa-icons.zip`，但那个文件不在电脑上**（我把 Downloads、桌面、全盘都搜过了，没有）。
- 我**没有硬等**——按你「橙底白 M、四角铺满无留白」的描述，先用代码生成了五张**临时占位图标**（橙 #F39200 底 + 白 M，四角实测全是橙色、零透明零白边），放进了 `student-portal/icons/`。这样 PWA 现在就能装、能测、你在 Pixel 上能立刻验收「安装应用」「无角标」「无地址栏」这些行为。
- **等你把 zip 找出来**（或重新导出），只要在原生终端跑一句就能换成你的正式图标，文件名一样、直接覆盖：
  ```
  cd /Volumes/Dev/MAXHOUSE && unzip -o ~/Downloads/maxhouse-pwa-icons.zip -d student-portal/icons/
  ```
  换完不用改任何代码，刷新即生效。
- 我没有拿我生成的图标冒充你的正式件（守铁律15：视觉件以你的为准）。占位图仅为让功能可测。

## 业主验收（你回来在 Pixel 上做）
线上部署后，用手机 Chrome 打开学生端 → 菜单应出现「**安装应用**」（不再是「创建快捷方式」）→ 装完桌面图标**无 Chrome 角标**、名字是 **Maxhouse**、四角橙色无白边、点开**无地址栏**。
（图标那一项要等你的正式 zip 覆盖后才是最终样子；占位图也是橙底白 M，形态一致。）

## 动过的文件
- 新增：`student-portal/manifest.webmanifest`、`student-portal/sw.js`、`student-portal/icons/icon-{512,192,180,32,16}.png`（占位）
- 改：`student-portal/maxhouse_student_portal_v383.html`（新版）、`student-portal/index.html`（跳转指针+图标）
- 测试脚本：`tests/job7-serve.mjs`、`tests/job7-01.mjs`

**主仓库未推**（守铁律）。本回执已推日志仓库。
