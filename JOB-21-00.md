# JOB-21-00 · 开工检查 + 七探针 + 旧病复现（先红）+ 三端按钮清单

> CC 无人值守。做了什么 / 结果 / 业主要做什么 / 新闸证据。数字带命令出处。

## 一、做了什么
1. `git -C ~/mh-jobs pull` = Already up to date。核对 QUEUE-21 §0.2 基线九件 md5 + HEAD + 四账号真登 + 0213 空。
2. `git rev-parse HEAD` → `jobs/JOB-21-00/base-head.txt`（c43d5f9），与 `jobs/QUEUE-21.md` 一起 commit（点名 add，未推主仓库）。
3. 七探针（只读；带身份 N3；SQL 走 db-run.sh，真页面走 realpage 本地服务 + Chromium `--no-proxy-server`）。
4. 旧病复现（N2 先红）：④⑤ 真页面 partner1；⑥ 真页面学生端；⑦ 真页面审核端 console。
5. 三端静态按钮清单（N1 基线）落 `jobs/JOB-21-00/inventory/`。

## 二、结果

### 基线核对（§0.2）——全等，无「必须停」
- admin v218 `73b36f0a…` ✅ · reviewer v45 `c2cf1f26…` ✅ · partner v168 `f1a734ab…` ✅ · student v399 `dd3a5bcc…` ✅ · school v234 `a2d2e593…` ✅ · viewer `ee351485…` ✅
- watermark-doc 源 `3eb5180e…` ✅ · precheck-ai `50a3df3c…` ✅ · admin-doc `ac5f227a…` ✅
- `git fetch` 后 HEAD == origin/main == `c43d5f9`；`git status --porcelain --untracked-files=no` 为空 ✅
- 迁移到 0212 已应用，**0213 空** ✅（`ls supabase/migrations` 无 0213）
- 账号（`node jobs/JOB-16-00/acct-check.mjs` + schX 单查）：stuA OK 43668ae4 / partner1 OK 1aad823a / reviewer1 OK 80c62372 / **schX OK db568ff7**（四个都真登过）✅
- → 无 `~/mh-verify/r21-STOP.txt`。

### 探针①（审核员缓存未命中根因）——详见 `jobs/JOB-21-00/probes/p1-reviewer-cache-findings.md`
- reviewer1（全库唯一有派单者）**104 派单：命中 96 / 未命中 8**。
- 未命中 8 = 视频 mp4×4（走 signed_video、本就不入缓存）+ docx×2（EF 415 不支持）+ **损坏 PDF×2（status=gave_up，`PDF_UNREADABLE`，被 EF `indexBlockResponse` 挡在渲染前返 422，是「拒」不是「慢」）**。
- **可渲染、非损坏的未命中 = 0**。prepare 触发器（0193）+ `*/5` 兜底 cron（0195，实测 active=t）已全覆盖。
- 「哪些事件预渲染/不预渲染」表 + 两条架构缺口（gap1 补传件改 file_path 无触发器 / gap2 撤单后 un-revoke 复用旧行不触发 prepare）——**均实测当前未触发**（documents `max(version)=1`；reviewer_assignments 无重复(doc,reviewer)对），列为 21-01 防御性补。

### 探针②（现场渲染后回写）——`jobs/JOB-21-00/probes/p2-writeback-analysis.md`
- 现场渲染分支（图 850–879 / PDF 881–907）**只回图、不回写**；prepare（445–455）是唯一写桶者。
- 回写可行、低风险、**不改缓存语义**（同对象名 `${uid}/${docId}.${ext}`、同索引行、upsert、无过期改动）；用 `EdgeRuntime.waitUntil` 放回图之后→零客户端等待、fail-open、限 `callerRole==='reviewer'`。→ 21-01 在 v36 落。

### 探针③（常驻渲染方案，只调研）——21-01 出方案书
- 冷启动是墙钟大头（JOB-20-01：boot 已从 5.4s 瘦到 0.5s，但 render 段涨到 4.0s，seq 多为 1=每请求即冷、实例几乎不复用）。方向 A=Cloudflare Worker（站点已在 CF，实例复用好、WASM 图片库）、B=常驻容器（Fly/Railway + sharp）。详细对比 + CC 推荐 + 业主三问放 `jobs/JOB-21-01/persistent-renderer-proposal.md`（21-01 交付，不实施不部署）。

### 探针④（中介端跳登记表单）——真页面 partner1，**RED**
- `state.wiz={}`（无 step）后调 `renderUploadGrid()`（11541 行守卫仅查 `state.wiz`）→ 主视图切到登记表单（`currentView===_wzRender`）= **RED**（守卫偏松，与其它 9 处 `state.wiz.step` 不一致）。
- 回归对照：弹窗开着暂存不一致护照 → 主视图**不**切走 = GREEN（v168 `_r2Repaint` 已避开）。
- 证据：`jobs/JOB-21-00/repro/partner-probe-findings.md`。

### 探针⑤（中介端卡片时长/悬停/重叠）——真页面 partner1，**RED**
- 一般卡实测消失 **~5567ms**（默认 5500；业主要 15000）；护照卡 **~8586ms**（`_r2SoftToast` 8500；业主要 20000）= RED×2。
- 悬停：卡上 mouseenter 后等 5s，卡消失（无悬停暂停）= RED。
- 重叠 @390：卡栈 rect{16,16,358,69}；弹窗标题条{20,40,350,93}；关闭钮{306,68,36,36} → **卡∩标题条 15750px²、卡∩关闭钮 612px²** = RED。@1280 亦重叠（卡∩标题条 5220px²、卡∩关闭钮 612px²）→ 21-02 两宽都要清零。

### 探针⑥（学生端法文两键）——真页面 4 语 eval，**UNREPRODUCED（premise ⑦ 已满足）**
- `t('doc.studyProof')`/`t('doc.studyPlan')` 真页面结果：zh 在读证明/学习计划 · en Proof of study/Study plan · ru Справка об обучении/План обучения · **fr `Attestation d'études` / `Plan d'études`（法文，非英文）**。
- 根因：`maxhouse_student_i18n_v360_fr.js` 早已含两键（L1181 studyProof、L76 studyPlan，均在 `I18N.fr={}` 内、无 _sup.fr 覆盖、无重复键）。→ **premise「法文回落英文」在真页面未复现**。21-03 转为核验+记录，无源码改动、不 bump v400（见 §四）。

### 探针⑦（审核端 preload 警告）——真页面 console，**UNREPRODUCED（本地 0 条）**
- 本地服务真页面收 console，两法（`load`+6s / `networkidle`+9s）各跑一遍：**preload 警告 0 条**，只有 `[VER] v45` 一行。
- 源码只有 1 条静态 preload（L33 `./vendor/supabase.js` as=script），与 L894 `<script src>` 同 URL、被执行、算「已用」；字体是 `@font-face`（L166–175）**未 preload**、不会产生该警告；无 JS 运行时注入 preload。
- 判定：源码内无可复现的三条警告。**极可能来自线上 Cloudflare 边缘**（Rocket Loader / Early Hints 自动注入 preload 后又延后加载）→ 属 CF 路由设置，非源码改动。21-04 见 §四。
- 证据：`jobs/JOB-21-00/repro/reviewer-preload-findings.md`。

### 旧版按钮清单（N1 基线）
- `jobs/JOB-21-00/inventory/`：partner-v168-static.json（ids=205 handlers=120 fns=751）/ student-v399-static.json（368/188/1116）/ reviewer-v45-static.json（74/31/157）。升版包 commit 前对差集。

## 三、新闸证据 N1–N7
- **N1**：本包不升版，三端静态清单入盘作后续差集基线。
- **N2**：④⑤ 真页面 partner1 先红（数字见上）；⑥⑦ 真页面复现结论为「未复现」（各两法/四语实测），按 N2 如实记、不硬造红。
- **N3**：探针①走 db-run 服务端只读；⑥ 走 realpage；partner1/reviewer1/schX 真登（uid 前 8 位见基线段），accounts.json 只脚本读、未打印。
- **N4**：本包不改云函数（探针只读源码），无鉴权路验证需求。
- **N5**：secret-scan 于 push 前跑（见 §五）。
- **N6**：本包只做 QUEUE-21 21-00 写明的探针/复现/清单，未顺手改代码。
- **N7**：⑤ 卡片按 N7 量了出现/消失时刻 + 计算后位置几何（琥珀 #FFF7ED/#F59E0B 已在 v168，本包不改色，只量时长/位置/悬停）。

## 四、对四条「未复现/已满足」premise 的处置建议（交业主/Claude 定，21-06 汇总）
1. **⑥/⑦ 学生端法文两键**：已满足 → 21-03 只核验、**不 bump v400**（只修不加：无可修）。
2. **③ 名单块英文标题冒号**：v167/v168 实测 `_p98T('rosterTitle')` 用的 `_p98I18N` en=`):` ru=`):` fr=`) :`（均半角，fr 按法文规范带空格）、zh=`：`（全角）→ **已符合「zh 全角/en·ru·fr 半角」**，无改动。21-02 其余三项（时长/位置/守卫）仍是真红，v169 照升。
3. **⑤ 审核员缓存 5s**：真页面路径未复现（v45 先问缓存、96/104 命中）；21-01 仍做「现场渲染回写 + 防御触发器 + 方案书」（对应业主观察、语义不变、防御冷窗口）。
4. **⑦ 审核端 preload 警告**：本地 0 条 → 极可能 CF 边缘；建议业主先在**线上真 URL** 复核控制台，若确有则调 CF（关 Rocket Loader/Early Hints 自动 preload），**非源码改动**。21-04 若源码确无可改点则不 bump v46、标「未放行·等业主线上核」。

## 五、业主要做什么
- 无需操作。CC 已 commit 主仓库（QUEUE-21 + base-head，未推）+ push 本回执到 mh-jobs。
- push 前跑 `jobs/JOB-16-00/secret-scan.mjs`（结果见 commit 说明），干净才推。

## 交接
- **下一包 = 21-01**（水印缓存回写 v36 + 防御触发器 0213 + 常驻渲染方案书）。
