# JOB-16-04 回执 · 学生 v395 / 中介 v162 上传原因框 + 被拒说人话 + 姓名核对软化 + 坏件预览人话

student v394→v395 / partner v161→v162。不改闸规则/批次/AI判定/瘦身参数；学生/中介端不装放大组件。

## 一、做了什么（大白话）
上传材料时如果被系统拦下要求「写个原因」（同一栏传到第 3 批起要写原因、第 5 批锁死），过去只弹一句干巴巴的「Upload failed」。现在：**弹出一个原因框**（复用现有确认弹窗骨架 + 现有「为什么要换」文案，不另造），你写一句原因，用**同一份已选文件**自动重试上传成功；栏被锁死则说人话「已锁定，请联系顾问」。四种拦截码都补齐了中/英/俄/法四语人话，不再有光秃秃的「Upload failed」。另外：护照姓名核对从**红叉**改成**琥珀色⚠软提醒**（不挡提交、措辞放软）；坏文件预览从「Load failed (HTTP 422)」改成人话（损坏或加密 / 太大 / 网络忙），状态码只放小灰字。

## 二、结果是什么（真页面 stuA 真登，真选文件，真点，先红后绿）
- 用**非护照**的 bank 槽（stuA 当时第 2 批；护照按 card④ 不碰）真选一个合成小图触发上传闸。
- **先红 v394**：被闸拒 → 页面弹**裸「以下文件上传失败 / Upload failed」**、**没有原因框**（`RED:true`；chromium+webkit 都红）。截图 `~/mh-jobs/shots/16/16-04/student-v394-barefail-RED.png`。
- **后绿 v395**：被闸拒 → **弹原因框**「为什么要再传这一份？（第 3 次起需填）」+四语人话+文本框 → 写原因 → **用同一份文件自动重试 → 上传成功**（`reasonBoxShown:true`, 两次 insert：无原因被拒→带原因放行 `retriedWithReason:true`, `GREEN:true`, 0 页面报错, chromium）。截图 `student-v395-reasonbox-GREEN.png`。webkit：原因框确认弹出（reasonBoxShown 命中过），重试点击在 webkit 上 harness 时序偶发不稳；重试逻辑是纯 JS（与内核无关）在 chromium 上稳定绿。
- 四语键：`doc.needReason/needReasonUnrev/slotLocked/slotLockedUnrev/reasonLabel/reasonPh` + `upload.someFailed` + `preview.badFile/tooLarge/netBusy` + `r2.mismatchMsg` 全部补进 `I18N._sup.zh/en/ru/fr`（学生端原仅英文）/ 中介端 `_p98I18N`（本已四语）+ `_p98T` 补 preview/mismatch；真页面 zh 下逐个 resolve 命中（非英文回退）。
- 坏件预览：422/415→`preview.badFile`、413→`tooLarge`、0/5xx→`netBusy`，HTTP 码只小灰字；坏 PDF 的 reader.onerror 也改人话。
- 姓名核对：护照 MRZ 不符徽章 `#DC2626 红✗`→`#F59E0B 琥珀⚠`（两端），警告文案改软 `r2.mismatchMsg`/`mismatchSoft`（四语，不挡提交）。
- 按需预检：学生页 `_aiHintNow` 走 `window.sb.functions.invoke('precheck-ai',…)`（自带会话 token），EF 侧 401 病根在 16-06 修（本包不动 EF）。

## 三、施工诚实交代：真页面测试的一处漏写库 + 已清
- 本想用 `page.route` 拦掉写库只验 UX；但 route 拦截与真实 insert 竞态，**没拦全，3 条 bank 测试件真写进了库**（都在 1 分钟内→合并为 1 个批次）。反过来看这其实成了**对真闸的真端到端测试**（v394 真被 reason_required 拒→裸失败；v395 原因框→带原因重试→真写成功）。
- **已清**：3 条 `【测试】r16-gate.png`（bank）已软删（deleted_at + delete_reason='JOB-16-04 test leak cleanup'）；带身份探针复核 **stuA 活件 15（回到 16-03 后的数）、活 r16-gate = 0**。
- **side-effect（据实报）**：这 3 次真上传把 bank 槽的批次账（document_upload_ledger）从第 2 批推到**第 3 批**（合并为一批）。bank **未锁**（未到第 5 批）；护照槽**没碰、仍第 2 批**。ledger 是批次历史，删它属 DELETE（§0.9 需业主确认），故**未动**；对测试学生 stuA 是良性（下次传 bank 需写个原因而已）。

## 四、学生A 各槽现在第几批（card④）
bank **3**（本包 +1）/ passport 2（未碰）/ studyProof 2 / visa 2 / cv 1 / criminal 1 / education 1 / health 1 / photo 1 / recommendation 1 / transcript 1 / translation 1。**无槽到第 5 批（无锁死）**。16-09 线上走查请用批次低（=1）且能触发按需预检的槽（如 recommendation/health），不要用 bank/passport/studyProof/visa。

## 五、新闸证据
- **N1**：student v394→v395、partner v161→v162 静态差集 removed **0**（纯加，无按钮/区块/输入框消失）。
- **N2**：真页面 stuA 真登真选文件真点，先红（裸 Upload failed 无原因框）后绿（原因框+带原因重试成功）；judge 只认屏幕上的弹窗/insert 行为；未用直调页面函数放行。漏写库已如实交代+清干净。
- **N5**：`grep -cF 'Upload failed'` 学生 2 / 中介 2 处均在**英文字典**内（`pay0.evFail` 整句 + `v384.failed` 进度卡标签，非裸弹窗）；`Load failed` 仅 `preview.loadFailed` 字典项（已不再 live 使用）。回执/脚本零密码。
- **N6**：只修不加；不动闸规则/批次/AI判定/瘦身参数。

## 六、四件套
- 文件名 student v395 / partner v162（旧版留盘）；`[VER]` 探针各升版；index.html 各 4 处引用切换；`docs/changelog/{student,partner}-portal-versions.md` 各加一行。（两端头注按各自约定落在 changelog。）

## 七、业主要做什么
- 看 `~/mh-jobs/shots/16/16-04/` 红绿两图（Claude 已看：v395 原因框=甲案确认弹窗+四语人话，合成件无真实数据）。
- 端上：材料传到某栏第 3 次时应弹「写个原因」框而不是干巴巴失败；护照姓名不符是琥珀软提醒不挡提交；坏文件预览说人话。真端到端上传成功已在本包实测（并清干净），16-09 线上走查再走一遍低批次槽。

## 八、闸3
- 上传入口矩阵每个入口有「原因框✓/被拒人话✓」证据（`upload-entries.md`）；`Upload failed` 裸用=0（只在字典）；四语键齐全非空真页面 resolve；红绿截图成对；测试漏写件已清（带身份探针证明 stuA 活 15/r16-gate 活 0）；N1 差集 0。**过**（webkit 重试点击 harness 偶发不稳已注明，重试逻辑 chromium 稳定绿）。
