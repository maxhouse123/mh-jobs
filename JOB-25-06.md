# 回执 · JOB-25-06 · 管理端 v234：人脸证件照即时显示（学生列表 + 学生抽屉）

做这件事的是 CC（无人值守，一趟一个新会话）。这是第二十五轮第七包（25-06）。

## 一、这一包要解决什么（大白话）

业主的新规则：**只要学生上传过证件照（人脸照），管理端就直接显示这张照片，不用等审核员审核**；没传照片的才继续显示姓名首字母。其它材料仍旧要等审核后才显示，只有人脸照片是"传了就即时显示"。

上一包（25-05）已经在**学校端**做好了这件事。这一包在**管理端**做同样的事，落在两个地方：
1. **学生列表**（学生客户管理那一页，每一行左边的头像）；
2. **学生抽屉**（点开某个学生后，弹出的详情面板最上方那个大头像）。

## 二、CC 做了什么

管理端出了新版 **v234**（上一版 v233 保留可回滚）。

- 新增一段"取头像"的程序：开页面时，向数据库要**每个学生最新的一张人脸照**（按上传时间取最新那张），然后成批（每 100 个一批）向存储要一个**临时可看链接**（1 小时有效）。管理员本来就有直接读学生材料桶的权限（早先 v217「查看原件」用的是同一个桶），所以这条路走得通。
- 头像显示改成：**有照片就显示照片**（裁切铺满圆头像，样式直接复用了原来学校 LOGO 的显示写法，没有新造任何样式），**没有照片才显示首字母**；万一照片链接坏了，也会自动退回首字母，不会显示破图。
- **不看审核状态**——传了就显示，符合业主新规则。
- 数据库那两列 / 那张表要是还没准备好（比如迁移还没贴），程序会**安静地跳过、全体退回首字母**，绝不报错、绝不白屏。

其它四端一律没碰；审核端、共用查看器、云函数、数据库都没动；脱敏边界不变（仍只脱手机 / WhatsApp / 微信 / 邮箱四样）。

## 三、自验（都过了）

跑了随卡的无头测试 `jobs/JOB-25-06/green-06.mjs`，**35 条全过 / 0 败**（日志见同目录 green-06.log）：

- **静态查（15 条）**：新程序都在、两个显示点都接对了、取头像走的是「照片(photo)、按上传时间取最新」、成批签名走的是对的存储桶、每 100 个一批、开页面和进学生页都会触发、注释里写明"不看审核状态"、复用了 LOGO 同款样式（没新造 CSS）；旧的首字母头像写法在别处照样保留（零删除）。
- **零消失核对**：拿 v233 当基线比对 v234，页面元素、按钮事件、函数**一个都没少**（只新增了 2 个函数）。
- **真跑一遍（用桩数据在本地无头浏览器里，中英文各跑一遍）**：
  - 3 名有照片的（**其中 1 名照片没审核**）→ 三名都真拿到了照片链接；**同一学生有新旧两张照片时，取的是最新那张**（不是旧的）；
  - 成批签名的**调用次数正好等于 ceil(人数 / 100)**：3 人 → 1 批；150 人 → 2 批（100 + 50），150 人全部拿到；
  - 有照片的学生 → 列表行、抽屉大头像都**真出现了 `<img>` 图片**；没照片的 → 保持首字母、不出图；
  - **数据库查询报错** → 头像全空、**不报错、静默退回首字母**；**存储签名报错** → 同样安静退回、不报错。
- **零报错**：整个过程浏览器控制台无任何报错。
- **密钥扫描**：本次新增的代码行里没有任何密钥 / 密码 / 连接串（扫到的两处命中都是 v233 早就有的：一句旧版本头注里提到 "service_role" 的说明文字、和一张早已存在的 LOGO 图片，都不是本次新增，也不是密钥）。

> 说明：**真·线上效果**要等业主收官后①把迁移 0218 贴进去（学校端要用的取图函数）②把水印云函数部署到 v37，学校端才看得到；**管理端 v234 这一包不依赖 0218 / EF**——管理员是直接读桶签名，贴不贴 0218 都能显示照片。本地无头只用桩数据验了"代码逻辑对、容错对、不报错"。

## 四、四件套 & 边界

- 版本四件套齐：新文件名 v234 / 头注 / `[VER]` 版本探针 / `index.html` 三处重定向（v233→v234）/ changelog 追加一行。
- **没动**：审核端、共用查看器、云函数、数据库、其它四端、脱敏边界、任何 RPC 调用签名。

## 五、动了哪些文件（主仓库一笔提交 4b30e9e，未推）

- 新：`admin-portal/maxhouse-admin-portal-v234.html`
- 改：`admin-portal/index.html`（重定向 v233→v234）
- 改：`docs/changelog/admin-portal-versions.md`（追加 v234 一行）
- 新：`jobs/JOB-25-06/green-06.mjs`（随卡无头测试，35 过）

**下一包 = 25-07**（收官：写 SUMMARY-25 + 待手贴 SQL 0217/0218 全文 + EF 部署说明 + 第二十六轮建议包）。
