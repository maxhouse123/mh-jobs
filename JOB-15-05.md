# JOB-15-05 · 用量巡检 · 迁移 0206（每小时汇总 + pg_cron + 超阈值提醒）+ 管理端 v213「用量巡检」卡

生成于 2026-09-19 · 0206 **已用 db-run 应用并验证** · admin v212→v213（未推未部署）

## 做了什么
- 上个周期被 402 掐了一天多才发现；`net._http_response` 只留约 6 小时（pg_net TTL），所以必须**按小时落、按天看**。
- **迁移 0206**（`supabase/migrations/0206_usage_hourly.sql`，已 db-run 应用 + 镜像到 `docs/supabase 文件/`）：
  - 表 `usage_hourly`（hour 主键 + precheck_runs/docs/kb、wm_prepares/prepare_fail/served/served_kb/rerenders、video_signed、net_5xx/402、egress_est_mb、computed_at、note）。RLS 开 + 管理员只读策略 + `grant select to authenticated`；写只经 security-definer 汇总函数。
  - 函数 `_usage_compute(h)`（单小时，从 `cron.job_run_details`/`ai_precheck_shadow`/`reviewer_access_log`/`wm_cache_index`/`net._http_response` 汇总，幂等 upsert；net 列用 `greatest` 只增不清，避免老整点 TTL 外被覆盖成 0）。
  - 函数 `usage_hourly_rollup(p_hour default 上一整点)`：算目标小时 + **顺带回填最近 6 整点**；再做**阈值判断**（当天累计 egress>400 或 该小时 net_402>0 或 net_5xx>50）→ 写 `admin_events(event_type='usage_alert')` + 调 `push_notification` 给所有管理员，**每天最多一条**（admin_events 当天已有则跳）。
  - 视图 `usage_daily_v`（按天 sum）。
  - **14 天历史回填**（迁移里循环 336 个整点调 `_usage_compute`；net 老整点无行→0，符合「只能从 net 取的项从现在起算」）。
  - `pg_cron` `usage-hourly-rollup`：**每小时第 10 分** `select usage_hourly_rollup();`（不改任何现有 cron 频率）。
  - `egress_est_mb = (precheck_kb + wm_served_kb + rerender_kb)/1024`。
- **admin v213**：驾驶舱底部加 `_v213UsageCard`（读 `usage_daily_v` 最近 14 天）：日 / 预检次数 / 预检下载估算MB / 水印发送次数·MB / 5xx / 402 / 合计估算MB；`egress>400` 或 `402>0` 的行**标红**；卡顶「估算值 · 真实账单以 Supabase Usage 页为准（周期 18 Sep–18 Oct）」。四件套：文件名/头注/[VER] v213（无残留 v212 探针）/index 3 处/changelog。

## 结果是什么（数字带出处）
### 0206 库内验证（`bash jobs/db-run.sh`，DB-RUN OK）
- 表 / 视图 / cron 都在（`V_table=true`、`V_view=true`、`V_cron` = usage-hourly-rollup `10 * * * *`）。
- 近 6 小时 **6 行**；近 14 天 **336 行**，**全部非零**（`V_rows_nonzero=336`）。
- `usage_daily_v` 样本：2026-09-18 预检 201 次/1.6MB、水印发送 43/15.3MB、rerender 72、video 2、**net_5xx 58 / net_402 0**、**合计估算 38.6 MB**；09-17 42.0MB；量级正常，**远低于 400MB 阈值 → 未触发提醒**（干净，无误报铃铛）。
- 以管理员 uid 读 `usage_hourly` = **336 行无 permission denied**；以 is_test 学生 uid = **0 行**（RLS）。

### admin v213 前端自验（`node ~/mh-verify/_run-1505.mjs`，GATE3-PASS，0 真实报错）
- stub 14 天（第 3 天 egress=512>400、第 5 天 402=3）→ 卡渲染 **14 行**、**2 行标红**、卡顶估算值声明在、表头含预检次数/合计估算MB。
- 报告：`docs/self-test-report-15-05.md`。
- **v213 md5 = `9b9336df04928bbf53b97f864e0ceb3e`**（旧 v212 `227bdfa5…`）。

## 业主要做什么
1. 推主仓库后到**管理端驾驶舱**底部看「用量巡检」卡：每天一行，估算流量、预检、水印、5xx/402；超 400MB 或有 402 的行会标红（验收表 7、9）。
2. 汇总每小时第 10 分自动跑；下次接近超量时，管理端会收到一条「用量预警」铃铛（每天最多一条）。
3. 记住卡顶那句：**这是估算值，真实账单以 Supabase Usage 页为准**。
4. 本包**不推主仓库、不部署**；0206 已应用于线上库（纯新增：建表/函数/视图/cron，无破坏性操作，未改现有 cron 频率）。

## 边界守住
估算不是账单；未改任何现有 cron 频率；不发邮件（只发站内铃铛）。
