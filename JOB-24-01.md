# JOB-24-01 · 管理端 v227：任务审批「一眼看全」+ 招生任务点「详情」看同一张卡

（CC 写给业主。大白话。这一包只改管理端，出了新版 v227，并写了一份数据库改动 0216（只写文件、没真跑）。）

## 一、做了什么

**毛病**：任务审批页原来每个待批任务只显示学校 / 任务号 / 层次 / 人数四样，学校建任务时写进去的**项目名、专业、开学季、截止日、奖学金包、录取要求、备注**一样都看不到——管理员没法判断该不该批。

**这一版怎么改**：
1. 新做了一张**完整卡**（函数 `_v227TaskCard`），分五段，全部说人话、中英双语：
   - **基本**：学校（可点开抽屉）· 任务号 · 项目中英名 · 学历层次（本科/硕士/博士/语言生）· 专业全列出 · 招生人数 · 开学季 · 截止日（带「距今 N 天」）· 创建时间；
   - **资助**：奖学金 / 自费 / 混合；每个奖学金包一张小卡——类型（校级/政府/省级/企业）· 覆盖项（学费/住宿/保险/体检/机票/注册/书本）· 年限 · 月补助 · 各项费用带周期 · 包内要求 · 说明；
   - **要求**：年龄 · GPA · HSK · CSCA · 限定国家（全列）· 必交材料（英语/推荐信等翻成中文）；
   - **备注**：原文，保留换行；
   - **配额**：该校在用任务数 / 上限 2，以及「为何 locked：超配额」。
2. 任务审批页每个待批任务现在就是这样一整张卡；**驳回**改成必须填理由（少于 4 个字不让提交）。
3. **招生任务列表**每行加了「详情」按钮：点开就弹同一张完整卡（数据经新函数 `get_admin_task_detail` 取）。**贴数据库 0216 之前**，点详情会显示精简信息 + 一行「贴迁移 0216 后可看完整要求」，不报错。
4. 写了数据库改动 `0216`（两件事：①新增只读函数 `get_admin_task_detail` 让「详情」能取完整内容；②驳回任务时补记一条审计事件，原先驳回没有任何留痕）。**这份 SQL 只写成文件、没有真跑**；等收官后由你在 Supabase 手贴。CC 用「先建后回滚」的方式在真库上验过语法能通过、跑完库里什么都没留下。

## 二、结果是什么

- 自查脚本 `tests/job24-01-admin-v227.mjs`：**26 项全绿**（中文一遍、英文一遍）。其中：
  - 审批卡把项目名、3 个专业、硕士、开学季、截止日、奖学金、校级、学费/住宿/注册、月补助 1500、年龄 17–25、GPA 3、HSK 4、Nigeria、英语、推荐信、「GPA 维持 3.0」、配额「1 / 2」、自费——**全都显出来了**；
  - 驳回填不到 4 个字**不提交**，填够了才提交且把理由传进去；
  - 招生任务「详情」在有数据时弹同一张卡，在数据库没贴时显示回退提示、**零报错**；
  - 0216 文件在、语法探针通过。
- **没有弄丢任何东西**（旧版按钮清单和新版逐个对比，删除数 = 0，全部是新增）。
- 页面零报错；没有新增任何颜色 / 字号 / 样式类（复用现成的卡片、药丸、按钮）。

## 三、业主要做什么

这一包不用你操作。等收官时会把 0216 的 SQL 全文和验证方法一起给你，届时手贴一次即可（贴之前一切正常，贴之后「详情」能看全、驳回任务会在审计里留痕）。

---
- 版本四件套：文件名 v227 / 头注 / [VER] / index.html 三处重定向 / changelog 各就位 ✅
- 迁移 0216：`supabase/migrations/0216_admin_task_detail.sql`（+ 归档 `docs/supabase 文件/`），begin/rollback 探针 DB-RUN OK、未持久化 ✅
- 闸3：26/26 绿（中英各一遍）；N1 零移除；零报错；零新 CSS/色/字号 ✅
- secret-scan：CLEAN ✅
- 主仓库：本包已 commit（de17d86），未 push（按规矩收官由业主推）。
- 下一包 = 24-02
