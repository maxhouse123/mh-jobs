# JOB-10 · 管理端加学校招生任务管理页（只读，含一条迁移）

**结果：核心两项做完（招生任务页 + 候选池），2 项延后，1 项早已完成。全闸通过。**

## ⚠️ 你要先跑迁移
候选池要能看到学生，需先给数据库加一条只读权限：Supabase → SQL Editor → 贴 `jobs/JOB-10/migrate.sql` 运行（前后策略数差应为 1）。不想要用 `rollback.sql` 撤。

## 做了什么（一句白话）
- **招生任务页**（item1）：左边侧栏新增「招生任务」，列出所有招生任务——任务号、哪所学校、招什么专业、状态、创建时间。数据来自现成的只读视图 `admission_tasks_lite`（管理员本就能读）。
- **候选池**（item2）：每个任务行有「查看候选池」，点开弹窗看这个任务圈了哪些学生（姓名 + 申请号）。
- 页面**纯只读**，没有给管理员改任务的按钮（那属于 JOB-11）。样式全用现成的英雄区+队列行，和别的页长一样。

## 业主要看什么
1. 左侧栏点「招生任务」→ 看到任务列表（库里实测 8 个任务）。
2. 点某任务的「查看候选池」→ 弹窗看候选学生（库里实测 27 条）——**需先跑迁移**。

## 十道闸（全过）
- 基底 `93a95a8245876a46e28a04bd120881d4` → 产物 `52e0dfc0cd3cfc69747fc499c543ca39`
- 闸5 语法 0 报错；闸6 新增 4 函数+2 id、消失 0；闸9 零新颜色/字号/类（复用 wf-hero/queue-row/_adminPill）；闸8 真浏览器渲染页面(任务号/学校/专业/状态/按钮/计数齐)+候选池弹窗(学生名+申请号)、390px 内。反向样本 v180 均失败。
- index.html 3 处 v180→v181；提交 `d1ec0d4`（未推送）。

## 偏离或疑点
1. **迁移我跑不了、只能你跑**（同 JOB-09，`.env` 禁读）。跑完候选池才出真数据；跑之前弹窗会显「读取失败/暂无」。
2. **item3「学校抽屉里的任务列表」延后**：要在学校详情抽屉里按学校列它的任务并可点进去。可做，但要改学校抽屉装配，本卡先聚焦独立的「招生任务页」。建议下一小卡补。
3. **item4「学校成员账号」延后**：`school_members` 管理员本就可读，但同样要改学校抽屉装配。建议与 item3 一起补一小卡。
4. **item5「学校自审意见」已完成**：在 JOB-05(v173) 已把 `school_doc_reviews.comment` 显示在学生抽屉材料区，本卡核对一致，无需重复。
5 真实数据验收在线上做（本地用受控数据证明逻辑）。
