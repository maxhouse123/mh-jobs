# JOB-12-06 · 第十二轮收尾（卡1084 + 卡1085）

结果：**四件事全过**（EF v30 已核 / 清桶 100→0 / 0198 已应用验证·测试审计行已删 / 重灌前后大小已量）

## 大白话（业主看这段）
把第十二轮彻底收尾了：
1. **核对**：线上 watermark-doc 已是 **v30**（部署前 v29），审计表的 outcome 约束也确实放宽了（含 `served_cached_direct`）。两条都对，才往下做。
2. **清桶重灌**：把桶里 100 个旧大件全清了（`wm_cache_purge` 调 3 次：100→50→1→0），然后每 5 分钟的定时任务按新尺寸（1600/JPEG75）自动重灌，约半小时补回来，稳定在 95 个、待补 4（回到开工水平）。
3. **审计标签改回**：把 `log_wm_view` 记的 outcome 从临时的 `served` 改回 `served_cached_direct`（约束已放宽），模拟审核员调了一次确认真写进去了、标签正确，然后把这条测试记录删掉了。
4. 本回执 + 给 SUMMARY-12 追加「收尾」一节。

**瘦身效果**（重点看这个）：
| | 桶对象数 | 平均大小 | 总大小 |
|---|---|---|---|
| 重灌前（旧 2000/80） | 100 | 398.6 KB | 38.93 MB |
| 重灌后（新 1600/75） | 95 | **340.9 KB** | 31.63 MB（−19%） |

分类看更清楚（这才是关键）：
- **图片 jpg：69 个，平均 202.2 KB** ← 审核端最常看的就是图片，瘦身在这里最明显（从混在 ~398 里降到 202）。
- PDF：25 个，平均 701.1 KB ← **本轮 PDF 没瘦**（卡明确 PDF 不变），它们占了桶总大小的一半多，把平均值拉高了。
- PNG：1 个，910.4 KB ← PNG 不重压。
> 结论：**审核端看图片会明显变快**（直读 + 图片瘦身双重生效）；PDF 还是老样子（下轮可另做 PDF 瘦身，见建议包）。

## 断言表
| 检查 | 期望 | 实测 | 过 |
|---|---|---|---|
| watermark-doc 版本 > 部署前(v29) | 是 | **v30** | ✅ |
| outcome 约束含 served_cached_direct | 是 | 含（served/denied_not_assigned/prepared/served_cached/served_original/served_video_url/served_cached_direct） | ✅ |
| wm_cache_purge 清桶 | →0 | 100→50→1→0（3 次调用，各返回 50/50/20；40s 间隔） | ✅ |
| cron 重灌回开工水平 | 待补≈4、连续两次不变 | 95 objs / 待补 4（18:47 与 18:52 两次一致） | ✅ |
| 0198 函数数不变 | 1→1 | 1→1 | ✅ |
| log_wm_view outcome | served_cached_direct | 模拟 reviewer1 调用写入 outcome=served_cached_direct、返回 RV-D09E | ✅ |
| 测试审计行删除 | 只删这一行 | DELETE 1，id `f8f5eeb4-2492-43e1-a205-24c3c732f32b` | ✅ |

## 重灌进度实录（cron 每 5 分钟一采）
```
18:18  0 objs  (avg 0)     待补 99
18:23  20     avg 647.1    待补 79
18:28  40     avg 484.3    待补 59
18:33  59     avg 393.2    待补 40
18:37  75     avg 352.4    待补 24
18:42  91     avg 343.8    待补 8
18:47  95     avg 340.9    待补 4
18:52  95     avg 340.9    待补 4   ← 稳定
```

## ⚠️ 收尾发现（非致命，列入建议包 #1）
`wm_cache_index` 表**重灌后仍 0 行**——本该由 v30 EF 的 prepare 每次渲染后写一行。查明原因：**service_role 对 wm_cache_index 没有 insert/select 授权**（0196 里 revoke 了 anon/authenticated，但没 grant service_role），EF 的 upsert 静默失败（我 12-02 的 try/catch fail-open 吞掉）。
- **影响：无（非致命）**。`log_wm_view` 取不到索引 rv_code 时会**兜底自算**（RV-hash4，确定性、正确，已实测）；直读提速走的是桶读策略、**不依赖索引**。所以功能全正常，只是这张索引表空着没派上用场。
- **修法**（一行 grant，新增型，下轮做）：`grant insert, select, update on public.wm_cache_index to service_role;`

## 动过的数据/文件
- DB：迁移 0198（create or replace log_wm_view，outcome→served_cached_direct）；清桶（wm_cache_purge ×3，只发 cleanup，未删 storage.objects 行）；删 1 条测试审计行。**桶只碰 wm-cache，原件桶 student-documents 一字节未动；EF 未改。**
- 归档：supabase/migrations/0198_*；jobs/JOB-12-06/*（迁移/探针/reflow 日志/回执）

---

## 📦 第十三轮建议包（替 Claude 先想；判据 + 边界；最终 Claude 拍板成文）

**建议包 A · wm_cache_index 通电（授权 service_role 写）**
- 为什么：本轮发现索引表 0 行——v30 EF 写不进（service_role 无 insert 授权）。
- 改法：迁移 grant insert/select/update on wm_cache_index to service_role（新增型，无 drop）。
- 判据：授权后触发一次 prepare（测试学生×测试审核员），wm_cache_index +1 行；log_wm_view 从索引取到 rv_code（非兜底路径）。
- 边界：只 grant；不改 EF、不动桶、不动策略。

**建议包 B · PDF 预渲染件瘦身（源码不部署）**
- 为什么：本轮只瘦了图片（jpg 202 KB）；PDF 仍 701 KB × 25 个，占桶总量一半多，审核端看 PDF 仍慢。
- 改法：watermark-doc `prepare` 的 PDF 分支加降采样/压缩（view 现场渲染分支不动）；deno check；不部署，出部署卡。
- 判据：结构自检 + 部署重灌后 PDF 平均大小明显下降。
- 边界：只动 prepare 的 PDF 段；view/signed_url/图片段零改。

**建议包 C · 审计完整性核对（outcome 放宽后）**
- 为什么：round 11/12 发现 EF 写的 prepared/served_cached 等曾被旧约束**静默拒**（logAccess 吞错），审计从建桶到放宽前有缺口；约束现已放宽，需确认各路径现在真落审计。
- 改法：只读——分别触发 prepare/view/signed_url/直读 各一次（测试对象），核 reviewer_access_log 出现对应 outcome 行。
- 判据：四类 outcome 各至少 1 行新审计；缺哪条即定位。
- 边界：只验；缺则改 EF 源码不部署，出部署卡；不改数据。

**建议包 D · outcome 约束归档进迁移账本**
- 为什么：放宽约束是业主在 SQL Editor 手贴的，未入 `supabase/migrations/`（铁律16 精神：SQL 即归档）。
- 改法：补一条归档迁移文件记录当前约束定义（现状快照，不重复改约束）。
- 判据：supabase/migrations/ 有该约束的归档 SQL，与 pg_constraint 现状逐字一致。
- 边界：只归档/文档；不动约束本身。

**建议包 E · 历史 backlog 收口**
- storage 3MB 孤儿对象（`43668ae4…/bank/1789571102495_4vtz_mh8-test-3mb.jpg`）业主后台手删；stuA 测试密码 Auth 里重置；邀请重发 RPC；迁移账本双目录收敛。
- 判据/边界：逐条小任务，各自只读盘点 + 最小改动或留业主手动。
