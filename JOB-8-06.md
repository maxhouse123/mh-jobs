# JOB-8-06 · 桌面项（验收项 13：7-07 驳回理由英文提示 · 7-08 分页）— 回执

**结果：两项都验过了，都正常。这两包不在手机上、且管理端/审核端线上被 Cloudflare Access（邮箱 OTP）挡着、本轮又只有 stuA 密码，所以没法无头"真登"这两端——改用「本机文件动态验（本机文件与线上部署 md5 一致，等于验线上逻辑）＋线上文件静态核对」。中介端线上没被 Access 挡，直接取回线上文件确认了分页器在。**

## 验法说明（诚实）
- 管理端/审核端线上首页 `curl` 回来的是 **Cloudflare Access 登录页**（HTTP 200 但内容是 OTP 登录，实测含登录页迹象）→ 无头浏览器过不了 OTP，且本轮只给了 stuA 密码 → **不能真登**。
- 本机的 admin v209 / reviewer v33 / partner v154 文件与线上部署 **md5 一致**（闸1 已核，SUMMARY-7 §2）→ 对本机文件做**真实浏览器动态验**＝验的就是线上那套逻辑。
- 中介端线上文件（不在 Access 段）直接取回，确认部署里有分页器。

## 7-07 · 驳回理由英文提示（管理端 v208→v209 承继 / 审核端 v33）
本机 Chrome 跑 `tests/job7-07.mjs`（真实渲染 + 触发 CJK 检测）：
| 断言 | 实测 | 过? |
|---|---|---|
| 管理端 驳回框下灰字英文提示存在 | 是 | ✅ |
| 管理端 输入英文不触发琥珀边框 | border 空 | ✅ |
| 管理端 输入中文→琥珀边框+提示加粗 | `rgb(240,160,32)` + `800` | ✅ |
| 审核端 同上三条 | 同上 | ✅ |
- 静态核对本机(=部署)：admin `_v7CjkWarn` 定义 1；reviewer `_v7CjkWarn` 1、两个退回框 `oninput` 各挂（2 处）。
- **6/6 过**。提交按钮不被禁用（只提醒不拦）符合设计；本包**未点驳回、未提交任何裁决**。

## 7-08 · 分页（管理端 v209 / 中介端 v154）
本机 Chrome 跑 `tests/job7-08.mjs`：
| 断言 | 实测 | 过? |
|---|---|---|
| 管理端 多页显「Page 1/3」+ 上/下一页(btn-ghost) | 是 | ✅ |
| 管理端 单页不显分页器 | 返回空 | ✅ |
| 管理端 中间页 prev→0 / next→2 | 是 | ✅ |
| 管理端 分页四语(fr/ru/zh) | 全有 | ✅ |
| 中介端 `window._dlInvSetPage` 存在可调用不抛 | 是 | ✅ |
- **线上确认**：中介端线上文件 `maxhouse-partner-portal-v154.html`（HTTP 200，933KB）grep `_v209Pager` = 1 → **部署里确有分页器**。
- 管理端静态核对本机(=部署)：`_v209Pager` = 1。
- **5/5 过**。

## 结论
- 验收项 13（7-07 英文提示 + 7-08 分页）：**逻辑与部署均确认到位**。
- 唯一没做的是"人肉真登点一遍"——因 CF Access + 无对应测试账号密码（环境限制，非缺陷）。若你要真登验收：本机原生浏览器登管理端/审核端（过 CF Access OTP），看驳回框灰字提示 + 打中文变琥珀边框、看委托邀请/已发邀请列表底部的分页按钮即可。

## 截图
- 本包为静态 + 本机动态验，无真登截图（管理端/审核端 CF Access 挡、无测试密码）。断言以脚本实测值为凭（见上表）。

**只读；未真登、未提交任何审核裁决、未点驳回确认、未发邀请、未改任何本地文件。回执推日志仓库。**
