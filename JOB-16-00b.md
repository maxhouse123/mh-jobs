# JOB-16-00b 回执 · 密码轮换后的一次性清理（卡1121 ①a–e）

只做一次。全程不打印任何密码/串；下面只写文件名与处数。

## a. 新密码真登
- `acct-check.mjs`：stuA / partner1 / reviewer1 三个必有账号用新密码各真登一次 → 3/3 OK（`ACCT-CHECK-OK`）。没有登不进的，不触发停。

## b. secret-scan 改严
- `jobs/JOB-16-00/secret-scan.mjs`：改回「新文件、旧文件一律拦」（去掉上轮的 git 感知放行）；扫描串 = accounts 现役密码 + `others` + **`retired` 退役串**；另扫 JWT 形状；命中即 exit 1。
- **一处要紧的坑（已处理）**：`retired` 里那条退役串**恰好与公开的 GitHub 组织名相同**（出现在所有 `github.com/<组织>/…` 仓库地址、以及交接文档里「account: …」的引用处），这些是公开标识、不是密码泄露。所以扫描/替换对退役串加了两道判据：① 不是斜杠包裹（排除 URL 路径）② 前 40 字里有「密码/password/pwd」这类字样（排除组织名引用）。现役密码是随机串、不加此宽松判据、照旧全量拦。

## c. ~/mh-jobs 清理
- `jobs/JOB-16-00/redact-retired.mjs`（只读改写、只打印文件名+处数）把退役串换成占位符「[已作废的旧测试密码]」。
- 命中并替换：`SUMMARY-5.md`（1 处）、`JOB-10-04.md`（1 处）。均为真「密码」上下文；两文件里没有组织名 URL，无误伤。
- 替换后 `secret-scan.mjs ~/mh-jobs` 全树 = `SECRET-SCAN-CLEAN`（0 命中）。
- 普通 commit + push，未改历史、未 force。

## d. 主仓库 + ~/mh-verify 旧脚本
- 主仓库命中并在**工作区**抹除（同占位符）：`jobs/JOB-10-04/report.md`（1 处）、`jobs/SUMMARY-5.md`（1 处）。
- **说明（已修正）**：`jobs/` 多数是未跟踪草稿，但**逐个核对后发现 `jobs/JOB-10-04/report.md` 确为已跟踪文件**（`git ls-files` 证），已把它的抹除**随本轮 commit 带上**（主仓库 commit `62694a0`，只 commit 不 push）；`jobs/SUMMARY-5.md` 为未跟踪，工作区抹除即足。主仓库自此**无任何已跟踪文件含旧密码**（secret-scan 复扫 CLEAN）。（原稿曾误判「jobs/ 全部未跟踪」，此更正。）
- 主仓库里另有若干 `github.com/<组织>/…` 与「account: 组织名」的引用（CLAUDE.md、docs 三份交接、jobs/HOWTO-CC队列模式.md）——那是公开组织名、**不是密码**，按 b 的判据不算命中、**不动**。
- `.env` 是被 .gitignore 的合法密钥库，扫描器已跳过、**不碰**。
- ~/mh-verify 旧脚本命中并处理：`probe_auth_job5_01.mjs`（1 处，仅出现在文件首行注释里、非活代码；已抹除）。
- 三棵树（主仓库工作区 / ~/mh-jobs / ~/mh-verify）复扫全部 `SECRET-SCAN-CLEAN`。

## e. 纪律
- 本轮起所有回执/日志/脚本一律不写密码明文；需提到写「见 accounts.json」。本包脚本只「读」accounts.json，回执零串。

## 业主要做什么
- 无需操作。已知会：a–e 已一次做完；`JOB-16-00b.md` 在此，后续不重做。
