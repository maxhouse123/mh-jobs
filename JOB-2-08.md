JOB-2-08  结果：全闸通过（迁移已跑 + 管理端 v188 → v189）

## 做了什么（一句白话）
两个新的管理能力：
1. **替换通知书**：学生抽屉 OFFER 区「录取通知书」那行，除了查看/下载，多了「替换」按钮——选一个新文件上传到通知书桶、填替换理由，旧文件留桶备查，通知书路径换成新的。
2. **撤销派单**：材料审核台（"N 份材料在审核员手上"）每行多了「↩ 撤销派单」——填理由后把这份材料从审核员手上收回，退回可派池重新等待派单。用的是"软撤销"（打标记，不删派单记录），符合"绝不删数据"的红线。

## 迁移（我经 db-run.sh 跑的，跑前跑后差值）
- `reviewer_assignments` 加 3 列：`revoked_at / revoked_by / revoke_reason` —— **列数 5 → 8（+3）**。
- 新增 2 个函数：`admin_replace_offer_letter(uuid,text,text)`、`admin_revoke_dispatch(text,text)` —— **本卡两函数 0 → 2（+2）**。
- 新增 `offer-letters` 桶的管理员上传策略 `offer_letters_admin_write`（原来只有学校能传，管理员传不了）—— **+1 策略**。
- 迁移 `jobs/JOB-2-08/migrate.sql`；回滚 `jobs/JOB-2-08/rollback.sql`。

## "回可派池"怎么实现的（重要说明）
`reviewer_assignments` 表没有状态列，"回池"最直接是删派单行——但**红线和 db-run.sh 都禁止 DELETE**。所以我改成**软撤销**：加 `revoked_at` 列，撤销 = 打上时间戳（UPDATE）。前端三处读派单表的查询都加了 `revoked_at is null` 过滤，于是被撤销的材料不再算"已派"、自动回到可派池。既不删数据，又达到"回可派池"效果。

## 业主要看什么
1. 学生抽屉 → OFFER → 录取通知书行：有「替换」按钮。
2. 「材料审核」台：每行有「↩ 撤销派单」；撤销后该件离开此列表、回到派单台可派池。
（真上传/真撤销是对数据库的真写入，需你登录管理员在线上点一次验收；本地只用受控桩验证了接线，未碰真库。）

## 十道闸（逐条）
- 闸1 基底 md5：`9bb020499ad55d1bba7e05d5ab816463`（=v188）✓
- 闸2 锚点唯一：8 处各 =1 ✓
- 闸3 补丁自检：两函数定义/两 rpc/offer select+映射补 id/替换按钮/撤销按钮/两处 revoked 过滤 各 =1 ✓
- 闸4 产物 md5：`3d0f8be1f75e1250937abfda84068c10`（v189）
- 闸5 语法：1 脚本块 0 报错 ✓
- 闸6 消失检查：774→776（+2 函数，无消失）✓
- 闸7 四语：管理端中英；新增文案（替换/撤销派单）中英齐全，零裸键 ✓
- 闸8 真点击：Playwright 桩 —— _v189RevokeDispatch 走 rpc('admin_revoke_dispatch',{p_doc_id,p_reason}) 理由透传；renderDocs 行含「↩撤销派单」+_v189RevokeDispatch；loadOfferDecisionsFromCloud 映射后 _offerDecCache[0].id 存在（替换按钮依赖它）；_v189ReplaceLetter 定义且空 id 守卫弹错。反向样本 v188：两函数未定义 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：新增写死颜色 0 / CSS 类 0 / 字号档 0（复用 btn-secondary/btn-ghost/_promptP）✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：9bb020499ad55d1bba7e05d5ab816463 → 3d0f8be1f75e1250937abfda84068c10
迁移跑前跑后：reviewer_assignments 列 5→8(+3)；本卡函数 0→2(+2)；offer_letters_admin_write 策略 0→1
index.html 改了几处：3（v188→v189）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 撤销派单用软撤销（UPDATE revoked_at）而非 DELETE，原因见上（红线+db-run 禁删）。**「回可派池」在管理端已完整生效**（三处派单查询过滤 revoked）；但**审核员端(reviewer 端)自己的"我的待审"列表若也按 reviewer_assignments 判断，需同样过滤 revoked_at 才能让审核员那边也看到该件消失**——那是 reviewer 端改动，不在本管理端卡范围，列为观察项。
- `admin_revoke_dispatch` 参数用 text（doc_id 实为 text）。
