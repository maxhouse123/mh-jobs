# JOB-17-02 · B 打开提速 —— 迁移 0210/0210b + 管理端 v217 直读 + 审核端 v42 预取跟着看的地方走

> 本包 CC 改：迁移 0210(已应用)/0210b(待业主手贴)、管理端 v216→v217、审核端 v41→v42。主仓库已 commit 未推(卡1135 收官推)。

## 一、做了什么

### 迁移 0210（CC 已用 db-run 应用）+ 0210b（业主 SQL Editor 手贴）
先只读探针实证（`jobs/JOB-17-02/probe-*.sql`，2026-09-21）：`admin_access_log` 列 = id/admin_uid/doc_id/file_kind/outcome/accessed_at（id 默认 gen_random_uuid()、accessed_at 默认 now()）；现约束 `admin_access_log_outcome_check` = {served_unwatermarked, denied_not_admin}；`is_admin(uid uuid) returns boolean`；student-documents 桶现无 admin/reviewer 直读 SELECT 策略（仅 Students read own files + partner_student_docs_select）；`documents` 有 file_path/file_name/doc_type、主键 doc_id(text)；库内无 admin_log_doc_view 同名函数。
- **0210_admin_direct_read.sql**（全新增，走 db-run）：①`create policy admin_read_student_documents on storage.objects for select to authenticated using(bucket_id='student-documents' and is_admin(auth.uid()))`（探针③证现无等价策略 → 新建；不用 drop policy，遵铁律9）；②`create or replace function admin_log_doc_view(p_doc_id text) returns jsonb security definer`：非管理员→插 denied_not_admin、返回 `{ok:false,error:'not_an_admin'}`；文件行不在/无路径→`{ok:false,error:'original_not_found'}`；否则插 served_direct(file_kind=扩展名)后返回 `{ok:true,file_path,file_name,doc_type}`；**插入失败=函数报错=不返回路径（记不上就不给看）**；③`revoke all … from public,anon` + `grant execute … to authenticated`。**已用 `bash jobs/db-run.sh` 应用成功**：V_policy=true / V_fn=true / V_grant_auth=true / V_grant_anon=false。
- **0210b_admin_outcome_served_direct.sql**（`alter … drop constraint` 被 db-run 红线挡，照 0204 先例 → 业主在 SQL Editor 手贴，卡1136 同批）：outcome CHECK 加 `served_direct`（{served_unwatermarked, denied_not_admin, served_direct} 三值）。**执行前**：admin_log_doc_view 的 served_direct 插入被旧约束拒 → 函数报错 → 管理端 v217 按设计**回落 admin-doc 云函数**（功能不破，只回到旧慢路）；denied_not_admin 拒绝路径立即可用（在旧约束内）。
- 正本 `supabase/migrations/`，同一 commit cp 到 `docs/supabase 文件/`。

### 管理端 v216 → v217（取件路改写）
- 新增 `_v217AdmFetchOriginal(docId)`：先 `sb.rpc('admin_log_doc_view')`，`ok` 才 `sb.storage.from('student-documents').download(file_path)` **直读**（`_v217TypeBlob` 按魔数 FFD8/PNG/%PDF/RIFF..WEBP 重设 blob.type，与 admin-doc EF 口径一致）；RPC 报错（含 0210b 未执行时 served_direct 被拒）/下载失败 → 原样**回落 admin-doc 云函数**（`_v217FetchViaFunction`，逐字保留原鉴权/15s 超时/错误映射）；RPC 明确 `not_an_admin` → 报错**不回落**。
- `adminViewOriginal`（真按钮「查看原件」的 onclick）与 `_v177FetchAdminDoc`（打包下载，每件各记一次审计）两个取件点都改走它。`ADM_DOC_CACHE` 语义不变，缓存对象加 `via`(direct/function)+`fileName`；信息行「取件用时 X.X s」后标「**直读**」或「**云函数**」。
- 四件套同版：文件名 v217 / 头注 / `[VER] admin portal v217` / index.html 重定向三处。

### 审核端 v41 → v42（预取跟着看的地方走）
- 本端学生组默认全展开、无收合开关 → 「展开哪组预取哪组」落为「**看到哪组预取哪组**」：`studentCaseCard`/`studentCaseDoneCard` 加 `data-scode`，`renderMaterials` 末尾重挂 `IntersectionObserver`（`_v42ObserveGroups`），学生组卡露出 **≥1/3**（ratio≥0.34）→ `_v42EnqueueGroupByCode` 把该组可看件**插到预取队列最前**（去重，跳过已缓存/判死/不可视）。
- 预取由「每次各起一泵」改为「**单一共享队列 `_v42Q` + 单泵 `_v42Pump`**」——原来 `_wmEnqueuePrefetch` 每次调用各起一泵、每泵≤3，多组同时会让**并发>3**；现在全部入同一队、只有一个泵以 **≤3** 并发抽干，`_wmEnqueuePrefetch` 保留原名走共享队列。「查看中暂停」「标签页隐藏暂停」照旧；悬停「查看」仍 `_wmPrefetch` 立即取。零 UI/RPC/库改动。
- 四件套同版：文件名 v42 / 头注 / `[VER] reviewer portal v42` / index.html 重定向三处。

## 二、结果是什么（闸3 逐格真跑，脚本 `jobs/JOB-17-02/`）

### ① N4 真身份表（`n4-real.mjs`，真 JWT 打 REST，**ALL-PASS**）
| 路 | 期望 | 实测 |
|---|---|---|
| anon 调 admin_log_doc_view | 拒 | http 401 permission denied for function（execute 已 revoke anon）✅ |
| reviewer1 JWT 调 RPC | not_an_admin 无路径 | http 200 `{ok:false,error:'not_an_admin'}`（顺带写 1 行 denied_not_admin 审计）✅ |
| reviewer1 JWT 直下无关学生对象 | 拒 | http 400 ✅ |
| stuA JWT 直下无关学生对象 | 拒 | http 400 ✅ |
| partner1 JWT 直下无关学生对象 | 拒 | http 400 ✅ |
| stuA JWT 直下**自己**的对象 | 放行 | http 200 ✅（新 admin 策略未误伤自读） |
- **管理员（业主）直读成功路（served_direct 真插+真下载）无 CC 凭据 → 进 SUMMARY 待业主手验**；且 0210b 未跑前该路本就回落云函数。

### ② 管理端 v217 取件路网络行为（`gate-admin.mjs`，chromium，stub 会话，真按钮真点，**ALL-AS-EXPECTED**）
| 场景 | RPC/storage/admin-doc | 判定 |
|---|---|---|
| **旧 v216** 打开原件 | 0 / 0 / **1** | **RED**（旧走 admin-doc 云函数）|
| 新 v217 顺路 | 1 / **1** / **0** | GREEN，信息行「原件 20 KB · 取件用时 0.0 s · **直读**」|
| 新 v217 RPC 桩失败 | 1 / **0** / 1 | GREEN（存储 GET=0，改走 admin-doc，标「**云函数**」）|
| 新 v217 桩 not_an_admin | 1 / **0** / **0** | GREEN（存储与 admin-doc 都 0，界面报错）|

### ③ 审核端 v42「看到哪组预取哪组」（`gate-reviewer.mjs`，chromium，reviewer1 真登，**AS-EXPECTED**）
- 滚到第 3 个学生组停一会，再真点该组首个「查看」，数「点开之后」针对该件的新请求：
  - **旧 v41**：preCached=false，点开后该件 **1** 个新请求（**RED**），画出 537ms。
  - **新 v42**：preCached=true（观察器已把该组预取到手），点开后该件 **0** 个新请求（**GREEN**，判据只看这一条），画出 **94ms** 秒开。
  - 并发峰值：旧 2 / 新 3，**任何时刻 ≤3**（现值未加大）。

### ④ N1 差集（commit 前，`inv.mjs diff`）
- **管理端 v216→v217**：ids removed=0 / handlers removed=0 / functions removed=**0**（+3：`_v217AdmFetchOriginal`/`_v217FetchViaFunction`/`_v217TypeBlob`）。
- **审核端 v41→v42**：ids removed=0 / handlers removed=0 / functions removed=**1** = `pump`——**这是旧 `_wmEnqueuePrefetch` 内嵌的私有泵函数，本包重构为模块级 `_v42Pump`**（非按钮、非公开 API、非用户可见入口），有意改名/上提，按 N1「有意删除/改名：原因」如实记；新增 5 个 `_v42*`。动态清单：无 id/handler 变化 → 可见按钮集不变。

## 三、业主要做什么
- 本包**先提交未推**（主仓库本地 commit，未 push；推送由收官后卡1135 做）。
- **卡1136 里跑**：`0210b_admin_outcome_served_direct.sql`（SQL Editor 手贴，与部署同批；跑前 admin 直读成功路回落云函数不破）。
- **要业主亲眼看的**（进 SUMMARY 清单）：
  1. 管理端**关代理**打开 3 份不同原件，「取件用时」标「**直读**」且每份 ≤ 2 s（0210b 已跑后）；桩失败/无管理员时标「云函数」。
  2. 审核端往下翻到后面的学生再点开，基本**秒开**（第 3+ 组也预取到手）。
- 截图见 `~/mh-jobs/shots/17/17-02/`（2 张元素级绿图 + INDEX.md，全合成 PDF、无 PII）。红版对照以脚本网络计数为准（非视觉件不单独出图）。

## 四、新闸证据（N1–N6）
- **N1**：见 ②④；管理 removed=0，审核 removed=1（`pump`→`_v42Pump` 有意重构，已交代）。
- **N2**：管理旧 v216 admin-doc（红）→ 新 v217 直读（绿）；审核旧 v41 点开发新请求（红）→ 新 v42 0 请求（绿），均真页面真点（reviewer1 真登 / admin stub 真按钮 page.click，未用 evaluate 直调）。
- **N3**：N4 表全走真 JWT（`restLogin` 真登拿 token 打 REST/RPC/storage）；探针走 db-run 超级用户读 schema。
- **N4**：admin_log_doc_view 六路 + 三端直读拒/自读放行逐格真验（ALL-PASS）；admin 取件三分支网络行为逐格（ALL-AS-EXPECTED）；served_direct 管理员成功路进待验清单。
- **N5**：`secret-scan` 对 `~/mh-jobs/shots/17/17-02` 与 `jobs/JOB-17-02` → `SECRET-SCAN-CLEAN`；主仓库 commit 前对改动文件另扫一遍。
- **N6**：只按图纸做 0210/0210b + v217 + v42，未加新活；发现的可留观察（PDF 直读也可推广到学校端录取通知书样张等，本轮不动）记入第十八轮建议。

## 五、动过的槽 / 测试写库
- **本包零材料上传、零测试派单、零 §0.11 批次占用**：管理端全在网络层 stub（page.route 拦 RPC/storage/admin-doc），审核端只读 doc 字节（route 成合成 PDF），N4 走真 REST/RPC/storage（读为主）。
- 唯一真库写：N4 里 reviewer1 真调 admin_log_doc_view → `admin_access_log` 多 **1 行 denied_not_admin**（真实拒绝审计，非测试件，按审计惯例保留不清；探针⑥同类）。带身份复核无残留测试材料行。

下一包 = 17-03
