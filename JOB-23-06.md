# JOB-23-06 回执 · 管理端 v225：左栏按 7 组重排（只搬菜单，不动页面）

做这一步的人：CC（无人值守）。只动管理端一端，版本 v224 → v225（旧版留着可回滚）。

## 一、做了什么（大白话）

管理端左边那一长条菜单原来是 5 段、18 项挤在「工作台」一大堆，待办、档案、日志、设置混在一起不好找。这一版把同样的 27 个菜单项**原封不动地搬**进 7 个分组，顺序按业主图纸排：

1. **总览** — 驾驶舱
2. **审核台** — 材料派单、材料审核、待二审队列
3. **待我裁决** — 学校待审、合作伙伴待审、资料变更、Quota 审批、任务审批、撞单裁决、信息更正
4. **录取与服务** — 录取通知分发、Concierge 顾问台、招生任务
5. **档案** — 学生、学校、合作伙伴、申请记录、填了一半的申请、付款流水
6. **日志** — 审计事件、学校行为日志
7. **设置与增长** — 开学季、邀请码、优惠码、委托邀请、发公告

关键点：**只是搬家**。每个菜单项的点击行为、跳转目标、上面的数字角标一个字都没改；点开每一项还是原来那个页面。只是多了 6 个新的分组小标题（中英两语），旧的 4 个分组名词（工作台/业务运营/增长·系统/系统设置）留在词典里没删（万一别处还引用）。

## 二、动的文件

- `admin-portal/maxhouse-admin-portal-v225.html`（新版，md5 `b17b9f4f7912eb93d4c429815b7ddda9`）
- `admin-portal/index.html`（三处跳转 v224 → v225）
- `docs/changelog/admin-portal-versions.md`（追加 v225 一行）
- `tests/job23-06-admin-v225.mjs`（随卡静态断言，新）
- `jobs/JOB-23-06/build.mjs`（搬家构建脚本，新）
- `jobs/JOB-23-06/repro/green.mjs` + `shots.mjs`（无头验证 + 截图脚本，新）

主仓库已提交一笔，**未推**（本轮铁律：CC 不推主仓库，收官由业主用卡1171 推）。

## 三、旧病对应

本卡不对应 23-00 的 R1–R7 任何一条（那七条已在 23-01～23-05 逐条转绿）。本卡是纯 UI 归整，无「先红后绿」目标，靠「搬家前后菜单项字节一致 + 无头真渲染」把关。

## 四、各道闸

- **闸3 静态断言**：`tests/job23-06-admin-v225.mjs` → **35/35 全绿**。含：版本四件套（头注/[VER]/MH_PORTAL_VER=225/index 三处/无残留 v224）、7 个 `.sidebar-section` 且组标题顺序精确、27 项 `data-view` 顺序精确、**data-view 集合 / nav-badge id 集合(18) / 侧栏 inline onclick 集合(27) 与 v224 完全相等（N1 纯搬家）**、6 个新组键中英各 1、旧组键 nav.workflows/operations/growth/settings 保留、v220 三条摘除注释仍在侧栏区、内联脚本可解析。
- **块字节一致（关键实证）**：单独脚本比对 v224 与 v225 的 27 个 nav-item 块（按 data-view 配对）→ **27/27 字节完全相同**，证明确为「只搬不改」。
- **闸8 无头真渲染**：`jobs/JOB-23-06/repro/green.mjs` → **8/8 全绿**：
  | 断言 | 结果 |
  |---|---|
  | 7 组标题中文精确（总览…设置与增长） | ✅ |
  | 7 组标题英文精确（Overview…Settings & growth） | ✅ |
  | 27 项 data-view 顺序精确一致 | ✅ |
  | nav-badge id 集合 = 18、与 v224 一致 | ✅ |
  | 搬家项 switchView 仍生效（dispatch→审核台 / broadcast→设置与增长） | ✅ |
  | 390px offcanvas 滚到底、末项「发公告」可见且可点（点后 view=broadcast） | ✅ |
  | 中英零裸键（侧栏无 nav.* 残留） | ✅ 中0/英0 |
  | 零 pageerror | ✅ 0 |
- **闸9 视觉零新增**：只复用既有 `.sidebar-section` / `.sidebar-section-title` / `.sidebar-nav` / `.nav-item` / `.nav-badge`；CSS 逐行 diff 证明**零新增样式**（无新类、无写死颜色、无新字号、无新 @media）。
- **N1（授权移除清单）**：本卡**零移除**——纯搬家，data-view / inline onclick / 所有 id（含各 nav-badge）集合与 v224 三项皆相等，差集为空。
- **上游回归**：23-01/02/03 静态重跑全绿（28/35/27）。23-04/05 各 1 条 FAIL，**均为 `index.html 三处指向 v223/v224` 这一条**——因 index.html 现已指向 v225（index 永远跟最新版），属预期陈旧断言、非回归；其余断言全绿。
- **shell-lint**：本包无 shell 脚本（构建/验证均 node .mjs）。
- **secret-scan**：改动文件全扫，无 service_role / JWT / 私钥 / 明文密码，**clean**。

## 五、要业主亲眼看的（线上验收，留业主）

- 左栏变成 7 组、顺序如上表；每项点开还是原来的页面。
- 手机宽度（≤768px）点汉堡打开侧栏，能一路滚到底，「发公告」可见可点。
- 截图见 `~/mh-jobs/shots/23/23-06/`（`sidebar-7groups.png` 全组 / `sidebar-390-bottom.png` 手机末项 + `INDEX.md`）。

## 六、STOP？

无。本轮暂无 `~/mh-verify/r23-STOP.txt`。

---
下一包 = 23-07
