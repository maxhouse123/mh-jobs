# JOB-24-00 · 第二十四轮开工：核对家底 + 造假数据 + 把六个毛病先量成「红」

（CC 写给业主。大白话。这一包不改任何页面，只做准备和体检。）

## 一、做了什么

1. **核对家底**（对不上就得停，结果全对上了）：
   - 五个端口的文件指纹（md5）跟卡里写的一模一样：管理端 v226、学生端 v400、中介端 v170、学校端 v235、审核端 v46。
   - 本地代码和云端 GitHub 同步（HEAD 一致），没有没推的东西。
   - 数据库迁移账本到 0213 已应用；本轮要新写的 0214/0215/0216 还没写、也不会真跑。
   - 没有触发「必须停」的情况，`~/mh-verify/r24-STOP.txt` 没建。

2. **给管理端 v226 拍了张「零件清单」**（供后面每一版比对，防止手一滑把按钮弄丢）：
   共 201 个 id、173 个点击动作、833 个函数，存进 `jobs/JOB-24-00/inventory/admin-v226.json`。

3. **造了一批假数据**（全是编的，没有任何真人信息），喂给管理端在本地跑，用来照出毛病。清单：
   - 6 名假学生（3 名有推荐中介、3 名没有，都带国籍、生日、护照号、申请号）；
   - 4 条 offer（其中 2 条已付「通知书解锁费」¥3,000，故意不给它们配订单，逼系统走兜底）；
   - 4 笔平台服务费（曝光加速 ¥4,500、录取加速 ¥1,500、顾问 ¥600、审核 ¥1,500，合计 ¥8,100）；
   - 5 笔付款订单（3 笔已核销的通知书解锁 ¥3,000 = ¥9,000、1 笔待核销、1 笔已驳回）；
   - 15 条审计事件（含一条「学生报到结束本轮」，以及几条管理员裁决类，专门带英文码 student_materials / withdrawn / referral / passed 用来验翻译）；
   - 60 条学校取件流水（同一所学校对同 2 份材料在 40 分钟里反复取 25 次 + 其它散点）；
   - 2 条顾问工单（一条带完整需求、一条不带）；
   - 2 个待审批招生任务（一条按学校端真实形状塞满了奖学金/要求/备注，一条自费无奖学金）。
   假数据在 `jobs/JOB-24-00/stub/fixtures.mjs`；本地测试壳在 `jobs/JOB-24-00/lib/adminpage.mjs`。

4. **把业主提的六个毛病，用机器人开真页面各量了一遍，全都量成了「红」**（红 = 毛病确实在）：

| 编号 | 毛病 | 量出来的红 |
|---|---|---|
| R1 | 任务审批卡看不到奖学金/要求 | 卡片里「奖学金」「HSK」「截止」三个字一个都没有 |
| R2 | 付款账没算全 | 左栏金额 = **¥8,100**（只算了平台服务费，漏了 ¥3,000 的通知书费）；导出只有 2 行兜底 |
| R3 | 审计事件不人话 | 「学生报到结束本轮」显示成「其他 · round_closed_enrolled」；详情里 student_materials/withdrawn/referral/passed 全是英文码；没有「学生/学校/中介/平台」角色标签 |
| R4 | 学校取件日志难看 | 取件行学校列显示「DB568FF7」（uid 前 8 位，不是校名）；60 行不合并；看不到材料文件名 |
| R5 | 导出不全 | 学生导出没有「出生日期」「推荐码」列；申请导出的「申请编号」印的是任务号「T-1」、阶段是英文「accepted」 |
| R6 | 顾问台看不到需求 | 工单行里没有学生填的需求原文 |

跑的时候页面**零报错**。日志在 `jobs/JOB-24-00/repro/red-all.log`。

## 二、结果是什么

六个毛病全部「先红」到位，零报错。这意味着后面每一版改完，只要同一把尺子量出「绿」，就能证明真修好了，不是自说自话。

## 三、动手前发现的三处「数据来路」隐患（记给后面几包，免得改出空面板）

1. **学校取件行的校名（给 24-05）**：取件流水里的 `school_uid` 是**学校账号的登录 id**，和管理端手里那份学校名册（按 `schools.id` 存）**不是同一套编号**，所以现在对不上、只能显 8 位码。24-05 要把校名接上，得先搞清楚这两套 id 怎么对应（名册里没有「登录 id」这一列），对不上就只能退回显 sc_code。这条按铁律14「数据血缘三问」记账。

2. **取件行的材料类型（给 24-05）**：管理端首屏缓存的材料信息（ADM_DOC_META）现在只存了「文件路径 + 文件名」，**没存材料类型**。24-05 想显示「护照/成绩单」这种中文类型，得顺手把材料类型也缓存进去（首屏那次查询本来就带了类型，取一下即可）。

3. **付款订单的列名（给 24-02）**：付款订单表里**没有** `channel`（通道）和 `confirmed_at`（核销时间）这两列，通道看 `lane`、时间看 `paid_at`。24-02 合账时按真实列名来，别用卡里举例的名字。（这属于卡里 §0.6 写明的「不是必须停」。）

## 四、业主要做什么

这一包不用你做任何事。继续下一包。

---
- 闸3：R1–R6 全红、零报错 ✅；零件清单三张表非空 ✅
- N1 基线：`jobs/JOB-24-00/inventory/admin-v226.json`
- secret-scan：CLEAN ✅；shell-lint：全绿 ✅
- 本轮暂无「必须停」。
- 下一包 = 24-01
