# JOB-14-06 · 存量大图压缩工具（CC 造 + 自测，业主跑）

## 做了什么
- 造了 3 个文件（`jobs/JOB-14-06/`）：`compress.mjs`（Node 用 ~/mh-verify 的 Chromium 把大图压到长边 2600、JPEG 0.88、EXIF 转正，与上传线同参数）、`run.sh`（`--selftest`/`--list`/`--apply`/`--restore`）、`list.sql`（只读清单）。
- **CC 只跑 `--selftest`**；`--list`/`--apply`/`--restore` 留业主原生终端挂代理跑。

## 结果是什么（命令带出处）
- `bash -n jobs/JOB-14-06/run.sh` → **SYNTAX OK**；`node --check compress.mjs` → OK。
- `bash jobs/JOB-14-06/run.sh --selftest`：造 30MP 假图（6000×5000, 1578KB）→ 压到 **2600×2167, 520KB，长边=2600，体积下降**。
- `--apply`/`--restore` 五关键点 grep：**备份路径 2 / x-upsert 3 / HEAD 确认 5 / DELETE 4 / pending 6**，全有。
- `bash jobs/db-run.sh jobs/JOB-14-06/list.sql`（只读）：
  - **jpg/jpeg>1500KB 候选 = 5 件**：2×「big size 2.jpg」6189KB（未缓存）、image.jpg 2215KB、IMG_5208.jpg 2102KB、image.jpg 1510KB。
  - **png/webp>1500 本轮不动 = 2 件**：IMG_8354.png 1803KB、IMG_8353.png 1720KB（只列出、不压）。

## 业主要做什么
一次性存量清理（**先恢复 Supabase 服务**）：
1. 卡1106 看清单：`cd /Volumes/Dev/MAXHOUSE && bash jobs/JOB-14-06/run.sh --list`（写 list.json）。
2. 卡1107 执行：确认 `/Volumes/Data/MAXHOUSE-backups` 已挂载后 `bash jobs/JOB-14-06/run.sh --apply`——每件：下载→备份到 `img-originals-20260919/`→压缩→采用则 x-upsert 覆盖→HEAD 确认→更新 size_kb→删该件 wm-cache 缓存→索引置 pending 让 cron 重灌；不采用的跳过；结果写 `apply.log`。
3. 若某件压后不满意，`bash jobs/JOB-14-06/run.sh --restore <doc_id>` 从备份目录（img-originals-20260919 / pdf-originals-20260917）取原件传回。
- 压完这 5 张大图后，配合 14-01 的止血（大图不再被反复下载）+ 14-04 的 2200/82 重灌，审核端打开这些件就快了。
- CC 全程没跑 --list/--apply/--restore（红线：碰不到 .env 和 Storage 字节）。
