# JOB-8-00 · 手机线自检 — 回执

**结果：6/6 全过，手机线通了，整轮可以往下跑。** 中间有个小插曲：手机 Chrome 的调试口在后台会「装死」（TCP 连上但不回话），把 Chrome 切到前台就活了——之后每包开跑前我都会先把 Chrome 唤到前台再连（只用允许的 `am start` 开 maxhouses.net，没碰任何开关、没装任何东西）。

## 大白话
先证明「电脑能隔空驱动这部手机上的学生端网页」这条线通。手机没锁屏、Chrome 在跑、学生端 v389 页面开着且登着 stuA、能截图、屏幕尺寸量到了。全绿。

## 机器断言表
| 脚本 | 断言 | 实测值 | 过? |
|---|---|---|---|
| 手动/adb | `adb devices` 有 192.168.1.50:5555 device | `192.168.1.50:5555  device` | ✅ |
| 手动/adb | 锁屏检查 mDreamingLockscreen=true 计数 | `0`（未锁）+ 已发 WAKEUP | ✅ |
| 手动/curl | forward 9222 → /json/version 有 Browser 行 | `Chrome/150.0.7871.187`（Protocol 1.3）| ✅ |
| job8_00 | 找到 stuA 已登录的学生端页 | `…student_portal_v389` · `maxhouseapp+stua@gmail.com` | ✅ |
| job8_00 | URL 含 v389（线上版本核对）| `maxhouse_student_portal_v389` | ✅ |
| job8_00 | `window.sb` 存在 | true | ✅ |
| job8_00 | 登录态 = stuA | `maxhouseapp+stua@gmail.com`（注：真邮箱是小写 stua）| ✅ |
| job8_00 | 首屏截图 > 50KB | `678123` bytes | ✅ |
| job8_00 | 视口 | `innerW=411 innerH=809 dpr=2.625`（Pixel 10a，符合预期 ~412）| ✅ |
| 附带 | service worker 活着 + scope | `active=true scope=…/student-portal/`（**7-01 的 PWA 后台在手机上真的注册着**）| ✅ |
| 闸1 开工 | 五端本地 md5 = 基底 | admin v209/student v389/partner v154/school v231/reviewer v33 **全对上** | ✅ |

## 环境备注（给业主 & 后续包）
- **手机 Chrome 调试口在后台会静默**：卡1062 你测的时候是前台所以能通；我从电脑连的时候它可能已滑到后台 → 调试口 TCP 能连但不回 HTTP。解法＝连之前用 `am start` 把 Chrome + student-portal 唤到前台（这是队列 0.1 允许的动作），再 `forward` + 连。已固化成 `~/mh-verify/wake.sh`，每包开跑前先跑它。**没碰 WiFi/设置/没装东西/没点屏幕。**
- 真邮箱是小写 `maxhouseapp+stua@gmail.com`（我一开始拿大写 stuA 比对，误判了一次，已改成不分大小写）。

## 截图
- `~/mh-android/shots/JOB-8/8-00-1-首屏.png`（678 KB）

**只读包，未写库、未改任何文件。回执推日志仓库；截图只在本机。**
