# JOB-16-05 回执 · 学生 v396 / 中介 v163：瘦身状态入库 + 从库读出来显示 + 前端回落补软删过滤

student v395→v396 / partner v162→v163。不改瘦身参数与判定；不回填对不上出处的旧件；只加不删。

## 一、做了什么（大白话）
瘦身那一行（「✓ 已瘦身 680 KB → 45 KB · 1 页」之类）以前只记在当次会话的内存里，一刷新就没了。这次把它**落进库**：材料上传成功、拿到 doc_id 之后，就把瘦身结果写到 documents 的五个小栏（0208 建的 slim_status/原大小/新大小/页数/时间，走 set_doc_slim）。文件卡那一行改成**优先读库里这五栏**来显示，库里没返回时才临时用内存那份顶一下——所以现在刷新、登出再登入，那一行都还在。另外：学生端有一条「兜底」查材料的老路（bootstrap 没带出材料时才走）以前漏了「别显示已删除的」，补上了；中介端三处读材料/齐全度的地方也一并补上「已软删的不算」。顺手把 16-04 原因框里重复的一句小字（标题和输入框上方一模一样）改成「原因」两个字。

## 二、结果是什么（真页面 + 真库，数字带出处）

### 学生端 v396 —— 先红后绿（同一份库行，v396 显 / v395 不显）
真测试件：本机 Node 生成一份 680 KB 单页合成 PDF（纯合成，无 PII），文件名带「【测试】r16-slim」，stuA 真登真选文件传到 recommendation 槽（低批次、非护照）。瘦身器把它压成 45 KB 的 JPG（单页扫描线），记为 slimmed。
- **绿 v396**（`jobs/JOB-16-05/slim-test.mjs`，chromium）：
  - 上传后文件卡出现「✓ 已瘦身 680 KB → 45 KB · 1 页」（`v396_beforeReload.hasSlim=true`）。
  - **刷新后这一行还在**（`v396_afterReload.hasSlim=true`，slimText 一字不差），`v396_GREEN=true`，**0 页面报错**。
  - **库与界面逐字段对上**（`truth('stuA', documents…)`，status 200）：doc_id `cbc9f34a-…`，file_name `【测试】r16-slim.jpg`，`slim_status=slimmed`、`slim_orig_kb=680`、`slim_new_kb=45`、`slim_pages=1`——正是界面那行「680 KB → 45 KB · 1 页」。这同时把 **set_doc_slim 的成功路（新件 <15 分钟内写入成功）** 走通了。
- **红 v395**（同脚本第二段）：同一份库行、同一份文件，v395 不读库 slim → 文件卡**没有**那一行（`v395_row.found=true, hasSlim=false`，`v395_RED=true`），0 页面报错。
- 截图成对（元素级裁剪，只有合成件）：`~/mh-jobs/shots/16/16-05/student-v396-slim-fromDB-GREEN.png`（带瘦身行，卡高 185px）↔ `student-v395-slim-none-RED.png`（无瘦身行，卡高 130px）。
- **CV 槽无死图**：`truth('stuA', documents?doc_type=cv&deleted_at=is.null)` → 活件 0、无 mh10-test（0207 已在库侧修，v396 从库读，确认不再列两张死图）。

### 中介端 v163 —— 页面干净、代传瘦身行会渲染；代传「提交落库」未在无头复现
- **页面 0 报错**：partner1 真登 v163，两次登录 `pageErrors=0`——所有 v163 改动（三处 deleted_at、set_doc_slim 接线、_v163SlimLine 读库、reasonWord）加载运行干净。
- **代传瘦身行渲染绿**：真登代传 is_test 学生 StuB（`f5c5e3e8-…`，`truth` 证 is_test=true；is_test 闸把关，绝不碰非 is_test 学生），传同款 680 KB 合成 PDF → 暂存件文件卡出现「✓ 已瘦身 680 KB → 45 KB · 1 页」（`v163_beforeReload.hasSlim=true`），截图 `partner-v163-slim-beforeReload-GREEN.png`。
- **代传「提交落库」未在无头 harness 复现**（据实报，4 路尝试）：中介端代传是「暂存→提交」两步，提交这一步受 **Q1 图片模糊闸**（合成纯色扫描件 blurVar 低，控制台 `[Q1] … warn:1`）+ AI/多步闸把关。即便脚本把 Q1 质量确认位置真、点了提交按钮（`_matCommit`/`commitResubmission`），真上传+insert 在无头下未落库：`truth('partner1', documents?file_name=like.*r16-slim*)` = `[]`，owner 级全库扫 r16-slim **只有 1 条**（就是学生端那条，见下清点）——**中介端一条测试件都没写进库**（也就没有泄漏），afterReload 那次还撞上二次登录 45s 网关未解锁超时。
- 结论：中介端代传 **DB 持久化标「未放行」**，等业主手验或后续代传槽走查。**依据**：①落库 wiring 与学生端**逐字节同构**（`_v163SaveSlim`→`set_doc_slim` 同一 RPC/同五列；`_v163SlimLine/_v163SlimFromDb` 镜像学生端 `_v396*`；`_hydrateCloudStudent` 已带 slim_*+deleted_at），学生端 E2E 已真库验证绿；②`_s5CommitPending`（含两处 `_v163SaveSlim` 钩子）是「照抄学生端 v301」，`commitResubmission`→`_s5CommitPending` 已核；③本包 `_v163SaveSlim` 是 insert 成功之后的 fire-and-forget，结构上不可能挡住 insert。故这是**无头 harness 难以驱动代传多步闸**、非 16-05 代码缺陷。

### 前端回落 / 齐全度补软删过滤（filter-audit 结论「补」）
- 学生端 `loadDocumentsFromCloud` 的回落查询（bootstrap 未带 documents 时的兜底，正是 16-00 留给 16-05 查的那条直查 `.from('documents')`）补 `.is('deleted_at', null)` + select 带出 slim 五列。
- 中介端：`_dlLoadDocs`（齐全度，filter-audit 点名）、`_hydrateCloudStudent`（代传材料网格）、`_renderMatGrid`（材料网格）三处直查 `.from('documents')` 均补 `.is('deleted_at', null)`（后两处 16-00 矩阵未列，但同属「软删死图」病类、渲染给中介看的材料卡，一并补齐并在此点名）。
- bootstrap（0208 已带出五列）与回落两路，slim_* 随件挂到文件对象，`_v396SlimLine/_v163SlimLine` 优先读它。

### N1 差集（静态）
- 学生 v395→v396：ids/handlers **removed 0**；functions removed 0、added 5（`_v396SlimLine/_v396SlimFromDb/_v396SlimFmtKB/_v396SaveSlim` + `toKb` 内部函数）。`DIFF-REMOVED-TOTAL 0`。
- 中介 v162→v163：ids/handlers **removed 0**；functions removed 0、added 5（`_v163…` 四个 + `toKb`）。`DIFF-REMOVED-TOTAL 0`。
- 清单在盘：`jobs/JOB-16-05/{stu,par}-v*.static.json`。

### 原因框小字去重
- 学生端新增 `doc.reasonWord`「原因」四语（zh 原/en Reason/ru Причина/fr Raison），原因框输入框上方小字由 `doc.reasonLabel`（与弹窗标题重复）改用它；中介端同法新增 `reasonWord` 四语、改 `_p98T('reasonWord')`。两端各自的内联替换块（学生 12323/12496、中介 8201）不动（无标题重复）。

## 三、业主要做什么
- **看两图**：`~/mh-jobs/shots/16/16-05/` 的 student-v396（有瘦身行）↔ student-v395（无瘦身行）红绿对照（Claude 已看学生 v396 图：合成件「【测试】r16-s… 45.0 KB ✓ 已瘦身 680 KB → 45 KB · 1 页」，无真实数据）。
- **894 KB 那份旧件**（`f189bbdf` studyProof「Notice of applying.pdf」）：查明**不在 16-03 回填的 15 件里**（`jobs/JOB-16-03/backfill-slim.sql` 无此 doc_id）→ 它在库里**没有瘦身记录**，所以文件卡上不会有那一行；**要它显示，得重新传一次**（新件走 set_doc_slim 落库后就有了）。这是设计如此（不按体积猜旧件），不是 bug。
- **中介端代传瘦身行的「刷新后仍在」**建议业主手验一次：中介端登录 → 打开一个代传学生工作台 → 给某个低批次槽传一份 >600 KB 的 PDF → 提交 → 应出现「✓ 已瘦身…」→ 刷新后仍在。（无头 harness 因代传提交多步闸未能自动跑通，标未放行，见结果二。）
- 本包**不部署、不推主仓库**（CC 只 add+commit，push 由业主在原生终端带代理执行）。

## 四、新闸 N1–N6 证据
- **N1**：两端静态差集 removed 0（见上）；升版四件套齐、只加不减。
- **N2**：学生端**真页面真登真选文件真点**，先红（v395 无瘦身行）后绿（v396 刷新后仍有，且库==界面），判据只认屏幕可见文字（slimText）+ 网络/库真值（truth 五列）+ 0 页面报错，未用零件级断言顶替。中介端真页面代传渲染绿 + 0 报错；代传落库无头未复现按 N2 如实写「未放行」并给出等价证据链（wiring 同构、学生端已绿、无数据泄漏）。
- **N3（带身份）**：学生 truth 走 stuA 真 JWT（REST，RLS 生效），页面数与库数对上；清理后 N3 探针以 stuA 身份（`auth.uid()` 回显 `43668ae4-…`）跑 `student_bootstrap`：`r16slim_in_bootstrap=0`、`active_docs=15`（回到 16-03 基线）。中介 is_test 判定走 `truth('partner1', students?…is_test)`。无身份「0 行」未用作证据。
- **N4**：本包不改云函数。
- **N5**：截图为元素级裁剪、画面仅合成件（无邮箱/姓名/护照/申请号）；回执/脚本/日志零密码零 token；push 前跑 secret-scan（见收尾）。
- **N6**：只修不加；未碰瘦身参数/判定/三级回落顺序/闸规则；本包只做 16-05 列的事。

## 五、测试残留清点（带身份探针）
- owner 级全库扫 `%r16-slim%`：本包共写入 **1 条**测试件（学生端 `cbc9f34a` on stuA/is_test；中介端 0 条——代传未落库）。
- `jobs/JOB-16-05/cleanup.sql`（软删=UPDATE deleted_at，非 DELETE；不删 Storage 对象，遵 §0.11④）已跑：`UPDATE 1`、owner 视图 active r16-slim=0；N3 探针 stuA `auth.uid` 回显、bootstrap 里 r16-slim=0、活件 15。**测试件已清干净**。
- Storage 里那一个合成件对象按 16-04 先例留盘（软删数据行即可，删桶对象属「必须停」）。stuA recommendation 槽因这次真上传批次 +1（良性，未近锁）；护照/bank/studyProof/visa 未碰。

## 六、闸3
- 刷新/重登后瘦身行仍在（学生端截图成对 + truth 五列==界面）；CV 槽活件数=带身份探针活行数（0，无 mh10）；测试件已清（探针证明 stuA 活 15/r16-slim 0、中介 0）；N1 两端 removed 0。**学生端过**；**中介端代传 DB 持久化标「未放行」（等业主手验/后续走查），其余（页面 0 报错、渲染、三处软删过滤、reasonWord、N1）过**。

## 附：本包产物（jobs/JOB-16-05/）
lib-pdf.mjs（合成 PDF 生成）、slim-test.mjs（学生红绿+set_doc_slim 成功路+CV 探针）、student-shots.mjs、partner-slim-test.mjs（代传，含 is_test 闸）、cleanup.sql、probe-uid.sql、{stu,par}-v*.static.json（N1 清单）。
