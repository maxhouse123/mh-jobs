JOB-2-13（重做）  结果：全闸通过（学校端 v226 → v227）

## 做了什么（一句白话）
给学校端四处行为加了服务端留痕（都调 `log_school_access`，写进 JOB-2-12 建的 `school_action_log` 表），并加了一页给学校自己看的「我的操作记录」：
1. **打开学生详情** → 记 `view_student`（含来源）
2. **下载/看材料** → 记 `download`（含 doc_id）
3. **导出已录取名单** → 记 `export`（行数）+ `export_list`（学生 id 列表）
4. 学校端「设置」页新增「📋 我的操作记录」入口 → 新页读自己的日志（裁决9：对学校可见）

埋点走 `_v227LogAccess` 是"发了就不管"（fire-and-forget），失败只 console 警告，绝不卡住用户操作。

## 业主要看什么
1. 学校端真导出一次 / 真下载一次材料 / 真打开一个学生 → 管理端「学校行为日志」页（JOB-2-12）会多出对应记录。
2. 学校端「设置」→「我的操作记录」→ 看到自己刚才的操作。
（真 +N 行需你用真实学校账号登录点一次——容器里没学校登录态、log_school_access 内部按 school_members 解析当前用户，psql/桩都出不了真行；本地已用桩证明四个埋点都以正确 action/参数调用了 log_school_access。）

## 十道闸（逐条）
- 闸1 基底 md5：`ce8c2a9a624a4a5cdac7c6b514fc9451`（=v226）✓
- 闸2 锚点唯一：7 处各 =1 ✓
- 闸3 补丁自检：_v227LogAccess/log_school_access rpc、renderAccessLogView、case accessLog、设置入口、四个埋点(view_student/download/export/export_list)、四语 ru/fr 命中 各 =1 ✓
- 闸4 产物 md5：`5b8e957d41fc89140be5d671bbe2df2e`（v227）
- 闸5 语法：2 脚本块 0 报错 ✓
- 闸6 消失检查：847→853（+6，无消失）✓
- 闸7 四语：学校端四语；新增文案（我的操作记录/导出数据/下载材料/查看学生/导出名单/返回设置…）用 _v227T 内联中英俄法四语齐全，零裸键 ✓
- 闸8 真点击：Playwright 桩 —— 触发 openCloudDocPreview/openCloudStudentDetail/exportAdmittedCloud，捕获到 log_school_access 被以 `download / view_student / export / export_list` 四个 action 依次调用（对应"导出+下载+打开"三次点击产生 ≥3 条埋点，导出额外产 export_list）；「我的操作记录」页渲染出记录（下载材料）。反向样本 v226：埋点函数未定义 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：复用 btn-outline/auth-link/card/var()，零新写死颜色/CSS 类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：ce8c2a9a624a4a5cdac7c6b514fc9451 → 5b8e957d41fc89140be5d671bbe2df2e
index.html 改了几处：见下（school-portal/index.html v226→v227）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 导出埋点挂在 `exportAdmittedCloud`（已录取名单云导出，最主的"导出学生数据"路径）；档案 ZIP 导出等其它路径未挂（避免多点重复记；如需全覆盖再补）。
- 下载埋点挂在 `openCloudDocPreview`（候选/学生材料的看件入口，走 watermark-doc）；证件照头像自动加载(_loadCloudCandidateAvatar)不算用户下载，未挂。
- 「我的操作记录」只读 school_action_log（新表，学校可读自己那部分）；旧 EF 取件日志(school_access_log)按 doc 维度、学校无自读策略，未并入学校自审页（管理端日志页可见全部）。
