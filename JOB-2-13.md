JOB-2-13  结果：**暂缓（依赖 JOB-2-12，等你定表名后一起做）**

## 一句白话
这一包是给学校端加埋点：导出/下载/打开学生时，各调一次 `log_school_access(...)` 把行为记到云端。
但它写入的那张表和那个函数，是 **JOB-2-12 建的**——而 JOB-2-12 因为表名 `school_access_log` 和库里一张不相干的 666 行 EF 日志表撞车，**已停下等你确认表名**（见 JOB-2-12 回执）。

## 为什么必须一起做
JOB-2-13 的验收铁条是「真导出一次、真下载一次、真打开一个学生，然后查日志表行数 +3」。表和函数还没建，这条根本没法验。硬把埋点代码加上、却没有可写入的表，等于加一堆会静默失败的调用，违背「零死调用」。

## 一旦 JOB-2-12 表名定了，我会做的（已想清楚）
1. 学校端 v226 → v227：导出学生数据 → `log_school_access('export', null, {字段映射快照, 行数, 筛选})`；导出完成 → `log_school_access('export_list', null, {学生id列表})`；下载材料 → `log_school_access('download', 学生id, {文件名, doc_type})`；打开学生详情 → `log_school_access('view_student', 学生id, {来源})`。
2. 学校端加一页「我的操作记录」（裁决9，读自己的日志——RLS 已设计成学校可读自己那部分）。
3. 闸8 真点击一次导出/下载/打开，查日志表 +3。

## 要你做的
就 JOB-2-12 那一个问题：表名用 `school_action_log`（推荐）还是坚持 `school_access_log`（需先处理现有 EF 表）。定了这两包一起交。

十道闸：未开工，不适用。
基底 md5 / 产物 md5：学校端仍 v226（未产出 v227）。
提交哈希：无代码改动。
偏离或疑点：纯依赖阻塞，非本包问题。
