# JOB-9-01 · 上传体验三修 — 回执

**大白话结论**：上传时那个右下角的小进度卡，做了三处改进。**学生端**三处全修：①以前选完一张图要等约 0.6 秒（图在后台压缩）卡才冒出来，现在**一选完卡立刻就出（显 0%）**，压缩和上传都在卡出现之后偷偷做；②以前上传失败点「重试」会**多冒一张僵尸卡**、旧的红卡赖在那儿，现在**重试就用原来那张卡**、不再多冒；③上传中那个「添加更多」按钮以前只是变灰点不动，现在**加了正式的「禁用」标记**（无障碍/键盘也认），传完自动解除。**中介端**按你上一条的裁决**只修①**（推荐向导那条线，选完立刻出卡）；②③在中介端本来就没这毛病（中介端上传失败根本不给「重试」按钮、提交按钮本来就是真「禁用」），所以据实没动，免得瞎改碰坏上传。

两端版本：**学生 v389 → v390**、**中介 v154 → v155**（旧版都留着能回滚）。**上传失败自动回落老函数那条红线、断点续传那层，一个字节没碰。**

## 版本四件套核对
| 端 | 文件名 | [VER]探针 | index 重定向 | 一致? |
|---|---|---|---|---|
| 学生 | maxhouse_student_portal_v390.html | `[VER] student portal v390` | 4 处均 v390 | ✅ |
| 中介 | maxhouse-partner-portal-v155.html | `[VER] partner portal v155` | 4 处均 v155 | ✅ |

## 断言表（脚本名 / 断言 / 实测值 / 过不过）
### tests/job9-01.mjs（学生，本机 Chrome 无头，真驱动 _v384 + 源码静态序）
| 断言 | 实测值 | 过? |
|---|---|---|
| 静态①：handleUploadCloud 里「出卡」调用在「压缩」调用之前 | add@位255 < compress@位1385 | ✅ |
| 静态②：重试走 `_uploadOne(item)`（复用同卡），函数内不再有 `_v384.add(file)`（无僵尸卡） | hasItemRetry=true, noPerFileAdd=true | ✅ |
| ①：选完→卡出现耗时 < 200ms 且显 0% | **dt=1.5ms**, card=true, 0%=true | ✅ |
| ①：drop() 撤掉占位卡（卡数 1→0） | before=1, after=0 | ✅ |
| ②：失败卡出「重试」按钮，此刻仍 1 张卡 | cntAfterFail=1, hasRetryBtn=true, wasRed=true | ✅ |
| ②：点重试→不新建卡（仍 1 张）、清红、回调触发 | cntAfterRetry=1, redCleared=true, called=1 | ✅ |
| ③：上传中 `aria-disabled="true"` + `tabindex="-1"` + 文字「· 1/2」 | aria=true, tab=-1, pe=none, txt=`Uploading · 1/2` | ✅ |
| ③：传完 aria-disabled/tabindex 均为 null + 文字还原 | aria=null, tab=null, txt=`添加更多` | ✅ |
| **合计** | **8/8** | ✅ |

### tests/job9-01-partner.mjs（中介，只验①+证明②③未被强改）
| 断言 | 实测值 | 过? |
|---|---|---|
| 静态①：_wzStageFiles 里 `_v384.add(_rawF)` 在 `_mhCompressUpload` 之前 | add@位542 < compress@位644 | ✅ |
| 静态②③：中介端仍无 aria-disabled、无 lockCard、`fail(null)`≥3（②③没被强改） | aria-disabled=false, lockCard=false, failNull=3 | ✅ |
| 运行时：window._v384.add 句柄含 progress/done/fail/drop | shape=true | ✅ |
| 运行时①：add() 出卡 < 200ms 且显 0% | dt=0.4ms, card=true, 0%=true | ✅ |
| 运行时：drop() 撤卡（1→0） | before=1, after=0 | ✅ |
| 运行时：_v384.txt(uploading) 四语非空且各异 | zh=上传中/en=Uploading/ru=Загрузка/fr=Envoi | ✅ |
| **合计** | **6/6** | ✅ |

## 三线走查（学生：自填 / 换版重传 / 推荐即传）
- **自填**（handleUploadCloud）：本次三修主战场，全改并测通（上表）。
- **换版重传**（_ru1UploadPending）与**推荐即传**（referral upload）：本次**未改**其卡逻辑，`_v384` 句柄只是**新增** drop()（不动 progress/done/fail），两线原调用照旧可用；语法整文件校验 0 错。
- 中介端**代传材料提交**（_matCommit）、**代传换版**（_s5CommitPending）：未改（预压缩件、无压缩延迟、无重试），只受益于新增 drop()；未引入 aria-disabled/lockCard。

## 「真登真传」闸（<200ms 真机版）
stuA 密码本轮留空 → **真登真传 2MB 的那一次实测留给业主**（本机 Chrome 真驱动 _v384 的 stub 闸已过：出卡同步、<200ms、0% 显示均实测通过）。**真登闸待业主。**

## 语法/回落红线
- 两端整文件 inline `<script>` 语法校验：**学生 6 块（BLOCK 1 为工具把注释里 "cloudflare" 误判，基底 v389 同样报，非本次引入）、中介 1 块 0 错**；我改动所在的主 App 脚本块 0 错。
- `_v384.upload()` 逐层回落函数（TUS→签名URL+XHR→老 .upload()）**整段未碰**；diff 已核对只动了卡句柄 drop()、lockCard aria、handleUploadCloud 线程化、中介向导出卡前置。

## 动过的文件
- `student-portal/maxhouse_student_portal_v390.html`（新增，旧 v389 保留）
- `student-portal/index.html`（重定向 v389→v390）
- `partner-portal/maxhouse-partner-portal-v155.html`（新增，旧 v154 保留）
- `partner-portal/index.html`（重定向 v154→v155）
- `tests/job9-01.mjs`、`tests/job9-01-partner.mjs`（随卡入仓）
- 主仓库已 **commit `6a437ee`**（未推，照规矩留业主原生终端带代理推）。

**中介端②③据实未改说明**：中介端 `_v384` 无 lockCard、三处 `.fail(...)` 全传 null（无重试按钮），提交按钮本身用真 `disabled`——学生端②③修的两个毛病中介端结构上不存在，强改会加大离上传回落红线的风险。已在开工时用问题卡向业主确认，裁决「只修①，②③据实说明」。
