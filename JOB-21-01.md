# JOB-21-01 · 水印缓存覆盖（v36 现场渲染回写）+ 防御触发器 0213 + 常驻渲染方案书

> CC 无人值守。做了什么 / 结果 / 业主要做什么 / 新闸证据。

## 一、根因回顾（承 21-00 探针①②，`jobs/JOB-21-00/probes/p1-reviewer-cache-findings.md`）
- reviewer1 现役 104 派单：命中 96 / 未命中 8（mp4×4 走 signed_video 不入桶 / docx×2 EF 415 / 损坏 PDF×2 gave_up 被 EF 挡在渲染前）。**可渲染、非损坏的未命中 = 0**——prepare 触发器（0193）+ `*/5` 兜底 cron（0195，active）已全覆盖。
- 业主 premise⑤「每次 5s」在真页面路径**未复现**（v45 `_wmFetch` 先直读桶/signed_url 缓存、96/104 命中；JOB-20-01 的 5s 是探针强制走渲染路所致）。
- 因此 21-01 = **(b) 现场渲染回写**（对应业主「现场渲染路只回图不回写缓存」的观察、闭合冷窗口、语义不变）+ **(a) 防御触发器 0213**（补 0193 未覆盖的两条事件，当前实测未触发，纯防御）+ **方案书**。

## 二、做了什么 + 结果

### A. watermark-doc v35→v36（源码改，**未部署**）
- **(b) 现场渲染回写**：新增 `writeBackCache` + `scheduleCacheWriteBack`；在 reviewer 现场渲染成功的图片分支（868 行后）与 PDF 分支（896 行后）调用。
  - 与 prepare（445–455 行）**同对象名 `${uid}/${docId}.${ext}`、同 wm_cache_index 索引行、upsert:true**——**不改缓存键/桶/过期语义**（不触 §0.8⑤ 停机线）。
  - `EdgeRuntime.waitUntil` 放回图之后 → **零客户端等待**；运行时无 waitUntil 则 fire-and-forget 不 await。
  - **限 `callerRole==='reviewer'`**（wm-cache 按 reviewer_uid 分桶，只有审核端两路读它；school/student/partner 无缓存读路，不回写）。
  - **fail-open**：内部全 try/catch，上传/索引失败只 `console.error`，绝不影响已回的图。
- **计时头删 mem 段**（本环境 `Deno.memoryUsage().rss` 恒不可读，JOB-20-01 实测 0/n-a）：删 `rssMB`、`WmCtx.memBefore/memAfter`、4 处赋值、buildWmTiming 的 mem 输出与 ctx 参数；保留 seq/cpu。console 标签 `[wm v35 timing]→[wm v36 timing]`。
- **鉴权/派单/缓存语义一字未改**；JWT 校验保持开（部署不带 `--no-verify-jwt`）。
- 逐行 diff：`git diff supabase/functions/watermark-doc/index.ts`（39 增 14 删，全文见本包 commit）。Node strip-types 解析通过（仅止于 Deno `https:` import，语法有效）；本机无 deno，类型检查待部署管道。

### B. 迁移 0213_wm_prepare_coverage.sql（**只写不执行**，业主 SQL Editor 手贴）
- 补两条 0193 未覆盖、**当前实测未触发**的架构缺口（防御性，只增不删）：
  - **gap2 撤单后就地再派**（un-revoke，UPDATE revoked_at 非空→空）→ prepare。实测无重复(doc,reviewer)行 → 当前再派走新 INSERT（已被 0193 覆盖）。
  - **gap1 补传件就地换 file_path**（UPDATE file_path 变）→ cleanup（清旧缓存，交 cron/回写重灌）。实测 documents `max(version)=1` → 当前无此路径。
- 幂等：全 `CREATE OR REPLACE FUNCTION / TRIGGER`；暗号零外泄（运行时从 `wm_prepare_fanout_fn` regexp 读，同 0195/0197）；headers 带公开 publishable 公钥 + x-mh-hook-secret 以过网关（仅 hook-secret 会被网关 401——探针①旁证：0193 原触发器不带 Authorization，verify_jwt 开时其即时 prepare 多半被网关挡，真覆盖靠 0195 cron）。
- 正本 `supabase/migrations/0213_wm_prepare_coverage.sql` + 同 commit cp 到 `docs/supabase 文件/0213_…sql`（铁律16）。
- **整段 SQL 见本回执末尾附录，供业主手贴**（P4 图纸自包含）。

### C. 常驻渲染方案书 `jobs/JOB-21-01/persistent-renderer-proposal.md`（只调研，不实施不部署）
- A（Cloudflare Worker）vs B（Fly/Railway 常驻容器）逐维度对比 + CC 推荐 + 业主三问。
- **CC 推荐：暂缓迁移**——稳态已快（缓存）+ v36 回写坐实「新件第一次略慢、之后秒开」；若业主坚持根除首次冷启动，选 A（同平台、冷启动最优、密钥不出 CF、可灰度回滚），单开一整轮 + 全套 N4 重验。**不选 B**（引入新供应商 + 常驻服务器长期维护，违「不加新服务」纪律）。

### D. 交付脚本（业主部署时/后跑）
- `jobs/JOB-21-01/deploy-watermark.sh`：部署 v36 + 三探针（OPTIONS 2xx / 无凭据 401 / reviewer1 真登取一份 200 且头**含 seq、不含 mem**）。shell-lint 全绿、`bash -n` 通过、正文无叹号/反引号。
- `jobs/JOB-21-01/wm-timing-probe.mjs`：逐份计时（无 mem 列）+ 复用探针 + **段3 写回断言**（现场渲染→signed_url 同件应 served=cached 命中）；写 `~/mh-jobs/JOB-21-01-timing.md` 并推（`node --check` 通过）。

## 三、新闸证据 N1–N7
- **N1**：本包不动任何端 HTML（只改云函数 + 迁移 + jobs 工装），三端按钮零变动。
- **N2**：premise⑤ 未复现已在 21-00 坐实；v36 回写属「防御 + 对应观察」，绿灯在**部署后**由 deploy 探针 + timing 段3 出（部署前客观变不了绿，如实标）。
- **N3**：探针①②只读（db-run 服务端 / 源码静读）；deploy/timing 脚本 reviewer1 真登、accounts.json 只脚本读、token 不打印。
- **N4**：v36 只改「reviewer 200 渲染路的响应后回写」+ 计时头，**无鉴权路改动**；鉴权路 × 状态码表（OPTIONS 2xx / 无凭据 401 / reviewer own 200 / not_assigned 403 / school·student·partner 不回写行为不变 / prepare·cleanup·signed_url 不变）静态未变，部署后 deploy 脚本三针实测。
- **N5**：secret-scan 于 push 前跑（见 §五）；0213/脚本含的是**公开 publishable 公钥**（非密钥，0197 已在库），无 service-role/密码/JWT。
- **N6**：只做 QUEUE-21 21-01 写明项，未顺手改他处。
- **N7**：本包无「给人看的新 UI 提示」，不涉。

## 四、闸3
- 迁移幂等：全 `CREATE OR REPLACE` **构造性幂等**；本机仅 libpq 客户端、无 postgres server → 无法本机跑两遍，**待 Claude 出业主 SQL 卡前在本地库跑两遍**（QUEUE-18 §0.5 既定流程）。
- 函数改动 N4 分支表（静态见上 + 部署后探针）。
- v35→v36 逐行 diff 见 commit。
- (a) 每类根因「补了什么、怎么验」：gap2 补 un-revoke→prepare（验：撤单后 UPDATE 复用行 → 桶应重新出现该件对象）；gap1 补 file_path 变→cleanup（验：就地换件后旧对象被删、cron/回写重灌）。

## 五、业主要做什么
1. （部署，可择期）挂代理 + `supabase login` 后：`bash /Volumes/Dev/MAXHOUSE/jobs/JOB-21-01/deploy-watermark.sh`（自带三探针，全过打印 OK）。
2. （部署后）`export SUPABASE_ANON_KEY=<公开anon> && node /Volumes/Dev/MAXHOUSE/jobs/JOB-21-01/wm-timing-probe.mjs`（出计时表 + 写回断言，自推 mh-jobs）。
3. （手贴 0213，可择期）在 Supabase SQL Editor 贴附录整段 SQL（Claude 会先在本地库测幂等再正式出卡）。
- 主仓库 CC 已 commit（index.ts + 0213 + docs 副本 + jobs/JOB-21-01/*），**未推**（等收官业主卡）。

## 交接
- **下一包 = 21-02**（中介端 v169：卡片 15s/20s + 悬停暂停 + 390&1280 不盖标题条 + 向导守卫收紧；名单块冒号已符合 → 记未复现不改）。

---

## 附录 · 0213_wm_prepare_coverage.sql 全文（业主 SQL Editor 手贴）

```sql
-- 0213 · JOB-21-01 水印缓存「预渲染事件覆盖」防御补丁（全新增型；不本机执行，业主在 SQL Editor 手贴）
-- 幂等：全部 CREATE OR REPLACE；破坏性：无；回滚见文件头注。
begin;

create or replace function public.wm_prepare_reassign_fn()
 returns trigger language plpgsql security definer set search_path to ''
as $function$
declare sec text;
begin
  sec := (regexp_match(pg_get_functiondef('public.wm_prepare_fanout_fn'::regproc), $q$'x-mh-hook-secret','([^']+)'$q$))[1];
  if sec is null then return new; end if;
  perform net.http_post(
    url := 'https://uexgzwfambanvhdhxome.supabase.co/functions/v1/watermark-doc',
    headers := jsonb_build_object(
      'Content-Type','application/json',
      'Authorization','Bearer sb_publishable_7kx6JBztyd5nzSqzSgW4MA_HavI0Wnr',
      'x-mh-hook-secret', sec),
    body := jsonb_build_object('mode','prepare','doc_id',new.doc_id,'reviewer_uid',new.reviewer_uid),
    timeout_milliseconds := 5000
  );
  return new;
end
$function$;

create or replace function public.wm_cleanup_fileswap_fn()
 returns trigger language plpgsql security definer set search_path to ''
as $function$
declare sec text;
begin
  sec := (regexp_match(pg_get_functiondef('public.wm_prepare_fanout_fn'::regproc), $q$'x-mh-hook-secret','([^']+)'$q$))[1];
  if sec is null then return new; end if;
  perform net.http_post(
    url := 'https://uexgzwfambanvhdhxome.supabase.co/functions/v1/watermark-doc',
    headers := jsonb_build_object(
      'Content-Type','application/json',
      'Authorization','Bearer sb_publishable_7kx6JBztyd5nzSqzSgW4MA_HavI0Wnr',
      'x-mh-hook-secret', sec),
    body := jsonb_build_object('mode','cleanup','doc_id',new.doc_id),
    timeout_milliseconds := 5000
  );
  return new;
end
$function$;

create or replace trigger wm_prepare_on_reassign
  after update on public.reviewer_assignments
  for each row
  when (old.revoked_at is not null and new.revoked_at is null)
  execute function public.wm_prepare_reassign_fn();

create or replace trigger wm_cleanup_on_fileswap
  after update on public.documents
  for each row
  when (old.file_path is distinct from new.file_path)
  execute function public.wm_cleanup_fileswap_fn();

select 'triggers' as k, tgname, tgrelid::regclass::text as on_table
  from pg_trigger
 where tgname in ('wm_prepare_on_reassign','wm_cleanup_on_fileswap')
 order by tgname;

commit;
```
