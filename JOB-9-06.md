# JOB-9-06 · 水印预渲染缓存施工 — 回执【EF/前端源码已改·EF未部署；迁移已上生产】

**大白话结论**：审核员每次点「查看」现在要冷启动云函数、现场下原件+打水印，慢（2.7–3.7 秒）。这一包把水印件**提前在派单时就渲染好、存进一个私有桶**，审核端查看时**先问桶、命中就直接秒下**已渲染好的文件；没命中就照旧走原来的实时渲染（慢但保底）。做了三件事：①**私有桶 `wm-cache` + 三个触发器**（派单就预渲染、撤单/材料软删就清桶）——**已上生产库**（安全：云函数新分支还没部署，触发器打过去会被现役函数判 401、静默无害）；②**云函数 `watermark-doc` 加三个新模式**（预渲染 / 清理 / 签发桶链接），**原来的「查看」分支一个字节没动**——**只改源码、没部署**；③**审核端 reviewer v33→v34**：查看先问桶、命中秒下、没命中/出错一律回落原实时渲染（原逻辑逐字保留）。桶只是加速层，云函数永远兜底，功能零风险。

**一处技术偏差（要业主知悉）**：设计原写「加一个 SQL 的 RPC `wm_cache_signed_url` 返回签名 URL」——**但 SQL 无法签发 storage 签名 URL**（那是 storage API 的能力，不是数据库能做的）。设计 §4 本身也列了「EF 返回」作为备选，故改由**云函数新增 `signed_url` 模式**（service_role 代签 300 秒短链）实现读取口。**桶依然对 authenticated 完全不直读**（守设计「不给 authenticated 直读」），签名由云函数用 service_role 代做。本迁移**不含该 RPC**。

## 一、迁移 0193（已应用生产 maxhouse_main）
> ⚠️ 编号说明：真账本是 `supabase/migrations/`（已到 0190），故本迁移用 **0193**（本轮 0191=get_my_offers / 0192=invite 已归位，见 9-02/9-05 更正）。可执行原件 `jobs/JOB-9-06/0193_wm_cache.sql`；归档 `supabase/migrations/0193_wm_cache.sql`（**密钥打码**）。
- ① 私有桶 `wm-cache`（public=false）。**存储策略**：现有 storage.objects 的 SELECT 策略**全部按 bucket_id 限定**（已核 14 条，无一命中 wm-cache）→ authenticated **默认拒读**（守「不给 authenticated 直读」，无需新增拒绝策略）；service_role 天然绕 RLS 可写/删/签。
- ② `reviewer_assignments` AFTER INSERT → `wm_prepare_fanout_fn`（打 `watermark-doc {mode:'prepare'}`）。
- ③ `reviewer_assignments` AFTER UPDATE（`revoked_at` 空→非空）→ `wm_cleanup_assignment_fn`（`{mode:'cleanup',doc_id,reviewer_uid}` 清该审核员这份）。
- ④ `documents` AFTER UPDATE（`deleted_at` 空→非空）→ `wm_cleanup_doc_fn`（`{mode:'cleanup',doc_id}` 清所有审核员这份）。
- 三个 fanout 函数照抄 `notify_email_fanout_fn`（`net.http_post` + `x-mh-hook-secret`）。

| 跑前跑后自检 | 跑前 | 跑后 | 过? |
|---|---|---|---|
| wm-cache 桶 | 0 | **1** | ✅ +1 |
| wm_* 函数（prepare/cleanup_assignment/cleanup_doc） | 0 | **3** | ✅ +3 |
| reviewer_assignments+documents 触发器 | 5 | **8** | ✅ +3 |
执行日志：`BEGIN / INSERT 0 1 / CREATE FUNCTION×3 / CREATE TRIGGER×3 / COMMIT / DB-RUN OK`。

## 二、EF watermark-doc（源码已改·未部署）
- **函数名 / 路径**：`watermark-doc` · `supabase/functions/watermark-doc/index.ts`
- **加三模式**（`handle` 顶部 `req.clone()` 无损窥探 `body.mode` 路由，**原 `POST {doc_id}` 分支逐字零改动**）：
  - `prepare`（暗号认证）：校验 `reviewer_assignments(doc_id,reviewer_uid)` → 复用现有下原件+打水印 → **upsert 到 `wm-cache/<reviewer_uid>/<doc_id>.<ext>`** → 审计 `outcome=prepared` → `{ok,cached}`。
  - `cleanup`（暗号认证）：带 reviewer_uid 清一份 / 不带清所有审核员目录下这份（list 后 remove，含 pdf/jpg/png 三种 ext）。
  - `signed_url`（审核员 JWT 认证）：校验归属 → service_role 找该审核员目录下该 doc 的对象 → 签 300s 签名 URL → `{ok,url}`；未命中/失败 → `{miss:true}`（前端回落整渲染）。
- **零回归**：原 reviewer/school/student/partner 「查看」路径不动（clone-peek 只对带 `mode` 的请求分流）。

## 三、reviewer v33 → v34（读侧）
- `_wmFetch` 改为**先问桶**（`signed_url` 模式）→ 命中直接下载已渲染件（[v34] wm cache HIT）→ 未命中/签名失败/桶下载失败/任何异常 **一律回落 `_wmFetchEF`**（= v33 原 `_wmFetch` 主体逐字保留的整渲染路径）。
- `_wmPrefetch` / `_wmPrefetchQueue` 经 `_wmFetch` **自动继承**「先桶后 EF」。内存缓存层 `_wmCache` 不变（命中桶或走 EF 都进同一缓存）。

## 四、断言表
| 闸 | 断言 | 实测 | 过? |
|---|---|---|---|
| 迁移自检 | 桶+1 / 函数+3 / 触发器+3 | 见上表 | ✅ |
| tests/job9-06.mjs 闸6 | openPreview/_wmFetch/_wmTryCache/_wmFetchEF/_wmPrefetch/_wmPrefetchQueue 均在 | 全 true | ✅ |
| tests/job9-06.mjs ① | 桶命中 → signed_url→bucket_dl, **不走** ef_render, 得 blob | `signed_url,bucket_dl` | ✅ |
| tests/job9-06.mjs ② | 未命中(miss) → 回落 ef_render, 得 blob | `signed_url,ef_render` | ✅ |
| tests/job9-06.mjs ③ | 桶下载 404 → 回落 ef_render, 得 blob | `signed_url,bucket_dl,ef_render` | ✅ |
| reviewer v34 版本四件套 | 文件名/[VER]/index(3处)同版 | 一致 | ✅ |
| reviewer v34 语法 | inline script 语法 | 1 块 0 错 | ✅ |
| EF 结构 | 花括号/圆括号平衡 + 原锚点在(6) + 新模式函数(2) | 269/269、626/626 | ✅ |
| deno check | 本机无 deno + 不挂代理无法装 → **未跑**；结构自检通过，**部署前请业主机 `deno check`** | ⚠️ 待部署前 | — |

## 五、必须停 / 待业主
- **`watermark-doc` 未部署**（必须停）。部署命令另出卡（Supabase 网页 Functions 编辑器粘贴 `supabase/functions/watermark-doc/index.ts` → Deploy；env 需有 `MH_HOOK_SECRET`）。**部署前审核端行为 = 现状**（触发器打到未部署分支被现役 EF 判 401，静默；`signed_url` 也不通 → reviewer 一律走原整渲染，零变化）。
- **rollback**：`jobs/JOB-9-06/rollback.sql`（归档 `supabase/migrations/0193_wm_cache_rollback.sql`）——drop 3 触发器+3 函数，含 `drop function` 故**业主手跑**；删桶需先清空对象（注释里给了步骤）。

## 六、边界 / 双盲
- 原件桶 `student-documents` **零改动**；原件绝不出网。
- 双盲不破：水印件本就是「给该审核员的那一份」（水印含 RV-<hash(reviewer_uid)>），按审核员分目录存，`signed_url` 只签调用审核员自己目录下的对象。
- 桶=加速层，EF 永远兜底；桶空/未命中→功能照常（只是慢）。

## backlog
- 兜底 cron：pg_cron 每 5 分钟扫「有 assignment 但桶里没对象」补渲染 + 清孤儿缓存（本版只上触发器即时清，cron 记 backlog）。
- 设计原 RPC `wm_cache_signed_url`：已由 EF `signed_url` 模式替代（SQL 不能签 storage URL），如业主坚持 SQL 侧，需在库内引 storage JWT 密钥手工签，风险高、不建议。

## 动过的文件
- `supabase/functions/watermark-doc/index.ts`（EF，加 3 模式，未部署）
- `reviewer-portal/maxhouse-reviewer-portal-v34.html`（新，旧 v33 保留）+ `reviewer-portal/index.html`
- `supabase/migrations/0193_wm_cache.sql`（打码归档）+ `_rollback.sql`
- `jobs/JOB-9-06/`（可执行迁移原件 + rollback + 探针，本机未跟踪）
- `tests/job9-06.mjs`（随卡入仓）
- 生产库 maxhouse_main：wm-cache 桶 + 3 函数 + 3 触发器（已应用）
- 已 commit `1ed46de`（未推）。
