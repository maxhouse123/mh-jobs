# JOB-18-02 · 审核端 v43：查看时预取让路 + 找慢件的原因 + 超高学生卡 + 未知类型名

CC 无人值守。本包只改审核端一个文件（v42→v43），零共用查看器改动、零 RPC/库改动、零 UI 骨架改动、零写库、零上传、零部署、未推主仓库。

## 一、做了什么（reviewer-portal v42 → v43，md5 `183215bc92cddf0a6b23add256bd43c3`）

**① 查看让路（第 5 条）**
- `_wmFetch(docId, signal)` 把中止信号透传给**两条可中止的 fetch 路**：`_wmTryCache`（signed_url + 桶下载）与 `_wmFetchEF`（整渲染 EF）——两处的 `fetch()` 都加 `signal`，三处 catch 改为「AbortError 直穿、其它异常照旧回落」。
- **中止不了的那一路**：`_wmTryDirect` 走 supabase-js `storage.from('wm-cache').download()`，**无 AbortSignal 入口**（回执写明）。故查看期间的做法是：泵 `_wmPrefetchPaused=true` 即**并行降 0（不再新发直读）**，已在途的直读听其自然跑完。
- 预取泵 `_v42Pump` 每路配一个 `AbortController`，存 `_v43PfCtrls[docId]`；完成时清除。
- `openPreview` 点「查看」→ 先 `_v43LetThroughForView(m.id)`：**中止除本件外全部在途预取**（可中止两路），把被中止的 docId **放回预取队列 `_v42Q` 最前（去重）**；本件复用在途 promise（不发第二次）；本件取完 `_wmPrefetchPaused=false` → 泵恢复、从队首**重取**被中止的件。中止请求不计失败（`err.name==='AbortError'` 直接 return）。控制台打 `[v43] view let-through: aborted N …`。

**② 找慢件（第 5 条，只加记录不改取件）**
- 每份查看在取件前打点，取完打一行 `[v43] wm timing <id8> ttfb=…ms total=…ms bytes=…`（total 用 `performance.now()` 差值，可靠；ttfb 尽力从 watermark-doc 的 resource-timing 取，取不到写 `-1`）。
- **18-00 ③ 慢件结论**（喂十九轮）：reviewer1 真登逐个直读队列 30 份，**15 份 ≥5 s**（中位 5.25 s，range 1.2–9.4 s），经 watermark-doc EF；**与文件大小、是否命中缓存无关**（几乎全 `wm_status=cached`、body 很小仍慢），415 不支持的行反而快返 → **成本在服务端渲染 / EF 冷启动，不是带宽**；响应头多为「CORS 不给」。→ **第十九轮建议**：查 watermark-doc 冷/热渲染路径与 EF 冷启动（预热 / 常驻 / 提前签发直链）。

**③ 超高学生卡（第 6 条）**
- IntersectionObserver 触发条件由「露出比 `intersectionRatio >= 0.34`」改为「`>= 0.34` **或** 露出部分高度 `intersectionRect.height >= innerHeight/3`」；阈值数组由 `[0,0.34,1]` 加密为 `[0,0.05,0.1,0.15,0.2,0.25,0.3,0.34,0.5,0.75,1]`，让高过 3 屏的卡在露出高度增长时也能触发判定。enqueue 的控制台文案未改（仍 `[v42] prefetch enqueue …(front)`）。

**④ 未知类型名（第 7 条）**
- 新增兜底 `_v43SafeType(raw)`：有原值显示原值（如 `studyProof` / `studyPlan`），原值也空（`null`/空串）才「未知类型 / Unknown type」。
- **9 处**类型兜底（卡标题、预览标题、退回弹窗、操作记录 approve/reject/upload-sample 等）统一由 `DOC_TYPES[m.docType] || { zh: m.docType }` 改为 `… || { zh: _v43SafeType(m.docType) }`（`studentDocRow` 的长形式同改，保留 icon/cls/minutes）。零残留裸 `{ zh: m.docType }`。

**版本四件套**：文件名 v43 / 头注 v43(2026-09-22) / `[VER] reviewer portal v43` / `reviewer-portal/index.html` 三处重定向目标 → v43；`docs/changelog/reviewer-portal-versions.md` 补 v43 一行。

## 二、闸3（真页面红→绿，reviewer1 真登 N3；证据 `jobs/JOB-18-02/gate-reviewer-result.json` + `~/mh-verify/r18-02-gate.log`）

真登陆 reviewer1、真队列（只在网络层路由 `wm-cache` 直读 → 404 强制走可中止 EF、`watermark-doc` EF → 延时 8 s 后回合成件；`/auth/`、`/rest/` 一律放行不桩）。**零写库、零上传。**

| 闸 | 旧版 v42（红） | 新版 v43（绿） | 判 |
|---|---|---|---|
| ① 让路 | aborted=0，点击后 3 个在途 EF 照常跑完 | **aborted=3**（≤400 ms 内），`[v43] view let-through` 控制台 ×1，取完后 **6 个新 EF** 重发被放回队首的件 | 过 |
| ② 超高卡（视口 1100×300） | 卡高 914 px、露出比 0.328（<0.34）→ **enq=0** | 同一张卡 **enq=1（front）**，由「露出高度≥1/3屏高」分支触发 | 过 |
| ③ 未知类型 | 空 docType 卡标题 `""`、退回弹窗「退回材料：」后为空 | 卡标题与退回弹窗均显示「未知类型 / Unknown type」 | 过 |
| ④ 计时行（仅 v43） | — | `[v43] wm timing … ttfb=-1ms total=7657ms bytes=34013` ×1 | 过 |
| ⑤ kit2 尺子（仅 v43 真页面） | — | PDF 尺子 `failed=[]`（J15 本轮亦通过，未动用例外）；图片尺子 28/28；`extras:false` J29n 确认组件条无下载/新标签 | 过 |
| ⑥ N1 差集 | — | 0 个 onclick handler 被删、0 个活动态 handler 被删；新增 `_v43LetThroughForView / _v43SafeType / _v43PfCtrls`（v43 独有、v42 皆无）；只增不减 | 过 |

`gate-reviewer-result.json` → `pass: true`（六闸全过）。

## 三、P3 随卡回归（入仓 `tests/static-check-v43-reviewer.mjs`，29 条全绿）
主脚本 parse 干净 + 版本四件套 + `_wmFetch/_wmViaSigned/_wmTryCache/_wmFetchEF` 带 signal + 三路 fetch 挂 signal + AbortError 直穿(5 处) + 预取泵 AbortController(`_v43PfCtrls`) + 泵传 signal + 中止不计失败 + `_v43LetThroughForView`(除本件外中止 + 放回队首去重) + openPreview 调用(本件) + 直读桶无 signal(已知限制) + `[v43] wm timing` 每份直读 + 计时 t0 在取件前 + 露出高度≥1/3屏高 + tall 条件 + 阈值加密 + `_v43SafeType`(9 处统一、零残留裸兜底) + 共用查看器引用未变(v2-1-1) + 并行仍 ≤3。

## 四、结果 / 业主要做什么
- 本包**只改前端一个文件、本地 commit、未推主仓库、未部署**；无迁移、无写库（gate 全在网络层桩，零真库写；只读 reviewer1 真登真队列）。
- **业主本包无需做任何事**；收官（18-07）后由业主用卡1140 统一推送。
- 业主验收项（进 SUMMARY「亲眼看」清单）：审核端 reviewer1 登录，往下翻到较靠后的一组再点「查看」——速度不被前面在途预取拖住；控制台每份查看有一行 `[v43] wm timing`；未知类型（studyProof/空类型）材料标题显示原值或「未知类型 / Unknown type」，不再是空标题 / 以「·」开头。

## 五、基线与不动项
- 共用查看器 `assets/mh-viewer-v2-1-1.js` md5 `ee351485…`（=基线，未动）；`jobs/JOB-17-01/kit2/` 未动；学生端 v397、学校端 v234 未动（md5 = 基线）；旧版 reviewer v42 md5 `68d136ee…`（=基线，留盘可回滚）。

## 六、新闸证据
- **N1**：v42→v43 静/动态差集只增不减、零新增死按钮（闸⑥ + n1-diff.json）。
- **N2**：①②③ 真页面真点先红（v42）后绿（v43），数字在案；④⑤⑥ 为 v43 新增/回归项。
- **N3**：reviewer1 真登真队列，路由只碰 `wm-cache`/`watermark-doc`，`/auth`·`/rest` 放行；无「0 行/无行」当证据。
- **N4/N5**：本包无云函数改动；secret-scan 对 `jobs/JOB-18-02` + tests + `~/mh-jobs/shots/18/18-02` = CLEAN（合成 PDF/图片，真实学生码覆写为 STU-SYNTHETIC 并模糊，无 JWT/service_role/密码/邮箱）。
- **N6**：只做 §18-02 四条；18-00 查到的慢件根因不在本包改（只加记录，结论归十九轮建议）。
- **N7**：本包无「给人看的软提醒」文案/颜色/停留改动（让路提速、计时日志、预取触发、未知类型名均非提示件），**N7 不适用**；G/H 的 N7 三件量在 18-03。

## 七、公开截图（`~/mh-jobs/shots/18/18-02/` + INDEX.md；合成件、真实学生码 mask，secret-scan CLEAN）
- `v42-unknown-type-title-RED.png`：旧版空类型卡标题为空 / 退回弹窗名为空。
- `v43-unknown-type-title-GREEN.png`：新版显示「未知类型 / Unknown type」。

---
**下一包 = 18-03**（中介端 v166：「两个名字」进琥珀名单块 + 代传出异常不转圈；N7 全套在此包量）。
