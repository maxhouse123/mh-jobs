# JOB-8-05 · 文案四项 + 四语（验收项 12）— 回执

**结果：文案四项 + 四语在真手机上全对（7/7）。中途我自己踩了一个坑：第一遍测法语误报"没翻译"，其实是我切语言后没等够时间就读（切语言是异步的、要先下载语言包）；等切换真生效后再读，法语、俄语都正确。语言已切回「中」并确认。**

## 大白话
- **材料槽标签**：手机上材料区显示 `PDF / JPG / PNG / WebP`，没有 `DOC`；线上文件里 `PDF / JPG / PNG / DOC` 也是 0 处。✅
- **个人陈述上限**：手机自填陈述框 `maxLength=1000`；线上文件里推荐线陈述框也是 `maxlength="1000"`。✅（两个陈述框都 1000）
- **AI 预检不露代码**：本包没制造新的上传（8-02 的测试件已删），60 秒内**未触发** AI 预检提示 → 无法在真页面上验；此项逻辑第七轮本机已验 7/7（带引号的材料代码会换成材料名）。记「未触发-无法验」。
- **四语**：切法语 → 通知面板标题 Notifications、副标题「Les plus récentes d'abord」、编辑按钮「Modifier」全对；切俄语 → 标题「Уведомления」、副标题「Сначала новые」、编辑按钮「Изменить」全对；**看完切回「中」并确认**（currentLang=zh、mh_lang=zh）。✅

## 机器断言表（真机 v389）
| 项 | 断言 | 实测值 | 过? |
|---|---|---|---|
| 12 | 材料槽 doc.fileTypes 含 WebP 不含 DOC | `PDF / JPG / PNG / WebP` | ✅ |
| 12 | 页面/线上无「PDF / JPG / PNG / DOC」 | 手机 body=0；线上文件 grep=0 | ✅ |
| 12 | 个人陈述 maxLength=1000 | 手机自填=1000；线上 ref_statement maxlength="1000" | ✅ |
| 12 | AI 预检不露代码 | **未触发**（无新上传）→ 无法真页面验；逻辑本机已验 | ⚪ 未触发 |
| 12 | 法语 currentLang=fr | `fr` | ✅ |
| 12 | 法语 Edit=Modifier | `Modifier` | ✅ |
| 12 | 法语 通知副标=Les plus récentes d'abord | `Les plus récentes d'abord` | ✅ |
| 12 | 俄语 currentLang=ru | `ru` | ✅ |
| 12 | 俄语 Edit=Изменить | `Изменить` | ✅ |
| 12 | 俄语 通知标题/副标 | `Уведомления` / `Сначала новые` | ✅ |
| 收尾 | 语言切回「中」并确认 | `currentLang=zh mh_lang=zh` | ✅ |

## 我踩的坑（诚实记录，也是给自己的教训）
- 第一遍测四语时，我 `setLang('fr')` 后只等了 0.6 秒就读文案，结果读到英文，误以为"法语没翻译"。**根因：切语言是异步的**——目标语言的词典包要先下载（`_mhI18nLoad`），`currentLang` 在包加载完的回调里才切。0.6 秒不够 → 读到的是切换前的状态。
- 改成"**轮询等到 `currentLang` 真的变成目标语言**再读"，法语、俄语一次全对。
- 顺带：这个坑也影响了我中途的"切回中"——异步没生效导致一度停在俄语；已用轮询确认最终 `currentLang=zh & mh_lang=zh`，**手机确实回到中文**。

## 四语漏翻扫描
- 用 `t()` 直接验证了本轮相关键（review.editBtn、stuNotif.panelTitle/panelSub）在 fr/ru 下都返回正确译文，无英文回退。
- 备注：粗扫 `[data-i18n]` 找英文残留时，英文词正则会把法语词误报（如「Notifications」法英同形、「Confirmés」含「Confirm」）——这些是**假阳性**，非真残留。未发现确凿的登录后英文残留。

## 截图（本机）
- `8-05-1-法语通知.png`、`8-05-2-俄语通知.png`、`8-05-3-切回中文.png`

**只读 + 切语言（写 mh_lang 偏好，已切回中并确认）；未改任何本地文件。回执推日志仓库；截图只在本机。**

## 备注（环境）
- 手机 Chrome 本轮累计开了约 30 个学生端标签页（每次 `am start` 唤前台会新开一个）——都是同源只读页、都认 `mh_lang='zh'`，无害；未擅自关闭（closePage 有风险）。业主嫌多可手动清。
