# JOB-7-07 · 驳回理由输入框加英文提示 — 回执

**结果：做完，管理端 v208、审核端 v33。走查 6/6 过。**

## 大白话
你定了「管理员/审核员写驳回理由一律用英文」（学生端不翻译，法语学生会收到看不懂的中文）。这条规矩现在**落在界面上**：
1. **每个驳回/二审反馈输入框下加一行灰字**：「请用英文填写 —— 学生端不翻译，非英文理由学生看不懂」（中文界面显中文提示，英文界面显英文提示）。
2. **打字时若检测到中日韩字符** → 输入框边框变**琥珀色** + 那行提示**变粗**，提醒一下。**不阻止提交**，只提醒（你要真想写中文也拦不住，但会很显眼）。

## 改了哪些框
- **管理端 v208**：`verdictReason`（二审驳回材料的反馈框——它写进 `admin_reason`，经「二审驳回材料」通知**送到学生**）。加提示 + CJK 检测。
- **审核端 v33**：两个退回框都加——
  - `rejectDetail`（单件退回·详细说明·学生可见）
  - `rejectAllReason`（一键全部退回·共享理由）
  - 顺手把这两处原有的「⚠️ 请中英双语」旧提示**改成「请用英文」**（旧提示与你的新政策矛盾，一并对齐），占位符也去掉中文示例、只留英文示例。

共用一个 `_v7CjkWarn()` 检测函数（每端定义一次）+ `_v7EnHintHtml()` 灰字生成。

## 十道闸
- **闸 3（静态）**：管理端 CJK 检测函数 `_v7CjkWarn` 定义 1 处、`verdictReason` 加 oninput 钩子 1 处、灰字提示 1 处；审核端 `_v7CjkWarn` 定义 1 处、两个退回框各挂 oninput（2 处）+ 各加灰字提示；用户可见的「中英双语」旧字样 0 处（仅版本探针注释里为记录变更保留一处描述性文字，非界面）。两端 JS 解析干净。
- **闸 8/走查** `tests/job7-07.mjs` **6/6**：两端各验——灰字英文提示存在 ✅；输入英文**不**触发琥珀边框 ✅；输入中文**触发**琥珀边框（rgb(240,160,32)）+ 提示加粗(800) ✅。

## 边界
- 只碰**学生可见**的驳回/二审反馈框。管理端「拒绝学校入驻/合伙人申请」的理由框（`schoolRejectReason`/`partnerRejectReason`）面向的是学校/合伙人、不是学生，**未动**（你的政策明确针对「学生端不翻译」）。如需一并加，说一声我补。

## 动过的文件
- 改：`admin-portal/maxhouse-admin-portal-v208.html` + `admin-portal/index.html`；`reviewer-portal/maxhouse-reviewer-portal-v33.html` + `reviewer-portal/index.html`
- 新增：`tests/job7-07.mjs`

**版本**：管理端 v207→v208、审核端 v32→v33。**主仓库未推。本回执已推日志仓库。**
