# JOB-12-02 · 预渲染件瘦身 + 写索引（watermark-doc 源码，**不部署**）

结果：**已改源码（未部署）** —— prepare 瘦身到 1600px/JPEG75 + 写索引；cleanup 删索引；view/signed_url 零改；deno 本机没装做结构自检

## 大白话（业主看这段）
审核端"命中了也不快"的另一半原因：**预渲染件太大**（0.3–1.5 MB），下载本身就慢。这轮把**预渲染（prepare）**那条路的出图尺寸从长边 2000px/JPEG80 降到 **1600px/JPEG75**，体积能再减一截；**审核员点开现场渲染（view）那条路一点没动**（还是 2000/80，肉眼质量不降）。
另外，prepare 渲染成功后会往一张索引表 `wm_cache_index` 记一行（这份是谁的、溯源码是啥），供 12-01 的直读审计函数取用；撤单/软删时（cleanup）连这行索引一起删。

**没有部署**——现在线上 EF 还是旧的（预渲染件还是大的）。要等你用下面的部署卡部署，之后新渲染的才会变小。旧的大件由 12-04 的清桶重灌函数（你部署后一句话触发）按新参数重灌。

## 断言表（结构自检，deno 未装）
| 断言 | 期望 | 实测 | 过 |
|---|---|---|---|
| prepare 调 watermarkImage(...,1600,75) | 1 | 1 | ✅ |
| view 仍 3 参调用(用默认 2000/80) | 1 | 1 | ✅ |
| MAX_SIDE 常量仍 2000（view 不变） | 1 | 1 | ✅ |
| encodeJPEG(quality)（参数化） | 1 | 1 | ✅ |
| encodeJPEG(80) 残留 | 0 | 0 | ✅ |
| prepare upsert wm_cache_index | 1 | 1 | ✅ |
| cleanup delete wm_cache_index | 2（撤单+软删两分支） | 2 | ✅ |
| signed_url 分支零改（11-02 served_cached 仍在） | 是 | served_cached 计数 2（含注释），未新增 | ✅ |
| 花括号平衡 | 相等 | 279/279 | ✅ |
| 圆括号平衡 | 相等 | 667/667 | ✅ |

## 改法说明
- `watermarkImage` 加两个**可选参数** `maxSide=MAX_SIDE, quality=80`：prepare 传 `(1600, 75)` 瘦身；view 不传 → 沿用默认 `(2000, 80)` **零变**（同一函数不同调用，互不影响，渲染算法本身没动）。
- prepare 成功 upsert `wm_cache_index`（doc_id/reviewer_uid/rv_code/object_name/rendered_at），**失败不阻断渲染**（索引缺行时 log_wm_view 自会兜底算 rv）。
- cleanup 撤单 → 删该审核员这行；软删 → 删所有审核员这份的索引行。

## 📋 部署卡（业主执行；EF 部署 = 上线）
```
cd /Volumes/Dev/MAXHOUSE
# 需已 supabase login 且 link 到 maxhouse_main(uexgzwfambanvhdhxome); JWT 校验保持开
supabase functions deploy watermark-doc --project-ref uexgzwfambanvhdhxome
# 若别处装了 deno, 部署前可先: deno check supabase/functions/watermark-doc/index.ts
```
部署后：**新**预渲染件即为 1600/75；**旧**大件由 12-04 的 `wm_cache_purge` 重灌（你部署后叫我一句话触发）。

## 边界
- 只动 prepare / cleanup 两段；view / signed_url / 渲染算法本身零改动；**未部署**。

## 动过的文件
- 改 supabase/functions/watermark-doc/index.ts（watermarkImage 参数化 + prepare 1600/75 + prepare 写索引 + cleanup 删索引；**未部署**）
- 新增 jobs/JOB-12-02/report.md
