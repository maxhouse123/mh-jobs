JOB-5-02  结果：全闸通过（管理端 v201 → v202）

## 做了什么（一句白话）
手机宽度（390px）下管理端「学生」页横向能拖出去一截（多出 71px）。根因找到了：学生页的搜索行是个横向 flex，里面搜索框最小宽 320px + 右边「显示 N/M 人」计数（不换行 75px），加起来 320+间距+75=403 > 390，把页面撑破。修法就一处：给那个搜索行加 `flex-wrap:wrap`，让计数在窄屏时换到下一行。其余五页（驾驶舱/学校/合作伙伴/材料派单/付款流水）实测本来就不溢出，一个字没动。

## 探到的根因（写进回执）
- 溢出页：**只有「学生」页**（v201 实测 scrollWidth=461，其余五页=390）。
- 最外层撑宽元素：`<span class="list-search-count">`（`white-space:nowrap`，right=461）——它前面的 `.list-search-wrap` 有 `min-width:320px` 硬底，在 flex 行里不能缩，把计数顶到 461。
- 学校/合作伙伴页也用同一个搜索框，但外层是普通 `<div>`（非 flex），计数自然换行，所以不溢出——差别就在学生页那层 flex 容器。

## 业主要看什么
手机上（或 F12 切 390 宽）开管理端「学生」页，横向拖不动了。

## 十道闸（逐条）
- 闸1 基底 md5：`f22e0561854b490c558ce7704f217d48`（=v201）✓
- 闸2 锚点唯一：3 处各 =1 ✓
- 闸3 补丁自检：学生搜索行加 flex-wrap =1、旧版归 0、[VER] v202 ✓；**六页 390px 实测 scrollWidth 全 =390**；反向样本 v201 学生页=461（>390）→ 有分辨力 ✓
- 闸4 产物 md5：`bc8d2a4d7a2886351f750268e7021107`（v202）
- 闸5 语法：1 块 0 报错；[VER] 探针断言 admin=v202 ✓
- 闸6 消失检查：828→828（纯 CSS 内联属性，无函数/id 变动）✓
- 闸7 四语：无新增文案 ✓
- 闸8 真渲染：Playwright 390px 逐页量 —— 修后 dashboard/students/schools/partners/dispatch/payments 全 =390 不溢出。✓
- 闸9 视觉：只加一个现有内联样式的属性 flex-wrap:wrap，零新颜色/类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底/产物 md5：f22e0561854b490c558ce7704f217d48 → bc8d2a4d7a2886351f750268e7021107
index.html 改了几处：3（v201→v202）
提交哈希：主仓库本地提交，未推送
探针脚本：`~/mh-verify/explore_job5_02.mjs`（六页量）+ `explore_job5_02b.mjs`（定位元素）
偏离或疑点：无。
