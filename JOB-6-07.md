# JOB-6-07 回执 · 审核端水印预渲染缓存（2026-09-16）· 只设计不改代码

> 大白话版。**这一包只出设计图，不动任何代码**（队列硬规矩）。读了水印函数、审核端查看逻辑、派单机制，写清了「派单时就先把水印件渲染好存起来、审核员点开直接下载」这套方案怎么做。完整设计图见 `JOB-6-07-design.md`（与本回执同目录）。

## 现在为什么慢
审核员每次点「查看」，都要**冷启动一次边缘函数**现场下原件、现场打水印 —— 实测 2.7–3.7 秒（等待首字节就 ~5.4 秒）。

## 方案一句话
**派单的那一刻，后台就悄悄把这个审核员专属的水印件渲染好、存进一个私有桶；审核员点开时直接下载现成的 → 1–2 秒。** 万一桶里没有，自动退回现在的现场渲染，功能零风险。

## 关键发现（决定方案的几点）
1. **水印是"每个审核员一份"的**（水印上印着该审核员的 RV 编号）→ 缓存必须**按审核员分文件夹**，不能全员共用一份。
2. 审核端**本来就有客户端缓存 + 悬停预取 + 登录后后台预取**，只是每次登录都从冷的重来。这次是把"冷"提前到派单时做掉。
3. 平台已有现成的**触发器打边缘函数**的范式（通知邮件那套 pg_net），照抄即可。

## 要做的七件（详见设计图）
1. 新建私有桶 `wm-cache`，路径 `<审核员>/<材料>.<后缀>`。
2. 水印函数加个 `prepare` 模式：给定材料+审核员，渲染后存桶、不回字节。
3. 派单表 INSERT 时挂触发器 → 打 `prepare`（照抄通知邮件那套）。
4. 审核端「查看」改成：先试桶（有就秒下），没有再走现在的现场渲染。
5. 撤单/材料软删时清掉对应缓存（触发器即时清 + 兜底扫描）。
6. 迁移内容清单已列（**注意：队列写的"迁移 0174"是笔误，0174 早用过了，本轮已到 0188；施工时取下一个空号**）。
7. 预期：**2.7–3.7 秒 → 1–2 秒**。

## 代价 / 前置
- 迁移全是新增型（建桶/加策略/加触发器），能走 db-run.sh；但**水印函数的代码改动要你手动 deploy**（跟 watermark-doc 一样）。
- 有两个"首版可简化"的点（兜底 cron、清理用触发器还是扫描）设计里给了取舍建议。
- **等你看过设计图再开施工包。**

**边界守住**：只读源码 + 写设计文档，零代码/零 EF/零迁移改动。
