# JOB-21-05 回执 · 走查脚本版本表自动化（期望版本改读主仓库 index.html · 先红自检后绿）

CC 执行。从 `jobs/JOB-20-04/online-walk.{mjs,sh}` 复制到 `jobs/JOB-21-05/`，旧文件一字不动。

## 一、做了什么（唯一逻辑改点 + 一个自测开关）
1. **期望版本不再手写**：`TARGETS` 的 `file`/`verRe` 改为**读主仓库 `<portal>/index.html` 的重定向目标**（单一真源，新增 `localExpected()` + `verReFromBase()` + `target()`）。线上 index 指向与主仓库不一致才判红。这样每次 bump 后无需手改版本表——脚本自动跟主仓库走。
2. **健壮性**：主仓库 index 读不到版本 → 该端直接判红（不再 `cfg.file.replace` 崩溃）；[VER] console 步骤的期望也随主仓库版本自动读（原写死 "v167/v166" 的 label 改为动态 `cfg.expectedBase`，防受骗式陈述）。
3. **自测开关**：新增 `MH_WALK_VERSIONS_ONLY=1` —— 只跑版本表①、跳过 ②③ 深走查（供闸3 廉价复跑）。默认不设时全走查行为与 20-04 一致（schX 拒绝走查 / partner1 走查 / 截图 / 自推**一字不动**）。
4. 输出改 `~/mh-jobs/JOB-21-05-online.md` + `~/mh-jobs/shots/21/walk/`（避免覆盖 20-04）。

## 二、本地对现役线上跑一遍（如实记录）
`~/mh-jobs/JOB-21-05-online.md`（`MH_WALK_NO_WAIT=1 MH_WALK_NO_PUSH=1`）：小结 **绿 6 / 红 2 / N/A 4**。

| 端 | 主仓库 index 指向(期望) | 线上实际 | 判 |
|---|---|---|---|
| student | v399 | v399 | 绿 |
| partner | **v169** | **v168** | **红(预期)** |
| school | v234 | v234 | 绿 |
| admin | v218 | CF Access 挡 | N/A |
| reviewer | v45 | CF Access 挡 | N/A |

- **partner 两条红是本自动化要暴露的正确信号**：本轮 21-02 已把主仓库 partner index bump 到 v169（已提交、未推、未部署），线上仍 v168 → 版本表红 + partner console [VER] 红（同因）。业主收官推送 + CF 部署后线上追上 v169，此两红自动转绿。**非脚本 bug、非假红**。
- 深走查旁证（全走查跑到）：schX 真登线上中介端 **被拒**（locked=true / session 空 / 无机构内容 / 见拒绝文案）= 绿（v167 角色门已随第二十轮上线生效）；两端 0 pageerror。

## 三、闸3 自检（先红后绿）
把 `school-portal/index.html` 临时改指向假版本 `v999`（工作区改、不提交），跑 `MH_WALK_VERSIONS_ONLY=1`：
- 证据 `jobs/JOB-21-05/selftest-fake-school.txt`：`[红] ① school index 指向主仓库版本 — 期望 maxhouse-school-portal-v999.html | 实际 线上=v234` → **假版本→该端判红 ✓**。
- 随即 `sed` 还原 school index 回 v234（`git status` 干净、无改动残留）→ school 复绿（见 §二表）。
- **lint 过**：`jobs/JOB-20-04/shell-lint.sh` 全绿（0 命中，含新 `.sh`/`.mjs`）。

## 四、N 闸
- **N1**：本包不升任何端版本（纯走查工具），无端 HTML 改动 → 静态清单无差集。
- **N3**：深走查走 realpage partner1/schX 真登线上（accounts.json 仅脚本读、未打印）；版本表①只读 index GET，无凭据。
- **N5**：secret-scan 于 `jobs/JOB-21-05`、`JOB-21-05-online.md`、`shots/21/walk` 皆 CLEAN。
- **N6**：只做 21-05 写明（版本表 + 自测开关 + 输出路径），未顺手改他处；旧 20-04 文件一字未动。

## 五、状态
- 主仓库已 add 点名 + commit（`jobs/JOB-21-05/online-walk.{mjs,sh}` + `selftest-fake-school.txt`），**未推**（§0.9）。无端 HTML 改动、无版本 bump。
- 本回执 + `JOB-21-05-online.md` + `shots/21/walk/INDEX.md`（无截图，PII 红线）已 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**：自检先红后绿、还原干净、lint 绿、secret-scan 绿。partner 两红是「主仓库领先线上」的**正确**信号（业主推送+部署后转绿），非 CC 闸不过。

下一包 = 21-06
