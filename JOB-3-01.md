JOB-3-01  结果：全闸通过（管理端 v192 → v193，学校端 v227 → v228）

## 做了什么（一句白话，七件）
1. **付款页「本月」**：原来月份写死在 2026-05，永远是那个旧数；改成读当前系统月（现在算的是本月真实解锁付款）。
2. **`[VER]` 控制台探针**：管理端从 v171 补到 v193、学校端从 v225 补到 v228（第三轮起每版跟着改，控制台看得到真版本）。
3. **删三块死代码**（都先证明零调用）：
   - 管理端 `_v22SchoolTierSection`（还写着青铜/白银/黄金）——层级段自 v191 起已改走 `_v191SchoolTierSection`，旧的零调用。
   - 管理端 `setSchoolTier`（还写本机缓存）——它唯一的引用就在上面那个死函数里，一并清。
   - 学校端 `scheduleMockQuotaReview` + `OFFER_QUOTA_REVIEW_DELAY`（10 秒假审核）——提额自 v226 起走真表，假审核链已断，零调用。
4. **学校页筛选条两组 chip 加小标**：状态组（全部/活跃中/试用期）前加灰字「状态」，层级组（试用/标准/高级/旗舰）前加灰字「层级」，不再把"试用期(状态)"和"试用(层级)"看混。
5. **推荐草稿同名文件**：两个 `1.jpg` 若来自不同路径 → 显示路径尾两段（如 `passport/1.jpg`、`degree/1.jpg`）区分；完全重复 → 去重。
6. **学校列表空邀请码**：没邀请码的学校原来显一个空绿框，改显破折号「—」。
7. **学校行为日志旧表标签**：核对旧表 `school_access_log`——它只有 `file_kind`(image/pdf/video) 和 `outcome`(served/served_original)，**不区分 view / download**（就是"取件"日志），所以保留「取件」标；顺手在取件行补上文件类型（如「取件·image」）。

## 闸 6 报告的函数消失（都是故意删，逐个说明）
- 管理端：`setSchoolTier`、`_v22SchoolTierSection` —— **零调用，故意删**（见第 3 条，证据：grep 全文除定义外无引用；setSchoolTier 的唯一 onclick 引用在 _v22SchoolTierSection 内部）。
- 学校端：`scheduleMockQuotaReview` —— **零调用，故意删**（提额已走真表，JOB-2-11 已断开它的调用）。`OFFER_QUOTA_REVIEW_DELAY` 常量同删（只被这个函数用过）。`applyQuotaRequestDecision` 本体保留（未在删除清单内）。

## 业主要看什么
1. 付款页「本月」不再是那个 2026-05 的旧数。
2. 控制台打印 `[VER] admin portal v193` / `[VER] school portal v228`。
3. 学校页筛选条两组各有灰字小标「状态」「层级」。
4. 没邀请码的学校显「—」不是空绿框。

## 十道闸（逐条）
- 闸1 基底 md5：admin `5e72294467868d50993d261611b5e935`(=v192) / school `5b8e957d41fc89140be5d671bbe2df2e`(=v227) ✓
- 闸2 锚点唯一：admin 8 处 + school 3 处 各 =1 ✓
- 闸3 补丁自检：2026-05 硬编码归 0、当月逻辑 =1、[VER] v193/v228、两死函数定义归 0、状态/层级小标、空码破折号、草稿同名区分、file_kind 补标 各命中 ✓
- 闸4 产物 md5：admin `1b870a6cad0fdbef4bc73da0bebd0c45`(v193) / school `745c914b8ef4cf5ac6d9a014904084c7`(v228)
- 闸5 语法：admin 1 块 / school 2 块 0 报错；**[VER] 探针断言**：admin=v193 ✓ / school=v228 ✓（第三轮新增断言）
- 闸6 消失检查：admin 800→798（消失 2=故意删）；school 853→852（消失 1=故意删）——见上逐条说明 ✓
- 闸7 四语：管理端中英、学校端四语；新增文案（状态/层级/Status/Tier）中英齐全；学校端本包无新增 UI 文案，零裸键 ✓
- 闸8 真点击：admin —— 草稿同名两路径→passport/1.jpg+degree/1.jpg 区分、唯一名去重、筛选条含「状态」「层级」小标、空码显—、旧表取件行显「取件·image」；school —— scheduleMockQuotaReview 已 undefined、submitQuotaRequest/loadTierQuota/loadSchoolQuotaRequests 仍在、页面零 pageerror。反向样本各 FAIL（有分辨力）✓
- 闸9 视觉：小标用 var(--ink-soft) + 11px(在允许字号档内)，零新写死颜色/CSS类 ✓
- 闸10 线上：不推主仓库，跳过

基底/产物 md5：admin 5e7229→1b870a；school 5b8e95→745c91
index.html 改了几处：admin 3（v192→v193）+ school 4（v227→v228）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 「本月」读的是 live+mock 合并的 PAYS；改当月后 mock(2026-05)自然不计入，本月数=当月真实解锁付款（真实数据在上方真收款面板；此 mock 统计口径本就是演示，改月是为去掉写死）。
- 旧表 school_access_log 确认不区分 view/download（只有 file_kind/outcome），故「取件」标保留 + 补文件类型；这是 item 7「能区分就分开标」的诚实结论。
