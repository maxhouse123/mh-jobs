# JOB-7-03a · 上传体验 · 反馈层（不换协议）— 回执

**结果：做完，红线（失败必回落老路）用测试实打实验过。学生端 v384、中介端 v152。本地 stub 走查：学生 8/8、中介 5/5 全过。stuA 真登真传那一闸因密码没给到（你启动指令末行是空的）延后，见文末。**

## 大白话：解决了什么
你在 Pixel 上传时看不到「正在传」，以为没反应就反复点、重复传。根子一半是家里代理绕美国慢，另一半是**界面根本不给反馈**。这次专治后者：
1. **选完文件当场出卡**：一个字节还没发，右下角就弹出一张带缩略图的小卡 +「上传中 0%」进度条。
2. **进度条走真字节**：改用「签名直传 + XHR」拿到真实上传进度，进度条跟着真字节走。
3. **失败必回落老路（你的红线）**：签名直传任何一步出问题，**自动回落到现有的上传函数**，学生的上传绝不因为这层新东西坏掉。测试专门验了这条。
4. **按钮锁**：文件在传时，那个槽的「+ 添加更多」变灰、显「上传中 · 第 N/M 个」，点不动。
5. **在途去重**：同名同大小的文件一批里只传一次。
6. **慢网提示**：单个文件超过 8 秒，卡上加一行「网速较慢，请耐心等待，不要重复点击」+ 已等秒数（四语）。
7. **失败明确**：传失败的卡变红、显「重试」按钮，不再默默无声。
8. **3 个并行**：多文件时最多 3 个同时传（原来是一个接一个串着等）。

**边界**：没碰 AI 预检、没碰压缩、没碰 documents 表。全部走一个共享模块，异常一律回落。

## 探出来的上传线（队列要求先探）
**学生端 v383 现有上传线：**
- `handleUploadCloud`（自填主线，选完 → 串行 for 循环 → `.upload()`）— 原来**上传完才出缩略图**，途中零反馈（就是你踩的坑）。
- `_ru1UploadPending`（材料换版/驳回重传，`commitResubmission` 调它）— 原来只有一行文字状态。
- `_handleReferralUpload`（推荐线草稿即传）— 原来只有预览转圈。
- 另有付款凭证 `payment-evidence` 线、concierge 暂存线（不在本轮范围，未动）。

**中介端 v150 现有上传线：**
- `_matCommit`（代传材料真提交，`handleMaterialUpload` 暂存后提交）— 原来只有 toast。
- `_wzStageFiles`/向导自定义（`referral-drafts` 即传）— 原来只有转圈。
- 代传换版/重传线（`_v134StageReplace` 系）。

## 改了什么（版本 +1）
- **学生端 v383 → v384**：新增共享模块 `_v384`（签名直传+XHR真进度、3并行池、在途去重、进度卡 dock、慢网、失败重试、按钮锁），接入 `handleUploadCloud`（全部 7 项）+ `_ru1UploadPending` + `_handleReferralUpload` 三线。新增 5 个四语文案键（en/zh/ru/fr 齐）。
- **中介端 v150 → v152**（v151 是 7-02 图标那一版）：同款模块（挂 `window._v384`，因中介端多层作用域），接入 `_matCommit`（代传）+ 向导暂存 + 换版重传各线；模块自带四语文案（中介端 ru/fr 是外部词典文件，本层不依赖它、自给自足）。
  - 中介端有一条 onboarding 自证材料线（`10517`）不在队列列出的三线内，**保守起见留老路未动**，避免给未列范围的路径引入风险。

## 十道闸
- **闸 3（静态）**：`onprogress` 处理函数定义 = 1（两端各 1）✅；每条上传线各接入一次（学生 4 处调用点、中介 6 处调用点）✅；**回落分支存在**（模块结尾 `return await window.sb.storage.from(bucket).upload(...)`）✅；新文案键四语齐 ✅；两端 JS 全部解析干净 ✅。
- **闸 8（真浏览器走查·stub 版）**：按铁律11 用 stub `window.sb` + stub `XMLHttpRequest`，只碰本地页、不碰生产、不碰真库：
  - 学生 `tests/job7-03a.mjs` **8/8**：选完 200ms 内出进度条 ✅；progress(0.5)→条宽 50% ✅；失败→红卡+重试按钮真触发 ✅；签名直传成功且进度回调触发、不回落 ✅；**红线：签名直传抛错→回落到 storage.upload** ✅；同名同大小去重 ✅；并发池封顶 3 ✅。
  - 中介 `tests/job7-03a-partner.mjs` **5/5**：`window._v384` 跨作用域可达 ✅；即时卡+进度 40% ✅；**红线回落** ✅；自带四语表（fr=Réessayer、ru 有值、非裸键）✅；并发≤3 ✅。

## ⚠️ 延后的一闸（等你）
队列闸 8 要求「**用 stuA 真登，Playwright 传一张 2MB 图**，断言选完 200ms 出条、按钮 disabled、完成 100%，再拦 500 验红卡」。**你启动指令最后一行「stuA 密码在下一行：」后面是空的**，密码没带出来。真登真传涉及真库变更（铁律11 本就要留你验收），我用 stub 把 UI 行为全验了；**真账号端到端那一下等你把 stuA 密码补给我**（走环境变量、绝不落盘），我十分钟内补跑并追一份证据到本回执。

## 动过的文件
- 改：`student-portal/maxhouse_student_portal_v384.html` + `student-portal/index.html`；`partner-portal/maxhouse-partner-portal-v152.html` + `partner-portal/index.html`
- 新增测试：`tests/job7-03a.mjs`、`tests/job7-03a-partner.mjs`

**主仓库未推。本回执已推日志仓库。**
