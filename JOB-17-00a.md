# JOB-17-00a · MHViewer2 v2.1.0：PDF 模式组件 + 自测（CC 交付，2026-09-21）

> 角色：本包 CC 只做「PDF 模式组件 + 自测」一个单元。没写 QUEUE-17，没给业主出卡，没装进三端，没碰 `assets/`、没碰端口文件，没 commit 主仓库。
> 工作副本全在 `/Volumes/Dev/MAXHOUSE/jobs/JOB-17-01/kit/`。对话里的 Claude 会拿自己的尺子独立复验本成品，再写 QUEUE-17。

## 一、做了什么
在 r17-kit-seed 的 v2.0.1（窄盒紧凑工具条）源码上**加了 PDF 模式**，图片模式一字未改：
- 组件（`mh-viewer-v2.src.js` → `python3 build.py` 出 `mh-viewer-v2.js`）：新增 `MHViewer2.mountPdf(host, {blob|bytes, pdfjsLib, lang, height, maxPages, extras})` 与内部 `PdfInstance`，与图片模式**共用同一条工具条 + 缩放/平移/夹取引擎**。版本号 v2.0.1 → **v2.1.0**。
  - 工具条 = 图片那条（− 百分比 + │ 整页 适宽 1:1 │ 旋转）**加页码组**（上一页 `n / N` 下一页）；窄盒两级紧凑（narrow→tiny），~274px 中英俄法都一行放得下。
  - 滚轮=上下翻页；Ctrl+滚轮 / Safari gesture / 触屏双指=以指针为中心缩放；拖动平移、双击 适宽⇄100%、键盘 `+ − 0 1 W R PageUp PageDown Space Home End`（点过取景框后才生效、文字框打字不触发）。
  - 缩放停手约 160ms 按新比例重渲可见页（先 CSS 缩放顶着、渲好无闪白换上）；单页 canvas 面积上限 ~1600 万像素。只渲可见页 ±1、滚远释放 canvas 留占位高度（活 canvas 有硬上限、不随总页数涨）。
  - 页码跟随取景框中线所在页；翻页到页顶。旋转=整份文档 90° 一步（每页转+竖排重排）、转后停当前页。坏/加密 PDF 不抛错：中央四语人话 + 工具条禁用 + `whenReady()` 得 false；渲染进度进工具条（不再深底深字状态行）。
  - 组件本身不带下载/打印；「新标签打开 / 下载」只由**管理端**经 `extras` 传入。pdf.js 由**宿主**传入（组件不自己加载）。`destroy()` 取消渲染任务、销毁 pdf 文档、解绑、释放 canvas；同宿主重复 mount 只留一套；脱离 DOM 自毁。
- 自测（真鼠标+量几何）：新增 PDF 检验器 `mhv2-pdf-check.mjs`（`checkPdfViewer`）+ 合成 PDF 生成器 `make-pdfs.mjs`（自托管 pdf-lib，无任何真实材料）+ PDF demo `demo/demo-pdf.html`，并入 `run-selftest.mjs`（图片 file:// + PDF 本地 http 同源加载 pdf.js）。图片检验器 `mhv2-check.mjs` **一字未改**（md5 `e12b422ffc15b95d50f6c463dc9e0beb`，`md5 -q`）。

## 二、结果是什么
- 成品 `mh-viewer-v2.js`：v2.1.0，**md5 = `d0a0dc272ede470f126b0facbf6390c9`**（`md5 -q mh-viewer-v2.js`），纯 ASCII、`python3 build.py` 可复现（同 md5）。
- 两内核自测各一遍（`cd ~/mh-verify && [MH_BROWSER=webkit] node .../run-selftest.mjs`）末行：
  - `SELFTEST MHV2-ALL-AS-EXPECTED (chromium)`
  - `SELFTEST MHV2-ALL-AS-EXPECTED (webkit)`
- 场景红绿（两内核结论一致；WebKit 无 CDP → 双指/Ctrl+滚轮记 SKIP）：
  - 图片 10 场景：8 正常 GREEN + 2 反例（S4 透明挡板 / S9 按钮在但没反应）RED。
  - PDF 16 场景：P1 三页+文字框 / P2 单页 / P3 25 页截断+canvas 回收 / P4 横竖混排 / P5 A3 大页 / P6 触屏双指 / P7 管理端 extras / P8 窄盒 274 四语（zh/en/ru/fr）/ P9 加密优雅失败 / P10 损坏优雅失败 —— 全 GREEN；N1 透明挡板 / N2 按钮没反应页码不动 / N3 缩放不重渲染（发糊）—— 全 RED（尺子对做坏的版本会报红）。全程 0 页面报错。
- 公开截图（元素级 `.mhv2`、全部合成 PDF）：`~/mh-jobs/shots/17/17-00a/` 11 张 + `INDEX.md`；CC 已逐张过目（默认适宽 / 放大 / 整页 / 1:1 / 旋转 / 下一页 / 窄盒 274 英 / 窄盒 274 俄 / 截断提示「仅前 20 页，共 25 页」/ 损坏优雅失败 / 管理端 extras）。
- 产物清单在 `jobs/JOB-17-01/kit/`：`mh-viewer-v2.src.js`、`mh-viewer-v2.js`、`build.py`、`mhv2-check.mjs`（原样）、`mhv2-pdf-check.mjs`、`make-pdfs.mjs`、`run-selftest.mjs`、`demo/`（`demo.html`/`demo-pdf.html`/`pdf/*.pdf`）、`KIT-README.md`、`KIT-DONE.txt`。

## 三、业主要做什么
- 本包**无需业主操作**。等对话里的 Claude 独立复验本成品并写出 QUEUE-17 后，由 QUEUE 指挥 CC 把 v2.1.0 装进 reviewer/school/admin 三端（新文件名、各升一版、PDF 分支改走 `mountPdf`），再做真页面先红后绿与你的人工验收。

## 四、新闸证据（自测过程留痕）
- 迭代中真实暴露并修掉的病（每一步都靠 checkPdfViewer 量几何抓出）：① Ctrl+滚轮 harness 用 CDP 发带 Ctrl 的滚轮（Playwright mouse.wheel 带不了修饰键）；② 适宽改为贴合「当前页」宽（横竖混排下才对）；③ PDF 缩放改比例步进（×1.25）避免 1.24×→1.25× 的 0.8% 死档；④ 重渲染门槛改「一旦放大超过已渲比例就升级」（原 0.18 松门槛让 1.24→1.5 不重渲→发糊）；⑤ 窄盒加 tiny 二级紧凑（PDF 比图片多页码组，274px 一级紧凑放不下）；⑥ 在途渲染回来时按当前可见带二次校验，杜绝滚远页被迟到的高清 canvas 换回来的泄漏；⑦ demo 舞台高度塌成 min-height（`height:'100%'` 给了无确定高度的宿主）——改成默认高度；⑧ 关键：宿主敌意 CSS `canvas{height:auto!important}` 覆盖了组件的非 important 内联尺寸→页栈几何全乱，改成用 `!important` 强制 canvas 显示尺寸（图片模式本来就是全程 important，PDF 补齐）。
- 反例三闸（N1/N2/N3）证明尺子有分辨力：透明挡板→按钮点不到 RED；按钮在但没反应+页码不动 RED；缩放不重渲染（backing÷显示=0.22<dpr×0.95）RED。
- secret-scan：见推送前记录（`~/mh-jobs` 全树无密码/token/JWT）。
- 边界守住：图片模式 `Instance` 零改动、图片自测 8 绿 2 红两内核照旧；`mhv2-check.mjs` md5 与 v2.0.1 一致；未碰 `assets/`、端口文件、主仓库。
