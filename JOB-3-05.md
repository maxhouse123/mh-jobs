JOB-3-05  结果：全闸通过（迁移已跑 + 管理端 v196 → v197）

## 做了什么（一句白话）
学生抽屉加两段（两张表原来管理员读不到，先加了只读权限）：
1. **分享记录**：读 media_shares（学生分享 offer 的链接），列 范围(scope)、创建时间、打开次数、最后打开、是否已撤销。
2. **所在地变更**：读 location_change_requests，列 从(境内/境外)→到、申请时间、生效时间、是否取消。

## 迁移（我经 db-run.sh 跑的，跑前跑后差值）
- media_shares：策略数 +1（新增 admin_read_media_shares）
- location_change_requests：策略数 +1（新增 admin_read_location_change_requests）
- 库里数据：media_shares **12 条**、location_change_requests **5 条**（验收有数据可看）
- 迁移 `jobs/JOB-3-05/migrate.sql`；回滚 `jobs/JOB-3-05/rollback.sql`

## 业主要看什么
- 分享过 offer 的学生（库里 12 条分享）抽屉能看到「分享记录」段。
- 申请过变更所在地的学生（库里 5 条）能看到「所在地变更」段。

## 十道闸（逐条）
- 闸1 基底 md5：`ef0dd0fde9223001b31ed9a20f35ec2d`（=v196）✓
- 闸2 锚点唯一：5 处各 =1 ✓
- 闸3 补丁自检：两段定义+两表查询+装配+填充、[VER] v197 各命中 ✓
- 闸4 产物 md5：`e32b190d820b677194d537290bed2918`（v197）
- 闸5 语法：1 块 0 报错；[VER] 探针断言 admin=v197 ✓
- 闸6 消失检查：806→812（+6，无消失）✓
- 闸7 四语：管理端中英；新增文案（分享记录/所在地变更/已撤销/有效/打开/境内/境外/已生效/待生效/已取消）中英齐全 ✓
- 闸8 真点击：Playwright 桩 —— 分享段渲染 full/打开 3/有效/已撤销；所在地段渲染「境外 → 境内·已生效」。反向样本 v196：函数未定义 → FAIL（有分辨力）✓
- 闸9 视觉：复用 dd-row/badge/var()，零新颜色/类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底/产物 md5：ef0dd0fde9223001b31ed9a20f35ec2d → e32b190d820b677194d537290bed2918
迁移跑前跑后：media_shares 策略 +1；location_change_requests 策略 +1
index.html 改了几处：3（v196→v197）
提交哈希：主仓库本地提交，未推送
偏离或疑点：分享记录按 media_shares.sharer_uid = 学生 uuid 取（该表 sharer_uid 即分享者=学生）。
