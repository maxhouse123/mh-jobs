# JOB-23-00 回执 · 第二十三轮开工检查 + 桩数据 + 七处旧病先量成红

做这一步的人：CC（无人值守）。这一包只看、不改任何端的代码，只在 `jobs/JOB-23-00/` 下放测试工具和假数据。

## 一、做了什么

1. **开工对表**：把五端现役版本和基线一个个核过，全部对得上（见下表），本地代码和云端（远端 GitHub）是同一笔，工作区是干净的。
2. **管理端旧版按钮清单**（给后面每包比对用，防止「原来有的按钮悄悄没了」）：对 v219 全文抽出了「元素 id / 按钮点击函数 / 函数定义」三张表，存成一份 json。
3. **造假数据 + 本地假页面**：管理端没有测试账号（登录要 Cloudflare 门禁 + 管理员密码，都不在文件里），所以按第二十二轮的老办法——在本机用无头浏览器打开管理端页面，把它跟云端的通话全部换成我们写好的假数据，不碰真库。
4. **七处旧病，先用机器量成「红」**：红 = 毛病确实在。后面每一包修完，会在同一把尺子上量成「绿」。这一包七处**全红**，浏览器控制台**零报错**。

## 二、结果是什么

### 开工对表（全部相符）
| 文件 | 版本 | md5 | 结果 |
|---|---|---|---|
| admin v219 | v219 | a472693c27426b0b1494c605d1072e3b | ✅ |
| student v400 | v400（不动） | 19391aa80ac10cb7835ef50f989432b0 | ✅ |
| partner v170 | v170（不动） | afe53f4be41237a5c36eb880914e918e | ✅ |
| school v235 | v235（不动） | f77dcba686c3b5e6d0dd005c796b7435 | ✅ |
| reviewer v46 | v46（不动） | 2d4269e9c570481b99b95d70a3691082 | ✅ |

- 本地 HEAD = `976e426…` = 远端 origin/main（相符）；`git status` 干净。
- 起点提交号记在 `jobs/JOB-23-00/base-head.txt`。
- `_headers` 现役 md5 = `bdd0eb1e1008a0759ac923cbb3b02c34`（本轮不动它，只登记）。

### 旧版按钮清单（N1 基线）
`jobs/JOB-23-00/inventory/admin-v219.json`：id **200** 个、点击函数 **172** 个、函数定义 **813** 个，三张表都非空。

### 假数据清单（都在 `jobs/JOB-23-00/stub/fixtures.mjs`，纯合成、无任何真人信息）
- `admin_events` 审计事件：12 行、5 类以上（含 联系方式解锁 / 移出候选池 / 设签证有效期 / 学生关轮 / 分享被打开），detail 照线上真形状。
- `school_access_log` 取件流水：**690** 行（全是取件），doc_id 指向 documents 假数据里的 **40** 个 id；`school_action_log` 埋点：**27** 行（导出/导出名单/下载/查看学生 混合）。
- `offer_decisions` 录取决策：**6** 行（其中 2 行 3 天内到期、未付款）。
- `students`：10 行（其中 3 行是测试学生）；documents / reviewer_assignments / review_verdicts 让「材料派单 / 审核中 / 待二审」各有 ≥4 行、且各含 ≥1 份测试学生的件。
- RPC `get_admin_pending_tasks` 返 2 行；RPC `admin_list_referral_corrections` 返 3 待审 + 2 已批。

### 七处旧病 —— 先红实测（`jobs/JOB-23-00/repro/red-all.mjs`，日志 `red-all.log`；CC 已独立复跑一遍，结果一致）
| 编号 | 病 | 量出来的红值 |
|---|---|---|
| R1 | 推荐归因页是假数据 | 页首有「演示数据」横幅、hero 里有假佣金合计 **¥4,800**（= 8 条推荐码 × ¥600）→ 红 |
| R2 | 五处「导出」是假的 | 学生页点顶栏导出图标 → 弹框点「所有学生」→ 300ms 内**没有**任何文件下载、只弹 toast「✓ 已生成 students.csv…」→ 红 |
| R3 | 录取通知/截止监控混了种子假行 | 通知分发页正文 **12** 行 = 真 6 + 种子 6，且有「演示」分隔线；截止监控页 **5** 行 = 真 2 + 种子 3，且有「混合视图」横幅 → 红 |
| R4 | 审计事件看不懂 | 「设签证有效期」这行标题直接显英文码 `visa_valid_until_set`，正文里裸露原始 `{"doc_id"…` → 红 |
| R5 | 学校行为日志被取件流水淹没 | 默认视图前 20 行里取件占比 **100%**，取件行学生列全是「—」→ 红 |
| R6 | 侧栏两个待办没角标 | 「任务审批」「信息更正」两个角标元素**根本不存在** → 红 |
| R7 | 三个材料队列混着测试件 | 材料派单/材料审核/待二审三页都**没有**「隐藏测试」筹码，且列表里确实有测试学生的件 → 红 |

浏览器控制台报错：**0**。

### 每处用的验法（透明起见）
七处全部靠「把云端通话换成假数据（stub window.sb）」这一种正规办法量出来的，没有一处是直接改内部变量硬凑的。R1、R6 连假数据都不用——R1 的假佣金是页面写死的常量、R6 是纯粹查两个 id 在不在。

## 三、业主要做什么

这一包无需业主操作，也没有要肉眼看的东西（都是机器量的数字）。真正要业主肉眼验收的视觉件，会在后面各包做完、收官时统一列给你。

## 附：闸账
- 闸3（23-00 判据）：R1–R7 全红 ✅；`inventory/admin-v219.json` 三张表非空 ✅；零 pageerror ✅。
- N1：本包不改端，无差集。
- secret-scan：`jobs/JOB-23-00` 全树扫描 = CLEAN ✅（无密码、无 token、无 JWT）。
- 主仓库：`jobs/JOB-23-00/` 工具与桩已 commit（不 push）；本回执在 ~/mh-jobs commit 后立即 push。

下一包 = 23-01
