# JOB-20-03 回执 · 学生端 v399：在读证明中文名对齐（doc.studyProof）

CC 交付。第二十轮 20-03。student v398 → **v399**。改源码、**不部署、不推主仓库**（等收官卡1153）。

## 一句话
zh 词典 `doc.studyProof`「学习证明」→「在读证明」，经 `I18N._sup.zh` 覆盖（`t()` v399:8489 `_sup` 优先）。`doc.studyPlan` 本已「学习计划」与管理端一致、**不动**；en/ru/fr 一字不动；**无 prepare 调用**（20-01 证伪：prepare 需 hook-secret + 已派单，前端调不了）。

## 前提证伪 → 按 N2 只做真实缺陷（不照错前提改，P2 走完）
- QUEUE-20 §20-03 前提「学生 zh 两栏显英文（红）」**不成立**：真页面 zh 本就显中文（外挂包覆盖），20-00⑥ 已如实记「未复现」。§0.9「单项未复现按 N2 处理后继续」→ 本包不 STOP、不照「显英文」前提改。
- **脚本实读（不手抄，§20-03 硬性）确认真实缺陷 = 本端内部术语 outlier**：

| 出处 | 键 | zh 值 |
|---|---|---|
| 管理端 v218 `ADM7_KINDS`（`admin-portal/maxhouse-admin-portal-v218.html:6417`） | `studyProof` | **在读证明** |
| 学生端本端 语言包（`maxhouse_student_i18n_v360_zh.js:235`） | `supDoc.studyProof` | **在读证明** |
| 学生端本端 主文件（`v398.html:14164`） | `studyProof` | **在读证明** |
| 学生端本端 语言包（`maxhouse_student_i18n_v360_zh.js:1282`） | **`doc.studyProof`** | **学习证明** ← 唯一 outlier |
| 学生端 `doc.studyPlan`（`:1289`） / 管理端 `studyPlan.zh` | studyPlan | **学习计划 = 学习计划**（已一致，不动） |

- 结论：QUEUE §20-03 明列映射 `doc.studyProof=在读证明`（**P2 图纸内明文预授权映射**）= 把 outlier 对齐到本端自己 supDoc + 管理端/审核端已在用的标准词。故按图施工，不属「照错前提自修」。低风险、zh-only、可回滚。20-00 交接的「术语不一致待裁决」经深挖已定性为**本端内部 outlier**（非跨端方向之争），并入「要业主亲眼看的」供业主一票否决。

## 施工（最小落点）
- `I18N._sup.zh` 增一句 `Object.assign(I18N._sup.zh, { 'doc.studyProof': '在读证明' });`（v399:8317，带 JOB-20-03 注释）。`_sup` 在 `t()`(8489) 里优先于语言包，故覆盖 zh 包仍存的「学习证明」而**不改语言包文件**（语言包不重命名 → en/ru/fr 三包零改动，满足「en/ru/fr 不动」）。
- 未加 prepare（架构不成立，见上）。

## 四件套（版本 bump，P4/版本铁律）
| 件 | 状态 |
|---|---|
| ①文件名 | `maxhouse_student_portal_v399.html`（新；v398 保留可回滚） |
| ②[VER] 探针 | v399:7386 `[VER] student portal v399 (JOB-20-03 …)` 已改 |
| ③index.html 重定向 | 4 处 v398→v399（location.replace / meta refresh / canonical / a 链接） |
| ④docs/changelog | `docs/changelog/student-portal-versions.md` +v399 条 |

## 三层自验（铁律11，全过）
### ① UI 对照 / N1
- **N1 全量 diff v398→v399 仅 2 hunk**：[VER] 行 + 新增 `Object.assign(I18N._sup.zh,{'doc.studyProof':'在读证明'})`（带注释）。无第三处。
- **零死按钮 / 按钮集不变**：`grep -cF -e onclick` 两版皆 **310**（310=310）。
- **零裸 i18n 键**：未新增任何 t() 键；`doc.studyProof`/`doc.studyPlan` 四语字典均已存在（zh 覆盖值 / en 内联 / ru/fr 包），t() 无回落 key 本身。
- en 未动：`'doc.studyProof':'Proof of study'`(6775) 原样；ru/fr 包文件零改动。

### ② 无头走查（Playwright，本地静态服 + stub，绝不连库/不碰生产）
脚本 `jobs/JOB-20-03/verify-C-green.mjs` → `RESULT ALL=GREEN`（exit 0）。红线守：127.0.0.1 静态服 over repo root；`stub()` 拦 `**/auth/v1/**`+`**/rest/v1/**`（返回 stub/[]），**无一请求到线上 Supabase**；未碰生产网址；无测试写库；无需登录（纯词典读，无云数据）。

| 语言 | `t('doc.studyProof')` | `t('doc.studyPlan')` | DOM 文本可见 | 判定 |
|---|---|---|---|---|
| zh | 在读证明 | 学习计划 | ✅ | ✅ |
| en | Proof of study | Study plan | ✅ | ✅ |
| ru | Справка об обучении | План обучения | ✅ | ✅ |
| fr | Attestation d'études | Plan d'études | ✅ | ✅ |

- **覆盖实证**：`I18N._sup.zh['doc.studyProof']` 命中（supHit=true）且真页面 zh 取到「在读证明」→ 证明 _sup 覆盖了 zh 包里仍存的 outlier「学习证明」。

### ③ 自验报告
`docs/self-test-report-JOB-20-03.md`，逐条 路径×断言×结果×证据；机器摘要 `RESULT ALL=GREEN`。

### 铁律12（student 基线功能）/ 铁律13（上下游闭环）
纯词典取值覆盖：零函数改动、onclick 恒等、无按钮增删 → 基线功能矩阵无一条状态变化，不产生/不砍功能；无新云端写入链路 → 无闭环缺口、admin 无需新增入口。

## 新闸
- **N1**：只升 student 一版、四件套同版；三端其余（partner/reviewer/admin/school/viewer）一字未改；按钮集 310=310 差集为空。
- **N2**：旧病 20-00 C 已如实先记「未复现（前提证伪）」；本包不虚构红，改做实证存在的 outlier 对齐，四语真页面转/保绿。
- **N3**：本包纯词典、无云读，无需带身份；测试用 stub 完全隔离线上（不连库、不碰生产、不写库），如实记 N3 n/a。
- **N5 secret-scan**：`jobs/JOB-20-03/` 与自验报告无硬编码 token/key/密码，SECRET-SCAN-CLEAN。
- **N7**：本包无「给人看的新提示」UI（词典标签非计时 toast/弹窗），不涉。

## 交付物（均在主仓库 /Volumes/Dev/MAXHOUSE，本地 commit、未 push）
- `student-portal/maxhouse_student_portal_v399.html`（新）、`student-portal/index.html`（重定向 bump）、`docs/changelog/student-portal-versions.md`（+v399）。
- `jobs/JOB-20-03/verify-C-green.mjs` + `verify-C-results.json` + `verify-run.log`。
- `docs/self-test-report-JOB-20-03.md`。

## 要业主亲眼看的（收官汇总用）
- 学生端 **v399 中文界面**：材料上传两栏栏目名——「在读证明」（原 zh 包 outlier「学习证明」已被覆盖）与「学习计划」。若业主认为学生端应保留「学习证明」措辞，此为**一票否决点**（改动仅一行 `_sup.zh` 覆盖，撤除即回「学习证明」）。en/ru/fr 措辞不变。

## 交接
- **下一包 = 20-04**（脚本卫生：交付 `jobs/JOB-20-04/shell-lint.sh` 扫 `$名字` 紧贴非 ASCII / 正文含 `!`或反引号 / `bash -n`+`zsh -n`；扫 `jobs/JOB-19-03/deploy-watermark.sh`(应报 29/58 行红)+`jobs/JOB-19-06/online-walk.sh`+`jobs/JOB-20-01/deploy-watermark.sh`+本轮所有 .sh，命中改 `${名字}` 后再扫为 0；`jobs/JOB-20-04/online-walk.sh`+.mjs 从 19-06 复制改 v167/v168 真类名 `#_v167CardStack ._v167-card`，本地对现役线上跑一遍）。
- 主仓库源码 v399 html + index + changelog + docs/self-test + jobs/JOB-20-03/* **本地 commit、未 push**（等收官业主卡1153）。
