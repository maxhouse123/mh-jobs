# JOB-18-06 回执 · 线上走查脚本(回执自动推送 + 元素级截图 + 真按钮删测试件)

CC 本包按 QUEUE-18 §18-06 从 JOB-17-07 复制后修出 `jobs/JOB-18-06/online-walk.sh` + `.mjs`(旧
JOB-17-07/* 一字不动),对**现役线上**跑通一遍并**证明「自动推送」与「元素级截图」真的发生**。
零端口升版、零迁移、零云函数改动、不推主仓库。脚本只推 `~/mh-jobs`(回执/截图)。

## 一、做了什么(相对 17-07 的五个修点,对齐 §18-06 ①—⑤)
- **① 自动推送**:走完写回执后脚本自己 `node secret-scan` → `git -C ~/mh-jobs add` **点名**(`JOB-18-06-online.md`
  `shots/18/walk`)→ commit → `git push origin main`,成功打印 `PUSHED <hash>`,失败打印原因(secret-scan 未过 /
  add / commit / push 各分支都不静默)。**实证**:末次 `PUSHED 12ce730`(origin 同步,`git log origin/main..HEAD`=空)。
- **② 元素级截图 + INDEX.md**(`~/mh-jobs/shots/18/walk/`):
  - 中介端邀请列表:截图前用样式把 `.dl-inv-name`(姓名)/`.dl-inv-sub`(邮箱)/`.dl-inv-av`(首字母头像)**打成黑条**,
    再截 `.dl-inv` 列表 → `par-invite-list-masked.png`(肉眼复核:姓名/邮箱/首字母全遮,只留结构/状态/按钮,零 PII)。
  - 学生端上传件:`other` 卡在 `display:none` 分区(见发现②),改截**该合成件的 `#previewModal` 预览浮层**(独立顶层、
    有稳定盒、只显合成图)→ `stu-upload-card.png`(肉眼复核:`SYNTHETIC TEST TRANSCRIPT / TEST STUDENT (synthetic) /
    ID 0000-TEST-0000 / not a real document`,带服务端水印,**零真实 PII、不含账户头部**)。
- **③ 真按钮删测试件**:刷新后按**文件名只认本轮合成件**(`mh18-online-synth`,绝不碰本槽真实件),循环真点其
  `.thumb-remove`(小圆×,22px 负偏移常被 thumb overflow 裁角 → 用左上角内 3px 点)→ `#confirmModalConfirmBtn` 确认,
  清到 0;**不兜底调页面内部函数**(去掉 17-07 的 `_s9CloudDeleteCore` 回落)。删完带身份 REST 复核。
- **④ 开头轮询五端 [VER]**:等新版最多 10 分钟(CC 单探用 `MH_WALK_NO_WAIT=1` 只探一次);admin/reviewer 被 CF Access 挡记 **N/A**。
- **⑤ 新增护照琥珀名单块**:中介端护照栏若有 `.r2-anchor-warn`,判其带两名句(**有才判**);本走查未主动触发姓名核对 → 无名单块 → N/A。

## 二、结果(现役线上单探;详见 `~/mh-jobs/JOB-18-06-online.md`)
小结 **绿 15 / 红 2 / 灰 5**。两红均为**第十八轮新版未上线**(现役 partner=v165,故 partner index/console 指向 v166 为红)——
属预期,真绿在业主收官推送后跑出。关键项:自动推送 `PUSHED`✓、两张元素级截图 PII 全遮✓、真按钮删除✓、
带身份复核 documents 活件 0 / storage 本轮合成孤儿 0✓、Copy link 提示条可见✓、五端 [VER] 逐端如实。

## 三、本包三处要点(过程中查实,记账)
1. **上传闸「批次」走 ledger、不走 documents(§0.11 取数纠偏)**:上传闸(迁移 `0160_doc_upload_ledger_gate`)按
   `document_upload_ledger`(`action='insert'`、按 1 小时窗计 gap)算批,`v_no=batches+1`,**≥3 需原因(`reason_required`)、
   ≥5 锁槽(`slot_locked_needs_review`)**。17-07 的批次探针按 `documents` 表数行是**错口径**(现役 stuA 名下 studyPlan
   documents=0 却 ledger=2 批,新传即第 3 批 → 首插 400 `P0001 reason_required_unreviewed`)。本包探针改走
   **`rpc/get_slot_batches`(N3 带身份、DB 真账)**,并**无条件兜住原因框**(`textarea#v395-reason-<slot>`→`#confirmModalConfirmBtn`);
   实测选中 `other`(0→1 批,不惹闸)传通。**学生端 v397 本身无 bug**——真人第 3 批本就弹原因框、填了即成。
2. **`other`(其他材料)卡在 `display:none` 分区**:`#card-other` 是 `display:contents`/隐藏模板(`boundingBox=null`,
   `locator.screenshot` 因页面 AI 提示轮询/缩略图重签一直重绘而超时;强制显形会破版)。为**绝不外泄账户头部 PII**,
   放弃「整页兜底」,改截独立顶层的 `#previewModal` 合成件预览(元素级、零 PII)。
3. **PII 修复(过程失误已收口)**:调试期我为「证明推送」连跑多趟带推送,早期截到的是**整页学生端**(含账户头部),
   已被自动推进 `~/mh-jobs` 公开仓 8 个提交。**已 `git push --force-with-lease origin 7fe8aa8:main` 把 origin 退回
   干净基线(JOB-18-05 回执),再落 1 个只含干净截图的提交**(现 origin 自 7fe8aa8 起仅 `12ce730` 一笔,PII 整页图不再在分支史)。
   教训:**迭代截图脚本一律 `MH_WALK_NO_PUSH=1`,只在末次干净后放推送**(已写进脚本头注与 §五)。

## 四、业主要做什么
- **本包无需业主做任何事**。脚本只在本机对线上跑、只推 `~/mh-jobs`;未升版、未迁移、未部署、未推主仓库。
- 收官(卡1140)业主把主仓库第十八轮各端推上线后,可选:关代理
  `export SUPABASE_ANON_KEY=<student-portal 里 sb_publishable_ 那串>` 后
  `bash /Volumes/Dev/MAXHOUSE/jobs/JOB-18-06/online-walk.sh`(不带 NO_WAIT,轮询等五端指向新版最多 10 分钟),
  届时 partner v166 的两红转绿、截图自动重出并自推。

## 五、新闸证据(N1–N7)
- **N1(按钮清单差集)**:本包**无端口升版、无 UI/按钮改动**,N1 不适用;五端 HTML 一字未动。
- **N2(先红后绿·真页面真点)**:走查即载体——真填登录表单、`setInputFiles` 触发 `onchange`、真点删除钮+确认框
  (非 evaluate 直调业务函数;隐藏区的预览眼/删除钮用真按钮 `dispatchEvent('click')`,仍是点那颗真按钮)。现役红(新版未上线)如实出。
- **N3(带身份)**:批次探针走 `rpc/get_slot_batches`、收尾复核走 stuA 真 JWT REST(documents 活件 + storage 孤儿),
  无身份的「0 行」不当证据;不打印任何 token。
- **N4(每条分支各验)**:五端 [VER] 逐端;上传→进度卡→precheck 200→刷新持久→真按钮删→带身份复核逐步;
  中介端列表在/Copy link 反应/pageerror/名单块逐条如实。
- **N5(secret-scan)**:自动推送前对 `JOB-18-06-online.md` + `shots/18/walk` + 脚本目录扫 → `SECRET-SCAN-CLEAN`;
  公钥读环境变量、脚本绝不打印 token/密码。
- **N6(只修不加)**:只按 §18-06 修走查脚本;未碰端口/云函数/迁移,未部署,未推主仓库;
  共用查看器 `assets/mh-viewer-v2-1-1.js` 与 `jobs/JOB-17-01/kit2/` 一字未改(仅 import kit2 的 `makeSyntheticTranscript`)。
- **N7(提示看得见)**:本包**不改任何给人看的提示**,N7 无施工对象;走查只**观测**中介端 Copy link 提示条
  (1.5s 内可见=绿)与护照琥珀名单块(有才判),不据此改样式/时长。

## 六、动过的槽 / 测试写库(§0.11)+ 一处旧残留
- **动过的槽**:`other`(现役 ledger 1 批,同小时窗多次并批不加码,始终 ≤2 批,**未近第 5 批锁**);开工另有 4 个
  `studyPlan` **storage 孤儿**(调试早期原因框未填→storage 已落/documents 未插)——**已带 stuA 身份清 0 并复核**。
- **收尾**:末次走查真按钮删除 + 带身份复核:**documents 活件 0 / `other` storage 合成孤儿 0(干净)**。
- **一处旧残留(非本轮,记账)**:更广的收尾复核发现 `recommendation` 槽 1 个 **storage 孤儿
  `…_r16-online-synth.png`(无 documents 行)——第十六轮 online-walk 的历史遗漏**,不在本包范围、未清(§0.9③ 删 storage 属敏感,
  且非「为继续本包所必需」)→ **列第十九轮 backlog:清一次历史 online-walk storage 孤儿**。

## 七、边界
- 未新建/改任何 migration、源码、云函数;未碰共用查看器;db/storage 只走带身份 REST(探针只读或清本人 test 残件)。
- `MH_WALK_NO_PUSH=1` 供离线自测(只写回执/截图不推);`MH_WALK_DEBUG=1` 供诊断(打印失败请求/console 尾,均无 token)。

---
**下一包 = 18-07**
