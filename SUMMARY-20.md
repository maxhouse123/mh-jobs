# SUMMARY-20 · 第二十轮收官（只修不加：水印冷启动 + 中介端护照名单块/卡片 + 学生端中文词典 + 脚本卫生）

> 全轮 5 包（20-00…20-04）已逐包推回执到 mh-jobs。主仓库 round-20 源码 **6 笔 commit 全部本地、未 push**（origin/main 仍在 round-19 尾 `6bd6c6e`）。watermark-doc v35 **改源码未部署**。等业主卡1153 处置（见文末 `PUSH_OK`）。

## 一、新版本 + md5
| 端 | 版本 | md5 | 状态 |
|---|---|---|---|
| student | **v399**（新） | `dd3a5bccc046270cf844f7ddbbc92a70` | 本地 commit，待推 |
| partner | **v168**（新，20-02 已出） | `f1a734ab7b99dc6ea15259757e29c985` | 本地 commit，待推 · **视觉件待业主肉眼验收** |
| reviewer | v45（不动） | `c2cf1f26cffee208cfba137b430b18a6` | 与基线一致 |
| admin | v218（不动） | `73b36f0a4d6c09891419a873ff4af013` | 与基线一致 |
| school | v234（不动） | `a2d2e593bc4694029d5a0ee07bc8332b` | 与基线一致 |
| 共用查看器 | mh-viewer-v2-1-1.js（不动） | `ee351485b3145e9ceb789caf4340802e` | 与基线一致 |

**云函数**：watermark-doc **v35**（源码 md5 `3eb5180ef6c0c729e6ff435e85955969`，v34 为 `adf9c70e…`）——**改源码不部署，待业主跑 `jobs/JOB-20-01/deploy-watermark.sh`**（JWT 校验保持开）。precheck-ai / admin-doc 一行未改。

## 二、每包一句话
- **20-00**：基线全等；6 探针——①实锤「本环境每请求即冷、实例从不复用」（12 连打 seq 恒 1、boot_age 0–1s）、prepare 前端不可调、⑥ 学生 zh 词典前提证伪（本就显中文）；复现 A/B 红、C 未复现（前提证伪）、D 红；三端 N1 按钮基线。
- **20-01**：watermark-doc v35——计时头补 seq/mem/cpu；boot 瘦身（模块级 base64 解码 2→0、顶层 import 6→3、285 KB 字体/logo + imagescript 全改惰性）；渲染数学零改、输出逐字节同 v34；交付 deploy + probe（§0.6 shell 卫生过）。
- **20-02**：中介端 v168——护照名单块「该在时在」（`_r2AnchorWarn` 补重画）/「该走时走」（删暂存件清 `_r2FileVerdicts`/`_r2Verdict` + 重画）；英文卡标题冒号按语言（半角）；`#_v167CardStack` 钉右上角零遮正文；partner1 真登 6 项红→绿、N7 几何全过。
- **20-03**：学生端 v399——`doc.studyProof` zh「学习证明」→「在读证明」（经 `I18N._sup.zh` 覆盖，对齐管理端 `ADM7_KINDS`/审核端/本端 `supDoc` 已在用的标准词，本端内部 outlier）；`doc.studyPlan` 已一致不动；en/ru/fr 不动；无 prepare（20-01 证伪）；四语真页面 ALL=GREEN。
- **20-04**：脚本卫生——`shell-lint.sh`（§0.6 三查，自身合规）扫出 `JOB-19-03/deploy-watermark.sh` 29/58 行红→`${B}：`/`${CODE}，` 全绿；`online-walk` 从 19-06 复制改卡片选择器 `#_v167CardStack ._v167-card` + 输出路径改 20-04；单探现役线上 绿8/红0/N4。

## 三、旧红 → 新绿 截图文件名（`~/mh-jobs/shots/20/`）
- **20-02（中介端 v168，业主推送前必看）**：`20-02/bug1-roster-visible-GREEN.png`（核对结果出 → 名单块 ≤1s 现）、`20-02/bug2-roster-gone-after-delete-GREEN.png`（删暂存件 → ≤1s 消失）、`20-02/bug3-en-halfwidth-colon-GREEN.png`（英文标题半角冒号）、`20-02/bug4-cardstack-clear-1280w-GREEN.png` + `20-02/bug4-cardstack-clear-390w-GREEN.png`（卡片零遮正文，桌面+手机宽）。均元素级、真实文字 mask、无 PII（CC 已肉眼抽查 bug1 = 合成件文案、无姓名/邮箱）。
- **20-00**：`20-00/` 探针/复现留证。
- **20-03**：无截图（纯 zh 词典取值，证据在 `jobs/JOB-20-03/verify-C-results.json` 四语 ALL=GREEN；业主可开 v399 zh 界面肉眼看「在读证明」）。

## 四、待业主手贴的 SQL 清单
- **无**。本轮 20-01…20-04 未写任何 SQL；迁移停在 0212，0213 仍空。

## 五、未放行清单（等业主动作，非本轮遗留缺陷）
1. **主仓库 round-20 六笔 commit 未推**（`6bd6c6e..c43d5f9`）→ 业主卡1153 推（认本文件 `PUSH_OK`）。
2. **watermark-doc v35 未部署** → 业主跑 `jobs/JOB-20-01/deploy-watermark.sh`（部署 + 探针，目标客户端中位 6.7s→3s 内；到不了则 probe 会写清差在哪段）。
3. **partner v168 视觉件未经业主本地肉眼验收**（铁律15 闸二：先验收后推送）→ 见「要业主亲眼看的」。
4. **student v399 术语改动待业主一票（可否决）**：`doc.studyProof` 学习证明→在读证明。CC 判为对齐本端 supDoc/管理端标准词的合理修正（P2 图纸内明文预授权映射），但属 live 标签改动，留业主确认；否决则撤 `_sup.zh` 一行即回「学习证明」。
5. **冷启动提速：保温不实施（§0.5 前提不成立，见第七节）**；prepare 前端架构不成立——均留第二十一轮。
6. **20-02 卡片落点诚实记录**：卡片浮在弹窗上沿的全屏半透明背景层（modal-bg）上、不压任何内容元素；§⑤ 点名五项重叠恒 0（见 JOB-20-02 回执）。

## 六、要业主亲眼看的
**A · 推送前必看（铁律15，视觉件本地肉眼验收）**
1. **中介端 v168**（看 `shots/20/20-02/` 五图，或本地开 v168 代传学生走一遍）：护照栏传姓名不一致护照 → 核对结果一出**名单块即现**（≤1s）；点缩略图红× 删 → **名单块随即消失**；英文界面卡标题冒号为半角「: 」；提醒卡片钉右上角**不盖**缩略图/提交按钮/Pre-check 框（桌面 1280 + 手机 390 各一眼）。
2. **学生端 v399**：中文界面材料上传两栏栏目名 =「在读证明」+「学习计划」（原 zh outlier「学习证明」已覆盖）；en/ru/fr 措辞不变。若认可，第五节 4 项即消。

**B · 部署后线上验收（watermark-doc v35）**
3. 部署后审核端「查看」任一图片/PDF：水印文字/位置/清晰度与部署前**肉眼一致**（v35 渲染数学零改、输出逐字节同 v34；本机无 Deno 无法本地跑 handler）。
4. 审核端控制台应出 `[v45] wm server … seq=… mem=…/…MB cpu=n/a`（v45 原样读头，自动多出 seq/mem，不需 reviewer v46）。
5. 跑 `jobs/JOB-20-01/wm-timing-probe.mjs` 看客户端中位是否进 3s（含 20-00① 的 12 连打）。

## 七、第二十一轮建议包
- **冷启动结论（实锤）**：本环境 watermark-doc **每请求即冷**——12 次连打（含 0.3s 间隔）seq 恒 1、`cold-n1-up0s`，实例**从不复用**。客户端中位 6.7s 里，~5s 是「函数开始干活之前」（冷启动 + 鉴权/权限门/取签名 URL 固定开销），render 中位仅 0.26–0.95s。**烧水印不是瓶颈，冷启动是。**
- **保温方案裁决 = 不做**：§0.5 触发前提是「实例因**空闲**被回收」；探针①证伪——是**每请求冷隔离**（非空闲超时回收），故定时保温 ping **无效**（连打 0.3s 都各自冷，没有可保温的常驻实例）。R21 若要提速冷启动，方向是 **① 部署 v35 后先量瘦身实效**（base64 2→0 / import 6→3，boot 应降；probe 出数）；**② 若仍 ≥ 目标，评估换持久运行时**（小型常驻服务/容器替代 Edge Function，或换平台），非前端 prepare、非保温 ping。
- **其它挂账**：student v399 术语若业主否决则回退登记；watermark-doc v35 部署后 probe 实效回填；20-04 `online-walk` 的 v167/v168 卡片选择器新值须待有现成 Q1 告警场景才量到样式（本轮无场景记 N/A）。
- **REJ-1 / 老观察项**（承前轮 backlog）：拒绝弹窗缺类别选择器（rejectCategory 恒传 null）、配额云化、vendor/tesseract LFS 评估——均非本轮范围，续挂。

PUSH_OK=yes

> 2026-09-24 盖章（卡1156）：PUSH_OK 由 no 改 yes。Claude 复查通过（指纹 / 语法 / 新旧逐行 / v35 惰性加载逐处核 await / 无头启动零新报错 / 卡片位置与颜色 / 清判定逻辑 / 脚本卫生零命中）；业主批准学生端「学习证明→在读证明」与中介端 v168 视觉改动（CC 真页面截图先看，线上再肉眼验收）。
