# JOB-11-02 · `signed_url` 模式的审计核查

结果：**缺审计 → 已补源码（未部署）** —— 审核端「命中缓存秒下」这条路原本不留审计，现补上，deno 本机没装故做结构自检，等业主用下方部署卡部署

## 大白话（业主看这段）
审核端打开材料时，如果水印预渲染件已在桶里（命中缓存），走的是 watermark-doc 的 `signed_url` 这条"秒下"路。核查发现：**这条路原来不写审计日志**（`reviewer_access_log`）——也就是说，凡是走缓存命中看的材料，**没有"谁在何时看了哪份"的记录**，将来查泄露会缺一段。原来的"现场渲染查看"那条路是有审计的，就这条缓存路漏了。

已补：在 `signed_url` 签发成功后，补写一条审计（和原查看分支同一张表、同样字段），outcome 记为 `served_cached`（缓存命中查看），溯源码 RV-XXXX 用和原分支**一模一样**的算法算。写审计是**响应后异步、失败也不挡查看**（fail-open），照抄文件里已有的写法。

**只动了这一个分支**；预渲染(prepare)/原查看(view)/清理(cleanup) 一个字没动。**没有部署**——现在线上还是旧版（缓存命中仍无审计），要等你用下面的部署卡部署后才生效。

## 断言表
| 断言 | 期望 | 实测 | 过 |
|---|---|---|---|
| signed_url 原来是否写审计 | — | **无** logAccess（只有 prepare@351 / view@525 / denied@545 有） | 确认缺 |
| 补后 signed_url 分支有 logAccess | 1 | 1 | ✅ |
| outcome 值 | served_cached | served_cached | ✅ |
| 字段与原分支一致 | reviewer_uid/rv_code/doc_id/file_kind/outcome | 一致 | ✅ |
| rv_code 算法与原分支一致 | SHA-256(uid) 前4字节→RV- | 一致 | ✅ |
| 异步 fail-open | EdgeRuntime.waitUntil + try/catch | 照抄 632-635 写法 | ✅ |
| 只动 signed_url 分支 | prepare/view/cleanup 零改 | 仅 handleSignedUrl 内新增 | ✅ |
| 语法结构自检（deno 未装） | 花括号/圆括号平衡 | 全文 {}=275/275；新增块 ()=26/26 | ✅ |
| 是否部署 | 否 | **未部署** | ✅（按卡「必须停」） |

## 补的源码（无密钥，照贴）
`supabase/functions/watermark-doc/index.ts` 的 `handleSignedUrl`，在签发成功 `return` 之前插入：
```ts
const _auditP = (async () => {
  try {
    const rvDigest = await crypto.subtle.digest("SHA-256", new TextEncoder().encode(uid));
    const rv = "RV-" + [...new Uint8Array(rvDigest)].map((b) => b.toString(16).padStart(2, "0")).join("").slice(0, 4).toUpperCase();
    await logAccess(admin, { reviewer_uid: uid, rv_code: rv, doc_id: docId, file_kind: ext === "pdf" ? "pdf" : "image", outcome: "served_cached" });
  } catch (_e) { /* 审计失败绝不阻断查看 */ }
})();
const _er = (globalThis as unknown as { EdgeRuntime?: { waitUntil?: (p: Promise<unknown>) => void } }).EdgeRuntime;
if (_er && typeof _er.waitUntil === "function") _er.waitUntil(_auditP); else await _auditP;
```

## 📋 部署卡（业主执行；EF 部署 = 上线，请在你方便时做）
watermark-doc **部署时 JWT 校验保持开**（现状即如此；prepare/cleanup 走 hook secret + Authorization，不受影响）。原生终端：
```
cd /Volumes/Dev/MAXHOUSE
# 需已 supabase login 且 link 到 maxhouse_main(uexgzwfambanvhdhxome)
supabase functions deploy watermark-doc --project-ref uexgzwfambanvhdhxome
```
部署后自验：审核端 v34 打开一份**命中缓存**的材料（console 出 `[v34] wm cache HIT`）→ 查 `reviewer_access_log` 应新增一行 `outcome='served_cached'`、`reviewer_uid=` 该审核员、`doc_id=` 该材料。deno 若在别处已装，部署前可先 `deno check supabase/functions/watermark-doc/index.ts`。

## 动过的文件
- 改 supabase/functions/watermark-doc/index.ts（仅 handleSignedUrl 补审计；**未部署**）
- 新增 jobs/JOB-11-02/report.md
