PUSH_OK=yes

# SUMMARY-23 · 第二十三轮收官（管理端左侧栏清整 · 只删只修不加）

> 本轮只改 `admin-portal/` 一端，v219 → v225 六个版本，每版四件套齐。零迁移、零手贴 SQL、零云函数改动、零部署；其它四端、共用查看器、`_headers` 一字未动。
> 主仓库 7 笔提交（JOB-23-00…06）全部 commit **未推**——按本轮铁律 CC 不推主仓库，推送由业主用卡1171 做（认本行 `PUSH_OK=yes`）。

## 一、版本 / md5 账

| 端 | 版本 | md5 | 状态 |
|---|---|---|---|
| admin | **v225**（本轮终版） | `b17b9f4f7912eb93d4c429815b7ddda9` | 本轮 6 版累积 |
| student | v400 | `19391aa80ac10cb7835ef50f989432b0` | **未动**（= 基线） |
| partner | v170 | `afe53f4be41237a5c36eb880914e918e` | **未动**（= 基线） |
| school | v235 | `f77dcba686c3b5e6d0dd005c796b7435` | **未动**（= 基线） |
| reviewer | v46 | `2d4269e9c570481b99b95d70a3691082` | **未动**（= 基线） |

四端 md5 与 QUEUE-23 §0.1 基线逐字节一致 → 本轮确实只碰 admin 一端。

## 二、六包各一句话

| 包 | 版本 | 做了什么 | 闸 |
|---|---|---|---|
| 23-00 | —（只读） | 开工核对 + v219 按钮清单(N1 基线) + 桩数据 + 七处旧病先红 | R1–R7 全红、0 报错 |
| 23-01 | v220 | 摘 3 个假/占位入口（推荐归因/数据洞察/材料范例库）+ 去左下角「账户设置」占位改填真登录邮箱 + 五处假「导出」改真 CSV 下载 | 静 28/28 + 无头 8/8 |
| 23-02 | v221 | 录取通知分发去种子行 + 截止日期监控并入「即将到期」标签页 + 侧栏摘 deadlines 入口 | 静 35/35 + 无头 |
| 23-03 | v222 | 审计事件人话（标题复用 41 类词表 + 动态筹码 + 详情键值小标签 + 原始 JSON 折叠） | 静 27/27 + 无头 |
| 23-04 | v223 | 学校行为日志默认不看取件（717→27）+ 取件行反查 documents 补学生名 | 静 26/26 + 无头 5/5 |
| 23-05 | v224 | 任务审批/信息更正补侧栏角标 + 三材料队列加「隐藏测试」筹码 | 静 30/30 + 无头 9/9 |
| 23-06 | v225 | 左栏按 7 组重排（纯搬家，27 项 .nav-item 块字节一致） | 静 35/35 + 无头 8/8 |

## 三、闸账

### ① 真页面旧病 红 → 绿（R1–R7 逐条）
| 旧病（23-00 量红） | 转绿包 | 绿的证据 |
|---|---|---|
| R1 推荐归因页 hero「¥4,800」+ demo banner | 23-01 | 入口已摘，左栏点不到 |
| R2 导出无 download、只弹 toast「已生成」 | 23-01 | 学生页点导出真下载 CSV，头 EF BB BF，行数=非测试学生数，表头含「申请号/Application No」 |
| R3 录取通知分发/截止监控混种子行 + 演示分隔线/横幅 | 23-02 | 分发正文=真 6 条、「即将到期」正文=真 2 条且有「三天内」分段，无分隔线 |
| R4 审计事件裸英文码 + 正文裸 JSON | 23-03 | 每行标题人话（visa_valid_until_set→设置签证有效期/Visa validity set），正文无裸 JSON、原始内容折进 details |
| R5 学校行为日志 fetch 淹没 + 学生列「—」 | 23-04 | 默认 27 行无 fetch，点取件才 690；取件行补学生名（stub 全补上；线上视策略，见未放行第 3 条） |
| R6 侧栏 `#navBadgeTaskApproval`/`#navBadgeCorrections` 不存在 | 23-05 | 两角标在，桩后 2 / 3 |
| R7 三队列无「隐藏测试」筹码、测试件在列 | 23-05 | 三队列各加筹码默认按下，页顶「已隐藏测试 N 份」，关掉测试件出现 |

### ② N1 有意移除清单（对照 QUEUE-23 §0.3，全轮共 4 项，全部授权内）
| 移除项 | §0.3 授权 | 包 | 保留物 |
|---|---|---|---|
| 侧栏 `data-view="attribution"` nav-item（1 inline onclick） | 第 1 项 | 23-01 | #view-attribution/renderAttribution/注册/nav.attribution 键 |
| 侧栏 `data-view="analytics"` nav-item（1 inline onclick） | 第 1 项 | 23-01 | #view-analytics/renderAnalytics/注册/nav.analytics 键 |
| 侧栏 `data-view="samples"` nav-item（1 inline onclick） | 第 1 项 | 23-01 | #view-samples/renderSamples/注册/nav.samples 键 |
| `.sidebar-user` 的「账户设置(敬请期待)」toast onclick | 第 2 项 | 23-01 | 登录后填真邮箱 |
| 侧栏 `data-view="deadlines"` nav-item（1 inline onclick + id `#navBadgeDeadlines`） | 第 4 项 | 23-02 | #view-deadlines/renderDeadlines(改调 _v221DeadlinesBody)/注册/nav 键 |
| `doExport(kind)` 函数体改写（§0.3 第 3 项，**非移除**） | 第 3 项 | 23-01 | 函数名保留，改真导出 |

23-03 / 23-04 / 23-05 / 23-06 **零移除**（差集空）。全轮所有函数、数组（ATTRIBUTIONS/LETTERS/DEADLINES/STANDARD_SAMPLES_ADMIN/PARTNERS…）、视图容器、VIEW_RENDERERS 注册项原样保留——「演示车道：销债不销码」。

### ③ P3 静态断言通过数
28/28（23-01）· 35/35（23-02）· 27/27（23-03）· 26/26（23-04）· 30/30（23-05）· 35/35（23-06）。全绿。
> 注：23-04/05 的 `index.html 三处指向 v223/v224` 断言在 23-06 后转为陈旧失败（index.html 永远跟最新版 v225）——属预期，非回归。

### ④ 无头真渲染（闸8）
各包中英各一遍、零 pageerror、零裸键；23-06 另量 390px offcanvas 滚到底、末项「发公告」可见可点。

### ⑤ shell-lint
六包均无 shell 脚本（构建/验证/截图全 node .mjs）→ 无需 lint。

### ⑥ secret-scan
每包改动文件全扫，无 service_role / JWT / 私钥 / 明文密码 → clean。（23-02 唯一命中是变更说明里「service_role」词，描述性文字非密钥。）

## 四、截图索引
`~/mh-jobs/shots/23/`：`23-01/`（导出 CSV + 侧栏摘除）·`23-02/`（分发/到期筹码）·`23-03/`（审计人话）·`23-04/`（日志默认不看取件）·`23-05/`（两新角标 + 隐藏测试）·`23-06/`（`sidebar-7groups.png` 全 7 组 + `sidebar-390-bottom.png` 手机末项）。各配 INDEX.md，元素级、登录邮箱/PII 打码，已过 secret-scan。

## 五、未放行清单（留业主线上肉眼验收 / 待确认）
1. **视觉件线上验收**：左栏 7 组排布、两个新角标（任务审批/信息更正待办数）、五处导出真下载 CSV、审计事件人话——本地 stub file:// 全绿，但「长得对不对/线上真登数值对不对」只有业主线上肉眼能判（闸15 双闸）。CC 提交停在「已提交未推送」。
2. **任务审批角标线上值**：该角标走 RPC `get_admin_pending_tasks`，需真登录 uid；本地 stub 无真会话 → 首屏早退显「—」（既有 uid 依赖、非本轮引入），注入模拟 uid 后逻辑证实=缓存长度=2。线上真登有会话，应自然显示真实待批数（线上两个 locked 任务应为 2）——留业主一眼确认。
3. **23-04 取件行学生名的 documents 读策略**：本地 stub 反查通（690 行全补名）；线上是否通取决于 admin 账号对 `documents` 表的读策略。若线上取件行显真名→策略够；若显「—」→ admin 读 documents 策略不足，需新增一条 admin 只读策略（新增型 DB 操作），建议并入第二十四轮由业主确认后做。本轮按边界「不写库」未动 DB。

## 六、要业主亲眼看的 7 条（各包业主验收汇总）
1. 侧栏没有推荐归因 / 数据洞察 / 材料范例库；点左下角显示你的登录邮箱、不弹「敬请期待」；学生页点顶栏下载图标真下载一个 CSV，Numbers 打开列数与页面表头对得上。（23-01）
2. 录取通知分发页没有「演示」分隔线与假学生行；页顶两枚筹码，点「即将到期」看到到期分段；侧栏没有「截止日期监控」。（23-02）
3. 审计事件每行标题是中文；下面是「申请号 / 轮次 / 有效期」这样的小标签；点「详情」才看原始内容；上方筹码按类别筛得动。（23-03）
4. 进「学校行为日志」默认只看到导出 / 下载 / 查看，点「取件」才看水印流水；取件行显示学生名（策略不足则显「—」，见未放行第 3 条）。（23-04）
5. 侧栏「任务审批」显示待批数、「信息更正」显示待审数；材料派单 / 材料审核 / 待二审页顶有「隐藏测试」筹码默认按下，关掉后测试件出现、页顶「已隐藏测试 N 份」。（23-05）
6. 左栏变成 7 组、顺序为 总览 / 审核台 / 待我裁决 / 录取与服务 / 档案 / 日志 / 设置与增长；每项点开还是原来的页。（23-06）
7. 手机宽度（≤768px）点汉堡打开侧栏，能一路滚到底，「发公告」可见可点。（23-06）

## 七、第二十四轮建议包（五项必含 + 附加）
1. **顾问台需求落库图纸**：迁移 `submit_concierge_brief(p_order_id, p_brief jsonb)`（或 `create_payment_order` 加 `p_meta jsonb`）；学生端 v400 `_conciergePendingPayment` 在 `_pay0Start('concierge', …)` 前后把需求送上云；中介端人工找校同法；管理端顾问台行显示需求。（QUEUE-23 §23-07 ①；写卡 Claude 已核 renderConcierge 只读现有列，学生需求从未落库）
2. **数据洞察真版**：学生增长曲线 + 注册→提交→有 offer→付款→入学 真漏斗，全部从首屏缓存算（替代 v220 摘除的写死洞察页；容器/渲染器已在，销债不销码，接真数据即复活）。
3. **材料范例库云端版**：先问审核员要不要（原 samples 读本机缓存已摘，容器保留）。
4. **门禁页横幅可见（低）**：管理端在 CF Access 后，「有新版请刷新」横幅取不到 index 时静默——确认线上是否需要一个可见提示。
5. **择期观察项**：`vendor/tesseract` 39MB 评估（LFS?）；`withdraw_referral` 线上 400 复现定性。
6. **附加（本轮浮现）**：23-04 若线上 admin 读 documents 策略不足 → 新增一条 admin 只读策略补齐取件行学生名（新增型 DB，业主确认后做）。

## 八、STOP
本轮**无** STOP。`~/mh-verify/r23-STOP.txt` 不存在（已核）。
