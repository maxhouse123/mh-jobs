# JOB-9-07 · 学生端 ?debug=1 手机控制台（eruda 自托管）— 回执

**大白话结论**：你在外面用手机测网站、旁边没 Mac 时，现在可以在网址后面加 `?debug=1` 打开一个手机版的「开发者控制台」（能看 console 日志、网络请求等），右下角会冒一个小工具浮标。平时（不加这个参数）**什么都不加载、零开销、普通学生永远看不到**。用的是**自托管**的 eruda（下载到我们自己 `assets/vendor/`，不走任何 CDN，守自托管铁律）。学生端版本 **v390 → v391**（只加不改，主文件只多了 720 字节）。

**业主验收**：手机打开 `…/student-portal/?debug=1` → 右下角出现小工具浮标（点开是控制台）。不加 `?debug=1` 则一切照旧、看不到任何东西。（也可在 Console 里 `localStorage.setItem('mh_debug','1')` 后刷新长期开启，改回 `'0'` 或删掉即关。）

## 做了什么
- **自托管 eruda**：`assets/vendor/eruda.min.js`（npm 包 `eruda` 最新版 **v3.4.3**，从 unpkg 下载）。
  - **md5 = `5a2e24acb1211d777723b3a57f9be8d5`**，大小 499,928 字节，文件头 `/*! eruda v3.4.3 https://eruda.liriliri.io/ */`。
- **门控懒加载**（student v391 末尾一段 IIFE，`</body>` 前）：仅当 URL 带 `?debug=1` **或** `localStorage.mh_debug==='1'` 时，才 `createElement('script')` 加载 `../assets/vendor/eruda.min.js`，`onload` 里 `eruda.init()`（去重，不重复 init）。默认 `if (!dbg) return;` 直接退出，**零加载**。

## 断言表
| 闸 | 断言 | 实测 | 过? |
|---|---|---|---|
| 闸3 静态 | 真正「加载」eruda 的引用恰 1 处，且在 `if (!dbg) return` 之后（受 debug 门控） | loadRefs=1, retIdx=341 < srcIdx=410 | ✅ |
| 闸8 ① | 无参数 → 不加载（无 `#eruda` / 无 eruda 脚本 / 无 `window.eruda`） | 全 false | ✅ |
| 闸8 ② | `?debug=1` → 加载 eruda 脚本(200) 且出现 `#eruda` 容器 | erudaEl=true, scriptAdded=true | ✅ |
| 闸8 ③ | `localStorage.mh_debug='1'`(无参数) → 也加载出 `#eruda` | erudaEl=true | ✅ |
| — | 主文件增量 < 2KB | **720 字节** | ✅ |
| — | 版本四件套（文件名/[VER]/index 4处）同版 v391 | 一致 | ✅ |
| — | v391 语法 | 7 块，仅 BLOCK 1 = 工具把注释里 "cloudflare" 误判(基底同报)，我新增块 0 错 | ✅ |
| — | 自托管（不用 CDN） | eruda 落 `assets/vendor/`，`s.src='../assets/vendor/eruda.min.js'` 同源 | ✅ |

（`tests/job9-07.mjs`：本地 http 真加载 `../assets/vendor/eruda.min.js`，真出 `#eruda`；`#eruda` 初始不可见，故断言用 `state:'attached'`。全 4/4。）

## 边界
- **只加不改**：仅在 `</body>` 前加一段门控 IIFE + 一个自托管 JS，未动任何现有功能/UI/其余四端。
- 增量 720B（< 2KB）。默认零加载零开销。

## 动过的文件
- `assets/vendor/eruda.min.js`（新，自托管 v3.4.3，入 git）
- `student-portal/maxhouse_student_portal_v391.html`（新，旧 v390 保留）+ `student-portal/index.html`（重定向 v390→v391）
- `tests/job9-07.mjs`（随卡入仓）
- 已 commit `3286d6f`（未推）。
