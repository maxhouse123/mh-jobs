# JOB-17-05 · E + G③ · precheck-ai v22 → v23（改源码不部署）

本包是云函数源码包：不动任何端口 HTML、不动库、不上传材料、不写库、不部署、不推主仓库。
主仓库本地 commit = `5f8b3bc`（未推）；v23 md5 = `654fcbb5e565ea9a6dda0c68179d476b`（v22 基线 `1c4ce932…` 开工核对一致）。

## 一、做了什么

只动 `supabase/functions/precheck-ai/index.ts` 两处，其余（secrets / 判定核心 / 批处理 / 归属校验 / 软删 410 / 三级回落 / CORS / 分流顺序）一字不动。

### E —— 英文界面下 AI 提醒不再是中文
- `buildPrompt(docType)` → `buildPrompt(docType, lang)`。新增小表 `ISSUE_LANG_NAME = {zh:中文, en:English, ru:русский, fr:français}` + `issueLangName(lang)`（未知语言回落 zh）。
- 只改「issues 用哪种语言写」：
  - 指令那一句 `issues里每个问题用一句中文描述` → `…用一句 <L> 描述`；
  - 同一处的 JSON 样例占位符 `"issues":["中文简短问题描述"]` → `"issues":["<L>简短问题描述"]`（不改则样例仍把模型引向中文，与指令冲突）。
  - 判定标准 1–5、full_page/readable/glare/type_match/verdict 的措辞与其余提示词逐字未动。
- `processDoc` 加末位可选参数 `lang`（默认 zh），把 `lang` 传进 `buildPrompt` 并把「实际用语言」写进行的 `lang` 列。
  - 按需路 `runOndemand`、草稿路 `runDraft` 把请求 `lang` 传进 `processDoc`；**定时路 `runCron` 显式传 `"zh"`**（定时路仍按 zh）。
- 缓存按 lang 生效（依 JOB-17-00 探针⑤：shadow 唯一键 `doc_id,model`，draft 唯一键 `path,model`，lang 不在唯一键内）：
  - 两路缓存查询各 `select` 补 `lang`；命中条件加 `&& String(hit.lang||"")===lang`；
  - lang 不同 = 未命中 → 按请求语言重判一次 → upsert 覆盖同一唯一键（lang 列随之改成新语言）。

### G③ —— 按需路限频改「按触发人计」
- 旧：近 1h `ai_precheck_shadow.student_id==uid`（= 按材料属主计）→ 429。
- 新：近 1h `raw->>trigger_uid == 登录人uid` 且 `raw->>triggered_at >= now-1h` 的行数 ≥ 30 → 429，**不论材料属谁**。
- 触发人与触发时间只记在**按需路真去调模型的写入行**里：`row.raw.trigger_uid = uid`、`row.raw.triggered_at = 本次 ISO 时间`（**不加列**，塞进现有 `raw` jsonb）。
  - 换语言重判会走 `need` → 覆盖同一行 → `triggered_at` 一并更新为本次时间 → 窗口不可靠来回切语言绕过。
  - 缓存命中不写、不计；定时路写的行无 `trigger_uid` → 天然不计。
- 计数查询：`.select("id",{count:'exact',head:true}).eq("raw->>trigger_uid",uid).gte("raw->>triggered_at",since)`。

### 交付物（不入主仓库，随本回执留 `jobs/JOB-17-05/`，业主在同一工作树跑）
- `jobs/JOB-17-05/matrix.cjs` —— N4 本地矩阵（注入 svc + processDoc，不连库、不调 AI、不碰 token；用 `mod.stripTypeScriptTypes` 载入真源码，等价一次本地编译）。
- `jobs/JOB-17-05/deploy-precheck.sh` —— 部署（`--no-verify-jwt`）+ 自带探针（业主卡1136 跑）。公钥优先读环境变量 `SUPABASE_ANON_KEY`，没有再向 supabase CLI 取。

## 二、结果是什么

### N4 本地矩阵（`node jobs/JOB-17-05/matrix.cjs`，末行）
```
ALL-AS-EXPECTED 21 checks
```
逐格全 `AS-EXPECTED`，含：
- 分流：OPTIONS→204 / GET→405(v:23) / 无凭据→401 / 定时钥匙→cron200(v:23) / 公开钥匙冒充→401 bad_token。
- 归属：stuA 本人→user路 / 他人件→403 not_yours / 中介推荐生→user路 / 中介无关件→403 not_yours / 有效委托→user路 / 已软删→410。
- **G③**：stuA 触发满 30→429；**中介给「别人的学生」触发满 30 也→429**；限频过滤器实证 = `raw->>trigger_uid(uid)` + `raw->>triggered_at(1h)`，且**不再按 student_id**。
- **E**：lang 相同→命中缓存 `cached:true`（不调模型）；lang 不同（zh 缓存、请求 en）→未命中、按请求语言重判 `cached:false`；四语 `buildPrompt` 的「issues 那一句」+样例占位符各带对应语言名（中文/English/русский/français）；未知语言回落 zh。

### 真页面红绿（N2）
- **红**：JOB-17-00 已在真页面复现 E —— stuA 对名下已缓存中文的 8bc0486b 按需预检 `lang=en` → issues 是中文（`ISSUES-CONTAIN-CJK=true` = RED）。
- **绿**：本包只改源码不部署，绿灯客观上要等云函数上线后才出 —— 在**卡1136 的部署探针**（`lang=en` 按需判定 → issues 无中日韩字符）与 **17-07 线上走查**里出，本包不据此放行。

## 三、业主要做什么
- 本包**已本地 commit（`5f8b3bc`）未推**；推送等收官后卡1135（按 SUMMARY-17 的 PUSH_OK）。
- **卡1136**：挂代理、`supabase login` 后，可 `export SUPABASE_ANON_KEY=<student-portal 里的 sb_publishable_ 那串>`，再跑：
  `bash /Volumes/Dev/MAXHOUSE/jobs/JOB-17-05/deploy-precheck.sh`
  期望末行 `✅ precheck-ai v23 部署 + 探针全过。`，其中 `lang=en` 按需判定回来的 issues 打印出来且无中日韩字符。
- **要业主亲眼看的**（进 SUMMARY 清单）：把学生端界面切成 English，对一份会报问题的护照/成绩单图触发预检 → 弹出的 AI 提醒是英文（不再是中文）；切 РУ/FR 同理跟着变。（此项须 v23 上线后在线上看。）

## 四、新闸证据（N1–N6）
- **N1（按钮清单差集）**：本包是云函数源码，**无端口升版、无按钮/UI**，N1 不适用；端口 HTML 一个字节未动（`git status` 只有 `supabase/functions/precheck-ai/index.ts` 一个 M）。
- **N2**：见上「真页面红绿」——红已在 17-00 真页面出；绿依赖上线，写明在卡1136/17-07 出，本包不放行绿。
- **N3（带身份）**：限频与归属全按登录人 `uid`；矩阵用 stuA/partner1/委托/管理员各自 uid 走真分支；真实身份（密码登录拿 JWT）在卡1136 部署探针里验（脚本只读 accounts.json、不打印 token）。
- **N4（每条鉴权/分支各验一次）**：21 格矩阵，含新增的 G③ 触发人限频（本人+中介给别人的学生）、E 的 lang 命中/重判/四语提示词，全 `AS-EXPECTED`。部署后真验在卡1136。
- **N5（secret-scan）**：`node jobs/JOB-16-00/secret-scan.mjs supabase/functions/precheck-ai/index.ts jobs/JOB-17-05/` → `SECRET-SCAN-CLEAN`；push 本回执前对 `~/mh-jobs` 全树再扫一遍。矩阵/部署脚本只读密码、不打印 token（deploy 脚本里 JWT 只进变量、绝不 echo）。
- **N6（只修不加）**：只做图纸写明的 E + G③，未加新活；AI prompt 除「issues 用什么语言写那一句」外一字未动，瘦身参数/三级回落/派单/邀请邮件/其它两只云函数均未碰。
  - 点名一处必要的连带改动：**定时路 `runCron` 现在给 shadow 行写 `lang="zh"`（原为 null）**——这是「写入 lang 列=实际用语言」的诚实值，且是 lang 感知缓存正确所必需（否则 zh 请求命不中 cron 产的旧行）；cron 的提示词语言本就是 zh，判定行为不变。
  - 提示业主：**库里现存 lang=null 的旧 shadow 行（探针⑤：34 行）**，首次被某语言的按需请求命中时会「按请求语言重判一次并覆盖」（符合图纸「不同就重判一次」），可能出现一小波重判，属预期、非 bug。

## 五、动过的槽 / 测试写库
本包**零材料上传、零测试派单、零 §0.11 批次占用、零 DB 写、零 AI 调用、零 token 接触**：矩阵全用注入桩在本地跑。无残留测试件，无需收尾软删。

下一包 = 17-06
