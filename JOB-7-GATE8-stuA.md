# JOB-7 · stuA 真账号闸 8 验证 + 7-05 病根修补 — 回执

**你把 stuA 密码给了之后，我按你本轮明确授权跑了真登+真传。结果：真登成功、7-03a 真上传成功、7-05 真数据把一个我之前没发现的病根暴露出来——我当场修好并再验证通过。密码全程只走环境变量、没落任何文件；测试脚本放在仓库外 `~/mh-verify`（邮箱不入库）。**

## 一句话
真机验证很值——它证明了「上传签名直传真进度」在生产环境真能跑（不是偷偷回落），也当场抓出「YOU'RE ALL SET 卡申请号」我上一版修得不彻底、其实还是错的，现在真数据实测已显示正确的 **MH-6KG2JN**。

## 真跑了什么（生产 Supabase，本地 127.0.0.1 载页 → 真连云）
| 项 | 结果 | 证据 |
|---|---|---|
| stuA 真登（`maxhouseapp+stuA@gmail.com`）| ✅ 成功 | `auth.getUser()` 返回真用户 |
| **7-03a 真传 2MB 图** | ✅ 成功 | 走 `_v384.upload` 的**签名直传+XHR 真进度路径**，`maxFrac=1`、途中 `sawMid=true`（真进度回调在跑，**不是静默回落**）；传完**已自动删除**测试文件，不留垃圾 |
| **7-05 成功卡申请号** | ✅ 修后正确 | stuA 真数据：2 轮（第2轮 MH-9F4AG7 在审 / 第1轮 MH-6KG2JN 已入学），offer 归属第1轮；解析结果 **MH-6KG2JN · 第 1 轮**（修前错显 MH-9F4AG7） |

## 7-05 病根（真机才暴露，诚实交代）
- 我在 v387 用「时间窗」兜底（因为 `get_my_offers` 不吐 round_id）。但我把轮次起点写成了 `opened_at / created_at / started_at`——**这三列 `get_my_rounds` 一个都没有**（它的真列是 `submitted_at`）。结果起点恒为 null → 时间窗匹配到列表第一个（当前在审轮）→ **和原 bug 一模一样的错**。
- **stuA 真数据一跑就现形**：v387/v388 解析出 MH-9F4AG7（错），不是 MH-6KG2JN。
- **v389 修法**：时间窗起点改用真列 `submitted_at`；再加兜底「已确认/完成的 offer 必属已结束的轮 → 优先取 accepted/enrolled>0 或最近已关闭的轮，绝不用当前在审开轮」。
  - 真数据核对：offer 下发 09-11 01:45 落在第1轮 [提交 09-07, 关闭 09-11 13:46] 窗内，且早于第2轮提交时间 09-11 13:48 → 自然归第1轮 → **MH-6KG2JN**。✅
- **数据血缘补记**：`offer_decisions.round_id` 真有值且真指第1轮（我查库确认 `is_offer_round=t`）。所以**最稳的修法仍是让 `get_my_offers` 吐 round_id**（直配，免时间窗）——那要 DROP+CREATE 函数（破坏性），你说本轮不做、下轮开例外。**时间窗（修正列名后）现在真数据够用**，直配留作下轮加固。（已写进 SUMMARY-7 待办。）

## 闸 8 结论
- 7-01：sw+manifest 走查 9/9（早前）。
- **7-03a**：真机 2MB 上传成功 + 真进度 + 回落红线（stub 已验）→ **闸 8 达标**。失败红卡+重试用 stub 验过（拦 500）；真机没主动制造失败以免污染。
- 7-03b（TUS 断网续传）：真「断网 3s 再恢复看 Upload-Offset」需在**线上部署版**跑（本地 127.0.0.1 也能打 TUS 端点，但断网续传时序更适合线上真验），留你部署后我补；决策/回落/开关 stub 已 5/5。
- **7-05**：真数据实测 **MH-6KG2JN**（修后）→ **闸 8 达标**。

## 版本 / 文件
- 学生端 v388 → **v389**（仅 7-05 修补；index 指针同步）。零迁移、零破坏性 DB 操作。
- 只读探针：`jobs/JOB-7-05/probe2..4.sql`（查真列名 + 确认 offer.round_id 归属）。
- 真验脚本：`~/mh-verify/gate8_job7.mjs`（仓库外、不提交；密码走 env、邮箱不入库）。

**主仓库未推。本回执已推日志仓库。**
