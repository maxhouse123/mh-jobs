# JOB-24-07 回执 · 顾问台「需求落库」（迁移 0215 + 学生端 v401 + 管理端 v232）

做的人：CC（无人值守，全新会话）。做完这一包就停，下一包 = 24-08 收官。

## 一句话
学生买「人工辅助选校 ¥600」时填的**需求 / 城市 / 资金 / 截止日 / 附件名**，以前只留在学生自己手机的内存里，付款走订单流程时没带过去，所以管理端「Concierge 顾问台」只看得到金额和状态、**看不到学生到底想要什么**。这一包把这些需求**存进云端工单**，管理端顾问台每一行都能看到需求原文了。

## 具体做了三件事

### 1) 数据库迁移 0215（只写文件，没有真执行）
- 文件：`supabase/migrations/0215_concierge_brief.sql`（同一份也放进 `docs/supabase 文件/0215_concierge_brief.sql`）。
- 内容：给云端的顾问工单表加两列（需求全文 `brief`、提交时间 `brief_at`），再加一个「学生自己写入需求」的安全函数 `submit_concierge_brief`。
- **没有真的往数据库执行**。CC 只用 `bash jobs/db-run.sh jobs/JOB-24-07/0215-probe.sql` 跑了「开事务→建好→验一遍→回滚」的探针，跑完数据库一点没变。探针结果：两列建成（cols=2）、函数签名对（`submit_concierge_brief(p_brief jsonb)`）、模拟写一行需求成功（+1）、回滚归零、退出码 0。**真正执行要业主贴 SQL**（见文末）。

### 2) 学生端 v401（v400 → v401）
- 学生点付款那一刻，先把填好的需求**存到自己手机本地**（防丢），再顺手往云端提交一次。
- 云端工单是「管理员核销付款后才生成」的，所以付款当下往往还没有工单可写 → 提交会被云端挡回（或迁移还没贴时函数不存在）。这两种情况都**不报错、不卡壳**：需求留在本地，**下次学生打开 App 首屏时自动补交一次**，补交成功就把本地那份清掉。
- 附件**只记文件名和大小**，不上传文件本身。
- 已提交的需求，学生在原有的「服务工单」卡片里就能看到（沿用现成的显示，没有新做界面）。

### 3) 管理端 v232（v231 → v232）
- 顾问台读工单时多读那两列（需求、提交时间）；**万一迁移还没贴、这两列不存在**，会自动退回读老字段、控制台提示一句「可能迁移 0215 未应用」，**页面照常显示、不报错、不空白**，那一行显示「学生未填需求」。
- 顾问台每条「人工辅助选校」工单下面新增一段：**需求原文（自动折行）· 城市 · 资金（翻成人话：可自费 / 需全额奖学金 等）· 截止 · 附件名列表 · 需求提交时间**。没填需求的行显示「学生未填需求」。

### 中介端 v170：不动（不需要）
按图纸要求查过：中介端**没有**把顾问需求放在内存里的代码（`PendingPayment` 搜到 0 处，`concierge` 只出现在共用付款弹窗的多语文案里，没有收集需求的表单）。顾问需求只在学生端收集，所以**中介端这一包不用改，保持 v170**。（符合 §0.6「不是必须停」）

## 版本四件套（都齐）
- 学生端：文件名 v401、头部 [VER] 探针改 v401、`MH_PORTAL_VER=401`、`student-portal/index.html` 4 处重定向、changelog 追一行。
- 管理端：文件名 v232、头注新增一段、运行时 [VER] 探针改 v232、`MH_PORTAL_VER=232`、`admin-portal/index.html` 3 处重定向、changelog 追一行。
- 学校端 / 审核端 / 共用查看器 / `_headers` / 云函数：一律没碰。

## 闸账（都过）
- **静态断言 + 无头真渲染**（本地 file:// + 桩 window.sb，中英各一遍）：
  - 学生端 `tests/job24-07-student-v401.mjs`：**37/37 全绿**。证到：付款成功桩 → 云端函数被调、参数五键齐（需求/城市/资金/截止/附件）、附件只 {名,大小}、成功后清本地；函数报错桩 → 本地存了待提交需求、首屏只补交一次（守卫生效）、全程零报错。
  - 管理端 `tests/job24-07-admin-v232.mjs`：**44/44 全绿**。证到：有需求的行显示需求原文/城市/资金人话/截止/附件名/提交时间；无需求的行显「学生未填需求」；**模拟「列不存在」时页面照常、零新增报错**。
- **N1 零移除**：没删任何入口/函数/数组/容器，只加代码、只加一段显示。移除数 = 0。
- **闸9 视觉零新增**：没加任何新颜色 / 字号 / 样式类（复用现成的 `.queue-row / .dd-row-sub / .cell-meta / var(--ink-soft)`）。已用脚本比对 v231↔v232、v400↔v401 的 CSS 选择器，**无新增**。
- **secret-scan**：改动文件里没有任何密钥 / JWT / 数据库连接串（唯一命中的 "service_role" 字样是一条老版本注释里的说明文字，不是密钥）。
- **旧病先红后绿**：本包是新增功能（R6 一类「需求看不到」），管理端桩里 CR-1 有需求→绿显、CR-3 无需求→显「学生未填需求」，红→绿成立。

## 要业主留意的几条
1. **jobs/db-run.sh 本机原先不见了**（跑数据库探针的本地小工具，历轮一直是「本机专用、不入库」）。CC 按历轮日志格式重建了一份、只用来跑「回滚探针」、只读 `.env` 里的连接串、不打印密钥、**没有入库**（沿用历轮惯例）。这是个反复出现的小坑（换机 / 清理后就没了），业主若愿意可考虑把它入库省事，但 CC 没自作主张改惯例。
2. **旧的几个测试脚本（job24-02/03/05/06）现在各有一条 `S4 index 三处` 变红**——这是**版本号往前走的正常现象**：`admin-portal/index.html` 现在指向 v232，旧版本的「index 还指向我」这条断言自然不成立了。已逐一核对：这几个脚本**只**红在这一条 index 断言上，没有任何一条业务/桩断言变红，即我这一包**没有碰坏它们的功能**。
3. **学生端线上真登验证留业主**：按图纸第 5 点 + 铁律 11（真登录 / 涉真库变更留业主验收），CC 没有自行拿测试账号线上真登去买顾问服务（那会往数据库真写）。「迁移未贴时前端不报错、需求进本机待重试」这条，**已由上面的无头桩测试确凿证明**；线上真登与贴迁移后的完整验证见文末验收步骤。

## 待业主手贴的 SQL（0215）
文件：`supabase/migrations/0215_concierge_brief.sql`（内容见 24-08 收官附录，或直接打开该文件）。
贴法：Supabase 网页 SQL Editor 整段贴、执行。
贴完验证：
1. 用测试学生登录学生端 → 买一次「人工辅助选校 ¥600」→ 按提示填需求（城市 / 资金 / 截止 / 附件）→ 完成付款并等管理员核销（核销后云端才生成工单）。
2. 学生端下次首屏会自动把需求补交上去（本地存的那份）。
3. 管理端进「Concierge 顾问台」→ 该学生那一行应能看到**需求原文 · 城市 · 资金 · 截止 · 附件名 · 提交时间**。
4. 贴之前（现在这样）：学生端买顾问服务**不会报错**，需求先存在手机本地等着补交；管理端顾问台该行显示「学生未填需求」。

## 主仓库提交
本包在主仓库一笔 commit（**未 push**，按规矩推送留收官后业主做）。`~/mh-verify/r24-STOP.txt` **不存在**（本包无「必须停」）。

下一包 = 24-08
