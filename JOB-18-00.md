# JOB-18-00 · 第十八轮开工检查 + 探针 + 旧病复现（先红）+ 三端按钮清单

CC 无人值守。本包只读侦察 + 先红复现 + 基线清单，零代码改动、零迁移执行、零上传、零写库（除下述只读下载/审核端本职读件外）。

## 一、做了什么

1. **基线核对（QUEUE-18 §0.3 / §0.9）——全绿**
   - `git rev-parse HEAD` = `5c83655f8750a42e671403888feb208755fc5997`，落 `jobs/JOB-18-00/base-head.txt`。
   - `git fetch` 后 `origin/main` = 同一笔；`git status --porcelain --untracked-files=no` 为空。
   - 五端 + 查看器 md5 逐个实测，与 SUMMARY-17 表**逐字相符**：
     admin v217 `a2e15910…` / student v397 `6e78713b…` / school v234 `a2d2e593…` / reviewer v42 `68d136ee…` / partner v165 `87ded571…` / `assets/mh-viewer-v2-1-1.js` `ee351485…`。
     云函数 `watermark-doc/index.ts` `55debf4d…`（= 基线）。
   - `~/mh-verify/accounts.json` 在、权限 600；stuA / partner1 / reviewer1 **三个真登全 OK**（走 GoTrue password grant，不打印任何密码/token）。
   - 结论：**未触发任何「必须停」**。

2. **探针①～⑦（只读，带身份 N3）** — 证据在 `jobs/JOB-18-00/probes/`

3. **旧病复现 A～I（先红 N2）** — 证据在 `jobs/JOB-18-00/repro/` + 探针文件

4. **三端旧版按钮清单（N1 基线）** — `jobs/JOB-18-00/inventory/`

## 二、探针结果

### ② 管理端取件路 + file_path 来源（喂 18-01）
- 现役链：`adminViewOriginal`(v217 L6815) → `_v217AdmFetchOriginal`(L6751) → **先 `await sb.rpc('admin_log_doc_view')`(L6753)，回来 ok 才 `sb.storage…download(row.file_path)`(L6760)**——两趟串行，就是那 3 秒底子的根。`_v177FetchAdminDoc`(L9878，打包下载) 也走它。
- **`file_path` 不在页面内存**：学生 `documents` 的三处 select（L6268 `doc_id,student_id,doc_type` / L9516 `…file_name,version,uploaded_at` / L14094 `…previous_issue`）**都不取 file_path**，只有 RPC 返回里才有。
- 管理员本来就可读 `documents`（is_admin RLS），故 **18-01 可在喂那三个渲染点（`adminViewOriginal` 调用处 L9095/L9684/L11632）的 select 里补 `file_path`**，让它进内存 → RPC 与下载可同时发；补不到就退回 v217 的先后两趟。**不新加 RPC**（照 QUEUE 18-01）。

### ⑤ 中介端 `showFormWarning` 调用点清单（喂 18-03）
- 垫片 L11222：`function showFormWarning(o){…showToast(m,'error');}` → **红底 error toast**。
- **护照姓名核对（R-2）两处**：L11453（无机读码 noMrz）、L11463（姓名不一致 mismatch）。
- **其它调用点（18-03 一律不动颜色/时长）**：L8036、L8037、L8043（删除守卫/失败）、L8651、L8659、L8685（q1 预检警）、L9143（cloudOffer 无授权）、L10433、L10443（resubmit 门）、L10585、L10588（文件被拒/重复）、L10648、L14047、L14640（向导门）。
- 名单块 `_r2RosterHtml`(L11408) 现只渲染「⚠ 文件名 · 姓名待核对」行，**不含**那句带两个名字的话（`state._r2Verdict.msg` 由 `_r2AnchorWarn` L11432 存下但名单块没用）。18-03 = 在名单块标题下补这句 + R-2 两处提示改琥珀 ≥8s。

### ⑥ 审核端未知类型（喂 18-02）—— 带身份实测
- `DOC_TYPES`(L1498) 有 28 键；渲染兜底 `DOC_TYPES[m.docType] || { zh:m.docType,… }`（L2254 等）。真实渲染点行号（v42 现值，与 QUEUE 引用的旧行号有偏移，18-02 以此为准）：卡标题 L2254、预览标题 **L2915**、退回弹窗 **L3288**、操作记录 L2435/L3111/L3392。
- `truth('reviewer1','reviewer_doc_queue?…')` 取 reviewer1 真队列 60 行：**不在 DOC_TYPES 的有 3 类 —— `studyProof`×2、`studyPlan`×2、`null`×1，共 5/60 行**渲染成裸英文代号 / 空标题。（studyProof/studyPlan 是学生端 v397 的键，审核端 DOC_TYPES 自 v21 起没补；null 是脏数据行。）

### ⑦ 审核端 `_wmFetch`/`_wmFetchEF` 能否中止（喂 18-02）
- `_wmFetch`(L2614) → ①`_wmTryDirect`(L2644) 用 **`sb.storage.from('wm-cache').download()`（supabase-js，无 AbortSignal 入口，中止不了）**；② `_wmTryCache`(L2662) 用 **`fetch()`**（可传 signal）；③ `_wmFetchEF`(L2684) 用 **`fetch()`**（可传 signal）。
- 结论给 18-02：②③两路可配中止信号；**①直读桶那一路中止不了 → 查看期间对它并行降到 0（不再新发）**，回执照 QUEUE 写明。

### ① 管理端两趟同时发的收益（无管理员密码，用 stuA 近似）
- RPC 腿均 ≈416 ms、下载腿（260KB）均 ≈190 ms；**串行 avg 608 ms ≈ 两腿和；同时发 avg 409 ms ≈ max(单腿)**，省 ≈199 ms（= 较小那条腿）。这正是 v217 先等 RPC 白丢的时间；管理端真件更大、admin_log_doc_view 更重时，省的更多。表在 `probes/probe1-timing.md`。

### ③ 审核端慢件的共同点（reviewer1 真登，逐个直读队列 30 份，并行 1）
- **30 份里 15 份 ≥5s**（中位 5.25s，range 1.2–9.4s），经 watermark-doc EF。**与大小、是否命中缓存无关**（几乎全 `wm_status=cached`、body 很小仍慢）；415 不支持的行反而快返 → 成本在**服务端渲染/开销，不是带宽**。响应头多为「CORS 不给」；ttfb 列因共享 URL 的 resource-timing 假象不可靠，total-ms 可靠。表在 `probes/probe3-slowdocs.md`。→ 十九轮建议：查 watermark-doc 冷/热渲染与 EF 冷启动。

### ④ 三张审计表 outcome 约束 + 差集（喂 18-05）—— db-run 只读
- `pg_constraint contype='c'` 对 `school_access_log`/`student_access_log`/`partner_access_log` **返回 0 行 —— 三张表 outcome 列都是纯 `text`、没有任何 CHECK 约束**。
- watermark-doc v33 各表会写的值（`audit()` 路由 L645-652 + 直写）：school={served_original,served,served_video_url,denied_doc_deleted,denied_not_candidate}；student/partner={served,served_video_url,denied_doc_deleted,denied_not_owner}。
- **既无约束，「函数会写、约束不收」差集 = 空**。近况实测：school 385 served_original/300 served/1 served_video_url；student 68 served/4 served_video_url；partner 0 行。
- **结论给 18-05：三张表都「无需改」，不写迁移、无待业主手贴 SQL**（证据：0 行约束）。SQL 在 `probes/probe4-audit-constraints.sql`。

## 三、旧病复现（A～I，先红）

| 项 | 端 | 结果 | 证据 |
|---|---|---|---|
| A 取件串行 | 管理(stub真页) | **红** 真点 adminViewOriginal，注入 800ms 审计延迟 → 下载在 RPC 回来后 5ms 才发（RPC@0→resp@800→download@805），非并发 | `repro/repro-A-admin-timeline.json` |
| B 下载无真名 | 管理 | **红**(代码实证) 图片下载 `<a … download>`(L6780) 无文件名值 → 存成 blob 名；PDF 回落云函数时 `c.fileName=''`(L6763/6768)→下载名空(L6794) | v217 L6780/6794 |
| C 新标签/下载放法不一 | 管理 | **红**(代码实证) 图片走上方 `tools` 行(L6779-6780)，PDF 走组件工具条 `extras`(L6792-6795)，位置+叫法不一致 | v217 L6777-6795 |
| D 查看不让路 | 审核(reviewer1真页) | **红** 真点「查看」，点击时在途的 3 个预取(278/447/870ms)**全跑完、0 中止**——v42 只暂停新入队、从不取消 | `repro/repro-D-reviewer-prefetch.json` |
| E 超高卡不预取 | 审核(reviewer1真页) | **红** 300px 视口下真卡 914px，露出比 0.328 < 0.34 门 → 0 入队、0 预取；矮卡正常入队 | `repro/repro-E-tallcard.json` |
| F 未知类型名 | 审核(reviewer1真队列) | **红** 真队列 studyProof/studyPlan 渲染成裸英文代号、null 渲染空/「null」标题(L2915/L3288 `docType.zh + ' · '`) | `probes/probe6-unknown-types.md` |
| G 两个名字只在红弹条 | 中介 | **红**(代码实证) 名单块 `_r2RosterHtml`(L11408-11421) 不含两名句；R-2 提示 L11453/11463 走 showFormWarning 垫片→红底 error toast(默认 ~2.2s)。**全套真页面 N7 红→绿在 18-03 gate3 量**（照 17 轮对 D 的办法：修与量同处一副 OCR 桩脚手架，避免重复搭） | v165 L11222/11408/11453/11463 |
| H 代传出异常不转圈 | 中介 | **红**(代码实证) 代传循环外层 catch(L9212) 只 `fail++`、**不调 `_s5cCard.fail()`** → 该件卡一直转。**真页面注入异常红→绿在 18-03 gate3** | v165 L9212 |
| I 审计表约束拒差集 | 库 | **未复现(N2)**——探针④证三张表**无 CHECK 约束**，无值可拒，前提不成立；对应 18-05「无需改」 | `probes/probe4-audit-constraints.sql` |

## 四、三端按钮清单（N1 基线）—— `jobs/JOB-18-00/inventory/`

| 端 | 静态(ids/onclickFns/fnDefs) | 动态可见控件 | 登录方式 | 备注 |
|---|---|---|---|---|
| admin v217 | 200 / 175 / 734 | 250 | stub+注入会话（无管理员密码） | 31 个 data-view 全走过；stub 空数据 → 数据行按钮不在基线，属 stub 边界，后续 diff 读时注意 |
| reviewer v42 | 74 / 33 / 141 | 199 | reviewer1 真登 | 首屏审核队列 + 通知面板 |
| partner v165 | 205 / 130 / 635 | 165 | partner1 真登 | 首屏 + 四语切换 + 设置 |

七个 JSON（{admin,reviewer,partner}-{static,dynamic}.json + _report.json）已落盘，供后续版本求差集（N1）。

## 五、结果 / 业主要做什么

- 本包**只读侦察 + 先红 + 清单**，无版本升、无迁移、无部署、无上传、无写库（探针①③是测试账号读自己的件 / 审核端读本职队列，只读）。
- **业主本包无需做任何事**。18-05 据探针④已定「三张审计表无需改」——本轮不再有审计表待手贴 SQL（若后续包发现别的手贴项另计）。
- secret-scan 对 `~/mh-jobs` 全树 = CLEAN；主仓库工作树对 tracked 文件干净，本包只本地 commit（base-head.txt + QUEUE-18.md），不推主仓库。

## 六、新闸证据

- **N1**：三端静/动态清单已落盘（上表），本包只建基线不求差集。
- **N2**：A/D/E 真页面真点先红（证据 JSON）；B/C/G/H 代码实证先红（行号在案，真页面红→绿随各自施工包）；F 真队列实测先红；I 前提不成立按 N2 记「未复现」。
- **N3**：探针⑥⑦①③④ 均带身份（reviewer1/stuA 真登 JWT 或 db-run 库内），无「0 行/无行」当证据。
- **N4/N5**：本包无云函数改动；secret-scan CLEAN（无密码/token/JWT 外泄）。
- **N6**：只做 QUEUE-18 §18-00 写明的事，新发现（审核端慢件根因、studyProof/studyPlan 未进 DOC_TYPES）记入十九轮建议/交给 18-02，不顺手改。
- **N7**：本包无「给人看的提示」改动；G/H 的 N7 三件量（1s/4s 可见、背景/文字色、不被顶掉）在 18-03 真页面上量。

---
**下一包 = 18-01**（管理端 v218：取件两趟同时发 + 下载用真文件名 + 新标签/下载统一放法）。
