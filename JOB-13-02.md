# JOB-13-02 · 「试 3 次放弃」+ 失败件清单 + 管理端可见（迁移 0201 + EF 源码 + admin v210）

## 做了什么
1. **迁移 0201**（已应用，走 db-run.sh）：给 wm_cache_index 加 5 列（status/attempts/last_error/last_attempt_at/last_request_id）；重写兜底函数 `wm_cache_backfill`——① 从网关响应回填错误文本 ② 试满 3 次且仍渲不出→标 `gave_up` ③ 候选排除 gave_up/cached ④ 发请求并记尝试次数/请求号；新增两只管理员 RPC `admin_wm_cache_failures()`（列失败件）、`admin_wm_cache_reset(p_doc_id)`（清零重试）。
2. **云函数 watermark-doc 源码改**（未部署）：预渲染失败（类型不支持 415 / 下载失败 / 水印失败含大图·坏 PDF / 上传失败 / 找不到原件）在返回前把索引行标 `failed` + 写错误文本；成功标 `cached`；写索引失败也不影响主流程。
3. **admin v210**：派单台底部新增可折叠区「水印缓存失败件 (N)」。

## 结果是什么（每个数字带命令出处）
- **迁移 0201 验证**（db-run 末尾）：`backfill_has_gave_up=true`、5 个新列都在、anon 不能执行两只 RPC、`status_counts={cached:95}`。
- **试 3 次放弃实证**（`bash jobs/db-run.sh jobs/JOB-13-02/backfill-once.sql` 连跑 4 次，中间 sleep 60）：4 件待补 attempts 从 1→2→3，第 4 次全部翻成 **`gave_up`**（attempts=3），`last_error` 非空。
- **清零验证**（`jobs/JOB-13-02/verify-cleared.sql`）：`candidate_count=0`（下一轮 cron 不会再打这 4 件 = 待补真正清零）、`failures_count=4`、`status_counts={cached:95, gave_up:4}`。因候选=0，同一个函数被 cron 调也只会发 0 条，无需再等 5 分钟确认。
- **失败件真身**（同上）4 件：
  - MH-W73M6F IRADUKUNDA RAHMA · transcript · Notice of applying.pdf · 894KB
  - MH-X88X8S KUMI RICHMOND AGYEMANG · health · Notice of applying.pdf · 894KB
  - MH-QH5NBK · studyProof · big size 2.jpg · 6189KB
  - MH-W73M6F IRADUKUNDA RAHMA · studyPlan · big size 2.jpg · 6189KB
- ⚠️ **点名（不是失败）**：`last_error` 现在写的是 `http 402: Service…restricted…egress_quota`，不是 queue 预期的 415/422 文字。因为项目正被 402 限流（见 JOB-13-00），云函数根本没跑到「渲染」那步就被网关挡了。**但这 4 件本来就渲不出**（13-00 探针实测真实死因 = IMAGE_TOO_LARGE:5712x4284 与 PDF_UNREADABLE），所以 gave_up 判定是对的；等业主恢复服务、reset 重试后，`last_error` 会换成真实的 500 文字。管理端「原因」列用 `_v210ReasonText` 扫原文，415/pdf_unreadable/image_too_large/546/402 都能转人话，覆盖两种情况。
- **EF 新 md5** = `9c291689af6ea604274500bdb2466e7d`（`md5 -q supabase/functions/watermark-doc/index.ts`），**未部署**。
- **admin v210 闸3**（Playwright file://，`jobs/JOB-13-02/gate3-admin-v210.mjs`）：`RESULT=GATE3-PASS` —— stub 2 行→区块渲染 2 行、2 个「重置重试」按钮、原因列正确转「图片像素过大」「PDF 损坏或加密」、点重置真调到 `admin_wm_cache_reset({p_doc_id:'DOC-A'})`、390px scrollWidth=390 不溢出、0 真实报错。
- 版本四件套：文件名 v210 / 头注 v210 / [VER] console v210 / admin index 重定向 3 处切 v210；changelog admin + edge-functions 各加一行。

## 业主要做什么
1. **部署云函数**（恢复服务后）：把 `supabase/functions/watermark-doc/index.ts`（新 md5 9c291689…）部署上去，以后预渲染失败会自动标 failed 并带真实原因。部署命令照旧（Supabase 控制台或 CLI，你之前部署 v30 的同一条路）。
2. **看失败件**：管理端 v210 → 派单台 → 拉到底「水印缓存失败件 (4)」，能看到这 4 件带原因。想让系统再试一次，点「重置重试」（但大图/坏 PDF 重试仍会失败，除非学生重传——那是 13-03/13-04 治的）。
3. 恢复 Supabase 服务仍是前提（见 JOB-13-00）。
