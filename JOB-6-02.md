# JOB-6-02 回执 · 派单台表格视图（2026-09-16）

> 大白话版。做完了：派单台右上角多了「卡片 / 表格」切换。切到表格是**一行一件材料**，可以逐件打勾，勾完在**底部固定条**选一个审核员点确认，一次全派出去。派单还是走原来那套写库逻辑，没新造。**主仓库已提交没推。**

## 解决什么
派单台原来是卡片式（按学生分组），132 份待派单时要滚很久。业主 9 月 9 日定了方案乙 = 表格视图，一行一件、可多选、勾完选审核员一次派。

## 管理端改了什么（v203 → v204）
1. 派单台右上加「**卡片 / 表格**」切换按钮，**记住你的选择**（存 localStorage 同一个键 `mh_dispatch_ui`，没新增键）。
2. **表格视图**：一行一件材料，列是 **学生 · 材料类型 · 上传时间 · 换版次数 · 上版问题 · 优先级 · 勾选框**。
3. 表头「**上传时间**」和「**优先级**」可以点着排序。
4. 勾选后**底部固定一条**：「已选 N 件 → 派给 [审核员下拉] → 确认」，**一次派完**。
5. 确认派单**复用原来的写库逻辑**（`dispatchSubmitToCloud`），**没新造任何派单接口**。
6. 表格套现成的表格容器（`.table-wrap`），390px 手机宽下**表格在自己容器里横向滚动，页面不横向撑破**。
7. 原来的卡片视图**一字没改**，只是多了一个切换。

## 验收
派单台右上有「表格」按钮，切过去一行一件；勾 5 件，底部选 reviewer1，点确认 → 派出去，今日已派 +5。（真派 5 件需登录管理端，留你验收。）

## 自验（三层全过）
- **UI 对照**：切换控件 1、表格渲染函数 1、六列齐全、表头可排序、底部条复用 dispatchSubmitToCloud、**新增派单接口调用 = 0**、零裸文案键、零新样式（全复用 .table/.filter-chip/.badge/.dispatch-checkbox）。
- **无头走查**：Playwright 本地开 v204、**全程 abort supabase 不碰生产**；表格渲染出六列两行、勾选态正确、上版问题显示、底部条含审核员下拉+确认；**卡片和表格两种模式 renderDispatch() 都正常返回不报错**；**非网络 console 报错 = 0**。
- 报告全文见主仓库 `docs/self-test-report-JOB-6-02.md`。

## 版本 / 提交
- 管理端 **v204**（旧 v203 保留）；index.html 3 处 + [VER] 探针 + 头注 均已改 v204。
- 主仓库：`admin v204 + index + 自验报告`，**已 commit 未 push**。零迁移。
- 自验脚本：`~/mh-verify/gate8_job6_02.mjs`。

**边界守住**：只动管理端派单台；零迁移；派单写库沿用原函数。
