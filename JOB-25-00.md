# JOB-25-00 · 第二十五轮开工：核对家底 + 造假数据 + 把五个毛病先量成「红」

（CC 写给业主。大白话。这一包不改任何页面，只做准备和体检。）

## 一、做了什么

1. **核对家底**（对不上就得停，结果全对上了）：
   - 五个端口的文件指纹（md5）跟卡里写的一模一样：管理端 v232、学生端 v401、中介端 v170、学校端 v235、审核端 v46。
   - 本地代码和云端 GitHub 同步，没有没推的东西。
   - 迁移账本到 0216 业主已贴；本轮要新写的 0217/0218 还没写、也不会真跑。
   - 没有触发「必须停」，`~/mh-verify/r25-STOP.txt` 没建。
   - 本机 `~/mh-verify/accounts.json` **在**，线上真登以后按需做；Playwright 装在 `~/mh-verify`，无头能跑。

2. **给四个端各拍了张「零件清单」**（供后面每一版比对，防手滑弄丢按钮/函数）：
   存进 `jobs/JOB-25-00/inventory/`，用同一把提取器（`gen.mjs`）出的，后面每次改完用同一把尺子做差集。
   - 学生端 v401：id 368 / 内联动作 197 / 函数 945
   - 学校端 v235：id 170 / 内联动作 226 / 函数 690
   - 管理端 v232：id 203 / 内联动作 185 / 函数 802
   - 中介端 v170：id 205 / 内联动作 125 / 函数 639

3. **造了一批假数据**（全是编的，没有任何真人信息），6 名假学生：
   - 2 名报了**两个层次**（语言生 + 硕士，硕士下两个专业 Computer Science and Technology、Business Administration），其中带头的叫 MUHAMMAD Ali（对应业主截图那位）；
   - 4 名单层次；
   - 3 名传了**证件照**（.jpg），其中 1 名（KHAN Sara）**照片还没审核**；1 名（SHARMA Priya）**没传照片**；
   - 配套：`documents`（含照片与其它材料）、`review_verdicts`（只给已审的两张照片记了审核，未审那张故意不记）、`search_students_global` / `get_task_candidates` / `get_candidate_documents` 三个查询的返回、以及本轮要新建的 `get_student_photo` 返回（供后面几包的绿检验用）。
   - 假数据在 `jobs/JOB-25-00/stub/fixtures.mjs`。

4. **把业主提的五个毛病，各量了一遍，全都量成了「红」**（红 = 毛病确实在），零报错：

| 编号 | 毛病 | 量出来的红 |
|---|---|---|
| R1 | 学生端提交只上传一个层次一个专业 | 提交 payload 里只写单值 `target_program`；全文根本没有 `target_programs` 这个键（0 处）→ 二三层次上不了云 |
| R2 | 学校端只显一个层次一个专业 | 学校端全文 0 处 `target_programs` → 只认单值 |
| R3 | 学校端头像多数是首字母 | 旧头像路径 `_loadCandidateAvatars` 只对「在本校任务候选池里的、且已审核」的照片取图；没有新的 `get_student_photo` 通道 → 不在任务里的、照片没审核的都只能显首字母 |
| R4 | 管理端头像全是首字母 + 层次列只显一个 | 管理端头像仍是 `avatarColor+首字母`、没有任何图片路径（无 `_v234Avatar`/`get_student_photo`）；全文 0 处 `target_programs` |
| R5 | 中介端学生卡层次只显一个 | 中介端用单值 `target_program` 重建 programs 数组；全文 0 处 `target_programs` |

   另外把四个端各在本地无头开了一遍（喂上面的假数据），**四个端全程零报错**。
   量红脚本 + 日志在 `jobs/JOB-25-00/repro/red-all.mjs` / `red-all.log`。

## 二、结果是什么

五个毛病全部「先红」到位、零报错。后面每一版改完，只要同一把尺子量出「绿」，就能证明真修好了。

## 三、动手前记下的「数据来路」隐患（给后面几包）

1. **多层次落库（给 25-01）**：库里现在只有 `target_program`（单值）+ `major`（首个专业）。要贯通得先加 `target_programs`（jsonb 数组）这一列（迁移 0217），并把学校侧几个按层次筛选的 RPC 也放开数组匹配。25-01 会先只读四个 RPC 现行定义，逐个列出改点。
2. **未审核照片也要显（给 25-04/25-05）**：学校端现在的头像通道 `get_candidate_documents` 按现行定义**只回已审核的材料**，所以刚传还没审的照片取不到。25-04 要新建一个 `get_student_photo`（迁移 0218）——**不看审核状态**，且带「这所学校能不能看到这个学生」的可见性判断。
3. **中介代填写入（给 25-03，记账到第二十六轮）**：中介端的写入 RPC 若只收单值，本轮 25-03 **只改中介端的显示、不改写入**，写入侧留到第二十六轮。

## 四、业主要做什么

这一包不用你做任何事。继续下一包。

---
- 闸3：R1–R5 全红、零报错 ✅；四端零件清单非空 ✅
- N1 基线：`jobs/JOB-25-00/inventory/{student-v401,school-v235,admin-v232,partner-v170}.json`
- secret-scan：CLEAN ✅（新文件零密钥）；本包无新增 shell 脚本
- 本轮暂无「必须停」（`~/mh-verify/r25-STOP.txt` 不存在）
- 下一包 = 25-01
