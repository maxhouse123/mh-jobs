# JOB-09 · 状态类补齐（含一条迁移）

**结果：前端 3 项做完（轮次/搜索/已入学），2 项延后；迁移已写好待你在 SQL Editor 跑。全闸通过。**

## ⚠️ 你要先做一步：跑迁移
学生「第几轮」这些要能显示，需要先给数据库加一条**只读权限**（管理员才能查轮次表）。步骤：
1. 打开 Supabase → SQL Editor。
2. 把 `jobs/JOB-09/migrate.sql`（也在日志仓库 JOB-09-migrate.sql）整段贴进去运行。
3. 它会在最前和最后各数一次「策略数」，**前后差应该是 1**（就是新加的这条）。把这两个数字发我或自己核对。
4. 跑完，学生抽屉的「申请轮次」段就会显示真数据。**不想要了可以用 `rollback.sql` 撤销**，只删这一条、不动任何数据。

## 做了什么（一句白话）
1. **申请轮次**（item1）：学生抽屉新增「🔁 申请轮次」段，显示这个学生申请了**第几轮**、每轮什么**学历层次**、每轮的**申请号**、**本轮状态**（进行中/已提交/已合格/已撤回/已结束）。毕业生再申请（本科→硕士）就是开新一轮。
2. **搜索命中第二轮**（item2）：以前搜第二轮的新申请号搜不到人，现在能搜到（库里实测有 1 个两轮学生）。
3. **「已入学」阶段**（item3）：学生阶段派生新增「已入学」一档，且**优先级高于「已付费解锁」**（真入学了就显示已入学，不再停在已付费）。

## 业主要看什么
1. （跑完迁移后）「学生」→ 点学生 → 抽屉「🔁 申请轮次」段：看到第几轮/申请号/状态。
2. 「学生」搜索框输入第二轮的申请号 → 能搜到那个两轮学生。
3. 学生列表/抽屉的阶段：真标记入学的学生会显示「已入学」（库里目前 0 条已标记入学，等有数据或 JOB-0.5 造数据后可见——见延后说明）。

## 十道闸
| 闸 | 结果 | 实测 |
|---|---|---|
| 1 基底md5 | PASS | `10d43d8d12b57819fc1a042237d97712`（=JOB-08 产物）|
| 2 锚点唯一 | PASS | 11 处锚点各唯一 |
| 3 补丁自检 | PASS | 轮次段/装载器定义各 1、首屏+视图装载 2、enrolled_at select/映射各 1、f3-enrolled 标签+判据、搜索轮次匹配 1。反向样本 v179 均失败 |
| 4 产物md5 | 记录 | `93a95a8245876a46e28a04bd120881d4` |
| 5 语法 | PASS | 脚本块 0 报错 |
| 6 功能清单 | PASS | 函数新增 5（_v180 族+loadRounds）+id 新增 1（v180RoundBox）；消失 0 |
| 7 四语 | PASS | 管理端中/英双语；新增轮次文案中英齐全、无裸键 |
| 8 真点击 | PASS | 真浏览器（桩数据）：已入学优先于已付费(k1=f3-enrolled/k2=f3-paid)、标签「已入学」；轮次段显两轮+申请号+状态(第2轮/已结束/已提交)、390px 内；搜索命中第二轮 app_no=1、反向=0 |
| 9 视觉一致 | PASS | 零新颜色/字号/CSS 类（复用 dd-grid2/dd-field/badge green）|
| 10 线上版本 | 跳过 | 本轮不推送 |

## 基底 / 产物 md5 / 提交
- 基底 `10d43d8d12b57819fc1a042237d97712` / 产物 `93a95a8245876a46e28a04bd120881d4`
- index.html 3 处 v179→v180；主仓库提交 `e1ac1e9`（未推送）

## 偏离或疑点
1. **迁移我跑不了、只能你跑**：我没有数据库连接权限（`.env` 在我的禁读清单里），按项目一贯规矩「CC 写 SQL、业主在 SQL Editor 跑」。迁移和回滚都写好了、可重复执行、带前后策略计数自检。**跑完前，抽屉「申请轮次」段会显示「读取失败/暂无」——这是正常的（还没授权）。**
2. **item4「四类材料闸门状态」延后**：卡里要求「与服务端返回值一致，不自己算」——这需要一个**返回闸门状态的服务端函数**。我在前端没找到现成的这个返回值来源，硬在前端自己算又违反「不自己算」。**需要你确认用哪个服务端函数（或先建一个）**，我再接。
3. **item5「三端邮箱验证状态」延后**：前端没有「邮箱是否已验证」这个数据源（通常在 auth.users.email_confirmed_at，管理端前端读不到，需要一个服务端函数或视图暴露它）。**需要定个取数方式**，我再接。
4. **已入学验收暂时没数据**：库里「已标记入学」目前 0 条，所以「已入学」档要等有真数据（或 JOB-0.5 造）才看得到。代码用桩数据证明了逻辑（优先级正确）。
