# JOB-20-00 · 第二十轮开工：基线核对 + 六探针 + 旧病复现（先红）+ 三端按钮清单

## 做了什么
1. **基线核对（§0.2）全等**：五端 md5、共用查看器、三只云函数源码 md5 全对上；主仓库 `git fetch` 后 HEAD == origin/main == `6bd6c6e`（0/0），`git status --porcelain --untracked-files=no` 空；迁移到 0212、0213 空；accounts.json 在（stuA/partner1/reviewer1/schX 四个都有，权限 600）。EF 线上可达（OPTIONS→200）、无凭据 POST→网关 401 `Missing authorization header`。**无「必须停」触发**。
2. `git rev-parse HEAD`（6bd6c6e）落 `jobs/JOB-20-00/base-head.txt`，与 `jobs/QUEUE-20.md` 一起点名 commit 进主仓库（本地，不推）。
3. **六探针**（①LIVE、②-⑥静态源码，只读，带身份处带身份）：见 `jobs/JOB-20-00/probes/probe-1-timing.md`（①）与 `probes-2-6.md`（②-⑥）。
4. **旧病复现 A/B/C/D**（真页面真点、先红）：A=① 的计时表；B=中介端护照四病 4/4 红；C=**未复现（前提证伪）**；D=旧 deploy 脚本 §0.6① 两行红。
5. **三端旧版按钮清单（N1 基线）**：partner v167 / student v398 / reviewer v45，静态+动态各一份，存 `jobs/JOB-20-00/inventory/`。

## 结果是什么

### 探针 ① 冷启动 LIVE 计时（reviewer1 真登，watermark-doc v34 线上）——本轮最要紧一问已定案
- 12 连打 `signed_url`（前 6@0.3s、后 6@8s）+ 3 件 render×2，**18 次全 HTTP 200**，全程 ap-southeast-1 / SIN。
- **每一次都是冷的**：所有 18 次响应头都是 `desc=cold-n1`、`seq=1`、`boot_age` 只在 0–2 s，**从不累积**。
- signed_url 客户端总耗时**中位 4281 ms**（3.5–5.1 s，一枚 49 s 离群）；render 段实测仅 **231–271 ms**。
- 三问答：**(a) 连续请求不复用实例**——seq 恒 1，实例内计数器 REQ_SEQ 从没到过 2；**(b) render 后下一次是冷的**，但不是 render 独有，紧接的第 2 次也 cold-n1；**(c) 回收不是空闲超时驱动，是每请求即回收**——0.3s 与 8s 两窗 seq 全 =1，boot_age 不向 8s 累积。
- **含义**：4–5 s 的墙钟大头 = 冷启动 + 鉴权/派单闸 + 签名取件，**不是烧水印**（与 19-03 结论一致，且更硬）。**§0.5 保温方案不成立**：既然是「每请求回收」而非「空闲回收」，定时 ping 预热的那个实例也接不到用户下一次请求 → 本轮不加保温，20-01 只做 boot 瘦身 + 渲染降内存。
- 注：reviewer1 的 105 条派单全是 `DOC-TEST-xxx`；`documents` REST 按 doc_id 查回 0 行（表非此键），image/pdf 未能区分，signed_url 与 render 都落在 DOC-TEST 件上；因 18 次全 200 且看的是 seq/boot_age，复用结论不受影响。

### 探针 ②③④⑤⑥（详见 probes-2-6.md）
- **② 渲染内存**：最大持有者=原图整解码 RGBA（`Image.decode(input)`，先解后缩），最坏 20 MP×4≈80 MB；1.6 MB 那份估峰值 rss 60–90 MB。EF 内存上限 256 MB、CPU 约 2 s/请求。render 中位仅 0.26–0.95 s，非慢因。
- **③ 启动包**：index.ts 55 KB + font_b64 218 KB + logo_b64 69 KB；boot 期解析 285 KB base64 字面量 + 顶层 `decodeBase64(FONT)`≈163 KB + `decodeBase64(LOGO)`≈51 KB + 初始化 imagescript/supabase-js。pdf-lib 已按需（③做过一半）。→ 20-01(a) 移 font/logo 解码出启动路、(b) imagescript 改按需。
- **④ prepare 预热**：`prepare` 有 `x-mh-hook-secret` 暗号闸 + 必须已有 reviewer_assignment；**学生/中介/审核端前端都调不了**（给前端=泄密）。缓存本就在**派单时**由 DB 触发器预热。→ 20-01「上传后预热」①②两路架构上不成立，写清原因、留第 21 轮。
- **⑤ 中介端 _r2**：`_r2FileVerdicts` 无删件清除点、`_r2Verdict` 仅新核对/30 分钟时效清；花名册 `_r2RosterHtml` 渲染在 8202（详情弹窗 `renderPartnerSubmitGrid`）与 13948（向导 `_wzRender`）；删暂存件入口 `_v141RemovePartnerPending`（10377）不碰这两个 state。整栏重画入口=`_wzRender`/`renderPartnerSubmitGrid`。病根链两条已坐实。
- **⑥ 学生端词典**：⚠**前提证伪**——学生 zh 由外挂包 `maxhouse_student_i18n_v360_zh.js` 覆盖，1282/1289 行已是「学习证明」「学习计划」（自 v351）；v398 主文件 6775/6782 的英文在 `I18N.en` 块。真页面 zh 显示中文。**真实差异另立**：学生 studyProof=「学习证明」≠ 管理端/审核端「在读证明」（术语不一致，改哪端需业主裁决）。

### 旧病复现
- **A（计时表）红**：= ① 的表，冷启动占大头，与 19-03 一致。
- **B（中介端护照四病）4/4 红**（partner1 真登、StuB、合成无 MRZ 护照暂存不提交，OCR 真跑走 noMrz 同码路）：
  1. verdict 出后 ~1 s 名单块 `.r2-anchor-warn` 不存在（403 ms 弹卡、名单块不现）→ 红
  2. 点红×删暂存件后名单块 666×107 仍在 → 红
  3. 英文卡标题含全角「：」（v167 line 11503）→ 红
  4. `#_v167CardStack` 与下方内容重叠：1280px **34840 px²**、390px **7424 px²** → 红
  - 复现产生的 1 个云暂存件已带 partner1 身份探针确认后删净（DELETE 200，复查 0；`referral_drafts` 0 行，未占材料批次）。
- **C（学生端 zh 两栏显英文）未复现（N2，前提证伪）**：真页面 zh 显示「学习证明」「学习计划」（非英文）。根因见⑥。按 P2/N2 如实记「未复现」，**20-03 不照错前提自修**，转业主裁决（学生 学习证明 vs 管理/审核 在读证明）。
- **D（旧 deploy 脚本 §0.6①）红**：`jobs/JOB-19-03/deploy-watermark.sh` 第 29 行 `$B：`、第 58 行 `$CODE，`（`$var` 紧贴全角标点）；`bash -n`/`zsh -n` 都过、查不出。20-04 lint 脚本正式判此红→绿。

### 三端按钮清单（N1 基线）
| 端 | 静态(id/onclick/fn) | 动态(首页/导航) |
|---|---|---|
| partner v167 | 205 / 120 / 749 | home=44（单视图无顶导） |
| student v398 | 368 / 188 / 1116 | home=77 + goStep(1..5)=14/18/81/38/77 |
| reviewer v45 | 74 / 31 / 157 | home=198（单视图无顶导） |

## 业主要做什么
- 本包**无需业主动作**。无迁移、无 SQL 手贴、无部署、无主仓库推送。
- 仅一处**需业主留意**（非本包待办，收官会汇总）：探针⑥ 揭出的**跨端术语不一致**（学生「学习证明」vs 管理/审核「在读证明」），20-03 前提被证伪，改哪端以谁为准待业主一句话裁决。

## 新闸证据（N1–N7）
- **N1**：三端旧版按钮清单基线已出（静态+动态，见上表），存 `jobs/JOB-20-00/inventory/`，供 20-02/20-03 升版后求差集。
- **N2 先红后绿**：B/D 均在**现役真页面**上跑出红（B 真点真登、真文件暂存；D 字节级 lint）；C 两法（真登 goStep(3) + 运行时词典 dump）皆不红→按 N2 记「未复现」并停手报冲突（P2），不自修不硬放行。
- **N3 带身份**：① reviewer1 真登拿 JWT 打 EF+REST；B partner1 真登、删件用 partner1 身份探针确认清净；C stuA 真登。无身份的「0 行」未用作证据（`documents` 0 行已注明为键不匹配、非权限结论）。
- **N4**：本包只探不改云函数，N4 分支表留 20-01。此处仅记 EF 线上鉴权路可达性：无凭据→网关 401（§0.4 过）。
- **N5 secret-scan**：`~/mh-jobs` 全树 + `jobs/JOB-20-00` 两次扫均 `SECRET-SCAN-CLEAN`；探针/复现脚本经查证无硬编码密码/token（凭据全 fs 运行时读）；动态清单里 1 个真测试邮箱已 mask。
- **N7 提示看得见**：B bug1 量到 verdict 后 ~1 s 名单块不可见（该在不在）、bug2 量到删件后名单块仍 666×107（该走不走）、bug4 量到卡片与内容重叠像素（1280/390 两宽）——均以真页面几何数字判，非只核代码。

## 交接
- 下一包 = 20-01（watermark-doc v35：计时头补 seq/mem/cpu + boot 瘦身(a)(b) + 渲染降内存(c)，改源码不部署；保温不做，据①）。
- 20-03 前提已证伪，施工时须按 P2 停工报业主裁决（术语不一致），不照「显英文」前提改。

（本包无视觉件上线，公开截图为复现红图，随收官干净后统一推。）
