# JOB-18-03 · 中介端 v166：「两个名字」进琥珀名单块 + 代传出异常不转圈

> 本包续跑：上一趟在 18-03 做到一半网络断线、回执未推。开工按任务书对照 QUEUE-18 §18-03 逐项核对工作区半成品（v166 文件 + gate + n1-diff 皆在），补齐回执与提交，未 `reset/checkout/clean`，无 18-03 之外的未提交改动。

## 一、做了什么（partner v165 → v166，只改中介端一个文件）
1. **「两个名字」进琥珀名单块（第 2 条）**：`_r2RosterHtml` 在名单块标题下补一行本页最近一次姓名核对的那句话 `state._r2Verdict.msg`（已填好两个名字的四语 `mismatchSoft`/`noMrz` 文案），30 分钟时效沿用名单块；名单块在就一直显示（此前两名句只在弹出提示里、2.2s 后消失）。verdictLine 经 `escapeHtml` 拼进 return，防注入。
2. **R-2 两处弹出提示改琥珀软提醒（第 2 条）**：R-2 的两处（无机读码 L11453 / 姓名不一致 L11463）由 `showFormWarning`（→`showToast(m,'error')` 红底 ~2.2s）改走**新增的独立 `_r2SoftToast`**：自有 `#r2SoftToast` 元素 + 自有计时器（不被后续 `showToast` 顶掉）、停留 ≥8s（8500ms）、琥珀色（bg `#FFF7ED` / border `#F59E0B` / text `#9A3412`）。`showFormWarning` 其它 **15** 处调用点（删除守卫/预检/resubmit 门/向导门等，见 18-00 探针⑤）颜色、时长一律不变；`showFormWarning` 垫片本体未改（仍 error）。
3. **代传出异常不转圈（第 8 条）**：代传循环（`_s5CommitPending`）外层兜底 `catch` 此前只 `fail++` 不收卡 → 本件上传卡一直转；现循环内 hoist 卡引用 `_s5cCardOuter` + 收尾标记 `_s5cSettled`，insert 成功（done）置 settled，兜底 catch 里本件卡未收尾一律 `_s5cCardOuter.fail(null)` 显示失败。

版本四件套齐：文件名 v166 / 头注 `[VER] partner portal v166` / index.html 三处重定向指向 v166 / docs/changelog 追加一行。v165 留盘 md5 `87ded57102b90f2f3be8a99a05044051`（= §0.3 基线，未动）；**v166 md5 `faabaa5f21eb62f3221123fe4b1e9290`**。

## 二、闸3（真页面红→绿，partner1 真登 N3；证据 `jobs/JOB-18-03/gate-partner-result.json`）

真登陆 partner1、真页面（合成两名字符串、零真实 PII、**零写库、零上传**；只在页面内驱动 R-2 提示与名单块渲染）。`gate-partner-result.json` → `pass: true`。

| 闸 | 旧版 v165（红） | 新版 v166（绿） | 判 |
|---|---|---|---|
| A · R-2 提示 | 共用 `#toast`、红底 `rgb(239,68,68)`/白字、2.2s、被后一个 `showToast` 顶掉（第 2.5s `visible:false opacity:0`） | 独立 `#r2SoftToast`、琥珀 bg `rgb(255,247,237)`/border `rgb(245,158,11)`/text `rgb(154,52,18)`、第 1s 与第 4s 均 `visible:true`、后来的竞争 toast **不顶掉**它 | 过 |
| B · 名单块两名 | `_r2RosterHtml('passport',[])` htmlLen 299、`hasVerdictLine:false`（只有 ⚠ 文件名行） | 同调用 htmlLen 427、`hasVerdictLine:true`（标题下带 ZHANG SAN / LI SI 那行） | 过 |
| C · 代传不转圈 | 抛异常后 `spinning:true`（卡一直转） | 抛异常后 `state:failed spinning:false settled:true`（2s 内显示失败） | 过 |

**C 闸取证方式（N2 说明）**：真代传路 `_s5CommitPending` 受 DB-write 闸约束，本轮 §0.9③「零写库、零上传」红线禁止真走。故用 **CODE-PATH 复现**：仿一次迭代，在 settle 前抛异常，分别跑两版**逐字照搬的 catch 逻辑**；并附源码片段核对（`v166HasGuard:true` / `v165BareCatch:true`）证明复现镜像真实源码。真库真代传的一次失败演示留业主「亲眼看」清单。

## 三、N7 · 提示看得见（本包重点）
`gate-partner-result.json.n7`：`visible1s:true` / `visible4s:true` / bg `rgb(255,247,237)` / color `rgb(154,52,18)` / border `rgb(245,158,11)` / `notDisplaced:true`。三件全量：①第 1s 与第 4s 各截元素图、第 4s 仍可见（`v166-r2-softtoast-1s/4s-GREEN.png`）；②算得琥珀（非红）；③紧接着的竞争 `showToast` 不把它顶掉。**只核文案代码不算绿——本包量了显示时长、颜色值、被顶掉与否。**

## 四、P3 随卡回归（入仓 `tests/static-check-v166-partner.mjs`，29 项全绿）
`[VER] v166` + 点名 + `_r2RosterHtml` 读 `state._r2Verdict.msg` + 30 分钟时效 + verdictLine 拼入 return + 经 escapeHtml + `_r2Verdict` 写入(msg+ts) + `_r2AnchorWarn` 被 noMrz+mismatch 两处调用 + `_r2SoftToast` 定义/自有元素/琥珀三色/停留 8500ms/自有计时器 + noMrz·mismatch 两分支走软提醒 + R-2 两处脱离 showFormWarning + `showFormWarning` 调用点 = v165−2（v166=15 / v165=17）+ 垫片未改 + `_s5cCardOuter/_s5cSettled` 声明/暴露/收尾/兜底收卡 + 共用查看器引用未变(v2-1-1) + 四语 `mismatchSoft` 齐 + R-2 用键均在 v165 已存在。

## 五、结果 / 业主要做什么
- 本包**只改前端一个文件、本地 commit（`241d427`）、未推主仓库、未部署**；无迁移、无写库、无上传。
- **业主本包无需做任何事**；收官（18-07）后由业主用卡1140 统一推送。
- 业主验收项（进 SUMMARY「亲眼看」清单）：partner1 登录，测试生护照栏暂存一张姓名不一致的护照 → 名单块标题下有「两个名字」那句话且一直在；R-2 弹出提示是琥珀色、第 4 秒仍在、不被别的提示顶掉；代传中途真出错时上传卡显示「失败」而非一直转圈。

## 六、基线与不动项
- 共用查看器 `assets/mh-viewer-v2-1-1.js` 未动（本包零引用改动，R1 断言查看器引用仍 v2-1-1）；`jobs/JOB-17-01/kit2/` 未动；学生端 v397、学校端 v234、admin v218、reviewer v43 本包一行不改；学生端 R-2 同源代码本包不动。旧版 partner v165 留盘 md5 `87ded57…`（= 基线，可回滚）。

## 七、新闸证据 N1–N7
- **N1**：`jobs/JOB-18-03/n1-diff.json` —— onclick handler v165=175 / v166=175，净变 0；`removed_symbols:[]`；新增 `_r2SoftToast / #r2SoftToast / _s5cCardOuter / _s5cSettled / verdictLine` 皆 v166 独有；`showFormWarning` 调用点 −2（仅 R-2 迁走）；零共用查看器 / RPC / 库 / UI 骨架改动；零新增死按钮（R-2 改的是 `showFormWarning`→`_r2SoftToast`，非 onclick；代传收卡在 catch 内，非按钮）。
- **N2**：A/B/C 三闸真页面（C 为受红线保护的 code-path 复现，已附源码核对）先红（v165）后绿（v166），数字在 `gate-partner-result.json`。
- **N3**：partner1 真登真页面，合成两名字符串，零真实 PII，零写库零上传。
- **N4/N5**：本包无云函数改动；secret-scan 对 `jobs/JOB-18-03` + `tests/static-check-v166-partner.mjs` + `~/mh-jobs/shots/18/18-03` = **CLEAN**（合成两名 ZHANG SAN/LI SI，无 JWT/service_role/密码/邮箱）。
- **N6**：只做 §18-03 三条（两名进名单块 / R-2 琥珀 / 代传不转圈），admin/reviewer/student/school 未碰。
- **N7**：见第三节，三件全量已量并写进 `gate-partner-result.json.n7`。

## 八、公开截图（`~/mh-jobs/shots/18/18-03/` + INDEX.md；合成两名、无真实 PII，secret-scan CLEAN）
- `v165-r2-toast-RED.png`：R-2 走 showFormWarning→共用 `#toast`、红底、2.2s、被竞争 toast 顶掉。
- `v166-r2-softtoast-1s-GREEN.png` / `-4s-GREEN.png`：独立 `#r2SoftToast`、琥珀、自有 8500ms 计时器，1s 与 4s 均可见、不被顶掉。
- `v165-roster-no-verdict-RED.png`：名单块只有 ⚠ 文件名行、无两名句。
- `v166-roster-two-names-GREEN.png`：名单块标题下 ZHANG SAN / LI SI 那行。

---
**下一包 = 18-04**（precheck-ai v24：英文/俄文/法文提醒说人话，改源码不部署；交付 `deploy-precheck.sh`）。
