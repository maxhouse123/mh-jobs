# JOB-9-04 · AI 预检 EF 侧根治 — 回执【源码已改·未部署】

**大白话结论**：AI 预检以前会在给学生看的提示里露出材料的「内部代号」（比如把无犯罪记录证明写成 `'criminal'`）。之前 7-06 只在**学生手机上显示前**临时换成人话，但**服务器那头生成和存库的还是带代号的**。这一包把病根挪到服务器（那只叫 `precheck-ai` 的云函数）：①发给 AI 的提示词里，材料类型**直接用英文材料名**（如 `Bank statement / deposit certificate`），不再出现内部代号；②AI 回来的结果**存进数据库之前再过一道保险**，把「带引号的代号」正则替换成英文材料名（和前端 7-06 同一套规则，普通英文词不会被误换）；③前端 7-06 那层**保留不动**，双保险。**按规矩这只云函数只改源码、没部署**，部署命令另出卡给业主。

英文材料名**逐项取自前端英文词典（`doc.*`），以前端为准**（如 education→`Highest diploma`、health→`Medical examination certificate`、bank→`Bank statement / deposit certificate`），不是我自己编的。

## 改了哪只 EF / 改了什么
- **函数名**：`precheck-ai`
- **源码路径**：`supabase/functions/precheck-ai/index.ts`
- **改动**：
  1. 新增 `DOC_NAMES` 常量表（22 个 doc key → 英文材料名，取自前端 EN 词典）+ `docName(code)`（未知代号原样回退）。
  2. `buildPrompt()`：把「材料类型代号:"<code>"」改为「这份材料应当是:"<英文材料名>"」；把「type_match: 与材料类型代号相符」改为「与上述材料类型相符」。→ 提示词模板里**零裸代号**。
  3. 落库前净化：`humanizeIssue()` 只把**带引号的纯英文代号**替换成英文材料名（正则 `['"‘’“”]code['"‘’“”]`，与前端 `_v706HumanizeIssue` 同规则），应用到 `row.issues`（达学生的字段）与 `row.raw.model_text`。
  4. 导出 `DOC_NAMES/docName/humanizeIssue` 供单测（在原 `__PURE__` 可测块内）。
- **未动**：前端（7-06 那层保留）、任何表/桶、`Deno.serve` 主流程其余逻辑、密钥。

## 本地检查（断言表）
| 脚本/检查 | 断言 | 实测 | 过? |
|---|---|---|---|
| tests/job9-04.mjs | `'criminal'`(带引号) → 英文名 且 结果无带引号代号 | `材料 Police clearance certificate 页面不完整` | ✅ |
| tests/job9-04.mjs | `"passport"`(双引号) → `Passport` | `图片应是 Passport 但模糊` | ✅ |
| tests/job9-04.mjs | 未加引号的 `bank`（`your bank statement`）不误替 | 原样返回 | ✅ |
| tests/job9-04.mjs | `buildPrompt('criminal')` 无「代号」字样、无裸 `criminal`、含英文名 | true | ✅ |
| tests/job9-04.mjs | 全 22 个 doc key 的 `buildPrompt` 模板均无带引号裸代号 | leak=[] | ✅ |
| tests/job9-04.mjs | `docName` 已知→英文名、未知→原样回退 | true | ✅ |
| tests/job9-04.mjs | `DOC_NAMES` 覆盖前端全部 doc key | missing=[] | ✅ |
| 结构自检 | 花括号/圆括号平衡 | `{`=81 `}`=81 `(`=203 `)`=203 | ✅ |
| — | `deno check` | **本机未装 deno + 不挂代理无法装 → 未跑**；PURE 块已 node 单测 7/7，全文结构自检通过；**部署前请业主机跑一次 `deno check`** | ⚠️ 待部署前 |

## 必须停
**未部署。** 部署命令由 Claude 另出卡给业主（Supabase 网页 Functions 编辑器整页粘贴 → Deploy，关 Verify JWT；不影响线上其他函数）。部署前线上行为不变（预检 cron 仍打旧版；本次仅改源码）。

## 动过的文件
- `supabase/functions/precheck-ai/index.ts`（源码，已 commit `b2cedbe`，未推、未部署）
- `tests/job9-04.mjs`（随卡单测入仓）
- 前端/五端 HTML / 表 / 桶：**零改动**
