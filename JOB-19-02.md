# JOB-19-02 回执 · 审核端 v44 —— **P2 图纸冲突，停工报告，未自修、未动一行代码**

## 一句话
19-02 开工第一步（19-00④ 与 19-01 交接明文要求的 P2 核对）已做完：**图纸 19-02 闸3 ①「入册后是四语正式名，切英/俄/法各截一张」与审核端现实（无语言切换器、`currentLang` 恒 'zh'、15 处标题恒渲染 `docType.zh`、现役 25 条 DOC_TYPES 全是 zh 单名）直接冲突**。按铁律14 P2「遇图纸与现实冲突，除图纸内明文预授权的映射外一律停工报告原文，不得自修」——CC **未改审核端一行、未升版**，把冲突原文与三条候选裁决交主审（Claude / 业主）定夺后再施工。

## 做了什么（只读核对，零改动）
基线核对：`reviewer-portal/maxhouse-reviewer-portal-v43.html` md5 `183215bc92cddf0a6b23add256bd43c3` = QUEUE-19 §0.2 基线，未动。

三条只读证据（源文件行号实证，非转述）：

1. **审核端没有语言切换器、`currentLang` 从不被赋值**
   - 头注两处白纸黑字：
     - v41 头注（第 3 行）：「…审核端无语言切换器故未挂 setLang。」
     - v39 头注（第 5 行）：「…审核端无语言切换器故未挂 setLang；…」
   - `grep -nE 'currentLang\s*=' v43` 只命中第 1903 行的**比较**（`typeof currentLang === 'undefined'`），**全文没有任何一处给 `currentLang` 赋值**。它在 1903 / 2454 / 3013 / 3292 处只被**读取**，且四处都写死「取不到就 'zh'」的守卫。
   - 结论：审核端页面上 `currentLang` 恒为 'zh'，**没有任何 UI 或代码路径能把界面切到 en/ru/fr**。

2. **15 处材料标题恒渲染 `docType.zh`（单语字段）**
   - `grep -cF 'docType.zh' v43` = **15**。关键渲染点：卡片标题 2306、AA 类型药丸 2370、审批日志 2442、退回弹窗 2473 等，全部只取 `.zh`。
   - 兜底 `_v43SafeType`（2253）在 `DOC_TYPES[code]` 未命中时用 `{ zh: <裸串或"未知类型"> }`，同样只有 zh。

3. **现役 25 条 DOC_TYPES 全是 `{ zh, icon, cls, minutes }` 单语**
   - 第 1499 行起，`'passport': { zh:'护照首页', … }` … 逐条都只有 `zh`，**没有 en/ru/fr 字段**。v21 头注亦注明「唯一变化的字段是 zh」。

## 冲突原文（图纸 vs 现实）
- **图纸 19-02 闸3 ①**：「reviewer1 真页面：入册前那些材料标题是兜底名（红）→ 入册后是**四语正式名，切英/俄/法各截一张（绿）**」。
- **现实**：审核端无语言切换器、`currentLang` 恒 'zh'、15 处标题恒渲染 `docType.zh`。**在真页面上无法把标题切成 en/ru/fr 出图**，闸3① 的「切英/俄/法各截一张（绿）」**无法达成**。
- **19-00④ 早已标出的并存不一致**：若为出图把 15 处改成 `docType[currentLang]`、再用 JS 强设 `currentLang=en` 截图，则**新入册条目显 en/ru/fr、现役 25 条（只有 zh）仍显中文**——同一张 en 截图里中英文混排，与「现役条目=真中文名」口径打架。要消除这不一致就得给 25 条现役类型逐条补 en/ru/fr 名——而「四语名按码原样读出、不许手抄」只覆盖**新增**类型，现役 25 条的四语名图纸没给来源，手补=违「不许手抄」。
- 任一消解路径都要么**新增审核端语言切换机制（新增 UI / 行为，违只修不加 + 铁律10）**，要么**改现役渲染语义 + 手补现役四语名（违不许手抄、且制造新老不一致）**。**这属设计口径裁决，CC 不得自决**（铁律14 P2）。

## 可施工的部分（已确认可做，等裁决后落）
- 入册数据侧本身**可做且安全**：19-00④ 已从学生端主文件/语言包按码读出四语名——
  - `studyProof` → zh/en「Proof of study」· ru「Справка об обучении」· fr「Attestation d'études」
  - `studyPlan` → zh/en「Study plan」· ru「План обучения」· fr「Plan d'études」
  - （未登记类型全集：`studyProof×2 / studyPlan×2 / null×1`，共 5/60；`null` 刻意不建条目、照旧走裸串兜底，与 v21「脏数据不背书」口径一致）
- 卡住的**只是**渲染/切换/出图这一侧（闸3①）与由此牵出的新老一致性口径。

## 给主审的三条候选裁决（请择一，CC 照裁决施工，不自选）
- **甲**：入册只写 `zh`（与现役 25 条完全同构，`docType.zh` 渲染不动、零新老不一致），达成「审核端真页面看到的是正式中文名而非英文代号 studyProof」这一**真实目标**；**闸3① 改为「入册后标题=正式中文名」并按 P3a 由主审明文授权改写断言**（不再要求 en/ru/fr 截图，因审核端本就 zh-only）。← CC 建议：最小、零冲突、达成实际目的。
- **乙**：入册写全四语 `{zh,en,ru,fr}`，渲染点 15 处一律不动（仍 `docType.zh`），四语数据只是**入册待用**；闸3① 同甲改断言。数据侧满足「四语名入册」字面，但真页面仍 zh。
- **丙**：确要审核端能切 en/ru/fr 出图 → 需**新增审核端语言切换机制 + 现役 25 条补四语名**——这已超「只修不加」且触铁律10（新 UI 须先请示），须主审单独立项授权来源与范围。

## 新闸证据
- **N1**：本包**零代码改动**，无差集可算；审核端仍 v43（md5 未变）。
- **N2**：旧病红已在 19-00 复现（C：studyProof/studyPlan/null 走 `_v43SafeType` 显英文代号/「未知类型」= 红）；本包因 P2 冲突未施工，绿测留裁决后。
- **N3–N4**：本包未登录测试账号、未改任何云函数、未写库。
- **N5**：无新增截图。
- **N6**：未改任何端一行。
- **N7**：本包无提示件改动。

## 业主要做什么
- 本包 CC 未改代码、未升版、不推主仓库、不部署、不手贴 SQL。
- **需要一次裁决**：审核端「四语名入册」在 zh-only、无语言切换器的现实下如何落——甲 / 乙 / 丙（见上，CC 建议甲）。裁决后由后续趟按裁决施工并按 P3a 改写闸3① 断言。

## 停工与下一步
- 按铁律14 P2 停工报告，未自修。为免外层脚本反复开新趟、每趟都在 19-02 撞同一冲突空转，已写 `~/mh-verify/r19-STOP.txt`（三行大白话）暂停本轮，等主审对甲/乙/丙裁决后再续。
- 下一包 = 19-02（同包，待裁决后按选定方案施工；届时闸3① 断言按 P3a 由主审授权改写）
