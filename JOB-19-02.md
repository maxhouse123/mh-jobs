# JOB-19-02 回执 · 审核端 v44 —— 未登记类型入册 + 预取日志标签（定案选甲，正式施工）

## 一句话
审核端队列里 studyProof/studyPlan 两种 doc_type 此前不在 `DOC_TYPES`，被 `_v43SafeType` 兜底成英文代号显给审核员；本包按 Claude 19-02 裁决**选甲**入册两条（只写 zh、与现役 25 条同构、15 处渲染一行不动），并把预取入队日志标签 `[v42]→[v44]`。**reviewer v43 → v44**，四件套齐；真页面红绿 + N1 差集空 + P3 回归 13/13 全绿。CC 只 commit 一笔、**未推主仓库、未部署、未手贴 SQL**。

## 停工经过（保留：19-02 首趟为何停工）
19-02 首趟开工第一步做 P2 图纸核对时，发现图纸原闸3① 要求「入册后是四语正式名，切英/俄/法各截一张」，与审核端现实（无语言切换器、`currentLang` 恒 'zh'、15 处标题恒渲染 `docType.zh`、现役 25 条 DOC_TYPES 全是 zh 单名）直接冲突：真页面无法把标题切成 en/ru/fr 出图。按铁律14 P2「遇图纸与现实冲突一律停工报告不自修」，CC 当趟未改一行、把冲突原文与甲/乙/丙三候选交主审定夺（首趟回执见 git 历史 / 本文件上一版）。**本趟依 Claude 裁决选甲重新施工。**

## 19-02 裁决（Claude 定案·选甲）落地
- ①入册**只写 zh**，与现役 25 条 `{zh,icon,cls,minutes}` 同构，15 处渲染一行不动；闸3① 断言按 **P3a 由 Claude 授权改写**为「入册后标题=正式中文名」，不再要求 en/ru/fr 截图（审核端本就 zh-only）。
- ②中文名来源=**管理端现役 `admin-portal/maxhouse-admin-portal-v218.html` 的 `ADM7_KINDS`**（非学生端——学生端 zh 词典里 `doc.studyProof/doc.studyPlan` 存的是英文 Proof of study/Study plan）；用脚本按键**原样读出 zh，未手抄**；icon 照**学校端 `school-portal` 现役上传格**同键读出（studyProof 📖 / studyPlan 📋）；cls/minutes 各照现役性质最接近的一条。
- ③`doc_type=null` 那 1 条**不建条目**、照旧走裸串兜底。

## 做了什么

### 1) 数据按码原样读出（不手抄）→ `jobs/JOB-19-02/doc-types-added.json`
脚本 `jobs/JOB-19-02/extract-doc-types.mjs` 从三份现役文件按键正则读出，落盘 json 并贴此：
```json
{
  "studyProof": { "zh": "在读证明", "icon": "📖", "cls": "doc", "minutes": 3,
    "_src": { "zh": "admin v218 ADM7_KINDS", "icon": "school v234 上传格 key=studyProof", "clsMinutes": "reviewer v43 education(最高学历)" } },
  "studyPlan":  { "zh": "学习计划", "icon": "📋", "cls": "doc", "minutes": 6,
    "_src": { "zh": "admin v218 ADM7_KINDS", "icon": "school v234 上传格 key=studyPlan",  "clsMinutes": "reviewer v43 statement(个人陈述)" } }
}
```
- **zh**：`admin v218` 第 6417 行 `ADM7_KINDS` 里 `studyProof:{zh:'在读证明',en:'Proof of study'}`、`studyPlan:{zh:'学习计划',en:'Study plan'}`，脚本按键原样读出。
- **icon**：`school v234` 第 19662/19664 行上传格 `{key:'studyProof', icon:'📖'…}` / `{key:'studyPlan', icon:'📋'…}`，按键读出。（注：`icon`/`cls` 在审核端现役均不参与渲染——v40 起卡上图标改按真实扩展名走 `_V40_EXT_CLS`；此二字段仅为「与现役同构 + 留源」入档。）
- **cls/minutes**：审核端现役无 studyProof/studyPlan 参照，照性质最接近的一条读出——**studyProof 照 `education`(最高学历)=doc/3**（学历/在校类）、**studyPlan 照 `statement`(个人陈述)=doc/6**（申请信/陈述类）。回执点名照的是哪一条。

### 2) 入册（reviewer v44）
`DOC_TYPES` 末补两条（源码，带 v44 注释说明来源）：
```js
'studyProof': { zh: '在读证明', icon: '📖', cls: 'doc', minutes: 3 },
'studyPlan': { zh: '学习计划', icon: '📋', cls: 'doc', minutes: 6 }
```
`_v43SafeType` 兜底原样保留（`doc_type=null` 那条仍走它 →「未知类型 / Unknown type」）。15 处 `docType.zh` 渲染点一行未动。

### 3) 预取日志标签 `[v42]→[v44]`
第 2807 行 `console.info('[v42] prefetch enqueue'…)` → `'[v44] prefetch enqueue'…`。**只改标签字符串**：预取队列/泵的逻辑标识符 `_v42Q`/`_v42Pump`/`_v42Enqueue` 等**一律未改名**（那是逻辑不是标签）。`[v43] wm timing` 是另一条、留 19-03 改 `[v45]`，本包不动。

### 4) 版本四件套
①文件名 `maxhouse-reviewer-portal-v44.html`；②头注 v44（2026-09-23）；③`[VER] reviewer portal v44` 探针（同步改）；④`reviewer-portal/index.html` 三处重定向目标 → v44。changelog `docs/changelog/reviewer-portal-versions.md` 追一行。
（VER 探针文案里**不含字面量 `[v42]`**，避免它自身 console 输出触发「控制台无 [v42]」误判——用「预取入队日志标签升到 [v44]」表述。）

## 结果 / 闸3

| 闸 | 结果 | 证据 |
|---|---|---|
| ①真页面红绿（改断言后：入册后标题=正式中文名） | **绿** | 本地 serve + 真调**live 渲染路径** `studentDocRow`（第 2233 行 `studentCaseCard`→`studentDocRow` 即队列真渲染路径；sb 全 stub、不连库）。v43: studyProof→`"studyProof"`/studyPlan→`"studyPlan"`（红）→ v44: `"在读证明"`/`"学习计划"`（绿）；null 两版都 `"未知类型 / Unknown type"`（兜底不回归）。`jobs/JOB-19-02/render-redgreen.json` PASS=true；元素级截图 `v43-title-RED.png` / `v44-title-GREEN.png` |
| ②控制台无 `[v42]`、有 `[v44]` | **绿** | 运行时 `console.*` 调用体 grep `[v42]`=0、预取入队日志已是 `[v44]`（test T6/T7；byte-diff 见下） |
| ③kit2 图片尺子 + PDF 尺子 | **绿** | 19-02 未碰任何查看器/PDF/图片/openPreview 代码——v43↔v44 逐字节 diff 仅 4 处（头注/DOC_TYPES 两条/标签/VER 探针），查看器集成路径 byte-identical；`assets/mh-viewer-v2-1-1.js` 引用不变；kit2 自检 `run-selftest.mjs` = `SELFTEST MHV2-ALL-AS-EXPECTED (chromium)`（正控全绿、负控如期红） |
| ④N1 差集 | **绿** | 同一提取器 v43 vs v44：ids 74→74、handlers 25→25、functions 140→140，**0 lost / 0 added**（`jobs/JOB-19-02/n1-diff.json`）。入册是数据、标签是字符串，无按钮/id/函数增减 |

### v43↔v44 逐字节 diff（只 4 处，证「只修不加」）
1. 头注新增一行（v44 说明）
2. `DOC_TYPES` 末 `'work'` 行补逗号 + 注释 + studyProof/studyPlan 两条
3. 第 2807 行标签 `[v42]`→`[v44]`
4. `[VER]` 探针文案 v43→v44
其余全等（含 openPreview / PDF / 图片 / 预取泵 / 队列逻辑）。

### P3 随卡回归（入仓）
`tests/static-check-v44-reviewer.mjs` = **ALL GREEN 13/13**（主脚本 parse 干净 / 四件套 / 两条入册同构 / v43 无此二条(旧红前提) / `_v43SafeType` 兜底原逻辑 / docType.zh 渲染点=15 不变 / 运行时 console 无 [v42] 有 [v44] / `_v42Q`+`_v42Pump` 逻辑标识符保留 / 查看器引用不变）。

## md5
- reviewer v44：`8f8707a85de12d56fd0dd4c4cc1a8408`

## 业主要做什么
- 本包无需操作（不推主仓库、不部署、不手贴 SQL）。主仓库本包 commit 一笔待收官卡统一推。
- 收官后可亲眼验收（进 SUMMARY「要业主亲眼看的」）：审核端队列里在读证明 / 学习计划两类材料的标题显示为正式中文名（而非英文代号 studyProof/studyPlan）。

## 新闸证据 N1–N7
- **N1**：差集空（ids/handlers/functions 各 0 lost/0 added），仅数据+标签字符串变化。
- **N2**：旧病红在 19-00 已坐实（C：studyProof/studyPlan 走 `_v43SafeType` 显英文代号=红）；本趟真页面红绿成对（v43 显代号 / v44 显中文名）。
- **N3**：本包渲染红绿走**本地 serve + sb 全 stub、只点本地、不连库、零登录、零 DB 写**；未登录任何测试账号（入册是纯前端数据，无需真队列即可红绿——studentDocRow 是队列 live 渲染路径，脚本喂的 docType 与真队列同值）。19-00④ 已用 reviewer1 真登证明真队列含 studyProof×2/studyPlan×2。
- **N4**：本包不改任何云函数（watermark-doc/precheck-ai/admin-doc 一行未动）。
- **N5**：`jobs/JOB-16-00/secret-scan.mjs ~/mh-jobs/shots/19/19-02` = **SECRET-SCAN-CLEAN**；截图为合成测试行、无真实学生 PII、无账号邮箱。
- **N6**：只改审核端一个文件（+ 其 index / changelog / 一支 tests 脚本 + 本包 jobs 工装）；未顺手动其它端；共用查看器/学校端/管理端/云函数零改动。
- **N7**：本包无「给人看的提示件」改动（未动 toast/卡片/提醒），N7 不适用；仅材料标题文案由代号改中文名，属静态渲染文字，红绿已量（见闸3①）。

## 测试写库收尾
- 本包**零 DB 写入、零登录**（render 用 stub sb，不连真库）。无测试件需清理；无带身份探针复核之必要（未写任何表/桶）。

## 给 19-03 的交接
- 审核端进到 v44。19-03 = watermark-doc v34（改源码不部署，加 `x-mh-wm-timing` 计时头 + boot_age + served）+ 审核端 v45（`[v43] wm timing` 旁加打 `[v45] wm server` 行；本包留的 `[v43]` 标签 19-03 改 [v45]）。
- 19-00⑤ 静态分段结论已喂 19-03：v33 已有 Server-Timing，能修的只有 (a) 角色判定并行；(b)(c) 基本无空间；冷启动占大头则按 §0.10 只写保温建议、不加定时任务。
- **第二十轮建议（收官 SUMMARY-19 须收录，见 `jobs/JOB-19-02/round20-note.md`）**：学生端/学校端 zh 词典里 `doc.studyProof`/`doc.studyPlan` 存的是英文（Proof of study/Study plan），**中文界面的学生看到的是英文栏目名**；本轮不改学生端词典，建议第二十轮把这类英文占位对齐成真中文名（在读证明/学习计划）。
- 下一包 = 19-03
