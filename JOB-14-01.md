# JOB-14-01 · 止血：坏件零下载 + 预检不啃大图 + 队列带状态（0203/0204 + 两只 EF + reviewer v37）

## 做了什么
- **迁移 0203（已应用）**：给缓存索引加「render_v」列 + 「wm_render_v」重灌开关；**重写兜底函数**——下载前先在 SQL 里按类型/体积过滤：不支持类型（docx/mp4…）标 `unsupported`、图片 >3000KB 标 `too_large`，**都不发请求不下载**；其余照旧 3 次放弃；审核队列视图加 wm_status/wm_error/wm_attempts 三列；失败件清单也返回 unsupported/too_large。
- **迁移 0204（业主跑）**：给审计值加 `served_video_signed`（14-03 用）。**需重建 CHECK 约束、被 db-run.sh 红线挡，留业主在部署卡 SQL Editor 跑**（照 round-13 0199 先例）。
- **云函数 watermark-doc（改源码未部署）**：prepare/render/signed_url 下载原件前先读索引，已判死的件（gave_up/unsupported/too_large 或 failed 满 3 次）直接返回错误、不下载；prepare 加下载前扩展名闸 + 体积闸。
- **云函数 precheck-ai（改源码未部署）**：批处理下载前先看体积，>3000KB 直接判 error 且永不重试（**这是烧流量的主凶**）；出错的件重试封顶 3 次。交付部署脚本 `deploy-precheck.sh`（带 `--no-verify-jwt`，因 14-00 查明它自校验 x-precheck-key）。
- **审核端 v37（第一部分）**：打不开的材料卡直接显示「🚫打不开·人话原因」+「退回·让学生重传」，点看不发任何请求；后台预取跳过这些；失败集挂 window 级刷新不清空；水印/预取控制台前缀统一 [v37]。

## 结果是什么（每个数字带命令出处）
- **0203 验证**（db-run 末尾）：render_v 列在、wm_render_v 开关在、函数体含 unsupported+wm_render_v、队列视图含 wm_status = 全 true。
- **止血实证**（`bash jobs/db-run.sh jobs/JOB-14-01/backfill-run.sql`）：跑一次 `wm_cache_backfill(20)` → **发出 0 条请求**（`NOTICE: 发出 0 条 prepare`）；分类结果：
  - 2 张 6MB「big size 2.jpg」→ **too_large**（err「image too large」），**零下载**
  - 6 个 docx/mp4 → **unsupported**，**零下载**
  - 2 份 894KB 坏 PDF → **gave_up**、attempts=3、last_error 含 **PDF_UNREADABLE**
  - status_counts = `too_large 2 / unsupported 6 / cached 95 / gave_up 2`
  → 闸3 全中：判死的件零新请求、docx/mp4→unsupported、6MB 图→too_large、坏 PDF→gave_up。
- ⚠️ **点名冲突（§0.1）**：14-01a 写「图片 >8000KB → too_large」，但闸3 要「6.0MB(6189KB) 图变 too_large、零请求」，6189<8000 矛盾；且 precheck 用 3000 为界。故 **too_large 阈值取 >3000KB**（与 precheck 对齐、真止血、过闸3），已在 0203 头注点名。
- **两只 EF**：括号平衡检查（无 deno/tsc，用括号配平做语法级）均 **BALANCED ok**；新 md5：watermark-doc `e3e89cf7442e3053f1dab60d2fc4d3ba`、precheck-ai `da52fad0b760a250c81f36ec8c657416`。均**未部署**。
- **deploy-precheck.sh**：`bash -n` **SYNTAX OK**。
- **reviewer v37 闸3**（`node jobs/JOB-14-01/gate3-reviewer-v37.mjs`）：`GATE3-PASS`——判定/人话原因正确、预取跳过判死件、卡显「打不开」+「退回」2 件、判死卡无查看按钮、退回预填英文、**0 个非 file 请求**、0 真实报错。
- 版本四件套：reviewer 文件名 v37 / 头注 v37 / [VER] console v37 / index 3 处切 v37；changelog reviewer + edge-functions 各加行；0203/0204 已 cp 进镜像（ledger 差集为空）。

## 业主要做什么
1. **部署卡**里先在 SQL Editor 跑 `supabase/migrations/0204_outcome_video_signed.sql`（加审计值）。
2. 部署两只云函数：watermark-doc（新 md5 e3e89cf…，控制台代码页）、precheck-ai（跑 `bash jobs/JOB-14-01/deploy-precheck.sh` 或控制台，**关 Verify JWT**）。**部署 precheck-ai 后账单主凶即止**（它不再每 5 分钟重下 6MB 坏图）。
3. 部署+推送后：审核端 v37 打开一份坏件 → 应直接看到「打不开·原因」+「退回·让学生重传」，点看不再转圈。
