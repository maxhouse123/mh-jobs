# JOB-22-01 回执 · `_headers` 预加载规则只挂网页文档

CC 执行。只改站点根 `_headers` 一个文件的 **Link 块**；缓存块一字未动；无端 HTML 改动、无版本 bump。未 push（§0.9）。

## 一、做了什么（唯一改点）
把 v3 挂在 `/<portal>/*` 上的五个 Link 块**删掉**，改挂到每端的**两条网页文档路径**：
- `/<portal>/`（目录 index 跳转页）
- `/<portal>/maxhouse*portal*v*`（版本文件，沿用缓存块已有的两种命名：student 下划线 `maxhouse_student_portal_v*`，其余连字符 `maxhouse-<portal>-portal-v*`）

`vendor/*`、`assets/fonts/*` 等资源响应**不再带 Link**。文件顶部加一段 v5(2026-09-25) 注释记录病根与改法。**Cache-Control 块一字未动**（diff 里无任何 cache-control 行改动，已核）。

**预加载载荷一字未改**（只搬 Link 归属路径）：
- preconnect Supabase 保留。
- `vendor/supabase.js` 的 `as=script` **不带 crossorigin**——与五端 `<script src=.../vendor/supabase.js>`（都无 crossorigin，22-00 ① grep 实证）一致。
- 字体：五端首屏 22-00 本地实测 400/700 **均无 unused 告警 = 都在用**，故 **700 保留不删**；partner 本就无字体预加载，维持。

### ⚠ 图纸「审核端去掉 700」未执行（承 22-00 P2 冲突，留业主/Claude 定）
QUEUE-22 ① 的前提「审核端 @font-face 只有 300/400/500/600」经亲读 reviewer v45 L166–171 **证伪**（L170 有 700、L801 有 800）；且本地实测 reviewer 首屏 700 在用（0 unused 告警）。盲删会把首屏在用字体降级、与提速相反。本包**只收 Link 作用域（根病、无争议）、不删 700**，gate3 结构信号照样过。不自修、原文冲突已在 22-00 回执与本包 `_headers` 注释里报明。

## 二、闸3（先红后绿，`jobs/JOB-22-00/probes/probe-preload.mjs` 读活 `_headers`）
| 项 | 改前（22-00） | 改后（本包） |
|---|---|---|
| Link 子资源泄漏（结构红信号） | 5 条（五端 vendor/supabase.js 各 1）**红** | **0 条 绿** |
| 网页文档带 Link | 5 端各 1（document） | 5 端各 1（document，载荷含 700 完整 295 chars/端） |
| console「preloaded but not used」 | 本地 0（CF 边缘，未复现） | 本地 0 |

- **结构红→绿达成**：`STRUCTURAL: RED (5 leaks)` → `STRUCTURAL: GREEN (Link only on documents)`。
- console 告警本地恒 0（CF Early Hints 本地发不了，22-00 已定性、检测器 `diag-detector.mjs` 已验真有效）；**线上真效果由 22-05 走查核**（响应头 Link 只出现在网页文档上；五端控制台不再有该告警）。
- `_headers` 语法自检：`parseHeaders` 解析 **34 条规则、0 条空规则**；每条规则一行路径 + 缩进头。document 拿全 Link（reviewer HAS-700）、vendor/supabase.js 与 fonts **无 Link**、Cache 各自不变（vendor 7d / fonts 1y immutable / 版本文件 30d immutable）—— 逐路径实证。

## 三、N 闸
- **N1**：本包**不改任何端 HTML、不升版**，只改站点根 `_headers` → 无按钮差集可比（N/A，回执点名）。
- **N5 secret-scan**：mh-jobs push 前扫 CLEAN（见下）。
- **N6**：只改 `_headers` Link 块，未顺手动别处；缓存块、五端 HTML、云函数、共用查看器一律未碰。
- **shell 卫生**：本包无新 `.sh`；`jobs/JOB-20-04/shell-lint.sh` 全绿。

## 四、状态
- 主仓库 `_headers`（+16/−5，仅 Link 块）点名 add + commit，**未 push**（§0.9；`_headers` 随主仓库推送由 CF Pages 生效，业主收官推）。
- 本回执 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**：结构红→绿、语法自检过、缓存块零改、载荷未删、shell-lint 绿、secret-scan 绿。线上效果待 22-05 走查 + 业主线上核。

下一包 = 22-02
