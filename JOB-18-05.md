# JOB-18-05 · 三张审计表补值 —— 结论：三张表都「无需改」，不写迁移，无待业主手贴 SQL ✅

> 依据 QUEUE-18 §18-05「某张表没有 outcome 约束或已全收 → 写『无需改』并附证据；三张都无需改就不写迁移」。18-00 探针④已定此结论，本包用**当场 db-run 只读**复核一次，证据附下。

## 一、要补的是什么
QUEUE-18 §18-05：按 18-00 ④ 的差集，**只**给每张 `*_access_log` 表补「watermark-doc 函数会写、但 CHECK 约束不收」的 outcome 值。前提是**存在 outcome 的 CHECK 约束**且**收的取值窄于函数会写的取值**。

## 二、当场复核（`bash jobs/db-run.sh jobs/JOB-18-00/probes/probe4-audit-constraints.sql`，只读 begin/rollback，N3 带库内身份）
（a）三张表的 `pg_constraint contype='c'`（CHECK 约束）：
```
 tbl | conname | def
-----+---------+-----
(0 rows)
```
（b）outcome 列类型：`school_access_log.outcome / student_access_log.outcome / partner_access_log.outcome` 均为 **`text`**（无约束、无枚举）。
（c）近况 outcome 分布（全时）：
- `school_access_log`：served_original 385 / served 300 / served_video_url 1
- `student_access_log`：served 70 / served_video_url 4
- `partner_access_log`：0 行

## 三、差集 = 空 → 无需改
- 三张表的 `outcome` 都是**纯 `text`、没有任何 CHECK 约束**（探针 0 行，今日复核仍 0 行）。
- watermark-doc v33 各表会写的值（`audit()` 路由 L645-652 + 直写，18-00 ④ 逐行列出）：
  - school = {served_original, served, served_video_url, denied_doc_deleted, denied_not_candidate}
  - student/partner = {served, served_video_url, denied_doc_deleted, denied_not_owner}
- **既无约束，「函数会写、约束不收」的差集 = 空**。任何 text 值都能插，没有值会被拒。
- **结论：三张表都「无需改」；本包不写 `supabase/migrations/0213_*.sql`，无待业主手贴 SQL。** 下一个空号仍是 0213，留给后续包。

## 四、闸3
- **18-00 ④ 的 rollback 插入测试 = N2「未复现」**（保留）：三张表无 CHECK 约束 → 无值可拒 → 「演前被拒、演后能插」的红/绿前提**不成立**（无约束可 drop/replace，也无迁移文件可演）。这与「无需改」结论自洽。
- 未产出迁移文件，故「本机临时库/rollback 事务里先演一遍」不适用（无 SQL 可演）。
- 证据文件：`jobs/JOB-18-00/probes/probe4-audit-constraints.sql`（只读探针，今日复跑输出如上，末行 `DB-RUN OK`）。

## 五、结果 / 业主要做什么
- **本包无迁移、无写库、无手贴 SQL、无部署、无主仓库推送、无版本升**。纯只读复核 + 定论。
- **业主本包无需做任何事**。SUMMARY-18 的「待业主手贴 SQL 清单」在本包为**空**。
- 若第十九轮要给 outcome **加**约束（把这些 text 值收进一个白名单 CHECK），那是**新增功能**、需业主拍板范围，不在本轮「只修不加」内 → 记入十九轮建议。

## 六、边界
- 未新建/修改任何 migration；未改任何源码；未碰 watermark-doc（本轮 §0.6 明令一行不改）；db-run 全程只读 begin/rollback。

---
**下一包 = 18-06**
