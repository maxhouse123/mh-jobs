# JOB-6-04 回执 · 三端改读开学季配置表（2026-09-16）

> 大白话版。做完了：学生、中介、学校三端的「意向入学学期」下拉，现在都先去读 JOB-6-03 建的那张后台表；**表里有配置就用配置，表空了或读不到就自动回落到原来写死的学期**（保证学生永远选得了）。存进库里的值还是老格式（英文 "Fall 2026"），显示按当前语言（中/英/俄/法）。**主仓库已提交没推。**

## 三端各改了什么
| 端 | 版本 | 改的下拉 |
|---|---|---|
| 学生 | v380→**v381** | 自填资料 + 委托填资料 两个学期下拉 |
| 中介 | v148→**v149** | 委托表单 + 发邀请向导 两个学期下拉 |
| 学校 | v229→**v230** | 建招生任务时的「意向入学学期」下拉 |

## 关键设计（三端一致）
1. 每端加一个「读配置」小函数 `_loadIntakeTerms()`：登录后预取一次、缓存住，调 `list_intake_terms()`。
2. 学期下拉的「学位」分支：**有配置用配置**（选项值仍用英文 label_en，跟老数据和三端对齐），**没配置/出错回落**原来的「当季+后几个 Spring/Fall」生成器。
3. **显示按当前语言**：中文显示"2026 年秋季"，英文显示"Fall 2026"，俄/法同理——文字直接来自配置表那四列，**没新增任何翻译词条，翻译包一个字没动**。
4. 短期语言/月度那种滚动月份的分支**不动**（那本来就是相对当月的，不入表）。

## 验收
管理端「开学季」加一个新学期并启用后，学生/学校/中介三端刷新，学期下拉里能看到它；切语言显示对应语言的标签。（真四语切换 + 新增可见需登录，留你验收；数据侧 JOB-6-03 已就位。）

## 自验（三层全过，Playwright 本地 file:// 全程 abort supabase 不碰生产）
- **学生 v381**：空配置→回落 Fall 2026/Spring 2027/Fall 2027；注入配置+中文→选项**值=英文、显示=中文**；两个生成器都改到；零报错。
- **学校 v230**：回落=英文 Spring/Fall；配置+中文→值"Fall 2026"、显示"2026 年秋季"；零报错。
- **中介 v149**：两个生成器 + 读配置函数都在；空配置回落；配置→值 Fall 2026、显示"秋"（中文）；零报错。
- 报告全文见主仓库 `docs/self-test-report-JOB-6-04.md`；脚本 `~/mh-verify/gate8_job6_04_student.mjs` + `gate8_job6_04_ps.mjs`。

## 版本 / 提交
- 学生 v381 / 中介 v149 / 学校 v230（旧版全保留）；各端 index 4 处 + [VER] + 头注/changelog 均已改。
- 主仓库：三端 HTML + 三 index + 三 changelog + 自验报告，**已 commit 未 push**。**零迁移**。

**边界守住**：只动三端学期生成器；存储值格式不变；翻译包引用不动；无新增文案键。
