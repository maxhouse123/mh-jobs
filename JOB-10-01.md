# JOB-10-01 · stuA 残留清查

结果：**通过（无需任何写操作）**

## 大白话
把测试学生 stuA 名下第八、九轮测试残留的文件清点了一遍。好消息：**数据库里已经没有一条测试文件记录了**（第八九轮那些 `mh8-`/`mh9-` 开头的上传当时全都失败，没在数据库落下行）。真正需要清的活文件数 = 0，所以本包**一个字节都没删、没改**。

唯一发现：网盘（storage）里躺着**1 个孤儿文件**——第八轮那个 3MB 的测试图 `mh8-test-3mb.jpg`，它在数据库里没有对应记录（是当时上传半截、清理时漏网的空壳）。按卡要求「只列不删」，把它记进 backlog，由业主决定要不要清（它不是真实学生资料，是测试残渣）。

stuA 的 `bank` 槽（银行资金证明，非闸门槽）现在是原来的 2 个文件，没有被锁，第十轮 10-03 手机测试可以继续用这个槽。

## 断言表
| 脚本 | 断言 | 实测值 | 过不过 |
|---|---|---|---|
| probe3.sql S1 | documents 里 stuA 的 mh8-/mh9- 行（含软删） | 0 行 | ✅ |
| probe3.sql S2 | **活文件里 mh\* 数 = 0**（卡的收尾判据） | 0 | ✅ |
| probe3.sql S3 | storage student-documents 桶 stuA 前缀 mh8/mh9 对象 | 1 个：`bank/1789571102495_4vtz_mh8-test-3mb.jpg`，3145728 字节（3MB） | ⚠️ 孤儿，只列不删→backlog |
| probe3.sql S4 | bank 槽当前活文件 | 2 个（Screenshot_20260822-140932.png / 17895371845752916358861810946978.jpg，均 version1、未删）；无锁 | ✅ 非锁定 |
| — | doc_slot_batches 表 | **该表不存在**（见下） | — |

## 数据血缘说明（铁律14）
- 卡步骤4 让查 `doc_slot_batches` 表，但库里**没有这张表**（也没有任何名字带 slot/batch 的表）。槽的「批次/锁」不是一张独立表。故改用等价判据：直接数 `documents` 里 bank 槽的活文件（=2，未锁）。这个结论足以支撑 10-03 的选槽决策（继续用 bank）。
- documents 表无 `id`/`created_at` 列，真实列是 `doc_id`/`uploaded_at`，回执按真实列名取。

## 动过的文件
- 无数据库写操作（step2 的 update 仅在有活 mh 文件时才跑，本次 0 个 → 未跑）。
- 新增：jobs/JOB-10-01/{probe.sql, probe2.sql, probe3.sql, report.md}（探针，只读）。

## backlog（交 10-07 收官）
- storage 孤儿对象 1 个：`student-documents/43668ae4…/bank/1789571102495_4vtz_mh8-test-3mb.jpg`（3MB，第八轮 mh8-test 残留，无 documents 行）。由业主决定是否手工清。
