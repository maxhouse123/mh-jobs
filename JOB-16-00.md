# JOB-16-00 回执 · 开工检查 + 带身份探针 + 旧病复现 + 按钮清单基线

第十六轮第 0 包。全程只读侦查 + 造检验脚手架，除下述测试件外**零写库**（本包连测试派单配方也未落库，留到需要时）。

---

## 一、做了什么（大白话）

1. **对表开工**：五端文件、迁移账本、主仓库 HEAD 逐个核对，全对上。
2. **三把钥匙试门**：学生 A、中介 1、审核员 1 各真登一次，都进得去。
3. **搬进新工具箱**：解包成功、8 个正常场景全绿、2 个故意做坏的按预期报红——Chrome 和 Safari 两个浏览器内核都过了（Safari 这次居然装上了，比预想的好）。
4. **造了一套“真页面”脚手架**：能把仓库里的整页在本机开起来、用真表单真登录、连真后端，后面每一包都用它。
5. **带着身份去查库**（这是本轮的重点新规矩）：以“具体某个人”的身份查那些“按登录人过滤”的表，拿到了真事实。
6. **在真页面上把中介端那条毛病点成了红**（先红），并存了图。
7. **给五端各存了一份“按钮清单”**，以后每升一版都拿它比对，防止“原来有的按钮悄悄没了”。

---

## 二、结果是什么（数字带出处）

### 开工闸（都过）
- 五端 md5 = SUMMARY-15 五端表，逐个相等（`md5 -q`）；主仓库本地 HEAD 与远端都是 `6180893`（`git log/rev-parse`）。
- 三账号真登：`stuA OK / partner1 OK / reviewer1 OK`（`acct-check.mjs`，只打印 uid 前 8 位，不碰密码）。
- 工具箱：`KIT-UNPACK-OK 7 files`；`mh-viewer-v2.js` md5=`5f84a4ee0fd239d46c177b75ec346f1d`（与卡内一致）。
- 自测：`SELFTEST MHV2-ALL-AS-EXPECTED (chromium)` 和 `... (webkit)` 两行都出。**WebKit（Safari 内核）本机装成功**，不再是“装不上”。

### 带身份探针（每条都回显了 auth.uid()，见 probes/pb*.sql 与 db-run 输出）
- **审核队列**（以 reviewer1 身份，uid 回显 `80c62372…`）：队列 **61 行**；`259f456a` **在队列里**（`has_259_in_queue=t`）。但换默认身份一查，`259f456a` 的派单 **早已撤销**（revoked_at=2026-09-17，理由「JOB-11-03 test cleanup」），材料本身没软删。队列视图 `reviewer_doc_queue` 的定义里 **既没有 `revoked_at is null`、也没有 `deleted_at is null`**（`pg_get_viewdef` 全文在 probes/b0-schema.sql 输出）——这就是撤销件还赖在队列里的根。
- **中介邀请**（以 partner1 身份，uid 回显 `1aad823a…`）：库里 **10 条**（sent 6 / opened 1 / claimed 1 / expired 1 / revoked 1）。两个开关都开着（平台 `delegate_invite_enabled=true`、本中介 `delegate_invite_enabled=t`）。
- **学生材料**（默认身份 + stuA 身份 uid 回显 `43668ae4…`）：stuA 名下 **15 活 + 2 软删**；两张软删正是 `mh10-test-2mb.jpg`(fd4a52eb)、`mh10-test-8mb.jpg`(bf3fb90b)。可 `student_bootstrap()` 返回 **17 条、把两张软删也带出来了**——死图根坐实。另：那份 894KB 旧件 = `f189bbdf` studyProof「Notice of applying.pdf」，未软删。
- **闸函数**（`trg_doc_upload_gate` 全文在 probes/b0b）：见下「结论四」。
- **管理端通知**（admin 身份 uid 回显 `88bd171a…`）：见下「结论五」（**与卡里的猜测不一致，重点看**）。

### precheck-ai 现状（对线上 v21 真打，probe-precheck-now.mjs）
| 鉴权路 | 现状 |
|---|---|
| OPTIONS | 204，`access-control-allow-origin=*` ✅ |
| POST 无凭据 | 401 `{"error":"auth"}` |
| POST 错 x-precheck-key | 401 `{"error":"auth"}` |
| **POST 学生真 JWT（浏览器那条路）** | **401 `{"error":"auth"}`** ← 学生按需预检发不出的根 |
| GET | 405 `method_not_allowed` |

### 旧病复现（N2，先红）
- **R2 中介邀请 = 已复现红**：partner1 真登，页面「已发邀请 **0**」「还没有邀请…」，而 truth 查得 **10** 条 → `R2-VERDICT RED`。图在 `~/mh-verify/shots/JOB-16/16-00/R2-*.png`。控制台 `[dl] flags {platform:true, partner:true}` 证明开关正常载入。**但**「邀请学生自传」按钮在我本机是**在的、可见的**（`btnExists:true`）——按钮消失这半在本机**未复现**（疑似线上时序/数据条件），如实记录，留 16-02 处理时再盯。
- **R4 管理端时间线红字 = 未复现**：见「结论五」，卡里的病因猜测与库里事实对不上。
- **R1 放大缩小 / R3 学生端 / R5 Word 图标**：**病根已用带身份探针/网络探针坐实**（259f456a 在队列、mh10-test 在 bootstrap、预检 401），但“真页面点出红图”这一步按 N2 要在各自的修复包（16-01/16-04/16-08）开头“先红后绿”配对时现做——脚手架已就位，本包先把根查透。

### 按钮清单基线（f）
- 五端静态清单在盘（`inventory/*.static.json`）：admin ids199/fns796、student ids368/fns1108、partner ids205/fns732、school ids170/fns856、reviewer ids74/fns148。
- 三个能真登端的“真页面可见交互件”动态清单在盘：student 71 / reviewer 201 / partner 47（`*.dynamic.json`）。admin/school 的动态清单要 stub，留 16-01 与其新版在同一 stub 下成对采集。

---

## 三、业主要做什么

- **本包无需验收**。但有两件重要的事要请您知道（都影响后面的活）：

  **① 安全提醒（要紧）**：本轮的密码扫描发现，测试账号那把密码（stuA/reviewer1/schX 共用同一把）**早在第 5 轮、第 10 轮就被明文写进了公开仓库 `~/mh-jobs` 的两份旧回执里**（`SUMMARY-5.md`、`JOB-10-04.md`）。这不是本轮造成的，但它在公开仓库里躺着。我**没有**擅自改这两份历史文件、也**没有**动密码——这两件都得您拍板：建议您**重设这把测试密码（重跑卡1116 更新 accounts.json）**，并考虑清一下 mh-jobs 的历史。在您处理前，我的扫描器已改成“新文件带密码就拦、旧文件只警告不拦”，所以不影响本轮推送。

  **② 卡子里 16-07 那条的病因，跟库里事实对不上**（详见结论五）——16-07 的“改用量预警正文”这一步，库里其实**已经是人话文字了**，那条迁移(0209)大概率是空动作。到 16-07 我会按“只做能坐实的那部分（前端兜底）、把对不上的部分停下写清楚请您定”来处理。

---

## 四、新闸 N1–N6 各自的证据

- **N1（按钮差集）**：本包只建**基线**（五端静态 + 三端动态清单在盘），未升版故无差集。`inv.mjs static/diff` 两个模式都已就绪，16-01 起每次升版跑 diff。
- **N2（真页面先红后绿）**：R2 已在真页面用真登录复现红（`repro-R2.mjs`，`R2-VERDICT RED`）；R4 两条思路都未复现，按 N2 如实写“未复现 + 看到了什么”；R1/R3/R5 根已坐实、红图留各自修复包配对。**没拿任何零件级断言顶替。**
- **N3（带身份探针）**：pb1/pb2/pb3/pb4 每条都 `select auth.uid()` 回显且 = 目标 uid；另用 accounts.json 真登拿用户 JWT 走 REST 交叉验（truth 的 named_invites=10 与页面 0 对照）。**无身份的“0 行”一处都没用来当证据。**
- **N4（云函数每路各验）**：precheck-ai v21 五条路各真打一次、状态码填满上表（现状列）。
- **N5（密码纪律）**：`secret-scan.mjs`（git 感知）对 `~/mh-jobs` 跑过——本轮新文件 0 命中；报出 2 处**旧文件**明文密码警告（见三①）。所有脚本只“读” accounts.json，回执/日志/截图**零密码零 token**。
- **N6（只修不加 + 每包重读）**：本包只侦查未改端口；开工重读了本包与 §0 全文。

---

## 五、本包末尾六段结论（卡要求）

1. **259f456a 从哪条路进预取清单**：审核端 v38 的 `loadMaterialsFromCloud`（L1693）**只从 `reviewer_doc_queue` 视图取 MATERIALS**（v38 自注 L2060「派单已由该视图服务端按 auth.uid() 精确投递，前端过滤冗余」）；预取 `_wmEnqueuePrefetch` 遍历 MATERIALS 里可视件。**该视图缺 `revoked_at is null`**，所以撤销件 259f456a 仍在视图 → 进 MATERIALS → 进预取。**单一来源 = 队列视图**，不是 localStorage 历史、也不是直查 reviewer_assignments。

2. **邀请数为 0 与按钮消失的病根**：
   - **数为 0（已复现）**：`_dlLoadInvites`（partner v160 L12896）里写的是**裸变量 `uid`**（`.eq('partner_id', uid)`），而当前会话 uid 实际存在 `_dlState.uid`（L12884 才赋值）。裸 `uid` 在该函数作用域里**没有声明** → 取值即抛 ReferenceError → 被 `catch(_e){}` 吞掉 → `_dlState.invites` 保持空 → 页面显示 0。**修法（16-02）**：`uid` → `_dlState.uid`。
   - **按钮消失（本机未复现）**：按钮由 `_dlDecorateDom()`（L12891，在 `_dlLoadInvites` 之前调用一次）插入锚点 `.partner-add-student-btn` 旁，本机锚点在、按钮在。`_dlLoadInvites` 那次 ReferenceError 顺带让它内部的 `_dlDecorateDom(true)`（L12897）没跑到——修好裸 uid 会**顺带**让这条 decorate 也恢复，按钮插入更稳。线上“按钮没了”疑为时序/数据条件，16-02 修 uid 时一并把 decorate 挂到会话就绪时机、并加“开关开+锚点在就必有按钮”的守卫。

3. **死图是哪条读路漏过滤**：`student_bootstrap()`（SQL 函数）的 documents 子查询是 `where d.student_id=auth.uid()`，**没有 `deleted_at is null`** → 软删的 mh10-test 两张照样返回。前端材料渲染吃的就是 bootstrap，所以 CV 槽列死图。**DB 侧根**（0207 修）；另 16-05 要查前端有无直接 `.from('documents')` 的回落查询（中介端 `_dlLoadDocs` 确有一处、无 deleted_at 过滤，16-05 补）。

4. **闸函数全部报错码与批次规则**（`trg_doc_upload_gate`，SECURITY DEFINER）：
   - 批次窗口 = **1 小时**；同槽 1 小时内再传算“并入当批（多页）”，**不加码**。
   - 批号 v_no = `doc_slot_batches(...)+1`；`v_seen` = 该槽历史上有没有被审过（review_verdicts 存在）。
   - **第 1–2 批：自由传**。
   - **第 3–4 批（v_no≥3）且没写原因**：报 `reason_required`（审过）/ `reason_required_unreviewed`（没审过）——**要写原因**。
   - **第 5 批起（v_no≥5）**：报 `slot_locked_needs_review`（审过）/ `slot_locked_unreviewed`（没审过）——**锁槽、不能再传**。
   - 四个错误码：`reason_required` / `reason_required_unreviewed` / `slot_locked_needs_review` / `slot_locked_unreviewed`（16-04 每个都要给四语人话）。**对所有 doc_type 一视同仁**（触发器按 student+doc_type 槽计，不分“四类”）。管理员补件、服务端路径放行。

5. **红字通知是谁写的、什么形状**：**查不到“正文不是文字”的通知——卡里的猜测与库里事实对不上**：
   - 近 30 天 admin 全部通知 `body` **都是对象 {zh/en/…}**，**没有一条**是非对象或缺 zh/en 的（`0 rows`）。
   - `usage_alert` 通知**一条都没有**（`usage_alert_count=0`）——用量预警根本没发过。
   - `usage_hourly_rollup` 的源码里，传给 `push_notification` 的 title/body **本来就是 `jsonb_build_object('zh',…,'en',…)` 的人话**，不是“一坨 JSON”。
   - `get_admin_student_timeline` 以 admin 身份跑**不报错**，notification 源行 title/body 都是规整对象，audit 源行走前端 `r.detail` 分支。
   - 前端 `_pickLang`（robust：null/字符串/对象都接得住）+ `renderNotifItem`（escapeHtml）也没有把对象打成 `[object Object]` 的路。
   - **结论**：R4“时间线红字”在当前库态**未复现**。16-07 的前端兜底 `_v215NotifText` 仍值得做（防御性），但迁移 0209“把预警正文改人话”**大概率是空动作**（已是人话）。到 16-07 我按“只做前端兜底、0209 停下请您定”。

6. **按需预检 401 的病根 + 页面带的是什么 token**：学生页走 `window.sb.functions.invoke('precheck-ai', {body:{doc_ids,lang}})`（v394 L9741），supabase-js **自动带的是当前会话的用户 JWT（Bearer）+ 匿名 apikey**，**不带 `x-precheck-key`**。而 precheck-ai v21 一进门只认 `x-precheck-key`、不认用户 JWT → 学生这条路必 401（上表实测坐实）。16-06 修：带钥匙走 cron、带登录态走 user 路。

---

## 附：本包产物
- `jobs/JOB-16-00/lib/`（env.mjs / realpage.mjs）、`inventory/`（inv.mjs / inv-dynamic.mjs / 10 份清单）、`secret-scan.mjs`、`acct-check.mjs`、`probes/*.sql`、`probe-precheck-now.mjs`、`repro-R2.mjs`、`filter-audit.md`、`smoke-login.mjs`。
- 截图：`~/mh-verify/shots/JOB-16/16-00/R2-*.png`（本机；含中介真实邮箱/姓名，未进公开仓库）。
