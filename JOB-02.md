# JOB-02 · 六类文件全部加真下载 + 材料打包下载

**结果：全闸通过**

## 做了什么（一句白话）
以前只有学校证明件和中介资质件有「真下载」按钮，其余四类只能「查看」、靠浏览器自己另存。这次：
- **录取通知书、JW202、付款凭证**三处都加了「下载」按钮，点了直接存进下载文件夹，文件名正常。
- 付款凭证的链接有效期从 60 秒延长到 300 秒（跟其他文件一致），过一会儿再点也能打开。
- 学生抽屉材料区顶部加了「📦 打包下载全部材料」，一键把这个学生的所有材料打成一个 zip 包下载，**有进度条、能中途取消**。视频、Word 这类不能预览的也照样打进包里，不报错。

**合规**：学生材料按硬规矩「绝不生成直链」，打包走的是带审计的服务端慢路（每取一份都记一条审计），存储桶策略和服务端函数**一个字都没动**。

## 业主要看什么（打开哪里、点哪个、应该看到什么）
1. 管理端 → 「学生」→ 点学生 → 抽屉「OFFER」区：如果有录取通知书/JW202，每行「查看」旁边多了「下载」，点了直接下载文件。
2. 抽屉「付款订单」区（JOB-06 那个）：待核销订单的「看凭证」旁边多了「下载」；付款页的待核销队列同理。
3. 抽屉「📎 材料（云端）」区顶部：有材料时会看到「📦 打包下载全部材料」按钮，点了弹出进度条（N/M）+「取消」，走完自动下载一个 zip；中途点取消就停下不下载。
4. 付款凭证下载链接现在 5 分钟内都有效（以前 1 分钟）。

## 十道闸（逐条 + 实测数字）
| 闸 | 结果 | 实测 |
|---|---|---|
| 1 基底md5 | PASS | `ae2eefc06e7c1c8f7e67bebe942f5d43`（=JOB-06 产物）|
| 2 锚点唯一 | PASS | 4 简单锚点 + 3 处按钮程序化提取插入（从文件原文取，不手敲带引号原文）|
| 3 补丁自检 | PASS | 四处新下载入口各就位；付款凭证 60→300、旧 60 归 0；`_v177Zip/_v177PackAll/_v177FetchAdminDoc` 各定义 1。反向样本拿 v176 跑 3/3 失败 |
| 4 产物md5 | 记录 | `c715957f23140cff153553ca60fbfeb4` |
| 5 语法 | PASS | 脚本块 0 报错 |
| 6 功能清单 | PASS | 函数新增 10（含 zip 内部 u16/u32）+元素 id 新增 3（进度条相关）+onclick 3；**消失 0** |
| 7 四语 | PASS | 管理端中/英双语；新增文案中英齐全、无裸键 |
| 8 真点击 | PASS | 真浏览器：**ZIP 结构有效**（magic PK、EOCD、文件数=2、STORE 解出的字节与原文一致）；付款凭证行同时有「看凭证」「下载」；打包运行时进度条+取消按钮真出现、进度显示「1/3」、走完产出 3 文件 zip（含视频）、浮层自动关闭；中途取消不产 zip；下载函数就绪 |
| 9 视觉一致 | PASS | **零**新增 hex/rgba 颜色值、**零**新字号、**零**新 CSS 类（进度条用现成 CSS 变量 `--purple`/`--purple-soft`/`--bg-card`/`--ink-mid`）|
| 10 线上版本 | 跳过 | 本轮不推送 |

## 基底 / 产物 md5
- 基底：`ae2eefc06e7c1c8f7e67bebe942f5d43`
- 产物：`c715957f23140cff153553ca60fbfeb4`

## index.html 改了几处
3 处，v176→v177，无残留。

## 提交哈希
主仓库 `29bc1ca`（已 commit，未推送）。

## 偏离或疑点
1. **ZIP 用自己写的极简打包器**（不压缩 STORE + CRC32），**没有引入任何第三方库**——因为项目要求所有资源自持、不能从 CDN 加载。测试已验证生成的 zip 结构合法、能正确解出原文件。（第一版我写漏了 zip 头里的日期字段导致文件解不出来，被闸8 抓到，已改正并复验通过。）
2. **打包下载是「慢路」**：学生材料按硬规矩不能生成直链，只能一份份走服务端函数取（每份都写审计），所以材料多时打包会花点时间——这是合规要求，进度条+取消就是为此加的。
3. **打包/下载的真实验收要在线上做**：本地测试用的是受控/桩数据证明逻辑正确；真实下载文件名、真实 zip 内容要业主在线上点一次确认（本地 file:// 连不了真库、也取不到真原件）。
4. 付款凭证「下载」按钮我加在了每条订单行（跟「看凭证」并排）。
