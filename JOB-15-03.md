# JOB-15-03 · 管理端 v212 + 迁移 0205 · 5 个 REST 403 补授权 + 失败件区块剔视频/补原因人话

生成于 2026-09-19 · 0205 **已用 db-run 应用并验证** · admin v211→v212（未推未部署）

## 做了什么
- **迁移 0205**（`supabase/migrations/0205_admin_reads_wm_failures.sql`，已 db-run 应用，已镜像到 `docs/supabase 文件/`）：
  - ① 给 4 张管理端直读表补 `grant select … to authenticated`：`ai_precheck_draft` / `application_drafts` / `media_shares` / `task_candidates`。这四张 RLS 早开、`admin_read_<表>` 读策略也早在，**只是缺表级 GRANT**（GRANT 先于 RLS 判定 → REST 直接 403）。**只补 grant、未新建策略、未给写权限。**
  - ② `admin_wm_cache_failures()` 用 `CREATE OR REPLACE`（**签名一字不改，无 DROP，不碰 db-run 红线**）：WHERE 改为 `status ∈ {gave_up,unsupported,too_large} 或 (failed 且 attempts≥3)`，且 **`file_name` 不匹配 `\.(mp4|mov|webm|m4v)$`**（视频走签名直播、不算失败）。
- **admin v212**：`_v210ReasonText` 人话表——`too_large→图片文件过大（>3 MB）` / `unsupported→文件类型不支持（Word 等）` / `gave_up→多次渲染失败 · <PDF 损坏或加密 | 文件过大渲染超限 | 原文>`；失败件区块标题旁小字「视频不算失败 · 审核端直接播放」；空区块「0 件 · 目前无失败件」。扩展名/updated_at 由前端从 `file_name` / `last_attempt_at` 取（故 RPC 签名无需动）。
- 版本四件套：文件名 v212 / 头注 / [VER] v212（无残留 v211 探针）/ `admin-portal/index.html` 三处重定向 → v212；`docs/changelog/admin-portal-versions.md` 加一行。

## 结果是什么（数字带出处）
### 0205 库内验证（`bash jobs/db-run.sh`，DB-RUN OK）
- 授权后 4 张表 `authenticated` **全部有 SELECT**（`V_grants_after` 四条 true）；各 **1 条策略**（`V_policy_counts`）；函数体 **含 'mp4'**（`V_fn_has_mp4=true`）。
- 以真实管理员 uid `88bd171a…` 逐表 `select count(*)`：**全部 OK，无 permission denied**（35 / 20 / 13 / 29 行）——403 已解。
- 以 is_test 学生 uid `43668ae4…` 逐表：**全部 0 行**（RLS 仍挡）。
- 以管理员实调 `admin_wm_cache_failures()`：返回 **4 行，全非视频**（2× `gave_up` PDF `Notice of applying.pdf` PDF_UNREADABLE + 2× `unsupported` `APPLICATION….docx`）。改造前该 RPC 会把视频列成「不支持」；现已不含任何视频扩展名。

### admin v212 前端自验（`node ~/mh-verify/_run-1503.mjs`，GATE3-PASS，0 真实报错）
- 五种原因人话逐一匹配（too_large / unsupported / gave_up×PDF / gave_up×546 / gave_up×其它）。
- stub 3 行 → 区块渲染三种人话、标题小字「视频不算失败」、无视频行。
- 空 stub → 「0 件」。
- 报告：`docs/self-test-report-15-03.md`。
- **v212 md5 = `227bdfa5f3995a2afc1c670c7156ed7d`**。旧 v211 = `30b6e79d…`。

## 业主要做什么
1. 推主仓库后到**管理端**登录，之前控制台 5 个 REST GET 403 应消失（4 张表已补授权；第 5 个「嫌疑」经排查恰为 4 张，全部覆盖，见回执数字）。
2. 到派单台底部「水印缓存失败件」区块：应只见 2 份坏 PDF + 2 份 docx，**看不到视频**；原因显示人话（验收表 4）。
3. 本包**不推主仓库、不部署**；0205 已应用于线上库（新增型授权 + 函数替换，无破坏性操作）。

## 边界守住
只加读权限不加写；未动派单逻辑；未改索引状态；RPC 签名未动（无 DROP）。
