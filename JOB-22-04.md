# JOB-22-04 回执 · 审核端 v46 + 管理端 v219：「有新版请刷新」

CC 执行。两端各只加横幅一段代码 + 各自 index.html + changelog。未 push 主仓库（§0.9）。
共用查看器 / 云函数 / 迁移一律未碰；管理端、审核端**其它一行不动**。

## 一、做了什么（唯一改点：横幅）
两端加入与中介端 v170 **同一段**「有新版请刷新」横幅逻辑：
- 载入 3s 后 `fetch('./index.html',{cache:'no-store'})` → 正则 `maxhouse[_-][a-z_-]*portal[_-]v(\d+)` 取**最大版本** vs 当前（`location.pathname` 解析，回退 `MH_PORTAL_VER`=219/46）。
- 线上更新 → 顶部固定横幅：四语、点去根地址 `'./'`、**绝不自动刷新**、× 关后本会话不再弹、显示时把 **`.header` 或 `.topbar`**（管理/审核端用 `.topbar`）与 body 顶到横幅下**不遮页头按钮**；每 30 分钟 + `visibilitychange` 复查；**取不到 / 解析不出静默**。
- **两处与五端同段代码的差异（已在头注/回执点明）**：① 顶栏选择器用 `.header, .topbar`（管理/审核端顶栏是 `.topbar`，非 `.header`）；② 审核端 zh-only、无语言切换器 → lang **回退 zh**（管理端回退亦 zh，与其 zh 默认一致）。
- **CF Access 处置（22-00⑤ 定案）**：两端在 Cloudflare Access 后，登录态 `fetch` 带 Access cookie 正常返回真跳转页含版本串；若返回 Access 页 / 任何不含 `maxhouse..portal_v<数字>` 的内容 → 正则不命中 → **静默**（天然安全）。

## 二、闸3（`jobs/JOB-22-04/probes/gate3.mjs`；日志 `repro/gate3-after.log`）

| 项 | 判据 | 结果 |
|---|---|---|
| 审核端横幅（reviewer1 真登） | 假更高版本 → 横幅出（zh「有新版本，点此刷新」/紫 `rgb(124,58,237)`/top=0/h=40）；与 `.topbar` 交集 **0px²**；`pageErrors=0` | **GREEN** |
| 审核端同版不出 | index 指向 v46 → 无横幅 | **GREEN** |
| 审核端取不到静默 | fetch abort → 无横幅、`pageErrors=0` | **GREEN** |
| 管理端（无测试账号） | 无头本地启动 **零新报错**（`pageErrors=0`）；横幅函数齐（`_mhCheckNewVersion/_mhShowVersionBanner/...`）；`_mhCurrentPortalVer()=219`；直接调 `_mhShowVersionBanner()` 生成元素、文案「有新版本，点此刷新」 | **GREEN** |

`OVERALL: ALL GREEN`。管理端在 CF Access 后 + 无测试账号 → 按 QUEUE 22-04「无头本地启动零新报错 + 静态断言」核（本地无 CF Access，页面正常加载；登录门后横幅可 0 尺寸，功能与静态均已证）。

## 三、N 闸
- **N1**：管理 v218→v219 移除 **0** / 新增 **5 函数**（`_mh*` 横幅）；审核 v45→v46 移除 **0** / 新增 **6**（`_mh*` 5 + `onclick` 系 `x.onclick=function(` 属性赋值被 inv 正则误当函数名·非真函数·无移除）。`DIFF-REMOVED-TOTAL 0`，零死按钮。（`jobs/JOB-22-04/inv-admin-v219.json` / `inv-reviewer-v46.json`）
- **P3 随卡 jsdom/vm 断言**（`tests/job22-04-admin-reviewer.mjs`）：**30/30 ALL PASS**（两端 md5、头注版本、div 平衡与基线同（admin −2 / reviewer 0）、index 三处同版、四语齐、`.header/.topbar` 选择器、MH_PORTAL_VER、lang 回退 zh、30min+visibilitychange、内嵌 script 可解析）。
- **四语**：横幅四语字典逐条实证（P3 B2）；显示语言按各端实际（审核端 zh）。
- **shell 卫生**：本包新增 `.mjs` 无 shell；`shell-lint.sh` **全绿（0 命中）**。
- **测试写库**：零上传 / 零写库——仅本地服务器 + fetch 路由桩 + reviewer1 真登只读。
- **secret-scan**：push 前扫 CLEAN。

## 四、截图（元素级 · 无 PII，调试期不推）
`~/mh-jobs/shots/22/22-04/`：`REV-version-banner.png`（reviewer1 真登触发、只截横幅条）+ `INDEX.md`。管理端无账号 + CF Access → 用无头零报错 + 功能核 + P3 代替截图（已在 INDEX 说明）。

## 五、要业主亲眼看的（线上验收）
- 审核端 / 管理端：线上版本一致时**不出**横幅；真出新版时才出、点它才刷新；不遮顶栏按钮。
- 管理端/审核端在 CF Access 后：若 fetch 拿不到 index（Access 拦截）则**静默不弹**、不多任何报错。

## 六、版本四件套
- 管理：①文件名 v219 ②[VER] v219（+VER-prev218）③L2 头注 v219 ④index 三处 v219。
- 审核：①文件名 v46 ②[VER] v46（+VER-prev45）③L1 头注 v46 ④index 三处 v46。changelog 各追加一行。

## 状态
- 无「必须停」触发（管理端/审核端 CF Access 后 fetch 走防御式静默，可静默降级，未触发 §0.8⑤）。
- 主仓库两端 html + 两 index.html + 两 changelog + `jobs/JOB-22-04/` + `tests/job22-04-admin-reviewer.mjs` 点名 add + commit，**未 push**（§0.9）。
- 本回执 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**。视觉件线上肉眼验收留业主。

下一包 = 22-05
