JOB-2-09  结果：全闸通过（迁移已跑 + 管理端 v189 → v190）

## 做了什么（一句白话）
学校抽屉新增管理能力：
1. **停用 / 恢复**：治理操作区有「⏸ 停用」（填理由），停用后学校抽屉显「已停用 · 理由」，可「▶ 恢复」。停用只挡新动作，**已发出的通知书照常走完**（裁决4）。
2. **改学校档案（每行可改带审计）**：基础档案 9 个字段（中文名/英文名/城市/联系人/邮箱/电话/**网站/手机/微信**）每行有「改」，改完写数据库+记操作历史。**网站/手机/微信**以前批准后就看不见了，现在补上了。
3. 「🗂 管理操作历史」段：看得到停用/恢复/改档案这些动作。

## 迁移（我经 db-run.sh 跑的，跑前跑后差值）
- `schools` 加 3 列：`suspended_at / suspended_by / suspend_reason` —— **列数 24 → 27（+3）**。
- 新增 3 个函数：`admin_suspend_school / admin_resume_school / admin_update_school` —— **本卡三函数 0 → 3（+3）**。
- 迁移 `jobs/JOB-2-09/migrate.sql`；回滚 `jobs/JOB-2-09/rollback.sql`。

## "停用"怎么表达的（重要说明）
`schools.status` 有硬约束只认 `onboarding/active`，加 `suspended` 得改约束（要 DROP，红线+db-run 都禁）。所以「停用」用新列 **`suspended_at` 打标记**表达（不改 status）。前端据 `suspended_at` 是否有值显示"已停用/正常"。

## 业主要看什么
1. 学校抽屉顶部「治理操作」：有「停用」；停用后显「已停用」并可恢复。
2. 「基础档案」每行有「改」；能看到并能改 网站/手机/微信。
3. 「管理操作历史」：改动都留痕。
（真停用/真改档是对数据库真写入，需你登录管理员在线上点一次验收；本地只用桩验证接线，未碰真库。）

## 十道闸（逐条）
- 闸1 基底 md5：`3d0f8be1f75e1250937abfda84068c10`（=v189）✓
- 闸2 锚点唯一：7 处各 =1 ✓
- 闸3 补丁自检：3 函数定义/3 rpc/loader select+映射/profile 换可改版(旧 _ddField 归 0)/治理段+历史段装配/历史填充 各 =1 ✓
- 闸4 产物 md5：`85eb3cfb6a66e8679f38634c0477f7aa`（v190）
- 闸5 语法：1 脚本块 0 报错 ✓
- 闸6 消失检查：776→785（+9，无消失）✓
- 闸7 四语：管理端中英；新增文案（停用/恢复/已停用/网站/手机/微信/修改档案/管理操作历史）中英齐全，零裸键 ✓
- 闸8 真点击：Playwright 桩 —— _v190SchoolProfile 渲染网站/手机/微信真值 + 每行「改」(_v190EditSchool)；_v190SchoolOps 未停用显停用按钮、已停用显恢复+「已停用·违规」；_v190SuspendSchool 走 rpc admin_suspend_school 理由透传；_v190EditSchool 走 rpc admin_update_school({name_zh:新值},理由)。反向样本 v189：函数未定义 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：新增写死颜色 0（复用 btn-secondary/btn-primary/var(--red)/var(--brand)/badge purple）/ CSS 类 0 / 字号档 0 ✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：3d0f8be1f75e1250937abfda84068c10 → 85eb3cfb6a66e8679f38634c0477f7aa
迁移跑前跑后：schools 列 24→27(+3)；本卡函数 0→3(+3)
index.html 改了几处：3（v189→v190）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 停用用 suspended_at 标记而非 status='suspended'（原因见上，status CHECK 约束 + 禁 DROP）。管理端显示与治理已完整；**"停用只挡新动作"的实际拦截**（如学校端不能再发 offer）需学校端/相关 RPC 检查 schools.suspended_at 才生效——那是学校端后续，本管理端卡未做，列为观察项。
- admin_update_school 白名单严格 9 列（id/display_code/tier 等不在内，tier 归 JOB-2-10）。
