JOB-2-12（重做）  结果：全闸通过（迁移已跑 + 管理端 v191 → v192）

## 表名怎么定的（按你的决定二）
用新名 **`school_action_log`**（避开被占用的 `school_access_log`）。旧那张 666 行的 EF 取件日志表**只读不动**——只给它加了一条管理员只读策略（否则它 RLS 开·0 策略，管理员前端根本读不到，就没法"并起来读"）。

## 做了什么（一句白话）
1. 侧栏新增「学校行为日志」页：把**新表 school_action_log（埋点：导出/下载/查看学生/导出名单）**和**旧表 school_access_log（取件日志，666 行，只读）**并成**同一张列表**，每行显示 🏫学校 · 👤成员 · 行为 · 🎓涉及学生 · 🕒时间；**旧表的行统一标成「取件」**。
2. 顶部按行为筛选（全部/导出/导出名单/下载/查看学生/取件）；点某行的学校名可按该校筛；导出/取件行可展开看 detail 字段快照（是否含护照号一眼可见）。
3. 学生抽屉新增「👁 谁看过/下载过我的材料」段（读 school_action_log 里针对该生的 view_student/download 记录）。

## 迁移（我经 db-run.sh 跑的）
- `migrate.sql`：建 `school_action_log` 表 + 两索引 + revoke 后开 RLS + 管理员只读 + 学校只读自己 + `log_school_access` 函数（SECURITY DEFINER，从 school_members 解析 school_id）。**表 0→1，函数 0→1，策略 2。**
- `migrate2.sql`：给旧 `school_access_log` 加一条 `admin_read_school_access_log_legacy` 只读策略（**只加读权限，不动其数据/结构**）。**策略 0→1。**

## 业主要看什么
1. 侧栏「学校行为日志」页：现在能看到旧的 666 条取件记录（标「取件」）；等 JOB-2-13 学校端埋点上线后，导出/下载/查看学生也会陆续进来。
2. 学生抽屉底部「谁看过/下载过我的材料」段（同样等埋点上线后有内容）。

## 十道闸（逐条）
- 闸1 基底 md5：`8b65a15c3d9def1faffed2460fd618d4`（=v191）✓
- 闸2 锚点唯一：10 处各 =1 ✓
- 闸3 补丁自检：renderSchoolAccessLog/loadSchoolActionLog 定义、两表并读查询、取件标签、nav/view/VIEW_RENDERERS/switchView/i18n中英、谁看过段装配+填充 各 =1 ✓
- 闸4 产物 md5：`5e72294467868d50993d261611b5e935`（v192）
- 闸5 语法：1 脚本块 0 报错 ✓
- 闸6 消失检查：791→800（+9，无消失）✓
- 闸7 四语：管理端中英；新增文案（学校行为日志/导出数据/导出名单/下载材料/查看学生/取件/谁看过下载过我的材料/清除学校筛选…）中英齐全，零裸键 ✓
- 闸8 真点击：Playwright 桩两表 —— 合并列表出「导出数据/下载材料/取件」三类 + 字段快照(passport_no) + 筛选 chip + 计数「3 条」(2 埋点+1 取件)；学生抽屉「谁看过」段渲染「查看学生」。反向样本 v191：renderSchoolAccessLog 未定义(超时) → 判 FAIL（有分辨力）✓
- 闸9 视觉：复用 wf-hero/queue-row/filter-chip/_adminPill/badge/var()，零新写死颜色/CSS 类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：8b65a15c3d9def1faffed2460fd618d4 → 5e72294467868d50993d261611b5e935
迁移：school_action_log 表+函数(migrate.sql) + 旧表 admin 只读策略(migrate2.sql)
index.html 改了几处：3（v191→v192）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 表名用 `school_action_log`（你拍板）；旧 `school_access_log` 只加读策略、数据/结构零改。
- 旧表 666 行是 doc 维度（无 member/student 列），合并列表里旧行的「成员」显示 sc_code、「涉及学生」显 —（旧表结构如此）；新埋点行字段齐全。
- 学生抽屉「谁看过」只读新表（有 student_id）；旧取件日志按 doc 维度，未在该抽屉小段按学生解析（主日志页可见全部）。
