JOB-2-12  结果：**停在图纸↔现实冲突（表名撞车），迁移已备好未执行，等你一句确认**

## 一句白话
要建的这张「学校行为日志」表，设计里叫 `school_access_log`。但我去库里一看，**这个名字已经被另一张完全不相干的表占了**——那是水印取件的审计日志（EF 用的），有 **666 行真实数据**，列也完全不一样（没有我们要的 `school_id` 列）。
硬用这个名字建表会：跳过建表（因为同名已存在）→ 后续索引/列对不上 → 直接报错（我实测 rc=3 了）。而红线禁止我动/删那张 666 行的现有表。

## 为什么停（不是我偷懒）
项目铁律 P2：**图纸和现实冲突时，我必须停下报告，不许自己改图**——尤其这是合规审计表、且表名是你在设计文档里点名的。我不擅自换名硬上。

## 我已经替你备好了（没执行）
迁移文件 `jobs/JOB-2-12/migrate.sql` 我改用**不撞名的表名 `school_action_log`**（其余照 JOB-14 设计：school_id/actor_uid/action/student_id/detail/created_at + 两索引 + revoke 后开 RLS + 管理员只读 + 学校只读自己 + log_school_access 函数）。**但我没经 db-run 跑它**，等你确认表名。

## 要你定的一件事（一句话即可）
1. **用 `school_action_log` 这个新名**（推荐，零风险，我下轮直接跑 migrate.sql + 做前端日志页 + JOB-2-13 学校端埋点）；
2. 还是**坚持 `school_access_log`**——那得先决定怎么处理现有那张 666 行的 EF 日志表（改名/迁数据/共存），那是破坏性操作，必须你拍板我才动。

## 连带影响
- **JOB-2-12 前端**（管理端「学校行为日志」页 + 学生抽屉「谁看过我的材料」）：依赖这张表，**暂缓**，等表名定了随迁移一起做。
- **JOB-2-13**（学校端埋点，调 log_school_access）：同样依赖，**暂缓**。

十道闸：本包未落代码（迁移未执行、前端未做），闸不适用。
基底 md5 / 产物 md5：管理端仍 v191（未产出 v192）。
提交哈希：无代码改动；本回执 + 备好的 migrate.sql 推日志仓库。
偏离或疑点：见上——表名撞车是设计文档未预见的现实（CLAUDE.md 里其实记过"三张 access_log 表在库"，此处正是其一）。
