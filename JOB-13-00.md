# JOB-13-00 · 开工检查（只读）

## 做了什么
- 把 `~/Downloads/QUEUE-13.md` 用 cp 复制成 `jobs/QUEUE-13.md`；核 md5、包数、五端基线、db-run.sh；跑 mh-verify 自测；跑 5 段只读探针（`jobs/JOB-13-00/probe.sql` + probe2/probe3 补样）。
- 全程零写库、零改端代码。

## 结果是什么
**校验（命令：md5 -q / grep -cE / git status / node selftest.mjs）**
- QUEUE-13.md md5 = `32d58f4f6585041114a90b83ea2b6f41`（对上），包头计数 = 9（13-00～13-08，对上）。
- 五端基线 md5：admin v209 ✅ / partner v156 ✅ / school v231 ✅ / reviewer v35 ✅；student v391 ✅（文件名是下划线式 `maxhouse_student_portal_v391.html`，md5 = `d62fa673478c458cfb9922a347c4630c` 对上）。
- 工作区 `git status --porcelain --untracked-files=no` = 空（干净）。最近提交 `667e35c`（第十二轮收尾）。
- ⚠️ **冲突点1（点名，不停工）**：queue 说 db-run.sh md5 应为 `3ceb69404156b1dc4176778de733c6ac`，实测 `13d840f0819c580f65caf40ca81658b1`。读了全文，它是正常可用的执行器（含红线闸/白名单/begin-commit 校验），且是 untracked 工具文件（历轮演进），功能无损 → 照用。回执点名，按 §0.1「以本文件为准、点名冲突点」处理。
- mh-verify 自测：`SELFTEST|MH-OK|width390` ✅。mh-jobs `git pull` = Already up to date。

**探针（命令：bash jobs/db-run.sh jobs/JOB-13-00/probe.sql）**
- 缓存索引行数 = **0**；wm-cache 桶对象 = **95** 个、总字节 **33,163,267**（≈31.6 MB）。（索引 0 行 = EF service_role 无 insert 权限的老症，13-01 修）
- **待补清单 = 4 件**（对上业主 09-17 傍晚「待补 4」）：
  1. `23f0f3af…` transcript「Notice of applying.pdf」894KB · MH-W73M6F IRADUKUNDA RAHMA · reviewer 80c62372
  2. `b336454a…` health「Notice of applying.pdf」894KB · MH-X88X8S KUMI RICHMOND AGYEMANG · reviewer 80c62372
  3. `35f5e7c9…` studyProof「big size 2.jpg」**6189KB** · MH-QH5NBK · reviewer 80c62372
  4. `940ec3e4…` studyPlan「big size 2.jpg」**6189KB** · MH-W73M6F IRADUKUNDA RAHMA · reviewer 80c62372
- 近 6 小时 net 响应按状态分组：**402×270 / 500×65 / 546×18 / null×7**。
- 500 的 body（3 条样本）= `watermark_failed` + `IMAGE_TOO_LARGE:5712x4284`（两张 6MB 大 jpg）与 `PDF_UNREADABLE`（坏 pdf）。→ **这 4 件待补是「文件本身渲染不了」，永远补不上**，正是「待补降不到 0」的病根，13-02 的「试 3 次放弃」就是治这个。
- 546 的 body = `WORKER_RESOURCE_LIMIT`（云函数内存不足）。
- 大 PDF（未删、>600KB）共 **24 件**，最大 4052KB（criminal record）；大图片（jpg/png/webp>600KB）共 **20 件**，最大 6189KB。
- reviewer_access_log.outcome 现约束 7 值：`served / denied_not_assigned / prepared / served_cached / served_original / served_video_url / served_cached_direct`。

## 🔴 头号发现（业主必须先处理）
探针 probe2/probe3 查出：**本项目自 2026-09-17 22:50(UTC) 起被 Supabase 服务限制**，错误码 402，原文：
`Service for this project is restricted due to the following violations: exceed_cached_egress_quota. The project owner must upgrade their plan or remove spend caps to restore service.`
- 近 30 分钟全部 30 条响应都是 402；最新一条（03:15 UTC）仍是 402。
- **含义（大白话）**：免费额度里的「CDN 缓存流量」这个月用超了，Supabase 把项目的边缘服务掐了。凡是走网关/云函数/存储外发的调用现在都被挡（返回 402）。这可能也在影响线上：学生/审核端打开材料、图片这些也可能被掐。
- **对本轮的影响**：任何「调云函数重灌缓存」「发邀请邮件」的**线上验证做不了**（会被 402 挡）。所以：
  - 13-01c「删 1 件缓存再重灌」**我不执行**——删了就再也灌不回来（云函数被 402 挡），会把系统弄得更糟。工具文件照造、留给业主恢复服务后跑。
  - 13-02c 的「等 cron 重灌确认」也做不了，但「试 3 次放弃」的判定逻辑不依赖云函数成功，仍可证。
  - 13-07d 的「重发邀请后收信」做不了，改证 RPC 逻辑本身。

## 业主要做什么
1. **最优先**：登录 Supabase 控制台 → 该项目 → Billing / Usage，看到 `exceed_cached_egress_quota`，**升级套餐或移除 spend cap 以恢复服务**（否则线上材料访问、云函数、缓存重灌全被掐）。恢复后告诉我，我再把 13-01c 的单件重灌证明补上。
2. 其余无需操作，本包零改动。
