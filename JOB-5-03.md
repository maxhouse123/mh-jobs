JOB-5-03  结果：**已实现，无需改（学校端 v225 就加过忘记密码，v228 里还在且可用）**

## 一句白话
这个功能**已经有了**——学校端在 v225 就加了「忘记密码」整条链，v228（本轮基底）里完整保留。我不需要再抄一遍，抄了反而是重复。

## 我核对了什么（Playwright 真开学校端登录页）
| 项 | 结果 |
|---|---|
| 登录框有「忘记密码？」链接 | ✅ 有（onclick=schShowForgot，data-gate-i18n=gate.forgotLink）|
| 点了弹出输邮箱的小框 | ✅ 弹出 schForgotBox（不是 window.prompt，是站内框）|
| 输邮箱点发送 → 调 resetPasswordForEmail | ✅ 调了，且**带 redirectTo 指回学校端**（_pwRedirect）|
| 重置落地页（接住 type=recovery 弹「设新密码」）| ✅ 有（__mhPwRecovery 检测 + PASSWORD_RECOVERY 监听 + schShowReset 设新密码 + updateUser）|

全部通过（脚本 `~/mh-verify/verify_job5_03.mjs`）。

## 一个说明（关于「四语」）
学校端**登录门本身是双语（中/英）**——它用的是 gate 专属词典 GATE_I18N，里面只有 en/zh 两块（进站后的 app 才是四语 en/zh/ru/fr）。忘记密码的文案跟着登录门走，也是中/英，和登录门其它文案（登录/注册/邀请码）一致。这是登录门的既有设计，不是这次的漏项。若你要登录门整体上四语，那是另一件事（要给 GATE_I18N 补 ru/fr 两块，牵涉登录门全部文案，建议单开一卡）。

## 为什么不出 v229
「版本演进=复制新增」——没有新内容就不该 bump 一个一模一样的版本。学校端保持 v228 不动。

十道闸：无代码改动，不适用（做了等价的验证）。
五端版本：全部未动（school 仍 v228）。
提交哈希：无代码改动。
偏离或疑点：QUEUE-5 说「学校端没有忘记密码」与实际不符——它 v225 就加了，本轮基底 v228 里在且可用。这条等于已完成。
