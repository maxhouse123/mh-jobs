# JOB-20-02 回执 · 中介端 v168：护照名单块「该在时在、该走时走」+ 提醒卡片不遮正文 + 标题冒号按语言

CC 本趟只做 20-02。基线 §0.2 逐项核对全等（partner v167 `89b4acf1…` / student v398 `e88cc0…` / reviewer v45 `c2cf1f…` / admin v218 `73b36f…` / school v234 `a2d2e5…` / viewer `ee3514…` 全对；viewer/学校/管理/审核端未碰）。无「必须停」触发。

## 版本
- **partner v167 → v168**，四件套齐：①文件名 `maxhouse-partner-portal-v168.html`；②头注（本端自 v108 起头部不追注，以 changelog 为准，已补 v168 条）；③`[VER] partner portal v168 (JOB-20-02: …)` 探针改版；④`partner-portal/index.html` 三处重定向 `v167→v168`。docs/changelog/partner-portal-versions.md 补 v168 条。
- **新 md5**：`f1a734ab7b99dc6ea15259757e29c985`（v167 基线 `89b4acf172d4f43996467b7096c8cb9b`）。**未部署无关**（纯前端 HTML，本轮不推主仓库、待收官业主卡1153）。

## 病根（20-00 探针⑤已坐实，逐字照修）
1. **名单块「该在不在」**：`_r2AnchorWarn`（v167 L11479）立完 `state._r2Verdict` 只滚动、不重画；`_r2SetFileVerdict`（L11439）只调 `renderUploadGrid`/`renderSubmittedMaterials`——二者都不是**代传详情弹窗**的渲染入口（该弹窗渲染入口=`renderPartnerSubmitGrid`→`_v141PartnerReplaceBlock` L8202 注 `_r2RosterHtml`，重画走 `_v134RepaintReplace`→`openStudentDetailModal`）。故代传暂存不一致护照后，核对结果虽出，名单块要等下一次整栏重画才现。
2. **名单块「该走不走」**：`state._r2FileVerdicts`/`_r2Verdict` 只按 30 分钟时效过期；删暂存件入口 `_v141RemovePartnerPending`（L10377，提交网格路）与 `_matRemovePend`（L14757，暂存区路）都不碰这两个 state → 删后名单块仍在。
3. **英文卡标题全角冒号**：`_r2SoftToast` 标题拼接 `+ '：' +`（L11503 noMrz / L11513 mismatch）写死中文全角，英/俄/法界面也是全角。
4. **卡片盖正文**：`#_v167CardStack`（L11232）`left:50%;bottom:84px;width:520px` 中央底部，20-00 量到与缩略图/「待提交」标签重叠 **34840px²@1280 · 7424px²@390**。

## 施工（8 处，全部 partner v168，零删按钮/零新增 t() 键；diff 见文末）
- 新增 `_r2Repaint()`：按上下文分派——`viewingStudentIdx` 非空→`_v134RepaintReplace()`（代传详情弹窗，内部再分 openStudentDetailModal / renderSubmittedMaterials）；否则本人自助走 `renderUploadGrid()`+`renderSubmittedMaterials()`（与旧行为等价，无自助端回归）。
- 新增 `_r2ClearVerdict(fileOrName)`：按文件名（`_r2FileKey`）删该件判定；若不再有任何 `mismatch/noMrz` 条目则连 `state._r2Verdict` 判词句一并清；30 分钟时效保留作兜底。
- ① `_r2SetFileVerdict`：两句 render 调用 → `_r2Repaint()`（补上代传详情弹窗这条漏掉的路）。
- ① `_r2AnchorWarn`：立完 `state._r2Verdict` 后 `_r2Repaint()`，再滚动 → 名单块（含两名判词句）≤1s 现。
- ② `_v141RemovePartnerPending` / `_matRemovePend`：删护照暂存件后 `_r2ClearVerdict(_rm81)` 再重画 → 名单块 ≤1s 消失。
- ③（代传提交后清理）`commitResubmission` 云路成功段（对 `_succ` 逐件）+ `_matCommit` 成功段（对 `pend` 逐件）：`_r2ClearVerdict` 清已交护照件判定。
- ③（标题冒号）L11503/L11513：`+ '：' +` → `+ ((currentLang==='zh')?'：':': ') +`（zh 全角 / en·ru·fr 半角），noMrz 与 mismatch 两处同改。`r2.noMrzTitle`/`r2.mismatchTitle` 两键 4 语本已齐（v166 起），本包零新增键。
- ④ `#_v167CardStack` cssText：`left:50%;bottom:84px;520px` → `top:16px;right:16px;left:auto;width:min(calc(100vw - 32px),360px)`（钉右上角，让开弹窗中下部的缩略图/提醒框/提交按钮，390px 不出屏）。「多张纵向排」`flex-direction:column` 不变。

**不加 prepare 调用**：20-01 已实锤前端不成立（`prepare` 有 `x-mh-hook-secret` 暗号闸 + 需已派单），故 20-02 §⑧ 不做，留 R21。未动鉴权/派单/AI 判定/瘦身参数/三级回落。

## 闸3 · 三层自验（真页面真点，全过）
自验脚本 `jobs/JOB-20-02/verify-B-green.mjs`（承 20-00 repro-B-partner.mjs：本地静态服 + 线上 Supabase，**partner1 真登**，进 StuB 详情弹窗，护照栏 `setInputFiles` 合成「无机读码/姓名不一致」护照**只暂存不提交**；判定全走脚本量的几何/颜色/可见文字）。结果 `jobs/JOB-20-02/verify-B-results.json`，一句话摘要：`ALL=GREEN`。

| 项 | 断言 | 结果 | 证据 |
|---|---|---|---|
| bug1 该在时在 | verdict 出后 ≤1s 名单块 present+visible | ✅GREEN | verdict 5221ms 出（真 OCR），1s 后名单块 666×107 visible=true（旧版红：verdict 已出名单块不现）|
| bug2 该走时走 | 删暂存件后名单块消失、缩略图归零 | ✅GREEN | before=true → 点真红× → after rosterStillPresent=false, stagedThumbs=0（旧版红：删后 666×107 仍在）|
| bug2b 再暂存重现 | 再传一张 → 名单块重现 | ✅GREEN | 二次 verdict 出，名单块 666×107 visible=true |
| bug3 冒号按语言 | 英文界面卡标题无全角「：」 | ✅GREEN | lang=en，卡文「⚠️ Passport check: We could not…」，hasFullwidthColon=false（旧版红：全角「：」）|
| bug4 卡片零重叠@1280 | 与提醒框/缩略图/提交按钮/名单块矩形零重叠、不出屏 | ✅GREEN | stack x904,y16,360×116；overlaps 全 0；offscreen=false（旧版红：thumb 34840px²）|
| bug4 卡片零重叠@390 | 同上，手机宽 | ✅GREEN | stack x16,y16,358×116；overlaps 全 0；offscreen=false（旧版红：7424px²）|

- **N7（提示看得见，真页面几何判非只核代码）**：名单块「出现 ≤1s / 删件后消失 ≤1s」两时刻均以真页面 rect 量得（present/visible + 尺寸 666×107）；卡片与下方内容零重叠 1280 与 390 两宽各量一次（named overlaps 全 0）。卡片文字色 `#9A3412`、底 `#FFF7ED`、琥珀边未改。
- **卡片落点说明（诚实记录）**：脚本另量 `beneath`（卡片顶心点正后方元素）在两宽均为 `modal-bg`（全屏半透明背景层），即卡片浮在弹窗**上沿的背景条**上、并非压在任何内容元素上；QUEUE §⑤ 点名的「提醒框/缩略图/提交按钮」＋名单块＋aiHint 五项重叠一律 0。视觉正确性另留业主肉眼看截图（见下）。
- **① UI 对照 / 零裸键 / 零死按钮**：本包零新增 t() 键（冒号为字面量，标题键 4 语本已齐）；v167↔v168 diff 仅 8 处施工 + 版本印记，**零 onclick 删除、零按钮增删**（N1 差集干净）。
- **② 无头走查**：即上表六项 Puppeteer 真驱动（partner1 真登、真 setInputFiles、真 OCR、真红×点击、真几何量取）。红线守：只点本地静态服的 `file` 源，未碰生产网址；未提交任何测试件（全程「只暂存不提交」），无测试写库残留需清。
- **③ 自验报告**：本回执上表即逐条「路径×断言×结果×证据」；机器摘要 `ALL=GREEN` 见 `jobs/JOB-20-02/verify-run.log`。

## 新闸
- **N1**：只升 partner 一版，四件套同版；三端其余（student/reviewer/admin/school/viewer）一字未改。
- **N2**：旧病 20-00 已先红（bug1/2 红、bug4 34840/7424px² 红），本包同一真页面同一测法转绿。
- **N3 带身份**：partner1 真登拿会话打真页面；账号只从 `~/mh-verify/accounts.json` 读，密码/token 绝不打印，脚本 mask #emailInput。
- **N5 secret-scan**：`jobs/JOB-20-02/` 无硬编码 token/key/密码，SECRET-SCAN-CLEAN（截图待收官统一 secret-scan 后再随最终干净版推）。

## 交付物
- `partner-portal/maxhouse-partner-portal-v168.html`（新）、`partner-portal/index.html`（重定向 bump）、`docs/changelog/partner-portal-versions.md`（+v168 条）。
- `jobs/JOB-20-02/verify-B-green.mjs` + `verify-B-results.json` + `verify-run.log`（自验脚本与结果）。
- 公开截图 `~/mh-jobs/shots/20/20-02/`（5 张 GREEN + INDEX.md，邮箱打码、测试数据）——**调试期不随本回执推**，收官过 secret-scan 后随最终干净版推（§0.7）。

## 要业主亲眼看的（收官汇总用）
- 中介端 v168 代传某学生、护照栏传一张姓名不一致护照：**核对结果一出来名单块就现**（≤1s）；点缩略图红× 删掉 → **名单块随即消失**；英文界面卡标题冒号为半角「: 」。
- 提醒卡片钉在右上角、**不盖缩略图/提交按钮**（桌面 + 手机宽各看一眼截图 `~/mh-jobs/shots/20/20-02/bug4-*`）。

## 交接
- **下一包 = 20-03**（学生端 v399：zh 词典 `doc.studyProof`=在读证明 / `doc.studyPlan`=学习计划，用脚本从管理端 v218 材料类型表按键读出不手抄；en/ru/fr 不动；**不加 prepare**，20-01 已证伪）。20-00⑥ 已记该两键现值，施工时若与管理端术语冲突按 P2 停工报业主裁决，不照错前提改。
- 主仓库源码（v168 html + index + changelog + jobs/JOB-20-02/*）本地 commit、**未 push**（等收官业主卡1153）。
