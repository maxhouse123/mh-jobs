JOB-2-07  结果：全闸通过（迁移已跑 + 管理端 v187 → v188）

## 做了什么（一句白话）
给管理员两个新的材料管理能力，都走服务端函数、都写审计、都可回滚：
1. **删材料（软删）**：材料行有「🗑 删除」按钮，点了要二次确认 + 必填理由，删完这份材料从列表消失。是"软删"——文件还留在桶里，数据库只打了个删除标记，可复原。
2. **解锁材料槽**：已通过审核（被锁住、学生不能再传）的材料行有「🔓 解锁重传」按钮，点了填理由就把这个槽解锁，学生可以重新上传。已解锁的行显示「已解锁」。
3. 学生抽屉新增「🗂 管理操作历史」段，能看到删除/解锁（以及后续替换通知书/撤销派单）这些管理动作的记录。

## 迁移（我经 db-run.sh 跑的，跑前跑后差值）
- `documents` 加 4 列：`deleted_at / deleted_by / delete_reason / slot_reopened_at` —— **列数 14 → 18（+4）**。
- 新增 2 个服务端函数：`admin_delete_document(text,text)`、`admin_reopen_doc_slot(uuid,text,text)` —— **本卡两函数 0 → 2（+2）**，都带 is_admin 门 + 写 admin_events + revoke public/anon/authenticated 再 grant authenticated。
- 迁移文件：`jobs/JOB-2-07/migrate.sql`；回滚：`jobs/JOB-2-07/rollback.sql`（含 DROP，需你手动在 SQL Editor 跑，db-run.sh 会拦）。

## 业主要看什么
1. 学生抽屉 → 材料区：每行有「🗑 删除」；点它 → 二次确认 + 要填理由 → 删完消失 + 「管理操作历史」多一条。
2. 已审核通过的材料行有「🔓 解锁重传」；解锁后该行显「已解锁」。
（真删除/真解锁是对数据库的真写入，需你登录管理员账号在线上点一次验收；我在本地只用受控桩验证了按钮和调用接线，未碰真库。）

## 十道闸（逐条）
- 闸1 基底 md5：`65b5eed9e8bfe55d90b0dab5e30d0821`（=v187）✓
- 闸2 锚点唯一：8 处各 =1 ✓
- 闸3 补丁自检：两函数定义/两 rpc 调用/select 补两列/软删过滤/slotReopenedAt 映射/删除+解锁按钮/历史段装配+填充 各 =1 ✓
- 闸4 产物 md5：`9bb020499ad55d1bba7e05d5ab816463`（v188）
- 闸5 语法：1 脚本块 0 报错 ✓
- 闸6 消失检查：768→774（+函数/id，无消失）✓
- 闸7 四语：管理端中英；新增文案（删除/解锁重传/已解锁/管理操作历史/删除材料/解锁材料槽）中英齐全，零裸键 ✓
- 闸8 真点击：Playwright 桩 —— _adm7Inner 三行分别渲染 删除×3 / 解锁重传×1(仅已审核未解锁行) / 已解锁×1；_v188DeleteDoc 走 rpc('admin_delete_document', {p_doc_id,p_reason}) 且理由透传；_v188FillHistory 渲染「删除材料·拍糊」；_adm7Load 过滤掉 deleted_at 非空的行(2 行→1 行)。反向样本 v187：函数未定义、无按钮 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：新增写死颜色 0 / CSS 类 0 / 字号档 0（复用 btn-ghost/badge purple/var(--line-soft)/_promptP）✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：65b5eed9e8bfe55d90b0dab5e30d0821 → 9bb020499ad55d1bba7e05d5ab816463
迁移跑前跑后：documents 列 14→18(+4)；本卡函数 0→2(+2)
index.html 改了几处：3（v187→v188）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- QUEUE 写 `admin_delete_document(p_doc_id uuid,...)`，但实际 `documents.doc_id` 是 **text** 不是 uuid，故函数参数用 text（否则调用报错）。已在迁移注释说明。
- 「锁槽状态」库内无专列，按 QUEUE 授权新增 `slot_reopened_at` 作「已解锁」标记（非破坏性；不删旧件、不删裁决）。前端把"已审核通过且未解锁"视为锁住的槽显示解锁按钮。
- 真删除/解锁的端到端（真 RPC 改真库）留你线上验收：psql 无管理员 auth 上下文，is_admin 门会拒绝，无法在容器内跑通真写入。
