# JOB-24-02 · 管理端 v228：付款流水「一本账」+ 侧栏金额算全 + 导出全字段 + 录取全景搬家

（CC 写给业主。大白话。这一包只改管理端，出了新版 v228，不碰数据库。）

## 一、原来的毛病

付款流水页是四段堆在一起：待核销 / 平台收入台账（只有服务费）/ 录取全景（那不是钱）/ 对账中心（通知书费，类型还是英文码、看不到支付方式）。两本账没合到一起，左边栏那个金额只算了「平台服务费 ¥8,100」，把 11 笔 ¥3,000 的通知书解锁费全漏了；导出也只导对账中心那一段。

## 二、这一版怎么改

1. **合成一本账**（新函数 `_v228Ledger`）：把三处的钱并到一张表——
   - 平台服务费（曝光加速 / 录取加速 / 顾问 / 审核）；
   - 通知书解锁订单（已核销 / 待核销 / 已驳回）；
   - 老的「offer 已付款但没走订单系统」的兜底行（不和订单重复计数）。
   每一笔都说人话、中英双语：类别（通知书解锁 / 曝光加速 / 录取加速 / 审核服务 / 顾问服务 / 其它）、支付方式（卡 / PayPal 人工 / 线下…）、通道（本人道 / 中介道）、状态（已完成 / 待核销 / 已驳回）全部翻成中文。

2. **页面重排**：待核销队列置顶 → 五张分类小卡（每类：笔数 / 合计 / 本月 / 待核）+「全部」卡 → 类别筹码 + 第二行（状态 / 通道 / 月份）筛选 → 一本账列表。每行看得到：金额、类别、通道、付款人（学生名+学号，点开抽屉；中介代付会标出来）、关联（学校·项目·任务号）、单号、支付方式、时间、经办、来源表；有凭证的能「看凭证」。

3. **左边栏金额算全**：侧栏「付款流水」那个数，从「只算平台服务费」改成「整本账已完成合计」——桩数据下从 ¥8,100 变成正确的 **¥23,100**（8,100 服务费 + 9,000 三笔已核销通知书费 + 6,000 两笔 offer 兜底）。

4. **导出全字段**：付款导出从 8 列扩到 **13 列**、全中文（日期 / 类别 / 金额 / 通道 / 付款人 / 学号申请号 / 学校 / 项目 / 单号 / 支付方式 / 状态 / 经办 / 来源表），所见即所导（当前筹码筛完的那些行），CSV 里不再出现 `exposure_boost` 这种英文码。

5. **「录取全景」搬家**：它本来不是钱、却挤在付款页。现在从付款页拿走，搬到「录取通知分发」页顶上的第三枚筹码「全景 · N」里，点一下才看——付款页从此干干净净只讲钱。

## 三、结果（自查）

- 随卡自验脚本 `tests/job24-02-admin-v228.mjs`：**42 项全绿**（中文一遍、英文一遍），关键几条：
  - 一本账 **9 笔已完成**（4 平台 + 3 已核销单 + 2 offer 兜底）+ 1 待核销 + 1 已驳回 = 共 11 行；
  - 「通知书解锁」类 **5 笔、合计 ¥15,000**；
  - 侧栏付款金额 = **¥23,100**；
  - 点「曝光加速」筹码 → 只剩 1 行，行里有「中介道」「4,500」「PayPal」；
  - 导出表头 13 列，无英文原码，类别是「曝光加速」这种人话；
  - 付款页里没有「录取全景」四个字了，录取通知分发页第三筹码点开能看到录取全景。
- **没有弄丢任何东西**（N1 零移除，同一把尺子量 v227→v228，删除数 = 0，只新增了 9 个 `_v228…` 函数）；`renderPlatformPaymentsPanel` 与 `renderAdminOffersPanel` 两个老函数**定义都还在**，只是付款页不再调用它们。
- 页面零报错；没有新增任何颜色 / 字号 / 样式类（六个色值、`kpi-delta flat`、`badge red` 等都在 v227 里本来就有）。

## 四、动手时修的一处桩数据小账（说明）

24-00 造的假订单里，把「已核销的 3 笔通知书订单」错挂在了和「2 笔 offer 兜底」同一批学生/同一个 offer 上，而且状态写成了 `confirmed`、类型写成 `letter`——这跟真库对不上（真库 payment_orders 状态叫 `paid`、通知书解锁类型叫 `offer_letter`，见迁移 0084），也跟 24-00 桩规「这 2 笔兜底必须没有对应订单」自相矛盾。CC 按真库列值把这 3 笔改成 `paid` / `offer_letter`、并挪到不与兜底学生重叠，兑现「合账 9 行、不重复计数」。此改只动测试桩、不动任何产品代码，也不影响 24-00 的红线基线（R1–R6 复跑仍全红、零报错）与 24-01 自验（复跑 25 绿；其原 S4「index 指向 v227」因本版把 index 指针前移到 v228 而按 P3a 同步改写）。

## 五、业主要做什么

这一包不用你操作、不用贴 SQL（本包零数据库改动）。想看效果就打开管理端「付款流水」页：上面是待核销，接着五张分类小卡，再往下是一本账；每笔看得到付款人、学校项目、单号、支付方式、时间、经办；左边栏金额把通知书费算进去了；导出的 CSV 二十来项…（更正：13 列）、全中文；「录取全景」在「录取通知分发」页的第三枚筹码「全景」里。截图见 `shots/24/24-02/`。

---
- 版本四件套：文件名 v228 / 头注 / [VER] 探针 / index.html 三处重定向 / changelog 各就位 ✅
- 闸3 静态：14 项全绿（[VER]、头注、index、changelog、函数定义、script 可解析）✅
- 闸8 无头真渲染：中英各一遍，28 项全绿（台账 9 行 / 侧栏 ¥23,100 / 曝光筹码 1 行 / 导出 13 列 / 全景搬家）✅
- 闸9 视觉零新增：零新颜色 / 字号 / 样式类（全复用 v227 既有）✅
- N1 零移除：同一提取器 v227→v228 removed=0，added 全是 `_v228*`（见 `jobs/JOB-24-02/n1-diff.json`）✅
- secret-scan：本包新增文件 CLEAN ✅
- 主仓库：本包已 commit（dca0fa0），未 push（按规矩收官由业主推）。
- 迁移：本包零迁移、零数据库改动。
- 本轮暂无「必须停」。
- 下一包 = 24-03
