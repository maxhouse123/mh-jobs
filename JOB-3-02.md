JOB-3-02  结果：全闸通过（管理端 v193 → v194）

## 做了什么（一句白话）
学生抽屉「各方操作汇总」的事件词表以前只认 12 种事件，其它一律显英文原始代码（截图里 `visa_valid_until_locked`、`round_closed_by_student`、`docs_all_passed` 一大片）。现在把词表扩到覆盖库里**实际出现的全部种类**（每种给：图标 + 中文 + 英文 + 来源方），认不出的兜底显「其他 · 代码」，不再裸显英文代码。

## 先探再改 —— 库里实际出现的事件种类全表
**admin_events（21 种）**：contact_unlock(50) / share_opened(29) / visa_valid_until_set(24) / visa_valid_until_locked(13) / share_created(12) / partner_profile_updated(9) / location_change_requested(5) / location_change_cancelled(4) / pool_remove(3) / student_location_changed(2) / share_revoked(2) / referral_signup(2) / exposure_boost_pending(1) / degree_level_corrected(1) / interview_result(1) / task_approved(1) / exposure_order_replaced(1) / exposure_boost_started(1) / referral_withdrawn(1) / round_closed_by_student(1) / referral_resubmitted(1)

**notifications（23 种）**：interview_scheduled(22) / offer_responded(12) / doc_assigned(11) / offer_issued(10) / interview_confirmed(9) / interview_declined(9) / interview_result(8) / doc_review_rejected(5) / partner_profile_requested(4) / profile_change_decided(4) / payment_evidence_submitted(3) / docs_all_passed(3) / school_application_decided(2) / boost_started(2) / letter_uploaded(2) / payment_confirmed(2) / letter_paid(2) / partner_application_decided(1) / announcement(1) / partner_application_submitted(1) / school_application_submitted(1) / task_reviewed(1) / doc_admin_rejected(1)

去重后共约 40 种，全部进了词表（原 12 种保留 + 新增约 31 种）。

## 业主要看什么
打开学生抽屉「各方操作汇总」，**一个英文原始代码都没有**了（认得的显中文标签，认不出的显「其他 · 代码」）。

## 十道闸（逐条）
- 闸1 基底 md5：`1b870a6cad0fdbef4bc73da0bebd0c45`（=v193）✓
- 闸2 锚点唯一：4 处各 =1 ✓
- 闸3 补丁自检：[VER] v194、新增多种键、兜底其他·kind、旧裸兜底归 0 各命中 ✓
- 闸4 产物 md5：`c723a63d0145aa244f16d53e9e2b7f2c`（v194）
- 闸5 语法：1 块 0 报错；[VER] 探针断言 admin=v194 ✓
- 闸6 消失检查：798→798，消失 0 ✓
- 闸7 四语：管理端中英；新增标签全部中英齐全；兜底「其他 · kind」是设计要求（非裸键）✓
- 闸8 真点击：Playwright —— 8 个原先裸显的代码(visa_valid_until_locked/round_closed_by_student/docs_all_passed/referral_withdrawn/share_created/partner_profile_updated/location_change_requested/contact_unlock)现在都返回中文标签(非代码)；未知 kind→「其他 · xyz_bogus_kind」；英文版返回 'Visa validity locked'。反向样本 v193：7 个仍裸显 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：只改词表数据，零新颜色/类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底/产物 md5：1b870a6cad0fdbef4bc73da0bebd0c45 → c723a63d0145aa244f16d53e9e2b7f2c
index.html 改了几处：3（v193→v194）
提交哈希：主仓库本地提交，未推送
偏离或疑点：来源方只用时间线认的四桶（学校/中介/学生/平台）；中介类事件归「中介」(agent 桶)、系统类归「平台」。
