# JOB-19-03 回执 · watermark-doc v34（改源码不部署）+ 审核端 v45 —— 把慢件的时间量出来 + 角色门并行

## 一句话
给 watermark-doc 加了一个专用计时头 `x-mh-wm-timing`（每段毫秒 + 冷启动龄 boot_age + 走了哪条路 served），把 19-00⑤ 查到的**唯一能修的一处**（四级角色判定串行 → 并行）修了，(b)(c) 无空间照实说明；审核端 v45 在每份查看的 `[v43] wm timing` 旁边加打一行 `[v45] wm server`，直读/云函数两路都读这个头（无头打 `server=n/a`）。**watermark-doc v33→v34、reviewer v44→v45**，鉴权/派单/缓存语义一字未改、JWT 校验保持开。CC 只 commit 一笔主仓库、**未部署、未推主仓库、无迁移、无手贴 SQL**。真·30 份线上计时表由业主部署后跑 `wm-timing-probe.mjs` 产出。

## 起点核对（§0.2）
- watermark-doc v33 源码 md5 = `55debf4dcc841c8370d2b4280d9e8768` == QUEUE-19 §0.2 基线 ✓（改前实测）。
- reviewer 上一版 = v44（19-02 落）。本包 v44→v45。
- 主仓库 HEAD 改前 = `8695d92`（19-02）；本包 commit 后 = `9cdd25c`（未推）。
- 本机无 Deno（只 node v24）→ EF 无法本地真跑。真·每分支执行 + 30 份线上计时 = 业主部署后由 `deploy-watermark.sh` 自探 + `wm-timing-probe.mjs` 产出（QUEUE-19 19-03 本就把线上计时表定为业主部署后步骤）。本地闸3 = 外科手术式 diff + 逻辑模型 + 静态断言 + N1，与 19-00⑤ 的静态分析同一取证方式。

## 做了什么

### 1) watermark-doc v34（源码改，未部署）—— md5 `adf9c70efdf51ca0ea3148c99570b3f2`
逐字节 diff 见 `jobs/JOB-19-03/wm-v33-v34.diff`（71 insertions / 14 deletions，14 处「删」全是角色门与包装器就地重写，无行为丢失）。三块改动：

- **① 计时头 `x-mh-wm-timing`**（`Deno.serve` 包装器内，与原 `Server-Timing` 并存）：新增纯函数 `buildWmTiming(marks,total,served)` 把 marks 折成业主/审核端可读段落——
  `auth=鉴权(JWT) / cache=权限门+缓存索引检查(role..own) / fetch=取原件(own..dl,含doc查询) / render=烧录水印(dl..wm) / upload=写审计(wm..audit)`，未及的段落省略；再追 `total`、`boot_age=<模块加载到本次请求的秒数>`、`served=<rendered|direct|cached|video|denied|other>`。写入响应头并加进 `Access-Control-Expose-Headers`（浏览器/前端才读得到），另 `console.log("[wm v34 timing]" …)` 供函数日志看。`served` 由 handler 在各终点标注（`ctx.served`）：图片/PDF 烧录=`rendered`、学校解锁原件=`direct`、signed_url 命中桶=`cached`、视频直链=`video`、prepare=`prepared`、cleanup=`cleaned`；未标且状态≥400=`denied`，其余 200=`other`。
- **② 修(a) 角色判定并行**：原 `is_reviewer→is_school→students(self)→students(partner)` 是**串行 if-链**（逻辑互斥，19-00⑤ 认定的唯一可修点），改为 `Promise.all` 一次并发。**优先级 reviewer>school>student>partner 与「逐级错误只在落到该级时才返回」的语义逐字保留**——每条查询套 `(async()=>…)().catch(e=>({data:null,error}))`（照抄本文件上方 `docLookupP` 同款写法），把网络级 reject 归一成 error 对象，与串行版「到该级才踩到错误」完全一致；上级已命中角色时下级的错误被忽略（reviewer 命中路零回归）。非 reviewer 的 caller（school/student/partner）由此**少走最多 3 趟串行往返**；reviewer 命中路时延不变（并发取 max）。代价：reviewer 请求现在总会并发多发 3 个廉价查询（SECURITY DEFINER 布尔 RPC + RLS 单行 select），DB 负载略增、时延不增。
- **(b) 缓存命中重取/重渲染 = 无可修**：render 主路本就**无取缓存渲染件的动作**（缓存命中走独立的 `signed_url` 分支，命中即签 URL 返回、不取原件不重渲染）——19-00⑤ 已证，本包如实记录、不动。
- **(c) 整读入内存改流式 = 无空间**：图片/PDF 烧录水印**必须拿到整份字节才能画**（无法边流边水印）；唯一可议的是 school 解锁后的 `served_original` 旁路，但收益微且非慢件路径，本轮不动（19-00⑤）。冷启动若占大头 → 按 §0.10 **只写保温建议、不加保温定时任务**（线上计时表出来后在 SUMMARY 落建议）。
- **鉴权/派单/缓存语义一字未改**：撤销门(`denied_revoked`)、软删门(`denied_doc_deleted`)、归属门四角色分派、415 兜底（原件绝不出网）、waitUntil 异步审计、signed_url/signed_video/prepare/cleanup 全部逐字保留（diff 可证）。

### 2) 审核端 v45（v44→v45，四件套齐）—— md5 `c2cf1f26cffee208cfba137b430b18a6`
- 整渲染 EF（`_wmFetchEF`，`resp` 在手）与 signed_url 桶命中（`_wmTryCache`，`r` 在手）两路读 `resp/r.headers.get('x-mh-wm-timing')`，原样挂到返回对象 `_srv`（**不改取件逻辑**，只多带一个诊断字段）；直读桶 `_wmTryDirect` 走 supabase SDK download 无 EF 头 → 无 `_srv`。
- 在每份查看统一日志点（`[v43] wm timing` 那一行）**旁边加打一行** `console.info('[v45] wm server', <id8>, _srv || 'server=n/a')`——直读命中/旧版函数未部署时打 `server=n/a`。纯诊断 console 输出，零 UI/DOM/RPC/库/查看器改动。
- 四件套：①文件名 `maxhouse-reviewer-portal-v45.html`；②头注 v45（2026-09-23）；③`[VER] reviewer portal v45` 探针（同步改）；④`reviewer-portal/index.html` 三处重定向目标 → v45；changelog 追一行。

### 3) 交付物（业主在卡1146 里跑，均未执行/未部署）
- `jobs/JOB-19-03/deploy-watermark.sh`：部署 v34（**JWT 校验保持开，不带 --no-verify-jwt**）+ 自带探针：①OPTIONS 2xx+CORS（现役 200 ok；见下说明）；②无鉴权 401 missing_authorization；③reviewer1 真登取自己队列一份 → 200 且响应头带 `x-mh-wm-timing`（含 served/boot_age 段落）。不打印任何 token/密码/key。
- `jobs/JOB-19-03/wm-timing-probe.mjs`：reviewer1 真登，逐份直读队列前 30 份，打印每份 `x-mh-wm-timing` 各段与总耗时，末尾一张按「慢在哪一段」分组的表，写 `~/mh-jobs/JOB-19-03-timing.md` 并 push（照 JOB-18-06 规矩，`MH_WALK_NO_PUSH=1` 只写不推）。
- `jobs/JOB-19-03/wm-v33-v34.diff`：v33↔v34 逐字节 diff 存档。

## 结果 / 闸3

### N4 分支矩阵（静态 + 逻辑模型；本机无 Deno，真执行在业主部署时）
每条响应都经包装器 → 都带 `x-mh-wm-timing`（OPTIONS 预检除外，见注）。段落之和 ≈ total（拒绝路只到某段即止，和=最后一个 mark ≈ total）。

| 分支 | 触发 | 状态码 | served | 计时头 | marks(段落) |
|---|---|---|---|---|---|
| OPTIONS | 预检 | 200 "ok"+CORS | —— | **无**(预检无正文，本轮不改 OPTIONS) | —— |
| 无鉴权 | render 无 Authorization | 401 missing_authorization | denied | 有 | total/boot_age/served |
| 非授权角色 | 四级角色全不中 | 403 not_authorized | denied | 有 | auth + total… |
| 未派单(reviewer) | assignP 空 | 403 doc_not_assigned_to_you | denied | 有 | auth/cache + total… |
| 已撤销 | revoked_at 非空 | 403 assignment_revoked | denied | 有 | auth/cache + total… |
| 已软删 | deleted_at 非空 | 410 doc_deleted | denied | 有 | auth/cache/fetch… |
| 缓存命中 | signed_url 模式命中桶 | 200 | cached | 有 | total/boot_age/served |
| 需渲染 | 图片/PDF 现场烧录 | 200 | rendered | 有 | auth/cache/fetch/render/upload/total |
| 学校解锁原件 | school 已同意 offer | 200 | direct | 有 | 至 fetch |
| 视频签名 | allow_video / signed_video | 200 | video | 有 | 至 fetch / total |

> 注·OPTIONS：watermark-doc 现役对预检返回 `200 "ok"+CORS`（非 204），本轮**不改 OPTIONS 行为**（§0.10 没写的不做；浏览器接受任意 2xx 预检）。deploy 探针据此核 2xx+CORS 并打印说明，非红。

### (a)(b)(c) 逐条旧红新绿
- **(a) 角色门并行** —— **绿**：`tests/static-check-v34-watermark.mjs` ③ 用带 60ms 延迟的假 client 建模：串行四级 ≈ **247ms**（四趟累加，旧红）→ 并行 Promise.all ≈ **62ms**（取 max，新绿），两法角色判定结果一致（partner）；断言 `并行 ≤ 串行/2`。
- **(b)** —— 无可修（render 路无重取缓存渲染件），静态断言 `schoolSeesOriginal`/signed_url 分支保留、render 路无缓存查找；如实记录。
- **(c)** —— 无空间（水印需整份字节），如实记录；served_original 旁路本轮不动。

### buildWmTiming 逻辑模型（v34 逐字复制到测试）
- 渲染路 marks[auth40,role95,own150,doc155,dl900,wm1750,audit1760] total1770 → `auth=40ms;cache=110ms;fetch=750ms;render=850ms;upload=10ms;total=1770ms;boot_age=..s;served=rendered`；段落之和=1760(=audit)≈total(1770) ✓；render=wm-dl=850ms（水印烧录=大头，印证 18-00③）。
- 拒绝路 marks[auth40,role95] → 只 `auth/cache/total/boot_age/served=denied`，和=95≈total ✓。
- 缓存路 marks[] served=cached → 至少 `total/boot_age/served=cached` ✓。

### 审核端 v45（[v45] wm server）
- `tests/static-check-v45-reviewer.mjs`：静态断言（四件套 + 两路捕获 `_srv` + `[v45]` 行 + 无头兜底 `server=n/a` + 直读桶未加 `_srv`）；逻辑模型逐字复现 `[v43]/[v45]` 日志块——有头→打服务端计时原文、无头→`server=n/a`、`[v43]` 行仍在（两行并存）；红对照 v44 不含 `[v45] wm server`。
- **N1 差集空**：同一提取器 v44 vs v45 → ids 74→74、handlers 25→25、functions 140→140，**0 lost / 0 added**（诊断行不增删按钮/id/函数）。
- 审核端 HTML 内联 JS `node --check` = 语法 0 错。

### 随卡回归（入仓）
`tests/static-check-v34-watermark.mjs` = **29/29 绿**；`tests/static-check-v45-reviewer.mjs` = **18/18 绿**。两支均随本包 commit 入主仓库 tests/。

## 业主要做什么
- **卡1146 部署 watermark-doc v34**：挂代理 + `supabase login` 后跑 `bash jobs/JOB-19-03/deploy-watermark.sh`（JWT 校验保持开）；探针全过后再 `export SUPABASE_ANON_KEY=<公开anon> && node jobs/JOB-19-03/wm-timing-probe.mjs` 出 30 份线上计时表（写 `~/mh-jobs/JOB-19-03-timing.md` 并 push）。
- 收官后可亲眼验收（进 SUMMARY「要业主亲眼看的」）：审核端队列往下滚后点某份「查看」，控制台每份除 `[v43] wm timing` 外多一行 `[v45] wm server …`（v34 部署后带 auth/cache/fetch/render/upload/boot_age/served；未部署或直读命中打 `server=n/a`）。
- 主仓库本包 commit 一笔（`9cdd25c`）待收官卡统一推。

## 新闸证据 N1–N7
- **N1**：reviewer v44→v45 差集空（ids/handlers/functions 各 0 lost/0 added）；watermark-doc 非 UI 端不适用按钮清单，改动全为 diff 可证的外科手术式增改。
- **N2**：旧病红在 19-00 已坐实（C/D 属 19-02；本包对象=慢件计时缺失 + 角色门串行）；(a) 用带延迟假 client 串行 247ms(红)/并行 62ms(绿) 成对；[v45] 有头/无头两态成对；红对照 v44 无 [v45]。
- **N3**：本包不连库、不登录（逻辑模型 + 静态；EF 无法本地跑）。真身份探针在 `deploy-watermark.sh`（reviewer1 真登取自己队列）与 `wm-timing-probe.mjs`（reviewer1 真登直读 30 份），均业主部署后跑、不打印 token。
- **N4**：分支矩阵见上（静态 + 逻辑模型）；每条响应带 `x-mh-wm-timing`、段落之和≈total、served/boot_age 齐；真执行由业主部署探针覆盖。
- **N5**：截图无（本包无视觉件）；交付脚本 `secret-scan` 由收官走查覆盖；脚本一律不打印 token/密码/key（deploy 与 probe 均已核）。
- **N6**：只改审核端一个文件（+ index/changelog）与 watermark-doc 一个云函数源文件（+ 两支 tests + 本包 jobs 工装）；共用查看器 `assets/mh-viewer-v2-1-1.js`、学校端、管理端、precheck-ai、admin-doc、send-invite-email **一行未动**。
- **N7**：本包审核端改动=纯 console 诊断行，**无「给人看的提示件」**（未动 toast/卡片/提醒），N7 不适用。

## 测试写库收尾
- 本包**零 DB 写入、零登录、零上传**（逻辑模型 + 静态，不连真库）。无测试件需清理；无带身份探针复核之必要。业主部署后的 `deploy-watermark.sh`/`wm-timing-probe.mjs` 只读队列 + 取自己的件（合法查看，审计属正常，非测试写库）。

## 给 19-04 的交接
- 审核端进到 v45；watermark-doc 到 v34（源码改，**待业主部署**）。
- 19-04 = 学生端 v398：`_v706HumanizeIssue`（v397 第 10103 行起）材料名包裹引号按当前语言（zh「」/ en ""/ ru «»/ fr « »），只改这一处，不动替换范围/判词/不重判。stuA 真页面英/俄/法各截一张（旧四语都「」=红 → 新版随语言=绿）；中文仍「」；四语逐键；N1。
- **迁移**：19-03 无迁移（0213 在 19-01 经探针②证明不需写；19-03 纯代码）。下一空号仍 0213（留给后续块）。
- 下一包 = 19-04
