# JOB-07 · 三端搜索补齐

**结果：全闸通过**

## 做了什么（一句白话）
当事人报得出来的编号，后台以前搜不出来。这次补齐：学生页能用**护照号 / 电话 / WhatsApp** 搜到人；学校页能用**学校编号**（就是 `SCH-1AD6C8` 那种）搜到；合作伙伴页能用**联系邮箱 / 联系电话**搜到。顶部的全局搜索框也同步支持这些。

## 业主要看什么（打开哪里、点哪个、应该看到什么）
1. **学生页**：上方搜索框输入某学生的护照号 / 电话 / WhatsApp → 能筛出这个人（以前只认姓名/学号/邮箱/学校）。
2. **学校页**：搜索框输入 `SCH-1AD6C8`（学校编号）→ 能搜到那所学校（**以前搜不到**）。
3. **合作伙伴页**：搜索框输入中介的联系邮箱或联系电话 → 能搜到这个中介。
4. **顶部全局搜索**（页面最上方那个框）：以上都能搜。

**顺带修的**：学校搜索框以前的中文提示写着「搜索 校名 / **简称** / 邀请码 / 城市」，但**云端学校数据根本没有「简称」这一列**（后台从没读过它），所以简称搜索一直是摆设。这次把提示里的「简称」删掉、改成「编号」（编号是真能搜的），不再误导。

## 十道闸（逐条 + 实测数字）
| 闸 | 结果 | 实测 |
|---|---|---|
| 1 基底md5 | PASS | `dc54b4324472cd5bf6246d71493cd76e`（=JOB-04 产物）|
| 2 锚点唯一 | PASS | 8 处锚点各出现 1 次 |
| 3 补丁自检 | PASS | 学生 passportNo/phone/whatsapp 搜索各 2 处（列表+全局）；学校新数组含 s.id 去 short=1、旧数组归 0；中介含 contactEmail/contactPhone=1；旧「简称」提示归 0。反向样本拿 v174 跑，3/3 失败 → 有分辨力 |
| 4 产物md5 | 记录 | `2ed0548ee3791d1c7336a93b6e4bb7f7` |
| 5 语法 | PASS | 脚本块 0 报错 |
| 6 功能清单 | PASS | 函数/id/window/onclick **全等**（本包只改函数体，无新增无消失）|
| 7 四语 | PASS | 管理端中/英双语；改动的提示文案中英齐全、无裸键 |
| 8 真点击 | PASS | 真浏览器造受控数据后真调三个搜索函数：护照/电话/WhatsApp/学校编号/中介邮箱/中介电话 **各命中 1 行**；反向输入不存在的值 **各 0 行**（空态）|
| 9 视觉一致 | PASS | 新写死颜色 **0**、新 CSS 类 **0**、新字号 **0**（纯逻辑+一处提示文案）|
| 10 线上版本 | 跳过 | 本轮不推送 |

## 基底 / 产物 md5
- 基底：`dc54b4324472cd5bf6246d71493cd76e`
- 产物：`2ed0548ee3791d1c7336a93b6e4bb7f7`

## index.html 改了几处
3 处，v174→v175，无残留。

## 提交哈希
主仓库 `ad1460d`（已 commit，未推送）。

## 偏离或疑点
1. **学校「简称」搜索取舍**：队列给了「真接上或从提示删掉」二选一。因为云端 `schools` 表没有短名列（装载器 select 里从来没有），真接不上，所以按第二个选项——从提示删「简称」、并把两个恒空的 shortZh/shortEn 从搜索匹配里移除（它们永远匹配不到，是死字段）。如果你其实希望有「简称」搜索，需要先在数据库/学校端加这一列，那属于另一个包。
2. **闸8 的 390px 溢出检查本包不适用**：这个包只改搜索匹配逻辑和一句提示文字，没有新增可视布局元素，所以闸8 做的是「搜索命中/空态」的行为断言（更贴合本包），没有做像素溢出测量。
3. 护照号按队列边界**只用于匹配、不显示在学生列表**——本包没有往列表加任何列。
