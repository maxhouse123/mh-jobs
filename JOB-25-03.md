# JOB-25-03 · 管理端 + 中介端也显示学生「全部层次 / 全部专业」

（CC 写给业主。大白话。这一包和 25-02 一样，要 25-01 那段 SQL（0217）贴了之后，云端才真的把多层次传给这两个端；没贴前照旧只显一个，不报错。）

## 一、解决什么

多层次贯通的第三步：让**管理端**（v233）和**中介端**（v171）也把学生报的**全部层次、全部专业**显示出来（此前都只显一个）。

## 二、CC 做了什么

### 管理端 v233（从 v232 拷贝改）
- **接住新数据**：学生装载后，额外轻查一次「多层次」列并按学生合并进来；查不到（SQL 没贴）就静默，全体退回老的单值，不报错。
- **四个小助手**（`_v233` 前缀）：把多层次数组翻成——全部层次顿号并列、专业按层次分组的文字。层次名六种全覆盖（语言生/短期/专科/本科/硕士/博士），认不出的原样显示、绝不显英文代码键。
- **导出 CSV**：「学历层次」列显**全部层次**（顿号并列，例 `语言生、硕士`）；「项目」列显**按层次分组的专业**（例 `硕士：CS、BA；语言生：—`）。学生页导出、申请页导出都同步。
- **学生抽屉**：意向段**新增「学历层次」一行**（全部层次），「意向专业」改成**按层次分组**。空的就退回老单值。

### 中介端 v171（从 v170 拷贝改）
- **接住新数据**：中介的「我推荐的学生」装载后，额外轻查一次多层次列（只查自己推荐的），按学生合并；查不到就静默退回单值。
- **一个小助手**（`_v171Programs`）：数组优先、空则退回单值，拼出该生的全部层次。
- 推荐学生卡上原本就有「按 programs 逐个显示」的代码，只是以前 programs 里只有一个层次；现在填成全量，**卡片自动显示全部层次和各层次专业**，没动卡片模板。
- **中介代填学生的写入**（把资料写回库那条）**本轮没动**——它目前只写单个层次。多层次写入留**第二十六轮**（要同时改库里的写入函数和代填表单）。本轮中介端只改**显示**。

## 三、一个重要的自查（差点酿事故）

管理端的 [VER] 版本探针（打印到控制台的一行字）里，CC 一开始把 `select('id,target_programs')` 连着单引号写进了另一段单引号字符串里，**单引号打架直接让整段管理端脚本报语法错、白屏**。闸门（无头加载）当场亮红抓到，已改成不带引号的写法，管理端加载零报错。**这正是"先红后绿"闸门的价值**——没让坏页面出门。

## 四、闸门（全过）

`node jobs/JOB-25-03/green-03.mjs` → **PASS=30 / FAIL=0**（日志 `jobs/JOB-25-03/green-03.log`）：
- 静态：管理端 7 项（映射补列、合并、四助手、导出两列接上、抽屉两处接上）、中介端 4 项（助手/合并/映射改/**写入侧确认仍是单值**）。
- **无头真的用页面翻译跑助手**：管理端两层学生→层次 `语言生、硕士`、专业 `硕士：CS、BA；语言生：—`；单值学生→一个层次一个专业；层次名不是英文代码键；管理端零报错。中介端两层→数组两条全在（第一条语言生、第二条硕士带两个专业）；单值→回退一条；中介端零报错。
- N1：管理端 v232、中介端 v170 的按钮/内联动作/函数**一个没少**（管理端新增 4 个助手，中介端新增 1 个）。
- secret-scan 干净；没加新翻译键；没加 shell 脚本。

## 五、业主要做什么

- **这一包本身不用你做事**。若 0217 还没贴，两端照旧只显一个层次，不报错。
- **贴了 0217 之后验收**：管理端学生页那位两层学生的抽屉/导出 CSV「学历层次」显两个层次；中介端「我推荐的学生」卡显两个层次。

## 六、边界与记账

- 没动查询函数调用、没动库、没动脱敏、没动审核端/学生端/学校端。
- **第二十六轮必含**：中介代填学生的**多层次写入**（库里写入函数加参数 + 中介代填表单支持多层次）。

---
- 闸3：green-03 PASS=30/0 ✅；N1 零消失(admin +4 / partner +1) ✅
- 本轮暂无「必须停」（`~/mh-verify/r25-STOP.txt` 不存在）
- 下一包 = 25-04
