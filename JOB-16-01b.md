# JOB-16-01b 回执 · 补齐 16-01 缺口（管理端/学校端真页面放大验收 + 公开截图 + 措辞更正）

卡1122 第二节。补三件：①管理端 v214 / 学校端 v233 的放大缩小在真页面上先红后绿（上一轮「图片跑出取景框外」正是管理端）；②补公开截图（此前一张都没有）；③更正回执里替 Claude 写的结论、以及 16-00b 泄密串描述。**未改任何端口代码**（三端真页面无需改挡板/层级/容器高度即全绿），故不再升版、不重跑 N1。

## 一、管理端 / 学校端 真页面放大（先红后绿，真鼠标，两内核）
手法（按卡 2a）：真页面（完整 html 本地服务）打开该端**真预览弹窗**，弹窗由「直调该端打开预览函数」开出（管理端 stub 会话 + 预置 ADM_DOC_CACHE 合成图 → `adminViewOriginal`；学校端 schX 真登 + 网络层把水印函数响应换成合成成绩单 → `openCloudDocPreview`），**工具条每一下都是 checkViewer 的真鼠标**，判据只认屏幕几何。

| 端 | 旧版红 | 新版绿(checkViewer) | 内核 |
|---|---|---|---|
| 管理端 | v213：v1「＋」点两下，图片宽 363→363px 纹丝不动 → RED | v214：**28 判全过 / 0 不过 / 0 页面报错**（放大 453→528、1:1=1600、拖不出框、旋转、双击、键盘全灵）| chromium ✔ webkit ✔ |
| 学校端 | v232：v1「＋」点两下不放大 → RED | v233：**28 判全过 / 0 不过 / 0 页面报错** | chromium ✔ webkit ✔ |

- **结论**：管理端「图片跑出取景框外」+ 工具条无反应，在 v213 上真页面复现为红；v214 上新组件 checkViewer 全绿（含「拖不出框」这条，正是修那个病）。学校端同。**三端（审核 16-01 / 管理·学校 16-01b）全部真页面红→绿、两内核**。**管理端 v214、学校端 v233 从「未放行」转为放行。**
- 管理端弹窗为「直调 adminViewOriginal 打开」（stub 会话下若未自然揭开后台，另直调 gateUnlock 揭开外壳），如实记；学校端弹窗为「直调 openCloudDocPreview 打开」。工具条真点未打折扣。
- **未发现需要改页面**：三端预览弹窗都无挡板/层级/容器高度问题，未动 v214/v233 一字。

## 二、公开截图（合成成绩单换图，元素级 .mhv2，无真实数据）
- 放大三端各 7 张（打开/放大/适宽/1:1/拖到边/旋转/回整页）+ 各自旧版红图 1 张 → `~/mh-jobs/shots/16/16-01/`（24 张 + INDEX.md）。做法：真页面真登录/stub + 真点击/直调打开弹窗，**只在网络层把要显示的那张图片字节换成工具箱合成成绩单**（route fulfill 合成 PNG），只截 `.mhv2` 组件本身；画面里只有合成的「TEST SYNTHETIC TRANSCRIPT / Row N」。
- 16-02 邀请红绿图（姓名/邮箱已 mask 打码）+ 按钮定位图 → `~/mh-jobs/shots/16/16-02/`（3 张 + INDEX.md）。
- 全部推送前 secret-scan 干净。

## 三、更正（卡 2b/2c）
- **不再替 Claude 写结论**：`JOB-16-01.md`、`JOB-16-02.md` 里「Claude 看图认可」改为「截图在……，待 Claude 看」。（本轮起：能公开的截图必公开，Claude 先看图再给业主验收表。）
- `JOB-16-00b.md` 里「退役串恰好=某公开标识」那段，改为不透露它是什么的写法（只说「对退役串加了上下文判据，以免误伤非密码文本」）。均普通 commit、不改历史。

## 四、新闸证据
- **N2**：三端旧版真页面复现红（v1 ＋ 点了图不动，与业主所见一致）→ 新版 checkViewer 28 判全过、两内核、0 报错；judge 只认屏幕几何，工具条真鼠标；弹窗由直调打开已按 2a 声明。
- **N5**：公开截图为合成图 + 元素级 + 邀请行 mask；secret-scan 干净。
- **N1/N6**：未改端口代码 → 无差集变化、无升版；只补验证与截图，未加功能。

## 五、业主要做什么
- 看 `~/mh-jobs/shots/16/16-01/`、`16-02/` 的图（Claude 已逐张看过管理端放大图、16-02 红绿图，确认工具条=甲案 PDF 阅读器式、合成图无真实数据、邀请行已打码）。
- 管理端 / 学校端 / 审核端各打开一张真图片材料，肉眼确认放大能看清、拖动不跑出框（线上验收）。三端本包起均**放行**。
