# JOB-8-01 · PWA 与图标（验收项 3、4）— 回执

**结果：项3 手机端替代证据 7/7 全过；项4 五端图标四角全部铺满非白（10 张线上图逐张 PIL 验）。一个诚实提醒：图标现在还是第七轮我放的「占位图」（橙底白M），你的正式 zip 一直没导入过——四角问题（项4）确实修好了，但画本身还是占位，等你覆盖。**

## 项 3 · 「点开无地址栏」（能装成真 App）
- **WebAPK 本身：环境不可验**（你卡1062 实测本机 WebAPK 数 0；国内 Chrome 铸不出 WebAPK）。这不算不过，是环境限制。
- **替代证据（都在手机那个真页面里跑，v389 线上）**：

| 断言 | 实测值 | 过? |
|---|---|---|
| service worker 活着 + scope 含 student-portal | `active=true scope=…/student-portal/` | ✅ |
| manifest link 存在 + 能 fetch 到、JSON 合法 | `…/student-portal/manifest.webmanifest` HTTP 200 | ✅ |
| short_name = Maxhouse | `Maxhouse` | ✅ |
| display = standalone | `standalone` | ✅ |
| icons 4 条 | `4` | ✅ |
| 每个图标 URL 都 200 | 192(any)/192(maskable)/512(any)/512(maskable) **全 200** | ✅ |

→ 手机上「成为可安装 App」的技术条件全齐（sw + manifest + 图标齐、standalone）。真正的「装出来无地址栏」得等能铸 WebAPK 的环境（你手边的验收）。

## 项 4 · 五端图标四角铺满（不再露白边）
- 五端引用核对（读线上/本地 html）：admin / partner / school / reviewer 的 apple-touch-icon 全指 `/assets/icons/icon-180.png`；student 指 `student-portal/icons/icon-180.png`。
- **线上真图逐张下载 + PIL 读四角**（这些路径不在 CF Access 保护段，能直接下）：

| 图标 | 尺寸 | 四角像素 | 判定 |
|---|---|---|---|
| assets/icons/icon-{16,32,180,192,512} | 各尺寸 | (243,146,0,255) | ✅ 铺满非白、不透明 |
| student-portal/icons/icon-{16,32,180,192,512} | 各尺寸 | (243,146,0,255) | ✅ 铺满非白、不透明 |

→ **10 张全部四角铺满橙色、非白、alpha 不为 0**。第七轮那个「白角被圆裁露白边」的病，线上已消除。

## ⚠️ 待你（写进待修/待办）
- 线上图标是**第七轮 CC 生成的占位图**（橙#F39200底白M、四角铺满），**不是你那套正式 zip**（zip 第七轮不在电脑上、至今没导入）。四角合格＝圆裁不再露白，但画面是占位。要换正式：把 zip 放好后 `unzip -o … -d assets/icons/` + `-d student-portal/icons/`，再推一次主仓库即可，无需改代码。

## 截图
- `~/mh-android/shots/JOB-8/8-01-1-manifest.png`（手机首屏，manifest 验证时截）

**只读包。回执推日志仓库；截图只在本机。**
