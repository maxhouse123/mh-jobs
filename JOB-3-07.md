JOB-3-07  结果：只探不改，两张表 + 草稿链探清（拿去开第四轮）

> 全程只读探针（probe.sql / probe2.sql），未动任何代码、未建任何东西。

## 三条逐条结论

### 1. AI 预检结果（两张表）
- **数据在哪**：
  - `ai_precheck_shadow`（65 行）：列 = id / **doc_id** / **student_id** / doc_type / file_path / model / verdict / full_page / **issues(jsonb)** / raw(jsonb) / latency_ms / created_at / content_hash / lang。关联键 = doc_id（对 documents）、student_id（对 students）。
  - `ai_precheck_draft`（35 行）：列 = path / model / **uid** / **doc_id** / doc_type / verdict / full_page / **issues(jsonb)** / raw(jsonb) / latency_ms / judged_at / content_hash / lang。关联键 = doc_id、uid（学生）。
  - 学生上传时看到的预检警告 = `issues`（jsonb）；`verdict` 是总判、`full_page` 等是标志。
- **管理员读得到吗**：**读不到**——两表都 RLS 开、**0 策略**（默认拒）。
- **要不要加策略**：**要**——各加一条 `admin_read_*`（is_admin 门，照 JOB-3-05 写法）。
- **建议怎么显示**：学生抽屉材料行下，按 doc_id 匹配显示该件的 AI 预检 verdict + issues（几条警告）。两表关系需你定：shadow 像"正式影子记录"（有 student_id）、draft 像"上传前草稿判定"（按 uid）——建议材料行优先取 shadow（按 doc_id），draft 作补充；第四轮开工前请你一句话确认取哪张为主。

### 2. save_my_draft / get_my_draft 写哪张表
- **数据在哪**：两个函数都读写 **`application_drafts`** 表（不是 ai_precheck_*）。
  - `save_my_draft(p_portal, p_step, p_payload)`：SECURITY DEFINER，按 (user_id=auth.uid(), portal) upsert 到 application_drafts（payload≤200KB）。
  - `get_my_draft(p_portal)`：SECURITY DEFINER，读 auth.uid() 自己 90 天内的草稿。
- 即：学生/学校/中介"填到一半"的草稿都进 `application_drafts`。

### 3. 草稿表 application_drafts 的列/行/策略
- **数据在哪**：列 = **user_id** / **portal**（student/school/partner）/ step / **payload(jsonb)** / updated_at。**18 行**（student 15 / school 2 / partner 1）。
- **管理员读得到吗**：**读不到**——RLS 开、**0 策略**；只有 SECURITY DEFINER 的 get_my_draft 按 auth.uid() 让本人读自己。
- **要不要加策略**：**要**——加一条 `admin_read_application_drafts`（is_admin 门），或建一个 admin RPC（`admin_list_drafts` 返回 user_id/portal/step/updated_at + payload 摘要）。
- **建议怎么显示**：管理端加一处「填了一半的申请」清单（谁 / 哪个端 / 停在第几步 / 多久没动 / 可展开看 payload），或在学生抽屉加「当前草稿进度」一行。**注意 payload 可能含 PII，展示时按双盲脱敏（联系方式四样）**。

## 小结（第四轮排期）
| # | 项 | 现状 | 要加策略吗 | 备注 |
|---|---|---|---|---|
| 1 | AI 预检 | ai_precheck_shadow(65)/draft(35), 0 策略 | 是(各一条 admin 只读) | 材料行按 doc_id 显 verdict+issues; 主表 shadow 建议 |
| 2 | 草稿函数 | save_my_draft/get_my_draft → application_drafts | — | 已定位 |
| 3 | 草稿表 | application_drafts(18), 0 策略 | 是(admin 只读 或 admin_list_drafts RPC) | 展示需脱敏 PII |

**未动代码。** 探针文件在 `jobs/JOB-3-07/`。请据此开第四轮：三处都各需一条 admin 只读策略迁移；AI 预检取 shadow/draft 哪张为主、草稿清单放哪里，请你先定。
