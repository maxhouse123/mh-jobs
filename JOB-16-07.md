# JOB-16-07 · 管理端 v215：通知时间线不再红字（前端兜底）+ 迁移 0209 按裁决不做

CC 做的活。本包按卡1126 对 QUEUE 的裁决执行：**迁移 0209 不做、编号留空；只做前端兜底 `_v215NotifText`；用另一套方法查红，两轮不红 → 兜底照做并标「未放行，等业主手验」**。

## 一、做了什么

**病因核查（16-00 b4 + 本包代码审 + 真页面走查，三路一致）**：卡里猜「某条通知正文不是文字 → 时间线红字报错」，嫌疑指向 0206 用量预警把一坨 JSON 当正文。查下来这条猜测**与事实对不上**：
- 库态（16-00 b4）：近 30 天 admin 全部通知 `body` 都是规整 `{zh/en/…}` 对象，没有一条非对象（0 rows）。
- 迁移源码：`0206_usage_hourly.sql` 的 `usage_hourly_rollup` 传给 `push_notification` 的 title/body **本来就是 `jsonb_build_object('zh',…,'en',…)` 的人话**（L98-101）；那坨结构化明细 `{day_egress_mb,net_402,net_5xx}` 只进 `admin_events`（L97），不进通知正文。故 0209「把预警正文改人话」是**空动作** → 按裁决不做、0209 编号留空。
- 代码审：学生抽屉「面试/录取事件时间线」`_v145FillTimeline` 的 notification 源行原本 `r.title[lang]/r.body[lang]` 取值——对**任何非 {zh/en} 对象值只会取到 undefined → 空串**，`escapeHtml` 走 `String().replace` 不抛；即该渲染路径**对任何形状都不 throw、不整块变红**。真正的毛病是**非 i18n 形状的正文被静默丢弃**（不是崩红）。

**改了 `admin-portal/maxhouse-admin-portal-v215.html`（从 v214 cp，四件套同版）**：
1. 新增 `_v215NotifText(x, lang, isZh)` 渲染兜底：字符串原样／null·undefined→空串／数字·布尔→String／i18n 对象 `{zh/en/ru/fr}` 按语言（缺该语言回落 en→zh）／用量预警明细形状 `{day_egress_mb/net_402/net_5xx}` 拼人话／认不出的对象·数组 → `JSON.stringify` 截 300 字；全程 try/catch，最坏返回空串。**一律按文字经 `escapeHtml` 插入，绝不拿原始正文拼 innerHTML。**
2. `_v145FillTimeline` 的 `r.source==='notification'` 分支：title 与 body 都改走 `_v215NotifText`（修「非 i18n 正文被丢」）。
3. `_v145FillTimeline` 每行渲染包 **try/catch**：单行抛错只显「这条通知显示不出来」，不让整块时间线变红。
4. 四件套：文件名 v215、头注 `[VER] v214→v215`、`[VER]` console 探针升 v215、`admin-portal/index.html` 三处重定向改 v215。changelog 一行。

## 二、结果是什么

- **真页面 stub 走查**（子代理，`~/mh-verify` Playwright + `--no-proxy-server`；realpage.mjs serve + 真访问码 ADMIN-2026 → stub 登录(is_admin) → 学生列表 → **真点**合成学生开抽屉，`_v145FillTimeline` 真触发；同一份夹具 3 条通知：i18n 用量预警 / 纯字符串正文 / null 正文）：
  - **v214（旧）**：3 行都渲染、0 控制台报错、**无崩红**；但**纯字符串正文那条正文被丢弃**（只剩标题「系统」）——`rowBText=false`。
  - **v215（新）**：3 行都渲染、**0 控制台报错**、纯字符串正文那条**正文补回**显示为文字（`rowBText=true`）、null 正文那条显示标题、无整块红、无单行崩溃兜底触发。
  - 证据：`jobs/JOB-16-07/realwalk.mjs` + `realwalk.log`（两版 `[RESULT-JSON]` 抄在此）；截图见下。
- **N2 裁决**：这**不是经典「先红后绿」**——旧版 v214 用这三种形状**跑不出崩红**（16-00 查库 0 条畸形 body + 代码对任何形状不 throw，两路已证）。v215 的改进是**把 v214 静默丢弃的正文补回**（绿→更绿）+ 每行 try/catch 防御。故 R4「时间线红字」**崩红形状未复现**，按 §0.11 与卡1126 **标「未放行，等业主手验」**（若确有能让 `[lang]` 路径抛错的畸形形状，不在这三种之内，需业主给出该真实形状才能复现真红）。
- **N1**（v214→v215，`jobs/JOB-16-00/inventory/inv.mjs`）：`DIFF-REMOVED-TOTAL 0`；ids 199=199、handlers 172=172、functions 仅 **added `_v215NotifText`**，零删除。
- **node 断言**（`jobs/JOB-16-07/assert.cjs`，`ALL-ASSERTIONS-PASS`）：`_v215NotifText` 11 种形状逐个不抛、出人话文字（含用量预警拼人话、认不出转 JSON）；`_v145FillTimeline` 编辑后 `new Function` 语法有效（try/catch 括号配平）。
- 主仓库已 commit（`6fdb4fd`：v215.html + admin index.html + admin changelog），**未 push**（收官后业主用卡1118 推）。secret-scan 对待推提交与 ~/mh-jobs 全树：CLEAN。

## 三、业主要做什么

- **本包无需现在做任何事。** v215 会在收官统一推送后随线上生效。
- **一处待手验（未放行）**：R4「通知时间线红字」的**崩红形状本轮未复现**（库里没有、代码也不会崩）。若您确曾在真实数据上看到某条通知让时间线**整块变红**，请把那条通知的样子（或截图/它的 kind）给出来，以便定位真正会抛错的形状；否则本项按「已补防御性兜底 + 静默丢正文已修，崩红未复现」结案。

## 新闸证据（N 闸）
- **N1**：removed 0（见上），仅加 `_v215NotifText`，逐条无删。
- **N2**：真页面真点开抽屉、判据只认屏幕文字（rowBText 真/假 = 正文有没有显示）；旧版「正文丢失」→新版「正文补回」成对（截图 timeline-214/215）；崩红未复现已按裁决标未放行。**未用 evaluate 直调页面函数**（子代理走真访问码+登录+真点行）。
- **N5**：公开截图元素级、全合成夹具（学生「【测试】甲」/stub/example.test）、`shots/16/16-07/INDEX.md` 在位；secret-scan CLEAN。
- **N6**：只改 admin 一端一版、迁移 0209 不做、未动阈值/cron 频率/历史通知/其它端。

## 截图
- 公开（`~/mh-jobs/shots/16/16-07/`）：`timeline-214.png`（旧·正文丢失=红证据）、`timeline-215.png`（新·正文补回=绿）；本机完整版在 `~/mh-verify/shots/JOB-16/16-07/`。

下一包 = 16-08
