# JOB-12-03 · 预取节流（同一个 reviewer v35）

结果：**全闸通过**（闸8 11/11）；reviewer v35 最终 md5 4cecdc939f9cca3edf31a236a06fb170

## 大白话（业主看这段）
病根之三：审核端**一登录就顺序预取 40 份**材料，和你点「查看」的下载抢同一条网络（走代理更明显）。这轮改：
- **登录只预取"第一个学生组"的材料（最多 10 份）**，不再一上来 40 份。
- 你**点「查看」时，后台预取自动暂停**，让这次下载独占网络；查看取完再自动续上。
- 并发 1 份、间隔 300ms、标签页切走暂停 —— 都保留。
- 控制台会打 `[v35] prefetch queue start: N docs`（N 就是新的、小得多的数）。

**一处说明**：本端审核台的学生卡是"默认全展开、没有收合按钮"的，所以卡里说的"其余学生组展开时再取"没有"展开"这个动作可挂——落成"**其余组点查看时按需取**"（现在直读已经很快，点开就下，不卡）。我另留了个 `_wmPrefetchGroup(学生号)` 接口备用（将来若加收合开关可一行接上）。

## 断言表（闸8 tests/job12-03.mjs · 11/11）
| 断言 | 实测 | 过 |
|---|---|---|
| 登录 prefetch queue start = 5（第一组，非全 13） | 5 | ✅ |
| 登录预取份数 ≤10 | 5 | ✅ |
| 登录只取第一组 A | 全 A | ✅ |
| 按需取第二组 B → queue start = 8 | 8 | ✅ |
| 取的是 B 组 | 全 B | ✅ |
| 查看中 paused=true → 未发新取 | 0 | ✅ |
| 恢复后取完（Z0） | 取到 | ✅ |
| 静态·openPreview 置 paused=true | 是 | ✅ |
| 静态·取完(成/败)均恢复 paused=false | 是 | ✅ |
| 静态·[v35] prefetch queue start 文案 | 是 | ✅ |
| 静态·不再 40 份（slice(0,10) 且无 slice(0,40)） | 是 | ✅ |

## 十道闸
| 闸 | 结果 |
|---|---|
| 1 基底 = 12-01 后的 v35 | ✅ |
| 4 产物 md5 | ✅ 4cecdc939f9cca3edf31a236a06fb170 |
| 5 语法 | ✅ 0 报错 |
| 6 功能消失 | ✅ v34→v35 特征 179→185（+6 函数：12-01 三级 + 12-03 三个预取函数），消失 0 |
| 7 四语 | ✅ 无新增用户文案（只 console 日志 + 注释） |
| 8 真点击 | ✅ 12-03 stub 11/11；12-01 三级闸8 仍 14/14 |
| 9 视觉 | ✅ 颜色 0 新增，无新 CSS |
| 反向样本 | ✅ v34 上 `slice(0,40)`=1、`slice(0,10)`=0 → 12-03 判据在 v34 失败（有分辨力） |

## 改法说明
- `_wmPrefetchQueue()` = 只预取 `_wmPrefetchFirstGroupDocs()`（复用页面同款 `groupMaterialsByStudent`+`_rqApplyFilter`+`_rqCmp` 取排序后第一组，pending+done，≤10）。
- `_wmEnqueuePrefetch(docs)`：并发1/间隔300ms/`document.hidden`暂停 + 新增 `_wmPrefetchPaused` 暂停位。
- `openPreview`：`_wmFetch` 前置 `_wmPrefetchPaused=true`，另挂一条 `.then(成,败)` 在取完(成/败)都置回 `false`（不动原显示 `.then` 逻辑）。
- `_wmPrefetchGroup(studentCode)`：按需预取某组的接口（本端无收合 UI 暂未由界面触发；闸8 直接调它验队列增长）。

## 动过的文件
- 改 reviewer-portal/maxhouse-reviewer-portal-v35.html（预取块重写 + openPreview 暂停/恢复 + [VER] 更新）
- 新增 tests/job12-03.mjs（闸8）+ jobs/JOB-12-03/report.md
- index.html 已在 12-01 指向 v35（本包不改）
