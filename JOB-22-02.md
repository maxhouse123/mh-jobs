# JOB-22-02 回执 · 中介端 v170：卡片进弹窗 + 缩略图续签 + 「有新版请刷新」

CC 执行。只动中介端一个文件 `partner-portal/maxhouse-partner-portal-v169.html → v170`（+index.html 重定向 + changelog）。
未 push 主仓库（§0.9）。共用查看器 / 云函数 / 迁移一律未碰。

## 一、做了什么（三件，全在 v170）

### ① 卡片进弹窗（修 22-00③：1220/1280/1440 卡压弹窗右下角 3066/2436/756px²）
- 新 `_v170CardContainer()`：详情弹窗开着（`viewingStudentIdx!=null` 且 `.show`）时返回弹窗内置顶容器 `#_v170ModalCardHost`，否则返回 v169 的右下角栈 `#_v167CardStack`。`_v167Card` 的挂载点改经它派发（一行改动，× / 悬停暂停 / 时长逻辑一字未动）。
- 置顶容器 `#_v170ModalCardHost`：`position:sticky;top:0`，挂在 `#studentDetailBody`（弹窗滚动容器）**顶部第一个子节点**，**在内容流里占位、把下面内容顶下去**（不悬浮遮挡）；可叠多张。
- **重画不丢**：容器节点常驻（module 变量 `_v170ModalCardHost` 引用），`body.innerHTML=...` 重画后被摘出但不销毁；`openStudentDetailModal` 末尾与 `closeStudentDetailModal` 调 `_v170SyncModalCards()` 把容器搬回 firstChild（卡片随之回来）。
- **关窗移回**：关闭弹窗时 `_v170SyncModalCards()` 把未到期卡片搬回右下角 `#_v167CardStack`。无弹窗（登记向导等）维持 v169 右下角 / 手机底部。

### ② 缩略图续签（修 22-00④：3600s 一次签不续签 → 久开断图）
- `_hydrateCloudStudent` 两处签名点补记 `f.previewSignedAt = Date.now()`（批签 + 单签回退各一处，其余取数逻辑零改）。
- `openStudentDetailModal` 打开时 `_v170MaybeResign(idx)`：扫该生所有**已上传件（有 filePath 的）**，签发距今 **> 50 分钟**或无 previewUrl 的整批 `_v170SignFiles`（`createSignedUrls` 批签 + 单签回退，bucket=`student-documents`，3600s）续签后轻量重绘。`s._v170Resigning` 守卫 + 续签后不再 stale → **不成环**。本地暂存件（无 filePath）不碰。
- 列表缩略图 `<img>`（`_thumbInnerHtml` 唯一图片渲染点）加 `data-v170-path` + `onerror="_v170ThumbErr(this)"`：失败对该件**单件重签一次**换 src；`img.dataset.v170Retried` 守卫只重试一次；仍失败置**内联 SVG 文件占位**（`onerror=null` 防死循环），不再出浏览器断图图标。

### ③「有新版请刷新」横幅（22-03/22-04 五端将复用同一段代码）
- 载入 3s 后 `_mhCheckNewVersion`：`fetch('./index.html',{cache:'no-store'})` → 正则 `maxhouse[_-][a-z_-]*portal[_-]v(\d+)` 取**最大版本**，与当前版本（`location.pathname` 解析，回退常量 `MH_PORTAL_VER=170`）比。
- 线上更新 → 顶部固定横幅：**四语**（zh/en/ru/fr，读 `currentLang`）、点整条去根地址 `'./'`（index 再跳最新）、**绝不自动刷新**、× 关后本会话不再弹；显示时把 `body.paddingTop` + `.header top` 顶到横幅下方（**不遮页头按钮**）。
- 每 30 分钟 + `visibilitychange`（可见）复查；**取不到（非 ok）/ 解析不出（无版本）一律静默**。

## 二、闸3（真页面 partner1 真登，`jobs/JOB-22-02/probes/gate3.mjs`；日志 `repro/gate3-after.log`）

| 项 | 判据 | 结果 |
|---|---|---|
| A 卡片进弹窗 ×3 视口 | 1220/1280/1440：卡在 `#_v170ModalCardHost` 内且容器=body.firstChild；七项（提醒框/提交/确认行/缩略图/名单块/标题条/关闭钮）交集 **0px²**；卡在屏内；× 可点 | **GREEN×3**（22-00 三档全红 3066/2436/756px² → 0） |
| B 重画不丢 | 卡片存在→触发 `_v134RepaintReplace` 重画→卡片仍在（before=1 after=1 hostFirst=true） | **GREEN** |
| C 缩略图续签 | 16 件全部置 stale（signedAt −60min + previewUrl 清空）→ 开弹窗 `_v170MaybeResign` → signed 16/16、fresh 16/16；onerror 打断 → **resigned** | **GREEN**（22-00 断图红 → 绿） |
| D1 横幅出现 | 假 v999 → 横幅出，紫底 `rgb(124,58,237)`，top=0 | **GREEN** |
| D1 四语 | zh「有新版本，点此刷新」/ en / ru「Доступна…」/ fr「Une nouvelle version…」逐语正确 | **GREEN** |
| D2 版本一致不出 | index 指向 v170 → **无横幅** | **GREEN** |
| D3 取不到静默 | fetch abort → 无横幅、`pageErrors=0` | **GREEN** |
| D4 · N7 几何 | @1s / @4s 横幅可见、紫底、与页头按钮交集 **0px²**（`maxHdrOv=0`） | **GREEN** |

`OVERALL: ALL GREEN`。先红证据在 22-00 `repro/card-overlap-before.json`（三档红）与缩略图 404 红；横幅系新件（无 before）。

## 三、N 闸
- **N1**（`jobs/JOB-22-02/inventory-partner-v170.json` vs 22-00 baseline v169）：ids 移除 **0** / handlers 移除 **0** / functions 移除 **0**；新增 15 个函数全为本卡新增（`_v170*` 11 个 + `_mh*` 5 个中 4 个，`DIFF-REMOVED-TOTAL 0`）。零死按钮、零删旧功能。
- **N7**（弹窗内卡片三档 + 横幅第1/4秒）：见闸3 A/D4，交集 0、颜色紫、不遮页头按钮。
- **P3 随卡 jsdom/vm 断言**（`tests/job22-02-partner-v170.mjs`）：**27/27 ALL PASS**（含 md5 锚定、div 平衡、index 四处同版、四语齐、内嵌 script vm 可解析）。
- **四语逐键**：闸3 D1 四语 + P3 D3 四条文案实证。
- **shell 卫生**：本包新增 `.mjs` 无 shell；`jobs/JOB-20-04/shell-lint.sh` **全绿（0 命中）**。
- **测试写库**：全程零上传、零提交、零写库——仅内存态编辑（改 previewUrl/signedAt）+ 本地静态服务器 + fetch 路由桩；缩略图续签走真 `createSignedUrls`（只读签发，不改桶）。
- **secret-scan**：mh-jobs push 前扫 CLEAN。

## 四、截图（元素级 · 无 PII，调试期不推）
`~/mh-jobs/shots/22/22-02/`：`A-card-in-modal-top-1280.png`（卡进弹窗顶部）、`D-version-banner-zh.png`（紫色横幅）+ `INDEX.md`。真实姓名不入镜（只截卡片/横幅元素）。按 §0.6 调试期不 push，随 22-06 收官统一过 secret-scan 后推。

## 五、要业主亲眼看的（线上验收）
- 中介端弹窗内**顶部**卡片：护照核对提醒现在出现在弹窗内容顶部、把下文顶下去，不再压右下角「提交新文件 / 我已看到…」。
- 缩略图久开不断：标签页开 1 小时后重开学生详情，护照/学历/成绩单缩略图仍在（自动续签）。
- 顶部横幅：线上版本一致时**不出**；只有真出新版时才出、点它才刷新。

## 六、版本四件套
①文件名 v170 ②[VER] 探针 v170（+VER-prev169 保留）③头注= [VER] 行（本端无独立头注版本行）④index.html 重定向四处 v170。changelog 追加一行。

## 状态
- 无「必须停」触发。主仓库 `partner-portal/maxhouse-partner-portal-v170.html` + `partner-portal/index.html` + `docs/changelog/partner-portal-versions.md` 点名 add + commit，**未 push**（§0.9）。
- 本回执 + `jobs/JOB-22-02/` 探针/日志/inventory + `tests/job22-02-partner-v170.mjs` 一并入库；回执 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**：闸3 全绿、N1 零移除、P3 27/27、四语齐、shell-lint 绿、secret-scan 绿。视觉件线上肉眼验收留业主（§0.6/铁律15）。

下一包 = 22-03
