# JOB-24-06 回执 · 管理端 v231：学生 / 申请导出全字段 + 值翻译 + 修列表「来源」列

**一句话**：以前管理端导出的学生表只有 13 列、还缺「出生日期」「推荐码」这些要紧字段；申请表更离谱——「申请编号」印的是任务号（T-1）、阶段印的是英文（accepted）、时间是一长串机器时间戳。这一版把学生导出扩到 25 列全字段、申请导出扩到 18 列，英文码全翻成人话，申请号改成正确的 MH- 编号、阶段中文、时间统一成「年-月-日 时:分」。只改管理端一处（导出的列定义 + 两个云端装载器各加几列），不动库、不动其它端。

## 一、业主能看到的变化（学生页 / 申请页顶栏「导出」）

1. **学生导出：13 列 → 25 列全字段**（中英列名，打开就是当前语言）
   学号 · 申请号（MH-）· 姓名 · 性别（M/F → 男/女）· **出生日期** · 国籍 · 护照号 · 邮箱 · 电话 · 来源（自助注册 / 中介名）· 来路（自填 / 中介代填 / 委托自传）· **推荐码**（有推荐中介的显 PA-…）· 注册时间 · 最近活跃 · 当前阶段 · 目标学校 · 项目 · 学历层次 · 轮次 · 材料（已批/总）· offer 状态 · 通知书付款 · 增值服务 · 沉默天数 · **是否测试**。
   - 云端没有的字段（如「最近活跃」暂无这一列）一律留空，不编造。
   - 「材料」在云端没连上时显空、连上显「3/5」这样的已批/总数。

2. **申请导出：6 列 → 18 列，三个老毛病全修**
   - **申请号**：以前印的是任务号「T-1」——现在按学生反查出真正的对外申请号「MH-000042」。任务号单独放一列「任务号」，两者分开。
   - **阶段**：以前印英文「accepted」——现在中文「已录取 / 待处理 / 已拒绝」。
   - **时间**：以前是机器时间戳——现在统一成「2026-09-21 08:07」这种年月日时分（本地时区）。
   - 另外补全了：学校决定 · offer 发出 · 有效期 · 付款 · JW202 · 报到 · 中介 · 更新时间 等列。

3. **列表页「来源」列口径核实**：业主提到「来源列混进国家、学生名」。CC 亲查了当前 v230/v231 的取值函数 `_v31SourceCell` / `_v31StudentSource`——它从 v90（卡 ADM-2）起就已经只认云端真列 `referred_by_partner`：有值显中介名、没值显「自助注册」，**绝不回退到国家或学生名**。列表页和导出用的是同一个函数（同源）。所以这一条现状已经是对的，本版核实沿用、未改这个函数。无头实测两遍（中/英）来源列 6 个值全是「自助注册/Self」或中介名「Test Agency A」，无一个国家名、无一个学生名。

## 二、动了哪几处代码

- `_v220Columns('students')`：13 列改 25 列，新增值翻译 helper `_v231Gender / _v231Nationality / _v231RefCode / _v231OfferStatus / _v231LetterPay / _v231Materials / _v231Round / _v231StuLevel / _v231IsTest / _v231DT`（时间统一格式）。
- `_v220Columns('applications')`：6 列改 18 列，新增 `_v231AppNo`（反查该生 app_no → MH-）、`_v231AppStage`（阶段中文）、`_v231SchoolDecision`、`_v231AppUpdated`、`_v231AppStudent`、`_v231PartnerName`。
- `loadStudentsFromCloud`：select 补 `created_at`，映射补 `registeredAt`（供「注册时间」列）。
- `loadApplicationsFromCloud`：select 补 `letter_expires_at / jw202_uploaded_at / enrolled_at`（库内已有列），映射补对应字段（供「有效期 / JW202 / 报到」列）。缺列时按 §0.4 容错（值留空、不报错）。

## 三、守规矩情况

- **N1 零移除**：同一把尺子抽 v230 与 v231 对比，删除数 = **0**（id / onclick 句柄 / function 一个没丢）；新增 18 个 `_v231*` 函数。见 `jobs/JOB-24-06/n1-diff.json`。
  - 注：学生导出里去掉了「下一步」「申请日期」「学号/邮箱」「联系方式」四个旧列的**调用**（换成 25 列新布局），但这四个函数 `_v46NextActionBadge / _f3AppliedCell / _id1Pair` 的**定义仍在**（列表页仍在用），所以 N1 不计移除。断言里专门核了这两个函数定义仍在。
- **零写库、零迁移**（本包不碰库；两个装载器只是加宽 select，读的都是库里已有的列）。
- **零新样式**：导出是纯数据（无 DOM 样式）；列表「来源」列复用现成 `.badge`，没新增任何颜色 / 字号 / 样式类。
- **只动管理端**，没碰学生 / 学校 / 审核 / 中介端，没碰共用查看器、_headers、云函数。
- **版本四件套齐**：文件名 v231 / 头注 v231 / [VER] 探针 v231 / index.html 三处重定向 v231 / changelog 追一行。
- **secret-scan**：CLEAN（唯一命中是 v140 起就有的一句提到 service_role 的**注释**，不是密钥，与 24-05 同）。shell-lint：本包无 shell 脚本。

## 四、自验（三层全过）

- **静态对照（41 项全绿）**：版本四件套齐、16 个新函数定义都在、两个装载器 select 加宽到位、`registeredAt` 映射在、内嵌 script 可解析、changelog 追一行；N1 现有函数（`_v220Columns / _v31SourceCell / _v31StudentSource / _v46NextActionBadge / _f3AppliedCell`）一个没少。
- **无头真渲染（中文一遍、英文一遍，共 38 项全绿）**：
  - 学生导出表头 25 列 ✅，含「出生日期 / 推荐码 / 是否测试」三列 ✅，桩 6 名学生行数 6 ✅；
  - 来源列 6 值 ∈ {自助注册/Self, 中介名} ✅、无国家名 ✅、无学生名 ✅；
  - 出生日期列有值（2005-01-0x）✅、推荐码列 3 名被推荐学生显 PA- ✅、是否测试列全「否/No」✅、注册时间列匹配 `YYYY-MM-DD HH:mm` ✅；
  - 申请导出表头 18 列 ✅，申请号列全 MH- 开头 ✅（任务号列单独全 T- ✅）、阶段列无英文原码 ✅（显「已录取/待处理」或「Accepted/Pending」）、提交时间列匹配 `YYYY-MM-DD HH:mm` ✅；
  - 列表页含中介名与「自助注册/Self」✅；导出值无 undefined、无裸 i18n 键 ✅；全程零 pageerror ✅；
  - 每列函数零抛错 ✅。
  - 合计 **79 / 79 全绿**（脚本 `tests/job24-06-admin-v231.mjs`，随卡入仓）。
- **旧病 R5 先红后绿**：24-00 记的 R5 三条（学生导出缺出生日期/推荐码 · 申请号印任务号 T-1 · 阶段英文 accepted）本包全部转绿。

## 五、一处工装记账（不影响验收，但要让业主知道）

开工时发现本机（Mac mini · /Volumes/Dev/MAXHOUSE）的**无头自验壳 `jobs/JOB-16-00/lib/realpage.mjs` 与仓库 `node_modules/` 都不在工作区里**（推测某次清理被删；realpage.mjs 属本地不入 git 的自验依赖，但它曾经进过 git 历史）。CC 从 git 历史（commit 4cb6b58）里把 `realpage.mjs` + `env.mjs` 两个壳文件**只还原到工作区、未 git add**（保持「自验壳本地装、不入 git」的纪律）；playwright 本体仍在 `~/mh-verify/node_modules` 里（壳用 createRequire 指向那里），所以无头照跑通了。**这两个还原的文件不进本次 commit**。第二十五轮如换机，注意这套自验壳需本地就位才能跑闸8。

## 六、账务

- 主仓库：本包已 **commit（未 push）**，改动六件——
  - `admin-portal/maxhouse-admin-portal-v231.html`（新文件）
  - `admin-portal/index.html`（重定向 → v231）
  - `docs/changelog/admin-portal-versions.md`（追一行）
  - `tests/job24-06-admin-v231.mjs`（随卡断言）
  - `jobs/JOB-24-06/inventory/*.json` + `jobs/JOB-24-06/n1-diff.json`（N1 基线与差集）
  - `jobs/JOB-24-00/stub/fixtures.mjs`（桩加宽：students 补 created_at + 新增 partners 桩与路由，供来源/中介列显真名——纯加，24-05 无头仍绿）
- 本轮暂无「必须停」；`~/mh-verify/r24-STOP.txt` 不存在。

---
- 闸3 静态断言 ✅ / 闸8 无头中英各一遍零报错零裸键 ✅ / 闸9 视觉零新增 ✅ / N1 removed=0 ✅ / secret-scan CLEAN ✅
- N1 基线：`jobs/JOB-24-06/inventory/admin-v231.json`；差集：`jobs/JOB-24-06/n1-diff.json`
- 下一包 = 24-07
