# JOB-22-05 回执 · 走查脚本加两项（④ _headers Link 归属 + ⑤ 控制台预加载告警）

CC 执行。从 `jobs/JOB-21-05/online-walk.{mjs,sh}` 复制到 `jobs/JOB-22-05/`（旧文件一字不动），加两项检查。
本地对现役线上跑一遍如实记录。未 push 主仓库（§0.9）。

## 一、做了什么（新增两项，承 21-05 版本表 + schX/partner 深走查）
- **④ `_headers` Link 归属**（纯 fetch，无需登录）：对五端「根地址」`/<portal>/` 与「版本文件」`/<portal>/maxhouse*portal*v*` 各发一次，断言响应头 **`Link` 存在**；对同端 **`/<portal>/vendor/supabase.js`**（病根所在——旧 `/<portal>/*` 规则曾命中它）与站点根 **`/assets/fonts/manrope-400.woff2`** 各发一次，断言**无 `Link`**。admin/reviewer 的网页文档在 CF Access 后（302→cloudflareaccess.com）→ 记 **N/A**；字体在站点根不受 Access → 可判。
- **⑤ 控制台预加载告警**：五端真页面无头启动，断言控制台无 `preloaded using link preload` 告警；admin/reviewer 在 Access 后无头进不去 → **N/A**。

## 二、闸3（本地对现役线上跑一遍，`MH_WALK_NO_WAIT=1 MH_WALK_NO_PUSH=1`；全表见 `~/mh-jobs/JOB-22-05-online.md`）

### ④ Link 归属（**改前应有红，已如实记录**）
| 项 | 现役线上实际 | 判 |
|---|---|---|
| 字体 `assets/fonts/manrope-400.woff2` 无 Link | status 200，无 Link | **绿** |
| student/partner/school 根地址有 Link | status 200，有 Link（preconnect+supabase.js+字体） | **绿 ×3** |
| student/partner/school 版本文件有 Link | 有 Link（status 404——主仓库 v400/v170/v235 尚未部署，文件不存在；但 `/<portal>/maxhouse*portal*v*` 规则按路径匹配仍挂 Link，部署后该文件 200+Link） | **绿 ×3** |
| **student/partner/school `vendor/supabase.js` 无 Link** | **status 200，仍有 Link** ← **病根未除（22-01 未部署）** | **红 ×3（预期）** |
| admin/reviewer 根/版本/vendor | CF Access 挡（302→cloudflareaccess.com） | **N/A ×6** |

→ **④ 的红正是本轮要暴露/待修的信号**：现役线上 `/<portal>/*` 旧规则仍把 Link 挂到 `vendor/supabase.js`；**业主收官推送 22-01 后**，该三条应转绿（vendor 不再带 Link）。这就是 22-05 走查此后要长期把守的线上判据。

### ⑤ 控制台预加载告警
| 端 | 实际 | 判 |
|---|---|---|
| student/partner/school | 无头启动 **0 条** `preloaded using link preload` | 绿 ×3 |
| admin/reviewer | CF Access 后无头进不去 | N/A ×2 |
- **诚实说明**：⑤ 在无头本地即便对现役线上也测得 0 条，与 22-00 的定性一致——该 console 告警系 **CF 边缘 Early Hints(103) 早发 + 同目录子资源响应叠加**、**时机依赖、无头难稳定复现**；**可靠的红→绿判据是 ④ 的结构信号**（vendor 是否带 Link）。⑤ 保留作线上业主实测的辅助项（业主浏览器里看控制台）。

### 承 21-05（一并记录，均为主仓库领先线上的预期红）
- ① 版本表：student/partner/school **红**（线上 v399/v169/v234 落后主仓库 v400/v170/v235——本轮已提交未推，预期）；admin/reviewer **N/A**（CF Access）。
- ② schX 登中介端被拒（v167 判据）**绿**（locked+无会话+无机构内容+见拒绝文案）、0 报错。
- ③ partner1 走查：版本 console **红**（v170 未部署）；Q1 卡片 N/A（无现成场景，不主动触发写库）；**0 报错**。

`exit 0`（走查红不中断）。全程零写库（②③ 只登录观察，④ 纯 GET，⑤ 只加载页面）。

## 三、N 闸 / 卫生
- **不升版**（本包只加走查脚本，不改任何端 HTML）→ N1 N/A。
- **shell 卫生**：`online-walk.sh` 仅 shebang `#!/bin/bash`（标准，非正文叹号）；`jobs/JOB-20-04/shell-lint.sh` **全绿（0 命中）**；`online-walk.mjs` `node --check` 通过。
- **测试写库**：零写库（GET + 只读登录观察）；②③ 登录系现役线上真登只读（承 21-05 既有做法）。
- **secret-scan**：push 前扫 CLEAN；脚本公钥读环境变量、不打印任何 token/密码。

## 四、产物
- `jobs/JOB-22-05/online-walk.mjs`（+④⑤ 两函数 + main 接线；PKG/回执名/截图路径/自推消息更到 22-05；旧 21-05 文件未动）
- `jobs/JOB-22-05/online-walk.sh`（同步更名 22-05 + ④⑤ 说明）
- 走查全表回执 `~/mh-jobs/JOB-22-05-online.md`（脚本自写）；打码截图 `~/mh-jobs/shots/22/walk/`（schX 登录态样式打码，调试期不推）。

## 五、要业主亲眼看的（线上验收，收官推送 22-01 之后）
- 业主推送后重跑 `bash jobs/JOB-22-05/online-walk.sh`（关代理）：④ 三端 `vendor/supabase.js` 应从红转绿（不再带 Link）；⑤ 五端控制台应无黄色 `preloaded using link preload`（业主本人浏览器里核，含 admin/reviewer 登录后）。

## 状态
- 无「必须停」触发。主仓库 `jobs/JOB-22-05/online-walk.{mjs,sh}` 点名 add + commit，**未 push**（§0.9）。
- 本回执 + 走查自写回执 push 到 mh-jobs（截图调试期不推）。
- **PUSH_OK（本包自闸）= yes**：④⑤ 已接入并对现役线上如实跑出（④ 预期红已捕获、部署后转绿）、shell-lint 绿、node --check 过、secret-scan 绿。

下一包 = 22-06
