# JOB-8-04 · 首页两张卡（验收项 10、11）— 回执

**结果：9/9 全过。两张卡在真手机上都对了——「开启新一轮」卡 390px 下不再断词、不撑破；「YOU'RE ALL SET」录取卡显示的是正确的第 1 轮申请号 MH-6KG2JN（不是那个在审的 MH-9F4AG7）。第七轮 7-04 和 7-05 的修复，线上真机确认到位。**

## 大白话
- **项10（7-04 断词修复）**：把手机屏幕压到 390px 宽，看那张「开启新一轮 / Start a new round」卡。标题在一行没断成「Availabl-e」，右边「In progress · 25 day(s)…」药丸整块摆好，整页和卡都不横向溢出。
- **项11（7-05 申请号修复）**：那张绿色「YOU'RE ALL SET / 录取材料已就绪」卡，写的是「**MH-6KG2JN** is confirmed … · **Round 1**」——正是真正被录取的第 1 轮，**没有**误用当前在审的第 2 轮号 MH-9F4AG7。第七轮我在真机上抓出并修好的那个 bug，这次在手机线上页面再次确认修对了。

## 机器断言表（真机 v389）
| 项 | 断言 | 实测值 | 过? |
|---|---|---|---|
| — | 选中含已渲染卡片的 tab | v389 学生端页 | ✅ |
| 11 | 录取卡显 MH-6KG2JN | `YOU'RE ALL SET \| … \| MH-6KG2JN is confirmed … · Round 1 \| View documents` | ✅ |
| 11 | 录取卡标「Round 1 / 第1轮」 | `hasRound1=true` | ✅ |
| 11 | 录取卡**不含** MH-9F4AG7 | `hasMH9=false` | ✅ |
| 10 | 找到「开启新一轮」卡 | `Start a new round \| Available once the current round closes … \| In progress · 25 day(s) until it can be ended` | ✅ |
| 10 | 390px 页面无横向溢出 | `docScrollW=390`（≤390）| ✅ |
| 10 | 卡内无横向溢出 | `overflow=0` | ✅ |
| 10 | 标题一行 | `titleH=21 fs=14`（21 ≤ 14×1.6）| ✅ |
| 10 | 卡带「In progress」药丸 | 文案含 `In progress · 25 day(s)…` | ✅ |

## 版本核对
- 页面 URL = `maxhouse_student_portal_v389`；两张卡的行为（7-04/7-05 修复）也与 v389 一致 → 线上确为 v389。
- 备注：直接抓 console 里的 `[VER]` 行需要重载页面挂监听，为不打扰这个已登录/已渲染的 tab，未重载；以 URL + 行为双证版本。

## 备注
- stuA 当前第 2 轮在审、第 1 轮已录取，两张卡都在其中一个 tab 上真实渲染（手机 Chrome 开了多个学生端 tab，脚本自动挑"已渲染卡片"的那个）。
- 量完 390px 已 `clearDeviceMetricsOverride` 复原，未残留尺寸覆盖。

## 截图（本机）
- `8-04-1-新一轮卡390.png`、`8-04-2-录取卡申请号.png`

**只读包（量尺寸 + 读文本），量完清了设备尺寸覆盖；未写库、未改任何本地文件。回执推日志仓库；截图只在本机（含 stuA 资料）。**
