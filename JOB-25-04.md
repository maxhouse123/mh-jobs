# JOB-25-04 回执 · 人脸证件照的「取用通道」（迁移 0218 + 水印函数 v37，只写不部署）

做的人：CC（无人值守，新会话）。做完时间：2026-09-27。
一句话：这一包是**主题 B（证件照即传即显）的地基**——学生上传的证件照，以后学校端和管理端要能**不等审核就直接显示**。
这包只**造好通道、造好开关**，写成文件；**没有真的开到线上**（贴库、部署由业主收官后另用卡做）。前端把照片摆出来是下一包 25-05（学校端）和 25-06（管理端）的事。

---

## 一、为什么要做这包（大白话）

- 现在学校端/管理端想显示某个学生的头像，只能走一条老路（`get_candidate_documents`）。这条老路有两个门槛：
  1. 那个学生得**先被拉进某个招生任务的候选池**（要有任务号）；
  2. 那张照片得**先被审核员审核通过**。
- 结果就是：**没进任何任务的学生、照片刚传还没被审核的学生**，头像只能显示名字首字母（比如「A」），看不到脸。
- 业主的新规矩：**只要学生传了证件照，学校端和管理端就直接显示照片，人脸照片不用等审核**；别的材料（护照、成绩单等）仍然要等审核员审完才显示。
- 所以这包**新开一条专门取人脸照片的通道**，这条通道：不看审核状态、只认「证件照」这一种材料、并且守住双盲和权限（谁能看、谁不能看）。

## 二、这包造了两样东西

### 1）数据库里两个新函数（写进文件 `supabase/migrations/0218_student_photo.sql`，**还没贴进数据库**）

- **`get_student_photo`（取照片）**：给它一个学生，它**只**把这个学生**最新的一张证件照**的信息返回来（照片文件名、上传时间、内部编号）。
  - **不看审核状态**（这是这包的关键，跟老路相反）。
  - 只返回「证件照」这一种材料，别的材料一律不碰。
  - 已被删除的照片不返回；有多张时只给最新那张。
  - **谁能取**：管理员可以取任何学生的；学校账号**只能取「这所学校本来就看得到的学生」**的（下面第 2 点）；其他人取到的是空的。
- **`_mh_school_can_see_student`（这所学校能不能看到这个学生）**：这是从**现有**学校端搜索学生的规则里**原样搬出来**的一段判断——「学校得是在营学校 + 学生已提交 + 学生没被拉黑」。**只是搬出来复用，原来的搜索功能一个字没改、行为不变**。

### 2）水印云函数升级 v36 → v37（改了源码 `supabase/functions/watermark-doc/index.ts`，**没有部署**）

- 新增一个「**取头像小图**」的分支：前端请求时多带一个标记 `kind: 'avatar'`，函数就：
  - **验身份**：管理员放行；学校账号要先过上面那个「能不能看到这个学生」的判断才放行；且**只对证件照生效**（拿别的材料想走这条会被挡）。
  - **出图**：把照片缩到**最长边 160 像素的小图**、**不打水印**（脸那么小打字也看不清、没意义），原件绝不出网（还是函数在服务器里现场重新编码后才吐出小图）。
  - **记账**：学校调用时在访问日志里记一笔 `avatar`；管理员不记（跟现在的规矩一致）。
- **最重要的安全点（零回归）**：**不带 `kind` 的老客户端永远进不了这条新分支**，水印函数原来那套（审核员看材料、学生看自己材料、学校看解锁原件……）**一个字没动、行为跟 v36 完全一样**。这条我用脚本逐条验过了。

## 三、自验（都过了）

- **数据库探针**（`bash jobs/db-run.sh jobs/JOB-25-04/probe-0218.sql`，连真库跑一遍、末尾自动回滚、**没往库里留任何东西**）：
  - 两个函数建得出来、返回的东西类型对（照片编号是文字型 `text`，跟真实数据一致）；✅
  - 造一个假学生 + 两张证件照（都没审核）+ 一张已删的 + 一张护照：取出来的**正好是最新那张证件照**，**证明「没审核也能取」**；✅
  - 已删的照片取不到；✅
  - 学生「已提交没拉黑」→ 判定可见；被拉黑 → 判定不可见；陌生学校账号 → 判定看不到。✅
  - 跑完 `ROLLBACK`，库里干干净净。
- **静态断言脚本**（`node tests/job25-04-ef-avatar.mjs`）：**38 条全过 / 0 败**。既查了新分支每处改动都在，也逐条查了「老路一个字没动」。
- **密钥扫描**：新增/改动的四个文件里没有任何密钥、密码、连接串（密钥都是函数运行时从环境变量取，没写死进代码）。✅

> 说明：数据库函数的「端到端」验（管理员真取到、学校成员真取到、陌生人真取空）需要真实登录态，本地 psql 里做不出来（跟上一包 0217 同样情况），**留业主贴库后用真实测试账号验一次**，验法见第五节。

## 四、图纸与现实的一处对不上（已按现实做，请业主知悉）

- 25-04 卡里写 `get_student_photo` 返回的照片编号是 `doc_id uuid`（一种编号格式），但**数据库里这一列的真实格式是文字 `text`**（比如 `DOC-2026-001`），而且姊妹函数（老路 `get_candidate_documents`）返回的也是 `text`。
- `uuid` 是卡里写错了：真返回 uuid 得去伪造/强转，根本行不通。CC 照**真实列 + 姊妹函数**做成了 `text`。这不是「凭空造列」（列本来就有、就是这个类型），只是把卡里的笔误纠回了现实。**这条没有触发「必须停」，已在此醒目标注。**

## 五、给业主的下一步（收官后做，不是现在）

1. **贴数据库**：把 `supabase/migrations/0218_student_photo.sql` 全文贴进 Supabase 的 SQL Editor 跑一次（全文会附在 SUMMARY-25 附录里）。跑完可用文件末尾「自检」那两段 select 核一下：两个函数在、`get_student_photo` 返回里有 `doc_id text`。
2. **部署水印函数**：`supabase functions deploy watermark-doc`，预期版本上到 **v37**（部署后函数日志里那行时间戳标签会从 `[wm v36 timing]` 变成 `[wm v37 timing]`）。
3. 贴库 + 部署后，效果要等 **25-05（学校端 v237）/ 25-06（管理端 v234）** 前端接上才看得到——那时学生传过证件照的一律显示照片（含刚传没审核的），没传的才显示首字母。
4. **端到端验一次**（贴库后）：用测试用的学校账号打开一个「传过证件照、但没进任何任务、也没被审核」的学生 → 应能显示照片；用陌生/无关账号 → 取不到。

## 六、动了哪些文件（这包主仓库一笔提交，未推）

- 改：`supabase/functions/watermark-doc/index.ts`（v36→v37，+139 行/-1 行；那 1 行删除=时间戳标签升 v37）
- 新：`supabase/migrations/0218_student_photo.sql`（迁移，未贴库）
- 新：`jobs/JOB-25-04/probe-0218.sql`（begin/rollback 探针）
- 新：`tests/job25-04-ef-avatar.mjs`（随卡静态断言，38 过）

边界守住：没部署、没贴库、没改其它材料的可见规则、没动 `search_students_global` 行为、没动任何前端、没删任何东西。

**下一包 = 25-05**（学校端 v237：把人脸照片真的摆到列表/搜索/候选卡/档案头像上）。
