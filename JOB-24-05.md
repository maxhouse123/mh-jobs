# JOB-24-05 回执 · 管理端 v230：学校行为日志升级

**一句话**：学校行为日志这一页，取件记录以前只显示一串账号编号（DB568FF7 这种）、看不出取的是什么材料、690 条重复取件把合规记录淹了；这一版把它改成人话——显校名、显材料类型和文件名、重复取件合成一行、能按天数筛、能按学校汇总、能导出。只改管理端一页，不动库、不动其它端。

## 一、业主能看到的六处变化（学校行为日志页）

1. **取件行的「学校」列变人话**
   - 以前：显示一串账号编号 `DB568FF7`。
   - 现在：先按校名册查真名（如「测试大学乙」）；查不到就显它的溯源码（如 `SC-DB92`）；再不行才回落编号前 8 位。桩数据下已经不再出现裸账号编号。

2. **取件行显示「材料类型 + 文件名」**
   - 以前：只知道有人取了件，不知道取的是什么。
   - 现在：每条取件下面多一行「📄 护照 · 护照扫描件.jpg」这样的说明。数据来自首屏就装好的材料档（护照/成绩单/学历…这些中文槽名），装载器反查材料时也顺手把类型和文件名存进缓存兜底。

3. **重复取件合并成一行**（新筹码「合并重复」，默认按下）
   - 以前：同一所学校在几十分钟里反复取同一份材料，690 条流水一条条铺开，把真正的合规记录（导出/下载/查看）淹没了。
   - 现在：同一学校 + 同一份材料、60 分钟内的取件合成一行，显示「×N · 首次…末次…」，点开能看到每一条的时间。桩里那一簇 25 次连续取件，合并后变成两行（护照 ×13、成绩单 ×12，因为交替取了两份材料）。关掉这个筹码就恢复一条条显示。
   - 桩实测：不合并 = 60 行；合并 = 37 行。

4. **顶上能按天数筛 + 按学校汇总**
   - 新增日期筹码「7 天 / 30 天 / 90 天 / 全部」，默认看 30 天，每个筹码带条数。
   - 新增一条「按校汇总」条：每所学校「导出 x · 下载 x · 查看 x · 取件 x」，点校名即按该校筛（沿用原有点校名筛选）。

5. **顶栏「导出」在本页导出当前筛选后的日志**
   - 9 列：时间 · 学校 · 动作 · 学生 · 材料类型 · 文件名 · 结果 · 操作人 · 详情摘要。导出的行数 = 页面当前显示的行数（合并了就导合并后的）。

6. **详情变人话**
   - 每条下面的摘要用人话小标签显示「材料类型 / 文件 / 结果（成功）/ 原因 / 备注」；原始那坨 JSON 折进「原始内容」里，不点不显。

## 二、守规矩情况

- **N1 零移除**：同一把尺子现抽 v229 与 v230 对比，删除数 = 0（一个 id / 按钮句柄 / 函数都没丢）；新增全是 `_v230*`（2 个按钮句柄 + 13 个函数）。见 `jobs/JOB-24-05/n1-diff.json`。
- **零迁移、零数据库改动**（这一包不碰库）。
- **零新样式**：日期/合并/汇总全复用现成的 `.filter-chip .filter-bar .queue-row .badge`，没新增任何颜色 / 字号 / 样式类。
- **只动管理端一页**，没碰学生 / 学校 / 审核 / 中介端，没碰共用查看器、_headers、云函数。
- **版本四件套齐**：文件名 v230 / 头注 v230 / [VER] 探针 v230 / index.html 三处重定向 v230 / changelog 追一行。
- **secret-scan**：CLEAN（唯一命中是 v140 起就有的一句提到 service_role 的注释，不是密钥）。shell-lint：本包无 shell 脚本。

## 三、自验（三层全过）

- **静态对照 24 项全绿**：版本四件套齐、9 个新函数 + 2 个新按钮句柄都在、反查 select 已加宽 doc_type/file_name、导出分支已加、内嵌脚本能解析、changelog 追了一行；N1 现有函数（renderSchoolAccessLog / _v192SetLogFilter / _v192SetLogSchool / _v223LogMatch / loadSchoolActionLog / _v192FillWhoViewed）一个没少。
- **无头真渲染（中文一遍、英文一遍，共 36 项全绿）**：
  - 取件行显校名「测试大学乙 / Test University B」✅、不再出现裸账号编号 `DB568FF7` ✅、查不到的回落溯源码 `SC-DB92` ✅；
  - 取件行含材料类型「护照 / Passport」与文件名「护照扫描件.jpg」✅；
  - 合并开 = 37 行、行内含 ×13 与 ×12 ✅；合并关 = 60 行 ✅；合并开 < 合并关 ✅；
  - 7 天日期筹码计数正确 ✅；
  - 导出 9 列 ✅、导出行数 = 显示行数 ✅；
  - 详情摘要含人话「材料类型 / Material type」✅、页面无未转义的原始 JSON 键 ✅；
  - 渲染区零 undefined、零裸 i18n 键、全程零报错 ✅。
  - 合计 **60 / 60 全绿**（脚本 `tests/job24-05-admin-v230.mjs`，随卡入仓）。
- **旧病 R4 先红后绿**：24-00 记的 R4 三条（学校列裸编号 / 无材料类型 / 60 行不合并）本包全部转绿。

## 四、一处与图纸的口径说明（不影响验收）

- 24-05「改哪里」写的是「同 school + **同 doc** 60 分钟内合成一行」，闸③举例写「×25」。桩里那 25 次取件是**交替取两份材料**（护照 + 成绩单）故意这么造的，按「同校 + 同材料」这条明文规则合并，只能是护照 ×13 + 成绩单 ×12，凑不出单行 ×25。CC 照「改哪里」的明文规则（同校同材料）实现，断言按桩真实算出的 ×13 / ×12 与合并后 37 行写死。这不是必须停的四种情形（§0.11），也在「CC 按桩算出并写死在断言里」的授权范围内，特此记明。

## 五、账务

- 主仓库：本包已 **commit（未 push）**，改动仅四件——
  - `admin-portal/maxhouse-admin-portal-v230.html`（新文件）
  - `admin-portal/index.html`（重定向 → v230）
  - `docs/changelog/admin-portal-versions.md`（追一行）
  - `tests/job24-05-admin-v230.mjs`（随卡断言）
- 本轮暂无「必须停」；`~/mh-verify/r24-STOP.txt` 不存在。

---
- 闸3 静态断言 ✅ / 闸8 无头中英各一遍零报错零裸键 ✅ / 闸9 视觉零新增 ✅ / N1 removed=0 ✅ / secret-scan CLEAN ✅
- N1 基线：`jobs/JOB-24-05/inventory/admin-v230.json`；差集：`jobs/JOB-24-05/n1-diff.json`
- 下一包 = 24-06
