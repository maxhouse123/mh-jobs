# JOB-7-08 · 分页最小自建（6-05 遗留）— 回执

**结果：做完，管理端 v209、中介端 v154。走查 5/5 过。零新 CSS 类（闸 9 达标）。**

## 大白话
第六轮 6-05 的分页当时停了——因为「全端没有现成分页组件可照抄」。这轮你授权「用现成按钮样式最小自建」，做好了：
1. **一个共用小函数 `_v209Pager(总数, 当前页, 每页, 翻页回调)`**：渲染「‹ 上一页 · 第 N / M 页 · 下一页 ›」，**只用现成的 `btn btn-ghost btn-sm` 按钮类 + 内联样式，不新造任何 CSS 类**。自带四语（中/英/俄/法）。只有一页时自动不显示。
2. **接到两处**：
   - **管理端「委托邀请管理台」的「已发邀请」列表**：原来一次最多显 100 条（`slice(0,100)`），改成**每页 50 条**分页（`_dlgInvPage` 记当前页，`_dlgInvSetPage` 翻页重渲染）。
   - **中介端「已发邀请」列表**：原来只加载 50 条封顶，改成**加载 500 条 + 前端每页 50 条**分页（`_dlState.invPage` + `window._dlInvSetPage`）。超过 50 条才出现分页器。

## 十道闸
- **闸 9（新增 CSS 类 = 0）**：`_v209Pager` 只用现成 `btn btn-ghost btn-sm` + `cell-meta` + 内联 style，**没往样式表加任何 `.类名{}` 规则**（diff 核对确认）。✅
- **闸 3/8 走查** `tests/job7-08.mjs` **5/5**：
  - 管理端：多页时显「Page 1 / 3」+ prev/next（btn-ghost）✅；单页时分页器返回空（不显）✅；中间页 prev→0、next→2 ✅；四语（法 Précédent/Suivant、俄 Назад、中 上一页/第1/3页）✅。
  - 中介端：`window._dlInvSetPage` 存在且可调用不抛 ✅（其 `_v209Pager` 与管理端同一份代码、封装在委托模块闭包内，逻辑经管理端同款验证）。
- 两端 JS 解析干净。

## 说明
- 中介端为了让分页「有意义」，把「已发邀请」的加载上限从 50 提到 500（否则永远只有 1 页、分页器不出现）；仍每页 50 显示。管理端委托邀请后端本就加载到 200，前端每页 50。
- 分页器是纯前端切片（数据已在内存），翻页不重新查库，快。

## 动过的文件
- 改：`admin-portal/maxhouse-admin-portal-v209.html` + `admin-portal/index.html`；`partner-portal/maxhouse-partner-portal-v154.html` + `partner-portal/index.html`
- 新增：`tests/job7-08.mjs`

**版本**：管理端 v208→v209、中介端 v153→v154。**主仓库未推。本回执已推日志仓库。**
