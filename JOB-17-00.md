# JOB-17-00 · 开工检查 + 两内核复验 + 探针 + 旧病复现（先红）+ 旧版按钮清单基线

> 本包 CC 做诊断，不改任何端口文件、不升版、不 commit 主仓库。产物在 `jobs/JOB-17-00/`。

## 一、做了什么
1. 核对 §0.2 基线（全部相符）：五端 md5 + assets/mh-viewer-v2.js md5 + 三只云函数源码 md5 + 迁移到 0208 + HEAD==origin/main==e6a9562 + 工作树干净。`git rev-parse HEAD` 落 `jobs/JOB-17-00/base-head.txt`。
2. 解包 kit2（24 文件，`KIT2-UNPACK-OK`，viewer md5 `ee351485…` 相符）。跑两内核 PDF/图片自测四遍。
3. 账号闸（§0.9②）：`acct-check.mjs` → stuA/partner1/reviewer1 三个真登全 OK。
4. 只读探针 ①–⑦（带身份 N3；`jobs/JOB-17-00/probes/`）。
5. 旧病复现（先红）。
6. 五端静态按钮清单基线（`jobs/JOB-17-00/inventory/*.static.json`）。

## 二、结果是什么

### 基线核对（§0.2）— 全相符
- admin v215 `baf5e020…` / student v396 `ad9c867f…` / partner v163 `71cb4032…` / school v233 `fb4e4052…` / reviewer v40 `97251499…`（全部 = QUEUE 期望值）。
- `assets/mh-viewer-v2.js` `5f84a4ee…`；precheck-ai `1c4ce932…`；watermark-doc `55debf4d…`；admin-doc `ac5f227a…`（全相符）。
- 迁移 tail = 0208；HEAD==origin/main==`e6a9562`；`git status --porcelain --untracked-files=no` 空。

### 两内核自测（§0.9③④）
- Chromium PDF：`CLAUDE-PDF-JUDGE ALL-AS-EXPECTED (chromium, MHViewer2 2.1.1, shard 0/1, ran 34)`
- WebKit PDF：`CLAUDE-PDF-JUDGE ALL-AS-EXPECTED (webkit, MHViewer2 2.1.1, shard 0/1, ran 34)`（MISMATCH=0；skip 主要 J17，另 J09/J08 各一处，均为 skip 非 mismatch，判官仍 want==got）
- Chromium 图片：`SELFTEST MHV2-ALL-AS-EXPECTED (chromium)`
- WebKit 图片：`SELFTEST MHV2-ALL-AS-EXPECTED (webkit)`
- §0.9③④ 全过（无 MISMATCH）。

### 探针 ①–⑦
**① reviewer_access_log（G①/0211 落点）**：列 id/reviewer_uid/rv_code/doc_id/file_kind/outcome/accessed_at。
outcome CHECK 现收 8 值：`served, denied_not_assigned, prepared, served_cached, served_original, served_video_url, served_cached_direct, served_video_signed`。
watermark-doc v33 会写 7 值：denied_not_assigned、denied_not_candidate、denied_not_owner、denied_revoked、prepared、served_cached、served_video_signed。
**差集（函数会写、约束不收 → 一直被静默拒的 INSERT）= `denied_not_candidate`、`denied_not_owner`、`denied_revoked`（三个）**。近 7 天计数：prepared 194 / served_cached_direct 193 / served 125 / served_video_signed 5，**dendenied_* 一行都没有**（正因被约束拒掉）。→ 0211 只加这三个值。

**② admin_access_log（0210 落点）**：列 id/admin_uid/doc_id/file_kind/outcome/accessed_at。
outcome CHECK 现收 2 值：`served_unwatermarked, denied_not_admin`。近 7 天计数：served_unwatermarked 8。→ 0210 需补 `served_direct`（且我的 t6 探针又写了 4 行 denied_not_admin，见 ⑥）。

**③ storage.objects 策略（student-documents 桶）**：SELECT 类只有两条命中该桶——`Students read own files`（bucket=student-documents AND foldername[1]=auth.uid()，学生读自己）与 `partner_student_docs_select`（partner_owns_student(foldername[1])，中介读名下学生）。**没有任何 admin/reviewer 直读 student-documents 的 SELECT 策略** → 管理员现在无法直读该桶，reviewer 走 watermark-doc EF。→ 0210 建 `admin_read_student_documents`（is_admin 直读）为新增策略。

**④ set_doc_slim（G②/0212 落点）**：签名 `(p_doc_id text, p_status text, p_orig_kb int, p_new_kb int, p_pages int)`，SECURITY DEFINER。归属判断 = `auth.uid()=v_owner OR auth.uid()=v_partner OR is_admin(auth.uid())`（v_partner=students.referred_by_partner，coalesce 收 false 防三值逻辑）。**未认 student_delegates**。其余：状态白名单 `slimmed/kept_not70/kept_pages/kept_error`、already_set、15 分钟窗口。
`student_delegates` 列：id/student_id/delegate_uid/role/granted_by/active/created_at/revoked_at。partner1 当前对 stuA **无** active 委托（0212 闸3 会在事务里临时加一条再回滚）。→ 0212 加谓词：`exists(select 1 from student_delegates where student_id=v_student and delegate_uid=auth.uid() and active and revoked_at is null)`，签名不变。

**⑤ precheck-ai（E/G③ 落点）**：
- `buildPrompt(docType)` **不带 lang**，提示词里硬写 `issues里每个问题用一句中文描述` + 样例 `["中文简短问题描述"]` → 模型被要求写中文（E 的病根）。
- 按需路 runOndemand：缓存来自 ai_precheck_shadow（keyed doc_id），命中条件 `hit && verdict!=='error'`，**不比 lang**；upsert onConflict `doc_id,model`。
- 草稿路 runDraft：缓存 ai_precheck_draft（keyed path），命中同样**不比 lang**；upsert onConflict `path,model`（写 lang 列）。
- 限频（按需路）：`ai_precheck_shadow.student_id==uid` 近 1h ≥30 → 429 → **现按材料属主（student_id）计，不是触发人**（G③ 要改成按触发人）。
- 唯一性：shadow 唯一 `(doc_id, model)`（doc_id 单独不唯一）；draft 唯一 `(path, model)`。lang 均不在唯一键内 → 换语言重判会命中同一唯一键、必须 upsert 覆盖，且要顺带更新时间列（否则 17-05 限频可被来回切语言绕过）。
- 两表 lang 分布：shadow en42/null34/zh5/fr1/ru1；draft null23/en6/zh6。

**⑥ 取原件耗时拆分（B 病根，t6-timing.mjs）**：
| 路径 | 冷 | 热1 | 热2 | 热3 | 状态 |
|---|---|---|---|---|---|
| admin-doc EF（reviewer1 JWT，非管理员）| 982ms | 429ms | 489ms | 373ms | 403 not_an_admin |
| stuA 直读自己 238KB 图片（storage download）| 876ms | 644ms | 173ms | 178ms | 200 |
| watermark-doc EF（reviewer1，assigned 246KB）| 6240ms | 4212ms | 4302ms | 3985ms | 403 doc_not_assigned_to_you |

→ **EF 路 = 秒级（admin-doc 冷~1s；watermark-doc 4–6s，连拒绝都要 4s）；直读存储 = 热 ~180ms**。B 的病根＝管理端/审核端开原件全走重云函数，直读存储快一个数量级。（此探针给 admin-doc 写了 4 行 denied_not_admin 审计，如②所记；watermark-doc 对该 assigned 件回 doc_not_assigned_to_you＝其归属校验比 reviewer_assignments 行更严，与 B 无关，记录备查。）

**⑦ 审核端 v40 预取（reviewer 源码）**：并行数 = **3**（`parallel=3`，峰值≤3，17-02 不加大）；预取范围 `_wmPrefetchQueue`→`_wmPrefetchFirstGroupDocs`＝当前组+下一组，登录后 1500ms 起；悬停 `_wmPrefetch(docId)` 立即 `_wmFetch`（跳过非可视件）；查看中/标签页隐藏暂停（`_wmPrefetchPaused`）。学生卡默认全展开、无收合开关（v40 第 2725 行注释）→ 17-02 落「看到哪组预取哪组」（IntersectionObserver）。真页面悬停→点开间隔计时（reviewer1 真登，`repro/TIMING7-hover-prefetch.mjs`）：`hover0.3=2739ms hover1=245ms hover3=249ms`（复跑 `hover0.3=2331 / hover1=669 / hover3=248`）——悬停引导越久开得越快，证明 `_wmPrefetch` 悬停即取确实生效，且组内命中缓存后点开 ~0.25s。

### 旧病复现（先红，N2）
- **E**（网络层，`e2.mjs`）：stuA 真登对名下已缓存中文的 8bc0486b（passport，只读缓存不上传不占批次）按需预检 `lang=en` → 返回 `["未展示护照页完整四边，页面边缘被裁切"]`，`ISSUES-CONTAIN-CJK=true` = **RED**（只调 1 次；缓存不比 lang + 提示词硬写中文双重成因）。90697e01 的缓存恰为英文（模型未遵/受图内容影响），在它上未复现——权威先红后绿在 17-05 gate3 的 N4 表。
- **A（PDF 无工具条）**（`repro/A-pdf-no-toolbar.mjs`）：reviewer v40 reviewer1 真登，点开一份 doc 预览、字节 route 成 kit2/pdf/c3.pdf → `judgePdf` `res.pass===false failed=J00`，`MHViewer2.version="2.0.0"`（旧 asset 非 2.1.1），PDF 走裸 pdf.js canvas 栈「已渲染 3 页」无工具条 = **RED**。school v233（`openCloudDocPreview` ~L22845）与 admin v215（`_v162AdmDocRender` iframe ~L6692）经源码确认同为 `<iframe>` 无工具条（同一结构缺口），但无「不写库就能开预览」的路 → **SKIP**（三端权威先红后绿在 17-01 gate3）。截图 `A-reviewer-v40-pdf-RED.png`。
- **B（点查看仍发新下载）**（`repro/B-click-refetch.mjs`）：reviewer1 真登，滚到第 3 个学生组点首个「查看」→ 点击后 3s 内发起 **2** 个新的取件请求（+374ms/+829ms），msToRender=567 = **RED**；对照：已预取组（0/1 组）点开 0 新请求、286ms = GREEN。病根＝预取只覆盖 `_wmPrefetchFirstGroupDocs` 的组 0+1。截图 `B-…-RED.png`/`B-…-GREEN.png`。
- **C（Copy link 无反应）**（`repro/C-copy-feedback.mjs`）：partner1 真登「已发邀请」（10 行、7 个复制链接钮）：①给剪贴板权限点复制→`writeText` 确有调用（内容写进剪贴板），但按钮字始终「复制链接」不变、无 toast、1.5s 内屏幕零可见反馈 = **RED**；②拒绝剪贴板→同样零可见 = **RED**。病根＝`_dlCopy` 复制成功但无任何可见反馈。截图 `C-…-GRANTED.png`/`C-…-DENIED.png`（名字邮箱全 mask）。
- **D（护照姓名硬失败）**（`repro/D-mrz-mismatch.mjs`）：本地合成图无 ICAO 机读码、OCR 读不出 → 不一致分支本地触发不了 → **UNRECONFIRMED（N2）**，权威先红后绿在 17-04 gate3。附色调勘查：学生 v396 现为琥珀 `form-toast variant:warning`（16-04 已软化措辞）；中介 v163 仍经 `showFormWarning`→`showToast(...,'error')`＝`.toast.error` **红色调**——两端不对称（中介侧更需 17-04 改软）。截图 `D-student-…`/`D-partner-…-mismatch-tone.png`。
- **F①/F②**：需真上传（占批次、动 §0.11 闸门），诊断包不做写操作；权威先红后绿在 17-04 gate3。源码落点已定：F① 学生/中介原因框开着期间慢网计时不暂停（17-04 改）；F② 瘦身行内存兜底按文件名匹配（学生 `_v393SlimInfo` v396 L8412、中介 `_v159SlimInfo` v163 L11244）→ 17-04 改按 docId/临时编号。

### 静态按钮清单基线（N1）
`jobs/JOB-17-00/inventory/`：admin-v215（ids199/handlers172/fns797）、student-v396（368/188/1115）、partner-v163（205/120/738）、school-v233（170/225/856）、reviewer-v40（74/31/151）。各升版包在 commit 前用 `inv.mjs diff` 出差集（N1）。动态清单随各升版包在真页面上抓（登录后首页+一级页），与静态一并比。

## 三、业主要做什么
- 本包无需业主操作（纯诊断）。

## 四、新闸证据（N1–N6）
- **N1**：五端静态基线已存盘（上）；差集闸在各升版包 commit 前跑。
- **N2**：E 已真登真调先红（RED）；A/B/C/D/F 见上；未复现项按 N2 标注。
- **N3**：所有按登录人过滤的查询都带身份或用 db-run 超级用户读聚合；t6/e2 用 restLogin 真登拿 JWT 打 REST/EF。
- **N4**：探针⑤⑥列出 precheck/admin-doc/watermark-doc 各路现状；改动包各自补全鉴权表。
- **N5**：secret-scan 见推送前记录；probes 脚本只经 env.mjs 读 accounts.json，不打印密码/token。
- **N6**：只诊断不加活；发现的新观察（259f456a 仍 live、watermark-doc 归属校验比 assignment 行严）记入建议，不顺手改。
