# JOB-18-04 · precheck-ai v24 —— 英/俄/法提醒说人话（改源码不部署）✅ 完成

> 卡1143 定案解锁了上一趟的停工：材料名表现成，在学生端 v397 同目录的语言包里。本趟按定案照单施工，产出 v24 源码（**不部署**）、材料名表、部署脚本、本地四语提示词核对、N4 矩阵。

## 〇、起点核对（§0.3 / §0.9）
- `git rev-parse HEAD` = `241d42767ef2b1a48d685de3a7c8abab42e2af7c`（= 18-03 提交），`git status --porcelain --untracked-files=no` 仅 `M supabase/functions/precheck-ai/index.ts`（本包唯一改动）。
- precheck-ai v23 基线 md5 = `654fcbb5e565ea9a6dda0c68179d476b`（与 SUMMARY-17 一致）✓。
- `~/mh-verify/accounts.json` 在（717 字节，权限 600，脚本只读不打印）。
- 本包不需要真登（无 UI、无写库、无上传）；stuA/lang=en 的线上探针留在 `deploy-precheck.sh` 与 18-06 线上走查。

## 一、材料名表怎么来的（不手抄，脚本原样读出）
定案指明：**en** 用 v397 主文件 `I18N.en` 的 `doc.<码>`；**ru/fr** 用同目录语言包 `maxhouse_student_i18n_v360_ru.js` / `..._fr.js` 的 `doc.<码>`；主文件若有「补充词典」`I18N._sup` 对同一 `doc.<码>` 另给 ru/fr，以补充词典为准（学生实际看到的）。

- 实证 v397 的查找函数（L8489，逐字）：`function t(key){ return (I18N._sup && I18N._sup[currentLang] && I18N._sup[currentLang][key]) || (I18N[currentLang]&&I18N[currentLang][key]) || I18N.en[key] || key; }` —— 顺序 `_sup[lang] → I18N[lang] → I18N.en → key`。
- 脚本 `jobs/JOB-18-04/gen-doc-names.mjs`：在受控 vm 里把 v397 的 `const I18N`（含 en）区段 + `I18N._sup` 区段 + ru/fr 语言包**逐字拼成一段**执行，再用与 v397 **逐字相同**的 `t()` 顺序，对 22 个 precheck 码各取 en/ru/fr。**缺一个码或回退到键本身即 STOP**（本次 22×3=66 项全部真取到，无回退）。
- 复核：脚本自带的 t() 定义前置自检（与 L8489 字符串不符即 exit 3）通过；`_sup` 对这 22 个码**无覆盖**（22 码的 doc.* 只在主 I18N.en 与 ru/fr 包里），故 en 走主文件、ru/fr 走包，与定案一致。
- 生成物 `jobs/JOB-18-04/doc-names.json`（下方全文）。

### doc-names.json（可读排版；仓库文件字符逐字等价）
```json
{
  "table": {
    "passport":       { "en": "Passport", "ru": "Паспорт", "fr": "Passeport" },
    "visa":           { "en": "Visa / residence permit", "ru": "Виза / вид на жительство", "fr": "Visa / titre de séjour" },
    "education":      { "en": "Highest diploma", "ru": "Диплом о наивысшем образовании", "fr": "Diplôme le plus élevé" },
    "transcript":     { "en": "Academic transcript", "ru": "Академическая выписка (транскрипт)", "fr": "Relevé de notes" },
    "translation":    { "en": "English translation", "ru": "Перевод на английский", "fr": "Traduction en anglais" },
    "criminal":       { "en": "Police clearance certificate", "ru": "Справка об отсутствии судимости", "fr": "Extrait de casier judiciaire" },
    "bank":           { "en": "Bank statement / deposit certificate", "ru": "Банковская выписка / справка о депозите", "fr": "Relevé bancaire / attestation de dépôt" },
    "recommendation": { "en": "Recommendation letter", "ru": "Рекомендательное письмо", "fr": "Lettre de recommandation" },
    "video":          { "en": "Self-introduction video", "ru": "Видео-самопрезентация", "fr": "Vidéo de présentation" },
    "health":         { "en": "Medical examination certificate", "ru": "Медицинская справка", "fr": "Certificat médical" },
    "cv":             { "en": "CV / Résumé", "ru": "Резюме (CV)", "fr": "CV" },
    "english":        { "en": "English proficiency certificate", "ru": "Сертификат о знании английского языка", "fr": "Certificat d'anglais" },
    "conduct":        { "en": "In-school performance record", "ru": "Характеристика из учебного заведения", "fr": "Attestation de conduite scolaire" },
    "honors":         { "en": "Honors / Awards certificate", "ru": "Дипломы и награды", "fr": "Certificats de distinctions / prix" },
    "attendance":     { "en": "Previous-school attendance record", "ru": "Справка о посещаемости из предыдущего учебного заведения", "fr": "Attestation d'assiduité de l'établissement précédent" },
    "photo":          { "en": "Personal photo", "ru": "Личное фото", "fr": "Photo d'identité récente" },
    "signature":      { "en": "Personal signature", "ru": "Личная подпись", "fr": "Signature personnelle" },
    "application":    { "en": "Application letter", "ru": "Заявление о поступлении", "fr": "Lettre de candidature" },
    "embassy":        { "en": "Embassy authentication", "ru": "Легализация в посольстве", "fr": "Authentification par l'ambassade" },
    "publication":    { "en": "Published papers / journals", "ru": "Публикации / статьи", "fr": "Articles publiés / revues" },
    "work":           { "en": "Work-experience certificate", "ru": "Справка о трудовом стаже", "fr": "Attestation d'expérience professionnelle" },
    "other":          { "en": "Other documents", "ru": "Прочие документы", "fr": "Autres documents" }
  }
}
```

## 二、v24 改了什么（只动 `buildPrompt` 两处，其余一字不动）
1. **材料名按请求语言给**：新增 `DOC_NAMES_RU` / `DOC_NAMES_FR`（由上表照贴，不手抄）+ `DOC_NAMES_LOC={ru,fr}`；`docName(code)` → `docName(code, lang)`：**ru/fr 用本地化表，zh/en/未知一律退回英文名**（英文名沿用 v23 既有 `DOC_NAMES`，与主文件 doc.* 逐字一致，已比对）。故「这份材料应当是:"…"」的名字：en=英文、ru=俄文、fr=法文、zh=英文（与 v23 相同）。
2. **追加一句**（只在 en/ru/fr，zh 无）：在「issues里每个问题用一句<语言>描述;没有问题给空数组[]。」那句后追加 `EXTRA_ISSUE_HINT[lang]`，三句**逐字照卡1143**：
   - en：`Write each item in issues as one natural English sentence, refer to the document by the name given above, use ordinary double quotes ("…") if you need quotation marks, and never mention internal codes or field names.`
   - ru：`Пиши каждый пункт в issues одним естественным предложением на русском языке, называй документ так, как он назван выше, если нужны кавычки — используй «…», и никогда не упоминай внутренние коды или названия полей.`
   - fr：`Rédige chaque élément de issues en une phrase naturelle en français, désigne le document par le nom indiqué ci-dessus, utilise les guillemets « … » si nécessaire, et ne mentionne jamais de codes internes ni de noms de champs.`
   - zh：`extra=""` → 该句为空 → **定时路与 lang=zh 提示词与 v23 逐字相同**。
- 版本号：头注 v24 + `json()` 里 `v: 23 → 24`。判定标准/判定核心/secrets/批处理/归属校验/软删410/限频/三级回落/`humanizeIssue`/`processDoc` 传参一字不动（`processDoc` 本就已 `buildPrompt(d.doc_type, useLang)`，无需改）。
- `humanizeIssue` 副作用抽验：它只把**被引号包住的已知英文码词**替换成英文名，对 ru/fr 材料名（Паспорт/Passeport）与非引号词无影响（已跑样例验证：ru 句原样不动）。

## 三、闸3 自验（全绿）
### ① 本地假请求四语提示词核对 `jobs/JOB-18-04/local-prompt-test.mjs`
- 四种 lang 的 `buildPrompt(passport,·)` 各打印一份（终端已见全文）。
- **zh(v24) vs zh(v23) 逐字对比：全 22 码 + 未知 lang「de/xx」= byte diff 空 ✅**（从 git `HEAD:` 的 v23 PURE 块生成 v23 zh，逐码比对）。
- 材料名按语言：passport → en=Passport / ru=Паспорт / fr=Passeport / zh=Passport ✅。
- 追加句：zh 无 ✅、en/ru/fr 各有 ✅。
- issues 语言词命中：zh中文 / en English / ru русский / fr français 全 ✅。
- ru/fr 全 22 码有值 ✅。→ 末行 `ALL GREEN`。

### ② N4 鉴权矩阵 `jobs/JOB-18-04/matrix.cjs`（注入 svc+processDoc，不连库/不调 AI/不碰 token）
`node jobs/JOB-18-04/matrix.cjs` → **ALL-AS-EXPECTED 25 checks**，含：
- OPTIONS→204；GET→405(v:24)；无凭据→401 no_credentials；对钥匙→cron 200(v:24)；公钥当 Bearer→401 bad_token；
- 学生本人→user 路；他人件→403 not_yours；中介推荐生→user 路；中介无关件→403 not_yours；有效委托→user 路；本人软删件→410 doc_deleted；
- 限频：本人到 30→429；中介触发到 30→429；限频过滤按 `raw->>trigger_uid`+`raw->>triggered_at(1h)`；
- 缓存 lang 命中/不命中；四语 issues 那句；未知 lang 回落 zh；
- **18-04 新增 4 项**：zh 全 22 码与 v23 逐字相同；材料名 ru/fr 本地化&zh/en 英文；追加句只在 en/ru/fr、zh 无；追加句引号形态正确（ru/fr 用 «»、en 用 "、无中文直角引号「」）。

### N1 版本
- 头注 v24 + `json()` `v:24`；本函数无 index.html 引用/无前端 [VER]（云函数），不涉版本四件套。
- N7 不适用（本包无给人看的页面提示，纯云函数提示词）。

## 四、交付物
- `supabase/functions/precheck-ai/index.ts` —— v24，**新 md5 = `50a3df3ceb3769bfe663305ee9830123`**（v23 = `654fcbb5e565ea9a6dda0c68179d476b`）。**改源码不部署。**
- `jobs/JOB-18-04/gen-doc-names.mjs`、`jobs/JOB-18-04/doc-names.json`（材料名表）。
- `jobs/JOB-18-04/local-prompt-test.mjs`（四语提示词 + zh 逐字对比）。
- `jobs/JOB-18-04/matrix.cjs`（N4 25 项）。
- `jobs/JOB-18-04/deploy-precheck.sh`（照 17-05 写法：`--no-verify-jwt`；公钥优先读 `SUPABASE_ANON_KEY`；探针 OPTIONS 204 / 无鉴权 401 / 错钥匙 401 / stuA 真登 `lang=en` 对名下现存未软删合成图按需判定 → issues **不含中日韩字符、不含中文直角引号「」、不含 code·代码**，全部打印；非归属件 403）。`bash -n` 语法通过。

## 五、业主要做什么
- **本包无需业主动手**（不部署、不推主仓库）。v24 的线上生效等收官后由业主的部署卡跑 `bash jobs/JOB-18-04/deploy-precheck.sh`（挂代理 + `supabase login` + `export SUPABASE_ANON_KEY=<student 端 sb_publishable_ 那串>`），期望末行 `✅ precheck-ai v24 部署 + 探针全过。`
- 「要业主亲眼看的」（进 SUMMARY）：部署后在英文/俄文/法文界面对一份新传件触发 AI 预检，看 issues 是否是对应语言的自然人话、按材料名称呼、无内部代码字样。

## 六、边界
- 只改 `precheck-ai/index.ts` 一个文件（tracked 改动仅此一处）；未部署、未推主仓库、无迁移、无写库、无上传、未造账号、未改真实数据；未碰共用查看器/学生端/学校端/其它云函数。
- `r18-STOP.txt` 本趟不存在（上一趟的停工已由卡1143 定案解除）。

---
**下一包 = 18-05**
