JOB-4-02  结果：全闸通过（迁移已跑 + 管理端 v199 → v200）

## 做了什么（一句白话）
把"填了一半的申请"（学生/学校/中介的草稿，全在 application_drafts 表，共 18 条）接到管理端，两处都放：
1. 学生抽屉 hero 下加一行「草稿：停在 X 步 · N 天没动」（没草稿不显）。
2. 侧栏新增一页「填了一半的申请」：三端合一，列 谁 / 哪个端 / 停在第几步 / 最后活动时间 / 多久没动；能按端筛、按「超过 7/30 天没动」筛；每行可展开看 payload 摘要。
3. **展示脱敏（双盲底线）**：payload 里 邮箱/电话/WhatsApp/微信（含常见别名键）一律显 `***`；另外对其余字符串再扫一遍——凡出现邮箱格式或连续 7 位以上数字也涂成 `***`（防有人把联系方式塞进别的键漏出）。统一走一个 `_v200Mask(payload)`。

## 迁移（我经 db-run.sh 跑的，跑前跑后差值）
- application_drafts：策略 +1（admin_read_application_drafts）
- 数据：18 条（学生 15 / 学校 2 / 中介 1）
- 迁移 `jobs/JOB-4-02/migrate.sql`；回滚 `jobs/JOB-4-02/rollback.sql`

## 业主要看什么
- 侧栏「填了一半的申请」页：约 18 条；点开任一条，**联系方式是 `***`**；能按端/闲置天数筛。
- 学生抽屉 hero 下：有草稿的学生显「草稿：停在第 N 步 · X 天没动」。

## 十道闸（逐条）
- 闸1 基底 md5：`a14494ec6cab24326a11b174f410f8cc`（=v199）✓
- 闸2 锚点唯一：11 处各 =1 ✓
- 闸3 补丁自检：application_drafts 读 =2 处(页面+抽屉)、_v200Mask 定义 1 调用 1、renderDraftsHalf/loadDraftsFromCloud 定义、nav/view/i18n/VIEW_RENDERERS/switchView、[VER] v200 各命中 ✓
- 闸4 产物 md5：`77bf6453457301af9b986dda88bf7b25`（v200）
- 闸5 语法：1 块 0 报错；[VER] 探针断言 admin=v200 ✓
- 闸6 消失检查：816→829（+13，无消失）✓
- 闸7 四语：管理端中英；新增文案（填了一半的申请/停在/N天没动/按端筛/payload摘要 等）中英齐全 ✓
- 闸8 真点击（**脱敏底线**）：Playwright 造带邮箱/电话/WhatsApp/微信+散落联系方式的桩 payload —— _v200Mask 后 JSON 里零 @、零 7 位连号、四键=***、非联系字段(张三)保留；renderDraftsHalf 真渲染后整页零 @、脱敏摘要零 7 位连号、非联系(Lagos/step3)保留可见。反向样本 v199：_v200Mask 未定义 → FAIL（有分辨力）✓
- 闸9 视觉：复用 wf-hero/queue-row/filter-chip/badge/var()，零新颜色/类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底/产物 md5：a14494ec6cab24326a11b174f410f8cc → 77bf6453457301af9b986dda88bf7b25
迁移跑前跑后：application_drafts 策略 +1
index.html 改了几处：3（v199→v200）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 脱敏在"四个键 → ***"之外，加了"其余字符串里的邮箱/7位以上连号也涂掉"的安全网——比 QUEUE「其余原样」略严，为的是死守双盲底线（联系方式换个键名也不能漏），非联系内容一律保留。
- 「谁」的名字：学生走 _v56StudentName 解析真名；学校/中介暂显 uid 前 8 位（跨端名册解析留后续，不影响主功能）。
