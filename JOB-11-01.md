# JOB-11-01 · 水印缓存「存量回填 + 兜底 cron」（迁移 0195）

结果：**全闸通过** —— 回填函数 + 每 5 分钟 cron 都已上生产并实测在补；暗号零外泄

## 大白话（业主看这段）
第九/十轮建的水印预渲染，只对**新派单**生效；**存量的老派单**（审核员现有一大批材料）桶里没预渲染件，审核端点开每次都要现场烧 3–5 秒水印。这轮加了个「兜底工人」：
- 一个数据库函数 `wm_cache_backfill`，专门找「有派单、桶里却没预渲染件」的材料，一次最多补 20 份，给水印函数发预渲染指令。
- 一个每 5 分钟自动跑一次的定时任务（cron），自动把存量慢慢补完（每 5 分钟 20 份），补完就基本空转。
- 这个函数**只有数据库管理员/后台能调**，普通登录用户（anon/authenticated）一律不能调。
- 暗号（发指令要用的口令）**不写进任何文件**——函数每次运行时从现役水印触发器里现读，只在数据库内部流转。

实测：开工时桶里 1 个对象、待补 104 份 → 我手动跑一次 → 桶 21 个、待补 84 → 到 05:05 定时任务自动又跑了一次（数据库记录显示"成功"）→ 桶 41 个、待补 64。**每 5 分钟稳定补 20 份，正在自动清账**，约再过 15 分钟补完。

## 断言表
| 断言 | 期望 | 实测 | 过 |
|---|---|---|---|
| 函数 wm_cache_backfill 存在 | 是 | 1 | ✅ |
| anon 可执行 | 否 | f | ✅ |
| authenticated 可执行 | 否 | f | ✅ |
| public 可执行 | 否 | f | ✅ |
| cron 任务 wm-cache-backfill | 1 条 */5 active | 有，`*/5 * * * *`，active=t | ✅ |
| 手动调 wm_cache_backfill(20) 返回 | 20 | 20（notice：发出 20 条） | ✅ |
| 桶对象数（手动后 +≤20） | 1→≤21 | 1→21 | ✅ |
| prepare 响应码 | 200 | 200 ×18（其余 2 条桶已 +20 落地，属响应表查询时窗差） | ✅ |
| 待补条数（手动后下降） | 104→↓ | 104→84 | ✅ |
| **cron 真的自动跑了** | job_run_details 成功 | runid 7108 status=succeeded @05:05:00 | ✅ |
| cron 跑后桶 +20 / 待补 −20 | 21→41 / 84→64 | 21→41 / 84→64 | ✅ |

## 技术说明
- 「桶里是否已有」判定：照 EF `signed_url` 分支的命中逻辑——按 `<reviewer_uid>/<doc_id>.` **前缀**匹配（不猜扩展名，因为 EF 的对象名后缀是按**文件内容嗅探**定的：jpeg→jpg / png→png / pdf→pdf，见 index.ts:349）。
- 候选过滤：文件名扩展 jpg/jpeg/png/pdf（实测存量里 mp4×4、docx×2 属不可渲染，已正确跳过；渲染类共 105）。
- headers 与 `wm_prepare_fanout_fn` 完全一致（Content-Type + Authorization:Bearer<公开 publishable 公钥> + x-mh-hook-secret<运行时从 catalog 读>）。
- 含真实学生的派单也在回填范围内（本包目的，卡明确允许；wm-cache 桶私有、仅 service_role 可读，双盲不破）。

## 归档 / rollback
- `supabase/migrations/0195_wm_cache_backfill.sql`（secret-free：暗号运行时读 catalog，文件里没有暗号，只有公开 publishable 公钥）
- `supabase/migrations/0195_wm_cache_backfill_rollback.sql`（unschedule + drop function；含 drop，**手动跑**，db-run.sh 会挡）

## 动过的文件 / 数据
- DB：新增函数 wm_cache_backfill + cron 任务 wm-cache-backfill（新增型）；回填已让 wm-cache 桶 1→41（补了 40 份真材料预渲染，正常业务动作）。
- 新增文件：jobs/JOB-11-01/*（迁移/rollback/探针/回执）+ supabase/migrations/0195_*（归档）
- 不改 EF、不改三个 fanout 函数、不改触发器。
