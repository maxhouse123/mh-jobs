# JOB-13-03 · 审核端一键「退回 · 让学生重传」（reviewer v36）

## 做了什么
- 从 v35 复制出 v36。审核端打开一份材料，如果最终「打不开」且属于**文件本身的问题**（类型不支持 415 / PDF 损坏 pdf_unreadable / 图片像素过大 image_too_large / 云函数内存不足 546），错误提示框下面就多出一个按钮「↩︎ 退回 · 让学生重传」。
- 点它 → 自动打开原来的退回弹窗，退回原因预选「格式不符」，说明框预填英文：`The file could not be opened (corrupted, protected, or unsupported type). Please re-upload it as a clear PDF or JPG.`，审核员可改；点确认走的是**原来那套退回流程一字没改**（写 review_verdicts）。
- 会话过期(401)/无权(403)/网络错误**不出**这个按钮（那不是文件的错）。
- 后台预取会跳过本次登录内已经失败过的材料（省得反复白试），但审核员手动再点「查看」仍会重试。

## 结果是什么（命令：node jobs/JOB-13-03/gate3-reviewer-v36.mjs，Playwright file:// 无头）
`RESULT=GATE3-PASS`，逐条：
- `btn_on_fileproblem=true`：stub 让查看返回 pdf_unreadable → 按钮出现。
- `reject_modal_open=true` + `detail_prefilled=true`：点按钮 → 退回框打开、说明框内容 = 那句英文。
- `reason_selected=true`：退回原因预选到「格式不符」(REJECT_REASONS[5])。
- `captured={table:'review_verdicts', status:'rejected', doc_id:'DOC-X', reason_has_en:true}`：点确认 → 拦到的写库动作 = 与正常退回**同一张表、同样的形状**（status=rejected、含英文理由）。
- `no_btn_on_401=true`：查看返回「会话已过期」→ 按钮不出现。
- `prefetch_skipped=true`：某 doc 标记失败后，后台预取第二次不再请求它。
- `real_errors=[]`：加载 0 真实报错（file:// 网络类已过滤）。
- 版本四件套：文件名 v36 / 头注 v36 / [VER] console v36（旧 v35 探针行已删，只留一条）/ reviewer index 重定向 3 处切 v36；changelog 加一行。

## 业主要做什么
- **推送后**（业主在原生终端推主仓库，本轮 CC 不推），审核端打开一份打不开的材料（如那两张 6MB 大图或坏 PDF）→ 应看到「退回·让学生重传」按钮 → 点 → 理由已填好 → 确认 → 该材料从队列消失、学生端收到退回通知去重传。
- 注意：真机验收要等 Supabase 服务恢复（见 JOB-13-00 的 402 限流），否则材料本身也打不开。
