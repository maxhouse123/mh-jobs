# JOB-25-05 回执 · 学校端「学生照片即时显示」（school 端 v237）

做的人：CC（无人值守，新会话）。做完时间：2026-09-27。
一句话：学校端的学生列表和学生档案，**只要学生传过证件照就直接显示照片**，不用等审核员审核；没传的还是显示名字首字母。这一包只改学校端前端，配合上一包（25-04）造好的数据库通道和水印函数。

---

## 一、为什么做这包（大白话）

- 之前学校老师看学生列表，很多人的头像是一个字母（比如「A」），看不到脸。
- 原因是老路子有两道门槛：这个学生得**先被拉进某个招生任务**、照片得**先被审核通过**——两样都满足才显示照片。
- 结果：**没进任何任务的学生、照片刚传还没审核的学生**，一律只能显示字母。
- 业主定的新规矩：**学生只要传了证件照，学校端就直接显示照片，人脸照片不用等审核**（别的材料——护照、成绩单——仍然要等审核）。
- 这一包就是把这条新规矩在**学校端**落地。

## 二、这包改了什么

学校端新版 **v237**（旧版 v236 保留可回滚），做了两件事：

1. **新开一套「取照片」的程序**（六个新函数，都带 `_v237` 前缀，一个旧函数都没删）：
   - 走上一包 25-04 造好的新数据库通道 `get_student_photo`——它**不看审核状态、不需要任务号**，只认「证件照」这一种材料，谁能看、谁不能看由数据库服务端把关（守住双盲）。
   - 取到照片后，经**水印函数**加工成一张**最长边 160 像素的小图**（省流量、够头像用），再贴到页面上。
2. **四个地方接上这套新程序**：
   - 学生列表「全部」那一页的**每一行**（以前只给进了本校任务的学生取照片，现在每一行都试）；
   - **搜索结果**里的每张卡；
   - **招生任务里的候选学生卡**；
   - 点开学生**档案弹窗**顶部那个大头像。

## 三、最要紧的「稳」——通道还没开也不会坏

业主还没贴数据库、还没部署水印函数（那是收官后另做的事）。所以这一包特别做了**三层兜底**，保证现在线上不会报错、不会白屏：

- 水印函数如果还是旧版（不认识「小图」这个新指令，会回 400/415）→ 自动**退回老办法**取一张带水印的大图缩着显示，照样有图。
- 新数据库通道如果还没贴（数据库说「没有这个函数」）→ 自动**退回原来那条老路**（走已审核件），行为跟 v236 一模一样。
- 取不到照片、或那个学生没传照片 → **保持显示首字母**，绝不报错。

另外守住的老规矩：**同一时刻最多只取 2 张照片**（不把浏览器和后台打爆）、**取过的存下来不重复取**、**原件绝不直接下载**（永远只走水印函数这一个口子）。

## 四、自验（都过了）

跑了随卡的无头测试 `jobs/JOB-25-05/green-05.mjs`，**34 条全过 / 0 败**：

- **静态查（11 条）**：六个新函数都在、四个接入点都接对了、走的是新通道 `get_student_photo`、有「小图」分支、有 400/415 退回大图的分支；**两个旧函数一个没删**（零移除）。
- **零消失核对**：拿 v236 当基线比对 v237，页面元素 id / 按钮事件 / 函数**一个都没少**（新增 6 个函数）。
- **真跑一遍（14 条，用桩数据在本地无头浏览器里）**：
  - 3 名有照片的（**包含 1 名照片没审核的、1 名不在任何任务里的**）→ 三行都真出现了照片 `<img>`；没照片那名保持首字母；
  - 把水印函数模拟成旧版（回 400）→ 自动退回大图，**照样出图**；
  - 把数据库通道模拟成「还没贴」→ 带任务号的自动退回老路出图；不带任务号的保持首字母且**不报错**；
  - **同一时刻在取的照片数=2**（并发上限守住）；
  - 档案弹窗那个大头像也真出了照片。
- **零报错**：整个过程浏览器控制台无任何报错。
- **密钥扫描**：改动文件里没有任何密钥/密码/连接串（`service_role` 只在旧注释里作说明文字出现，不是密钥）。

> 说明：**真·线上效果**要等业主收官后①把 0218 数据库文件贴进去②把水印函数部署到 v37，才看得到。本地无头只用桩数据验了「代码逻辑对、容错对、不报错」。

## 五、四件套 & 边界

- 版本四件套齐：新文件名 v237 / 头注 / `[VER]` 版本探针 / `index.html` 三处重定向 / changelog 追加一行。
- **没动**：审核端、共用查看器、云函数、数据库、其它四端、脱敏边界（仍只脱 手机/WhatsApp/微信/邮箱四样）、任何 RPC 调用签名。

## 六、动了哪些文件（主仓库一笔提交 65c567e，未推）

- 新：`school-portal/maxhouse-school-portal-v237.html`
- 改：`school-portal/index.html`（重定向 v236→v237）
- 改：`docs/changelog/school-portal-versions.md`（追加 v237 一行）
- 新：`jobs/JOB-25-05/green-05.mjs`（随卡无头测试，34 过）

**下一包 = 25-06**（管理端 v234：管理端学生列表 / 抽屉也即时显示人脸照片）。
