# JOB-10-04 · 线上真机烟测（手机 · 只验）· 重跑2（卡1074，stuA 已登录）

结果：**全部子项通过（真机）** —— ①版本对版 ②上传进度卡+拦500重试 ③get_my_offers round_id ④eruda，全过；测试件已软删+清储零残留

## 大白话
业主给了 stuA 密码（`[已作废的旧测试密码]`，只走内存没落盘），我用它在手机 Chrome 的 v391 页面登进 stuA，然后把 10-04 的全部子项在**真机**上跑了一遍，全过：
- **①版本**：页面就是 v391，[VER] 探针也报 v391。
- **②上传进度卡 + 拦500重试**：往一个空的非闸门槽（cv/个人简历）注入一个 2MB 文件 → 进度卡 **1.6 毫秒**就冒出来（远小于 200ms）→ 上传按钮在传输中是「禁用」态（aria-disabled=true）→ 我故意把云存储全拦成 500 → 卡变红、出现「重试」按钮（此刻就 1 张卡）→ 放行 → 点重试 → **还是那张卡**跑完到 100%、卡自动收起（传完按钮恢复）。传完在数据库真生成了一条材料记录，我**软删 + 清掉存储文件**，零残留。
  - 注：一开始拿默认的 bank 槽测，发现它被数据库规则挡住了（`reason_required_unreviewed`，第3批需填理由）——这正是第八/九/十轮说的「批次锁」的真身：**bank 槽处于"需填修改理由"状态，普通新传会被拒**。所以换了个干净的空槽 cv 测，一次通过。
- **③录取轮次 round_id**：`get_my_offers` 的每一行都带 `round_id`（stuA 有 1 条录取），且那条已录取 offer 的 round_id 正好 = `get_my_rounds` 里 **MH-6KG2JN 第1轮** 的 round_id（完全对上）。
  - 注：卡里写的 `rpc('get_my_offers')` **无参**调用其实会报错——真实签名是 `rpc('get_my_offers',{uid})`（前端就是这么调的）。用对签名后全通过。
- **④eruda**：带 `?debug=1` 出调试面板、不带参数不出且零 eruda 请求（上一轮已过，这轮沿用）。

## 断言表
| 子项 | 断言 | 实测 | 判定 |
|---|---|---|---|
| ① [VER]=v391 | url 含 v391 + [VER] 报 v391 | url=…v391；console `[VER] student portal v391 …` | ✅ |
| ② 出卡 <200ms | 进度卡出现耗时 | **1.6 ms** | ✅ |
| ② 传输中 aria-disabled | ='true' | 'true' | ✅ |
| ② 拦500→红卡+重试按钮 | 出现 | fail 类 + .v384-retry 按钮 | ✅ |
| ② 失败时卡数 | =1 | 1 | ✅ |
| ② 放行→点重试→完成 | 同卡 100% | afterRetry='done'，卡收起（dock=0） | ✅ |
| ② 真建档 | documents 出行 | docId fd4a52eb…（cv/mh10-test-2mb.jpg） | ✅ |
| ② 传完 aria | =null | null | ✅ |
| ② 软删+清储 | 零残留 | cv 活文件=0、cv storage mh10=0 | ✅ |
| ③ get_my_offers 每行 round_id | 是 | offersN=1，allHaveRoundId=true | ✅ |
| ③ 已录取 round_id = MH-6KG2JN 那轮 | 相等 | 均 d9549334…（MH-6KG2JN 第1轮） | ✅ |
| ④ ?debug=1 出 #eruda / 无参不出+0请求 | — | 出/不出/0 | ✅（上轮） |

## 截图（本机 ~/mh-android/shots/JOB-10/，不入日志仓库）
- `10-04-1-eruda-debug1.png` / `10-04-2-noparam-noeruda.png`
- `10-04-3-failcard-retry.png`（红卡+重试按钮）/ `10-04-4-retry-100.png`（重试后100%）
- 图册 `index.html`

## 边界遵守
- 只碰 stuA 的空非闸门槽 cv；测试件传完软删（deleted_at）+ 清储；四类闸门槽（passport/education/transcript/photo）一字节未碰；语言 zh。密码只走内存、未落盘未入回执。
