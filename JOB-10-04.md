# JOB-10-04 · 线上真机烟测（手机 · 只验）· 重跑（卡1072）

结果：**部分完成 —— 不需登录的子项(版本对版 / eruda)在真机通过；需登录的子项(上传 / get_my_offers)因 stuA 未登录且无密码而延后**

## 大白话
手机已解锁、Chrome 调试口已通。但学生端 v391 上 **stuA 未登录**（会话为空）。所以：
- **不需要登录的两项——真机上跑通了**：①版本对版（页面就是 v391、[VER] 探针也报 v391）；④eruda 手机调试口（带 `?debug=1` 出调试面板、不带参数不出、也不发 eruda 请求）。
- **需要登录的两项——延后**：②注入 2MB 测上传进度卡、③`get_my_offers` 每行带 round_id——这两项都要 stuA 登录态，卡里没给密码，按规矩不试密码，等业主登录后重跑。
- ⑤中介端 v155 / 审核端 v34 线上对版：上一轮已用电脑 curl 核过（partner v155 [VER] 200、reviewer v34 CF Access 200）。

## 断言表
| 子项 | 断言 | 实测 | 判定 |
|---|---|---|---|
| ① [VER]=v391 | page.url 含 v391 + [VER] 报 v391 | url=…v391；console `[VER] student portal v391 …` | ✅ |
| ④a ?debug=1 | 页面出现 #eruda | #eruda present = true | ✅ |
| ④b 无参 | 无 #eruda 且无 eruda 请求 | #eruda=false、eruda 请求=0 | ✅ |
| ② 上传进度卡 <200ms + 拦500重试 | — | **待登录**（上传需登录态） | ⏳ 延后 |
| ③ get_my_offers 每行 round_id | — | **待登录**（RPC 需 auth） | ⏳ 延后 |
| ⑤ partner v155 / reviewer v34 线上 | 200 | 上轮 curl 已核：v155 [VER]200 / v34 CF Access 200 | ✅（上轮） |

## 截图（本机 ~/mh-android/shots/JOB-10/，不入日志仓库）
- `10-04-1-eruda-debug1.png`（?debug=1 → #eruda 面板在）
- `10-04-2-noparam-noeruda.png`（无参 → 无 #eruda）
- 图册 `~/mh-android/shots/JOB-10/index.html`

## 业主该做什么
手机登录 stuA 后，让我重跑 10-04 的 ②③（+ 10-03 断点续传）。

## 边界遵守
- 只读 + eruda 探针(无参/带参切换)；未登录、未上传、未改任何数据；未试密码；截图仅在无学生资料的页面（未登录态 / eruda 面板）。
