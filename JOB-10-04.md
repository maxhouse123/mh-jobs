# JOB-10-04 · 线上真机烟测（手机 · 只验）

结果：**跳过（手机锁屏，DevTools 端口不可达）—— 不影响其他包**

## 大白话
这包要在手机 Chrome（stuA 登录态）上对线上 v391 做一圈烟测（上传秒出进度卡、录取轮次 round_id、eruda 调试口等）。跟 10-03 同一个原因：**手机停在锁屏、Chrome 调试端口连不上**（详见 JOB-10-03.md 的自检表）。手机没解锁就没法跑，所以这包**跳过**，等业主解锁后与 10-03 一起重跑。

其中「中介端 v155 / 审核端 v34 线上文件对版」这一小项**不依赖手机**，我用本机直接核了线上版本（见下），算部分完成。

## 手机相关子项：全部待手机解锁后重跑
- v391 [VER] 行 / page.url 含 v391 —— 待手机
- 9-01 真机版（注入 2MB→进度卡<200ms→拦 500→红卡→重试→卡数=1）—— 待手机
- 9-02 真登闸（get_my_offers 每行 round_id）—— 待手机（且需登录态）
- 9-07 eruda（?debug=1 出 #eruda / 无参无 eruda）—— 待手机

## 不依赖手机的子项：线上版本对版（本机 curl）
见下表（curl 需 -L 跟 CF Pages 308 跳转）。

## 断言表
| 检查 | 期望 | 实测值 | 判定 |
|---|---|---|---|
| 手机 DevTools 可达 | 是 | 否（锁屏，端口空） | ❌ 跳过手机子项 |
| 中介端 v155 线上文件 | HTTP 200 + [VER] 对版 | HTTP 200；页面含 `[VER] partner portal v155` | ✅ |
| 审核端 v34 线上文件 | HTTP 200（CF Access 只需版本文件 200） | HTTP 200（返回 Cloudflare Access 登录页，内容被门禁挡住，符合预期） | ✅（按卡降级判据） |

## 业主该做什么
同 10-03：解锁 Pixel、确认 Chrome 前台是 stuA 登录态的 v391，再让我重跑 10-03 / 10-04。

## 边界遵守
- 未碰手机开关、未装任何东西、未试密码、未截图。
