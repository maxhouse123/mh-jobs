# JOB-25-02 · 学校端显示学生「全部层次 / 全部专业」+ 候选池按数组匹配

（CC 写给业主。大白话。这一包不用你贴 SQL 也能先看，但要 25-01 那段 SQL 贴了之后云端才真的把多层次传给学校端。）

## 一、解决什么

业主截图：学生（Ali）报了「语言生 + 硕士（两个专业）」，学校端只显「硕士 · 一个专业」。
25-01 已把「装多层次的新格子」加进库、并让学校侧三个查询函数把这一列传出来。
这一包（学校端 v236）把传出来的多层次**真的显示出来**，还让「按层次筛/匹配」也认多层次。

## 二、CC 做了什么（学校端 v236，从 v235 拷贝改，旧版留盘）

1. **接住新数据**：学校端有六处把云端学生行转成页面对象的地方，每处都补上「多层次数组」这一列（云端没传就是空数组，自动退回老的单层次，不报错）。
2. **五个小助手**（新函数，都带 `_v236` 前缀）：把「多层次数组」翻成——全部层次的中文并列文字、按层次分组的专业文字、卡片上的层次/专业小标签、以及「这个学生算不算某层次」的判断。数组空就退回老的单值，绝不显乱码或英文代码键。
3. **学生档案（脱敏）弹窗**（就是业主截图那张）：
   - 「目标层次」从只显一个，改成**显示全部层次、中文顿号并列**（例：`汉语言项目、硕士`）。
   - 「专业」从只显一个，改成**按层次分组**（例：`汉语言项目：—　硕士：Computer Science and Technology、Business Administration`）。
4. **候选卡 / 名单行**的小标签：**每个层次一枚标签**；专业跨层次去重后**最多显 3 枚，多的显「+N」**。
5. **按层次筛选 / 匹配**两处（一键匹配打分、任务分配判断）：从「只认那一个层次」改成「**那一个层次 或 多层次数组里含这个层次**」——多层次学生也能被正确圈进对应任务。

## 三、一个措辞提示（要业主知道）

- 业主卡里写的验收词是「语言生、硕士」。**学校端对「chinese_lang」这个层次的既有叫法是「汉语言项目」**（学生端才叫「语言生」，是同一个层次、两端用词不同）。按房规（复用本端既有词典、不自造），学校端显示的是**「汉语言项目、硕士」**——意思完全对，只是用词随学校端。若业主想让学校端也叫「语言生」，说一声，单独改词典。

## 四、闸门（全过）

`node jobs/JOB-25-02/green-02.mjs` → **PASS=32 / FAIL=0**（日志 `jobs/JOB-25-02/green-02.log`）：
- 静态 13 项：六处映射都补了、六个助手都在、档案弹窗/卡片/两处筛选都接上了助手。
- **无头真的用页面里的翻译词典跑助手**：两层学生→层次显「汉语言项目、硕士」（顿号并列两个）、专业分组含 Business Administration、卡片两个层次标签+专业标签；单值学生→只显一个层次一个专业、不冒「+N」；层次匹配「两层学生算语言生=是、算博士=否」「单值硕士生算语言生=否、算硕士=是」；层次标签**不是**英文代码键（非裸键）。
- 加载零报错；N1：学校端 v235 的按钮/内联动作/函数在 v236 里**一个没少**（丢失 0，新增 6 个助手函数）。
- secret-scan 干净；没加新翻译键（复用现成的 `prog.*`）；没加 shell 脚本。

## 五、业主要做什么

- **这一包本身不用你做事**。若 25-01 的 SQL（0217）还没贴，学校端照旧只显一个层次，不报错。
- **贴了 0217 之后验收**：学校端打开那位两层学生的档案，「目标层次」显示两个层次、「专业」两个都在；候选卡也显两个层次标签。

## 六、边界

- 没动脱敏边界（仍只脱手机/whatsapp/微信/邮箱四样）、没动查询函数的调用、没动库、没动其它四个端。
- 管理端/中介端的多层次显示在 25-03；人脸照片即时显示在 25-04 起。

---
- 闸3：green-02 PASS=32/0 ✅；N1 零消失(added 6 助手) ✅
- 本轮暂无「必须停」（`~/mh-verify/r25-STOP.txt` 不存在）
- 下一包 = 25-03
