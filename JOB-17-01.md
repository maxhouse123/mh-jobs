# JOB-17-01 回执 · A：PDF 与图片同一条工具条 —— MHViewer2 v2.1.1 装进 审核 v41 / 学校 v234 / 管理 v216

包号 17-01（第十七轮第 2 包，接 17-00 之后）。CC 全新会话，只修不加、不部署、不推主仓库。

## 一、做了什么

### 组件上线
- `cp jobs/JOB-17-01/kit2/mh-viewer-v2.js assets/mh-viewer-v2-1-1.js`——新文件名，md5 `ee351485b3145e9ceb789caf4340802e`（＝kit2 版，与 §0.3 一致）；旧 `assets/mh-viewer-v2.js`（v2.0.0，md5 `5f84a4ee…`）**一字未动**。
- 三端 `<script src="../assets/mh-viewer-v2.js">` → `../assets/mh-viewer-v2-1-1.js`。图片路一行未改（同一组件，图片引擎与 v2.0.1 相同，**窄盒紧凑工具条 F④ 随组件上线**）。

### 审核端 v40 → v41
- `openPreview` 的 PDF 分支（`bt === 'application/pdf'`）：由 `renderPdfBlobInto`（pdf.js 逐页 canvas）+ iframe 回落 → 改为 `_ensurePdfJs()` 拿 pdfjsLib → `MHViewer2.mountPdf(el, { blob, pdfjsLib, lang四语守卫, height:'100%', maxPages:20 })`。
- pdf.js 加载不上、或 `MHViewer2.mountPdf` 不存在时才回落旧 `renderPdfBlobInto` → iframe。
- 坏/加密件 `whenReady()` 为 false 时保留组件框内四语人话，另 `insertAdjacentHTML` 追加 `_v36ReturnBtnHtml(m.id)`（退回·让学生重传）。
- 那条深底深字「✓ 已渲染 N 页」随主路改走 mountPdf **消失（F③）**。`closeModal` 已有 `MHViewer2.destroyAll()`；审核端无语言切换器，故未挂 `setLang`。

### 学校端 v233 → v234
- `openCloudDocPreview` 的 PDF 分支（`/pdf/i.test(blob.type)`）：由 `renderPdfPagesInto`（blob→dataURL 逐页 canvas）→ 改为 `_ensurePdfJs()`→`MHViewer2.mountPdf(box, {...maxPages:20})`；pdf.js/mountPdf 不可用才回落旧 `renderPdfPagesInto`。
- 图片分支 / `setLang` / `destroyAll` 沿用 v233。录取通知书样张、入驻文件等**其它 PDF 预览本轮不动**（列第十八轮候选）。

### 管理端 v215 → v216
- 管理端此前**没有 pdf.js 加载器**（v215 全文件无 `_ensurePdfJs`）：照审核端 v40 原样搬一份 `_v216EnsurePdfJs`（自托管 `../assets/vendor/pdf.min.js` + worker，8 秒超时）。
- `_v162AdmDocRender` 的 PDF 分支（原 `<iframe>`）改挂 `MHViewer2.mountPdf(_pf, {..., maxPages:20, extras:[新标签打开 window.open / 下载 <a download>]})`——**新标签/下载移进组件工具条 extras**，PDF 分支不再另起上方 tools 行（N1 不重复、不消失）；pdf.js/mountPdf 不可用才回落旧 iframe。
- 图片分支抽出共用 `_v216AdmShowImg`（jpeg/png/webp），**补认 WebP（F⑤）**：未知/octet-stream 类型嗅探首 12 字节 `RIFF….WEBP` 魔数，是 webp 就按图片挂 MHViewer2，否则维持「无法预览·下载原件」。图片 `attach` 路一字未改。
- **图纸「若已在上方 tools 行存在就不再重复传 extras」与闸3「管理端带 extras: true」的取舍**：judgePdf 以 `extras:true` 判定时，J29a 要求工具条内正好 2 个 `data-extra`；管理端旧的新标签/下载是弹窗外的独立 `<a>`（不在工具条里）。为同时满足闸3绿灯与 N1「不重复」，CC 把这两个按钮从 PDF 分支的外置 tools 行移进 mountPdf 的工具条 extras（图片分支仍保留外置 tools 行）。据此闸3以 `extras:true` 判、J29a 绿。此取舍已记，供 Claude/业主复核。

四件套（文件名 / 头注 / `[VER]` 探针 / index.html 重定向）四处同版。

## 二、结果是什么（闸3，真页面 judgePdf，详见 `jobs/JOB-17-01/gate/RESULT.md`）
（大块真页面红绿检验交子代理跑，结论与文件带回，脚本 `jobs/JOB-17-01/gate/`，日志 `gate3-final.log`）

| 端 | 旧版（临时从 HEAD 取出） | 新版核心 PDF 尺 | 新版失败 | 页面报错 | N1 removed |
|---|---|---|---|---|---|
| 审核 v40→v41 | RED（failed=J00 无 pdf viewer root）| **GREEN** | — | 0 | 0 |
| 学校 v233→v234 | RED（J00）| **GREEN**（见 J15 注）| — | 0 | 0 |
| 管理 v215→v216 | RED（J00）| **GREEN** | — | 0 | 0 |

- **窄盒**（审核 390px）：英文 **GREEN** / 俄文 **GREEN**——J02c「两端不裁」两语都过。（审核端无语言切换器，viewer lang 取自 currentLang，窄口 J02c 与内核无关照过。）
- **坏件**（expectError，字节换 corrupt.pdf）：审核 **GREEN** / 管理 **GREEN**（E05：管理端 extras 在坏件上仍可点：newtab:on download:on）。
- **图片无回归**：三端仍挂 `[data-mhv2="root"]`（非 data-pdf）、工具条 6 键在、0 页面报错；审核端另过 kit2 图片尺子 28/28。
- **版本**：三端 `MHViewer2.version === '2.1.1'`，各只加载一份查看器脚本。
- **页面报错**：全程 0（三端所有跑合计）。
- **N1 差集**：removed **＝0**（admin 200/172/802 vs 基线 199/172/797＝加为主 +1id/+5fn；reviewer/school 加为主）。

### 一个 borderline（非阻断，记组件负责人 Claude + 第十八轮）
- **school J15**「硬拖四方向不把页面拖出取景框」：仅在 **1280px 宽弹窗（stage≈1160px）+ ~373% 峰值缩放 + 过量合成拖拽**时，PDF 的 pan-clamp 停在 gapRight=13.02px，比同台图片路的 12.0px 宽约 **1px**（越界 1.02px、判据容差 13px）。窄口（720px）school 全绿含 J15；kit2 作者尺子 `claude-pdf-run.mjs` ALL-AS-EXPECTED（其宿主弹窗到不了 1160px 宽）。判为**组件既有亚像素容差、PDF 路比图片路松约 1px**，仅现于最宽弹窗+最大缩放+过量甩动，**非用户正常平移可达、0 页面报错、非功能阻断**。组件与尺子归 Claude，CC 不改 kit2；按 §0.9⑤「borderline，不停」处理，标给业主/Claude 复核。

## 三、业主要做什么
- 本包**先提交未推**（主仓库本地 commit `e6756a2`，未 push；推送由收官后卡1135 做）。
- **要业主亲眼看的**（进 SUMMARY 清单）：三端各打开同一类 PDF，工具条长得一样（−%+ / 整页·适宽·1:1 / 旋转 / 翻页）；滚轮翻页、Ctrl+滚轮 / 双指缩放、放大后小字清楚；**管理端还多出新标签打开 / 下载**。截图见 `~/mh-jobs/shots/17/17-01/`（54 张 + INDEX.md，全合成 PDF、真实文字已 mask）。

## 四、新闸证据（N1–N6）
- **N1**：三端差集 removed=0（`jobs/JOB-17-01/gate/inv/`；基线 `jobs/JOB-17-00/inventory/`）。
- **N2**：三端旧版真页面先红（J00），新版后绿；未复现项无（本包全复现）。
- **N3**：审核端 reviewer1、学校端 schX 均真登（scaffold `login()` 驱真表单，无会话注入，脚本经 env.mjs 读 accounts.json）；管理端 stub 会话（无写库）。
- **N4**：闸3 逐格真跑（核心 PDF / 窄盒英俄 / 坏件 / 图片 / 版本），矩阵见 RESULT.md。
- **N5**：`secret-scan` 对 `~/mh-jobs/shots/17/17-01` 与 `jobs/JOB-17-01/gate` → `SECRET-SCAN-CLEAN`。
- **N6**：只把 v2.1.1 装进三端、只改 PDF 分支 + 补 webp，未加新活；school J15 亚像素容差记入建议不顺手动 kit2。

## 五、动过的槽 / 测试写库
- 本包**零写库**：真页面全走网络层字节替换（watermark-doc / signed_url / wm-cache 路由成合成 PDF），管理端 stub 直接把合成 blob 喂进渲染函数。无上传、无测试件、无测试派单，故无收尾软删。

下一包 = 17-02
