# JOB-7-05 · 「YOU'RE ALL SET」卡引用错的申请号 — 回执

**结果：做完（不动数据库、零破坏性操作），学生端 v387。逻辑走查 4/4 过。stuA 真登断言 MH-6KG2JN 那一闸因密码没给延后。**

## 大白话：错在哪、怎么修
巡检第 1 张图底部那张绿卡写「**MH-9F4AG7** is confirmed · 录取材料已就绪」，但 MH-9F4AG7 是**第二轮**（进度条还显示 In review 在审），真正录取的是**第一轮 MH-6KG2JN**。学生会以为第二轮也录了——虚惊/误导。

根因：那张成功卡的申请号取的是 **hero 顶部「当前轮」的编号**（`state.appId`），可它展示的录取其实属于**上一轮**。等于「拿本轮的号，挂上一轮的喜报」。

修法：新增 `_v705OfferAppNo(offer)`——**申请号改从「那个已确认 offer 所属的轮次」取**，并在卡上标「第 N 轮」。取号三级：
1. offer 若带 `round_id` → 直接匹配轮次表 `application_rounds` 取该轮 `app_no`；
2. 没 round_id → 用**时间窗**匹配（offer 的下发时间落在哪一轮的开/关区间，就归哪轮，与现有轮次结果推导 `_v377RoundOutcome` 同法）；
3. 都匹配不到 → 回落到**最早一轮（账号档案号）**——已确认的 offer 必属已结束的轮次，**绝不会**误用当前在审轮。

## ⚠️ 数据血缘三问（铁律14）我查清了，有一处要你拍板
- **(a) 号从哪来**：`offer_decisions.round_id` → `application_rounds.app_no`。我**实测数据库**：`offer_decisions` 确有 `round_id` 列、`application_rounds` 确有 `app_no` 列，**队列描述的链路真实存在**。
- **(b) 哪条路会缺**：**当前 `get_my_offers` 这个读函数并没有把 `round_id` 吐给前端**（我逐列核对了 0034 迁移的返回表）。所以「用 round_id 直配」这条路现在**取不到值**。
- **(c) 缺了走哪**：我让它**回落到时间窗匹配**（这条今天就能正确工作，无需动数据库），再回落到最早一轮。
- **要你拍板的**：想让第①条（round_id 直配，最精准）生效，得给 `get_my_offers` 的返回表**加一列 round_id**。但 Postgres 改函数返回列**必须先 DROP 再 CREATE**（属破坏性操作），按**铁律9 我不能自作主张 DROP 函数**。**我没做这一步**。时间窗兜底已能修好你看到的现象；等你一句话授权，我再补一版「DROP+CREATE get_my_offers 加 round_id」迁移让它更稳。

## 版本
学生端 v386 → v387（index 指针同步）。**零迁移、零数据库改动。**

## 闸 8（走查）
- 真登断言（`用 stuA 真登 → 卡显示 MH-6KG2JN 而非 MH-9F4AG7`）**需 stuA 密码 + 线上真数据**，密码你没带出来 → **延后**，等你补密码我追证。
- 逻辑走查 `tests/job7-05.mjs`（喂真值 _718Rounds：第2轮当前在审 MH-9F4AG7 + 第1轮已录取 MH-6KG2JN）**4/4**：
  - offer 带 round_id=第1轮 → 取到 **MH-6KG2JN** ✅
  - offer 无 round_id、下发时间落第1轮窗口 → 时间窗取到 **MH-6KG2JN**（不是当前 MH-9F4AG7）✅
  - 无任何线索 → 回落最早轮，**绝不误用当前在审轮 MH-9F4AG7** ✅
  - 卡上有「第 N 轮」标签 ✅
- JS 解析干净。

## 边界说明
- 只改了**云端真数据**那条成功卡分支（就是显示 MH- 申请号、你踩坑的那条）。另有一条纯 mock 演示分支用的是学校 sysId（不是申请号、语义不同、仅云 offer 为空时才走），**未动**。

## 动过的文件
- 改：`student-portal/maxhouse_student_portal_v387.html` + `student-portal/index.html`
- 新增：`tests/job7-05.mjs`、`jobs/JOB-7-05/probe.sql`（只读探针）

**主仓库未推。本回执已推日志仓库。**
