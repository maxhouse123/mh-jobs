# JOB-14 · 学校端留痕改服务端 —— 设计（必须停）
**结果：只做设计、不动代码。完整设计见 `jobs/JOB-14/design.md`。**
合规项：学校对学生数据的四类行为（导出历史含护照号/已导出名单/材料下载日志/看过哪些学生）现全在学校浏览器里、平台零记录。design.md 给了建表方案（`school_access_log` 一表四类 + `log_school_access` 写入 RPC，**建表迁移显式 revoke 再 grant**）、四类行为各写什么、管理端只读日志页（复用招生任务页同款组件）。
## 等你回答一个问题
这条记录**要不要对学校可见**？（不可见=纯平台合规留痕；可见=学校自我审计+学生看谁看过我，需再定显校名还是匿名计数）
