# JOB-8-03 · TUS 断网续传（验收项 9）— 回执

**结果（诚实版）：好消息——手机上传 3MB 图、中途断网 3 秒再恢复，最终真的传成功了、不用重选（学生实际体验的"断网不白传"达到了）。但有个重要发现：这次并不是"从断点接着传"，而是"整份重发"——因为分片大小设成了 6MB，3MB 的文件一口气就发完，没有分片、也就没有真正的"断点续传"。对小于 6MB 的文件，断网续传其实退化成了"重发"。这条写进待修，建议下轮把分片调小。**

## 大白话
你担心的是"学生在烂网下传到一半断了，得从头再来"。我在真手机上模拟：限速 200KB/s 传 3MB，传到一半把网断 3 秒再恢复。
- **最终结果：传成功了**（数据库里真出现了这个文件，进度卡正常完成消失、没有红卡）。学生**不用重新选文件**。这一点是达到的。
- **但机制不是"接着传"**：全程只有 1 个创建请求（POST 到 TUS 的 resumable 接口），**0 个 PATCH、0 个 HEAD**，也就没有"从第 X 字节接着传"的证据。原因：代码里 TUS 分片设成 6MB，而文件才 3MB → 一个请求就发完 → 断网后是靠 TUS 的"自动重试"把**整份重发**了一遍，不是从断点续。

## 机器断言表（真机 v389，CDP 限速 + 断网模拟，数据库做最终真相）
| 断言 | 实测值 | 判定 |
|---|---|---|
| 前置 mh_upload_tus 非 off（默认开）| `null`（未设=默认开）| ✅ |
| ① 走 TUS（有 /upload/resumable 请求）| `POST RESUMABLE` 有（tus 开时）| ✅ |
| ② 真断点续传证据：PATCH Upload-Offset>0 或 HEAD Upload-Offset>0 | **0 个 PATCH、0 个 HEAD**（3MB≤6MB分片=单请求）| ❌ 未出现 |
| ③ 最终完成、无红卡 | 数据库出现 `mh8-test-3mbR.jpg`(3072KB)，进度卡完成消失、无红卡 | ✅ |
| 对照组 tus 关（mh_upload_tus=off）→ 3MB | `usedResumable=false`，只走 `SIGN`(7-03a 直传)，上传成功 | ✅ 开关有效 |
| 收尾 mh_upload_tus 恢复默认 | `null` | ✅ |
| 收尾 测试件删净 | 5 个 mh8-test-3mb* 删除 ok(5)，DB 复核 bank 活文件回到 2（原样）| ✅ |
| 收尾 网络条件还原 | latency0 / throughput -1 还原 | ✅ |

## 结论定性
- **验收项 9 的"用户结果"达到**：断网 3 秒不导致失败、不用重选、最终 100% 成功（③ + 对照都印证）。
- **验收项 9 的"真断点续传机制"未达到（3MB）**：没有 Upload-Offset>0 的续传证据（②❌）。根因是 **6MB 分片**——文件 ≤6MB 时一个请求发完，断网只能整份重发，谈不上"从断点续"。真要看到"从第 X 字节续"，得传 >6MB 的文件（超出本轮队列指定的 3MB 测试尺寸，我没擅自加大）。

## ⚠️ 待修清单（本轮只记不修）
- **把 TUS 分片 chunkSize 从 6MB 调小（例如 1MB）**：这样 3MB 会切成 3 片，断网后能从已传的片接着传（真续传），而不是整份重发。对你真正的痛点（大文件/烂网学生）才有实效。当前 6MB 分片下，≤6MB 文件的"续传"＝"重发"。建议包 9-0X。
  - 注：Supabase 官方示例常用 6MB 分片；权衡是分片小则请求多。可只对"大文件 + 移动网络"场景调小，或做自适应。

## 截图（本机）
- `8-03-1-断网中.png`（断网那 3 秒的卡状态）、`8-03-2-续传完成.png`（最终完成）

**只碰 bank 非闸门槽；5 个测试件已删净、bank 复原；mh_upload_tus 恢复默认；网络已还原；未改任何本地文件。回执推日志仓库；截图只在本机。**
