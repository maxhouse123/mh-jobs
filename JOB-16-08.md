# JOB-16-08 回执 · 审核端 v40 + watermark-doc v33：已撤销/已软删的件不进队列、云函数也拒 + 文件图标按真实类型

给业主的一句话：审核端升到 v40、水印云函数改到 v33（源码改**未部署**）。修了两个业主看得见的毛病——① Word 件卡上的图标不再乱显成 JPG/PDF，改成按**真实文件后缀**显（Word 件显「DOCX」绿标）；② 派单被撤销、或材料被软删的件，不再赖在审核员的队列里、云函数也当场拒绝（撤销 403、软删 410），审核员看到人话「这份材料的派单已撤销/已被删除」并从清单里自动消失。真页面先红后绿都过了，两版零控制台报错。

## 一、改了什么（只修不加）

### 审核端 reviewer-portal v39 → v40（四件套齐）
1. **图标按真实扩展名**：新增 `_v40FileKind(fileName)`，按**文件后缀**（不是 `doc_type`）定图标配色与文字角标——
   图片(jpg/jpeg/png/webp/gif/bmp/heic)→蓝｜pdf→红｜word(doc/docx)→绿｜excel(xls/xlsx)→青｜ppt(ppt/pptx)→橙｜视频(mp4/mov/webm/m4v)→紫｜压缩(zip/rar)→灰｜其它→灰；角标显真实后缀大写，无后缀→「文件」。材料卡图标落点 `studentDocRow` 改用之；CSS 补 `.material-icon.xls/.ppt/.vid/.zip/.file` 四配色（复用既有渐变族）。**不改库里的 `doc_type`**。
2. **撤销/软删防护**：队列**单一来源 = `reviewer_doc_queue` 视图**（0207 起服务端已滤 `revoked_at`/`deleted_at`，无 localStorage 历史来源、无直查 `reviewer_assignments`——16-00 b1 已证）。竞态（材料载入后被撤单/软删）时云函数回 403 `assignment_revoked` / 410 `doc_deleted`：
   - `_v40QueueGone(code,status)` 认这两码 → 出人话「这份材料的派单已撤销，不能再查看」/「这份材料已被删除，不能再查看」；
   - `_v40DropFromQueue(docId)` 卡上显示人话 + 从 `MATERIALS` 拿掉该件 + `renderMaterials()` 重渲染 + 加入 `_v36FailedDocs`（不再预取）+ 不重试；
   - `_wmFetchEF` 与 `_v37FetchSignedVideo` 错误映射认这两码并挂 `err.queueGone`；`openPreview` / 视频 / 预取三处 catch 接上。
3. 四件套：文件名 v40、头注 `[VER] v39→v40`、`[VER]` console 探针升 v40、`reviewer-portal/index.html` 三处重定向改 v40。changelog 一行。

### watermark-doc v32 → v33（`supabase/functions/watermark-doc/index.ts`，源码改**未部署**）
- **reviewer 归属门**：`assignP` 补选 `revoked_at`；派单存在但 `revoked_at` 非空 → **403 `{error:"assignment_revoked"}`**（审计 `denied_revoked`）。撤销了的审核员不再能取材料。
- **软删门**：任何非管理员路（reviewer/school/student/partner）对 `documents.deleted_at` 非空的件 → **410 `{error:"doc_deleted"}`**（审计 `denied_doc_deleted`）；`docLookup` 补选 `deleted_at`，归属门通过后、下载前判。
- 其余路径（`handleSignedUrl` / `handleSignedVideo` / `handleCacheOp`(prepare/cleanup) / 回填）**一字不差**——`git diff` 只改 header 注释与 render `handle()` 三处。撤销/软删时 wm-cache 已由 cleanup 清桶 → 这些路 miss → 前端回落 render EF 得上述码。
- **v33 md5 = `55debf4dcc841c8370d2b4280d9e8768`**。

## 二、结果是什么（先红后绿 + N 闸）

- **真页面 stub 走查**（`jobs/JOB-16-08/realwalk.mjs` + `realwalk.log`；`~/mh-verify` Playwright + `--no-proxy-server`；realpage.mjs serve 本地出完整端口 html + **真填登录表单 + 真 `locator.click()`**；网络层按 §0.4 注入受控夹具：queue 3 条合成【测试】件、watermark-doc 按 doc_id 前缀回 403/410、storage 回落）：
  - **v39（旧）**：`{"iconBadge":"PDF","revPresent":true,"delPresent":true,"clickMsgSample":"无权查看(该材料未派给你)","consoleErrors":0}` → **红**（docx 图标显 PDF；撤销/软删件留在队列；点开显误导文案）。
  - **v40（新）**：`{"iconBadge":"DOCX","revPresent":false,"delPresent":false,"clickMsgSample":"dropped by prefetch before click (green)","consoleErrors":0}` → **绿**（docx 显 DOCX；撤销/软删件从队列拿掉；早一轮抓到点开显「🚫 这份材料已被删除，不能再查看」= `card-v40.png`）。
  - 两版**0 未捕获报错**（控制台里的 403/410/404 是被拦截的网络响应，全部已处理，非未捕获异常）。
- **N2 裁决**：icon(R5) 与撤销/软删防护两项均**真页面先红后绿成对**。
  - **「259f456a 在预取清单里」这一路的红本轮不可复现**——迁移 0207（16-03 已应用）已把撤销/软删件滤出 `reviewer_doc_queue` 视图（前端单一来源，16-00 b1），故对**现库**跑 v38/v39 也不再有 259。N3 真登真库探针：`reviewer_doc_queue` **60 行、259f456a present = false**。这是「DB 已修好、尺子量不出旧红」，按 N2 如实写；前端 403/410 兜底是针对竞态的防护，已用受控网络注入证红→绿。**未拿零件级断言顶替。**
- **N1**（v39→v40，`jobs/JOB-16-00/inventory/inv.mjs`）：`DIFF-REMOVED-TOTAL 0`；ids 74=74、handlers 31=31、functions 仅 **added `_v40DropFromQueue` / `_v40FileKind` / `_v40QueueGone`**，零删除。
- **node 断言**（`jobs/JOB-16-08/assert.cjs`，`ALL-ASSERTIONS-PASS`）：`_v40FileKind` 26 种后缀/无后缀/带 query 形状全对（含「docType=photo 但文件是 docx → 图标 DOCX」R5 bug 修复）；`_v40QueueGone` 认 revoked/deleted 出人话、其它回 null；v40 全部内联 `<script>` `new Function` 语法有效；studentDocRow 落点已改用 `_v40FileKind`；五处接上 queueGone；四件套齐。
- **watermark-doc 本地鉴权矩阵**（`jobs/JOB-16-08/matrix.cjs`，`node:module.stripTypeScriptTypes` 载**真实 index.ts**、注入假 Deno.env+假 createClient，逐格跑 `handle()`）：**7 格全 `ALL-AS-EXPECTED`**——无凭据 401｜有效派单活件→到下载阶段(空下载桩 404，鉴权全过)｜**已撤销派单→403 assignment_revoked ★**｜没派给他→403 doc_not_assigned_to_you(同 v32)｜学生活件→到下载阶段 404(同 v32)｜**学生自己软删件→410 doc_deleted ★**｜管理员无角色→403 not_authorized(同 v32)。细节见 `jobs/JOB-16-08/auth-matrix.md`（deno 本机缺席，成功件 200 像素由卡1119 部署探针出；新门都在下载前返回故本矩阵完整判定）。
- **部署脚本** `jobs/JOB-16-08/deploy-watermark.sh`（业主在卡1119 跑）：JWT 校验**保持开**（不带 `--no-verify-jwt`）；`bash -n` 通过；不打印任何 token/密码/anon key；探针 = 无凭据 401｜reviewer1 真登 JWT + 一份有效派单件(取自视图)→200(无有效件则跳过)｜reviewer1 JWT + 已撤销派单件→403 assignment_revoked｜下一跳 wm-cache-backfill 可选。**真页面绿灯在卡1119 的线上走查里出。**
- 主仓库已 commit（reviewer v40.html + reviewer index.html + watermark-doc index.ts + reviewer/edge 两份 changelog），**未 push**（收官后业主用卡1118 推）。secret-scan 对待推文件与 `~/mh-jobs` 全树：`SECRET-SCAN-CLEAN`。

## 三、测试残留（带身份探针证明清干净）
- 本轮**没有造过测试派单**：16-00 明确「测试派单配方留到需要时」未落库；16-01 的放大缩小红绿也没用它；本包 259f456a 本身就是库里现成的「已撤销」真例，加上受控网络注入即可证前端兜底，**无需新建派单**。故 16-08 收尾**无派单可撤、无测试件可软删**。
- 走查用的 3 条【测试】件全是**网络层注入的合成夹具**（`page.route` 拦截），**从未写库**。N3 真登真库探针：reviewer1 队列 60 行、无 259f456a、无任何【测试】残留。

## 四、业主要做什么
- **本包无需现在做任何事。** v40 会在收官统一推送后随线上生效；watermark-doc v33 由业主在**卡1119** 用 `deploy-watermark.sh` 部署（JWT 校验保持开）。

## 新闸证据（N 闸）
- **N1**：removed 0（见上），仅加 3 个新函数，逐条无删。
- **N2**：真页面真填登录+真点，判据只认屏幕文字与行在不在；icon(PDF→DOCX)、撤销/软删件(留→拿掉)两项成对红→绿；259 路旧红因 0207 已修不可复现，按裁决如实写（N3 真库 60 行无 259 佐证）。未用 evaluate 直调页面函数。
- **N3**：truth() 真登 reviewer1 走 REST 查真库队列（60 行、无 259）。无身份 0 行未当证据。
- **N4**：watermark-doc 矩阵载真函数逐格跑，两条新门（403/410）★实证。
- **N5**：公开截图元素级、全合成夹具、`shots/16/16-08/INDEX.md` 在位；secret-scan CLEAN。
- **N6**：只改 reviewer 一端一版 + watermark-doc 源码（不部署）；不动派单规则/回填候选其它条件/doc_type 数据；不给 259 重新派单；SUMMARY-15 建议包 C–G 不做。

## 截图
- 公开（`~/mh-jobs/shots/16/16-08/`）：`icon-v39.png`(旧·docx 显 PDF=红)/`icon-v40.png`(新·显 DOCX=绿)；`queue-v39.png`(旧·撤销删除件留队列=红)/`queue-v40.png`(新·已拿掉=绿)；`card-v40.png`(新·点开显「已被删除」人话=绿)/`card-v39.png`(旧·误导文案)；`INDEX.md` 配文。本机完整版在 `~/mh-verify/shots/JOB-16/16-08/`。

下一包 = 16-09
