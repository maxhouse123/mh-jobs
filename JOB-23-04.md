# JOB-23-04 回执 · 管理端 v223：学校行为日志默认不看取件 + 取件行补上学生名

做这一步的人：CC（无人值守）。只动管理端一端，版本 v222 → v223（旧版留着可回滚）。

## 一、做了什么（大白话）

「学校行为日志」这页原来有两个毛病，本卡都修了：

1. **进页默认不再被取件流水淹没**。
   - 线上 717 条里有 690 条是「学校点开某个学生材料时系统自动记的水印取件流水」，只有 27 条是真正要看的「导出 / 下载 / 查看学生」合规记录。以前默认「全部」，那 27 条被 690 条冲得看不见。
   - 现在默认停在新加的「全部·不含取件」，进页只看到那 27 条合规记录。想看取件流水，点一下「取件」筹码就全出来（690 条）；点「全部」还是 717 条都在。
   - 筹码顺序调成：全部·不含取件 / 导出数据 / 导出名单 / 下载材料 / 查看学生 / 取件 / 全部。页顶副标题也写明了「默认不含取件，点取件查看」。

2. **取件行现在显示学生名了**。
   - 取件流水那张旧表里只存了材料编号（doc_id），没存学生，所以以前取件行的「学生」列永远是「—」。
   - 现在装载完日志后，把这些取件行的材料编号收集起来（去重、最多 1000 个），分批（每批最多 200 个）去 `documents` 表反查「这份材料属于哪个学生」，查到就把学生名补到行上。
   - 万一线上管理员读 `documents` 的权限不够（反查被拒），就保持「—」，并在控制台提示一次，不会报错、不影响其它内容——这是卡里预先允许的降级（见第五节）。

## 二、动的文件

- `admin-portal/maxhouse-admin-portal-v223.html`（新版）
- `admin-portal/index.html`（三处跳转 v222 → v223）
- `docs/changelog/admin-portal-versions.md`（追加 v223 一行）
- `tests/job23-04-admin-v223.mjs`（随卡静态断言，新）
- `tests/job23-02-admin-v221.mjs` / `tests/job23-03-admin-v222.mjs`（把里面「index 指向某版」这条改成前向稳定写法，理由同 23-03 回执——index 是单一最新指针，每 bump 一版就前移，否则老脚本会永远变红）
- `jobs/JOB-23-04/repro/green.mjs` + `shots.mjs`（无头验证 + 截图脚本，新）

主仓库已提交一笔（commit `41fa1d0`），**未推**。

## 三、旧病由红转绿（对应 23-00 的 R5）

23-00 已量成红：默认视图前 20 行里 fetch 占比 ≥ 50%、fetch 行学生列为「—」。

本卡后，无头真渲染（桩：27 条埋点 + 690 条取件，40 个 doc 全可映射到学生）实测：

| 断言 | 期望 | 实测 |
|---|---|---|
| 进页默认行数 | 27（合规记录） | 27 ✅ |
| 默认视图里有没有取件行 | 无 | 无 ✅ |
| 点「取件」后行数 | 690 | 690 ✅ |
| 点「全部」后行数 | 717 | 717 ✅ |
| 取件行学生列非「—」的行数 | 690（全可映射） | 690 ✅ |
| 校名筛选仍可用（点校名） | = 该校行数 | 14（该校）✅ |
| 中英切换裸键 / 控制台报错 | 0 / 0 | 0 / 0 ✅ |

无头 5/5 全绿（`jobs/JOB-23-04/repro/green.log`）。

## 四、各道闸

- **闸3 静态断言**：`tests/job23-04-admin-v223.mjs` → **26/26 全绿**（版本四件套、默认 nonfetch、_v223LogMatch、新筹码序、hero 注明、反查 documents 的分批/去重/报错降级/补 r.student、school_access_log 装载列未改）。
- **闸8 无头真渲染**：见第三节，中英各一遍、零报错、零裸键。
- **闸9 视觉零新增**：只复用 `.filter-chip` `.filter-bar` `.queue-row` `.wf-hero` `_adminPill`；无新 CSS 类、颜色、字号。
- **N1（授权移除清单）**：本卡**零移除**——只加筹码、改默认与装载逻辑，nav-item / 函数 / 数组 / 容器全不动，差集为空。
- **上游回归**：`tests/job23-01/02/03` 重跑全绿（28 / 35 / 27）。
- **shell-lint**：本包无 shell 脚本。
- **secret-scan**：改动文件全扫，无真实密钥，**clean**。

## 五、留业主决定的一处（§0.5 预授权的降级）

卡里写明：若 admin 读 `documents` 的策略不够，反查会失败，取件行学生名保持「—」。本地 stub 里反查是通的（690 行全补上了名字）；**线上是否通，取决于 admin 账号对 `documents` 表的读策略**。

- 若业主线上进页看到取件行学生名是真名 → 策略够，无需动作。
- 若看到「—」→ 说明 admin 读 `documents` 策略不足，需要新增一条 admin 只读策略（属新增型 DB 操作），**建议并入第二十四轮**由业主确认后做。本卡按边界「不写库」未动数据库。

## 六、要业主亲眼看的（线上验收，留业主）

进「学校行为日志」，默认应只看到导出 / 下载 / 查看这类记录（不含取件）；点「取件」才看到水印流水；取件行应显示学生名（策略不足则显「—」，见第五节）。

## 七、截图

`~/mh-jobs/shots/23/23-04/`：`log-default-nonfetch.png`（默认 27 条 + 新筹码序）、`log-fetch-chipbar.png`、`log-fetch-row.png`（取件行补名，均已模糊）+ `INDEX.md`。

## 八、STOP？

无。本轮暂无 `~/mh-verify/r23-STOP.txt`。

---
下一包 = 23-05
