# JOB-22-03 回执 · 学生端 v400 + 学校端 v235：缩略图续签（学生）+「有新版请刷新」（两端）

CC 执行。动两个端各一个文件（v399→v400 / v234→v235）+ 各自 index.html 重定向 + changelog。
未 push 主仓库（§0.9）。共用查看器 / 云函数 / 迁移一律未碰。学校端只加横幅一段、其它一行不动。

## 一、做了什么

### 学生端 v400（续签 + 横幅）
- **缩略图续签（与中介端 v170 同一套）**：四处 `student-documents` 签名点（自流程 `_348` 批签 / 单签 + 卡679 弹窗批签 / 单签）补记 `f.previewSignedAt=Date.now()`；`renderSubmittedMaterials` 开画时 `_v400MaybeResign()` 扫**当前上下文**（`_getDocumentsContext()`，覆盖自流程与代看）上传件，签发距今 **>50 分钟**或无 previewUrl 的整批 `_v400SignFiles`（`createSignedUrls` 批签 + 单签回退，3600s）续签后重画（`state._v400Resigning` 守卫 + 续签后不再 stale → **不成环**）；本地暂存件（无 filePath）不碰。
- **缩略图 onerror（`_thumbInnerHtml` 唯一图片渲染点）**：从 v345 内联「small→full」改 `_v400ThumbErr`：**先回退原图**（沿用 v345）→ 单件 `_v400SignOne` **重签一次**换 src（`dataset.v400Retried` 守卫只一次）→ 仍失败显**内联 SVG 文件占位**（`onerror=null` 防死循环，非浏览器断图）；带 `data-v400-path`。

### 学校端 v235（仅横幅）
- 22-00 读 + 本轮**再核**：学校端所有 `previewUrl` 皆 FileReader / canvas 的 **dataURL**（本地件，不过期）或 `300/600/60s` **按需短签**，**无**「签发存起来不续签」的持久缩略图 → **无续签可改**，只加横幅，其它一行不动。

### 两端「有新版请刷新」横幅（五端同段代码，与中介端 v170 逐字一致）
载入 3s 后 `fetch('./index.html',{cache:'no-store'})` → 正则 `maxhouse[_-][a-z_-]*portal[_-]v(\d+)` 取**最大版本** vs 当前（`location.pathname` 解析，回退常量 `MH_PORTAL_VER`=400/235）；线上更新 → 顶部固定横幅：**四语**、点去根地址 `'./'`、**绝不自动刷新**、× 关后本会话不再弹、显示时 `body.paddingTop`+`.header top` **顶到横幅下不遮页头按钮**；每 30 分钟 + `visibilitychange` 复查；**取不到 / 解析不出静默**。

## 二、闸3（真页面真登，`jobs/JOB-22-03/probes/gate3.mjs`；日志 `repro/gate3-after.log`）

| 项 | 判据 | 结果 |
|---|---|---|
| 学生续签 | stuA **无真实上传件**（自流程 0 件带 filePath+previewUrl）→ 无法真数据复现；改**注入假 doc + 桩住签名**端到端验接线：`reSigned=true / fresh=true / onerrorResigned=true / placeholder=true` | **GREEN(逻辑桩)** |
| 学生横幅 | 假更高版本 → 横幅出（紫 `rgb(124,58,237)` top=0）；四语逐语正确；页头交集 0px²；同版不出；取不到静默(pageErrors=0) | **GREEN** |
| 学校横幅 | 同上四项 | **GREEN** |

`OVERALL: ALL GREEN`。
- 学生续签的**同族逻辑已在 22-02 中介端 v170 真数据（partner1 16 件）证过绿**（`_v400*` 与 `_v170*` 行对行同构）；stuA 缺真件按 N2 记「未复现·逻辑桩验接线」。
- 学生/学校真页面真登**零新报错**（gate3 各 context `pageErrors=0`）。

## 三、N 闸
- **N1**：学生 v399→v400 移除 **0** / 新增 **11 函数**（`_v400*` 6 + `_mh*` 5）；学校 v234→v235 移除 **0** / 新增 **5 函数**（`_mh*` 横幅）。`DIFF-REMOVED-TOTAL 0`，零死按钮。（`jobs/JOB-22-03/inv-student-v400.json` / `inv-school-v235.json`）
- **P3 随卡 jsdom/vm 断言**（`tests/job22-03-student-school.mjs`）：**35/35 ALL PASS**（含两端 md5、div 平衡与基线同=1、index 四处同版、四语齐、续签四签名点/onerror/占位、学校端无续签代码、内嵌 script 与基线同 6/7——唯一不解析块系既有 Cloudflare beacon 片段·非本卡引入·v399 基线同 6/7）。
- **四语逐键**：gate3 两端四语 + P3 两端四条文案实证。
- **shell 卫生**：本包新增 `.mjs` 无 shell；`shell-lint.sh` **全绿（0 命中）**。
- **测试写库**：零上传 / 零提交 / 零写库——仅内存态编辑（改 previewUrl/signedAt / 注入假 doc）+ 本地服务器 + fetch 路由桩；桩签名不碰真存储。
- **secret-scan**：push 前扫 CLEAN。

## 四、截图（元素级 · 无 PII，调试期不推）
`~/mh-jobs/shots/22/22-03/`：`STU-version-banner.png` / `SCH-version-banner.png`（登录后触发、只截横幅条）+ `INDEX.md`。横幅登录后正常 1280×41；登录门阶段 body 未起来时零尺寸不显（业主 ④ 场景为登录态旧标签页，不受影响）。

## 五、要业主亲眼看的（线上验收）
- 学生端：标签页久开后缩略图仍在（自动续签）；顶部横幅只在真出新版时出、点它才刷新。
- 学校端：顶部横幅同上；学校端本就无久开断图问题（本地/短签）。

## 六、版本四件套
- 学生：①文件名 v400 ②[VER] v400（+VER-prev399）③头注= [VER]（本端无独立头注版本行）④index 四处 v400。
- 学校：①文件名 v235 ②[VER] v235（+VER-prev234）③L2 头注 v235 ④index 四处 v235。changelog 各追加一行。

## 状态
- 无「必须停」触发。主仓库两端 html + 两 index.html + 两 changelog + `jobs/JOB-22-03/` + `tests/job22-03-student-school.mjs` 点名 add + commit，**未 push**（§0.9）。
- 本回执 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**：闸3 全绿、N1 零移除、P3 35/35、四语齐、shell-lint 绿、secret-scan 绿。视觉件线上肉眼验收留业主。

下一包 = 22-04
