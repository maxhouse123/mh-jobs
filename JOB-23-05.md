# JOB-23-05 回执 · 管理端 v224：任务审批/信息更正补侧栏角标 + 三个材料队列加「隐藏测试」筹码

做这一步的人：CC（无人值守）。只动管理端一端，版本 v223 → v224（旧版留着可回滚）。

## 一、做了什么（大白话）

1. **侧栏「任务审批」「信息更正」补上待办数角标**。
   - 其它待办队列侧栏都有一个数字角标，只有这两个没有。现在照同款写法各加一个：任务审批显示待批数（桩里 2 个）、信息更正显示待审数（桩里 3 条 pending）。
   - 这两个数字进页前就先在后台拉一次（首屏挂载），不用等点进去才更新；点进去也照样刷新。

2. **材料派单 / 材料审核 / 待二审 三个队列加「隐藏测试」筹码**。
   - 学生/学校/中介列表早就有「隐藏测试」筹码，这三个材料队列没有。线上这三个队列里堆满了 6–7 月的测试件。
   - 现在三个队列筹码行各加一个「隐藏测试」（默认按下），把测试学生的件藏起来，页顶小字写「已隐藏测试 N 份」。关掉筹码，测试件就全出来。
   - 判定规则：这一行的件属于哪个学生，如果这个学生在名册里标了「测试」就藏；**如果找不到这个学生、或学生名册还没加载好，就不藏**（宁可多显示也不误藏真实件）。
   - 侧栏那几个角标仍然是真总数，不随这个筹码变（角标是给你看全量待办的）。

## 二、动的文件

- `admin-portal/maxhouse-admin-portal-v224.html`（新版）
- `admin-portal/index.html`（三处跳转 v223 → v224）
- `docs/changelog/admin-portal-versions.md`（追加 v224 一行）
- `tests/job23-05-admin-v224.mjs`（随卡静态断言，新）
- `jobs/JOB-23-05/repro/green.mjs` + `shots.mjs`（无头验证 + 截图脚本，新）

主仓库已提交一笔（commit `8925853`），**未推**。

## 三、旧病由红转绿（对应 23-00 的 R6、R7）

23-00 已量成红：R6 侧栏 `#navBadgeTaskApproval` / `#navBadgeCorrections` 不存在；R7 三队列无「隐藏测试」筹码、测试件在列。

本卡后，无头真渲染（桩：10 学生含 3 测试；三队列各含测试件）实测：

| 断言 | 期望 | 实测 |
|---|---|---|
| 任务审批角标 | 2 | 2 ✅（注入登录态后，见第五节） |
| 信息更正角标 | 3（pending 数） | 3 ✅（首屏自然） |
| 派单默认行数 < 关筹码后 | 是（有测试被隐） | 7 → 10 ✅ |
| 材料审核默认行数 < 关筹码后 | 是 | 1 → 4 ✅ |
| 待二审默认行数 < 关筹码后 | 是 | 1 → 4 ✅ |
| 三队列小字「已隐藏测试 N 份」 | 显示 | 显示 ✅ |
| 侧栏角标是否随筹码变 | 不变 | 不变（派单 32/32、审核 4/4）✅ |
| 清空学生缓存后（隐藏仍开） | 三队列 = 全部（不误隐） | 派单 10 / 审核 4 / 待二审 4 ✅ |
| 中英裸键 / 控制台报错 | 0 / 0 | 0 / 0 ✅ |

无头 9/9 全绿（`jobs/JOB-23-05/repro/green.log`）。

## 四、各道闸

- **闸3 静态断言**：`tests/job23-05-admin-v224.mjs` → **30/30 全绿**（版本四件套、两新角标 id、_v224SetNavBadge、两装载器设角标、首屏挂载两 loader、_v203HideTest 加 docs、_v224IsTestDoc、三队列各加 _v203TestChip('docs') 与 _v224IsTestDoc 过滤与已隐藏小字、nav-item 仍 27、RPC 名未动）。
- **闸8 无头真渲染**：见第三节，中英各一遍、零报错、零裸键。
- **闸9 视觉零新增**：只复用 `.nav-badge` `.filter-chip` `.filter-bar` `.filter-divider`；无新 CSS 类、颜色、字号。
- **N1（授权移除清单）**：本卡**零移除**——只加两个角标 span + 三个筹码 + 过滤逻辑，nav-item 仍 27、函数/数组/容器全不动，差集为空。
- **上游回归**：`tests/job23-01/02/03/04` 重跑全绿（28/35/27/26）。
- **shell-lint**：本包无 shell 脚本。
- **secret-scan**：改动文件全扫，无真实密钥，**clean**。

## 五、一处 stub 环境限制（不影响线上，已如实标注）

「任务审批」角标走 RPC `get_admin_pending_tasks`，这个 RPC 需要当前登录管理员的 uid。本地无头 stub **没有建真实登录会话**，`getUser()` 返回空 → uid 取不到 → 首屏那次装载会提前退出、角标暂时是「—」。这是这条 RPC **本来就有的 uid 依赖**（非本卡新增）。

- 「信息更正」角标不需要 uid，首屏自然就设成 3，无此问题。
- 无头验证时**注入了一个 uid 模拟登录态**再调一次装载器，角标随即变 2，证明「角标 = 缓存长度」这套逻辑本身是对的。
- **线上是真登录、有会话**，uid 天然存在，角标会自动显示真实待批数（线上两个 locked 任务应显示 2）——留业主线上一眼确认。

## 六、要业主亲眼看的（线上验收，留业主）

- 侧栏「任务审批」显示待批数、「信息更正」显示待审数。
- 材料派单 / 材料审核 / 待二审 页顶有「隐藏测试」筹码（默认按下），关掉后测试件出现，页顶有「已隐藏测试 N 份」。

## 七、截图

`~/mh-jobs/shots/23/23-05/`：`sidebar-badges.png`（两新角标 2 / 3）、`docs-hide-test.png`（材料审核台隐藏测试筹码 + 已隐藏 3 份）+ `INDEX.md`。

## 八、STOP？

无。本轮暂无 `~/mh-verify/r23-STOP.txt`。

---
下一包 = 23-06
