# JOB-10-02 · 水印预渲染「通电」检查 + 修

结果：**停在方案（按卡「必须停」+ 铁律14·P2）—— 已查清病根、给出可一句话授权的修法，未动手改库/改 EF/改部署**

## 大白话（业主看这段就够）
第九轮建的「审核端水印预渲染缓存」现在是**白装的**——它一次也没成功过。原因：给它发指令的三个数据库触发器只带了「暗号」，没带「门禁卡」（Authorization 头）；而 watermark-doc 这只云函数部署时把「门禁」打开了，于是网关在函数还没开始跑之前就把这些指令挡在门外，回一句「缺 Authorization 头」。所以预渲染从来没发生，审核端一直走「现场渲染」的回落路（**功能不坏，只是这层缓存白建**）。

我实测了两次：
1. 照现在触发器的发法（不带门禁卡）→ 网关回 401「缺 Authorization 头」。
2. 我把前端那把**公开**的 publishable 密钥当门禁卡带上 → 网关放行了，请求进到了函数里（函数又因为我故意给了假暗号而拒绝）。

这第 2 点很关键：它证明**只要在三个触发器里加一行「带上这把公开密钥」，就能通电**，不用改云函数、不用改部署参数。这是个纯数据库的小迁移（0194）。

**但是**：卡里写死了「门禁卡必须是 `eyJ…` 开头的老式 JWT」，而咱们五端早就全换成 `sb_publishable_` 新式公钥了，库里、归档里都找不到老 JWT。卡遇到这种情况的指令是「停下写方案」。加上启动指令里 10-02 的「必须停」红线，我**停在这里**，把修法摆好，等你一句话授权再应用。

## 断言表
| 检查 | 断言 | 实测值 | 判定 |
|---|---|---|---|
| net._http_response 近 6h | 有打到 watermark-doc 的 401 记录 | 20 条 `401 UNAUTHORIZED_NO_AUTH_HEADER / Missing authorization header` | ✅ 通电失败已实锤 |
| curl A（不带 Authorization，复现触发器发法） | 网关 401 | `HTTP 401 {"code":"UNAUTHORIZED_NO_AUTH_HEADER"...}` | ✅ 复现 |
| curl B（`Authorization: Bearer sb_publishable_…` + 假暗号） | 若网关放行→回 EF 层错误 | `HTTP 401 {"error":"unauthorized"}`（EF 自身响应，非网关 code） | ✅ 网关放行，publishable 密钥可作 Bearer |
| 前端 anon 密钥形态 | eyJ… JWT？ | 五端均 `sb_publishable_7kx…`（新式公钥，非 eyJ） | ⚠️ 与卡前提冲突 |
| 库/归档旧 anon JWT | 存在？ | 无 config.toml；全仓无 eyJ 形态 anon JWT | ❌ 找不到 → 卡令「停」 |

> 说明：curl B 用了**不存在的 doc_id**（全 0）和**假暗号 DUMMY**，故**没有渲染任何真实学生材料**，也没往 wm-cache 桶写任何东西——只探网关这一层。真暗号全程未使用、未出现在任何文件/回执/命令里。

## 判定
网关拦截三个 fanout 调用 → 预渲染永不发生 → 审核端一直走原「查看」回落（第九轮说的「功能不坏但白装」被证实）。

## 修法（两个选项，待业主授权其一）

**选项① · 推荐 · 纯迁移，不碰 EF/部署**
迁移 `0194`（全 `create or replace`，无 drop）：三个 fanout 函数的 headers 里各加一行
`'Authorization','Bearer sb_publishable_7kx6JBztyd5nzSqzSgW4MA_HavI0Wnr'`（这把是**公开**的前端 publishable 密钥，入库无泄密风险）。暗号照 0193 原样保留。
- 已实测该密钥能越过网关（curl B）；配上真暗号，EF 即可正常预渲染。
- 应用后自验：同一对真实**测试**学生 doc + 测试审核员 `curl {mode:'prepare'}` → 200 `{ok,cached}`；wm-cache 桶出现 `<reviewer_uid>/<doc_id>.<ext>`；再 `cleanup` 删掉验证消失。
- 方案稿（打码）已落 `jobs/JOB-10-02/0194_wm_auth_PLAN_masked.sql`（Bearer 用公钥；暗号打码 `__MH_HOOK_SECRET__`，应用时从库内现役函数体原样复制，无需重打）。
- **为何停而不直接应用**：卡把 0194 的 Bearer 值限定为 eyJ JWT，与现实（新式公钥）冲突；铁律14·P2 规定「图纸与现实冲突、无明文预授权映射 → 停工报告，不得自修」，且启动指令 10-02 标了「必须停」。你回一句「用选项①，应用 0194」我就照办（跑前跑后函数数 3→3、含 Authorization 函数数 0→3、rollback 用 pg_get_functiondef 原样恢复）。

**选项② · 卡的 STOP 分支原方案（改动更大，我不推荐）**
把 watermark-doc 部署改为 `--no-verify-jwt`，让 EF 自己用 `auth.getUser` 校验审核员 JWT。
- 需改 EF 源码 + 重新部署（碰 EF/部署参数）——属启动指令「必须停」范畴，且改动面比选项①大得多。仅在你不愿把 publishable 密钥写进 DB 函数体时才选。

## 边界遵守
- 未改 EF 源码、未改部署参数、未应用任何迁移、未渲染真实学生材料、未动 notify/invite 触发器。
- 暗号零出现（curl 全程用 DUMMY / 公钥）。
- 未做「修后验」(卡步4) 与「读侧」(卡步5)——二者须在修应用后才有意义；读侧另需测试审核员账号，留业主在审核端 console 看 `[v34] wm cache HIT`。

## 动过的文件
- 新增（只读探针 + 方案稿，均未应用）：jobs/JOB-10-02/{probe_net.sql, probe_wm.sql, 0194_wm_auth_PLAN_masked.sql, report.md}
