# JOB-16-09 收官回执（第十六轮末包）

## 一、做了什么

1. **收尾自检（§0.10 收官条件逐项过）**
   - 迁移账本：`bash jobs/ledger-check.sh` → **LEDGER OK（差集 0）**；正本/镜像各 196 编号；0207/0208 两端都在，0209 按裁决空号（不做）。
   - 变更端 changelog 每版一行：reviewer v39/v40、school v233、admin v214/v215、student v395/v396、partner v161/v162/v163、edge-functions（precheck-ai v22 / watermark-doc v33）——逐个 `grep -c` 命中。
   - 工作树：`git status --porcelain --untracked-files=no` = 空（tracked 干净）。
   - 本地领先远端 6180893 的提交 = 9 条，全为本轮 JOB-16-xx（清单抄进 SUMMARY-16 §八），主仓库 CC 不推。
   - secret-scan：`node jobs/JOB-16-00/secret-scan.mjs` 对主仓库待推提交 + `~/mh-jobs` 全树 = **SECRET-SCAN-CLEAN**。

2. **测试残留清点（N3 带身份探针，`jobs/JOB-16-09/residue-probe.sql`，只读三段 begin;...commit;）**
   - 邮箱取自 QUEUE-16 §0.4（非密码），uid 先以默认角色查进自定义 GUC `mh.uid` 再切 `authenticated`（authenticated 无权读 auth.users）；每段回显 `auth.uid()` 核对身份。
   - 结果（每条都带身份回显）：
     - **学生A** uid 43668ae4…：docs 活 15 / 软删 6；**本轮测试特征件活着的 = 0**。
     - **reviewer1** uid 80c62372…：`reviewer_doc_queue` 60 行；**259f456a present=false**；队列内测试件 = 0。
     - **partner1** uid 1aad823a…：`named_invites` 共 10（sent6/claimed1/opened1/expired1/revoked1），**与开工一致**。

3. **线上走查脚本（业主卡1119 末尾跑）**
   - `jobs/JOB-16-09/online-walk.sh` + `online-walk.mjs`：用 `~/mh-verify` 的 Playwright、Chromium 带 `--no-proxy-server`；全程人话 echo；**跑完一律 exit 0**，红绿写进 `~/mh-jobs/JOB-16-09-online.md`（不让业主的卡因走查红而中断）。
   - 做三件事：① 每 20 秒轮询线上 学生端/中介端 index 直到指向 v396/v163（最多 10 分钟，超时照实写）；② 学生A 真登线上学生端——[VER]=v396｜CV 槽无 mh10｜在 **recommendation 槽**（批次1、不锁死）传合成图（出原因框就填一句）→ 上传成功→控制台 `ai now 1 sent 1 got` + 网络 precheck-ai=200｜刷新仍在｜界面删除；③ partner1 真登线上中介端——[VER]=v163｜「邀请学生自传」按钮（`#dlInviteBtn`）可见且点得开（不提交）｜页面邀请数 = truth。
   - 不写死线上地址：`MH_WALK_STUDENT/MH_WALK_PARTNER` 可覆盖；`MH_WALK_DRY=1` 起本地静态服务把「线上地址」指到本地新版页面做干跑（干跑不真传不真删，只彩排定位）。

## 二、结果是什么

- **闸3 判据全过**：
  - `bash -n jobs/JOB-16-09/online-walk.sh` → **SH-SYNTAX-OK**；`node --check jobs/JOB-16-09/online-walk.mjs` → **MJS-SYNTAX-OK**。
  - **本地干跑（MH_WALK_DRY=1，线上地址指本地服务 + 新版页面）走完并写出回执草稿**，10 项判定全绿：
    ```
    [绿] ① 学生端 index 指向新版 (maxhouse_student_portal_v396)
    [绿] ① 中介端 index 指向新版 (maxhouse-partner-portal-v163)
    [绿] ② [VER]=v396 命中
    [绿] ② CV 槽无 mh10-test
    [绿] ② recommendation 上传输入框已定位(干跑不真传)
    [绿] ② 学生端 0 未捕获报错
    [绿] ③ [VER]=v163 命中
    [绿] ③ 「邀请学生自传」按钮 可见且点得开(不提交)
    [绿] ③ 页面邀请数 = truth(10)
    [绿] ③ 中介端 0 未捕获报错
    ```
  - `grep -cxF -e 'PUSH_OK=yes' -e 'PUSH_OK=no' SUMMARY-16.md` = **1**（首行顶格 `PUSH_OK=yes`）。
- **SUMMARY-16.md 已写**：五端版本+md5、迁移清单、两只 EF md5 与部署状态、每包一句话、10 项旧账逐项红→绿文件名、收尾残留清点、未放行清单、第十七轮建议包，首行 `PUSH_OK=yes`。
- **`PUSH_OK=yes` 依据**：各包闸3 与新闸全过，未过项均已标「未放行」且不影响其它功能；N1 差集逐包为 0；secret-scan 干净；工作树 tracked 干净、ledger OK。
- 干跑期间的 `~/mh-jobs/JOB-16-09-online.md` 是 **DRY 草稿**（顶部标「本地干跑 DRY」），业主卡1119 的 live 跑会**覆写**为真红绿版本后 commit+push；本收官提交**不含**该 online 回执（留给 live 跑）。

## 三、业主要做什么

- 本包无需现在操作。收官后：
  1. **卡1118**：认本轮 `SUMMARY-16.md` 首行 `PUSH_OK=yes`，在原生终端挂代理推主仓库（9 条 JOB-16-xx）。
  2. **卡1119**：部署 precheck-ai v22（jobs/JOB-16-06 部署脚本）+ watermark-doc v33（jobs/JOB-16-08/deploy-watermark.sh，JWT 校验保持开），末尾跑 `bash jobs/JOB-16-09/online-walk.sh`（关代理），把线上真红绿收进 `~/mh-jobs/JOB-16-09-online.md`。
  3. **手验未放行 6 条**（见 SUMMARY-16 §七）：放大缩小 school/admin 开图、中介代传提交落库、admin 时间线崩红形状、旧 894KB 件重传一次才出瘦身行。

## 新闸证据（N 闸）

- **N1**：本包不升端口版本（收官包），无差集需跑；本轮各端升版的 removed=0 见各包回执。
- **N2**：线上走查脚本即为「真页面真点、先红后绿」的载体——真填登录表单 + `locator.click()` / `setInputFiles`（Playwright 上传正路，非 evaluate/dispatch）；判据只认屏幕文字（[VER]/按钮/邀请数）与网络响应（precheck-ai 200）；真红绿在业主 live 跑出，本地干跑已证脚本控制流走得通、10 项全绿。
- **N3**：残留清点三段各回显 `auth.uid()` 对上测试账号；无身份的「0 行」未当证据；partner1 邀请数 10 与走查③ 的 truth 对齐。
- **N4**：本包不改云函数（EF 鉴权矩阵在 16-06/16-08）；online-walk 学生端走查含用真登会话打 precheck-ai 的鉴权路验证（live 跑执行）。
- **N5**：secret-scan 对待推与 `~/mh-jobs` 全树 CLEAN；residue-probe 用公开邮箱、不打印密码/token；本包无新增公开截图（干跑截图仅落本机 `~/mh-verify`）。
- **N6**：只做本文件规定的收官事（自检 + 残留清点 + 走查脚本 + SUMMARY）；SUMMARY-15 建议包 C–G 顺延不做，记入第十七轮建议包；新发现（REJ-1 / 邀请按钮门控）只记不修。

下一包 = 无（第十六轮收官）
