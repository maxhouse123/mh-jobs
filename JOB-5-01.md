# JOB-5-01 回执 · 登录预取的 422（2026-09-16 续做）

> 大白话版。昨天缺密码停在门口，今天密码到位（stuA / 实测可登），把「真登学生端抓 422」这一步补做完了。**结论：那个 422 不在学生端。** 按本轮硬指令「不在学生端就写回执并停、不要猜」，我停在这里，等你一句话授权再查别的端。**主仓库没动、没推；本回执推日志仓库 mh-jobs。**

## 做了什么（一句白话）
用 Playwright 拿 stuA 真账号登学生端 v380（本地 `file://`，真连 Supabase），把登录**之后**的所有网络返回全抓下来，长等 15 秒收全延迟预取，专找那个报 422 的请求。

## 探到的（硬事实）
1. **学生端登录后：0 个 4xx**（排除未登录态 401）。
2. 登录成功、gate 正常解锁；抓到登录后**每一条** supabase 请求的状态，全绿：
   - `student_bootstrap` → **200**（20 keys，~1.0–1.4s）
   - `ai_precheck_for_docs` / `close_my_round` / `get_my_offers` / `get_unlocked_offer_school` / `record_referral_signup` → 全 **200**
   - `students` PATCH → **204**；文件签名 `storage/.../sign/...` GET → **200**
3. 我昨天的头号嫌疑是 `student_bootstrap`（登录后第一发、失败静默回落，症状「报错但不影响功能」对得上）——**实测 200，不是它**。

## 结论
**那个 422 不在学生端。** 它是在**别的端**登录后才出的（总检验腿 2 说的「某个端」不是学生端）。

## 为什么停（不是漏做）
本轮指令白纸黑字：**「如果 422 不在学生端（在别的端登录后才出），写回执说明并停，不要猜。」** 学生端已实测干净，我不猜是哪个端、更不盲改任何端。

## 往下走需要你一句话
- 我已顺手实测：**schx（学校端）用同一密码可登**（reviewer1 / admin 未试）。
- 给我一句「**查学校端**」（或指定 reviewer / admin），我就把同一套抓包对准那个端：真登 → 抓到 422 → 看它是哪个 RPC/REST、传了什么、服务端拒的理由（PostgREST 422 一般是参数类型/缺参）→ 改前端传参或删掉这个没用的预取 → 再登一次断言该端 0 个 4xx → 那个端版本 +1。

---
十道闸：未改代码，不适用（本包只探未改）。
五端版本：全部未动（admin v202 / student v380 / partner v148 / school v229 / reviewer v31）。
提交哈希：无代码改动。主仓库未推；本回执 + SUMMARY-5 推日志仓库 mh-jobs。
探针脚本：`~/mh-verify/capture_job5_01.mjs`（真登抓 4xx）· `capture_job5_01_full.mjs`（全量 supabase 请求清单，15s 长等）· `probe_auth_job5_01.mjs`（确认 stuA/schx 均可登）。密码全程只经环境变量内联单次传入，未写进任何文件。
偏离或疑点：无。学生端确定干净；422 待在其它端定位，等你授权。
