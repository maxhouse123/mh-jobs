# JOB-5-01 回执 · 登录预取的 422（2026-09-16 三端排查完）

> 大白话版。密码到位后，按你给的顺序真登了**学生 / 学校 / 审核**三端抓包。**三端登录后全干净，0 个 4xx，没一条 422。** 你不让我碰管理端（没给密码），所以我停在这里。**按排除法，那个 422 只可能在管理端**——就是 9 月 9 日总检验腿 2 登过、我唯一没测到的那个端。**没改任何代码、没 bump 版本、主仓库没推；本回执推日志仓库 mh-jobs。**

## 做了什么（一句白话）
用 Playwright 拿真账号依次登三端（本地 `file://`，真连 Supabase），把登录**之后**的所有网络返回全抓下来，专找那个报 422 的预取请求。三端账号密码都实测可登。

## 三端结果（全绿）

| 端 | 版本 | 登录 | 登录后 4xx | 预取请求（全 200/204） |
|---|---|---|---|---|
| 学生端 | v380 | ✅ stuA | **0** | student_bootstrap(20keys) · ai_precheck_for_docs · close_my_round · get_my_offers · get_unlocked_offer_school · record_referral_signup · students PATCH 204 · 文件签名 GET |
| 学校端 | v229 | ✅ schx | **0** | school_bootstrap · get_task_candidates ×3 · admission_tasks_lite · offer_decisions · school_quota_requests · tier_quota · schools |
| 审核端 | v31 | ✅ reviewer1 | **0** | is_reviewer · reviewer_doc_queue(61 条) · get_my_notifications · **watermark-doc EF ×3(全 200**，仅慢：7.2s/TTFB 5.4s，但不是 422) |

- 学生端等 15s、学校端等 20s、审核端等 25s（覆盖 `_wmPrefetchQueue` 登录后 +1.5s 启动的水印预取）——延迟预取全收进来了，仍 0 个 4xx。
- 审核端全程**未见任何学生 PII 请求**，双盲没破；本包只读抓包，未动审核端一行代码。

## 结论（诚实·按排除法）
**那个 422 不在学生端、不在学校端、不在审核端。** 9 月 9 日总检验腿 2 登的是**审核员 + 管理员**两个端；审核端已实测干净 → **剩下的唯一可能是管理端**。我按你「不查管理端（密码不给）」的指令停手，不猜、不盲改。

**顺带一个可能性**：管理端本轮已从 v201 bump 到 **v202**（JOB-5-02 改了一处 flex-wrap）。若那个 422 恰好在管理端登录后的某个预取里，它可能仍在 v202、也可能已随版本变化。**要确认只有两条路**：
1. 你给我一个管理端测试账号密码，我把同一套抓包对准 admin v202，10 分钟内定位+修+断言 0 个 4xx（+版本）。
2. 你自己在原生浏览器登管理端、F12 看 Network 里那条 422 是哪个请求，把请求名/参数/响应贴给我，我照单改。

---
十道闸：未改代码，不适用（本包三端只读抓包）。
五端版本：全部未动（admin v202 / student v380 / partner v148 / school v229 / reviewer v31）。
提交哈希：无代码改动。主仓库未推；本回执 + SUMMARY-5 推日志仓库 mh-jobs。
探针脚本：`~/mh-verify/capture_job5_01_full.mjs`（学生）· `capture_job5_01_school.mjs`（学校）· `capture_job5_01_reviewer.mjs`（审核）· `probe_auth_job5_01.mjs`（确认账号可登）。三端密码全程只经环境变量内联单次传入，未写进任何文件。
偏离或疑点：无。三端确定干净；422 按排除法在管理端，等你给 admin 密码或自查贴回。

---
## 最终结论（2026-09-16 结案）
业主亲自在浏览器控制台挂 fetch/xhr 探针（登录前挂上）、真登**管理端 v202**、等 30 秒收全预取——**登录后 0 条 4xx（不含 401），所有云端装载全部正常**。至此**四端（学生/学校/审核/管理）全部实测干净**。

那个 422 是 **9 月 9 日总检验腿 2 在管理端 v162–164 时代**记下的；从那时到本轮 **v202** 之间已被修掉，**现已不存在**。

**JOB-5-01 以「已不存在，关闭」结案，无任何代码改动、无版本 bump、主仓库未动。**
