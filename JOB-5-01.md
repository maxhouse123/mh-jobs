JOB-5-01  结果：**只探未改（没有密码，只能抓未登录页；422 是登录后才发的预取，需密码复现）**

## 做了什么（一句白话）
用 Playwright 依次打开五个端的登录页（管理/学校/学生/中介/审核），监听所有网络返回，找那个报 422 的预取请求。

## 探到的（诚实）
1. **五个端登录页（未登录态）：0 个 4xx**（含 0 个 422）。
2. 更关键：**登录页在登录之前根本不向 Supabase 发任何请求**——我把网络放开抓了整轮，五个端只加载了本地的 vendor/supabase.js，没有一条打到云端 API。
3. 结论：那个 422 **不是登录页加载时发的，是"登录成功、拿到会话之后"的预取请求**（总检验说的「登录时」= 登录动作完成后那一下）。要复现它，必须真登进去——**而我没有测试账号密码**。

## 为什么没修
- 按队列规矩「没有密码就只做未登录页面的抓取，写明」——我照做了。
- 422 在登录后才发，未登录抓不到；抓不到就无法定位是哪个 RPC/哪个参数，也就无法验证「修后 0 个 4xx」（闸3 判据要求登进去再抓一遍）。**没定位就改是盲改，违背铁律。**

## 要你给一样东西
**任一端的测试账号密码**（学生 maxhouseapp+stuA / 学校 maxhouseapp+schx / 审核 reviewer1@maxhouse.test / 管理 maxhouseapp@gmail.com 任选其一即可）。
拿到后我会：真登一次 → 抓到那个 422 → 看它是哪个 RPC/REST、传了什么、服务端拒的理由 → 改前端传参或去掉这个没用的预取 → 再登一次断言 0 个 4xx。

十道闸：未改代码，不适用。
五端版本：全部未动（仍 admin v201 / school v228 / student v379 / partner v147 / reviewer v31）。
提交哈希：无代码改动。
探针脚本：`~/mh-verify/explore_job5_01.mjs`（抓 4xx）+ `explore_job5_01b.mjs`（确认零 supabase 请求）。
偏离或疑点：见上——422 是登录后预取，无密码不可复现；这不是漏做，是缺一个前置条件（密码）。
