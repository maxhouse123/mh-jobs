# JOB-13-04 · 上传时 PDF 瘦身（student v392 / partner v157）

## 断线复核（卡1095 要求）
- 主仓库工作区干净（`git status --porcelain --untracked-files=no` 空）；`667e35c..HEAD` 有 13-01/13-02/13-03 各 commit；~/mh-jobs 干净、JOB-13-00～03 四份回执均已 push（`git -C ~/mh-jobs log --oneline` 可见）。三项全对，无需补齐。

## 做了什么
- 新增共享脚本 `assets/mh-pdf-slim-v1.js`（一套核心，student/partner/存量工具都用它）。上传时如果文件是 PDF 且 >600KB：
  - **单页** → 转成 JPG（长边 2600 像素、质量 0.88），改名 .jpg，之后当图片走（Q1 检查、AI 预检）；转出来不比原文件小就保留原 PDF。
  - **多页（2–15 页）** → 每页渲染（长边 2200、质量 0.82）用 pdf-lib 按原页尺寸重组成新 PDF；新文件 ≤ 原文件 70% 才采用，否则原样。
  - **>15 页 / 加密 / 损坏 / 解析失败 / 超 25 秒 / 任何异常** → 原样上传（fail-open），控制台记 `pdf slim skipped <原因>`。
- 自托管库：pdf.js 已有（复用端上 `_ensurePdfJs`）；新下载 **pdf-lib@1.17.1** 放 `assets/vendor/pdf-lib.min.js`。
- 两端唯一上传收口 `_mhCompressUpload` 里，在图片压缩线之前加了这段 PDF 拦截，所以暂存/草稿/真上传/size_kb/AI 预检看到的都是瘦身后的文件。
- 进度条「正在压缩 PDF 第 n/N 页…」四语 + 完成 toast「已瘦身 X→Y · N 页」四语（学生走 t() 键 `sup.pdfSlim*`，中介照它自己的内联四语口径）。

## 结果是什么（命令带出处）
- **pdf-lib 取库**：`curl cdnjs pdf-lib@1.17.1` 成功，525,099 字节，md5 `dec481b1abd5119d3fbbd2695bdaebb1`（`md5 -q assets/vendor/pdf-lib.min.js`）。一次就成，没走备用源。
- **共享核心闸3**（`node jobs/JOB-13-04/gate3-pdfslim.mjs`，Playwright 造夹具 F1-F5 存 `jobs/JOB-13-04/fixtures/`）：`RESULT=GATE3-PASS`
  - F1 三页扫描件 1.73MB → 采用，比 0.52（≤0.70）、页数 3、第1页渲染长边 2200。
  - F2 单页扫描 1.65MB → 出 .jpg，长边 **2600**。
  - F3 文字 PDF 896B（<600KB）→ 原样。
  - F4 20 页 654KB → 原样，`console: pdf slim skipped >15pages(20)`。
  - F5 截断坏 PDF 720KB → 原样，`console: pdf slim skipped parse:Invalid PDF structure`。
  - **清晰度**（用替代法，因无头环境不便跑 Tesseract）：F1 输出第 1 页渲染长边=2200（≥2000）且拉普拉斯方差 756 ≥ 原页同尺寸方差 819 的 50%（=409.5）→ 达标（回执写明替代法）。
- **两端端侧闸3**（`node jobs/JOB-13-04/gate3-portals.mjs`）：`RESULT=GATE3-PASS`——两端加载 0 真实报错、`window.mhPdfSlim` 挂上、四语进度文本齐（zh/en/ru/fr 都出真文本含页码，无裸键）、390px 进度条 scrollWidth=390 不溢出、真跑 `_mhCompressUpload`：单页 f2→`stock2.jpg`(image/jpeg)、多页 f1→`stock1.pdf` 比 0.52。
- **主文件体积增量**（gzip）：学生 +1193 字节、中介 +782 字节，均 ≤10KB（`gzip -c | wc -c` 前后对比）。
- 版本四件套：文件名 v392/v157、[VER] console 各切新版（旧 v156 探针行已删）、index 重定向各 4 处切新版、changelog 各加一行。（两端头注自 v350/v108 起统一指向 changelog，故只改 changelog。）

## 业主要做什么
- **推送后**用测试账号在学生端 v392 传一个 >600KB 的 PDF → 应看到「已瘦身 X→Y · N 页」提示，控制台有 `[v392] pdf slimmed` 行；单页扫描 PDF 会变成 .jpg。中介端 v157 同理。
- 本包只改上传瘦身，不动图片压缩线、不动 school 端、不动云函数、不动上传闸门。
