# JOB-16-01 回执 · 放大缩小重做(甲案) MHViewer2 装进三端

审核端 v38→v39 / 学校端 v232→v233 / 管理端 v213→v214；新组件 `assets/mh-viewer-v2.js`（md5 `5f84a4ee0fd239d46c177b75ec346f1d`，从工具箱原样 cp，未改）。旧 `assets/mh-viewer-v1.js` 留盘不动。

---

## 一、做了什么（大白话）
把三端「点了没反应、图还会跑出框」的旧放大镜，换成新的「PDF 阅读器式」放大镜：顶上一整行工具条（−、百分比、＋｜整页、适宽、1:1｜旋转），支持滚轮以鼠标为中心放大、触控板双指、拖动（拖不出框）、双击整页⇄100%、键盘。三端改的都是「图片预览」那一处，PDF 预览、视频、退回/通过等一律没碰。

## 二、结果是什么（先红后绿，真页面真点）
**审核端 v39（本轮主证，真页面 + reviewer1 真登 + 真材料图，真鼠标点）：**
- **先红（旧版 v38）**：打开一张图片材料，v1 工具条的「＋」按钮**在**（找得到），真鼠标点两下 → 图片宽度 227px → 227px **纹丝不动**（`imageGrewOnClick:false`, `RED:true`）——和业主说的「点工具条没反应」一模一样。Chrome、Safari 两内核都红。
- **后绿（新版 v39）**：同样打开真材料图 → `checkViewer` **28 判全过 / 0 不过 / 1 跳过（无旁边文字框）/ 0 页面报错**，Chrome、Safari 两内核都绿。点「放大」644→694px 真变大、百分比与实测一致、1:1=2104px、滚轮以指针为中心、拖不出框、旋转宽高对调、双击整页⇄100%、键盘全灵。
- **四条附加断言**：①连开三张图片→每次只有 1 套工具条、关掉后 0 残留（`destroyAll` 生效）✅；②打开 PDF→仍走 PDF 预览、无 MHViewer2 ✅；③视频卡仍能播（有 video 元素）✅；④390px 宽→工具条**一行**（distinctTops=1）、**页面无横向溢出** ✅，但审核端预览弹窗在 390px 视口下只有 **274px** 宽，工具条实际需 282px → **右缘旋转键被裁掉约 8px**（仍在一行、仍点得到，中心在框内）。这是弹窗窄、非组件本身，组件自带 ≤480px 压缩档已生效；不为这 8px 去改动已双内核自测通过的组件 md5，**记入第十七轮建议包**（超窄宿主用 container-query 再压缩）。

**学校端 v233 / 管理端 v214：代码四件套已核 + 组件与审核端逐字节同源，但真页面开图这一步本机未走通 → 标「未放行，待业主手验」**：
- 学校 schX 能真登、有 4 个真任务，但导航到「候选池→学生→材料预览」这一步本机脚本没稳定点到候选材料（学校看材料走 get_candidate_documents + 水印 EF，需候选有已传件）；管理端**无密码**（只能 stub），本轮未搭到预览那步。
- 组件是**同一个 asset、同一套 `MHViewer2.attach(img,{lang,height})` 调法**，审核端真页面 28/28 已证其在真弹窗里挂载正常、工具条真点全灵；学校/管理端弹窗的层级/CSS 风险与审核端同类，审核端已过。真页面开图绿灯留：学校端业主 schX 手验 / 管理端业主登录手验（管理端在 CF Access 后、桌面用），或 16-09 线上走查补。

## 三、业主要做什么
- **审核端**：本包已可用，Claude 看图认可（红→绿成对，本机 `~/mh-verify/shots/JOB-16/16-01/`；因含真实材料图，未进公开仓库）。
- **学校端 / 管理端**：请各自打开一份**图片**材料预览，肉眼确认新工具条（−%＋/整页·适宽·1:1/旋转）能放大看清、拖动不跑出框；有异样告诉 Claude。这两端标「未放行」直到您手验或 16-09 线上走查过。

## 四、新闸 N1–N6 证据
- **N1（按钮差集）**：三端静态差集——审核端仅 `_v38ShowImage→_v39ShowImage` 一处函数改名（本包有意重命名，非删按钮/区块/输入框）；学校、管理端 removed=0/added=0。审核端动态首页 v38→v39：可见交互件 201→201，removed 0 added 0。**无任何按钮/区块/输入框消失。**
- **N2（先红后绿真页面）**：审核端真登真点，先红（v1＋点了不动）后绿（checkViewer 28 过），两内核；judge 全靠屏幕几何（外框尺寸/位置/可见百分比），未用 style.transform/内部变量；未用 evaluate(el.click())/dispatchEvent/直调页面函数/force。学校/管理端未强绿、按 N2 标「未放行」不放水。
- **N3**：本包不涉按登录人过滤的取数（纯前端组件替换）。
- **N4**：本包不动云函数。
- **N5**：secret-scan 对待推 mh-jobs 干净（本包回执与截图无密码/token；真材料截图只留本机、不进公开仓库）。
- **N6**：只换放大镜、只修不加；PDF/视频/退回/通过/三级回落死件短路一律不碰。

## 五、四件套 + 版本
- 文件名：`maxhouse-reviewer-portal-v39.html` / `maxhouse-school-portal-v233.html` / `maxhouse-admin-portal-v214.html`（旧版留盘）。
- 头注、`[VER]` console、index.html 重定向（reviewer/admin 各 3 处、school 4 处）——三端各自同步到新版，已核。
- `grep -cF -e 'MHViewer.attach('` 三端均 = 0；`mh-viewer-v1` 无 `<script>` 引用；`MHViewer2.attach(` 三端各有 1 处 live 调用。
- changelog：`docs/changelog/<端>-versions.md` 各加一行（随主仓库 commit）。

## 六、闸3
- 审核端：真页面 chromium+webkit checkViewer 全绿、0 页面报错、红绿成对、四附加断言（D 记 8px 裁剪备注）、`MHViewer.attach(`=0、N1 差集仅有意改名——**过**。
- 学校/管理端：代码/四件套/N1 过；真页面绿灯**未放行**（见二、三）。
- 主仓库三端 + 新 asset 已 commit（未推）；本回执已推 mh-jobs。

（第十七轮建议包备忘：MHViewer2 工具条在 <280px 超窄宿主用 container-query 再压一档，免旋转键被裁 8px。）
