# JOB-8-02 · 上传反馈层真机版（验收项 5、6、7、8）— 回执

**结果：手机上"选完就有反应、能锁按钮、去重、慢网提示、失败能重试"整体都真跑通了。抓到 3 个不影响功能但值得下轮修的小毛病（见待修）。测试用的 5 个文件已全部删干净，bank 槽恢复原样（2 个你原有的文件）。全程只碰 bank 这一个非闸门槽，四类闸门槽一个字节没传。**

## 选槽（只读探针 `jobs/JOB-8-02/probe.sql`）
stuA 各槽：bank(2 文件/版本1/**0 已通过审核**) 是唯一「非闸门 + 没通过审核 + 批次最少」的槽 → 选 **bank**。criminal/translation/visa 都有已通过审核，跳过；四类闸门槽（passport/education/transcript/photo）一字节不碰。

## 机器断言表（真机 v389，CDP 限速/注入文件驱动）
| 项 | 断言 | 实测值 | 过? |
|---|---|---|---|
| 5 | 选完→进度卡出现 < 200ms | **637ms**（见待修①：2MB 图先压缩再出卡）| ⚠️ 慢于目标但卡确实出 |
| 5 | 上传中途进度 >0 且 <100 | `width=35% "Uploading 35%"` | ✅ 真进度 |
| 5 | 完成到 100% | `width=100% "Uploading 100%"` | ✅ |
| 6 | 上传中按钮锁：文字含「上传中」| `"Uploading · 1/1"` | ✅（英文因手机当前是英文界面）|
| 6 | 按钮锁：等效禁用 | `pointer-events=none`（见待修②：用 pe 不是 disabled 属性）| ✅ 等效 |
| 6 | 在途去重：同名同大小 2 份 → storage PUT 只 1 次 | `PUT=1 sign=1` | ✅ |
| 7 | 单文件 >8s 出「网速较慢」提示 | 9s 出现：`"Slow connection — please wait, don't tap again · 9s"` | ✅（英文，见下）|
| 8 | 拦截 storage→500 → 失败红卡出现 | `red=true`（拦截 2 次：签名PUT+回落POST）| ✅ |
| 8 | 「重试」按钮出现 | `"Retry"` | ✅ |
| 8 | 解除拦截 → 点重试 → 真的传成功 | 重试后 `storage 写 200 = 1 次`，无红卡残留 | ✅ |
| 收尾 | 网络条件还原 | 每个脚本结束都 `emulateNetworkConditions` 还原（latency0/throughput-1）| ✅ |
| 收尾 | 测试件删除 | storage.remove ok(5) + documents.delete ok → DB 复核 **mh8-test 活文件 = 0**，bank 恢复 2 个原文件 | ✅ |

## 大白话结论
- **项5**：手机上传一张 2MB 图，进度条真的跟着字节走（35%→100%），完成会 100%。唯一小遗憾：**卡不是"选完瞬间"出，而是等了约 0.6 秒**——因为 2MB 图要先在手机上压缩，压完才出卡。功能没问题，就是不够"秒出"。
- **项6**：上传中那个槽的按钮变灰、显"上传中·1/1"、点不动；同名同大小的文件一批里只传一次（真数了请求：只发 1 个）。全对。
- **项7**：传得慢（>8秒）会冒出"网速较慢，请耐心等待"那行——**冒出来了**，只是现在手机界面是英文，所以显示的是英文版那句。文案机制对，语言跟着界面走。
- **项8**：把上传强行打成失败 → 卡变红、出"重试"按钮 → 点重试（恢复网络后）→ 真的传成功了。全链路通。

## ⚠️ 待修清单（本轮只记不修，建议下轮）
1. **手机上进度卡不"秒出"（~637ms）**：2MB 图的压缩跑在出卡之前。建议：选完先出"0%"占位卡，再在后台压缩，让反馈真正"秒出"。（建议包 9-0X）
2. **按钮锁用 `pointer-events:none` 而非 `disabled` 属性**：视觉/交互等效（灰+点不动+显"上传中·N/M"），但严格 DOM 层面 `.upload-btn` 是 `<label>` 没有 disabled 属性。若要 100% 满足"disabled=true"字面，可换成真 button/加 aria-disabled。
3. **失败重试会多留一张"僵尸卡"**：点"重试"时新开了一张进度卡，**旧的失败卡没清掉、停在"Uploading 0%"**。重试本身成功（真传了 200），但 dock 里会残留旧卡。建议：重试复用同一张卡，别新建。
4. **手机界面当前是英文**（不是中文）：慢网提示、按钮文字都显英文。若基线该是中文，需在手机上切回中（本轮 8-05 会处理语言并切回中）。

## 截图（本机）
- `8-02-1-进度中.png` `8-02-2-完成.png` `8-02-3-慢网提示.png` `8-02-4-失败红卡.png` `8-02-5-重试成功.png` `8-02-6-已删.png`

**只碰 bank 非闸门槽；测试件已删净；网络条件已还原；未改任何本地文件。回执推日志仓库；截图只在本机。**
