# JOB-7-02 · 五端图标资源审计 + 换新 — 回执

**结果：审计做完（下面那张表），四角留白的方形应用图标全部换成四角铺满的新图标，og:image 分享卡片一个没动，邮件模板 + 水印里的 logo 只列不换。四端各出一版，全部本地走查 16/16 过。图标文件本身仍是占位（你的 zip 不在电脑上），待你覆盖。**

## 大白话
你发现旧的 M 图标四角有白边——因为手机/系统会把图标裁成圆形，图标四角是白的或透明的，裁完就露出白角。我把全站每一张图标都用程序读了四个角的像素，揪出了真正有问题的两张，换掉；横图分享卡片（那是宽的，不会被圆裁）一张没碰。

## 图标审计表（程序逐张读四角像素）
| 文件 | 尺寸 | 用在哪 | 四角实测 | 判定 | 处理 |
|---|---|---|---|---|---|
| `favicon.png`(根) | 64×64 | admin/partner/school 的 apple-touch-icon/favicon、student 兜底 | **白** | ⚠️ 方形应用图标留白 | **已换**：四端 head 改指 `/assets/icons/*` |
| `assets/og-cover.png` | 1200×1200 | 根 `index.html` 当 favicon+apple-touch | **白** | ⚠️ 方形应用图标留白（大方图当图标） | **已换**：根 index 改指 `/assets/icons/*` |
| `assets/og-cover.jpg` | 1200×630 | og:image 分享卡片（横图） | 橙/紫实底 | OK | **不换**（横图，不受圆裁；铁令要求不动 og:image） |
| `assets/logo.png` | 760×110 | 页眉横向 wordmark | 透明 | 横向 logo，非方形应用图标 | **不换** |
| 水印 EF `watermark-doc/logo_b64.ts` | 500px 宽横图 | 水印平铺单元里的 logo | 透明（横图） | 内嵌在 Edge Function、需重新部署 | **只列不换**（另开部署包） |
| `notify_templates`（10 个邮件模板） | — | 邮件正文 | 查库：**无任何图片/logo 引用** | 邮件当前根本没嵌 logo 图 | **只列不换**（notify-email / send-verify-email 两个 EF 也无 logo） |
| `student-portal/icons/*`（7-01 建） | 512/192/180/32/16 | 学生端 PWA | 橙实底四角铺满 | OK | 7-01 已建（占位） |
| `assets/icons/*`（7-02 新建） | 512/192/180/32/16 | 四端 + 根的 favicon/apple-touch | 橙实底四角铺满 | OK | 7-02 新建（占位） |

## 换了哪几个（版本 +1）
- **管理端 v206 → v207**：apple-touch-icon 从白角 favicon 改指 `/assets/icons/icon-180.png`，加 32/16 rel=icon，加 `application-name=Maxhouse Admin`。
- **中介端 v150 → v151**：原本只有一个白角 favicon → 补齐 apple-touch + 32/16 + `application-name=Maxhouse Partner`。
- **学校端 v230 → v231**：三个白角 favicon 引用改指新图，加 `application-name=Maxhouse School`。（通知书/JW202 打印模板里的图不动，那不是应用图标。）
- **审核端 v31 → v32**：审核端**以前一张图标都没有**（桌面/收藏夹裸白）→ 补齐 apple-touch + 32/16 + `application-name=Maxhouse Review`。
- **根 `index.html`**（首页，版本锁不改名，就地改）：白角大方图 `og-cover.png` 当 favicon 的两处 → 改指 `/assets/icons/*`，加 `application-name=MAXHOUSE`。**og:image（`og-home.png`）一字未动。**
- 四端的 `index.html` 跳转指针同步指到新版本。

## 十道闸 / 走查
- **闸 9（不新造样式）**：本包纯 `<head>` meta/link 改动，**新增 CSS 类 0**。
- **走查**（`tests/job7-02.mjs`，本地 127.0.0.1 + 你电脑 Chrome 无头）：四端 + 根共 5 处，逐个断言 apple-touch-icon 存在、图标能 200 取到且是 png、application-name 正确、根 og:image 仍是 og-home.png 未动 → **16/16 全过**。
- 四端 JS 全部解析干净（只动 head，未碰逻辑）。

## ⚠️ 同一件待补（和 7-01 同一个 zip）
新图标文件现在是**占位**（橙 #F39200 底 + 白 M，四角实测全橙、零透明零白边）。等你的 `maxhouse-pwa-icons.zip` 找出来，两句覆盖即成正式：
```
cd /Volumes/Dev/MAXHOUSE
unzip -o ~/Downloads/maxhouse-pwa-icons.zip -d assets/icons/
unzip -o ~/Downloads/maxhouse-pwa-icons.zip -d student-portal/icons/
```
（文件名一致，覆盖即生效，无需改代码。视觉最终样子以你覆盖后为准——守铁律15，我没拿占位图冒充正式件。）

## 动过的文件
- 新增：`assets/icons/icon-{512,192,180,32,16}.png`（占位）、`tests/job7-02.mjs`、`jobs/JOB-7-02/probe.sql`（只读探针）
- 改：`admin-portal/maxhouse-admin-portal-v207.html`、`partner-portal/maxhouse-partner-portal-v151.html`、`school-portal/maxhouse-school-portal-v231.html`、`reviewer-portal/maxhouse-reviewer-portal-v32.html`（新版）+ 四端 `index.html` + 根 `index.html`

**主仓库未推。本回执已推日志仓库。**
