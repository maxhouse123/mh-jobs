# JOB-16-02 回执 · 中介端 v161 邀请数归零修复

partner v160→v161（只改一处逻辑：`_dlLoadInvites`）。不动邀请 RPC / 邮件 / 开关值，不新建/重发/撤销邀请（只看不动）。

## 一、做了什么（大白话）
中介端首页往下「我的委托邀请」区块，业主看到「已发邀请 0」——其实库里 partner1 有 10 条。病根：那段读邀请的代码里，写学生归属用的 `uid` 是个**没定义的变量**，一取值就报错，又被一个「装看不见」的 `catch` 吞掉，结果邀请永远是 0。这次把它改成用会话就绪时already存好的 `_dlState.uid`（取不到再兜底查一次登录态；再取不到就明确跳过并 `console.warn` 一声，不再装看不见）。0 条态/10 分钟倒计时/重发/复制链接/撤销/分页，一律沿用 15-04 没重写。

## 二、结果是什么（真页面 partner1 真登，先红后绿）
- **先红 v160**：页面「已发邀请 **0**」、列表 0 行（只有空态那行），而 `truth('partner1', named_invites)` = **10** 条 → `RED:true`（`stateInvites:0`）。截图 `~/mh-verify/shots/JOB-16/16-02/partner-v160-invites-RED-chromium.png`。
- **后绿 v161**：页面「已发邀请 **10**」、列表 **10** 行、`_dl.state.invites=10`，与 truth 10 完全对上；点「✉邀请学生自传」→ 邀请弹窗真打开（不提交、关掉）；**强刷后仍 10**；**登出再登入仍 10**；chromium 与 webkit **两内核都绿**、0 页面报错。
- **口径**：页面「已发邀请」用的是全部条数（10），列表也是全部 10 行；库里 10 条分布 sent 6 / opened 1 / claimed 1 / expired 1 / revoked 1（撤销/过期在列表里是划线态，仍计入总数——按页面既有口径，未改）。

## 三、「按钮消失」那一半（本机仍未复现，已定位位置）
- 按 card ③ 在 **1440 / 1024 / 390 三种宽度 × 中/英两种语言 × 基础/强刷后/登出再登入** 共 18 种组合逐一量：「✉邀请学生自传」按钮**全部 exists+visible+clickable**（滚到它时中心点 `elementFromPoint` 命中自己）→ `BUTTON-MATRIX ALL-VISIBLE-CLICKABLE`（chromium）。
- **按钮位置**（告诉业主去哪找）：在首页「我推荐的学生」区块内、**「＋提交新学生推荐」按钮的正下方、「已发邀请」列表的正上方**（页面要往下滚）。截图 `partner-v161-button-located-LOCAL.png`（红框圈出，邀请行的姓名/邮箱已打码）。
- 结论：本机三宽两语都在、都点得到；业主线上看到的「按钮没了」**本机复现不出**，照 N2 如实记；**线上那一半留 16-09 线上走查判**（partner1 线上真登看按钮在不在、点不点得开）。

## 四、新闸 N1–N6 证据
- **N1**：v160→v161 差集 = ids/handlers/functions **removed 0 / added 0**（纯逻辑改，无任何按钮/区块/输入框增减）；另按 card 要求补跑 **v159→v161** 差集 = removed **0**（v159 有的东西 v161 一个没少；added 的是 15-04 已加的 dlInviteList+3 函数，属正常）。**无消失。**
- **N2**：真页面 partner1 真登，先红（v160 显0）后绿（v161 显10+列表+刷新+重登），judge 只认屏幕上的数字/行数与 truth 对照；未用直调页面函数/force。按钮消失半未复现，照 N2 记「本机未复现、线上 16-09 判」。
- **N3**：truth 走 partner1 真登 REST（`named_invites` RLS 生效）= 10，与页面 10 对上；未用无身份查询。
- **N4/N6**：不动云函数；只修不加（只改 _dlLoadInvites 取 uid 一处 + catch 改 warn）。
- **N5**：secret-scan 对待推 mh-jobs 干净；截图里邀请行姓名/邮箱用 Playwright mask 打码、且只留本机（未进公开仓库）。

## 五、四件套
- 文件名 `maxhouse-partner-portal-v161.html`（v160 留盘）；`[VER] partner portal v161` 探针=1（另留 `[VER-prev] v160` 一行历史）；index.html 4 处引用全切 v161；`docs/changelog/partner-portal-versions.md` 加一行。（partner 头注按其自身约定「自 v108 起不在文件头追加注释」，版本史落在 changelog。）

## 六、业主要做什么
- 红→绿截图（邀请区块，姓名邮箱已打码）在 `~/mh-jobs/shots/16/16-02/`，**待 Claude 看图**再给验收表（原稿曾替 Claude 写「看图认可」，已更正）。业主端：中介端首页往下滚到「我推荐的学生」，确认「已发邀请」显示真实条数、「✉邀请学生自传」按钮在「＋提交新学生推荐」下方可点。线上按钮那半 16-09 会再判。

## 七、闸3
- 真页面 chromium+webkit 红→绿全过、0 报错；按钮矩阵 18 组全可见可点；N1 两份差集 removed 0；四件套齐。**过**（线上按钮半留 16-09）。
