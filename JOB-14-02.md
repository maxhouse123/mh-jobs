# JOB-14-02 · 预取并行与刷新保留（reviewer v37 第二部分）

## 做了什么
- 把审核端的后台预取从「一次取一份」改成「同时取 3 份」（Promise 池），顺序 = 当前展开的学生组 → 下一组。
- 查看某份材料时，立刻预取它同组的下一份（点开时下一份多半已到手）。
- 刷新队列时**只把已经离开队列的缓存丢掉**（省内存），仍在队列的缓存**保留不清**，所以刷新后不会重复下载。
- 同一份材料一次登录内只取一次；正在预取时点「查看」复用同一个下载，不发第二次请求（这本来就由内存缓存保证，本轮确认并保持）。

## 结果是什么（命令：node jobs/JOB-14-02/gate3-prefetch.mjs）
`RESULT=GATE3-PASS`：
- 12 件预取 → **并发峰值 = 3**（不超 3 路）。
- 真实并行时长 **169ms**，串行估算 480ms → **< 一半**（控制台实测 `[v37] prefetch 12 docs 169ms parallel=3`）。
- 首轮 12 次下载；**再刷新同 12 件 → 0 次新下载**（缓存保留、0 重复）。
- 预取途中点「查看」→ **0 次新请求**（复用在途下载）。
- 0 报错。
- 版本：reviewer v37 [VER]/头注已含 14-02（同一文件跨 14-01～04 递进）。

## 业主要做什么
- 无需单独操作。推送 + 部署后，审核端翻材料会更「点开即显」（后台已并行预取好）。验收并入验收表第 1 项（审核端提速）。
