# JOB-21-03 回执 · 学生端法文两键：核验后判定「已满足、无可修」→ 不 bump v400

CC 执行。承 21-00 探针⑥（premise「fr 回落英文」未复现）。本包只读核验，未改任何源码，未升版。

## 一、结论
`doc.studyProof` / `doc.studyPlan` 在学生端现役 v399 的**法文界面已显示法文**，非英文回落。QUEUE-21 21-03 的前提不成立 → **只修不加：无可修**，不新建 v400、不动 index.html、不动 changelog。

## 二、闸3 证据（真页面 student v399 四语 eval）
探针 `jobs/JOB-21-00/probes/p6-student-fr.mjs`（只读加载真页面 + setLang + t()），本包重跑存 `jobs/JOB-21-03/probes/p6-student-fr-v399.txt`：

| 键 | zh | en | ru | fr |
|---|---|---|---|---|
| doc.studyProof | 在读证明 | Proof of study | Справка об обучении | **Attestation d'études** |
| doc.studyPlan | 学习计划 | Study plan | План обучения | **Plan d'études** |

- fr 两键均为法文（非英文）→ **GREEN**；zh/en/ru 与基线一致、不变。
- 四语逐键：上表即四语实测，无裸键、无回落。

## 三、根因（为何 premise 不成立）
- 现役 v399 法文包 = `maxhouse_student_i18n_v360_fr.js`（v399 第 6938/6946 行动态 `document.write` 按 lang 加载）。该包 **L76 `doc.studyPlan: "Plan d'études"`、L1181 `doc.studyProof: "Attestation d'études"`** 早已就位。
- 补充词典 `I18N._sup`（先于语言包查找）：仅 `_sup.zh` 覆盖 `doc.studyProof`（v399 L8317，第二十轮 JOB-20-03 对齐中文名用），**无 `_sup.fr` 覆盖这两键**（L8316 注明「en/ru/fr 一字不动」）→ fr 直接落到 v360_fr 包的法文值。故真页面 fr = 法文。

## 四、N1
本包零源码改动、v399 不变 → 静态清单无差集（N1 沿用 21-00 `student-v399-static.json` 基线，trivially 满足）。

## 五、观察（不改，交 Claude/业主留档）
- `doc.studyProof` fr = "Attestation d'études"；同页 `supDoc.studyProof` fr = "Attestation de scolarité"（v360_fr L604）。两者分属不同键命名空间、均为「在读/学籍证明」的合规法文，用词略异；非缺陷、非本包范围，仅记录。若业主要求两处法文用词统一，另开小卡（属文案对齐，非回落修复）。

## 六、状态
- 主仓库仅新增 `jobs/JOB-21-03/probes/p6-student-fr-v399.txt`（证据），已 add 点名 + commit，**未推**（§0.9）。学生端源码零改动。
- 本回执已 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**：核验绿、四语逐键、N1 满足；无视觉件、无待部署。

下一包 = 21-04
