# JOB-17-06 回执 · G①② 迁移 0211 / 0212

CC（无人值守，新会话）做完 17-06。0212 已 db-run 落库并 RED→GREEN + N4 回归全绿；0211 交付迁移文件 + RED 证据，GREEN 待卡1136 业主 SQL Editor 手贴。**主仓库已本地 commit、未推**（不推主仓库，照 §0.10）。

---

## ★★★ 必读：0211 一处「图纸与现实冲突」，业主先裁决再手贴（铁律14-P2）★★★

**QUEUE-17 §17-06 / §17-00 探针① 写的 reviewer_access_log.outcome 差集 = `{denied_not_candidate, denied_not_owner, denied_revoked}`（3 个）。CC 09-21 逐行读 watermark-doc v33 源码证实这个差集不对：**

| 值 | 源码写入函数 | 落哪张表 | 是否进 reviewer_access_log |
|---|---|---|---|
| `denied_not_candidate` | `logSchoolAccess`（index.ts:680） | **school_access_log** | ❌ 从不进 |
| `denied_not_owner` | `logStudentAccess`/`logPartnerAccess`（index.ts:695 / 708） | **student/partner_access_log** | ❌ 从不进 |
| `denied_revoked` | `logAccess`（index.ts:672） | reviewer_access_log | ✅ 进，约束不收→静默拒 |
| `denied_doc_deleted` | `audit()` 闭包（index.ts:721），对 reviewer 角色路由到 `logAccess`（index.ts:644-646） | reviewer_access_log | ✅ 进，约束不收→静默拒（**QUEUE 漏列**） |

- **对 reviewer_access_log 而言，「函数会写、约束不收」的真差集 = `{denied_revoked, denied_doc_deleted}`（2 个）。**
- QUEUE 那 3 个里有 2 个（`_not_candidate`/`_not_owner`）本表根本不写 → 加进来只是死值，违「一个不多加」；且漏了 `denied_doc_deleted`（真被静默拒的审计）。
- **CC 按「一个不多加」的本意，把 0211 迁移文件落成源码真差集（+denied_revoked, +denied_doc_deleted，共 10 值），并在文件头注醒目标出本冲突，不擅自应用**（0211 本就是业主 SQL Editor 手贴类，drop constraint 被 db-run 红线挡）。
- **业主裁决点**：手贴前确认走 CC 这版（+denied_revoked, +denied_doc_deleted）；若要坚持 QUEUE 原字面（+3），请自行改 `supabase/migrations/0211_reviewer_outcome_denied.sql` 再贴。CC 建议走真差集版。
- 确定性 RED 证据（下）已同时证明：QUEUE 误列的 `denied_not_candidate` 在本表也被拒，但 watermark-doc 从不往本表写它 —— 加它=多加死值。

---

## 一、做了什么

### 0212（G② · set_doc_slim 加有效委托人）—— CC 用 db-run 落库
- 正本 `supabase/migrations/0212_slim_delegate.sql`；归档 `docs/supabase 文件/0212_slim_delegate.sql`（铁律16）。
- `create or replace`（无 drop，走 db-run）。在 not_yours 前的归属 OR 链**尾部追加**：
  `or exists (select 1 from public.student_delegates sd where sd.student_id=v_owner and sd.delegate_uid=auth.uid() and sd.active and sd.revoked_at is null)`
- 其余**一字不动**：签名/SECURITY DEFINER/search_path=public、bad_status 白名单（slimmed/kept_not70/kept_pages/kept_error）、not_found、already_set、15 分钟 too_late 窗口、update 集合、返回值、防三值逻辑的 coalesce。
- 源码基线核对：db-run 前先 `pg_get_functiondef` 打出现役旧体逐行核对（reverify）。

### 0211（G① · reviewer_access_log.outcome 补值）—— 属手贴类，db-run 拦（drop constraint 红线）
- 正本 `supabase/migrations/0211_reviewer_outcome_denied.sql`；归档 `docs/supabase 文件/0211_reviewer_outcome_denied.sql`。
- 照 0204 / 0210b 先例：`drop constraint if exists` + `add constraint … check(outcome = any(array[…]))`，现役 8 值 + `denied_revoked` + `denied_doc_deleted` = 10 值。
- CC 不应用（业主卡1136 手贴）；本包交付文件 + RED 证据。

## 二、结果是什么

### 0212 —— RED→GREEN（全 rollback，零持久写）+ N4 回归 + 已落库
- `redgreen-0212.sql`（身份=partner1 via request.jwt.claims GUC；setup 以连库 owner 身份做委托/改件；测试对象=recommendation 件 `3b2389ff`，非闸门槽）：
  - **RED**：现役旧函数对 partner1 委托 → `not_yours`。
  - **GREEN**：事务内 create or replace 成 0212 新体 → 同一调用返回 `t`，`slim_status=slimmed`。
  - 全部 `rollback;` + 空 `commit;`。
- **N4 越权回归**（`regress-0212.sql`，应用后跑，全 rollback）：
  - CASE1 无委托 → `not_yours`；CASE2 active=false → `not_yours`；CASE3 revoked_at 非空 → `not_yours`；CASE4 有效委托 → `t`。**新函数不越权。**
- **落库验证**（迁移自带）：`has_delegate_predicate=t, has_revoke_guard=t`。

### 0211 —— 确定性 RED（不依赖 EF）
- `red-0211.sql`（rollback 事务，DO/EXCEPTION 捕 check_violation）：
  - `RED-OK denied_revoked 被 CHECK 约束拒（今天静默丢审计）`
  - `RED-OK denied_doc_deleted 被 CHECK 约束拒（今天静默丢审计）`
  - `CTRL denied_not_candidate 也被拒`（但 watermark-doc 从不往本表写它 → 加入=多加死值）
  - `CTRL-OK denied_not_assigned 被接受`（现役约束确实放这个值 → 证明拦截只因约束值，非别的）
- **GREEN 待卡1136**：业主 SQL Editor 手贴 0211 后，watermark-doc 写 denied_revoked / denied_doc_deleted 的审计行才留得下。届时线上真验（撤销派单 → reviewer1 调 watermark-doc → 403 denied_revoked，审计从 0 行变 1 行）。

### 清洁核验（`clean-probe.sql`，带身份/带状态）
`DELEG-LEFT=0`（无 stuA 委托残留）· `RVLOG-LEFT=0`（无 REDPROBE 审计残留）· `DOC-SLIM=NULL`（测试件未被持久改）· `SLIM-FN delegate=true`（0212 已落库）· `RVCONS has_revoked=false`（0211 未落库，待卡1136）。

## 三、业主要做什么
1. **卡1136 手贴 0211 前先裁决上面的 P2 冲突**（建议走 CC 真差集版 +denied_revoked/+denied_doc_deleted）。手贴 `supabase/migrations/0211_reviewer_outcome_denied.sql` 全文；执行后线上真验 denied_revoked 审计从 0→1。
2. 0212 已落库，无需业主动作；要眼见的话，卡1136 可让某中介对其名下委托学生的新传件在 15 分钟内触发瘦身写回，看是否成功（属线上验收，进 SUMMARY 待看清单）。
3. **本包主仓库已本地 commit、未推**；推送等收官后卡1135（按 SUMMARY-17 的 PUSH_OK）。**CC 不推主仓库。**

## 四、新闸证据（N1–N6）
- **N1（按钮清单差集）**：本包纯迁移，**无端口升版、无按钮/UI**，N1 不适用；五端 HTML 一个字节未动（`git status` 只有 4 个新增迁移/归档文件）。
- **N2（先红后绿）**：0212 = SQL 层 RED（not_yours）→ GREEN（返回 t + slim_status 落）；0211 = 确定性 RED（约束拒两个真值），GREEN 待卡1136 手贴后线上出（本包不据此放行 0211 绿）。migration 层等价「真页面先红后绿」的可判据版本。
- **N3（带身份）**：0212 测试以 partner1 的 `request.jwt.claims`（sub=1aad823a-…）驱动 auth.uid()，走真委托分支；is_admin(partner1)=false。setup 的越权写（建委托/改件）以连库 owner 身份做、与被测函数的调用身份分离；探针只读密码经 db-run，不打印任何 token。
- **N4（每条分支各验一次）**：0212 = {无委托 / inactive / revoked / 有效} 四态 + RED（旧函数）+ GREEN（新函数）；0211 = {denied_revoked / denied_doc_deleted 真拒 + denied_not_candidate 死值对照 + denied_not_assigned 现收对照}。逐格如实。
- **N5（secret-scan）**：push 本回执前对 `~/mh-jobs` 全树扫一遍（见推送段）；所有 SQL/probe 只读密码、不含 secret，rollback 事务不打印 token。
- **N6（只修不加）**：0212 只在 OR 链尾追加委托谓词，其余一字未动；0211 只补源码真差集两值（本意「一个不多加」，QUEUE 字面的多余 2 值/漏列 1 值已在 P2 段说明并交业主裁决）。未碰云函数、未碰端口、未部署、未推主仓库。

## 五、动过的槽 / 测试写库
本包**零材料上传、零测试派单、零 §0.11 批次占用**：0212 的 RED→GREEN / N4 回归全在 rollback 事务里改（委托行、测试件 uploaded_at/slim_status），事务结束即撤销；`clean-probe` 已证零残留。唯一持久写 = 0212 函数体 create or replace（设计内）。无测试件需收尾软删。

下一包 = 17-07
