# JOB-06 · 学生抽屉付款订单区 + 付款页按学生筛

**结果：全闸通过**

## 做了什么（一句白话）
学生说「我付了钱怎么还没解锁」，以前管理员在抽屉里只看得到**已完成**的流水，看不到**待核销/已驳回**的订单。这次：
- 学生抽屉里加了一段「🧾 付款订单」，把这个学生的**全部**付款订单列出来（单号、类型、金额、优惠码是第几次、状态、驳回理由都有）；待核销的订单直接在抽屉里就能「核销 / 驳回」。
- 付款页每条订单的学生栏，以前显示一串看不懂的编号前 8 位，现在显示**姓名 + 学号**。
- 付款页顶部加了搜索框，能按**姓名 / 学号 / 申请号 / 单号**筛。

## 业主要看什么（打开哪里、点哪个、应该看到什么）
1. 管理端 → 「学生」→ 点任意学生 → 抽屉里找到「🧾 付款订单」段：能看到该生所有订单，每条有单号/金额/状态。
   - 如果有「待核销」的订单，这一行右边会有「核销」「驳回」按钮，点了跟付款页效果一样（走同一套后端调用）。
2. 管理端 → 「付款」页：
   - 每条订单的学生栏现在显示姓名+学号（以前是一串编号前 8 位）。
   - 页面上方多了搜索框，输入学生姓名 / 学号 / 申请号 / 单号能筛出对应订单。

**库里实测**：某学生有 9 条订单（7 条待付款 created / 1 条已过期 / 1 条已核销）——打开对应学生抽屉就能看到全部 9 条。

## 十道闸（逐条 + 实测数字）
| 闸 | 结果 | 实测 |
|---|---|---|
| 1 基底md5 | PASS | `2ed0548ee3791d1c7336a93b6e4bb7f7`（=JOB-07 产物）|
| 2 锚点唯一 | PASS | 9 处锚点各出现 1 次 |
| 3 补丁自检 | PASS | 四个新函数定义/调用点计数逐项对齐；`_v176StuName` 用于 `_poRow` 两处；`_v176PayMatch` 用于筛选两处；旧 8 位学生栏归 0。反向样本拿 v175 跑 3/3 失败 |
| 4 产物md5 | 记录 | `ae2eefc06e7c1c8f7e67bebe942f5d43` |
| 5 语法 | PASS | 脚本块 0 报错 |
| 6 功能清单 | PASS | 函数新增 4（`_v176StuName/_v176PayMatch/_v176OrdersSection/_v176OrdersFill`）+元素 id 新增 1（`v176OrdBox`）；**消失 0** |
| 7 四语 | PASS | 管理端中/英双语；新增文案中英齐全、无裸键 |
| 8 真点击 | PASS | 真浏览器：`_v176StuName` 解析出「Alice Wang · STU-001」；`_poRow` 含姓名+学号+单号+核销/驳回按钮+驳回理由（中英都过）；搜索按姓名/申请号/单号各命中正确条数、反向 0；面板含搜索框；抽屉订单段用桩数据异步填充成功；**390px 不横向溢出** |
| 9 视觉一致 | PASS | 我的新增代码**零**写死颜色/新类/新字号（`#888`与`font-size:11px`在 v175/v176 计数均相等：26=26、78=78，是编辑既有行带出的差异，非新增）|
| 10 线上版本 | 跳过 | 本轮不推送 |

## 基底 / 产物 md5
- 基底：`2ed0548ee3791d1c7336a93b6e4bb7f7`
- 产物：`ae2eefc06e7c1c8f7e67bebe942f5d43`

## index.html 改了几处
3 处，v175→v176，无残留。

## 提交哈希
主仓库 `fec7db8`（已 commit，未推送）。

## 偏离或疑点
1. **「核销 / 驳回」在真数据下暂时验不了**：库里目前订单状态是 created/expired/paid，**没有 awaiting_review（待核销）也没有 rejected（已驳回）**，所以抽屉里的核销/驳回按钮要等有「待核销」订单才会出现。代码用桩数据证明了：待核销订单会显示这两个按钮、点了走现成的 `poConfirm/poReject`（同付款页）。等 JOB-0.5 造出待核销订单后可真点验收。
2. **抽屉里核销/驳回后不自动刷新那一段**：我按队列要求「复用付款页现成的调用，不新造」，现成的 `poConfirm/poReject` 成功后刷新的是付款页而不是抽屉。所以在抽屉里核销一笔后，需要**关掉再打开该学生抽屉**才看到状态更新。功能本身正确，只是抽屉不即时刷新。要不要我后续补一个抽屉内即时刷新，你说一声。
3. 抽屉订单段是按该生 `student_id` **直接查库**（不是从付款页那份「最近 100 条」里过滤），所以订单再多也不会漏。
