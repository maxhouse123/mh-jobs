# JOB-11-03 · 撤第十轮测试派单（验清理触发器）

结果：**全闸通过** —— 撤单后清理触发器自动把桶里那份删掉了

## 大白话
第十轮留下一条测试派单（stuA 的一份材料 → 测试审核员 reviewer1），桶里有对应的水印预渲染件。这轮把它**撤单**（revoked_at 置为现在，行不删），撤单会自动触发"清理触发器"→ 通知水印函数把桶里那份删掉。实测：撤单后 35 秒，桶里那份对象**没了**（0 个），清理函数回 `200 {cleaned}`。撤单-清理这条链路端到端验通。

## 断言表
| 断言 | 期望 | 实测 | 过 |
|---|---|---|---|
| 撤单前 revoked_at | 空 | 空 | ✅ |
| 撤单前桶里对象 | 1 | 1 | ✅ |
| 撤单 update | 1 行 | UPDATE 1，revoked_at=2026-09-17 05:10:35Z | ✅ |
| 撤单后 revoked_at | 非空 | 2026-09-17 05:10:35Z | ✅ |
| 撤单后桶里对象（35s） | 0 | 0（清理触发器已删） | ✅ |
| cleanup 响应码 | 200 {cleaned} | `200 {"ok":true,"cleaned":true}` | ✅ |
| 行是否删除 | 否（只撤单） | 未删，仅 revoked_at 置值 | ✅ |

## 说明
- 11-01 的回填未把这条又补回来：该对象一直在桶里（回填的"桶里已有则跳过"逻辑把它跳过了）；撤单后该派单进入 `revoked_at is not null`，回填选取条件（`revoked_at is null`）天然排除它，不会再补。
- 只动这一行（cd7389f7），不删行；四类闸门槽/其他派单未碰。

## 动过的文件 / 数据
- 数据：reviewer_assignments 一行 revoked_at 置值（撤单，未删）。
- 新增：jobs/JOB-11-03/{pre.sql, revoke.sql, check.sql, report.md}
