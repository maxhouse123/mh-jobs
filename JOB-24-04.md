# JOB-24-04 回执 · 迁移 0214（管理员裁决进 admin_events）—— 停工报告，需业主定夺

给业主看的大白话版。**这一包 CC 没有写迁移，因为图纸的前提和真实数据库对不上，硬做会造出错东西。** 已按铁律停工报告，等你或写卡的 Claude 定夺后再继续。

## 一、这一包要干啥（图纸原意）

给管理端「审计事件」补上「管理员自己的裁决」——管理员审批学校、审批中介、核销付款、改配额、处理撞单、放行等动作，本来一条都没进 `admin_events` 这本账。图纸要求写一份迁移 0214，用**数据库触发器**（表一有裁决变化就自动记一行），并且**闸门要求「至少 12 个触发器」**。迁移只写文件、只跑 begin…rollback 空转探针，不真执行。

## 二、CC 只读核对了真实库况（grep supabase/migrations/*.sql），发现三处对不上

### 1）图纸点名的三张表，库里根本没有
| 图纸要挂触发器的表 | 真实库况 |
|---|---|
| `school_quota_requests`（学校配额申请） | **不存在**（全迁移搜不到建表） |
| 撞单表（collision） | **不存在**——撞单是靠 `check_collision()` 函数比对 + `students.partner_locked/partner_suspected` 两个布尔标记实现的，没有独立的「撞单裁决表」 |
| 放行表（release/放行裁决） | **不存在** |

无表就无从挂触发器，这三项做不了。

### 2）图纸要「新记」的两类，库里其实早就在记了——再记就是重复记账
| 图纸要新记 | 真实库况（已在记） |
|---|---|
| `referral_corrections` → `correction_decided` | `0070/0071` 裁决 RPC 已记 **`referral_correction_decided`**（0071 第 70 行） |
| `exposure_requests` → `exposure_decided` | `0164` 已记 **`exposure_set` / `exposure_pause_requested` / `partner_flagged`**；`0166/0168` 记 `exposure_boost_*` |

这两张表若再挂触发器，同一次裁决会被记两遍。按图纸「不重复记」原则，应跳过。

### 3）图纸说「8 类服务端已在记」，但 admin_events 里一条都搜不到
图纸（24-04 步 0 + 附录）说这 8 类「服务端已在记，24-04 不重复记」：
`school_suspended / school_resumed / school_updated / school_tier_set / doc_deleted / slot_reopened / letter_replaced / dispatch_revoked`。
**实测：这 8 类在全部迁移的 `insert into admin_events` 里一条都没有。** 图纸里 admin_events 现存的 event_type 只有下面 21 类（写卡的 Claude 说的「管理端抽屉里按类型读到的」很可能是从 `notifications`/时间线读的，不是 admin_events）：

> contact_unlock · pool_remove · task_approved · task_reviewed · visa_valid_until_set · visa_valid_until_locked · location_change_requested · location_change_cancelled · student_location_changed · referral_withdrawn · referral_resubmitted · referral_self_correction · referral_correction_decided · exposure_set · exposure_pause_requested · partner_flagged · exposure_boost_pending · exposure_boost_started · exposure_boost_extended · exposure_order_replaced · round_closed_by_student

（这不直接卡施工——反正图纸叫跳过这 8 类；记这里是提醒 24-03 词典里补的这 8 类目前在 admin_events 无对应记录源，将来若真要显示需另立数据源。）

## 三、把不存在的、会重复的、图纸叫跳过的都去掉后，真正能诚实新建触发器的裁决表只剩这些
| 表 | 主键 | 裁决列 | 拟记 event_type（都在 24-03 词典里） | 能建？ |
|---|---|---|---|---|
| schools | id | status onboarding→active | `school_application_decided` | ✅ |
| partners | id | status；referral_quota | `partner_application_decided` / `partner_status_set` / `partner_quota_set` | ✅ |
| partner_profile_requests | id | status pending→approved/rejected | `profile_change_decided` | ✅ |
| quota_requests | id | status pending→approved/rejected | `quota_decided` | ✅ |
| payment_orders | id | status →paid / →rejected | `payment_confirmed` / `payment_rejected` | ✅ |
| concierge_requests | id | status | `concierge_status_set` | ✅ |
| announcement_log | id | 只增行（无状态） | `announcement`（AFTER INSERT） | ✅ |

**合计约 7 张表**。就算把 partners 拆成 3 个触发器、payment_orders 拆 2 个，也就 ~10 个触发器，**够不到「≥12 个触发器」这条硬闸**。

## 四、为啥 CC 停工而不是硬做

- **铁律14-P2**：图纸与现实冲突，一律停工报告原文，不得自修。
- **铁律9**：数据库是最敏感的地方，不臆造设计——不在不存在的表上编触发器、不为凑数在已记账的表上重复挂。
- **不悄悄降闸**：把「≥12」偷偷改成「~10」并宣称过闸，是自欺，违反「标准不降」。

所以 CC 不硬凑数目、不臆造表、不重复记账，停在这里等定夺。这属于「图纸前提错」的冲突，不在 §0.11 四种「必须停」之列，但会每趟都撞同一堵墙，故已写 `~/mh-verify/r24-STOP.txt` 让外层脚本停下、不空转。

## 五、给业主/写卡 Claude 的建议（二选一，或你另有裁断）

1. **把数目闸改成「按真实可记裁决表尽数建」**（约 7–10 个触发器，清单见上表第三节），并明确 referral_corrections / exposure_requests 因已在记而跳过、三张不存在的表删除；这样 CC 下趟即可诚实施工。
2. 若确实想要 12+ 触发器，请写卡时**指明还有哪些真实存在的裁决表/裁决列**可挂（CC 只读核到的裁决表就上表这些），或允许把「学校停用/恢复/等级/更新」等也用触发器补记进 admin_events（图纸原说跳过，但它们其实不在 admin_events）。

改好后，24-05（学校行为日志升级）、24-06（导出全字段）、24-07（顾问台需求落库）、24-08（收官）都还没做，可从 24-04 重定版接着跑。

---
- 本包**未写迁移、未 commit 主仓库、未改任何代码**（纯只读核对 + 停工报告）。
- 上一包 24-03 已完成：admin v229，49/49 绿，主仓库已 commit（c1775b2）未推，回执已推 mh-jobs。
- STOP：`~/mh-verify/r24-STOP.txt` 已写。本趟结束。
