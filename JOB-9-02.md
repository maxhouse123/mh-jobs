# JOB-9-02 · 执行器白名单 + get_my_offers 吐 round_id — 回执

> **⚠️ 迁移改号更正（9-06 时发现）**：本包原把迁移编成 **0174**、放进 `docs/supabase 文件/`（该目录是停在 0173 的旧账本）。真账本是 `supabase/migrations/`（已到 0190），其 `0174_students_profile_extra.sql` 早已占用 0174。故已改号归位为 **`supabase/migrations/0191_get_my_offers_round_id.sql`**（+ `_rollback.sql`），旧 docs/0174 文件已删。**DB 里的函数改动本身已正确应用、不受影响**（只是归档文件改号）。下文出现的“0174”一律以 **0191** 为准。

**大白话结论**：给数据库执行器开了一道**很窄的白名单口子**，只允许对 `get_my_offers` 这一个函数做「先删再重建」（业主 2026-09-16 授权），别的函数、别的删除/清空一律照拦。先用**三个假迁移**当场证明这口子只放它、其余都拦住了，才动真的。然后把学生查录取用的那个函数**在末尾加了一列 `round_id`（轮次编号）**——这样以后学生端「录取卡」上显示第几轮，就能**直接按轮次编号对上号**，不用再靠「时间窗猜」那套兜底。真迁移已在生产库跑成功，函数照原样重建、只多一列、权限一分不差地还原了。**学生端不用改**（它早就按 `offer.round_id` 读，只是以前函数没吐这列），所以学生端版本不动。

**肉眼验收（业主）**：录取卡应仍显示 **MH-6KG2JN · 第 1 轮**，和现在一模一样；区别是背后从此靠真轮次编号对号，不再靠时间窗猜。**真登录实测这一条留给业主**（本轮没给 stuA 密码）。

## 一、白名单口子证明（三假迁移 + truncate，全用干跑闸不连库）
| 假迁移 | 内容 | 期望 | 实测 | 过? |
|---|---|---|---|---|
| A `fake-a-not-whitelisted.sql` | drop 一个**不在白名单**的函数(带 create) | 拦 | `STOP: drop function some_other_fn 不在白名单` exit=11 | ✅ |
| B `fake-b-whitelisted-no-create.sql` | drop `get_my_offers`**但无 create** | 拦 | `STOP: ...在白名单但同文件该 drop 之后无 create 同名` exit=11 | ✅ |
| C `fake-c-whitelisted-with-create.sql` | drop `get_my_offers`**且有 create** | 放行 | `白名单放行 get_my_offers` + `GATE OK` exit=0 | ✅ |
| D `fake-d-truncate.sql` | `truncate table foo` | 拦 | `STOP: ...truncate，红线` exit=11 | ✅ |

口子实现：`jobs/db-run.sh` 新增段——truncate / drop table·schema·type·column 无条件拦；`drop function` 只在**函数名在 `jobs/db-run-drop-allow.txt`（本轮只 `get_my_offers`）且同文件该 drop 之后有 `create[ or replace] function 同名`**时放行，并往 `jobs/db-run.log` 记「白名单放行 <名>」；放行也可用 `DBRUN_GATE_ONLY=1` 干跑（只判定、不连库、不跑 psql），三假迁移即用它证明、零 DB 改动。

## 二、真迁移 0174（生产库 maxhouse_main，已应用成功）
迁移文件 `jobs/JOB-9-02/0174_get_my_offers_round_id.sql`（同步归档 `docs/supabase 文件/0174_get_my_offers_round_id.sql`，铁律16）。做法：`drop function get_my_offers(uid uuid)` → `create or replace` 同签名，返回表**末尾加一列 `round_id uuid`（取 offer_decisions.round_id）**，其余 34 列名/类型/顺序一字不改、函数体除末尾多取 `od.round_id` 外一字不改，`security definer`/`search_path='public'` 与原一致；drop 会丢权限 → 末尾 `revoke ... from public; grant ... to authenticated;` 重发还原。

| 跑前/跑后自检 | 跑前(pre) | 跑后(post) | 期望 | 过? |
|---|---|---|---|---|
| 返回表列数 | **34** | **35** | +1 | ✅ |
| 同名函数个数 | 1 | **1** | 不变 | ✅ |
| security_definer(prosecdef) | t | **t** | 不变 | ✅ |
| 权限(proacl) | `{postgres=X/postgres,authenticated=X/postgres}` | **同前** | 前后一致(authenticated execute, 无 public) | ✅ |
| 返回表末列 | — | **round_id uuid** | 末尾是 round_id | ✅ |

迁移执行日志：`白名单放行 get_my_offers` → `BEGIN / DROP FUNCTION / CREATE FUNCTION / REVOKE / GRANT / COMMIT` → `DB-RUN OK`（整体一个事务，失败即回滚）。

## 三、Rollback（原样恢复旧函数 + 原权限）
`jobs/JOB-9-02/rollback.sql`（同步归档 `docs/supabase 文件/0174_get_my_offers_round_id_rollback.sql`）：`drop function get_my_offers(uid uuid)` → `create or replace` 恢复**34 列(无 round_id)**的原全文（取自 2026-09-17 `pg_get_functiondef` oid=27507）+ 同样 revoke public / grant authenticated。已干跑闸验证会被白名单放行（`bash jobs/db-run.sh jobs/JOB-9-02/rollback.sql` 即可回滚）。

## 四、前端（只验，不改）
`_v705OfferAppNo` 第①级 `var rid = offer.round_id;`（v390 第 21254 行）**已按 `offer.round_id` 读**，字段名与 RPC 新列同名；源码注释(21244–21246)本就写明「待 RPC 补 round_id 列后①级生效」。→ **一致，不改前端，学生端版本不动（无 v391）。**

## 五、真登闸（待业主）
没给 stuA 密码 → 浏览器真登 `window.sb.rpc('get_my_offers')` 每行带 `round_id`、已录取那条对应第 1 轮(MH-6KG2JN)、成功卡显「MH-6KG2JN · 第 1 轮」这条**真登闸待业主**。静态侧已证：返回表定义末列 `round_id uuid`、函数体 `select ... od.round_id`。

## 动过的文件
- `jobs/db-run.sh`（加白名单口子 + 干跑闸；本地执行器，未入主仓库）
- `jobs/db-run-drop-allow.txt`（白名单，仅 `get_my_offers`）
- `jobs/JOB-9-02/`：三假迁移 + 0174 迁移 + rollback + probe/pre/post 自检 SQL
- `docs/supabase 文件/0174_get_my_offers_round_id.sql` + `..._rollback.sql`（归档，已 commit `1bd66cd`，未推）
- 生产库 maxhouse_main：`get_my_offers` 已加 round_id 列（真迁移已应用）
- 前端/五端 HTML：**零改动**

**主仓库仅新增两份 SQL 归档(commit 1bd66cd)，未推，留业主原生终端带代理推。**
