# JOB-21-04 回执 · 审核端 preload 警告：源码无可改点 → 不 bump v46，标「未放行·等业主线上核 + CF 设置」

CC 执行。承 21-00 探针⑦（本地 0 条 preload 警告·未复现）。本包只读核验，未改源码，未升版。

## 一、结论
审核端现役 v45 本地真渲染 **preload 警告 = 0**；源码里唯一的 `<link rel=preload>` 是被正确使用的，字体是 `@font-face`（从不 preload，本就产不出「preloaded but not used」）。**源码层没有可改点** → 不新建 v46、不动 index.html/changelog。业主 09-24 在**线上**看到的三条警告，几乎必为 **Cloudflare 边缘注入**（Rocket Loader / Early Hints 自动 preload），只能在 CF 侧关，非源码编辑。**本项标「未放行·等业主线上核」。**

## 二、闸3 证据（真页面 reviewer v45 无头启动 console）
探针 `jobs/JOB-21-00/probes/p7-reviewer-preload.mjs`，本包重跑存 `jobs/JOB-21-04/probes/p7-reviewer-preload-v45.txt`（serve() 本地仓库 → Chromium `--no-proxy-server` → `waitUntil:load` 等 6s，再 `networkidle` 等 9s，收 `page.on(console)`）：
- **PRELOAD WARNINGS (0)**：两轮均零条含 "preload"/"was preloaded"/"not used" 的 console；唯一输出是 `[VER] reviewer portal v45 …` info。
- 旧版本地本就 0 条 → 无「红」可转「绿」，闸3「新版不再出现三条」在本地不适用（本地从未复现）。

## 三、静态清单（grep 全文）
v45 全部资源提示三条，无字体 preload、无运行时注入：
| 行 | tag | 资源 | as | crossorigin |
|---|---|---|---|---|
| 31 | preconnect | supabase.co | — | yes |
| 32 | dns-prefetch | supabase.co | — | — |
| 33 | **preload** | `./vendor/supabase.js` | script | none |
- 第 33 行 preload 的 href 与第 **894 行** `<script src="./vendor/supabase.js">` **完全同 URL**，`as=script` 与请求目的匹配、同源非 crossorigin 无凭据模式冲突 → Chromium 判「已用」→ 不告警。加 crossorigin 反而会与非 crossorigin 的 script 标签产生模式不匹配（21-00 Option B 明确「今日不需要」），故**不动**。
- 字体（v45 第 166–175 行）：6×Manrope + 5×Sora `@font-face`（`../assets/fonts/`, `font-display:swap`），**无任何 `<link rel=preload as=font>`** → 不可能产「字体 preload 未用」警告。
- 运行时注入 preload/prefetch：grep 全文**无**（那 1 条命中即第 33 行静态标签本身）。

## 四、为何三条只在线上
- 源 HTML 本地零告警；三条只可能来自 CF 边缘：Rocket Loader / Early Hints 会为它随后 reorder/defer 的资源在边缘注入 `<link rel=preload>`，这些**边缘注入**的 preload 才告警（与 CLAUDE.md「CF 边缘改写 HTML」既有注记一致）。源码里删不掉不存在的东西。

## 五、给业主（留 21-06「要业主亲眼看的」+ CF 动作，非本包命令）
1. 线上真 URL 打开审核端 → F12 Console，确认三条「resource … preloaded … not used」的具体 href / as（大概率是 CF 注入的字体或某脚本）。
2. 若确在线上：CF 控制台该路由（reviewer-portal/*）**关 Rocket Loader / 关 Early Hints 自动 preload**（Speed → Optimization），非源码改动。CC 可据业主贴回的线上 href 再评估是否有源码侧配套（如给对应字体加显式匹配的 preload 以消 CF 猜测），但**须先有线上 href 实证**（铁律14：无源不臆改）。

## 六、N1
零源码改动、v45 不变 → 静态清单无差集（沿用 21-00 `reviewer-v45-static.json` 基线，trivially 满足）。

## 七、状态
- 主仓库仅新增 `jobs/JOB-21-04/probes/p7-reviewer-preload-v45.txt`（证据），已 add 点名 + commit，**未推**（§0.9）。审核端源码零改动、未 bump v46。
- 本回执已 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**：核验绿（本地 0 告警）、N1 满足；无源码改动、无待部署。**preload 三条本项状态 = 未放行（需业主线上核 + CF 设置）**，非 CC 闸不过。

下一包 = 21-05
