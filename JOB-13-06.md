# JOB-13-06 · 审计完整性核对 + 约束归档（0199 只归档）

## 做了什么
- 把 09-17 卡1083 在 SQL Editor 手改的 `reviewer_access_log.outcome` CHECK 约束（放宽到 7 个值）**补进账本**：`supabase/migrations/0199_ledger_reviewer_access_log_outcome_check.sql`，文件头两行注明「已应用、仅入账本、不再执行」，正文是用 `pg_get_constraintdef` 从生产一字抄出的现约束（**本文件没跑**，铁律16 归档用）。
- 只读核对（`jobs/JOB-13-06/audit-check.sql`，走 db-run.sh，零写库）跑了 5 项。

## 结果是什么（命令：bash jobs/db-run.sh jobs/JOB-13-06/audit-check.sql）
- **① 约束**：7 值 = served / denied_not_assigned / prepared / served_cached / served_original / served_video_url / served_cached_direct（与 0199 归档一字一致）。
- **② 近 24h outcome 计数**：`prepared 115 · served_cached_direct 30 · served 2`。**served_cached_direct = 30 > 0 ✓**——直读命中的审计真的在落地。
- **③「我的操作历史」数据源**：⚠️**点名（铁律14 P2 图纸-现实冲突，只报不自修）**——审核端「我的操作历史」是**浏览器 localStorage**（v35 L1739 `logActivity`/L1754 `getActivityLog`，键 AUDIT_KEY），**不是数据库表**。所以「每条查看 → reviewer_access_log ±60s」这个方向**SQL 侧无法枚举**（操作历史根本不在库里）。改报 DB 侧「查看」真相：近 24h reviewer `80c62372` 共 **32 次查看**（30 次直读 served_cached_direct + 2 次 served），全部有审计行。命中率概念在此不适用（无 DB 侧操作历史清单可比对）。
- **④ EF 写的 prepared/served_cached 在 09-17 17:00 后是否落地**：`prepared_after_1700=0`、`served_cached_after_1700=0`；最后一条 prepared 在 2026-09-17 10:45，served_cached 从来没有（null）。**这是正常的**：`served_cached` 是旧的 signed_url 云函数路径，自 12-01（0196）读侧改直读后就由 `served_cached_direct` 取代（所以 served_cached=0 是设计使然）；`prepared` 是缓存预渲染写的，全库派单基本都已缓存 + 4 件待补渲不出 + 22:50 起 402 限流，所以 17:00 后没有新 prepared。非数据完整性问题。
- **⑤ log_wm_view**：定义确认 `insert … outcome='served_cached_direct'`（卡1084 已把值落回）；`anon 不可执行`（has_function_privilege=false）、`authenticated 可执行`（true）。

## 业主要做什么
- 无需操作。本包只读 + 归档一份 SQL 到账本。
- 一句话结论：审计链是通的（直读查看有 30 条 served_cached_direct 真落地、约束已归档、log_wm_view 权限正确）；唯一「查不了」的是「审核端操作历史↔access_log 对不上号」——因为操作历史存在浏览器本地、不在数据库，这是设计，不是 bug。
