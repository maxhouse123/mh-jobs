# JOB-13-07 · backlog 收口：账本合一 + 邀请重发（迁移 0202 + partner v158）

## 做了什么
### a. 迁移账本合一
- 定 `supabase/migrations/` 为**正本**、`docs/supabase 文件/` 为**镜像**。此前两边各存一半（正本有 0001–0049+0174–0201，镜像有 0050–0173，**互不重叠**）。
- 把两边的 `*.sql` 互相 cp 补齐（只 cp、不 mv、不删），现在两目录都含 0001–0202 全量。
- 写了规则文件 `docs/supabase 文件/README.md`（新迁移 = 正本一份 + 同一 commit cp 到镜像）。
- 写了自检脚本 `jobs/ledger-check.sh`（比对两目录 *.sql 编号集合）。
### b. 邀请重发（迁移 0202，已应用）
- `named_invites` 加列 `resent_at` / `resend_count`；复用 0192 的发信扇出函数，加一只「resent_at 一变就再发一封」的触发器（**不改 send-invite-email 源码**）；新 RPC `resend_delegate_invite` 做四道校验（本人/待接受未过期/距上次≥10min/≤5 次）。
### c. 前端 partner v158
- 待接受邀请卡加「重发邀请」按钮（10 分钟内或已达 5 次自动置灰），点击确认 → RPC → 成功 toast、失败按原因四语提示。

## 结果是什么（命令带出处）
- **账本合一**：`bash jobs/ledger-check.sh` → 合并前差集 190、合并后 **差集为空 LEDGER OK**。两目录 .sql 各由 82/141 补到 215/223（含各自的 rollback 变体；0136 仅有作废说明 .md、无 .sql，不计入编号集合）。
- **0202 验证**（db-run 末尾）：新列 `resend_count/resent_at` 都在、`resend_trigger_present=true`、`anon_cannot_resend=true`、`authed_can_resend=true`。
- **重发实证**（`jobs/JOB-13-07/proof-resend2.sql`，用 is_test 中介 1aad823a 冒充登录态跑）：造/取到发往 `maxhouseapp+invitetest@gmail.com` 的待接受测试邀请（id `fff3e4f2…`，留着不删）→
  - 刚发过立即重发 = **too_soon**（10 分钟节流生效）
  - 把「上次发送」回拨 11 分钟后重发 = **ok**，`resend_count=1`
  - 紧接着再发 = **too_soon**
- **扇出确实触发**（`jobs/JOB-13-07/probe-netresp.sql`）：`net._http_response` 里对应两条请求（创建 id 9176 / 重发 id 9177）都发出去了，**状态 402**——不是代码 bug，是项目当前被 egress 限流（见 JOB-13-00）；业主恢复服务后即为 200、邮件真送达。
- **partner v158 闸3**（`node jobs/JOB-13-07/gate3-partner-v158.mjs`）：`GATE3-PASS`——加载 0 真实报错、`dl.resend*` 7 个键在 en/zh/ru/fr **四语全解析**（含 too_soon 提示）。静态接线核（grep）：按钮 `data-dl-resend`、handler→`_v158Resend`、`_v158Resend`→`rpc('resend_delegate_invite',{p_invite_id})`、原因四语 `_dlT('dl.resend.'+code)`、select 补 `resent_at/resend_count`、10min 灰 `600000` 均在。
- **无 student v393**：命名邀请只有中介能看（`named_invites` RLS `partner_id=auth.uid()`），学生端不列邀请、不共用该函数 → 按 queue 条件不需要 v393。
- 版本四件套：文件名 v158 / [VER] console v158 / partner index 重定向 4 处切 v158 / changelog 加一行。

## 业主要做什么
- **恢复 Supabase 服务后**（见 JOB-13-00 402），中介端 v158 的「已发邀请」列表里，待接受的邀请会多一个「重发邀请」按钮，点后学生邮箱会再收到一封（10 分钟内不能连点、最多 5 次）。
- 那条测试邀请（fff3e4f2，发往 maxhouseapp+invitetest@gmail.com）留在库里没删，可用来复验。
