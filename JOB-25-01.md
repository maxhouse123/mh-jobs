# JOB-25-01 · 学生「多层次 / 多专业」真正上云 + 回填 + 学校侧匹配放开

（CC 写给业主。大白话。这一包把毛病 R1 修好了，但要业主贴一段 SQL 才算全通。）

## 一、这一包解决什么

业主截图那位学生（Ali）报了「语言生 + 硕士（两个专业）」，可学校/管理/中介三端只看见「硕士 · 一个专业」。
根子在两处：
- **提交时**：只把第一个层次、第一个专业上传到云，第二三层次和其余专业根本没上去。
- **库里**：只有一个「单层次」格子（`target_program`）和「单专业」格子（`major`），装不下多个。

这一包做两件事：
1. **给库加一个能装数组的新格子** `target_programs`（迁移 0217，还没执行、等业主贴）。
2. **改学生端**（v402）：提交时把全部层次和专业写进新格子；下次打开时从新格子把全部层次专业还原回页面。

## 二、CC 做了什么

### 1）迁移 0217（只写文件 + 本地空跑验证，绝没真执行）
`supabase/migrations/0217_target_programs.sql`：
- 给 `students` 加一列 `target_programs`（jsonb 数组，每个元素是「一个层次 + 该层次下的专业们」），带「必须是数组」的约束。
- 给学生本人开这一列的写权限（照老规矩 0023/0030 的写法）。
- 学校侧三个查询学生的函数各**多回这一列**（这样学校/管理端才拿得到全部层次去显示）。
- 学校侧「按层次筛选 / 一键匹配」的地方，从「只认单格子」放开为「单格子 **或** 数组里含这个层次」——多层次学生也能被正确圈进对应任务。
- 另一个函数 `add_task_candidate` 本来就不按层次筛，没动（回执写明）。
- **中介代填学生的多层次写入**：本轮不做，留到第二十六轮（见下）。

**本地空跑验证**（`bash jobs/db-run.sh jobs/JOB-25-01/probe-0217.sql`，全程 `begin…rollback`，库里什么都没留）：
- 新列确实是 jsonb、默认空数组 ✅
- 三个查询函数确实都多回了 `target_programs` ✅
- 造一名两层学生（单格子是「硕士」、数组里有「语言生 + 硕士」）：**按第二层次「语言生」能查到（命中 1）**、按没报的「博士」查不到（命中 0，不乱认）✅
- 往新列塞一个「不是数组」的值被约束挡下 ✅
- 最后整段回滚，库无任何改动 ✅

### 2）学生端 v402（从 v401 拷贝改，旧版留盘）
- **提交**：主写照旧写单格子（老功能一点不动）；主写成功后**再补写一次**新数组格子（最多 3 个层次、每层最多 3 个专业，只收六种合法层次）。
- **关键容错**：万一 0217 还没贴、库里没这列，补写会失败——这时只在控制台**提醒一句**、**照样提交成功**，绝不因为新列缺失就让学生交不了。
- **回填**：下次登录，先看开机数据里有没有新数组；没有就单独轻查一次这列（查不到也不报错）。有→把**全部层次、全部专业**还原回第 2 步页面；没有→照老样子只还原一个层次。
- 四件套齐（新文件名 / [VER] 探针 / 版本号 402 / 首页跳转 4 处 / changelog 追加一行）。

## 三、闸门（全过）

`node jobs/JOB-25-01/green-01.mjs` → **PASS=20 / FAIL=0**（日志 `jobs/JOB-25-01/green-01.log`）：
- 静态 5 项：补写块、回填块、单值主写仍在、六值域仍在、`target_programs` 从 25-00 的 0 次变 13 次。
- **拿真页面里那段提交映射代码直接跑**：两层→长度截到 3、第二项是硕士、每层专业截到 3、非法层次被过滤。
- **无头真的调 `loadProfileFromCloud()`**：云端两层→页面重建成 2 层、硕士下 2 个专业；**故意让新列查询报 400（模拟 0217 没贴）→ 退回单层次、零报错**。
- 加载冒烟零报错；N1：学生端 v401 的按钮/内联动作/函数在 v402 里**一个没少**（新增 0、丢失 0）。
- secret-scan 干净；本包没加 shell 脚本；没加新 i18n 键（提示语是硬编码，不会有裸键）。

## 四、业主要做什么

- **贴 SQL**：把 `supabase/migrations/0217_target_programs.sql` **整段**贴进 Supabase SQL Editor 跑一次（幂等/安全，末尾自带只读自检）。**贴完才算全通**。
- **贴完验收**：用测试学生选「语言生 + 硕士（填两个专业）」提交 → 刷新回来，第 2 步两个层次都在、硕士下两个专业都在。
- 贴之前**先不用管**：没贴 SQL 时，学生端照旧只显/只存一个层次，不会报错、不会挡提交。

## 五、边界

- 没动 `target_program` / `major` 老格子的含义；没动其它四个端（学校/管理/中介的**显示**在 25-02 / 25-03，人脸照片在 25-04+）。
- 中介代填学生的**多层次写入**（要给中介那条写库函数也加参数 + 改中介端表单）留第二十六轮，本轮只保证学生本人自填的多层次能上云。

---
- 闸3：green-01 PASS=20/0 ✅；探针 begin/rollback 过 ✅；N1 零消失 ✅
- 迁移 0217 只写文件 + 本地空跑，**未真执行**（待业主手贴）
- 本轮暂无「必须停」（`~/mh-verify/r25-STOP.txt` 不存在）
- 下一包 = 25-02
