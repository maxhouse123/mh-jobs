# JOB-24-04R 回执 · 迁移 0214（管理员裁决进审计事件）—— 修订版做完

大白话：上一趟 CC 停工，是因为老图纸点的几张表跟真库对不上。写卡的 Claude 认了账、改了图纸（修订版 R1）。这一趟 CC **照真库实际情况**把迁移 0214 写好了，并在真库上空跑验证通过（跑完自动回滚，真库一个字没改）。这一包只写数据库脚本，不改任何页面，所以没有版本号变化。

## 一、这一包干了啥
给「管理员自己的裁决」补一本账。以前管理员审批学校/中介、核销或驳回付款、停用/恢复学校、改等级、删材料、换通知书、撤销派单、处理配额/资料变更申请、推进顾问台、发公告——这些动作**审计事件里一条都不记**。现在用数据库触发器（某张表一有裁决变化，数据库自动记一行到 admin_events），而且**只在操作人是管理员时才记**，学校/学生/中介自己的普通更新不会混进来。

## 二、先只读核了真库（这是修订的关键）
CC 没有凭老图纸硬写，而是对真库跑了三个只读探针（都在 `jobs/JOB-24-04/`，跑完回滚，真库零变化）：
- `0214-colcheck.sql` / `0214-colcheck2.sql`：查「表在不在、裁决列在不在」。
- `0214-rlscheck.sql`：查「管理员到底能不能写这些表」。

**结果推翻了上一趟停工报告里的两处判断**：
1. 上趟说 `partner_profile_requests`（中介资料变更申请表）不存在——**真库里其实有**（连同 status / decided_by / decided_at 列都在）。它的建表 SQL 当年是直接在网页 SQL Editor 里建的、没进仓库（历史欠账），所以只搜仓库会漏。
2. 上趟说 `schools` 的等级(tier)/停用等没落点——**真库 schools 有 tier 列，而且管理员对 schools 有「全权直写」策略**（admin_all_schools），所以停用/恢复/改等级/改资料都能被记。

真库确认：schools / documents / offer_decisions / reviewer_assignments / payment_orders 都有「管理员全权(is_admin)」直写策略；partners / quota_requests / concierge_requests / announcement_log 有管理员专用的写入函数（函数内部仍认得出调用人是不是管理员，闸成立）。

## 三、最终建了 10 个触发器，覆盖 17 类事件
| 表 | 变化 | 记成的事件类型 |
|---|---|---|
| schools | 审批(onboarding→active/rejected) | school_application_decided |
| schools | →停用 / 停用→恢复 / 改等级 / 改其它资料 | school_suspended / school_resumed / school_tier_set / school_updated |
| partners | 审批 / 停用恢复 / 改配额 | partner_application_decided / partner_status_set / partner_quota_set |
| partner_profile_requests | 批准/驳回 | profile_change_decided |
| quota_requests | 批准/驳回 | quota_decided |
| payment_orders | 核销 / 驳回 | payment_confirmed / payment_rejected |
| concierge_requests | 状态推进 | concierge_status_set |
| announcement_log | 发公告(新增即记) | announcement |
| documents | 管理员软删材料 | doc_deleted |
| offer_decisions | 管理员换通知书文件 | letter_replaced |
| reviewer_assignments | 管理员撤销派单 | dispatch_revoked |

**跳过 1 类**：`slot_reopened`（重开材料槽）——真库 documents 里找不到可辨识的「重开槽」落点列，也没有对应的管理员函数。CC 不臆造，跳过，留给业主日后若真需要再定落点。

**给业主的一个提示**：`partner_profile_requests` 这张表当年是直接在 SQL Editor 建的、建表脚本没进仓库（跟本轮铁律16「SQL 即归档」是同类欠账）。CC 已按真库实况给它挂了触发器；触发器只在管理员裁决时才记，空转也不会记错。若业主愿意，日后可把这张表的建表 SQL 补进仓库归档。

## 四、验证（真库空跑，跑完回滚，库零变化）
`bash jobs/db-run.sh jobs/JOB-24-04/0214-probe.sql` → **DB-RUN OK**，实测：
- 10 个函数 + 10 个触发器在真库全部能编译、能挂（pg_trigger 实数 = **10**）。
- 实弹一发：借真库现有管理员身份，把一条 concierge_requests 从 open 改成 done → admin_events **确实多出 1 行** `concierge_status_set`（`concierge_status_set_plus1 = 1`）。
- 末尾 `rollback` 执行，真库**零变化**（这一发记账连同临时行全部回滚）。

## 五、闸账（闸3）
- ① `supabase/migrations/0214_admin_decision_log.sql` 存在，含 `_mh_log_admin_event`。✅
- ② 真库「表在+列在+管理员可写」三者都成立的落点都建了触发器（10 个），不存在的/无法辨识落点的（slot_reopened）跳过、不臆造；数目不设上限下限。✅
- ③ begin/rollback 探针：模拟变化 → admin_events +1；rollback 后归零。✅
- ④ 17 类事件类型**逐一比对** admin v229 的 `_v145KindMeta` 词典——**全部已在词典里**（含图标/角色），24-05 无需再补词典。✅

## 六、本包边界与状态
- **不执行迁移**（0214 只写文件，业主收官后 SQL Editor 手贴）；**不改任何前端**；不动学校端/审核端/共用查看器/_headers/云函数。
- **零移除**：本包不碰任何 html，无 id/handler/function 变化。
- 无版本 bump（本包无端代码变化，故无四件套）。
- 主仓库将本包 commit 一笔（0214 迁移 + 4 个只读探针脚本），**不推**（照铁律）。
- STOP 文件 `~/mh-verify/r24-STOP.txt`：本趟不写（图纸冲突已由修订版 R1 化解，不再撞墙）。

下一包 = 24-05
