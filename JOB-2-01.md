JOB-2-01  结果：**停在数据缺失（五条全 FAIL —— 不是代码问题，是测试数据根本没进库）**

## 一句白话
你以为"测试数据已经造好了"，但其实**没有**。造数据的那段 SQL（`jobs/JOB-0.5/seed.sql`）今天 18:41 跑过一次，**失败了**（数据库执行器日志 `RC=3`），而且它是"一整笔一起成或一起废"的写法，所以中间一条错，**五条全部没落地**。我去库里数了，五样东西都是 0 条。

## 为什么失败（我查到的确凿原因）
seed 里造"待核销订单"和"已驳回订单"两行，付款方式写的是 **`wechat`（微信）和 `alipay`（支付宝）**。
但库里 `payment_orders` 表有一道硬规矩（CHECK 约束），付款方式**只认这四个**：`paypal_manual / offline / stripe / flutterwave`。
`wechat`/`alipay` 不在名单里 → 这两行被拒 → 整笔事务回滚 → 连带前面三条更新（二审理由、更换原因、已入学）也一起没了。

> 顺带：状态 `awaiting_review`/`rejected`、类型 `offer_letter`/`concierge` 都是合法的，唯一的病灶就是 `method` 那两个值。

## 逐条结果（都因"库里 0 条"而 FAIL，非页面代码问题）
| # | 验收项 | 库里实测 | 结果 |
|---|---|---|---|
| 1 | JOB-05 二审驳回理由（admin_reason 带【测试】） | 0 条 | FAIL·无数据 |
| 2 | JOB-05 学生更换原因（replace_reason 带【测试】） | 0 条 | FAIL·无数据 |
| 3 | JOB-09 已入学（enrolled_at 非空） | 0 条 | FAIL·无数据 |
| 4 | JOB-06 待核销订单 PO-TEST-AWAIT | 0 条（无任何 PO-TEST 单号） | FAIL·无数据 |
| 5 | JOB-06 已驳回订单 PO-TEST-REJECT | 0 条 | FAIL·无数据 |

页面本身没截图 —— 库里没有这五样，页面上必然是空的，截图只会是"空态"，没有诊断价值；真正的证据是上面的数据库计数。

## 我没做什么（守规矩）
- **没跑 seed，没改 seed，没改任何代码**。你明令"不要再跑 seed"，而且 seed 是写真实数据的操作，我不擅自动。
- 只用只读探针数了数、查了约束根因（探针文件 `jobs/JOB-2-01/probe.sql`）。

## 要你定的一件事
seed 只要把两处 `method` 从 `'wechat'`/`'alipay'` 改成库允许的值（比如都写 `'offline'`），就能跑通。
**要不要我改这一处、然后由你（或让我走 db-run.sh）重跑 seed？** 你点头我再动 —— 在那之前这五条验收无法完成。
（这条不挡后面的包：JOB-2-02 及以后靠的是库里已有的真实数据，与这批 seed 无关，我继续往下做。）

十道闸：本包不改代码，闸1-10 不适用（仅只读探针）。
基底 md5 / 产物 md5：无（未改文件）。
index.html 改了几处：0。
提交哈希：无（无代码改动）。
偏离或疑点：见上——"测试数据已造好"与实测不符，根因是 seed 的 method 值违反 CHECK 约束。
