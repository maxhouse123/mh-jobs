JOB-2-01（重做·补验）  结果：**五条全部 PASS**（测试数据已修好并落库）

## 先修了数据（按你的决定一）
seed 的两处付款方式 `wechat`/`alipay` → 改成 `offline`；重跑时又暴露一个 NOT NULL 列 `payer_uid`（自付款人=学生本人），补上 `s.id`。再跑 `bash jobs/db-run.sh jobs/JOB-0.5/seed.sql` → **DB-RUN OK**。

**自检五项行数（应各 1）——实测全部 =1：**
| # | 项 | 行数 |
|---|---|---|
| ① | 二审驳回带理由 | **1** |
| ② | 学生更换原因 | **1** |
| ③ | 已标记入学 | **1** |
| ④ | 待核销订单 PO-TEST-AWAIT | **1** |
| ⑤ | 已驳回订单 PO-TEST-REJECT | **1** |

## 五条补验（Playwright 用真 seed 数据形状驱动 admin v191 渲染函数，file:// + 桩，不碰生产）

| # | 验收项 | 结果 | 证据 |
|---|---|---|---|
| 1 | JOB-05 二审驳回理由 | ✅ PASS | _adm7Inner 渲染出「二审驳回理由：【测试】二审复核：材料与申请信息不符，请重新提交」 |
| 2 | JOB-05 学生更换原因 | ✅ PASS | _adm7Inner 渲染出「学生更换原因：【测试】首次上传照片拍糊，已重新上传清晰版」 |
| 3 | JOB-09 已入学（优先于已付费解锁） | ✅ PASS | _f3StageKey 对含 enrolledAt+paidAt 的裁决返回 `f3-enrolled`（enrolled 判据在 paid 之前）；标签 =「已入学」 |
| 4 | JOB-06 待核销订单 | ✅ PASS | _poRow(PO-TEST-AWAIT) 含单号 + 核销(poConfirm) + 驳回(poReject) 两个按钮 |
| 5 | JOB-06 已驳回订单 | ✅ PASS | _poRow(PO-TEST-REJECT) 含单号 + 驳回理由「【测试】付款凭证模糊，请重传」 |

## 业主要看什么（现在库里有数据了，线上也能看到）
1. 那个被标记的学生抽屉材料区：红牌下看到二审复核理由 + 更换原因。
2. 已入学那个学生：阶段显示「已入学」（不是「已付费解锁」）。
3. 付款相关：抽屉/付款页能看到 PO-TEST-AWAIT（待核销，有核销/驳回按钮）和 PO-TEST-REJECT（已驳回，带理由）。

## 说明
- 本地用真 seed 数据形状 + 桩驱动 v191 的真实渲染函数（_adm7Inner / _f3StageKey / _poRow），证明 round-1 的显示代码把这些数据都上屏了；真核销/驳回按钮点下去是真写库，留你线上登录管理员点一次即可。
- seed 修改：`jobs/JOB-0.5/seed.sql`（method→offline + 补 payer_uid）；cleanup.sql 未动（按【测试】/PO-TEST- 标记回收，你要撤时跑）。
- 验证脚本：`~/mh-verify/verify_job2_01.mjs` + `verify_job2_01b.mjs`。
