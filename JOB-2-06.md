JOB-2-06  结果：全闸通过（管理端 v186 → v187）

## 做了什么（一句白话）
学生列表和抽屉上那个「🛡️ 证件已核验」绿标，以前读的是管理员本机浏览器缓存 + 三个写死的演示学生（STU001/002/006），所以是假的。现在改成读云端真实审核结果：一次性并行拉 documents + review_verdicts，按学生归并出「护照通过 且 学历（学历证书或成绩单）通过」，只有真通过的学生才亮绿标。演示种子函数保留但不再被调用。

## 判据（照代码写）
- 绿标亮 = 该学生有一份 `passport` 材料审核通过 **且** 有一份 `education`（学历证书）或 `transcript`（成绩单）审核通过（二审驳回的不算）。
- 装载器首屏 + 进学生页各跑一次，两条查询并行（不串行）。

## 业主要看什么
1. 学生列表/抽屉：绿标只在真过审的学生身上亮。
2. 以前假亮的 STU001/002/006（本机演示种子）现在不再亮（除非它们真有云端过审材料）。

## 十道闸（逐条）
- 闸1 基底 md5：`919af0b05a5619bdde26bb6e49aaa9af`（=v186）✓
- 闸2 锚点唯一：6 处各 =1 ✓
- 闸3 补丁自检：loadIdVerifyFromCloud 定义 =1、调用 =2（首屏+学生视图）；_v187ResolveSid =1；_v43IdVerified 改读缓存 =1、旧 LS 读法归 0；_v43SeedIdDemo 调用归 0、本体保留 =1；Promise.all 并行 =1 ✓
- 闸4 产物 md5：`65b5eed9e8bfe55d90b0dab5e30d0821`（v187）
- 闸5 语法：1 脚本块 0 报错 ✓
- 闸6 消失检查：766→768（+2 函数，无消失）✓
- 闸7 四语：管理端中英；未新增 UI 文案（复用现成「证件已核验」标签），零裸键 ✓
- 闸8 真点击：Playwright 桩 documents+review_verdicts —— 护照+学历都过审的学生 _v43IdVerified=true（display_code 和 uuid 两种入参都 true）、只过护照的 =false、故意往 LS 塞 STU001 假记录后 _v43IdVerified('STU001')=false（证明不再读本机缓存），绿标只在真过审学生显示。反向样本 v186：loadIdVerifyFromCloud 未定义 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：新增写死颜色 0 / CSS 类 0 / 字号档 0（绿标样式沿用原 _v43IdBadge）✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：919af0b05a5619bdde26bb6e49aaa9af → 65b5eed9e8bfe55d90b0dab5e30d0821
index.html 改了几处：3（v186→v187）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 装载器按 QUEUE 要求并行查 documents + review_verdicts，按 doc_id join 取权威 doc_type（review_verdicts 自带 doc_type，join 只为更稳；documents 读不到时回落用 verdict.doc_type）。
- 学历判据用云端真实 doc_type `education`/`transcript`（原本机版用中文标签「最高学历证书/成绩单」，云端无中文标签，等价映射）。
