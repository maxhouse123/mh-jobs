JOB-3-04  结果：全闸通过（管理端 v195 → v196）

## 做了什么（一句白话）
两个主段 + 三个"半对"收口：
1. **中介抽屉「委托邀请」段**：读 named_invites（该中介发的邀请），列 姓名/邮箱、状态、是否已被接受、时间。
2. **中介抽屉「代付的款」段**：读 payment_orders（lane=agent 且付款人=该中介），列 单号、类型、状态、应付金额、时间。
3. 三个半对：
   - (a) 中介「操作历史」段并入 撤回推荐/重新提交/推荐注册（referral_withdrawn/resubmitted/signup）——这些事件里没存 partner_id，改按"该中介推荐的学生 uuid"去拉，和原按 partner_id 的记录合并按时间排序。
   - (b) 学生「曝光」段加一行「暂停申请：无 / 待处理 / 已裁决」（走 admin_list_exposure_requests；库里目前 0 条申请，故显"无"，通道已通）。
   - (c) 学校「招生任务」段每个任务加「移出过 N 人」（读 admin_events 的 pool_remove 按任务号计数）。

## 业主要看什么
- pakistan / 那个有委托邀请的中介抽屉：能看到「委托邀请」和「代付的款」两段。
- 中介操作历史里能看到撤回/重新提交推荐。
- 学校招生任务行显示「移出过 N 人」。

## 十道闸（逐条）
- 闸1 基底 md5：`c0a679a119cf722c785a9da957aee2cb`（=v195）✓
- 闸2 锚点唯一：10 处各 =1 ✓
- 闸3 补丁自检：两 fill 函数、named_invites/payment_orders(agent) 查询、两段装配+填充、历史并入 referral、曝光暂停申请算+显、任务移出计数算+显、[VER] v196 各命中 ✓
- 闸4 产物 md5：`ef0dd0fde9223001b31ed9a20f35ec2d`（v196）
- 闸5 语法：1 块 0 报错；[VER] 探针断言 admin=v196 ✓
- 闸6 消失检查：802→806（+4，无消失）✓
- 闸7 四语：管理端中英；新增文案（委托邀请/代付的款/暂停申请/待处理/已裁决/移出过N人 等）中英齐全 ✓
- 闸8 真点击：Playwright 桩 —— 委托邀请段渲染出「张三·已被接受」；代付款段渲染出「PO-9·3000」；学校任务行显「移出过 3 人」；学生曝光段显「暂停申请·待处理」。历史并入 referral 已在源码核（gate3），因驱动整个中介抽屉状态过重未跑真渲染。反向样本 v195：函数未定义 → FAIL（有分辨力）✓
- 闸9 视觉：复用 dd-row/badge/queue-row/var()，零新颜色/类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底/产物 md5：c0a679a119cf722c785a9da957aee2cb → ef0dd0fde9223001b31ed9a20f35ec2d
index.html 改了几处：3（v195→v196）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 曝光「暂停申请」库里 0 条，通道已通显"无"；有数据时会显 待处理/已裁决。
- referral_withdrawn 事件 detail 无 partner_id，按该中介推荐学生 uuid 关联（用 _partnerAgg.sids）——若某中介的 sids 尚未载入则该类不并入（不报错）。
- patch.py 因含大量中文长行，Python 3.9.6 tokenizer 需显式 `# -*- coding: utf-8 -*-` 声明（已加）。
