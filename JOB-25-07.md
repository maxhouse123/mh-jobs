# 回执 · JOB-25-07 · 第二十五轮 收官

做这件事的是 CC（无人值守，一趟一个新会话）。这是第二十五轮第八包，也是收官包。

## 一、这一包做了什么

第二十五轮七个施工包（25-00 ~ 25-06）已全部做完、全部自验绿、全部 commit（未 push）。本包不改任何代码，只做收官账：

1. **写了总账** `~/mh-jobs/SUMMARY-25.md`：
   - 第一行顶格 **`PUSH_OK=yes`**（卡 1187 推送时认这一行）。
   - 版本 / md5 账（student v402 / school v236·v237 / admin v233·v234 / partner v171 / EF v37）。
   - 七包一句话 + 提交号。
   - 闸账：R1–R5 旧病先红后绿、N1 零消失、P3 随卡断言、无头真渲染中英各一遍、闸9 视觉零新增、secret-scan 干净；线上真登冒烟本轮未做（原因写清了——新版还没部署，线上是旧版，验不出新功能）。
   - **待业主手贴的 SQL 全文附录**：0217（多层次多专业）、0218（人脸照取用通道）两段逐字节贴在文末，配各自的验证法。
   - **云函数部署说明**：命令、预期版本 v37、部署后学校端头像应从"水印大图"变成"160px 人脸小图"；并注明管理端 v234 不依赖云函数。
   - 未放行清单、要业主亲眼看的条目、第二十六轮建议包（含必含六项：中介代填多层次写入 / 数据洞察真版 / 范例库云端版 / 门禁页横幅 / 审计筛选记忆 / 付款月报）。
   - STOP 声明：`~/mh-verify/r25-STOP.txt` 不存在（本轮无"必须停"）。

2. **确认了推送前置**：主仓库本地领先远端的七笔提交全是本轮 JOB-25-00 ~ 25-06，没有杂项；干净可推。

## 二、给业主的三件事（收官后做）

1. **推主仓库**：用卡 1187（那张卡认 `SUMMARY-25.md` 里的 `PUSH_OK=yes`）。推完 Cloudflare Pages 会自动部署五端新版。
2. **贴两段 SQL**：把 `SUMMARY-25.md` 文末【附录 A】0217、【附录 B】0218 各整段贴进 Supabase SQL Editor 跑一次，再按第五节的自检语句验一下。
3. **部署云函数**：`supabase functions deploy watermark-doc`（挂代理，仓库根跑），部署后为 v37。

做完这三件，主题 A（全部层次专业）和主题 B（人脸照即时显示）就在线上全生效了。验收条目见总账第七节。

## 三、本包动了哪些文件

- 新：`~/mh-jobs/SUMMARY-25.md`（收官总账 + 两段 SQL 全文附录）
- 新：`~/mh-jobs/JOB-25-07.md`（本回执）
- **主仓库零改动**（本包不施工，只收官）；无需 commit 主仓库。

## 四、收官

第二十五轮八包全部完成。`~/mh-verify/r25-STOP.txt` 不存在。**下一包 = 无（本轮收官）**。
