# JOB-10-03 · TUS 大文件真断点续传（手机 · 只验）

结果：**跳过（手机锁屏，DevTools 端口不可达）—— 不影响其他包**

## 大白话
这包要用电脑隔空驱动那部 Pixel 上的学生端网页做「传大文件传一半断网再续传」的验证。开工自检时发现：**手机停在锁屏界面**（需要指纹/密码解锁）。按规矩我只能发一次「唤醒」键、绝不试密码——唤醒后屏幕亮了，但仍停在锁屏，Chrome 的调试端口（9222）连不上（`/json/version` 和 `/json` 都返回空）。手机没解锁就没法驱动页面，所以这包**跳过**，等业主解锁手机后重跑。这不影响别的包（10-05、10-06 照常做）。

## 断言表（开工自检）
| 检查 | 期望 | 实测值 | 判定 |
|---|---|---|---|
| `adb devices` | 有 `192.168.1.50:5555 device` | 有 | ✅ |
| forward tcp:9222 | 成功 | 返回 9222 | ✅ |
| KEYCODE_WAKEUP（一次） | 唤醒 | mAwake=true / mScreenOnFully=true | ✅ 屏亮 |
| 锁屏状态 | 已解锁 | `mDreamingLockscreen=true`（仍锁屏） | ❌ 锁屏 |
| `/json/version` | 回 Chrome 版本 | 空（不可达） | ❌ |
| `/json`（页面列表） | 回 JSON | 空（不可达） | ❌ |

## 停的依据
QUEUE-8 §0.2：「手机锁屏了 → 先 KEYCODE_WAKEUP 一次，还锁着就停、写回执『手机锁屏，请业主解锁后重启本轮』，不要试密码。」已 WAKEUP 一次、未试任何密码。

## 业主该做什么
解锁那部 Pixel（指纹/密码），确认 Chrome 前台是学生端 v391 页面且 stuA 仍登录，然后告诉我重跑 10-03 / 10-04。

## 边界遵守
- 只做了 devices / forward / WAKEUP（一次）/ dumpsys（只读查锁屏）；未碰手机任何开关、未装任何东西、未试密码、未截图（屏幕锁着且断网红线未触及）。
