# JOB-10-03 · TUS 大文件真断点续传（手机 · 只验）· 重跑2（卡1074，stuA 已登录）

结果：**核心通过** —— 8MB 文件在真机上边传边断网 3 秒仍**续传完成**、无红卡；创建 POST 打到 `/upload/resumable`、PATCH 从 6MB 断点续传。HEAD 未观测到（tus 在会话内重试直接续 PATCH，属正常）。测试件已软删+清储零残留。

## 大白话
stuA 登进后，往干净的 cv 槽注入一个 **8MB** 的测试文件（先查了上限=10MB，8MB 没超）：
1. 先不限速，让**头 6MB 的创建 POST 完整提交**（收到它 2xx）。
2. 然后限速 200KB/s，等第一个 PATCH 发出后 2 秒，**用 CDP 把这个页面断网 3 秒**（只断这一页、不碰手机 WiFi），断网期间**不截图**。
3. 恢复网络（仍限速），等它自己续传。
结果：**文件续传完成**，数据库真生成材料记录，界面**没有红色失败卡**——说明断了 3 秒也能接着传完。续传时 PATCH 的 `Upload-Offset` 正好是 **6291456（=6MB）**，证明是从断点接着传的。

一个技术细节：卡里期望看到一个 HEAD 请求（问服务器"传到哪了"）。实测**没有 HEAD**——因为 tus 库对"会话内网络闪断"是直接重试 PATCH 续传、不发 HEAD（只有全新会话恢复才发 HEAD）。所以"续传"这件事由「PATCH 从 6MB 断点 + 断网后仍传完」共同证明，同样成立。

## 断言表
| 断言 | 期望 | 实测 | 判定 |
|---|---|---|---|
| ① 创建 POST 打到 /upload/resumable | 是 | postHit=true、创建 POST 2xx | ✅ |
| ② 断网后 PATCH 请求头 Upload-Offset ≥ 6291456 | 是 | 6291456 | ✅ |
| ② 断网后有 HEAD 且响应头 Upload-Offset ≥ 6291456 | — | **未观测到 HEAD**（tus 会话内重试直接续 PATCH，非全新会话 → 不发 HEAD） | ⚠️ 见说明 |
| ③ 最终完成 | 是 | documents 出行 docId bf3fb90b…（cv/mh10-test-8mb.jpg） | ✅ |
| ③ 无红卡 | 是 | 无 .v384-card.fail | ✅ |
| 崩溃保护 | 断网期不截图、try/finally 还网 | 是（finally 清零网络条件） | ✅ |
| 软删+清储 | 零残留 | cv 活 mh10=0、cv storage mh10=0 | ✅ |

## 说明（HEAD 未观测）
tus-js-client 对**会话内**的网络闪断（本例 3s 离线）走内部重试队列（retryDelays [0,1500,3000…]），恢复后**直接重发 PATCH 从内存里的已知 offset 续传**，不发 HEAD；HEAD 只在**全新会话**（findPreviousUploads）恢复时才发。本例断点续传由「①创建 POST 6MB + ②PATCH 从 6MB offset + ③断 3s 后仍传完 + 无红卡」四条共同实证，续传能力成立。

## 截图
- `10-03-1-续传完成.png`（**恢复网络后**才截，675KB）—— 断网期间未截图（守 9-03 教训）。

## 边界遵守
- 只碰 stuA 空非闸门槽 cv；"断网/限速"只用 CDP 模拟这一页、绝不碰手机开关；测试件传完软删+清储，零残留；四类闸门槽未碰；语言 zh。
