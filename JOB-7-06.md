# JOB-7-06 · 文案与一致性四项 — 回执

**结果：四项全做完，学生端 v388，走查 7/7 过。statement 列已是 text → 零迁移。**

## 大白话（四项）
1. **材料槽标签去 DOC**：`PDF / JPG / PNG / DOC` → `PDF / JPG / PNG / WebP`（共 3 处：材料槽通用提示、concierge 附件提示、以及那句提示的 HTML 默认文本）。因为上传框 8 月起就不收 doc 了，标签一直在骗人。format 词四语通用，改一处四语齐显。
2. **AI 预检不再露内部代码**：像「does not match the material type code **'criminal'**」这种，**在上屏前**把带引号的代码换成当前语言的材料名（criminal → 「无犯罪记录证明 / Police clearance certificate」）。
   - 用学生端已有的「材料代码→标签」字典（DOCS 数组 key→i18n）复用，**只替换带引号的代码**，绝不动句子里普通英文词（实测「your **bank** statement」不会被误替）。
   - **注意**：这条只做**前端显示替换**。代码本身来自 AI 预检 Edge Function（服务端提示词/输出），**EF 侧的根治留另开包**（要改 EF 并重新部署）。
3. **法俄漏翻补齐**：
   - 「✎ Edit」原本是**写死的英文**（法/俄/中都显 Edit）→ 改成 `t('review.editBtn')`，四语齐：中「编辑」/ 俄「Изменить」/ 法「Modifier」/ 英「Edit」（3 处审核页按钮）。
   - 通知面板标题「Notifications / Most recent first」原本写死英文 → 挂 `data-i18n`，四语齐：中「通知 / 最新在前」、俄「Уведомления / Сначала новые」、法「Notifications / Les plus récentes d'abord」。
   - **四语漏翻扫描**：Playwright 切法/俄，扫所有 `data-i18n` 可见元素找残留英文 → **登录前页面（gate + 静态结构）零残留**。⚠️ 登录后的深层页面需真账号全量渲染才能扫，属线上验收范畴；本次覆盖静态层。
4. **个人陈述上限 200 → 1000**：`students.statement` **实测已是 `text` 类型（无长度限制）→ 零迁移**，纯前端改。
   - 自填页 `statementInput` maxlength 200→1000 + 占位符「max 200」→「max 1000」+ `step3.statementPh` 词典同改。
   - **顺带发现推荐线也有一个个人陈述** `ref_statement`（maxlength 200、计数 /200）——按业主「个人陈述上限 1000」政策**一并改成 1000**（含 /200 计数改 /1000），保持两处一致。

## 十道闸
- **闸 3（静态）**：`PDF / JPG / PNG / DOC` **0 处** ✅；`maxlength="1000"` = **2 处**（自填 + 推荐两个 statement）——⚠️ 队列闸 3 写「1 处」，实际现役有**两个个人陈述输入框**，按业主上限政策两处都改，故为 2；`✎ Edit` 写死 **0 处**、`review.editBtn` 四语齐、fr/ru 包里无「Edit」字面 ✅。
- **闸 8/走查** `tests/job7-06.mjs` **7/7**：DOC→WebP ✅；statement maxlength=1000 + 占位 max 1000 ✅；✎Edit 四语（fr=Modifier/ru=Изменить/zh=编辑）✅；通知标题四语 + 挂 data-i18n ✅；AI 预检 'criminal'→材料名且普通英文词不误替 ✅；四语漏翻扫描完成（fr/ru 静态层 0 残留）✅。
- JS 解析干净。

## 动过的文件
- 改：`student-portal/maxhouse_student_portal_v388.html` + `student-portal/index.html`
- 新增：`tests/job7-06.mjs`、`jobs/JOB-7-06/probe.sql`（只读探针：确认 statement=text）

**版本**：学生端 v387 → v388。**主仓库未推。本回执已推日志仓库。**
