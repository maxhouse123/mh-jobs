# JOB-6-08 回执 · 系统发邀请邮件（2026-09-16）· 只探不改

> 大白话版。**这一包只查不动代码**（队列硬规矩）。查清楚了：平台已经有一套成熟的发邮件机制（用 Resend 发，四语模板齐全），但它现在只能发给「已经有账号的人」；而命名邀请是发给「还没注册的陌生邮箱」，所以现成这条路直接用不了，得加一个小的新函数 + 一个新触发器。下面写清链路、要加什么、代价。**没动任何代码、没建表、没碰 .env。**

## 一、邮件是怎么发出去的（现状链路，已摸清）
- **用的服务**：**Resend**（`api.resend.com/emails`），密钥 `RESEND_API_KEY`，发件人固定 `MaxHouse <no-reply@maxhouses.net>`，页脚客服 `service@maxhouses.net`。
- **两个发信函数（Edge Function）**：
  1. **notify-email**：站内通知扇出成邮件。**触发方式** = 数据库里 `notifications` 表每插一行，AFTER INSERT 触发器 `notify_email_fanout_fn()` 用 `net.http_post`（pg_net）打这个函数（带暗号 `x-mh-hook-secret`）。只有 priority=high/urgent 才发。**收件人靠 `audience_id` 反查 auth 账号邮箱**——所以只能发给已有账号的人。
  2. **send-verify-email**：注册验证信。前端带登录令牌直接调，**只发给调用人自己的邮箱**。
- 两个函数都：Resend 限速自动退避重试（429）、每封信成败都写 `email_dispatch_log`（留投递单号，日后查"没收到"用）。

## 二、模板齐不齐（notify_templates 表，已查）
- 表**存在**，10 个模板，**四语（中/英/俄/法）标题正文全部齐全**：报到提醒 / 行前事项 / 缴费催办 / 材料催交 / 申请季开放 / 系统维护 / 新项目 / 院校入驻 / 新奖学金 / 面试招生。
- **但没有「命名邀请」这个模板** —— 要新加一个。
- 注意：这些模板是"喂给 notifications 行"用的（给已有账号的人），命名邀请发给陌生邮箱，用不上这条组装线。

## 三、命名邀请表长什么样（named_invites，已查）
- 列：`id / token（邀请码，用来拼邀请链接）/ partner_id（哪个中介发的）/ email（可空）/ display_name / passport_no / intents / status / expires_at（有效期）/ claimed_by / claimed_at / opened_at / created_at`。
- 现有 **6 行**。`email` 可空（有些邀请只出链接、没填邮箱）。

## 四、核心结论：为什么现成的用不了
命名邀请是发给**还没有账号的陌生邮箱**。而：
- notify-email 那条路**要求收件人已有 auth 账号**（靠 uid 反查邮箱）→ 邀请对象没账号，走不通。
- send-verify-email **只发给调用人自己** → 也不是发给第三方。

**所以：系统发命名邀请邮件，需要新做一小块，不能纯复用。**

## 五、要加什么（给下一张施工卡）
1. **一个新 Edge Function**（就叫 `send-invite-email`）：**照抄 send-verify-email 的骨架**（Resend 发信块 + 429 退避 + email_dispatch_log 全部原样搬），只改三处：
   - 收件人 = `named_invites.email`（不是登录人）；
   - 邀请链接 = 用 `token` 拼（**待确认**：现在中介端生成的转发链接是什么格式，施工前先读 partner 端确认 URL 参数名，例如 `https://maxhouses.net/student-portal/?invite=<token>`）；
   - 正文 = 命名邀请四语文案 + 变量填充（中介名 / 被邀请人 display_name / 有效期 expires_at / 邀请链接）。
2. **一个新触发器**：在 `named_invites` 表加 AFTER INSERT 触发器，**照抄 `notify_email_fanout_fn()` 的写法**（`net.http_post` 打新函数 + 同款 `x-mh-hook-secret` 暗号），**加个条件：`email` 非空才发**（只出链接的邀请不发信）。
3. **一个模板**：给 `notify_templates` 加一行 `named_invite`（四语），或直接把文案内联进新函数（像 send-verify-email 那样）。**建议内联**——因为要填 token/有效期/中介名等动态变量，内联更直，和验证信一个风格。

## 六、代价 / 风险
- **工作量小**：新 EF ≈ 复制 send-verify-email 改 30 行；触发器 ≈ 复制 fanout 函数改 URL；模板可内联。约 1 张施工卡。
- **要你部署 EF**（CC 只改源码，EF 部署要你在 Supabase 手动 deploy，跟 watermark-doc 一样）。
- **一个决策点**：重发邀请要不要也发信？建议先只在**首次 INSERT** 发（触发器天然如此）；"重发"以后单独做一个 RPC。
- **一个待确认**：邀请链接的确切 URL 格式（施工前读 partner 端确认，别拼错参数名）。
- 暗号 `MH_HOOK_SECRET` 现明文写在触发器函数里（库内），新触发器沿用同一个即可；这是既有现状，不在本包处理。

**边界守住**：只读探针 + 读源码，零代码改动、零建表、零迁移、没碰 .env。
