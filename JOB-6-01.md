# JOB-6-01 回执 · 测试数据标记 + 学校端过滤（2026-09-16）

> 大白话版。做完了：给数据库三张表加了个「这是测试数据」的开关，把现在库里的假学生/假学校/假中介全打上了测试标；真学校登进来浏览就再也看不到这些假学生了；管理端你自己看得到，还能一键隐藏。**主仓库已提交没推**（等你验收后自己挂代理推）。

## 一、这包解决什么
你决定保留测试数据。但真学校登进来浏览候选池会看到 35 个假学生，很尴尬。现在加了个标记：**真学校看不到测试学生，管理员看得到、还能一键隐藏，新注册的默认不是测试。**

## 二、数据库改了什么（迁移 0188，已实跑成功）
1. students / schools / partners 三张表各加一列 `is_test`（是不是测试），默认「否」。
2. 把现在库里的**每一行**都标成测试（你 9 月 1 日确认过平台还没上线，所以现存的全是测试数据）：
   - 学生 **35** 行、学校 **12** 行、中介 **10** 行，全部打上测试标。
   - 跑前测试行 = 0，跑后 = 35 / 12 / 10，**正好等于各表总行数**。
   - **一行都没删**（这是打标，不是删除；db-run.sh 的红线守卫也拦着不让删）。
3. 学校端浏览候选池的两个函数，各加了一句「不要测试学生」：
   - `search_students_blind`（学校搜学生）、`match_task_candidates`（一键匹配候选）。
   - 其余一个字没动，只加这一个条件。
   - 效果：现在 35 个学生全是测试的 → 真学校搜出来 **0 个**。等真学生注册后（默认不是测试）就会出现。**这就对了。**

## 三、管理端改了什么（v202 → v203）
- 学生 / 学校 / 中介**三个列表**：测试的那行名字后面多一个灰色「测试」小徽章。
- 三个列表的**筛选条各加一个「隐藏测试」按钮，默认就是开着的**（所以默认看不到测试的，点一下才显示）。
- 学生 / 学校 / 中介**抽屉详情页顶部**：测试账号会显示一个灰色「测试账号」标。
- 用的都是现成的灰徽章样式和筛选按钮样式，**没造任何新颜色/新样式**。

## 四、验收（你来点）
1. 管理端 v203 开学生/学校/中介三个列表：默认都因为「隐藏测试」开着而是空的（因为现在全是测试数据）；点掉「隐藏测试」按钮 → 全冒出来，每行带灰「测试」徽章；点开抽屉顶部有「测试账号」标。
2. 用 schx 登学校端浏览候选池 → **看不到任何学生**（全是测试的，被挡了）。这是对的。

## 五、自验（三层全过）
- **迁移层**：跑前 0/0/0 → 跑后 35/12/10 = 各表总数；两个函数各含过滤条件 1 次；零删除。
- **UI 层**：三列表徽章、三抽屉标、三筛选筹码、三 select 补列、三映射补字段——grep 全部实证；零裸文案键、零死按钮、零新样式。
- **无头走查**：Playwright 本地开 v203、**全程 abort supabase 网络不碰生产**，新函数全部定义且行为正确、页面**零解析报错**。
- 报告全文见主仓库 `docs/self-test-report-JOB-6-01.md`。

## 六、版本 / 提交 / 迁移
- 管理端 **v203**（旧 v202 保留可回滚）；index.html 3 处重定向已指向 v203；[VER] 探针已改 v203。
- 迁移 **0188_is_test_flag.sql** 已实跑 + 落盘 `supabase/migrations/`（铁律16 归档）。回滚脚本 `jobs/JOB-6-01/rollback.sql`（含删列，破坏性，要你手动跑）。
- 主仓库提交：`admin v203 + index + 0188 + 自验报告`，**已 commit 未 push**。
- 探针/自验脚本：`~/mh-verify/gate8_job6_01.mjs`。

**边界守住**：没删任何行；学生端/中介端 HTML 没动（只动了管理端 + 数据库）。
