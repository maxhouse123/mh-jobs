JOB-2-02  结果：全闸通过（管理端 v182 → v183）

## 做了什么（一句白话）
驾驶舱和合作伙伴页上的四个"永远显 0"的汇总数，全部换成按真实数据算：
- 驾驶舱「已录取」：以前数的是学生表里一个几乎全空的列（stage），所以恒 0；现在改成"真的接受了 offer 的学生有几个"（去重，同一人接受多份也只算 1）。
- 驾驶舱「转化率」：改成"已录取人数 ÷ 已提交申请的学生数"。
- 合作伙伴页头「累计推荐 X 人」+ KPI 卡「累计推荐人数」：以前读一个空字段（referral_count）恒 0；现在改成"各中介推荐数（_partnerAgg.total）加起来"。
- 四处一律：数据还没从云端到货时显「—」，绝不显 0（避免把"没读到"误当成"真是 0"）。

## 先读清四个数字原来的判据（不猜，照代码写）
| 数字 | 原判据（v182 代码） | 为什么恒 0 |
|---|---|---|
| 驾驶舱 已录取 | `已提交学生.filter(stage==='admitted')` | students.stage 云端几乎全空 |
| 驾驶舱 转化率 | `(admitted+enrolled)/已提交` | 分子同上恒 0 |
| 页头 累计推荐 | `sum(合伙人.referralCount)` | partners.referral_count 列空 |
| KPI卡 累计推荐人数 | 同上 | 同上 |

## 业主要看什么
1. 「驾驶舱」顶部：「已录取」应≈16（真接受 offer 的去重学生数）、「转化率」非 0（例如 16/已提交）。
2. 「合作伙伴」页头副标题：「… · 累计推荐 9 人」；下方 KPI 卡「累计推荐人数」= 9。
3. 断网/未登录时这四处显「—」不是 0。

## 十道闸（逐条）
- 闸1 基底 md5：`93b7f4fcef2b01f8a5c02d2538c51feb`（=v182）✓
- 闸2 锚点唯一：5 处各 =1 ✓
- 闸3 补丁自检：旧 stage/referralCount 消费点归 0；新 _v183Decs/_admReady/_partnerAgg 各 =1 ✓
- 闸4 产物 md5：`0c41256c529bf23119811f5178b12909`（v183）
- 闸5 语法：1 个脚本块 0 报错 ✓
- 闸6 消失检查：v182→v183 函数/id/window.* 特征 756→756，消失 0 ✓
- 闸7 四语：管理端仅中英（全站无俄法，符合第一轮结论）；本卡零新增 UI 文案（复用现成标签 已录取/转化率/累计推荐人数），零裸键 ✓
- 闸8 真点击：Playwright 桩数据真调 renderDashboard/renderPartners —— 已录取=16、转化率=80%、累计推荐(页头+KPI)=9；未到货三处显「—」；390px scrollWidth=390 无溢出。反向样本 v182：正例全显 0，与断言不符 → GATE8 FAIL（证明有分辨力）✓
- 闸9 视觉：新增写死颜色 0 / 新增 CSS 类 0 / 新增字号档 0（纯 JS 数字逻辑）✓
- 闸10 线上：本卡不推主仓库，跳过

基底 md5 / 产物 md5：93b7f4fcef2b01f8a5c02d2538c51feb → 0c41256c529bf23119811f5178b12909
index.html 改了几处：3（v182→v183）
提交哈希：见 mh-jobs 推送；主仓库本地提交未推送
偏离或疑点：
- **增长漏斗里的「已录取」我没动**。它也读 stage、也会显 0，但它不在本卡明列的"四个数字"里，边界写着"只动这四个数字"。所以漏斗那格暂时仍是 0，会和上面 KPI 的 16 不一致。建议后续单开一小卡把漏斗「已录取」也换成同一判据。
- 「已录取」KPI 点进去仍按 stage 筛学生列表（可能筛不出人），这是既有行为，本卡只改数字未改筛选，留意。
