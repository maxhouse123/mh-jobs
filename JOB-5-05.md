# JOB-5-05 回执 · 老 doc/docx 文件盘点（2026-09-15）· 只探不改

> 大白话版。**这一包只查不动代码**（队列硬规矩：探完写回执就停）。数据库只跑了只读探针，没改任何数据、没碰 .env、没推主仓库。

## 一句话结论

审核端看不了的老 Word 文件，**总共就 5 个，全是学生材料，全是 7 月 7 日～7 月 24 日的老件**；其中**真正还需要审的只有 4 个**（1 个已经退回、0 个已通过）。而且**产生这些文件的上传口早就关了**（学生端、中介端的学生材料槽现在都不收 Word 了），所以**不会再有新的 Word 进审核端**。这是纯粹的历史遗留，数量很小。

## 各项探到的数字

### ① `documents` 表里的 .doc/.docx（学生材料）
- **总数 5 个**，全是 `.docx`（没有 `.doc`），**0 个被软删**。
- 涉及 **5 个不同学生**（一人一个）。
- 上传时间：**最早 2026-07-07 14:36，最晚 2026-07-24 01:09** —— 全是老件，跟"8 月 26 日起传不上 Word"对得上。
- 按材料类型分：照片 photo 2 个、考勤 attendance 1、无犯罪 criminal 1、健康 health 1。

### ② 这 5 个的审核状态（这是最关键的一格）
| 状态 | 个数 | 说明 |
|---|---|---|
| **无裁决（还没审）** | **4** | ← 真正需要处理的就这 4 个 |
| 已退回 rejected | 1 | 已经退给学生了，学生重传时会是 PDF（口已关），这个不用管 |
| 已通过 approved | 0 | 没有已通过的，所以不存在"已批的不用转"这部分 |

（参照：`review_verdicts` 全表就两种状态值——approved 21 条、rejected 6 条。）

### ③ `school_documents` / `partner_documents` 里的 doc/docx
- `school_documents`：**3 个** doc/docx（学校自己的资质证明件）。
- `partner_documents`：**1 个** doc/docx（中介机构自己的入职证明件）。
- **注意**：这两张表是学校 / 中介**自己的证明件**，id 是独立的 uuid 空间，**不进 `review_verdicts`、不走审核端**（由管理端看）。所以它们**跟审核端 415 无关**，不在本次"审核员看不了"的问题范围内。

### 参照：整个 documents 桶的文件类型分布（未软删）
jpg 157 / pdf 38 / mp4 6 / **docx 5** / png 5 / jpeg 3 —— Word 只占极小一撮。

## ④ 还有没有"活入口"（现在还能不能传上 Word）

| 端 | 学生材料上传口（→ `documents`，会进审核端） | 结论 |
|---|---|---|
| 学生端 v380 | `accept` 全是 `.pdf,.jpg,.jpeg,.png,image/*`，槽白名单 `_slotFilePolicy` 也不含 doc | **已关，传不上 Word** |
| 中介端 v148 | 学生材料槽 `_slotFilePolicy`（v84 注释原话："doc/docx 预览链不认…不收"）**排除 doc/docx** | **已关** |

**所以通往审核端的那条路，两个入口都已经关死了，不会再有新 Word 进来。**

还开着 Word 白名单、但**不进审核端**的地方（供业主知情，非本问题）：
- 中介端 v148：**机构入职上传** `onbFile_`（`accept` 含 .doc/.docx）→ 写 `partner_documents`（中介自己的件，管理端看）。
- 学校端 v229：**任务附件 / 多附件上传**（`handleTaskAttachUpload` 的 allowedExts、`_matPickAttachment` 的 DOC 白名单都含 .doc/.docx/.xls/.ppt）→ 是学校自己的任务附件；**学校端全文没有一处 `insert into documents`，它根本不往学生材料表写**。

## ⑤ 审核端 v31 看 Word 时到底怎样

审核端「查看」走服务端水印函数（watermark-doc EF）取件，函数**只认 jpg/jpeg/png/pdf**（预取过滤 `/\.(jpe?g|png|pdf)$/i`）。遇到 .docx：
- 函数返回 **HTTP 415** → 预览框显示「**该文件类型暂不支持预览**」。
- 预览弹窗里**只有「关闭预览 / 退回 / 通过」三个按钮，没有下载按钮**。下载/打印是 v17 为"仅在线预览·禁止下载/打印/复制"的**水印双盲安全模型特意去掉的**，EF 也不给 Word 烧水印。

**结论：审核员对这些 Word 件既看不了内容、也下不了文件** —— 与队列描述完全一致。

## 三条路的代价对比

**① 一次性本地批量转 PDF、传回桶**（推荐，但需业主点头）
- 真正要转的就 **4 个**未审的 docx（1 个已退、0 个已通过，无需处理）。
- 做法：下载这 4 个 → 本地 LibreOffice `--convert-to pdf` → 传回存储桶 → 更新 `documents` 那 4 行的 `file_name`/`file_path`。
- 代价：文件数极少，一次性人工，半小时内能完事。
- **红线提示**：这要**改已有真实数据**（4 行的 file_name/file_path），属破坏性改动范畴 → **必须业主明确确认后才能做**，本包不做。
- 变体（0 代码）：直接让这 4 个学生**重传成 PDF**（现在上传口已只收 PDF/图片），老 docx 行留着不管即可。

**② 审核端给 doc 加下载按钮、不预览**（不建议）
- **与现有"禁止下载/打印/复制"的水印双盲安全模型直接冲突**（下载按钮正是 v17 特意拿掉的，EF 也不给 Word 烧水印）→ 等于给 Word 开一个绕过水印的下载后门，破双盲底线。
- 而且只解决"下不了"，审核员**仍看不到内容做判断**。

**③ 学校端 / 中介端也收紧白名单**（可选，但不解决 415）
- 通往审核端的口（学生端、中介端学生材料槽）**本来就已经关了**。
- 还开着的是"中介机构入职上传"和"学校任务附件"，这俩**不进审核端**。收紧它们只是让全平台"一律 PDF"更整洁，**对审核端 415 没有帮助**。

## 建议（供业主拍板，本包不实施）

数量这么小（4 个待审），最省事、最不碰红线的是**变体①**：让这 4 个学生把材料重传成 PDF（口已只收 PDF），老 docx 行留档不动，审核端 415 问题自然消失。若业主要保留原件不打扰学生，再走**①转 PDF 回填**（需授权改那 4 行数据）。**②③ 不推荐**（②破安全模型、③不解决问题）。

## 边界

**全程只读**：只跑了 `jobs/JOB-5-05/probe_schema.sql` + `probe_counts.sql` 两个 select 探针（走 db-run.sh，含 begin;/commit;），**没改任何数据、没建表、没改代码、没碰 .env、没推主仓库**。
