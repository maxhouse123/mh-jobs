# JOB-17-04 回执 · D + F①F②：学生端 v397 / 中介端 v165（真页面先红后绿）

做的是 QUEUE-17 的 17-04：护照姓名核对一律软提醒（D）、原因框开着时慢网提示暂停计时且取消改文案（F①）、瘦身行内存兜底不再按文件名以致同名件串槽（F②）。纯前端，零 DB、零云函数、零主仓库推送。

## 一、做了什么

### D 护照姓名核对全程琥珀软提醒（学生端 + 中介端）
- **缩略图外框 + 底色**：`_thumbWrapperHtml` 里 mismatch/noMrz 的 `r2vStyle` 由红改琥珀——学生端 `var(--c-red-600)/--c-red-50` → `#F59E0B/#FFF7ED`；中介端 `#DC2626/#FEF2F2` → `#F59E0B/#FFF7ED`（复用本端软提醒框既有色，不新造）。
- **名单块**（`_r2RosterHtml`，仅 passport）：红叉 `✗` → `⚠`；标题四语软化（去「核对未通过 / 建议删除」失败口吻）。
- **徽章**：`姓名不符 / Name mismatch` → `姓名待核对 / Check name / Проверьте имя / Nom à vérifier`。学生端落 `I18N._sup`（保 zh/ru/fr 不回落英文）；中介端 `badgeMismatch/rosterTitle` 落 `_p98I18N` 四语、渲染点改走 `_p98T`。
- **提醒正文四语换新文案**且 `{mrz}/{form}` 填真名：学生端 `r2.mismatchMsg`（沿用既有 {mrz}/{form} 替换）；中介端 `_p98I18N.mismatchSoft` 换新文案，并**补回被删掉的 {mrz}/{form} 真名替换代码**（调用点 `_r2CheckPassportName`，用 OCR 解出的 mrz 名与表单 formSur/formGiv）。

### F① 原因框开着 → 慢网提示暂停计时；取消原因 → 新文案
- 上传卡 `_v384.add` 的句柄新增 `pauseSlow / resumeSlow / failReason`：`started` 改 `let`、慢网计时抽成 `_slowTick`；pauseSlow 清定时器+记 `pausedAt`+隐藏 `.v384-slow`（在走的秒数不再显示），resumeSlow 把暂停时长补进 `started` 后重开（计时不含填原因那段），failReason 把卡文案改成「需要写原因，未上传」（四语键：学生端 `v397.reasonNeeded` 走 `t()`，中介端 `_p98I18N.reasonNeeded` 走 `_p98T()`）。
- 学生端主上传路（`_uploadOne`）：`_v395AskReason` 前 pauseSlow、后 resumeSlow；取消/留空 → 删已传件 + `failReason`，不再裸「上传失败」。
- 中介端代传路（`_s5CommitPending` 主 insert 支）：把 `_s5cCard.done()` 后移到 insert 成功后（原来在原因框之前就 done 掉、卡已消失无从改文案）；原因框开着 pauseSlow、关框 resumeSlow、取消 → failReason；非原因类报错补 `card.fail(null)`（done 后移后需要显式落红）。

### F② 瘦身行内存兜底改按本件临时编号（不再按文件名）
- `_mhCompressUpload` 瘦身分支生成 `_slimKey`（`sk-序号-时间`）挂到瘦身件、`_v393RecordSlim/_v159RecordSlim` 改按 `_slimKey` 记。
- 暂存/直传的 fileObj 都带上 `_slimKey`；瘦身行兜底 `_v396SlimLine/_v163SlimLine` 与落库 `_v396SaveSlim/_v163SaveSlim`（两个 insert 点）一律按 `_slimKey`，**绝不再按文件名匹配**。
- 结果：同名文件传到另一个槽，旧槽不再冒出别人的瘦身行；刷新后纯按 docId+库 slim_* 渲染，也不冒。

### 四件套（每端）
文件名 v397 / v165（旧 v396 / v164 一字未动，可回滚）；头注 [VER] v397 / v165（并留 `[VER-prev396]` / `[VER-prev164]`）；index.html 重定向各 4 处；docs/changelog 各追一行。

## 二、结果是什么（真页面先红后绿，chromium，`jobs/JOB-17-04/gate/`）
真页面：scaffold `serve()` 本地服务真页 + stuA/partner1 真表单登录 + Chromium `--no-proxy-server`，零 DB 写。判据只认 getComputedStyle 量到的颜色、屏幕可见文字、句柄 typeof。OCR / 真慢网无法无头跑（tesseract 资产 CORS、定时器），故按 N2 处理：把 OCR 会写的 verdict 状态 / 瘦身内存记录按生产口径 seed，再量**真页面真渲染函数**的输出。全量 old/new JSON：`~/mh-verify/JOB-17-04/`；对照表：`jobs/JOB-17-04/gate/RESULTS.md`。

| 修复 | 端 | 旧版（红） | 新版（绿） | 量到的关键值 |
|---|---|---|---|---|
| D | 学生 | **RED** | **GREEN** | 外框 rgb(220,38,38)→rgb(245,158,11)；底色 var(--c-red-50)→rgb(255,247,237)；名单块 ✗→⚠；徽章 姓名不符→姓名待核对；正文含 ZHANG SAN/ZHAN SAN 两名 |
| D | 中介 | **RED** | **GREEN** | 外框 rgb(220,38,38)→rgb(245,158,11)；底色 rgb(254,242,242)→rgb(255,247,237)；✗→⚠；徽章 未定义→姓名待核对；mismatchSoft 含 {mrz}/{form} 两名 |
| F① | 学生 | **RED**（pauseSlow/resumeSlow/failReason=undefined） | **GREEN** | 三法存在；pause→`.v384-slow` display:none；failReason→「需要写原因，未上传」 |
| F① | 中介 | **RED**（三法 undefined） | **GREEN** | 同上，卡 pct 文案「需要写原因，未上传」 |
| F② | 学生 | **RED**（同名件无自身记录也冒瘦身行） | **GREEN** | fileA(`sk-TEST`)→「9页」行；fileB 同名不同 `_slimKey`→`''` |
| F② | 中介 | **RED**（同名串行） | **GREEN** | fileA→行；fileB 同名不同 `_slimKey`→`''` |

四次运行（两端各 old/new）页面报错均 0。无未放行项。

- 元素级绿图（合成姓名 ZHANG SAN↔ZHAN SAN，无 PII）：`~/mh-jobs/shots/17/17-04/{01-student-v397-D-amber.png, 02-partner-v165-D-amber.png}` + INDEX.md；本机全量图 `~/mh-verify/shots/JOB-16/17-04/`（脚本 shot() 目录名固定 16）。红版对照以脚本 getComputedStyle 判定为准。F①/F② 为行为判定、非视觉件，不单独出图（判据见 gate JSON）。

## 三、业主要做什么
- 本包**先提交未推**（主仓库本地 commit，未 push；推送由收官后卡1135 做）。
- **要业主亲眼看的**（进 SUMMARY 清单）：
  1. 学生端 / 中介端传一张「护照机读码姓名和申请表不一致」的护照件：缩略图是**琥珀外框+琥珀底色**、左下角徽章「姓名待核对」、槽位上方名单块是琥珀条带 ⚠（不再红叉），提醒里能看到**两个名字**；切 EN/РУ/FR 文案跟着变。
  2. 学生端 / 中介端在第 3 批触发原因框，开着等十几秒：卡上**不再有往上跳的秒数**；点取消 → 卡上写「需要写原因，未上传」（不是「上传失败」）。
  3. 同一个文件名先后传到两个不同的槽：**旧槽不再冒出别人的瘦身行**，刷新后也不冒。

## 四、新闸证据（N1–N6）
- **N1**：静态差集 v396→v397、v164→v165 均 `removed=0`（ids/handlers/functions），added 仅 `_slowTick`（`_v384.add` 内新抽的私有慢网计时函数，非按钮/非公开入口/非用户可见，照 N1「有意新增」如实记；`pauseSlow/resumeSlow/failReason` 是对象方法字面量、不入 `function 名(` 差集）。清单 `jobs/JOB-17-04/inv/*.static.json`、`~/mh-verify/JOB-17-04/n1.json`。
- **N2**：两端旧版真页面先红（D 红外框/✗/姓名不符；F① 三法 undefined；F② 同名串行），新版后绿；均真登、量真渲染。OCR / 真慢网无头跑不了 → 按 N2 seed 生产态后量真页面真函数输出，逐条在 RESULTS.md 注明。
- **N3**：本包纯前端、无按登录人过滤的库查询；stuA/partner1 身份由 scaffold 真表单登录取得（脚本读 accounts.json，密码不落任何输出）。
- **N4**：D 的 `_thumbWrapperHtml` / `_r2RosterHtml`、F① 的 `pauseSlow/resumeSlow/failReason` 三分支、F② 的 SlimLine 兜底两分支（自身 _slimKey 命中 / 同名不命中）逐条真页面验（ALL-GREEN，见对照表）。
- **N5**：`secret-scan` 对 `~/mh-jobs`（含本回执与 shots/17）→ `SECRET-SCAN-CLEAN`；主仓库 commit 前对 student-portal / partner-portal → `SECRET-SCAN-CLEAN`。
- **N6**：只按图纸做 D + F①F②，未加新活；闸规则 / 批次 / AI 判定 / 瘦身参数 / 三级回落一字未动。F① 中介端把 done() 后移是「原因框期间保住卡」所必需（否则卡已消失无从改文案），已在回执点名。

## 五、动过的槽 / 测试写库
- **本包零材料上传、零测试派单、零 §0.11 批次占用、零 DB 写**：gate 全在真页面内 seed 状态 + 量 DOM/句柄，未调任何写库 RPC、未真传文件（无 OCR、无 storage upload）。无残留测试件，无需收尾软删。

下一包 = 17-05
