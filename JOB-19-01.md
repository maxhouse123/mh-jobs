# JOB-19-01 · 中介端 v167：登录角色门 + 温和提醒改卡片

## 做了什么
1. 中介端 v166 → **v167**（复制留旧版可回滚）。
2. **登录角色门 `_v167Gate(uid)`**：补上中介端此前唯一漏掉的角色校验（学校/学生/审核账号都能登进中介端注册表单的病根）。
3. **温和提醒改真卡片**：`showFormWarning` 从红底 2.2s toast 垫片改成独立琥珀卡片；18-03 的 `_r2SoftToast` 两处并入同一张卡片。
4. 版本四件套：文件名 / 头注[VER] / index.html 三处重定向 / docs/changelog 同步 v167。
5. 三层自验：静态断言 35/35 绿 + 真页面 green 全过（三账号拒绝 + partner1 放行 + 会话恢复 + 卡片 N7）+ N1 差集空。
6. 迁移 0213 **不写**（19-00② 已证 partners 表无 INSERT 策略、学校 uid 本就插不进自己的 partners 行，触发条件为假）。

## 结果

### 角色门口径（照 19-00① 的真实表示，不猜）
- app_roles 只有 admin/reviewer/school 三种 role；**中介身份 = partners 有一行（任意状态）**，`is_partner` 只认 `active` 会把待审/被驳回/暂停的正常中介误挡，故门不用 is_partner，改查「有没有 partners 行」。
- `_v167Gate(uid)` 返回 `allow` / `deny` / `error`：
  - ① partners 有行(任意状态) → **allow**（`partners.select('id').eq('id',uid).maybeSingle()`，照 hydrate 现成自读策略）
  - ② 无 partners 行 → 查别的角色：`is_school`/`is_reviewer`/`is_admin` 三个 SECURITY DEFINER RPC 并发（全无错才采信；任一出错回落直查 `app_roles`，照学校端 v234 第 4882 行）+ `students` 本人行
  - ③ 无 partners 行且无别的角色 → **allow**（新机构注册正常路径）；持别的角色或有 students 行 → **deny**
  - 任一查询出错 → **error**（宁挡不放，提示「网络错误，请重试」四语）
- 三条路都过门，且都在 `partner_bootstrap` 之前：**登录**（`signInWithPassword` 后取 `data.user.id`，deny/error 即 `signOut`+`gateErr` 四语文案+return，不进 hydrate）／**注册**（`signUp` 拿到 session 后）／**会话恢复**（`hydratePartnerAccount(preGated)` 顶部、`_mhBootFetch` 之前；`preGated=true` 由登录/注册传入以免重复过门；两处已认证内部调用 submitOnboarding/refreshPartnerStatus 也传 true）。
- 四语拒绝文案：该账号无中介权限 / This account has no partner access / У этого аккаунта нет доступа партнёра / Ce compte n'a pas d'accès partenaire（走本端现成 `_schL(zh,en,ru,fr)`）。

### 温和提醒卡片（替红底垫片）
- `_v167Card(o)`：独立 `#_v167CardStack` 容器（**不用 `#toast`**，故不被后续 `showToast` 顶掉），白底琥珀（背景 #FFF7ED / 边框 #F59E0B / 字色 #9A3412，与 18-03 同一组），⚠ 加粗标题 + 文件名/说明第二行 + `items` 逐条 `<li>` 分行 + 右上 `×`，默认停 **5500ms**（调用方传 `duration` 优先），多张纵向排列不覆盖；卡片渲染兜底挂了才回退 `showToast`（不静默丢提醒）。
- `showFormWarning({title,message,items,duration})` → `_v167Card`；`_r2SoftToast(msg)` 并入同卡片、停留 8500ms（≥8s 不变）。`showToast(m,'error')` 仍用于真正的报错。

### 三层自验（全绿）
- **① 源码静态断言** `tests/static-check-v167-partner.mjs`：**35/35 绿**（主脚本 parse 干净 / 版本四件套 / 角色门 10 条 / 三条路过门 8 条 / 卡片 N7 10 条 / 4 条无回归红线）。
- **② 真页面 green**（playwright，真登录表单，脚本量数字；结果 `jobs/JOB-19-01/green-results.json`）：
  | 用例 | 判定 | 关键数字 |
  |---|---|---|
  | schX 真登中介端 | **绿** | 停在登录页(locked)、gateErr=「该账号无中介权限」、getSession 空、无机构内容、**partner_bootstrap 请求 0 次** |
  | stuA 真登中介端 | **绿** | 同上 |
  | reviewer1 真登中介端 | **绿** | 同上 |
  | partner1 真登 | **绿** | 正常解锁进入 app |
  | 会话恢复：种一份真实 schX 会话进中介端自己的 storage(mh-partner-auth/sessionStorage) 再打开 | **绿** | 被 `signOut` 登出、gateErr 四语、getSession 空、partner_bootstrap 0 次、控制台 `[v167gate] session-restore blocked: deny` |
  | 卡片 N7（Q1 调用点原样触发） | **绿** | 背景 rgb(255,247,237)=#FFF7ED、字色 rgb(154,52,18)=#9A3412、2 条 items 分行、标题加粗、第 1 秒可见、第 4 秒仍可见、随后 showToast **顶不掉**、× 可关 |
  | 卡片 N7（向导「先勾选」调用点） | **绿** | 同色、可见性/不被顶/×关 全过 |
  | 卡片 N7（R-2 护照软提醒 _r2SoftToast） | **绿** | 同色、可见性/不被顶/×关 全过 |
- **③ 自验报告**：本回执即报告（路径 × 断言 × 结果 × 证据齐）。

### N1 差集（换显示方式，非按钮消失）
`v166 → v167`：onclick handlers 108→108（0 丢失/0 新增）、element ids 205→205（0 丢失）、function defs 582→588（**0 丢失**，仅 +6：`_v167CardStack`/`_v167Card`/`_close`/`_v167Gate`/`_v167DenyMsg`/`_v167NetErr`）。红条变卡片是**换显示方式**，`showFormWarning`/`_r2SoftToast` 函数名与全部调用点原样保留。

### md5
- partner v167：`89b4acf172d4f43996467b7096c8cb9b`

## 业主要做什么
- 本包无需操作（不推主仓库、不部署、不手贴 SQL）。主仓库已 commit 一笔（`7f9bfbd`，未推）待收官卡统一推。
- 收官后可亲眼验收（进 SUMMARY「要业主亲眼看的」）：① 用学校账号登中介端 → 被挡且提示「该账号无中介权限」；② 中介端选一张糊图后弹出的是白底琥珀图标卡片、分行、停 5 秒以上。

## 新闸证据 N1–N7
- **N1**：差集空（见上），仅新增 6 个函数，零按钮/id 丢失。
- **N2**：旧病红在 19-00 已真页面坐实（A 学校账号进注册 RED / B Q1 红底垫片 RED）；本包真页面 green 与之成对（三账号被挡 / 卡片琥珀）。
- **N3**：拒绝测试**真填账号密码真登**；会话恢复用一份**真实 schX 会话**（GoTrue 口令验证取回，从不落盘/打印）；partner_bootstrap 计次为带请求监听的实测。
- **N4**：本包不改任何云函数（watermark-doc/precheck-ai/admin-doc 一行未动）。
- **N5**：`secret-scan.mjs`（严格版）跑 `~/mh-jobs/shots/19/19-01` = **SECRET-SCAN-CLEAN**；公开截图账号邮箱打码、卡片为元素级合成文案、无真实学生 PII。
- **N6**：只改中介端一个文件（+ 其 index / changelog / 一支测试脚本）；未顺手动其它端。
- **N7**：卡片每个调用点都在真页面上量了第 1 秒与第 4 秒可见性、计算后背景/文字色（琥珀一组）、被紧接的 `showToast` 顶不掉、× 能关——全过（见 ② 表）。

## 测试写库收尾（除会话登出外零写库）
- 卡片 N7 走**直接调用**调用点（不开向导、不传文件），**零 DB 写入**；三个拒绝测试是被服务器/前端挡下的登录，**不写业务表**。
- 会话恢复用例：角色门命中 deny 后调 `signOut()`（这是被测的产品行为），只**登出**测试账号会话、**不改角色、不写任何业务表**。带身份探针复核：partner1 名下 referral-drafts 前缀含 q1/blur/synth 的对象 = **0**（本包未上传任何件）。

## 给 19-02 的交接
- 审核端 v43 → v44：未登记类型入册（studyProof×2 / studyPlan×2 / null×1，四语名从学生端主文件/语言包按码原样读出，见 19-00④ 表）+ 预取日志 `[v42]`→`[v44]`。
- **19-00④ 的设计红旗要先按 P2 核**：审核端头注两次写「无语言切换器」、`currentLang` 恒→'zh'、9 处标题恒渲染 `docType.zh`；要落「四语正式名 + 切英/俄/法各截一张」与「现役条目=真中文名」的口径并存会不一致（新条目 en/ru/fr、老条目 zh）。19-02 开工先核这条与图纸是否冲突，冲突则**停工报告不自修**。
- 下一包 = 19-02
