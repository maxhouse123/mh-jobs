# JOB-10-05 · 邀请邮件真发一封

结果：**成功（真发出一封，已被 Resend 接收；发完即作废）**

## 大白话（业主看这段）
按第九轮建的邀请信触发器，真发了**一封**邀请信，**只发给你自己的别名** `maxhouseapp+invitetest@gmail.com`，没发给任何别人。
做法：以最早那家测试中介（巴基斯坦机构，`is_test=true`）的名义，往邀请表插了一行（收件人=你的别名、显示名「邀请信测试」、7 天有效、token 用跟正式流程一样的 64 位随机码）。插进去 3 秒后，系统触发器自动调了发信云函数，**Resend 回了 200「已接收」**，没有报错。发完我把这条邀请的有效期改成「现在」= 当场作废（行没删，只是失效），免得这条测试邀请被真拿去注册。

**请你去 `maxhouseapp+invitetest@gmail.com` 收一封四语邀请信**，链接形如 `https://www.maxhouses.net/student-portal/?invite=<token>`（token 已打码不贴）。如果收件箱没有，翻一下垃圾邮件。

## 断言表
| 检查 | 断言 | 实测值 | 过不过 |
|---|---|---|---|
| 插入行数 | 只 1 行 | INSERT 0 1 | ✅ |
| 收件人 | 只这个别名 | maxhouseapp+invitetest@gmail.com | ✅ |
| token 长度 | 64（与现有行一致） | 64（前缀 7e81f32f…，其余打码） | ✅ |
| 有效期（插入时） | now()+7d | 2026-09-24 03:25:04Z | ✅ |
| email_dispatch_log kind=invite | 出现该行 | 1 行 | ✅ |
| ↳ provider / status / http_status | resend / accepted / 200 | resend / accepted / 200 | ✅ |
| ↳ provider_id | 有 | 01a0ad65-8bb3-7395-b30e-9a16c23fd372（Resend 消息号） | ✅ |
| ↳ error_detail | 空 | 空 | ✅ |
| 收尾作废 | expires_at=now()、不删行 | UPDATE 1，expires_at=2026-09-17 03:25:45Z，status 仍 sent（行在） | ✅ |

## 说明
- `net._http_response` 里没查到「打到 send-invite-email」的独立记录：触发器→EF 的 pg_net 调用应已成功（2xx）并被 6h 窗口/内容过滤略过；而 EF 自己调 Resend 走的是 fetch、不入 pg_net 表。**决定性证据是 email_dispatch_log 的 accepted/200/provider_id**——EF 确认 Resend 收下了这封信。
- 只发一封、未重试、未动 EF、未动触发器。

## 关键 id（供业主核对，token 不贴）
- 邀请行 id：`13dd7f31-7a74-4ffd-ac4c-807272376107`
- Resend 消息号：`01a0ad65-8bb3-7395-b30e-9a16c23fd372`

## 动过的文件 / 数据
- 数据：named_invites 插 1 行（后置作废，未删）。
- 新增：jobs/JOB-10-05/{probe.sql, probe2.sql, probe3.sql, probe4.sql, insert-invite.sql, invalidate-invite.sql, report.md}
