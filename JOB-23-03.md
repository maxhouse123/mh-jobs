# JOB-23-03 回执 · 管理端 v222：审计事件页说人话

做这一步的人：CC（无人值守）。只动管理端一端，版本 v221 → v222（旧版留着可回滚）。

## 一、做了什么（大白话）

「审计事件」这页原来有三处让人看不懂的地方，本卡全改成人话：

1. **每行标题不再是英文代码**。
   - 以前像 `visa_valid_until_set`、`round_closed_by_student`、`share_opened` 这样直接把系统里的英文码摆出来。
   - 现在复用「学生抽屉时间线」里那套已经写好的 41 类中文词表（带图标）：`visa_valid_until_set` → 「🛂 设置签证有效期」、`share_opened` → 「👁 分享链接被打开」、`round_closed_by_student` → 「🔚 学生结束本轮」……英文界面则显示英文名称。
   - 万一遇到 41 类里没有的新类型，先回落到原来那 7 类，再不行才显示「其他 · 代码」，不会再裸奔一串英文。

2. **上方筛选筹码从写死 6 枚改成自动生成**。
   - 以前不管库里有什么，就固定摆 6 个筹码；库里冒出的新类型没法单独筛。
   - 现在是「全部」+ 库里实际出现的每一个类型，按出现次数从多到少排，每个筹码后面带数量（例：「设置签证有效期 · 2」）。点哪个就只看那一类。

3. **详情不再甩一坨原始 JSON**。
   - 以前每行末尾直接把 `{"doc_id":"...","valid_until":"..."}` 这种原始内容拼出来。
   - 现在把认识的键翻成小标签：`offer_id`→「Offer 前8位」、`doc_id`→「材料 前8位」、`app_no`→「申请号 值」、`round_no`→「第 N 轮」、`valid_until`→「有效期至 值」、`open_count`→「打开 N 次」、`reason`→「原因 值」、`task_code`→「任务 值」等（中英各一套）；不认识的键就原样「键: 值」。
   - 原始 JSON 收进一个「原始内容」折叠块，默认收起，想看点开才有。

## 二、动的文件

- `admin-portal/maxhouse-admin-portal-v222.html`（新版）
- `admin-portal/index.html`（三处跳转 v221 → v222）
- `docs/changelog/admin-portal-versions.md`（追加 v222 一行）
- `tests/job23-03-admin-v222.mjs`（随卡静态断言，新）
- `jobs/JOB-23-03/repro/green.mjs` + `shots.mjs`（无头验证 + 截图脚本，新）

主仓库已提交一笔（commit `b3dc365`），**未推**（收官时留业主）。

## 三、旧病由红转绿（对应 23-00 的 R4）

23-00 已量成红：`visa_valid_until_set` 行标题原样显示英文码、行内出现原始 `{"doc_id"` 文本。

本卡后，无头真渲染（桩：12 条 admin_events，7 类，含 visa/round/share 等）实测：

| 断言 | 期望 | 实测 |
|---|---|---|
| 每行标题 ≠ 原始英文码 | 全部不等 | 全部不等 ✅ |
| visa 行标题（中文界面） | 含「设置签证有效期」 | 是 ✅ |
| visa 行标题（英文界面） | 含「Visa validity set」 | 是 ✅ |
| 筹码数 | distinct 类型(7) + 1 = 8 | 8 ✅ |
| 每类筹码计数之和 | = 桩行数 12 | 12 ✅ |
| 正文（折叠块之外） | 不含 `{"offer_id"`/`{"doc_id"`/`{"scope"` | 不含 ✅ |
| 展开「原始内容」后 | 含原始 JSON | 含 ✅ |
| 点某类筹码后行数 | = 该类计数（visa=2） | 2 ✅ |
| 中英切换裸键 / 控制台报错 | 0 / 0 | 0 / 0 ✅ |

无头 7/7 全绿（`jobs/JOB-23-03/repro/green.log`）。

## 四、各道闸

- **闸3 静态断言**：`tests/job23-03-admin-v222.mjs` → **27/27 全绿**（版本四件套、四个新函数定义、renderAuditEvents 走新 label/detail/counts、旧写死 typeLabel 与直拼 JSON 已去、_v222DetailHuman 覆盖 14 个已知键 + details 折叠、_v145KindMeta 与 loadAdminEventsFromCloud 未动）。
- **闸8 无头真渲染**：见第三节，中英各一遍、零报错、零裸键。
- **闸9 视觉零新增**：只复用 `.filter-chip` `.badge gray` `.queue-row` `.wf-hero` + `<details>` 原生元素；无新 CSS 类、无写死新颜色、无新字号。
- **N1（授权移除清单）**：本卡**零移除**——只改函数体，nav-item / 函数 / 数组 / 容器全不动，差集为空。
- **上游回归**：`tests/job23-01-admin-v220.mjs` 28/28、`tests/job23-02-admin-v221.mjs` 35/35 重跑仍全绿。
- **shell-lint**：本包无 shell 脚本。
- **secret-scan**：改动文件全扫，无真实密钥，**clean**。

## 五、要业主亲眼看的（线上验收，留业主）

进「审计事件」，应看到：每行标题是中文（带图标）；下面是「申请号 / 第N轮 / 有效期至」这样的小标签；点「原始内容」才展开原始 JSON；上方筹码按类别（带数量）筛得动。

（本地 stub 只证结构与文案，线上真实 admin_events 长啥样留业主线上看。）

## 六、截图

`~/mh-jobs/shots/23/23-03/`：`audit-events-human.png`（元素级，时间/学生名已模糊）+ `INDEX.md`。

## 七、STOP？

无。本轮暂无 `~/mh-verify/r23-STOP.txt`。

---
下一包 = 23-04
