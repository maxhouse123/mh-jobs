# JOB-15-07 · 收官

生成于 2026-09-19

## 做了什么 / 结果是什么
- 迁移镜像：0205 / 0206 均已 `cp` 到 `docs/supabase 文件/`（0207 未用——见 15-06a，259f456a 判为撤销测试件）。
- `bash jobs/ledger-check.sh` → **LEDGER OK（差集为空）**，正本=镜像=194。
- `git status --porcelain --untracked-files=no` → **空**（本轮所有改动已 commit）。所有 JOB-15-xx 目录已入 git（历史 JOB-01…JOB-14 目录是开工前就未跟踪的旧物，非本轮产物，不动）。
- 主仓库 `fa8ee16..HEAD` 共 **7 笔**（8c36c2b→6180893），**未推**（等业主原生终端挂代理推，四段式推后即验）。
- `docs/changelog/*` 各改动端已各加一行（reviewer/admin/student/partner）。
- 写 `~/mh-jobs/SUMMARY-15.md`（五端版本+md5、迁移清单、两只 EF md5 与部署状态、每包一句话、9 项验收表、第十六轮建议包 A–G）。

## 业主要做什么
1. 原生终端挂代理**推主仓库**（7 笔）。
2. 部署 **precheck-ai v21**（`bash jobs/JOB-15-01/deploy-precheck.sh`）。
3. 逐项做 SUMMARY-15 的 9 项验收表。
- 不推主仓库、不部署、不清桶均由 CC 守住；0205/0206 已在库内（纯新增/软删/配置开关，无破坏性操作）。
