# JOB-16-06 · precheck-ai v22：有钥匙走定时路、有登录态走按需路、都没有才 401（改源码不部署）

CC 做的活。EF 只改源码，不部署——真页面绿灯在卡1119 的线上走查里出（`jobs/JOB-16-06/deploy-precheck.sh` 业主在卡1119 里跑）。

## 一、做了什么

**病根（16-00 已坐实，本包据此修）**：学生端页面走 `window.sb.functions.invoke('precheck-ai',{body:{doc_ids,lang}})`（student v394 L9741），supabase-js 自动带的是**用户 JWT（Bearer）+ 匿名 apikey，不带 x-precheck-key**；而 precheck-ai v21 一进门只认 x-precheck-key → 学生这条按需预检路必 401（16-00 对线上实打坐实）。另发现：仓库 index.ts（=线上 v21，md5 5ad6bbe… 与基线一致）是**只有定时批扫的一条路**，返回 `processed`；而前端一直发 `doc_ids/drafts` 并读 `r.data.results`——按需路/草稿路早被 v20/v21 简化掉了。归档里的 v16（`docs/supabase 文件/precheck-ai-v16.ts`）才有完整三路（返回 results、带 getUser/Bearer/归属）。

**改了 `supabase/functions/precheck-ai/index.ts`（v21→v22，头注升级）**，把三条路按凭据重新分流，处理顺序：
1. `OPTIONS` → 204 带 CORS（不变）
2. 非 `POST` → 405 `method_not_allowed`
3. 取 `x-precheck-key`：**带了**必须等定时钥匙——不等 → 401 `bad_key`；相等 → `mode='cron'`，**行为与 v21 一字不差**（批扫 documents ≥SHADOW_START、doneSet/retrySet、BATCH=3、upsert 影子表），仅两处：①候选查询加 `.is('deleted_at',null)` 跳软删件；②单件处理抽成 `processDoc`（逻辑逐字来自 v21 的循环体，含「下载前看体积 >3000KB 不下」「错误件 attempts<3 才重试」「落库前净化」）。
4. **没带钥匙** → 取 `Authorization: Bearer`：没有 → 401 `no_credentials`；有 → `auth.getUser(token)` 验，失败（含拿公开 anon key 冒充）→ 401 `bad_token`；成功 → `mode='user'`。
5. user 路只许**按需路**（`body.doc_ids`）/**草稿路**（`body.drafts`）；`body.mode` 为 cron/batch/all，或既无 doc_ids 又无 drafts → 403 `forbidden_mode`。
6. 按需路**显式归属校验**（service 客户端绕 RLS，靠代码判）：本人 `documents.student_id==uid` / 该生推荐中介 `students.referred_by_partner==uid` / 有效委托 `student_delegates(active=true,revoked_at IS NULL)` / 管理员 `app_roles.role='admin'`——否则 403 `not_yours`（未找到的 doc_id 也算 not_yours，不泄露存在性）；归属但**已软删**（`documents.deleted_at` 非空）→ 410 `doc_deleted`。
7. 用量护栏：`processDoc` 两路共用，体积闸/3 次封顶天然生效；按需路再**按 uid 限频**——近 1h `ai_precheck_shadow.student_id==uid` 行数 ≥30 → 429 `too_many`（计数来源用现成影子表，**不新建表**）。
8. 所有 JSON 响应统一带 `fn:'precheck-ai'`、`v:22`、`mode`；**body/结果形状与前端今天发的完全兼容**（doc_ids→`{results,mode:'user'}`；drafts→`{results,mode:'draft'}`；cron→`{processed,mode:'cron'}`），前端不用为此再改。判定核心/prompt/批处理逻辑/secrets 一字不动。

**列名先证明**（`jobs/JOB-16-06/probe0.sql`+`probe1.sql`，`bash jobs/db-run.sh` 实跑，DB-RUN OK）：`documents.deleted_at`、`students.referred_by_partner`、`student_delegates(delegate_uid,active,revoked_at)`、`app_roles(user_id,role)`、`ai_precheck_shadow(student_id,created_at)` 均实证在库；stuA 的 id=`43668ae4-…` 即其 auth uid（本人校验 `student_id==uid` 成立）。

**交付物**：`jobs/JOB-16-06/{probe0.sql,probe1.sql,matrix.cjs,auth-matrix.md,deploy-precheck.sh}`。

## 二、结果是什么

- **N4 本地矩阵**（`node jobs/JOB-16-06/matrix.cjs`，Node 24 类型剥离载入 handle，注入假 svc+processDoc，**不连库/不调 AI/不碰 token**）：**ALL-AS-EXPECTED 15 cases**。逐格：OPTIONS 204｜GET 405｜无凭据 401 no_credentials｜错钥匙 401 bad_key｜对钥匙 200 mode:cron｜公开钥匙当 Bearer 401 bad_token｜stuA+自己的件 200 mode:user results=1｜stuA+别人的件 403 not_yours｜stuA+mode=cron 403 forbidden_mode｜stuA+空请求 403 forbidden_mode｜stuA+软删件 410 doc_deleted｜partner1+名下学生件 200 mode:user results=1｜stuA+近1h30次 429 too_many｜stuA+自己前缀drafts 200 mode:draft results=1｜admin+别人的件 200 mode:user（通吃）。
- **闸3**：分支顺序 grep 行号 `handle()` 内 OPTIONS(442) < method !==POST(443) < x-precheck-key(445) < bearer(454)（升序）；括号配平 curly 202/202、paren 511/511；`bash -n jobs/JOB-16-06/deploy-precheck.sh` OK；**v22 md5 = `1c4ce932208c0b63181d223c5ed4cf49`**。
- 主仓库已 commit（`c367531`，只改 `index.ts`+`docs/changelog/edge-functions.md`），**未 push**（等收官后业主用卡1118 推）。本包不升端口版本 → N1 不适用；无公开截图（EF 无 UI）。
- 部署脚本 `deploy-precheck.sh` 全程人话 echo、任何一针不符 exit 1 并打印状态码+响应前 200 字，**不打印任何 token/密码/anon key**（anon 优先取 `$SUPABASE_ANON_KEY`，否则 `supabase projects api-keys`；stuA 密码只由 node 读 accounts.json 不落 echo；登录 JWT 只存变量不打印）。secret-scan 对待推提交与 ~/mh-jobs 全树：CLEAN。

## 三、业主要做什么

- **本包无需现在做任何事。** 到卡1119（线上走查前置）时跑一次部署+探针：
  `cd /Volumes/Dev/MAXHOUSE && bash jobs/JOB-16-06/deploy-precheck.sh`
  （需先 `supabase login` + 挂代理；探针要连 REST 时若提示取不到 anon key，可先 `export SUPABASE_ANON_KEY=<公开anon>` 再重跑；全绿打印「✅ precheck-ai v22 部署 + 探针全过」。）
- 学生端按需预检的**线上 200 绿灯**由 16-09 online-walk 出（stuA 传一张合成图触发 → 控制台 `ai now` sent 1 got 1、precheck-ai=200）。

## 新闸证据（N 闸）
- **N2 先红后绿**：本包是云函数、无「业主看得见的页面毛病」可在本地真页面点红——按 §0.8「云函数改源码不部署」办，绿灯归卡1119 线上走查。旧红=16-00 对线上 v21 实打「POST 学生真 JWT → 401」；新绿= v22 同请求走 user 路（本地矩阵 case 7 已代证分支，线上 200 待卡1119）。
- **N4 云函数每路各验**：见「结果」矩阵 15 格全 AS-EXPECTED（覆盖 QUEUE 列的 12 格 + admin/drafts/空请求 3 格）。
- **N5 密码/截图纪律**：deploy 脚本不打印 token/密码/anon；secret-scan CLEAN。
- **N6 只修不加**：只改 precheck-ai 源码，未新建表、未动 prompt/批处理逻辑/secrets、未部署、未推主仓库。

下一包 = 16-07
