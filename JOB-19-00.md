# JOB-19-00 · 开工检查 + 六探针 + 旧病复现（先红）+ 旧版按钮清单

## 做了什么
1. 核对 §0.2 基线，写 `jobs/JOB-19-00/base-head.txt`（HEAD=94313ba7…），与 QUEUE-19.md 一起 commit（`git add` 点名）。
2. 跑六道探针（只读；按登录人过滤的带身份 N3）。
3. 真页面真点复现五处旧病（先红 N2），存证。
4. 三端（partner/reviewer/student）旧版按钮清单：静态 + 动态，存 `jobs/JOB-19-00/inventory/`（N1 基线）。
5. 测试写入的草稿件当场用 partner1 自助丢弃清干净，带身份探针复核 alive=0。

## 结果

### 基线核对（§0.2）—— 全对，无「必须停」
- 五端 md5 全等：admin v218 `73b36f0a…` / reviewer v43 `183215bc…` / partner v166 `faabaa5f…` / student v397 `6e78713b…` / school v234 `a2d2e593…`；查看器 `assets/mh-viewer-v2-1-1.js` `ee351485…`。
- 云函数源码 md5 全等：precheck-ai `50a3df3c…` / watermark-doc `55debf4d…` / admin-doc `ac5f227a…`。
- `git fetch` 后 HEAD=origin/main=94313ba7…；`git status --porcelain --untracked-files=no` 为空。
- accounts.json 在，stuA / partner1 / reviewer1 / schX 四个真登全 OK（脚本实测，未打印任何密码/token）。四个 uid：stuA=43668ae4… / partner1=1aad823a… / reviewer1=80c62372… / schX=db568ff7…。
- 迁移账本到 0212，下一空号 0213。

### 探针①：角色在库里怎么表示（角色门依据）—— 查清了（§0.9③ 不触发）
`probe1-roles.sql` / `probe1b-rpcdefs.sql`（超级用户 ground truth）：
- `app_roles(user_id uuid, role text)`，全库 role 值只有 **admin / reviewer / school** 三种（各 1/1/9 行）。**没有 'partner' 这个 role**——中介身份=**在 `partners` 表有一行**，不在 app_roles 里。
- `partners` 主键 `id`(=uid)；`students` 主键 `id`(=uid)；`school_members(id,school_id,user_id,role_label,created_at)`。
- 四账号落点：
  | 账号 | app_roles | partners | students |
  |---|---|---|---|
  | stuA(43668ae4) | 无 | 无 | **有(by id)** |
  | partner1(1aad823a) | 无 | **有(active)** | 无 |
  | reviewer1(80c62372) | **{reviewer}** | 无 | 无 |
  | schX(db568ff7) | **{school}** | 无 | 无 |
- RPC 全在（都 SECURITY DEFINER，boolean，参数 `uid uuid`）：`is_admin` / `is_partner` / `is_reviewer` / `is_school`，另有 `partner_bootstrap`(jsonb)。
- **关键坑**：`is_partner(uid)` 定义 = `exists(select 1 from partners where id=uid and status='active')`——**只认 active**。而角色门要「有 partners 行就放行」（含 onboarding/被驳回/暂停等未 active 态，否则会把待审/被驳回的正常中介挡在外面）。所以 **v167 门要查「有没有 partners 行(任意状态)」，不能用 is_partner**——照 `hydratePartnerAccount` 现成的 `partners.select('id').eq('id',uid)`（自读策略在，见探针②）。
  → **v167 门口径定案**：①有 partners 行(任意状态)=放行；②无 partners 行且 app_roles 无任何行、students 无本人行=放行进注册；③无 partners 行但 app_roles 有(school/reviewer/admin) 或 students 有本人行=`signOut()`+「该账号无中介权限」；查询本身出错=按拒绝并提示网络错误。

### 探针②：partners 写入策略 —— **0213 不需要写**
`probe2-partners-policy.sql`：
- `public.partners` RLS 开。策略只有三条：`partners_admin_select`(SELECT is_admin) / `partners_self_select`(SELECT id=auth.uid()) / `partners_self_update`(UPDATE id=auth.uid() AND status='active')。**没有任何 INSERT 策略**。
- 在 rollback 事务里模拟 schX 身份(`set local role authenticated`+jwt.claims sub=schX，`auth.uid()` 回显=schX)`insert partners(id=自己)` → **`ERROR: permission denied for table partners`**（连 INSERT grant 都没有）。
- 结论：**学校角色 uid 无法插入自己的 partners 行**（数据库层已挡死，与业主观察的「save_my_draft 被 42501 拒」一致）。19-01 的迁移 0213 触发条件（「学校角色能插自己 partners 行」）**为假 → 0213 不写**。前端角色门是纯 UX/纵深防御补丁。

### 探针③：中介端 showFormWarning 全部调用点
`showFormWarning` 定义在 **v166 第 11224 行 = 垫片**：`var m=(o.title?o.title+': ':'')+(o.message||''); if(o.items&&o.items.length){ m+=' - '+o.items.join(', '); } showToast(m,'error');`——把 {title,message,items} 拍平成一行、走红色 error 条、**丢弃 duration**。`showToast`(14820)=`#toast` 单行 textContent + class error + 固定 2200ms。
14 处调用点（行号/title 键/有无 items·duration）：
- 8036 `s9.deleteBlockedApproved`(message) · 8037 `s12.deleteBlockedRequired`(message) · 8043 `toastForm.filesRejectedTitle`+items
- **Q1 三处**：8651-8653(`q1.warnTitle`+message=文件名+items[pdfUnreadable]+duration:8000) · 8658-8660(同+items[pdfPages]) · **8684-8685(`q1.warnTitle`+message=文件名+items=iss+duration:8000 ← 图片糊/分辨率就是这条)**
- 9143 `cloudOffer.noAuth`(message) · 10435 `resubmit.noPending` · 10445 `q1.warnTitle`+`q1.gateOne` · 10586-10587 filesRejected+items · 10589-10590 `fix.dupTitle`+items · 10649-10650 filesRejected+items · 14064 `q1.warnTitle`+`q1.gateOne`+items=_wzNeed(向导「先勾选」) · 14657 `q1.warnTitle`+`q1.gateOne`
- 18-03 的 `_r2SoftToast`（琥珀软提醒，独立 #r2SoftToast 元素）：定义 11432，调用 11470 / 11480（护照无机读码 / 姓名不一致两处）。→ 19-01 这两处并入同一张卡片。

### 探针④：审核端未登记类型 + 学生端四语名
`probe4-queue-names.mjs`（reviewer1 真登 REST 取自己队列 60 行 + vm 模拟学生 t() 取名）：
- v43 `DOC_TYPES` 25 键（无 studyProof/studyPlan）。队列里**未登记类型**：**studyProof×2 / studyPlan×2 / (null)×1**（共 5/60）。这些走 `_v43SafeType` 兜底 → 显示 doc_type 原值（英文代号）或「未知类型 / Unknown type」。
- 学生端四语名（学生 t() 顺序 `_sup[lang]→I18N[lang]→I18N.en→key`，与 18-04 逐字一致）：
  | 码 | zh | en | ru | fr |
  |---|---|---|---|---|
  | studyProof | Proof of study | Proof of study | Справка об обучении | Attestation d'études |
  | studyPlan | Study plan | Study plan | План обучения | Plan d'études |
  - 注：学生端 **zh 材料名=英文**（平台既定「zh 仍英文名」），学生端无这两码的中文名。
- **给 19-02 的设计红旗**：审核端 v39/v41 头注两次写明「审核端无语言切换器」，`currentLang` 恒 undefined→'zh'，9 处标题恒渲染 `docType.zh`（现役已登记类型都是真中文名，如护照首页/推荐信）。要落「四语正式名 + 切英/俄/法各截一张」，需让新登记条目带 {zh,en,ru,fr} 且渲染按 currentLang 取（reviewer 可用 mh_lang/JS 设 currentLang 出图）——这与「现役条目=真中文名」的口径并存会不一致（新条目 en/ru/fr、老条目 zh）。19-02 开工按 P2 先核这条与图纸是否冲突，冲突则停工报告，不自修。

### 探针⑤：watermark-doc v33 静态分段（子代理只读通读源码）
一次请求依次：①鉴权(JWT 本地解码，失败才 `auth.getUser()`=0–1 次 DB) → ②角色/派单(assignP `reviewer_assignments` + `is_reviewer` 已并行 spawn；docLookupP `documents` 并行) → ③软删门(用 docLookupP 结果) → ④取原件(1 次 Storage download，`arrayBuffer()`) → ⑤嗅探类型 → ⑥水印(image/pdf，纯 CPU) → ⑦审计(1 次 DB，图片走 waitUntil 异步) → ⑧返回。已有 `Server-Timing` 头(mark auth/role/own/doc/dl/wm/audit + region + iso cold/warm-n<seq>)。三问：
- (a) 可并行未并行：角色判定 if-链 `is_reviewer`(604)→`is_school`(611)→`students`(617)→`students`partner(624) 是**串行 if-链**，逻辑互斥、可一次并发。（assignP/docLookupP/dlP 已并行/已最优。）
- (b) 缓存命中：主渲染路**无缓存查找**；缓存只在独立的 `handleSignedUrl`(signed_url 模式) 用——命中即签 URL 返回，**不取原件、不重渲染**（已最优，19-03 (b) 无可修）。
- (c) 是否流式：**全缓冲**（line 729 `arrayBuffer()` 整读入内存）。但水印本身必须拿到整份字节才能画（image/pdf 无法边流边水印）；唯一能流式的是 school 直看原件的 `served_original` 旁路。→ 19-03 (c) 对水印路基本无空间，只 served_original 可议，回执里如实说。
- outcome 值：denied_not_assigned/denied_revoked/denied_doc_deleted/denied_not_candidate/denied_not_owner/served/served_original/served_video_url/served_cached/prepared/served_video_signed/unsupported_file_type。
- 结论喂 19-03：v33 已有 Server-Timing，19-03 主要是加 `x-mh-wm-timing` 专用头 + `boot_age_ms` + `served=` + console 行；能修的只有 (a) 角色判定并行；(b)(c) 基本无空间——若线上 30 份计时坐实冷启动占大头，按 §0.10 只写保温建议、不加定时任务。

### 探针⑥：存储孤儿（只查不删）
`probe6-orphans.sql`：stuA(43668ae4) 前缀下含 online-synth/MH17/MH18/synthetic 的对象**只有 1 个**：
- `43668ae4-…/recommendation/1789916981387_j0sl_r16-online-synth.png`，220489 字节，创建 2026-09-20 15:09:43 UTC，`documents`（含软删）**0 行** → 确证孤儿（第十六轮 online-walk 遗留，SUMMARY-18 建议包已点名）。→ 交 19-05 出清单，业主手删。

### 旧病复现（真页面真点，先红 N2）
- **A 登录角色门缺失**（`repro-A.mjs`，中介端 v166 真登真表单）：schX/stuA 各真登 → **落进「机构注册」表单**（onbInputs=3、getSession 有会话、`partner_bootstrap` 已发）；reviewer1 → getSession 有会话 + bootstrap 已发（被无关的 E3d 邮箱验证门挡在表单前，但**中介会话与 bootstrap 已泄漏**）。三者都拿到了本不该有的中介会话。截图 `A-v166-schX-in-register-RED.png`。**红**。
- **B Q1 提醒垫片**（子代理真页面：partner1 真登 → 推荐向导四步走到材料步 → transcript 槽 setInputFiles 一张 600×600 纯灰糊图）：Q1 命中 2 项(`warn:2`：分辨率<800 + blurVar=0<80)，`showFormWarning` 垫片把「标题+文件名+两条问题」拍成**一行**、`#toast` 背景 **rgb(239,68,68)（红）**、白字、class `toast error show`、**1 秒可见 / 4 秒已消失**（8000ms 被忽略、2200ms 生效）、**无独立琥珀卡**。截图 `B-v166-q1-red-toast-1s.png`。**红**。（向导未提交即弃；测试草稿件已清，见下。）
- **C 审核端未登记类型**（探针④+源码）：studyProof/studyPlan/null 走 `_v43SafeType` 显示英文代号/「未知类型」，非四语正式名。**红**。
- **D 审核端预取日志标签**：v43 第 **2807 行** `console.info('[v42] prefetch enqueue'…)` 仍是 **[v42]**（计时日志已是 [v43]，独预取入队标签漏改）。**红**。
- **E 学生端引号**（v397 `_v706HumanizeIssue` 第 10103 行起）：把带引号的英文代号替换成材料名时**恒用中文括号** `'「'+map[code]+'」'`，英/俄/法界面下也是「」（如「Police clearance certificate」）。**红**。

### 旧版按钮清单（N1 基线）
`jobs/JOB-19-00/inventory/`：
- 静态：partner-v166-static.json(ids 205/handlers 120/fns 743) · reviewer-v43-static.json(74/31/157) · student-v397-static.json(368/188/1116)。
- 动态（登录后首屏可见交互件，文字已 mask）：partner-dynamic.json(69) · reviewer-dynamic.json(198) · student-dynamic.json(63)。

### 测试写库收尾（除测试件外零写库）
- 唯一写库=向导暂存把糊图上传到 `partner1/referral-drafts/transcript/…_q1-blur.png` + clobber 了 partner1 的 referral_drafts 草稿行（partner1 为测试账号）。
- 用 partner1 自己的会话走 app 的丢弃路（storage remove 200 + referral_drafts delete 204）清掉；带身份探针复核：q1-blur alive=**0**、partner1 referral_drafts 行=**0**。
- 其余 partner1 referral-drafts 前缀下的旧对象是往轮遗留、非本包所建，未动（本轮孤儿清单 19-05 只管 stuA online-synth 关键词范围）。

## 业主要做什么
- 本包无。（不推主仓库、不部署、不手贴 SQL。）

## 新闸证据 N1–N7
- **N1**：三端静态+动态旧版清单已存 `inventory/`（本包只建基线，差集在各施工包比）。
- **N2**：A/B 先红均为**真页面真点**（真登录表单 / 真向导传真文件），红的样子与业主所述一致；C/D/E 有源码+队列实证坐实红。
- **N3**：角色/策略/队列/孤儿探针全部带身份或超级用户 ground truth；无身份 0 行未用作证据。四账号真登拿 uid。
- **N4**：本包不改云函数，watermark-doc 鉴权路矩阵留 19-03。
- **N5**：`secret-scan.mjs` = **SECRET-SCAN-CLEAN**；动态清单/截图无邮箱等 PII（grep 0）。
- **N6**：只做本包写明的探针与复现，未顺手改任何端。
- **N7**：B 已按 N7 量第 1 秒/第 4 秒可见性 + 计算后颜色（红 rgb(239,68,68)，非琥珀）——正是要在 19-01 改绿的对象。

## 给 19-01 的交接
- v167 角色门口径已定（见探针①末），**0213 不写**（探针②证明）。
- showFormWarning 垫片(11224) 改真琥珀卡；14 处调用点 + `_r2SoftToast` 两处并入同一张卡（清单见探针③）。
- 下一包 = 19-01
