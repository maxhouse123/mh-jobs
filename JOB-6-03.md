# JOB-6-03 回执 · 开学季选项后台可配 A（2026-09-16）

> 大白话版。做完了：数据库建了一张「开学季」表，把现在写死在代码里的入学学期搬进去（先塞了 3 个：2026 秋 / 2027 春 / 2027 秋）；管理端侧栏多了「系统设置 → 开学季」一页，你能加学期、启用/停用、调顺序，中英俄法四语各填一格。**下半场（三端改成读这张表）是 JOB-6-04。主仓库已提交没推。**

## 为什么
「意向入学学期」的选项现在写死在三端的生成器里，每年都要改代码。这包把它挪到后台可配。

## 数据库（迁移 0189，已实跑成功）
- 新表 `intake_terms`：中/英/俄/法四语标签 + 年份 + 季节 + 排序 + 启用开关。
- 回填了学生端现在生成器会产出的 **3 个学位学期**：Fall 2026 / Spring 2027 / Fall 2027（英文那栏就是提交时存库的规范值，跟学生端下拉一致）。
- 加了读函数 `list_intake_terms()`（只回启用项、按排序），权限：读给所有登录用户，改（增/删/停用）只有管理员能做。
- 自检：表 3 行、启用 3 行、函数产出 3 行，全对上。

## 管理端（v204 → v205）
- 侧栏新增「**系统设置**」分区 + 「**开学季**」页。
- 页面 = 列表（排序/中文/EN/RU/FR/年季/状态/操作）+ 新增表单（四语各一格 + 年份 + 季节 + 排序）+ 每行「启用/停用」+「上移/下移」调顺序。
- 增/改/排序直接写库（管理员权限），改完自动刷新。

## 验收
管理端多一页「开学季」；能加一个「2027 秋」并启用；能停用某个、能上下移调顺序。（真写库需登录 admin，留你验收。）

## 自验（三层全过）
- **迁移层**：建表 + 回填 3 + RLS + 函数，自检数字全对，零删除。
- **UI 对照**：nav/view/注册齐全；i18n 四键双语（零裸键）；四语表单 + 列表 + 启停 + 排序按钮全在；零新样式。
- **无头走查**：Playwright 本地开 v205、**全程 abort supabase 不碰生产**；页面渲染出四语列、两行合成数据、状态徽章、排序/启停按钮；**非网络 console 报错 = 0**。
- 报告全文见主仓库 `docs/self-test-report-JOB-6-03.md`。

## 版本 / 提交 / 迁移
- 管理端 **v205**（旧 v204 保留）；index 3 处 + [VER] + 头注均改 v205。
- 迁移 **0189_intake_terms.sql** 已跑 + 落盘 `supabase/migrations/`。
- 主仓库：`admin v205 + index + 0189 + 自验报告`，**已 commit 未 push**。
- 自验脚本：`~/mh-verify/gate8_job6_03.mjs`。

**下一半**：JOB-6-04 让学生/学校/中介三端下拉改读这张表（读不到就回落原生成器）。
