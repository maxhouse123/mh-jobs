# JOB-14-03 · 视频签名播放（watermark-doc signed_video + reviewer v37 第三部分）

## 做了什么
- **云函数 watermark-doc（改源码未部署）**：加一个新模式 `signed_video`——审核员看学生的自我介绍视频。派单归属校验和看图一样；文件必须是 mp4/mov/webm/m4v（否则 415）；用 service_role 给该视频签一条 600 秒有效的直链（不做服务器端水印）；审计记 `served_video_signed`；返回 {url, rv_code, expires_at}。
- **审核端 v37**：视频材料的卡片按钮改成「▶ 查看视频」（不再走水印渲染、不 hover 预取）；点开 → 取签名直链 → 用 `<video controls playsinline controlsList="nodownload noremoteplayback" disablePictureInPicture>` 播放、禁右键；播放器上叠一层**软水印**（审核员编号 + 时间，半透明、平铺、每 30 秒换位、不遮内容）；顶部仍是「仅在线预览·禁止下载」条；签名到期（约 590 秒）自动重取一次；错误照旧映射（415/403/401）。

## 结果是什么（命令带出处）
- **EF 源码**（`grep -cF`）：`signed_video` 2 处、`served_video_signed` 2 处；括号配平 **BALANCED ok**；新 md5 = `64f09b82aedb27ea59cd7223b8ccf1f4`，**未部署**。
- **reviewer v37 闸3**（`node jobs/JOB-14-03/gate3-video.mjs`）：`GATE3-PASS`——
  - stub 返回 {url} → `<video>` 出现、controlsList 含 nodownload、disablePictureInPicture 在、软水印层含审核员编号 RV-TEST；
  - stub 返回 415 → 出现「退回·让学生重传」按钮（走 14-01 的退回）；
  - 0 真实报错（假 blob URL 的加载错已过滤）。
- 版本：reviewer v37 [VER]/头注已含 14-03；edge-functions changelog 加 signed_video 一行。

## 业主要做什么
- 部署 watermark-doc（新 md5 64f09b82…）**前**先在 SQL Editor 跑 `0204_outcome_video_signed.sql`（否则视频审计值会被旧约束拒；不跑也不影响播放，只是审计写不进）。
- 部署 + 推送后：审核端打开一份 mp4 自我介绍 → 应能在线播放、带禁下载 + 软水印，不能右键下载。验收并入验收表第 2 项。
- 学校端/管理端的视频查看本轮不动。
