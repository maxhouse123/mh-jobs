# JOB-5-04 回执 · 学生端手机登录框显示大写 + 密码眼睛（2026-09-15）

> 大白话版。做完了：学生端、中介端、学校端三个登录门一起修。**主仓库没推**（本地已提交 `c222be8`，等你验收后自己挂代理推）。

## 一句话结果

手机上（尤其安卓 Pixel）打开登录页，**邮箱框再也不会自动变大写、能正常登进去了**；密码框加了个**「眼睛」按钮，点一下能看见自己打的密码**。三个端都修了。

## 先探到的根因（铁律19：先读源码再下结论）

7 月 28 日移动巡检卡的那个「邮箱显示大写登不进去」，我把三样都查了：

1. **不是 CSS 把它变大写的** —— 登录框上 `text-transform: uppercase` **一处都没有**（学生端只有「邀请码框」有大写样式，那跟登录无关）。
2. **不是 JS 把值改大写的** —— 全文搜 `toUpperCase`，只出现在邀请码 / 证件识别（OCR）那些地方，**没有一处动邮箱或密码的值**。
3. **真正的原因是手机键盘的「首字母自动大写」** —— 输入框少了 `autocapitalize="none"` 这个属性，手机键盘就默认把首字母大写，打出来就成了 `Maxhouseapp@...`，跟注册时的小写对不上，所以登不进。

**各端当时的实际情况不一样，这次分别处理：**

| 端 | 邮箱框原状 | 密码框原状 | 有没有大写病 |
|---|---|---|---|
| 学生端 v379 | 已有 `autocapitalize=none`（前版修过）| 缺 `autocapitalize`/`autocorrect` | 邮箱已不犯，密码框待补 |
| 中介端 v147 | **完全没有** `autocapitalize` | 也没有 | **真有大写病，这次才修上** |
| 学校端 v228 | 已有 `autocapitalize=none` | 已有 | 没有大写病，只缺眼睛 |

密码「眼睛」按钮：**三个端原来都没有**（我到中介端、学校端登录门找过，都没现成的可抄），所以这次是新做的一个最小按钮。

## 具体改了什么

**学生端 v379 → v380**（主目标）
- 邮箱框补 `inputmode="email"`（手机弹出邮箱专用键盘，带 @ 键）。
- 密码框补 `autocapitalize="none" autocorrect="off"`。
- 密码框右边加「眼睛」👁，点一下变 🙈 并显示明文，再点回去。

**中介端 v147 → v148**（顺手同修，它真有大写病）
- 邮箱框补齐 `spellcheck="false" autocapitalize="none" autocorrect="off" inputmode="email"`。
- 密码框补 `autocapitalize="none" autocorrect="off"` + 眼睛。

**学校端 v228 → v229**（顺手同修，只缺眼睛和两个小属性）
- 邮箱框补 `autocorrect="off" inputmode="email"`（大写属性它早有了）。
- 密码框补 `autocorrect="off"` + 眼睛。

**眼睛按钮怎么做的**：纯一个 emoji 图标（👁/🙈），用内联样式定位在密码框右侧，**没有新增任何颜色 / 类 / 字号 / 文案**，所以**不需要动四语翻译包**——学生端主文件里写死的翻译包文件名保持不变，引用的还是原来存在的包，不会引到不存在的文件。只动了登录门，注册框、忘记密码框都没碰。

## 十道闸自验结果

- **闸1 基底 md5**：三端全对上 §0 的表（学生 `fcc25c68…`、中介 `d311af40…`、学校 `745c914b…`）。✅
- **闸2 锚点唯一**：每处改动锚点都恰好命中 1 次。✅
- **闸3 判据**（队列原文）：登录门 `text-transform` **0 处**；`autocapitalize="none"` 三端各 ≥ 2 处；眼睛切换函数各定义 1、调用 ≥ 1。✅
- **闸5 语法 + [VER]**：中介 0 错、学校 0 错且 `[VER] school portal v229` 自动核对通过；学生端有 1 处「block1 cloudflare」报错，**是 v379 基底就有的老毛病（同样报，非本次引入）**，我的改动在后面的脚本块，块数不变仍 5。学生 / 中介的 `[VER]` 因工具只自动认 admin/school 文件名，我用闸3 计数核实：新 `[VER] student portal v380` / `partner portal v148` 各 1 处、旧号归 0。✅
- **闸6 功能消失比对**：三端各只 +1 个「特征」（就是新加的眼睛按钮），**消失项 0**。✅
- **闸8 无头真点**（Playwright，Pixel 手机 UA + 390 宽）：三端都往邮箱框敲 `Test@Example.com` —— **值原样保留不变大写**、computed `text-transform` = `none`、`autocapitalize=none`、`inputmode=email`、密码 `autocorrect=off`；点眼睛断言密码框 `password→text→password` 来回切。**三端全 PASS**。✅
- **闸9 视觉**：只加了一个 emoji 按钮，无新颜色 / 类 / 字号。✅
- **闸10 线上**：待你推送后线上验收。

## 三端最终版本 + md5

| 端 | 版本 | md5 |
|---|---|---|
| 学生端 | **v380** | `8dc87aff4826888c0609279962521041` |
| 中介端 | **v148** | `5cbbc6d9d23a86a7c9c4ca8887a3deee` |
| 学校端 | **v229** | `4126050798a6c4f607f7ef3f4a906164` |

旧版 v379 / v147 / v228 文件都留着可回滚；三端 index.html 各 4 处引用已指向新版。学生端版本说明写进 `docs/changelog/student-portal-versions.md`（不写头注，照 §0 规矩）；中介端同样写 changelog；学校端头注 + changelog 都记了。

## 业主怎么验（照做）

1. 手机上（或电脑 F12 切成 Pixel/390 宽）打开学生端登录页。
2. 邮箱框敲小写 —— **显示小写、不再自动大写**。
3. 密码框敲几个字，点右边的**眼睛** 👁 —— **能看见明文**，再点变回小圆点。
4. **用 stuA 账号真登一次，应该能进去。**
5. 中介端、学校端登录页同样看一眼：邮箱不大写、密码有眼睛。

## 边界

只动了三个登录门，**零迁移，没碰数据库，没碰 .env**。主仓库**未推**。
