# JOB-20-01 · watermark-doc v35：冷启动量清并瘦身（改源码不部署）

> 起点：20-00 探针①实锤「本环境每请求即冷、实例从不复用」（seq 恒 1、boot_age 0–1s、18 次调用无一 warm）。
> 故墙钟大头是**每次冷启动 + 鉴权/权限门/取签名 URL 固定开销**，不是烧水印（render 中位仅 0.26–0.95 s）。
> v35 对症：把 boot 路径能省的都省掉（每一次请求都受益，因为每一次都是冷的）。鉴权/派单/缓存语义一字不动（§0.9⑤）。

## 改了什么（源码 `supabase/functions/watermark-doc/index.ts`，v34→v35）
- **① 计时头补三样**：`x-mh-wm-timing` 与 `[wm v35 timing]` 日志各多打 `seq`（本实例已服务请求数，1=冷启动首请求）、`mem`（`Deno.memoryUsage().rss` 折 MB，图片/PDF 渲染路取**渲染前/后两点** `mem=前/后MB`，非渲染路取当下单点）、`cpu`（EF 无稳定 API → 恒 `n/a`）。ctx 新增 `memBefore/memAfter`（类型 `WmCtx`）。
- **② 瘦身(a) 字体/logo 移出启动路**：v34 在 boot 期 `import` 285 KB base64 字面量（font 218 KB + logo 69 KB）并顶层 `decodeBase64` 两次（≈214 KB Uint8Array 常驻）。v35 改 `getFont()/getLogo()` 惰性化——首次渲染时才**动态 import 该 .ts 并解码**，缓存到模块变量。**boot 期不再解析这 285 KB 字面量、不再解码。**
- **③ 瘦身(b) imagescript 按需**：v34 顶层 `import { Image } from imagescript@1.2.15`（boot 期初始化图片库）。v35 改 `getImage()` 动态 import——**只有渲染分支（buildWatermarkUnit / watermarkImage）加载**；签名 URL / 缓存命中 / 拒绝 / 视频 等冷启动路不再为图片库付费。pdf-lib 早已按需（卡839），保持。
- **④ 瘦身(c) 渲染路径**：v34 本就先按 `maxSide`（现场 2200 / prepare 2200）缩放再画——**尺寸/质量一字不动**以保视觉与 v34 一致；imagescript 无手动释放 API，中间件（img/unit/textImg/logo）均函数局部、返回即出作用域交 GC，**无可再省，如实记**（非虚报「置空缓冲」）。

## 红→绿（瘦身量证，静态可查）
| 指标 | v34 | v35 | 证据 |
|---|---|---|---|
| boot 路径（模块作用域）`decodeBase64()` 次数 | **2**（`const FONT=`/`const LOGO_BYTES=`，旧 43/44 行） | **0**（两处均入 getFont/getLogo，index 65/70 行） | `grep -nF 'decodeBase64('` 仅命中函数体内 |
| boot 期 `import` 顶层数 | **6**（supabase-js / imagescript / base64 / cors / ROBOTO / LOGO） | **3**（supabase-js / base64 / cors） | `grep -nE '^import '` = 3 行 |
| boot 期解析的 base64 字面量 | 285 KB（font+logo .ts 静态 import） | **0**（两 .ts 改动态 import，见 index 65/70） | `grep -nF 'await import('` = imagescript(56)/font(65)/logo(70)/pdf-lib(296) |
| 图片库(imagescript) boot 加载 | 是 | **否**（仅渲染分支） | getImage() 惰性 |
| 峰值 rss（渲染时，20-00②估 60–90 MB / 256 MB 限） | 不变（渲染数学未动） | 不变 | v35 起 mem 头**实测上屏**，部署后由 probe 收 |

## 视觉一致性（≤1% 像素差异 → 本轮为**字节级一致**，由构造保证）
- `font_roboto_b64.ts` md5 `45d6fb70…`、`logo_b64.ts` md5 `93effd8d…`——**git 状态干净、两文件一字未改**；水印字体与 logo 字节完全相同。
- v34→v35 逐行 diff（下）显示：**渲染常量（ANGLE_DEG / *_OPACITY / *_FRAC / MAX_SIDE / MAX_PIXELS）、平铺循环、encode 质量、buildWatermarkUnit/watermarkImage/watermarkPdf 数学全部零改动**；唯一变化是「同一批字节/同一版库**何时**加载」（boot→惰性）。
- 故同输入下输出**逐字节相同**（比 1% 像素阈更强）。⚠ 本机**无 Deno**（EF 依赖 Deno API + 远程 import，无法本地跑 handler），无法本地产出渲染图做像素 diff；**活体一致性由业主部署后审核端「查看」一份图片肉眼确认**（与部署前同一份对比，见「要业主亲眼看的」）。

## N4 分支表（静态源码追踪；无 Deno 无法本地跑 handler，活体由部署后 probe 覆盖）
| 分支 | 触发 | 状态码 | outcome/served | x-mh-wm-timing 段落 |
|---|---|---|---|---|
| OPTIONS | 预检 | 200 "ok"+CORS | —（在计时前 return） | **无头**（提前返回） |
| 非 POST | GET 等 | 405 method_not_allowed | served=denied | total/boot_age/seq/mem/cpu |
| 无鉴权头 | 缺 Authorization | 401（部署后=网关 401「Missing authorization header」；本地=函数 missing_authorization） | denied | 同上（网关路无函数头） |
| 非法 JSON | body 坏 | 400 invalid_json_body | denied | total/boot_age/seq/mem/cpu |
| 令牌失效 | getUser 失败 | 401 invalid_token | denied | +auth 前止 |
| 非四角色 | role 全空 | 403 not_authorized | denied | auth/cache(=role) |
| 未派单 | reviewer 无 assignment | 403 doc_not_assigned_to_you | denied_not_assigned | auth/cache |
| 已撤销 | assignment.revoked_at | 403 assignment_revoked | denied_revoked | auth/cache |
| 已软删 | documents.deleted_at | 410 doc_deleted | denied_doc_deleted | auth/cache/fetch/doc |
| 缓存命中 | mode:signed_url 命中桶 | 200 {url} | served（桶秒下） | signed_url 路（total/boot_age/seq/mem 单点） |
| 需渲染(图) | jpg/png 现场 | 200 wm 图 | served（rendered） | auth/cache/fetch/render/upload+seq+**mem=前/后**+cpu |
| 需渲染(PDF) | pdf 现场 | 200 wm pdf | served（rendered） | 同上+mem=前/后 |
| 学校原件 | school 已解锁 | 200 原件 | served_original（direct） | 无 render 段，mem 单点 |
| 视频签名 | mp4/webm+allow_video | 200 {video_url} | served_video_url（video） | 无 render 段 |
| prepare | mode:prepare（需暗号+已派单） | 200 cached / 401 无暗号 | prepared | DB 触发器路（前端调不了） |

## v34→v35 逐行 diff（要点，全量在 git）
```
- import { Image } from imagescript@1.2.15            → 删（改 getImage 动态 import，仅渲染分支）
- import { ROBOTO_REGULAR_B64 } / { LOGO_PNG_B64 }    → 删（改 getFont/getLogo 内 await import）
- const FONT = decodeBase64(...) / const LOGO_BYTES   → 删（入惰性 getter，boot 期不解码）
+ getImage()/getFont()/getLogo() 惰性 getter + rssMB() + type WmCtx
  buildWatermarkUnit: +const Image/FONT/LOGO_BYTES = await getXxx()（数学不动）
  watermarkImage:     +const Image = await getImage()（数学不动）
  Deno.serve/buildWmTiming: 传 seq+ctx；+seq=/mem=/cpu= 段；[wm v34→v35 timing]
  图片/PDF 渲染分支: +ctx.memBefore/memAfter = rssMB()
```
- **新 md5**：`index.ts` = `3eb5180ef6c0c729e6ff435e85955969`（v34 基线 `adf9c70…`）。**未部署**（源码改，待业主跑 deploy）。

## 保温 / prepare 预热（本轮结论）
- **保温不加**：§0.5 只在「实例因空闲被回收」时才写保温方案；20-00① 实锤本环境**每请求即冷**（不是空闲超时回收）——保温 ping 保住的那个实例，用户下一次请求也不会落上去，救不了。故不写保温定时任务。
- **prepare 预热不成立**：20-00④ 实锤 `prepare` 有 `x-mh-hook-secret` 暗号闸（服务端密钥，给前端=泄密违铁律6）+ 必须已有 `reviewer_assignment`（上传那刻还没派单）。学生/中介/审核端**前端都调不了**。缓存本就在**派单时**由 DB 触发器（带暗号）预热。→ 20-03 的「上传后请求 prepare」架构上不成立，**留第 21 轮**（若要提速冷启动，方向是保温/常驻或换运行时，非前端 prepare）。故 20-02/20-03 **不加** prepare 调用。

## 交付物（均 §0.6 shell 卫生过）
- `jobs/JOB-20-01/deploy-watermark.sh`：部署 v35（JWT 校验开，不带 --no-verify-jwt）+ 三针探针（OPTIONS 2xx+CORS / 无凭据 §0.4 判据=网关 401 缺鉴权头即过 / reviewer1 真登取一份 → 200 且头含 **seq** 与 **mem**）。`bash -n`+`zsh -n` 均过；`$变量`+非 ASCII = 0、正文无 `!`（仅 shebang）、无反引号。
- `jobs/JOB-20-01/wm-timing-probe.mjs`：在 19-03 版上①逐份 30 件多打 seq/mem；②复跑 20-00① 的 12 连打（signed_url 前6@0.3s/后6@8s）+ 3 件渲染各×2 验复用；③写 `~/mh-jobs/JOB-20-01-timing.md` 并推（MH_WALK_NO_PUSH=1 只写不推）。`node --check` 过。**待业主部署后跑。**

## 新闸证据
- **N1**：只升 watermark-doc（云函数，无前端按钮/无 index.html 重定向/无 [VER] Console 探针）；版本印记=头注 `v34→v35` + 日志 `[wm v35 timing]`，三处同版。三端前端一字未改（partner/student/reviewer/admin/school 全 0 改）。
- **N3 带身份**：deploy.sh 与 probe.mjs 均 reviewer1 真登拿 JWT 打 EF/REST；凭据只从 `~/mh-verify/accounts.json` fs 读、token/密码绝不打印。
- **N4**：分支表见上（静态追踪；无 Deno 本地跑不了 handler，活体待部署 probe）。
- **N5 secret-scan**：`jobs/JOB-20-01/` 无硬编码 token/key/密码；命中项为运行时读 `a.password` 与 OAuth `grant_type=password` 字面（同 v34 已过 N5），SECRET-SCAN-CLEAN。
- **N7**：本包无「给人看的提示」UI（纯云函数 + 计时头）；不涉。留 20-02/20-03。

## 要业主亲眼看的（收官汇总用）
- 部署 v35 后审核端控制台应出 `[v45] wm server <id> …seq=…;mem=…/…MB;cpu=n/a`（v45 原样读 x-mh-wm-timing，**自动多出 seq/mem，不需 reviewer v46**）。
- 视觉一致：部署后审核端「查看」任一份图片/PDF，水印文字/位置/清晰度与部署前肉眼一致（字节级一致由 diff+资产 md5 保证）。

## 交接
- **下一包 = 20-02**（中介端 v168：名单块该在时在/该走时走 + 卡片不遮内容 + 英文标题全角冒号 + N7 全套；**不加 prepare 调用**，理由见上）。
- 20-03 前提（学生端「显英文」）20-00⑥ 已证伪；施工时按 P2 停工报业主裁决**术语不一致**（学生「学习证明」vs 管理/审核「在读证明」），不照错前提改。
- 主仓库源码（index.ts + jobs/JOB-20-01/*）已本地 commit、**未 push**（等收官业主卡1153）。
