JOB-2-05  结果：全闸通过（管理端 v185 → v186）

## 做了什么（一句白话）
学校抽屉里补了两段：
1. 「📋 招生任务」：列出这所学校自己建的招生任务（任务号 / 专业 / 招生人数 / 状态 / 创建时间），每行可点、每行有「候选池」按钮，点了弹出该任务的候选学生名单（复用第一轮招生任务页那个弹窗）。
2. 「👤 成员账号」：列出这所学校的成员（角色 role_label + 创建时间），库里 9 条成员。

## 业主要看什么
1. 「学校」→ 点任意学校 → 抽屉往下拉：看到「招生任务」段（库里共 8 个任务，按校归属显示），点任务能看到候选池里的学生。
2. 同一抽屉「成员账号」段：看到该校成员的角色和创建时间。

## 十道闸（逐条）
- 闸1 基底 md5：`a4caef83adc06edaa990a7f479e51f7c`（=v185）✓
- 闸2 锚点唯一：5 处各 =1 ✓
- 闸3 补丁自检：_v186LoadSchoolTasks/_v186LoadSchoolMembers 定义+调用各 =1；两段 _ddSec 定义各 =1；装配 =1；两个 id 各 =2（div+getById）✓
- 闸4 产物 md5：`919af0b05a5619bdde26bb6e49aaa9af`（v186）
- 闸5 语法：1 脚本块 0 报错 ✓
- 闸6 消失检查：762→766（+2 函数 +2 id，无消失）✓
- 闸7 四语：管理端中英；新增文案（招生任务/成员账号/候选池/人）中英齐全，零裸键 ✓
- 闸8 真点击：Playwright 桩数据 —— 招生任务段渲染出 TASK-1 + 候选池按钮(onclick=_v181Candidates) + 人数 5；成员段渲染出 staff/admin 两角色。反向样本 v185：_v186LoadSchoolTasks 未定义 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：新增写死颜色 0（复用 queue-row/_adminPill/badge purple/var(--line-soft)）/ 新增 CSS 类 0 / 新增字号档 0 ✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：a4caef83adc06edaa990a7f479e51f7c → 919af0b05a5619bdde26bb6e49aaa9af
index.html 改了几处：3（v185→v186）
提交哈希：主仓库本地提交，未推送
偏离或疑点：招生任务的「人数」取自 admission_tasks_lite.detail.headcount；若某任务 detail 里没填 headcount 则该行不显人数（不误显 0）。候选池弹窗直接复用第一轮 _v181Candidates，行为一致。
