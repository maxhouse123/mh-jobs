# JOB-15-02 · 审核端 v38 · 放大组件补到每条图片路 + 死件按扩展名短路

生成于 2026-09-19 · reviewer v37→v38 · **学校端 v232 不动**（15-00 i 已证明其图片模态本就有工具条）

## 做了什么
1. **图片显示统一走 `_v38ShowImage(container,url)`**：三级回落（直读命中 / signed_url / 现场渲染）都汇入这一个 helper，内部 `MHViewer.attach` 挂放大缩小工具条。顺手修两处脆弱点：①用 `container.querySelector('#_v37img')` 取图（不再 `document.getElementById`，防同 id 多预览撞）；②`currentLang` 加 `typeof` 守卫（防未初始化时 ReferenceError 吞掉 attach）。
2. **判死改两级**：
   - ① 索引级 `_v38IndexDead`：`wm_status` ∈ {gave_up, unsupported, too_large}，或 `failed` 且 `attempts≥3` → 死。
   - ② 索引无状态时按扩展名 `_v38ExtUnsupported`：`doc/docx/xls/xlsx/ppt/pptx/zip/rar/txt` → 卡显「打不开·文件类型不支持」+「退回·让学生重传」，不预取、点查看 0 请求（`openPreview` 顶部加 `_v38Blocked` 守卫）。
   - `mp4/mov/webm/m4v` → 视频卡「▶ 查看视频」（14-03 播放），不预取。
   - 仅 `jpg/jpeg/png/webp/pdf` 进预取（`_v38Viewable`，**补回了漏掉的 webp**）与查看。
3. **预取集合** `_wmEnqueuePrefetch` 改用 `_v38Viewable`；hover `_wmPrefetch` 对非可视件直接跳过。
4. 版本四件套：文件名 v38 / 头注 / [VER] v38（旧 v37 [VER] 行已删，只留一条）/ `reviewer-portal/index.html` 三处重定向 → v38；`docs/changelog/reviewer-portal-versions.md` 加一行。

## 结果是什么（自验 GATE3-PASS，命令 `node ~/mh-verify/_run-1502.mjs`）
- `_v38ShowImage` 三条路各一次 → **每次 `.mhv-bar` 四钮**；点＋后 `img.style.transform` **含 `scale(`**；双击 transform 切换。全 true。
- 判死/类型逻辑 14 项断言全 true（docx blocked+「文件类型不支持（Word/Excel 等）」；mp4 视频态；jpg/webp/pdf 可视；gave_up/unsupported/too_large 死；failed<3 不死、failed≥3 死）。
- **预取 [docx,mp4,jpg,jpeg] → 只发 2 请求**（j1,j2）。
- 卡渲染：docx 卡含「打不开」「文件类型不支持」「退回」且无查看钮；mp4 卡含「▶ 查看视频」。
- **点 docx 的查看 → `_wmFetch` 0 次调用**。
- 学校端 v232 图片模态落点 `MHViewer.attach` → **四钮**（未改，仅核）。
- reviewer 与 school 均 **0 真实报错**（benign 过滤 supabase/i18n/tesseract 等）。
- 报告全文：`docs/self-test-report-15-02.md`。
- **v38 md5 = `e3a07663fe3cfdeb09cf37ae0c406617`**（`md5 -q`）。旧 v37 = `c32bbcb9…`。

## 业主要做什么
1. 推主仓库后（原生终端挂代理），到**审核端**打开一张图片材料（成绩单/护照）→ 右上角应有 ＋－1:1⤢ 工具条，滚轮/双指放大看清数字（验收表 2）。
2. 遇到 Word/Excel/压缩包等材料 → 卡上直接显示「打不开·文件类型不支持」+「退回·让学生重传」，点不转圈（验收表 3）。
3. 本包**不推主仓库、不部署**。学校端有意不动（其图片模态本就有工具条，见 15-00 i）。

## 边界守住
未动 EF；未动管理端；未改三级回落顺序；未动学校端。
