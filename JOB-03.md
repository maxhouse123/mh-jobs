# JOB-03 · 看原件提速

**结果：全闸通过（第三项按卡授权跳过，见说明）**

## 做了什么（一句白话）
1. **抽屉材料列表更快出**：以前打开学生抽屉，后台要**排队跑三次查询**（先查材料清单，再查审核记录，再查学校意见）。现在后两次改成**同时跑**（材料清单必须先跑，因为要先拿到材料编号，这是硬依赖）。模拟测试：原来约 304 毫秒，现在约 210 毫秒。
2. **断网不再一直转圈**：点「查看原件」时，如果 15 秒还没取回来（比如断网），会**明确报错**「取件超时（15 秒），请重试」，不再无限转圈。
3. **缩略图没做**（按卡里的授权跳过）——原因见下方偏离说明。

## 业主要看什么（打开哪里、点哪个、应该看到什么）
1. 管理端 →「学生」→ 点学生 → 抽屉「📎 材料（云端）」列表出得比以前快一点（尤其材料多、网络慢时更明显）。
2. 打开某份材料「查看原件」时**故意断网**：十几秒内会弹出明确的「取件超时」提示，而不是一直转圈。
3. 「取件用时 X.X 秒」的显示、以及同一份材料第二次点开秒开（会话缓存）——都保持原样没动。

## 十道闸（逐条 + 实测数字）
| 闸 | 结果 | 实测 |
|---|---|---|
| 1 基底md5 | PASS | `c715957f23140cff153553ca60fbfeb4`（=JOB-02 产物）|
| 2 锚点唯一 | PASS | 2 处锚点各出现 1 次 |
| 3 补丁自检 | PASS | `Promise.all` 并行=1、旧顺序 r1 await 归 0；15000 超时=1、AbortController signal=1、超时文案=1。反向样本 v177 跑 2/2 失败 |
| 4 产物md5 | 记录 | `20169599c46287c9416c10c2aa26dbc2` |
| 5 语法 | PASS | 脚本块 0 报错 |
| 6 功能清单 | PASS | 函数/id/window/onclick **全等**（只改函数体，无新增无消失）|
| 7 四语 | PASS | 管理端中/英双语；新增的超时提示中英齐全、无裸键 |
| 8 真点击 | PASS | 真浏览器（模拟 100ms/查询）：**并行 210ms vs 顺序 304ms**；断网（fetch 抛 AbortError）→ 弹「取件超时（15 秒），请重试」 |
| 9 视觉一致 | PASS | 零新颜色/字号/CSS 类（纯逻辑改动）|
| 10 线上版本 | 跳过 | 本轮不推送 |

## 基底 / 产物 md5
- 基底：`c715957f23140cff153553ca60fbfeb4`
- 产物：`20169599c46287c9416c10c2aa26dbc2`

## 超时阈值
**15 秒（15000 毫秒）** —— 超过就报错。可按需要调整（告诉我改成几秒即可）。

## index.html 改了几处
3 处，v177→v178，无残留。

## 提交哈希
主仓库 `10fbd9e`（已 commit，未推送）。

## 偏离或疑点（重要）
1. **第三项「缩略图」没做，是按卡里明写的授权跳过的**。原因：卡里硬约束是「学生材料绝不生成直链」，缩略图要么走直链（被禁），要么跟看原件一样一张张走服务端函数——那意味着**每张缩略图都要多一次服务端调用+多写一条审计**，既违反「不牺牲审计」，又和「提速」的目标完全相反（本来就是要少跑请求）。卡里原话：「如果做不到不牺牲审计，就只做前两项，第三项停下来写回执说明」。所以我做了前两项、跳过第三项。如果你愿意接受「缩略图=每张多一次审计」的代价，我可以后续单独做。
2. **提速的毫秒数是模拟测的**：本地连不了真库（也不该连），所以用「每次查询固定延迟 100ms」的桩来对比并行 vs 顺序，证明确实少跑了一轮往返。真实网络下的实际快多少，业主线上打开抽屉能直接感受到（网络越慢、材料越多，省得越明显）。
3. **超时只加在「查看原件」（adminViewOriginal）**这条路上（卡里指定的）。打包下载（JOB-02）那条慢路暂时没加同样的超时——如果你要，我后续给它也加一个。
