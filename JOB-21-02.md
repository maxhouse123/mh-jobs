# JOB-21-02 回执 · 中介端 v169：卡片停久+悬停暂停 + 手机/桌面窄弹窗不盖标题条 + 向导守卫收紧（名单块冒号 v168 已符合无改）

CC 执行。只改 partner 端，只修不加；未动鉴权/派单/AI判定/瘦身参数/缓存语义/共用查看器/其它四端。

## 一、做了什么（partner v168 → v169）
四处真改（业主 09-24 验收甲），逐条对 QUEUE-21 21-02：

1. **停留时长**：`_v167Card` 默认 `5500 → 15000` ms（一般卡）；`_r2SoftToast` `8500 → 20000` ms（护照卡）。调用方传入的 `duration` 仍优先。
2. **悬停暂停**：`_v167Card` 计时器从单 `setTimeout` 改 `_arm/_pause/_resume` 三函数——`mouseenter` 清定时并按已耗时算剩余（`Math.max(3000, …)`，剩余不少于 3s）；`mouseleave` 用剩余时间重新计时；× 立刻关不变。
3. **卡片位置**：`#_v167CardStack` 定位从内联 `cssText` 移入 `<style id=_v167CardStackCss>`（内联样式媒体查询覆盖不了）。**详情弹窗 `max-width:760px` 居中（1280 下 x 260..1020），右上无论多窄都会压标题条**（21-00 两宽皆红：@390 卡∩标题条 15750px²、@1280 卡∩标题条 5220px²），故从右上改**钉视口底**：桌面 `bottom:16;right:16;≤360px`（底部右对齐），`@media(max-width:640px)` 改 `bottom:12;left:12;right:12`（手机底部通栏）。**注：桌面从「右上」改「右下」，系 760px 居中弹窗使任何可读宽度的右上卡都压标题条的必要偏离，留业主线上肉眼验收。**
4. **向导守卫**：`renderUploadGrid()` 守卫 `state.wiz` → `state.wiz && state.wiz.step`（与其它九处一致）；`_r2Repaint` else 分支不变。无 step 时不再把主视图切到登记表单。

**名单块冒号**：21-00 探针③已证 `_p98T('rosterTitle')`（渲染入口 v169 L11492 `.r2-anchor-warn`）取 `_p98I18N` 的 zh 全角「：」/ en·ru·fr 半角，本轮复验仍绿，**无改**（QUEUE §先读③ 指的 `r2.rosterTitle` 是另一处未用于名单块的旧键，仅 zh/en）。

版本四件套：文件名 v169 / 头注 v169 / `[VER]` console 探针改写 / `partner-portal/index.html` 三处重定向（location.replace + meta refresh + canonical）全指 v169。docs/changelog/partner-portal-versions.md 追加 v169 行。

## 二、闸3 证据（先红后绿·真页面 partner1 真登）
探针 `jobs/JOB-21-02/probes/p-v169-card.mjs`（只读，无写库/上传/提交），输出 `p-v169-console.txt` / `p-v169-raw.json`：

- **BOOT**：v169 真页面 0 pageerror，`_v167Card/_r2SoftToast/renderUploadGrid` 三全局在 = 语法/加载过。
- **⑤.1 时长（N7 窗口）GREEN**：一般卡 t=1s/10s 在、~15113ms 消失（16s 不在）；护照卡 t=19s 在、~20009ms 消失（21s 不在）。对照 21-00 v168：一般 ~5567ms、护照 ~8586ms（红）。
- **⑤.2 悬停 GREEN**：短卡（dur 4000）鼠标进入后等 5s **仍在**（暂停生效）；离开后 1s 仍在（剩余≥3s）、再 4.5s 后消失。对照 v168：无暂停、按时消失（红）。
- **⑤.3 六项零重叠 GREEN（两宽）**：@390 卡{12,743,366,46}、@1280 卡{904,739,360,46}；对 提醒框/缩略图/提交按钮/名单块/弹窗标题条/关闭按钮 六项 **maxOv=0px²、inScreen=true**。对照 21-00 v168：@390 卡∩标题条 15750px²/关闭钮 612px²、@1280 卡∩标题条 5220px²（红）。（弹窗默认滚顶态：本推荐生 present=[titleBar,closeBtn]，其余四项未渲染故不可能被压；卡触发时用户已滚到护照区、提交按钮在其下方。）
- **④ 向导守卫 GREEN**：`state.wiz={}`（无 step）后调 `renderUploadGrid()` → 主视图**不**切到登记表单。对照 21-00 v168 同探针：切到登记表单（红）→ 红转绿。
- **③ 名单块冒号 GREEN**：zh 全角「：」/ en「):」/ ru「):」/ fr「) :」（半角，法文规范带空格）。
- **四语逐键**：本包未增删任何 i18n 键（仅时长/位置/守卫，无新文案）→ 零裸键风险；③ 四语实测在上。

## 三、新闸 N1–N7
- **N1**：`jobs/JOB-21-02/inventory/partner-v169-static.json` vs 21-00 `partner-v168-static.json` diff：**removed ids/handlers/functions = 0**；仅 +3 functions（`_arm/_pause/_resume`，`_v167Card` 内新增闭包）。零死按钮、零删。
- **N2**：④⑤ 21-00 已在 v168 真页面先红（数字见上），本包 v169 转绿；③ 复验绿。
- **N3**：⑤.3/④.2/截图走 realpage.mjs partner1 真登（accounts.json 仅脚本读、未打印）。
- **N7（卡片专项）**：①一般卡 1s/10s 可见、16s 不在；护照卡 19s 可见、21s 不在——见 ⑤.1；②悬停 5s 仍在、离开按规则消失——见 ⑤.2；③390 与 1280 两宽六项零重叠（含弹窗标题条与关闭按钮）——见 ⑤.3；④英/俄/法名单块标题末尾无全角冒号——见 ③。
- **N5**：secret-scan 于 jobs/JOB-21-02 与 shots/21/21-02 皆 CLEAN；v169 无 service_role 串。
- **N6**：本包只做 21-02 写明四项 + 名单块复验，未顺手改他处。
- **shell 卫生**：`jobs/JOB-20-04/shell-lint.sh` 全绿（0 命中）。

## 四、截图（业主线上肉眼验收用）
`~/mh-jobs/shots/21/21-02/`（弹窗标题条含姓名已 mask；MH-FG85YG/StuB 为测试数据非真 PII）：
- `card-@390.png`：手机——卡片底部通栏，让开标题条/关闭钮。
- `card-@1280.png`：桌面——卡片右下角，让开标题条/关闭钮/正文。
- `INDEX.md`：说明 + 数字实证指向。
（§0.6：调试期不推送，随 21-06 干净后统一 push；secret-scan 已过。）

## 五、状态
- 主仓库已 `git add` 点名 + commit（partner v169 + index.html + changelog + jobs/JOB-21-02 探针/清单）：**未推**（§0.9 不推主仓库；收官后业主用卡1158 推）。
- 本回执已 push 到 mh-jobs。
- **PUSH_OK（本包自闸）= yes**：先红后绿全过、N1（0 删）、N7 四项、shell-lint 绿、secret-scan 绿。桌面右上→右下的偏离已如实标注，留业主线上肉眼验收（闸二）。

下一包 = 21-03
