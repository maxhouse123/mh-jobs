# JOB-23-02 回执 · 管理端 v221：录取通知分发页去假数据 + 截止日期监控并进来 + 侧栏去掉「截止日期监控」

做这一步的人：CC（无人值守）。这一包只动管理端一端，版本从 v220 升到 v221（旧版 v220 原样留着，随时能回滚）。

## 一、做了什么（大白话）

1. **「录取通知分发」页不再混假学生行**。
   - 原来这页把 6 条真实的云端录取记录，后面硬接了一串演示用的假学生，中间还夹一条「以下为演示数据」的黄条分隔线，看着像真的一样，容易误判。
   - 现在只留真云端那几条，假行和黄分隔线都不显示了（生成假行的老代码没删，只是不再往页面上接——这是本轮定下的「销债不销码」做法，保留将来可查）。

2. **页顶多了两枚小圆按钮（筹码）：「分发进度 · 6」和「即将到期 · 2」**。
   - 默认停在「分发进度」，看到的就是全部录取通知的进度。
   - 点「即将到期」，页面切成到期提醒视图：三天内要到期、还没付中介费的那几条会单列出来（本次桩数据里是 2 条），上面有「紧急：三天内到期」的分段标题。
   - 这两枚按钮复用页面本来就有的筹码样式，没造任何新样式、新颜色。

3. **左侧栏去掉了单独的「截止日期监控」入口**。
   - 它的内容已经并进上面那枚「即将到期」筹码里了，不必在侧栏再占一格。
   - 侧栏那格连同它的小角标一起删掉；负责刷角标的代码本来就对「找不到这个角标」有容错，不会因此报错。老的「截止日期监控」页面容器和渲染代码都留着，万一以后要用还能调出来。

## 二、动的文件

- `admin-portal/maxhouse-admin-portal-v221.html`（新版，从 v220 复制后改）
- `admin-portal/index.html`（三处跳转指针 v220 → v221）
- `docs/changelog/admin-portal-versions.md`（追加 v221 一行）
- `tests/job23-02-admin-v221.mjs`（本卡随卡静态断言脚本，新）
- `tests/job23-01-admin-v220.mjs`（上一卡的脚本里「index 指向 v220」这条，改成前向稳定的「index 已离开 v219、三处指向某个 v2xx」——因为 index 是单一「最新指针」，每 bump 一版它就前移，否则上一卡脚本会永远变红。详见第五节）
- `jobs/JOB-23-02/repro/green.mjs`（无头真渲染验证脚本，新）
- `jobs/JOB-23-02/repro/shots.mjs`（截图脚本，新）

主仓库已提交一笔（commit `3910402`），**未推**（按规矩推送留业主收官时做）。

## 三、旧病由红转绿（对应 23-00 的 R3）

23-00 已把这处旧病量成红：录取通知分发页正文 = 真 6 条 + 假种子行、且有演示分隔线；截止日期页 = 真 2 条 + 假种子行、且有「混合视图」横幅。

本卡后，无头真渲染（合成桩：6 条 offer，其中 2 条三天内到期未付；种子数组仍非空）实测：

| 断言 | 期望 | 实测 |
|---|---|---|
| 分发进度正文行数 | 6（只真云端行） | 6 ✅ |
| 演示分隔线 | 无 | 无 ✅ |
| 两枚筹码文案 | 分发进度 · 6 / 即将到期 · 2 | 一致 ✅ |
| 点「即将到期」后正文行数 | 2 | 2 ✅ |
| 「三天内到期」分段 | 出现 | 出现 ✅ |
| 侧栏还有没有「截止日期监控」 | 没有 | 没有 ✅ |
| `switchView('deadlines')` 老入口 | 仍能渲染、零报错 | 2 行、零报错 ✅ |
| 中英切换裸键 | 0 | 0 ✅ |
| 控制台报错 | 0 | 0 ✅ |

无头结果 5/5 全绿（`jobs/JOB-23-02/repro/green.log`）。

## 四、各道闸

- **闸3 静态断言**：`tests/job23-02-admin-v221.mjs` → **35/35 全绿**。含：版本四件套、renderLetters 去种子行/去分隔线、两枚筹码与状态机、_v221DeadlinesBody 去种子行/去横幅、renderDeadlines 薄封装、侧栏 nav-item 由 28 减到 **27**、#view-deadlines 容器与 VIEW_RENDERERS 注册保留、_adminDemoDivider/_adminDemoBanner 定义仍在。
- **闸8 无头真渲染**：见第三节，中英各一遍、零报错、零裸键。
- **闸9 视觉零新增**：只复用 `.filter-chip` `.filter-bar` `.queue-row` `.wf-hero` `.badge`，无新 CSS 类、无写死新颜色、无新字号。
- **N1（授权移除清单）**：本卡差集**只含 §0.3 第 4 项**——侧栏 `data-view="deadlines"` 的 nav-item 整块删除（消失 1 个 inline onclick + 1 个 id `#navBadgeDeadlines`）。函数、数组、视图容器、VIEW_RENDERERS 注册**一律保留**，functions 零消失。
- **上一卡回归**：`tests/job23-01-admin-v220.mjs` 重跑 **28/28 全绿**（index 断言前向稳定化后）。
- **shell-lint**：本包无新增 shell 脚本，无需 lint。
- **secret-scan**：改动文件全扫，无任何真实密钥（唯一命中是某条中文变更说明里出现「service_role」词，是描述迁移权限的说明文字，非密钥）。**clean**。

## 五、一处图纸与现实的措辞对不上（不属「必须停」，照施工指令执行 + 记明）

QUEUE-23 §23-02 的闸3判据①写「`_adminDemoDivider()` 调用 = 0（定义仍在 1 处）」。但「改哪里」第 1 条明确只让在 **renderLetters** 里去掉这个调用。CC 亲查 v221 源码后发现：**付款流水页（renderPayments，约 11726 行）也调用了 `_adminDemoDivider()`**，那是本包边界外的另一个页面（本卡边界写明「不动其它页逻辑」）。

- 判据①按全文字面读是「全文 0 次调用」，但施工指令（改哪里）只覆盖 renderLetters，二者措辞对不上。
- CC 的处理：**严格照施工指令执行**——只去掉 renderLetters 里的调用；付款页那处不碰（在本卡边界外）。随卡静态断言因此**按施工范围量**（renderLetters 函数体内 `_adminDemoDivider()` = 0、`.concat(LETTERS)` = 0；_v221DeadlinesBody 函数体内 `.concat(DEADLINES)` = 0、`_adminDemoBanner('mixed')` = 0），而不是按全文 0 次。
- 这不是「图纸与现实冲突需停工」的情形（施工指令本身清楚可执行、且已执行），只是闸3措辞把 renderPayments 那处漏算了。**建议**：付款流水页的演示分隔线是否也要去，留业主一句话决定，可并入第二十四轮。

## 六、要业主亲眼看的（线上验收，留业主）

1. 进「录取通知分发」，页顶应有两枚筹码，正文没有黄色「演示数据」分隔线、没有假学生行。
2. 点「即将到期」筹码，应看到「紧急：三天内到期」分段和到期的那几条。
3. 左侧栏应**没有**「截止日期监控」这一项了。

（本地无头 stub 只证结构和行数，线上长啥样只有肉眼能判——留业主线上看。）

## 七、截图

`~/mh-jobs/shots/23/23-02/`（元素级、学生名与校名已模糊，合成桩数据无真实 PII）：
- `letters-progress.png` —— 分发进度标签，两枚筹码 + 6 条真行、无分隔线。
- `letters-deadlines-tab.png` —— 即将到期标签，「三天内到期」分段 + 2 条。
- `INDEX.md` —— 索引。

## 八、STOP？

无。本轮暂无 `~/mh-verify/r23-STOP.txt`。

---
下一包 = 23-03
