# JOB-15-06 · 存量收口 · 259f456a + 悬空行软删 + 重灌关灯

生成于 2026-09-19 · 三项都走 db-run（新增/软删/配置开关，无破坏性操作）· 未推主仓库

## 做了什么 / 结果是什么（数字带出处）

### a. 259f456a —— 不改库，出设计报告（图纸-现实冲突，停手）
- **实证**：259f456a 是 670 KB jpg 银行流水，属**测试学生 A**（`43668ae4`，is_test）。`wm_cache_index` 无行、wm-cache 桶无对象；**唯一派单已 revoke**（reviewer `80c62372`，`revoked_at=2026-09-17 05:10`，`revoke_reason='JOB-11-03 test cleanup'`）。
- **根因**：`wm_cache_backfill` 的缺件候选硬条件是 `ra.revoked_at is null`——它的派单已撤销，故被**正确排除**，这就是「从未进缓存」的原因。
- **为什么卡 15-06 a① 两个处置都不合适**：①补 pending 行无效（backfill 由 `reviewer_assignments` 驱动，不读游离 pending 行，只会留一条永不推进的假在途行）；②改候选条件纳入它 = 放宽 `revoked_at is null` = 把**所有撤销/测试清理的派单**重新拉回缓存，有害。两条都在给一个无活跃审核员的测试件强行缓存。
- **处置**：按铁律14 P2「图纸与现实冲突→停手报告不自修」，**本轮不改库**，出设计 `jobs/JOB-15-06/design-259f456a.md`。结论：它是测试残留、非生产隐患；候选条件是对的；业主若要复测，把它**重新派单**即可，下一跳自动缓存。

### b. 悬空行软删 —— 4 行（`jobs/JOB-15-06/cleanup.sql`，DB-RUN OK）
- 15-00 g 复核：学生 A 的两张 `mh10-test` 图**已于 09-17 软删**（无需再动）；现存 **4 条「活行但 storage 对象缺失」**，全属**测试学生 836845a0（is_test）**：`20260515105839.pdf`(education) / `高中学历.jpg`(transcript) / `无犯罪.jpg`(passport) / `签名.jpg`(photo)——signedUrl 必失败。
- 软删：`update documents set deleted_at=now(), delete_reason='test residue: storage object missing (JOB-15-06)'`，**只这 4 个 doc_id、只软删**（不删任何对象/行）。
- 验证：`UPDATE 4`，4 行 `deleted_at` 均非空；**`documents` 总行数 = 230 不变**（软删不减行）。**未碰其它学生的行。**

### c. 重灌关灯 —— `wm_render_v` 1（`jobs/JOB-14-04/rerender-off.sql`，DB-RUN OK）
- 15-00 b 实证：`cached` 且 `render_v=1` 的**图片件 = 0**（图片件重灌已 100%）→ 满足 15-06 c 关灯条件。
- 执行：`platform_flags.wm_render_v` 由 2 → **1**（`UPDATE 1`）。
- 安全性已核（读 `wm_cache_backfill` 源码）：该 flag **只**门控「④重灌通道」（`v_render_v>=2` 才把 cached render_v<2 的图重渲染）；关掉后 **缺件缓存照常**，且 EF v32 新水印件仍固定 2200/82（EF 不读此 flag）。可逆（rerender-flag.sql 可再开）。

## 业主要做什么
1. 无需任何操作；三项均已在库内落地。
2. 知会：①259f456a 是测试残留、非隐患，要复测就重新派单；②4 条测试悬空行已软删（signedUrl 失败面消除）；③水印重灌已 100% 完成、重灌通道已关（回到只补缺件）。
3. 本包无前端/EF 改动，无版本 bump。

## 边界守住
不删 Storage 对象；不清桶；不批量重渲染；不 un-revoke 撤销；软删只碰确认悬空的 4 行、总行数不变；关灯仅在图片件重灌 100% 后按条件执行、可逆。
