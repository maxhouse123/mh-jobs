# JOB-13-01 · wm_cache_index 授权 + 存量回填（迁移 0200）

## 做了什么
- 写并应用迁移 `0200_wm_cache_index_grants.sql`（走 `bash jobs/db-run.sh`）：
  ① 给 service_role 授 select/insert/update/delete（EF 预渲染写索引、撤单删索引都要）；重申 revoke anon/authenticated。
  ② 加管理员只读策略 `admin_read_wm_cache_index`（照 0176 的 is_admin 先例）。
  ③ 把 wm-cache 桶里现有 95 个缓存对象反推成 95 行索引灌进去（rv_code 用与 log_wm_view 兜底一致的 SHA-256 算法，rendered_at = 对象创建时间）。
- 写了单件功能证明脚本 `jobs/JOB-13-01/cleanup-one-cache-object.sql`（**本轮未执行**，原因见下）。

## 结果是什么（命令：bash jobs/db-run.sh jobs/JOB-13-01/0200_…sql 的末尾验证）
- service_role 权限 = `["DELETE","INSERT","SELECT","UPDATE"]` ✅
- anon/authenticated 授权行数 = **0** ✅
- 索引行数 = **95** = wm-cache 对象数 **95** ✅（原来是 0，现在补齐）
- 管理员只读策略存在 = true ✅
- 回填日志：`NOTICE: 0200 存量回填将插入 95 行` + `INSERT 0 95`（命令出处：db-run 输出）。
- 这样以后 EF 每次预渲染成功就能真的把索引行写进去了（权限通了），审计取 rv_code 不必再靠兜底算。

## ⚠️ 单件重灌证明：本轮做不了（不是失败，是被 402 挡）
- 13-00 查出项目自 09-17 22:50 UTC 起被 Supabase 限流（402 exceed_cached_egress_quota），云函数被掐。
- 若现在删掉那 1 件缓存副本（选中最小的 jpg：reviewer1 名下 `f7e31919….jpg`，28286 字节），watermark-doc 会被 402 挡、灌不回来，等于白白弄丢一件、系统更糟 → **我不删**。
- 脚本已写好留在 `jobs/JOB-13-01/cleanup-one-cache-object.sql`，业主恢复服务后可手跑（删 → `select wm_cache_backfill(20)` → 30s 轮询看对象回来 + 索引 rendered_at 刷新）。

## 业主要做什么
1. 先恢复 Supabase 服务（升级 / 去 spend cap，见 JOB-13-00 头号发现）。
2. 恢复后想验单件重灌，去原生终端（挂代理）跑：
   `bash jobs/db-run.sh jobs/JOB-13-01/cleanup-one-cache-object.sql`，
   再 `bash jobs/db-run.sh` 跑一句 `select public.wm_cache_backfill(20);`（可临时放一个 begin/commit 文件），30 秒后看那件 jpg 是否回到桶里。
   —— 不做也不影响：索引已全量补齐，授权已修好。
