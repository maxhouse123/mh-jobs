# JOB-15-04 · 学生 v394 / 中介 v160 · 瘦身四态常驻行 + 中介端「我的委托邀请」常驻区块

生成于 2026-09-19 · student v393→v394 / partner v159→v160（未推未部署）

## 做了什么
### 瘦身结果四态常驻行（两端）
- 之前只有「瘦身成功」记一行；保留原件（未达 70% / 超 15 页 / 解析失败）时静默，业主以为没跑。
- 现在瘦身器 `mhPdfSlim` 的 `onSkip(reason)` 也记一态：`_v394ClassifySkip`（student）/`_v160ClassifySkip`（partner）按 reason 归类——`not-70%(…)`→`kept_not70`、`>15pages(N)`→`kept_pages`、其它→`kept_error`，并从 reason 解析出 B(KB)/N(页)。
- 渲染四语一行（≤600KB 不记不显示）：
  - `slimmed`「✓ 已瘦身 A→B · N 页」（沿用）
  - `kept_not70`「✓ 已检查 · 保留原件（瘦身后 B KB 未小于原件的 70%）」
  - `kept_pages`「✓ 已检查 · 保留原件（N 页，超过 15 页不处理）」
  - `kept_error`「✓ 已检查 · 保留原件（无法解析，按原件上传）」
- 学生走 `t()` 词典（zh/ru/fr/en 各补 3 键）；中介走内联四语字典 `_V160_SLIM_I18N`（无 t() 缺键风险）。按结果文件名记（瘦身→新名 / 保留→原名）。**未动瘦身参数。**

### 中介端「我的委托邀请」常驻区块（partner v160）
- 病根（15-00 h）：邀请列表 `dlInviteList` 原来只在委托开关开且锚点按钮在时由 `_dlDecorateDom` 动态插入，0 条时还直接空白 → 业主「找不到列表、重发无处点」。
- 修法（不造第二套，复用 `_dlLoadInvites` / `_dlRenderList`）：
  - 容器 `<div id="dlInviteList">` **写进「我推荐的学生」区块 markup**（紧接添加学生按钮之后），**常驻**；`_dlDecorateDom` 不再动态建它（邀请按钮仍随开关）。
  - `_dlLoadInvites` 改为**无论开关都载**（0 条也显示区块）。
  - **0 条态**：标题常驻 +「还没有邀请 · 点「邀请学生自传」创建」。
  - 每卡：显示名 / 邮箱或「无邮箱 · 仅链接」/ 状态药丸 / 发出时间 / **最近重发时间** / 按钮：**复制链接**、**重发**（有邮箱且距上次 ≥10min 才亮；灰时显示「N 分钟后可重发」，`_dlStartCountdown` 每 30s 刷新；无邮箱显示「无邮箱·仅链接」；≥5 次封顶灰）、**撤销**（仅待接受）。
  - 分页沿用 7-08（`_v209Pager`）；语言切换重画；造新邀请后 `_dlCreate` 已重画。**未动邀请 RPC / 邮件 / AI 预检。**
- 四件套两端：文件名 / [VER]（student & partner 各无残留旧探针）/ index（学生 4 处、中介 4 处）/ changelog 各一行。

## 结果是什么（自验 GATE3-PASS，`node ~/mh-verify/_run-1504.mjs`，390px，两端 0 真实报错）
- 学生：4 个 i18n 键 × 4 语齐全非空；`_v394ClassifySkip` 三态 + B=700/N=20 解析正确；四态渲染无裸 `sup.pdf` 键。
- 中介：`_v160ClassifySkip` 三态正确；四态行文案正确；邀请区块 0 条态（标题 +「还没有邀请…」）；2 条（3 分钟前→「N 分钟后可重发」灰倒计时；12 分钟前→可点重发）渲染 2 行 + 复制 + 撤销；**390px 无横向溢出**（scrollW=clientW=390）。
- 报告：`docs/self-test-report-15-04.md`。
- **student v394 md5 = `0b2d2a22b616f96ad3db402852826e9d`**（旧 v393 `d3f26618…`）。
- **partner v160 md5 = `5d1a64a45353d2daeff9418299a594a7`**（旧 v159 `1fd5d1ae…`）。

## 业主要做什么
1. 推主仓库后到**学生/中介端**传一个多页 >600KB 的 PDF、一个单页扫描 PDF、一个 >15 页 PDF、一个坏 PDF → 文件卡下应各常驻一行「已瘦身 / 已检查·保留原件（未小于 70% / 超过 15 页 / 无法解析）」（验收表 5、6）。
2. 到**中介端**首页「我推荐的学生」下方：应见「我的委托邀请」区块常驻，0 条时有提示文案；已发邀请每条能复制链接、10 分钟内显示倒计时、10 分钟后能重发、待接受能撤销。
3. 本包**不推主仓库、不部署**。

## 边界守住
未动瘦身参数；未动邀请 RPC / 邮件；未动 AI 预检调用。
