# MAXHOUSE 三端对齐工程 · 总结（2026-09-15）

一轮跑完整个队列。**管理端从 v172 推进到 v182（本轮做了 v173→v182 十个版本）**，全部只在本地 commit、**未推主仓库**（等你看过界面自己推）。回执 + 迁移 SQL 已全推到日志仓库 `~/mh-jobs`。

---

## 1. 十五包各自结果

| 包 | 版本 | 结果 | 一句话 |
|---|---|---|---|
| JOB-01 | v172 | ✅ 全过（**你已推送**） | 中介「已录取」判据换真派生 |
| JOB-05 | v173 | ✅ 全过 | 材料退回理由三种上屏 |
| JOB-04 | v174 | ✅ 全过 | 抽屉档案 7→全字段(分四组) + 材料时间到分 |
| JOB-07 | v175 | ✅ 全过 | 三端搜索补齐(护照/电话/编号/邮箱) |
| JOB-06 | v176 | ✅ 全过 | 抽屉付款订单区 + 付款页按学生筛 |
| JOB-02 | v177 | ✅ 全过 | 六类文件真下载 + 打包下载(自持ZIP+进度+取消) |
| JOB-03 | v178 | ✅ 全过 | 看原件提速(查询并行+15s超时) |
| JOB-08 | v179 | ✅ 4/7 项过，2 项延后，1 项早已做 | 清假数据(增值服务/付费筛选/CSCA/本月收入) |
| JOB-09 | v180 | ✅ 前端 3/5 项过 + 迁移待跑 | 申请轮次/搜索/已入学阶段 |
| JOB-10 | v181 | ✅ 核心 2 项过 + 迁移待跑 | 招生任务只读页 + 候选池 |
| JOB-12 | v182 | ✅ 1/10 项过 + 迁移待跑 | 中介推荐学生补齐(其余9项列backlog) |
| JOB-0.5 | — | ⏸ 必须停·SQL已出未执行 | 造5种测试数据的 seed/cleanup |
| JOB-11 | — | ⏸ 必须停·设计完成 | 管理端补管理能力设计 |
| JOB-13 | — | ⏸ 必须停·设计完成 | 学校层级链设计 |
| JOB-14 | — | ⏸ 必须停·设计完成 | 学校端留痕改服务端设计 |

**每包都过了它该过的十道闸**（基底/产物 md5 链式相扣、锚点唯一、补丁自检+反向样本、语法、功能清单零消失、四语(管理端=中英)、Playwright 真渲染、视觉零新增写死色/字号/类）。10 个代码包的验证脚本在 `~/mh-verify/gate8_jobXX.mjs`。

## 2. 哪些包因缺测试数据/权限没验全（要后续补验）
- **JOB-05**：二审驳回理由、学生更换原因——库里 0 条，等 JOB-0.5 造数据后补验。
- **JOB-06**：待核销/已驳回订单——库里 0 条（核销/驳回按钮验不了），等 JOB-0.5。
- **JOB-08**：真实增值服务/付费——本地用受控数据证明逻辑，真数据要线上看。
- **JOB-09 / JOB-10 / JOB-12**：三条**迁移我跑不了**（`.env` 在我禁读清单、无 DB 连接），迁移 SQL 已写好在 `jobs/JOB-0X/migrate.sql`（+ rollback）。**你在 SQL Editor 跑完这三条，对应功能才出真数据**：
  - JOB-09 `application_rounds` 只读策略 → 抽屉「申请轮次」
  - JOB-10 `task_candidates` 只读策略 → 招生任务「候选池」
  - JOB-12 `referral_drafts` 只读策略 → （backlog item5 用）
  - 每条跑前后都数一次策略数，**差应为 1**。

## 3. 四个「必须停」包要你回答的问题
- **JOB-0.5**：SQL 已写好未执行。点头后跑 `seed.sql`，验完跑 `cleanup.sql`（按【测试】标记精确回收）。若列名与你库不符会报错，把报错发我。
- **JOB-11**（管理能力）：①删材料软删还是硬删？②撤销派单回可派池还是终态？③停用学校后已发 offer 冻结还是照常？
- **JOB-13**（学校层级）：①层级统一用哪套词（+旧 A/B/C 各对应哪档）？②每档给多少名额？
- **JOB-14**（学校留痕）：这条合规记录要不要对学校可见？

## 4. 五端最终版本号 + md5
| 端 | 版本 | 说明 |
|---|---|---|
| **admin** | **v182** · md5 `93b7f4fcef2b01f8a5c02d2538c51feb` | 本轮唯一改动端；index.html 已指 v182 |
| student | v379 | 本轮未动 |
| school | v225 | 本轮未动 |
| reviewer | v31 | 本轮未动 |
| partner | v147 | 本轮未动 |

链式基底逐版相扣：`5e9c04(v172)→376a90(v173)→dc54b4(v174)→2ed054(v175)→ae2eef(v176)→c71595(v177)→201695(v178)→10d43d(v179)→93a95a(v180)→52e0df(v181)→93b7f4(v182)`。

## 5. 我发现的、指令里没写到的问题（你该知道）
1. **管理端只有中/英双语**（全站 `currentLang==='zh'`、无俄法、`LL()` 用 0 次）。所以队列的「四语」闸对管理端所有包 = 「中英都在、无裸键」。俄法不适用（加了反破坏一致性）。
2. **迁移我一律跑不了**：`.env` 在我禁读清单，无法拿 DB 凭据连库——按项目一贯规矩「CC 写 SQL、业主跑」。三条迁移 + JOB-0.5 的 seed/cleanup 都等你在 SQL Editor 执行。
3. **头注/[VER] 探针在基底就停在 v155**（v156→v171 只靠文件名+index 推进）。本轮从 v173 起**每个新版都补了当版头注 + [VER] vN→vN+1**，照 v155 写法；v156–v172 的历史缺口没补课（按你的指示）。
4. **JOB-08 有 2 项延后**：item2 证件核验绿标（需批量按生取云端裁决，无首屏缓存，要新建批量装载器）；item7 学校收入（云端 platform_payments 无 school_id 列，**根本没有 per-school 收入源**）——建议单独定夺是「做一条按校归集收入的链」还是「撤掉这个字段」。
5. **JOB-09 item4/5、JOB-10 item3/4、JOB-12 九项**都因「需要一个新服务端函数或新数据源」而延后，各包回执里有逐条 backlog（写清打开哪里、要接什么源、卡点）。这些不是遗漏，是需要你先定源/先建函数才能接。
6. **闭环审计（铁律13）观察**：本轮管理端补了不少下游可见入口（付款订单/申请轮次/招生任务/候选池），但**这些下游的写入方（学生/学校/中介端）本轮未动**——若要「上下游一致」还需对应端配合，属跨端后续。
7. **本轮全部未推主仓库**：10 个 admin 提交（v173→v182）在本地等你验收后自己推（原生终端挂代理 `git push`）。日志仓库 `~/mh-jobs` 已实时推送（回执 + 迁移 SQL + 设计文档全在）。

---

**下一步在你**：①本地开 admin v182 看界面 → 满意就推主仓库；②在 SQL Editor 跑三条迁移 + （可选）JOB-0.5 seed；③回答四个「必须停」包的问题，我再开对应施工卡。
