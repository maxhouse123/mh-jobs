# JOB-9-05 · 邀请邮件施工 — 回执【EF源码已改·未部署；触发器已上生产】

> **⚠️ 迁移改号更正（9-06 时发现）**：本包原把迁移编成 **0175**、放 `docs/supabase 文件/`（旧账本，停在 0173），与真账本 `supabase/migrations/` 的 `0175_delegate_invites.sql` 撞号。已改号归位为 **`supabase/migrations/0192_invite_email_trigger.sql`**（打码，+ `_rollback.sql`），旧 docs/0175 文件已删。**触发器/函数本身已正确应用生产、不受影响**。下文“0175”一律以 **0192** 为准。

**大白话结论**：给「中介命名邀请陌生学生」补上了自动发邀请信这一小块。以前平台的发信只会发给「已经有账号的人」，命名邀请是发给「还没注册的陌生邮箱」，走不通。现在做了两件事：①一只**新云函数 `send-invite-email`**，照抄现成验证信的发信骨架（用 Resend 发、限速自动重试、每封信落投递单号），改成收件人取邀请表里的邮箱、链接用邀请码拼、正文四语（含中介名 / 被邀请人名 / 有效期 / 邀请链接）；②一个**新触发器**：邀请表每新增一行、且**填了邮箱**，就自动去调这只新函数（只出链接没填邮箱的邀请不发信）。**触发器已经上了生产库**（安全：新函数还没部署，触发器打过去会静默失败、异步、不会卡住"发邀请"这个动作）；**新云函数只改了源码、没部署**，部署留给业主。

链接格式**不是猜的**，取自中介端 partner v154 第 12745 行：`var _DL_BASE = 'https://www.maxhouses.net/student-portal/?invite=';` → 邀请链接 = `https://www.maxhouses.net/student-portal/?invite=<邀请码>`。

## 一、新云函数
- **函数名**：`send-invite-email`
- **源码路径**：`supabase/functions/send-invite-email/index.ts`
- **照抄**：`send-verify-email` 的 Resend 发信块 + 429 退避重试 + `email_dispatch_log`（本函数 `kind='invite'`）。
- **三处改**：①入口认证走 `x-mh-hook-secret`（同 notify-email，因触发器无用户 JWT）；②收件人 = `named_invites.email`；③正文四语内联——中介名取 `partners.org_name`（回退 `org_name_en`）、被邀请人 `display_name`、有效期 `expires_at`、邀请链接。
- **只首次 INSERT 发**（触发器天然如此）；「重发也发信」不做，记 **backlog**。

## 二、迁移（已应用生产 maxhouse_main）
- 文件：`jobs/JOB-9-05/0175_invite_email_trigger.sql`（可执行原件，本机）；归档 `docs/supabase 文件/0175_invite_email_trigger.sql`（**密钥打码**）。
- 内容：`invite_email_fanout_fn()`（照抄 `notify_email_fanout_fn`，`net.http_post` 打新函数 URL + 同款暗号头，**加判据 `if new.email is not null`**）+ `named_invites` AFTER INSERT 触发器 `invite_email_fanout`。全 `create or replace`（无 drop），走 db-run.sh。
- **跑前跑后**：`named_invites` 触发器数 **0 → 1**、`invite_email_fanout_fn` 函数数 **0 → 1**。执行日志：`BEGIN / CREATE FUNCTION / CREATE TRIGGER / COMMIT / DB-RUN OK`。

## 三、断言表
| 闸 | 断言 | 实测 | 过? |
|---|---|---|---|
| 触发器体 grep | 含 `send-invite-email` | 命中 3 | ✅ |
| 触发器体 grep | 含 `new.email is not null`（email 非空才发） | 命中 1 | ✅ |
| 迁移自检 | 触发器 +1 / 函数 +1 | 0→1 / 0→1 | ✅ |
| EF 结构 | 花括号/圆括号平衡 | 78/78、128/128 | ✅ |
| EF 内容 | 四语键 zh/en/ru/fr 各齐 | 各 3 | ✅ |
| EF 内容 | 邀请链接 = `?invite=<token>`（第140行, encodeURIComponent） | ✅ | ✅ |
| EF 内容 | 入口 `x-mh-hook-secret` 读 env、无密钥明文 | 读env=1, 明文=0 | ✅ |
| EF 内容 | 投递日志 `kind:"invite"` | 命中 2（成/败各一） | ✅ |
| deno check | 本机无 deno + 不挂代理无法装 → **未跑**；结构自检通过，**部署前请业主机跑一次 `deno check`** | ⚠️ 待部署前 | — |

## 四、零信任处理（密钥）
暗号 `MH_HOOK_SECRET` 明文只存在于**库内**的触发器函数（既有现状，沿用 notify_email_fanout_fn 同一个）与**本机未跟踪的 `jobs/JOB-9-05/0175_...sql`**。归档到 `docs/` 的那份**已把密钥替换成占位符 `__MH_HOOK_SECRET__`**（+ 头注说明），**不让密钥进 GitHub**。新 EF 自身**不含密钥明文**（从 env 读）。

## 五、必须停 / 待业主
- **`send-invite-email` 未部署**（必须停）。部署命令另出卡（Supabase 网页 Functions 编辑器粘贴 `supabase/functions/send-invite-email/index.ts` → Deploy，**带 `--no-verify-jwt`**；需确保 env 有 `MH_HOOK_SECRET`/`SB_SECRET_KEY`/`RESEND_API_KEY`/`SUPABASE_URL`）。部署前：命名邀请照常建、只是暂不发信（触发器异步 404 静默）。
- **rollback**：`jobs/JOB-9-05/rollback.sql`（归档 `docs/supabase 文件/0175_..._rollback.sql`）——含 `drop function`，白名单只放 get_my_offers 故自动闸会拦，**业主需要时在 SQL Editor 手跑**。

## 六、backlog
- 邀请「重发也发信」→ 需单独一个 RPC（本包只首次 INSERT 发）。

## 动过的文件
- `supabase/functions/send-invite-email/index.ts`（新, 源码, 未部署）
- `docs/supabase 文件/0175_invite_email_trigger.sql`（归档·打码）+ `..._rollback.sql`
- `jobs/JOB-9-05/`（可执行迁移原件 + rollback + 探针, 本机未跟踪）
- 生产库 maxhouse_main：`invite_email_fanout_fn` + `invite_email_fanout` 触发器（已应用）
- 已 commit `584ee7e`（未推）。**未动 notify-email / send-verify-email / named_invites 表结构 / 前端。**
