JOB-2-10  结果：全闸通过（迁移已跑 + 管理端 v190 → v191）—— 核查报告里最严重的层级链，管理端半边通了

## 做了什么（一句白话）
把学校"层级/名额"这条断了四截的链，管理端这一半接通：
1. 层级从三套互不相干的词（库里 A/B/C/空、界面青铜白银黄金、学校端 trial/standard/…）**统一成四档**：试用/标准/高级/旗舰。旧的青铜白银黄金退场。
2. 管理端显示的层级**改读数据库真值**，改层级用服务端函数存库（换电脑登录也在），不再存本机浏览器。
3. 每档名额进库做成配置表：试用 5 / 标准 20 / 高级 100 / 旗舰 500。
4. Quota 审批台新增「学校提额申请」分区（学校端 JOB-2-11 能提之后，这里就能收到并批/驳）。

## 迁移（我经 db-run.sh 跑的，跑前跑后差值）
- 新建 `tier_quota` 配置表（4 行：trial 5 / standard 20 / advanced 100 / enterprise 500）。
- **`schools.tier` 值域统一（影响 12 行）**：
  - 跑前分布：`空=8, A=1, B=2, C=1`
  - 跑后分布：`trial=9（含 8 个空值回填 + C）, standard=2（B）, advanced=1（A）, enterprise=0`
  - **跑后只剩四档、空值 0** ✓；并加了四档 check 约束。
- 新建 `school_quota_requests` 表（照 quota_requests 列，用 school_id；RLS：管理员 ALL + 学校读写自己的）。
- 新增函数 `admin_set_school_tier` / `admin_decide_school_quota`（本卡 2 个）。
- 迁移 `jobs/JOB-2-10/migrate.sql`；回滚见同目录（回滚未附，因 tier 回填不可逆——A/B/C 原值已归并，无法还原，见疑点）。

## 业主要看什么
1. 学校列表：筛选 chip 变成 试用/标准/高级/旗舰；层级徽章同步。
2. 学校抽屉「合作层级」段：显示当前档 + 四档名额；「手动覆盖」下拉改档 → 刷新后还在、换电脑登录也在。
3. Quota 审批台：多出「学校提额申请」分区（现在是空的，等 JOB-2-11 学校端能提交才有内容——正常）。

## 十道闸（逐条）
- 闸1 基底 md5：`85eb3cfb6a66e8679f38634c0477f7aa`（=v190）✓
- 闸2 锚点唯一：12 处各 =1 ✓
- 闸3 补丁自检：resolveSchoolTier 读 DB / _v22TierOverrides 归空 / tierBadge+tierLabel 四档 / 学校层级段换新函数 / 四档计数+chip（旧 gold 计数/chip 归 0）/ 学校提额分区+装载+decide 各命中 ✓
- 闸4 产物 md5：`8b65a15c3d9def1faffed2460fd618d4`（v191）
- 闸5 语法：1 脚本块 0 报错 ✓
- 闸6 消失检查：785→791（+6，无消失；旧 tierBadge/_v22SchoolTierSection 函数体保留只换消费）✓
- 闸7 四语：管理端中英；新增文案（试用/标准/高级/旗舰/学校提额申请/当前层级/四档名额…）中英齐全，零裸键 ✓
- 闸8 真点击：Playwright 桩 —— resolveSchoolTier 读 s.tier（advanced=advanced，null→trial 回落）；tierBadge 显 试用/旗舰；_v191SchoolTierSection 渲染四档+当前标准+下拉+_v191SetSchoolTier；_v191SetSchoolTier 走 rpc admin_set_school_tier(p_tier=advanced)；学校提额分区渲染 20→100 + 批准/驳回按钮；_v191SchoolQuotaDecide 走 rpc admin_decide_school_quota(p_approve=true)。反向样本 v190：函数未定义/仍青铜白银黄金 → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：四档徽章复用现有 badge 类（gray/green/purple/solid amber），零新写死颜色/CSS 类/字号（旧青铜的内联 #FBE7D2 随旧函数退场）✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：85eb3cfb6a66e8679f38634c0477f7aa → 8b65a15c3d9def1faffed2460fd618d4
迁移跑前跑后：tier 空8/A1/B2/C1 → trial9/standard2/advanced1/enterprise0；tier_quota +4 行；school_quota_requests +1 表；本卡函数 +2
index.html 改了几处：3（v190→v191）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- **tier 回填不可逆**：A/B/C/空 已归并进四档，原始 A/B/C 区分无法还原（裁决6 明授权）。故 rollback.sql 只提示不还原 tier 值（附了删表/删函数/删约束的回滚，但 tier 列数据不回退）。
- 「停用只挡新动作」的 status 用 suspended_at 见 JOB-2-09；tier 的四档 check 允许 null（防未来注册无 tier 时报错），非 null 一律四档。
- admin_decide_school_quota 只改申请状态+写事件（照 QUEUE），**不自动改学校名额**；管理员批准后如需真加名额，另用「手动覆盖层级」升档。
