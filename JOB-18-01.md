# JOB-18-01 · 管理端 v218：取件两趟同时发 + 下载用真文件名 + 新标签/下载统一放法

CC 施工。基线 admin v217 `a2e159102e85f4f4cbd1addcca99c3b6` → 新版 **admin v218 `73b36f0a4d6c09891419a873ff4af013`**。仅前端取件/渲染路改写，零其它端改动，不改共用查看器。**本地 commit，未推主仓库、未部署。**

## 一、做了什么（QUEUE-18 §18-01 三条）

### 第 1 条 · 取件两趟同时发（省掉 v217 先等 RPC 那趟排队 = 验收未过的 ~3 秒底子）
- 新增全局 `ADM_DOC_META = {}`（doc_id → `{file_path, file_name}`）+ `_v218StashDocMeta(rows)`。按 18-00 探针② 结论——`file_path` 原本不在 admin 页面内存（三处 documents select 都不取），管理员本可读 documents（is_admin RLS），故在**五处** documents select 里补 `file_path`（不新加 RPC），读回后 stash 进 `ADM_DOC_META`：
  - `loadIdVerifyFromCloud`（证件核验）、`_adm7Load`（学生详情材料）、`_v162EnrichVerdicts`（待二审 join）、`loadNavBadgesFromCloud`（导航徽标）、`loadDispatchDocsFromCloud`（可派池，喂派单台 + 派单历史两处「查看原件」）。
- 新 `_v218AdmFetchOriginal(docId)`：
  - `file_path` **已在内存** → 审计 RPC `admin_log_doc_view` 与 `storage.from('student-documents').download(file_path)` **进同一个 `Promise.all([rpcP, dlP])` 同时发**；两腿各自 `.then` 记完成毫秒。
  - 两者都回、且 `row.ok===true` 且下载成功 → 交显示（`via:'direct'`）。
  - `row.ok===false && error==='not_an_admin'` → **丢弃已下载内容、报错、`_noFallback:true` 不回落**。
  - RPC 记不上（非 not_an_admin：报错/约束拒）或下载失败 → **丢弃、回落 `_v217FetchViaFunction`（admin-doc 云函数，自带服务端审计）**。
  - **拿不到 `file_path` → 退回 `_v217AdmFetchOriginal` 的先后两趟**（原样保留）。
  - 「记不上就不给看」不变；控制台每次打 `[v218] fetch rpc=…ms download=…ms total=…ms via=direct|function`。
- `adminViewOriginal` 与 `_v177FetchAdminDoc`（打包下载）两个取件点都改走 `_v218AdmFetchOriginal`。「取件用时 + 直读/云函数」标注沿用 v217。

### 第 4 条 · 下载用库里真文件名
- `_v162AdmDocRender` 顶部算 `dlName = c.fileName || _v218FallbackName(docId, c.blob)`（兜底 `原件-<doc_id前8>.<扩展名>`，扩展名按 blob 魔数）。
- 图片那条 tools 行的「⬇下载」`<a … download="<dlName>">`（v217 是裸 `download` 无值 → 存成 blob 名）；「无法预览」兜底的下载键同样带名。直读时 `c.fileName` = RPC 返回的 `file_name`；回落云函数时取 `ADM_DOC_META` 里该件已有名；都没有才兜底。

### 第 3 条 · 新标签/下载统一放法
- PDF 分支改成 `host.innerHTML = tools + '<div … id="_v216pdfframe">' + info`（与图片同一条上方 tools 行、同一叫法「↗新标签打开 / ⬇下载」），`MHViewer2.mountPdf(_pf, {blob,pdfjsLib,lang,height:'100%',maxPages:20})` **不再传 `extras`**。
- **N1「移位不删」**：PDF 的两个按钮从组件工具条（extras）移到上方 tools 行的两个 `<a>`；数量不变、叫法统一、位置与图片一致。

四件套：文件名 v218 / 头注 v218 / `[VER] admin portal v218` / index.html 三处重定向 → v218；changelog 追加。

## 二、闸3（真页面红→绿，stub 会话真点 `adminViewOriginal`；`jobs/JOB-18-01/gate-admin.mjs`，全 10 条绿）

结果 JSON：`jobs/JOB-18-01/gate-admin-result.json`；日志 `~/mh-verify/r18-01-gate.log`。

| 检 | 结果 | 数字 |
|---|---|---|
| T1① 旧版 v217 顺序（注入 800ms 审计 RPC 延迟，下载晚于 RPC） | **红-如期** | gap=808ms |
| T1② 新版 v218 并发（file_path 在内存，下载与 RPC 同发） | **绿** | gap=0ms（≤50ms 门） |
| T1③ 新版无 file_path → 退回 v217 先后两趟 | 兜底证实 | gap=808ms |
| T2a v218 happy 图片：直读(rpc1/storage1/admindoc0)+显示+tools 两键+下载真名 `gate.jpg` | 绿 | — |
| T2b v218 happy PDF：mountPdf `hasExtras=false`+上方 tools 两键+下载真名 `gate.pdf` | 绿 | mount=[{hasExtras:false}] |
| T2c v218 not_an_admin：**不显示任何原件内容**（无 img/canvas）+ admindoc=0（不回落） | 绿 | storage 并发发出但内容被丢弃 |
| T2d v218 rpc-fail：丢弃直读、回落 admin-doc（admindoc≥1）仍显示 | 绿 | — |
| T2e 旧版 v217 图片「下载」`download` 属性为空 | **红-如期** | dlName='' |
| T3① kit2 PDF 尺子 `extras:false`（两键已移出组件条，J29n 通过） | 绿* | *J15 见下 |
| T3② kit2 图片尺子 | 绿 | 28 条判、0 不过 |

\* **J15**（共用查看器「极限拖动多出约 1 像素」）是 QUEUE-18 §先读**明列本轮不做**的项（改共用组件要三端整套重验），在未改的 v216/v217 共用查看器上同样存在、非 v218 引入 → 作为唯一允许的已知例外记录，不因此判红。

## 三、N1（`jobs/JOB-18-01/n1-diff.mjs`，全绿；`n1-diff.json`）
- v217 fnDefs=706 / handlers=185 → v218 fnDefs=**709** / handlers=**185**。
- ① v218 无新增死按钮（onclick handler 集合 185→185 未动，`getAttribute/getElementById/click` 等是正则把 `x.method(` 也抓进来的既有噪声，v217 同样存在）。
- ② 无活 handler 被删。③ 新增函数含 `_v218AdmFetchOriginal / _v218StashDocMeta / _v218FallbackName` 三件。④ onclick handler 集合无净删（PDF 两键改为 tools 行 `<a>`，非 onclick，不影响此集）。

## 四、P3 随卡回归（入仓 `tests/static-check-v218-admin.mjs`，22 条全绿）
主脚本 parse 干净 + 版本四件套 + `_v218AdmFetchOriginal`/`ADM_DOC_META`/`Promise.all([rpcP,dlP])` 并发 + 无 file_path 退回 v217 + not_an_admin 不回落 + 记不上回落 admin-doc + `[v218] fetch` 日志 + 两取件点改走 v218 + 五处 select 带 `file_path`（≥5）+ `_v218StashDocMeta` 调用点≥6 + 下载真名 `download="…"` + 兜底名 + 无裸空 download + mountPdf 去 extras + PDF 分支拼 tools 行。

## 五、公开截图（`~/mh-jobs/shots/18/18-01/` + INDEX.md；合成 PDF/图片，无真实学生数据，secret-scan CLEAN）
- `v217-pdf-extras-in-component-RED.png`：旧版 PDF 两键在组件工具条（extras）。
- `v218-pdf-buttons-on-top-row-GREEN.png`：新版 PDF 两键移到上方 tools 行（↗新标签 / ⬇下载），组件工具条不再有这两键。
- `v218-image-download-realname-GREEN.png`：新版图片下载键带真文件名。

## 六、结果 / 业主要做什么
- 本包只改前端、**本地 commit、未推主仓库、未部署**；无迁移、无写库（gate/shots 全在网络层桩，零真库写）。
- **业主本包无需做任何事**。收官（18-07）后由业主用卡1140 统一推送。
- 业主验收项（进 SUMMARY「亲眼看」清单）：关代理，管理端打开 3 份几百 KB 原件——「取件用时」各 ≤ 2 秒且标「直读」；图片与 PDF 两处「下载」存下来的是库里真文件名。

## 七、新闸证据
- **N1**：v217→v218 静态差集全绿（上表），零新增死按钮、只增不减。
- **N2**：T1①/T2e 先红（旧版顺序取件 / 空下载名），T1②/T2a–d 后绿；kit2 两尺子绿（J15 为已知例外）。
- **N3**：gate 为 stub 会话（管理端无管理员密码，照 §0.4 stub-only），网络行为与内核无关，无「0 行/无行」当证据。
- **N4/N5**：本包无云函数改动；secret-scan 对 jobs/JOB-18-01 + tests + shots = CLEAN（fakeJwt 动态拼、无 eyJ 字面量，无密码/service_role）。
- **N6**：只做 §18-01 三条；18-00 查到的 studyProof/studyProof 未知类型、慢件根因不在本包顺手改（归 18-02 / 十九轮）。
- **N7**：本包无「给人看的提示」文案/颜色/停留改动（取件提速、下载名、按钮放法均非提示件），N7 不适用；G/H 的 N7 三件量在 18-03。

---
**下一包 = 18-02**（审核端 v43：查看时预取让路 + 找慢件的原因 + 超高学生卡 + 未知类型名）。
