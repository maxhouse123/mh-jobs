# JOB-12-01 · 读侧去云函数（迁移 0196 + reviewer v34 → v35）

结果：**全闸通过**（迁移已应用生产 + RLS 模拟实测；reviewer v35 三级回落 + 闸8 14/14）。⚠️ 一处 DB 约束限制已绕开并落 backlog（见下）

## 大白话（业主看这段）
审核端命中缓存原来还要**先绕一趟云函数**拿签名链接（冷启动 1–2 秒），所以"命中了也不快"。这轮改成：**审核员用自己的登录态直接从桶里下预渲染件，不再经云函数**；审计改由一条数据库函数记。
- 桶加了**一条只读策略**：审核员只能读**自己文件夹里、且派单仍有效、材料未删**的对象；换个不相干的人一份也读不到。原件桶 `student-documents` 一字节没动。
- 审核端 v35 的取件逻辑改成**三级**：① 直读桶（最快）→ 失败就 ② 走原签名链接路 → 再失败 ③ 现场渲染（老逻辑一字没动兜底）。控制台会打 `[v35] wm direct HIT <毫秒> <KB>`，应该明显比 v34 的 2.7–7 秒快。

## 迁移 0196（已应用生产）
| 断言 | 期望 | 实测 | 过 |
|---|---|---|---|
| 新表 wm_cache_index | +1 | 1 | ✅ |
| 读策略 wm_cache_owner_read | +1 | 1 | ✅ |
| RPC log_wm_view | +1 | 1 | ✅ |
| log_wm_view anon 可执行 | 否 | f | ✅ |
| log_wm_view authenticated 可执行 | 是 | t | ✅ |
| wm_cache_index anon/authenticated select | 否 | f / f | ✅ |
| **RLS 模拟·reviewer1 看到桶对象** | =有效派单数 | **100** | ✅ |
| **RLS 模拟·不相干 uid 看到** | 0 | **0** | ✅ |
| **撤单的（259f456a）可读** | 否 | wm_cache_can_read=false | ✅ |
| 非 uuid 文档（DOC-TEST-001）可读 | 是 | true | ✅ |
| log_wm_view 有效派单 → 返回 rv + 写审计 | 是 | 返回 RV-D09E，写入一行 outcome=served | ✅ |
| log_wm_view 无派单 → 报错 | not_assigned | raise not_assigned | ✅ |

## reviewer v35（闸）
| 闸 | 结果 |
|---|---|
| 1 基底 md5 = v34 | ✅ 8a4b3f27551569ddaca6460cb29b3009 |
| 4 产物 md5 | ✅ 6d95e696c3688ee2a9fa4fee46ed643d（本包后；12-03 还会再改 v35） |
| 5 语法 | ✅ 0 报错 |
| 6 功能消失 | ✅ v34→v35 特征 179→182（+3 函数 _wmTryDirect/_wmViaSigned/_wmExt），消失 0 |
| 7 四语 | ✅ 无新增用户文案（只加 console 日志+注释） |
| 8 真点击（stub 驱真 _wmFetch） | ✅ tests/job12-01.mjs **14/14**：①直读命中走①+调 log_wm_view+日志；②直读403→signed_url+不调RPC+不走EF；③直读失败+signed miss→现场渲染EF；静态 _wmFetchEF 逐字保留 v34==v35 | 
| 9 视觉 | ✅ hex 颜色分布 v34=v35（0 新增），无新 CSS |
| bump 四件套 | ✅ 文件名 v35 / [VER] v35 / index.html 3 处 v34→v35（残留 0） |

**三级回落顺序**：直读 → signed_url → 现场渲染；`_wmFetchEF` 逐字保留（闸8 静态断言 v34==v35）。

## ⚠️ 发现 + backlog（业主决定）
`reviewer_access_log` 的 `outcome` CHECK 约束**只允许 `served` / `denied_not_assigned`**。
- 卡要的 `served_cached_direct` 被约束拒 → 本轮 log_wm_view **落 `served`**（"谁在何时看了哪份"照样留痕，泄露追溯目的达成；缓存/现场之别只是运维标签）。
- **顺带发现一个老问题**：watermark-doc EF 自己写的 `prepared` / `served_cached` 等 outcome **也一直被这条约束拒**（EF 的 logAccess 吞错 → 静默失败），即这些审计**一直没真正写进去**。
- 放宽约束需改约束语句（被 db-run.sh 红线挡 + 铁律9），留业主授权。**现成 SQL**（业主在 SQL Editor 跑）：
  ```sql
  alter table public.reviewer_access_log drop constraint reviewer_access_log_outcome_check;
  alter table public.reviewer_access_log add  constraint reviewer_access_log_outcome_check
    check (outcome in ('served','denied_not_assigned','prepared','served_cached',
                       'served_original','served_video_url','served_cached_direct'));
  ```
  放宽后，一行 `create or replace log_wm_view` 即可把落地值改回 `served_cached_direct`（我下轮代做）。

## 施工纪要（踩坑 → 最终形态）
- `documents.doc_id` / `reviewer_assignments.doc_id` 是 **text**（有非 uuid 存量 DOC-TEST-001，桶里 3 个非 uuid 对象）→ 索引表 doc_id、RPC 参数都用 text（0196 已修）。
- 策略里 EXISTS 直读 `documents` 会被 documents 的 RLS 挡（审核员看不到 documents）→ 判定挪进 SECURITY DEFINER 函数 `wm_cache_can_read` 绕过（否则审核员一份也看不到，实测修前=0、修后=100）。
- 归档 `supabase/migrations/0196_wm_cache_direct_read.sql` 是合并最终版；jobs/JOB-12-01/ 保留 0196/0196b/0196c/0196d 的演进过程。db-run-drop-allow.txt 加 `log_wm_view`（供 0196c 改签名 drop+create，受控 replace）。

## 动过的文件
- DB：新增表 wm_cache_index + 策略 wm_cache_owner_read + 函数 wm_cache_can_read/log_wm_view（新增型，已应用）
- 归档：supabase/migrations/0196_wm_cache_direct_read.sql + _rollback.sql
- 前端：新增 reviewer-portal/maxhouse-reviewer-portal-v35.html；改 reviewer-portal/index.html（3 处 v34→v35）
- 测试：新增 tests/job12-01.mjs（闸8）
- jobs/JOB-12-01/*（迁移演进 + 探针 + 回执）；jobs/db-run-drop-allow.txt 加 log_wm_view
- **不动 EF**（12-02 才改 EF 源码）
