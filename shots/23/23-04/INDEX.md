# JOB-23-04 截图索引（管理端 v223 · 学校行为日志）

数据来源：本地 file:// 无头 + stub window.sb 的合成桩数据（27 条埋点 + 690 条取件，无真实 PII）。
学生名/成员/时间那一行已模糊（blur）。

- `log-default-nonfetch.png` —— 进页默认「全部·不含取件」：筹码序「全部·不含取件(27) / 导出数据 / 导出名单 /
  下载材料 / 查看学生 / 取件(690) / 全部(717)」，正文只显 27 条合规记录，取件流水不再淹没。
- `log-fetch-chipbar.png` —— 筹码栏特写（当前停在「取件」）。
- `log-fetch-row.png` —— 一条取件行：学生名已由反查 documents(doc_id→student_id) 补上（图中已模糊）。

线上肉眼验收留业主；线上若 admin 读 documents 策略不足，取件行学生名会显「—」（见回执第五节）。
