# JOB-23-03 截图索引（管理端 v222 · 审计事件人话）

数据来源：本地 file:// 无头 + stub window.sb 的合成桩数据（12 条 admin_events，7 类，无真实 PII）。
时间/学生名一行已模糊（blur）；截图为 #view-auditEvents 容器的元素级截图。

- `audit-events-human.png` —— 顶部动态筹码「全部 + 每类型（学校解锁联系方式·2 / 设置签证有效期·2 / 分享链接被打开·2 …）」；
  每行标题是中文（带图标），下面是「Offer OD-1 / 材料 00000000 / 有效期至 2027-01-01 / 申请号 MH-000042 / 第1轮 /
  范围 referral / 打开1次 / 任务 T-3」这样的键值小标签；「原始内容」默认收起（点开才看原始 JSON）。
  未知键（如 interview_result 的 passed）按「键: 值」原样显示。

线上肉眼验收留业主。
