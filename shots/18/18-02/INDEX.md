# JOB-18-02 reviewer v42→v43 red→green shots

READ-ONLY verification. Synthetic PDF/image bodies at the network layer. Real student names masked (CSS blur) before capture. No tokens/JWTs/emails.

- `v42-unknown-type-title-RED.png` — reject modal for a null-docType doc on v42: title `退回材料：` with blank docType (RED).
- `v43-unknown-type-title-GREEN.png` — same on v43: `_v43SafeType` fills `未知类型 / Unknown type` (GREEN).

See gate-reviewer-result.json for per-gate numbers.