# JOB-17-07 回执 · G④⑤ —— 线上走查脚本修好 + 第十六轮收官单勘误

CC 本包只做两件：**G④** 把线上走查脚本从 JOB-16-09 复制后修好，并对**现役线上**（第十六轮版本）
真跑一遍如实记录；**G⑤** 新写 `SUMMARY-16-ERRATA.md` 勘误三端放大缩小的真实放行状态。
零端口升版、零迁移、零云函数改动、未推主仓库。

## 一、做了什么

### G④ 线上走查脚本（`jobs/JOB-17-07/online-walk.sh` + `.mjs`，从 16-09 复制后修；旧文件不动）
相对 16-09 的修点（对齐 QUEUE-17 §17-07）：
1. **五端 [VER]**：轮询五端 index 各自的目标版本文件（student v397 / partner v165 / school v234 /
   admin v217 / reviewer v42）；admin/reviewer 在 CF Access 后，如实记「挡」（http 302，非红非绿记 N/A）。
   `extractVer` 按线上重定向目标（**不带 .html 扩展名**）解析，实际指向的旧版本号如实打印。
2. **上传按真类名 + §0.11 挑槽**：先以 stuA 真身份 REST 读 `documents`（含已软删行，删不减批次）
   算每个安全槽的批次，挑「上传后 ≤ 第4批」且最小的槽真传；只在
   `studyPlan / other / cv / recommendation` 里挑，**不碰四闸门槽（护照/成绩单/学历/照片）与 bank/studyProof/visa**。
   本次开工探针实证：studyPlan 0批 / other 0批 / cv 1批 / recommendation 2批 → 选中 **studyPlan（上传后=第1批）**。
   上传用真页面真类名定位（`#file-studyPlan`、`#card-studyPlan`、`.thumbs-grid`、`.v393-slim-line`），
   `setInputFiles` 触发 `onchange`（Playwright 上传正路，非 evaluate/dispatch）。
3. **检查项**：五端 [VER]、进度卡（卡出现已上传件）、瘦身行（`.v393-slim-line`）、刷新后文件与瘦身行仍在、
   英文界面下 AI 提醒不是中文（有提醒时才判）、中介端 Sent invites 列表（`[data-dl-copy]`）与 Copy link 有可见反应
   （提示条「链接已复制」/「Link copied」或按钮字变「已复制」，探到 1.5s）。
4. **收尾软删**：真点 `.thumb-remove` → 真点确认框 `#confirmModalConfirmBtn`；兜底直调页面云删核心
   `_s9CloudDeleteCore`（storage+documents 真软删，非只弹确认框）。**收尾用带身份探针证明干净**。
5. **公钥读环境变量 `SUPABASE_ANON_KEY`**（缺省回落 env.mjs 的公开 anon）；元素级公开截图进 `~/mh-jobs/shots/17/walk/`；
   回执 `~/mh-jobs/JOB-17-07-online.md` 由脚本自动写、跑完一律 exit 0（不中断业主的卡）。

CC 对现役线上单探用 `MH_WALK_NO_WAIT=1`（轮询只探一次，不空耗 10 分钟）；业主卡1136 跑不带该变量
（轮询等新版最多 10 分钟）。本机可直连线上（`--no-proxy-server` 正路，实测直连 200）。

### G⑤ 勘误（`~/mh-jobs/SUMMARY-16-ERRATA.md`，SUMMARY-16 原文一字不改）
依 `JOB-16-01b.md` + 业主 09-21 验收，勘正 SUMMARY-16 §七#1：**学校端 v233 / 管理端 v214 的放大缩小**
已由 16-01b 真页面红→绿（checkViewer 28 判全过 / 0 / 0，chromium+webkit 双内核）、**转为放行**，
不再是「待业主手验·未放行」；审核端 reviewer v39 维持放行。三端预览弹窗均无挡板/层级/容器高度问题、未改端口代码。

## 二、结果是什么（`JOB-17-07-online.md` 全文；现役线上单探）
小结：**绿 8 / 红 7 / 灰 4**。所有红都是**第十七轮新功能尚未上线**（要等卡1135 推送 → 卡1136 部署）的预期红：
- 五端 [VER] 指向新版：student→v396 / partner→v163 / school→v233（红，现役=旧版）；admin/reviewer CF Access 挡（N/A）。
- ② student console [VER]=v397：红（现役旧版）。
- ③ Copy link 有可见反应：红（现役 partner v163，正是 17-03 要修的病）；③ 中介端 pageerror ×1：红
  （现役 v163 剪贴板被拒时的**未捕获拒绝**，也正是 17-03 修的那条 —— v164 起消失）。

**绿的**（现役线上已支持、真跑通过）：真定位上传框、进度卡出件、precheck-ai=200、刷新后文件仍在、
学生端 0 报错、收尾活着的测试件=0（干净）、Sent invites 列表在（7 条）。
**灰的**（如实非红非绿）：瘦身行（合成图非 PDF，常无瘦身行）、英文 AI 提醒（本次合成图未触发 issue，无提醒不判）、
admin/reviewer 的 CF Access 挡。

真「红→绿」要在**业主卡1136**（推送后新版上线 + precheck-ai v23 部署）里再跑一次才出（脚本不带 NO_WAIT，轮询等新版）。

## 三、业主要做什么
1. **卡1136**：挂代理 `supabase login` 后 `export SUPABASE_ANON_KEY=<student-portal 里的 sb_publishable_ 那串>`，
   关代理再跑 `bash /Volumes/Dev/MAXHOUSE/jobs/JOB-17-07/online-walk.sh`（不带 NO_WAIT，轮询等五端指向新版最多 10 分钟）。
   届时 Copy link 提示条、英文 AI 提醒非中文 应转绿，元素级红→绿截图自然产出到 `~/mh-jobs/shots/17/walk/`。
2. 本包**已把回执/勘误/online md/shots 提交并推 `~/mh-jobs`**；**主仓库未推**（无主仓库代码改动，本包只有 jobs/ 脚本与迁移无关）。

## 四、新闸证据（N1–N6）
- **N1（按钮清单差集）**：本包**无端口升版、无按钮/UI**，N1 不适用；五端 HTML 一字未动。
- **N2（先红后绿）**：走查脚本即「真页面真点、先红后绿」的载体 —— 真填登录表单 + `setInputFiles`/`locator.click()`
  （非 evaluate/dispatch）；判据只认屏幕文字（[VER]/列表/提示条）与网络（precheck-ai=200）。现役线上红已如实出，
  绿依赖新版上线（卡1136），本包不据此放行绿。
- **N3（带身份）**：批次探针、收尾复核全走 stuA 真 JWT（REST）或带身份 SQL 探针（`clean-check.sql` 回显 auth.uid()）；
  无身份的「0 行」不当证据；探针只读密码经 db-run，不打印任何 token。
- **N4（每条分支各验一次）**：五端 [VER] 逐端、上传→进度卡→即判→刷新持久→软删逐步、partner 列表在/Copy link 反应/pageerror 逐条如实。
- **N5（secret-scan）**：`node jobs/JOB-16-00/secret-scan.mjs jobs/JOB-17-07/` → CLEAN；push 前对 `~/mh-jobs` 全树再扫。
  脚本公钥读环境变量、绝不打印 token/密码。
- **N6（只修不加）**：只按图纸修走查脚本 + 写勘误；未碰端口、未碰云函数、未加迁移、未部署、未推主仓库；
  `jobs/JOB-17-01/kit2/` 一字未改（只 import 其 `makeSyntheticTranscript`）、`jobs/JOB-17-01/kit/` 未装未用未删。

## 五、动过的槽 / 测试写库（§0.11）
- **动过的槽**：`studyPlan`（本轮开工探针实证开工时 0 件 = 无历史，全部为本轮测试残留）。三次走查 + 两次截图尝试
  各真传 1 件合成成绩单到 studyPlan（每次「上传后=第1批」，**全程未触发第3批原因框、未近第5批锁**，删不减批次故批次数
  仅随小时窗计、始终第1批）。
- **收尾**：run3（收官走查）改用「真点 .thumb-remove + 确认框」/ 兜底 `_s9CloudDeleteCore` 后**已删自身上传**；
  run1/run2（旧收尾只弹确认框未真删）与两次截图尝试的残留，用 `sweep-residue.mjs` / `purge-studyplan.mjs` 全清。
  **带身份 SQL 探针 `clean-check.sql` 最终实测：`studyPlan alive = 0`**（REST 复核亦 0）。零残留。
- 公开截图目录 `~/mh-jobs/shots/17/walk/` 现只放 `INDEX.md`：现役线上新功能未上线（无绿可截）、中介端列表含真实 PII 不宜公开、
  studyPlan 卡在折叠区 boundingBox 为空无法元素级截图 —— 真红→绿元素级截图留业主卡1136 新版跑时产出（INDEX.md 已写明）。

下一包 = 17-08
