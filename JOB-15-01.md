# JOB-15-01 · precheck-ai v21 · 浏览器预检(CORS)修复 + 自带探针的部署脚本

生成于 2026-09-19 · 只改源码**未部署**（业主跑部署脚本）

## 做了什么
- 改 `supabase/functions/precheck-ai/index.ts`（v20→v21），三处最小改动：
  1. 新增 `CORS` 常量：`Access-Control-Allow-Origin:*`、`Methods:POST, OPTIONS`、`Headers` 含 `x-precheck-key`+`x-supabase-api-version`、`Max-Age:86400`。
  2. `Deno.serve` **最前面**（L152，先于 method 判断 L153、先于 x-precheck-key 校验 L154）加 `if(req.method==='OPTIONS') return 204 带 CORS`。
  3. `json()` 统一带 CORS 头 → 200/401/405/5xx 所有响应都带 `access-control-allow-origin`。
  4. 头注升 v21。**v20 的下载前看体积、错误件 3 次封顶、prompt、三条路径全部一字未动。**
- 写部署脚本 `jobs/JOB-15-01/deploy-precheck.sh`：deploy（`--no-verify-jwt`）→ OPTIONS 探针（无 ACAO 头即 exit 1 并打印全头）→ 无鉴权 POST 探针（期望 401）→ 每步 echo 人话。

## 结果是什么（自验，命令带出处）
- **分支顺序**（grep）：OPTIONS 分支在 **L152** < method 判断 **L153** < key 校验 **L154** ✅。
- **CORS 头**：`grep -cF -e 'Access-Control-Allow-Origin'` = **1**（常量，经 `json()` 展开进每个响应）；allow-headers 明含 `x-precheck-key` ✅。
- **括号配平**：注释/字符串剥离后 `node bracket-check.cjs` → **BALANCED** `{"(":0,"[":0,"{":0}`（deno 本机未装，用 node 代替）。
- **假请求走查**（`node handler-test.cjs`，复刻 handler 头分支）：
  - OPTIONS → **204 带 ACAO=*** PASS
  - 无 key 的 POST → **401 带 ACAO=*** PASS
  - GET → **405 带 ACAO=*** PASS
  - 带正确 key 的 POST → **PROCEED（进原逻辑）** PASS
- `bash -n deploy-precheck.sh` = **语法 OK**。
- **v21 新 md5 = `5ad6bbe86c386449a5072a423a3397e3`**（`md5 -q`）。旧 v20 = `da52fad0…`。

## 业主要做什么
1. 在**原生终端（挂代理）**跑：`cd /Volumes/Dev/MAXHOUSE && bash jobs/JOB-15-01/deploy-precheck.sh`
   - 脚本会自动部署并当场探两针：应看到「OPTIONS 有 access-control-allow-origin 头 ✅」与「无 x-precheck-key 的 POST 被挡为 401 ✅」。
   - 若 OPTIONS 仍无 ACAO 头，脚本会 exit 1 并打印全部响应头，把那段贴回来。
2. 部署后到**学生端**刷新，触发一次 AI 预检，看是否从 `ai now 1 sent 0 got` 变成 `ai now 1 sent 1 got`（验收表 1）。
3. 本包**不推主仓库**（EF 源码 + 脚本已 commit 到 main 待你原生终端 push）。

## 边界守住
未动批处理逻辑、未动 prompt、未动 secrets；未部署。
