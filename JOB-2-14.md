JOB-2-14  结果：只探不改，七条数据源已探清（下面给你拿去开第三轮）

> 全程只读探针（probe.sql / probe2.sql / probe3.sql），未动任何代码、未建任何东西。

## 七条逐条结论

### 1. 四类材料闸门状态
- **数据在哪**：函数 `get_my_doc_statuses(uid)`（学生自己用）返回 `(doc_type, verdict, comment, reviewed_at)`；另有 `get_partner_student_doc_statuses(p_sid)` 给中介用。
- **管理员读得到吗**：`get_my_doc_statuses` 内部有 `where uid = auth.uid()` 硬门，管理员传学生 id **拿不到**（会返回空）。
- **要不要加策略**：不用加策略。两条路：①**推荐**——管理员**自己算**：闸门状态就是 documents + review_verdicts + school_doc_reviews 的归并，这三张表管理员本就有 ALL 权限、JOB-05/06 已经在抽屉里加载了，直接派生四类闸门即可，零后端。②或新建一个 `get_admin_doc_gate(p_student_id)`（is_admin 门，复用同逻辑）。
- **建议显示**：学生抽屉材料区顶部加「四类核心材料闸门」四个状态药丸（护照/学历/成绩单/照片：通过/退回/待审），用①派生。

### 2. 邮箱验证状态
- **数据在哪**：`is_my_email_verified()` 只认 auth.uid()（管理员用不了）。但 **`students.email_verified` / `email_verified_at` 两列就在 students 表**，管理员已在加载。
- **管理员读得到吗**：**学生**的——读得到（students.email_verified 直接用）。**学校/中介**的——`auth.users.email_confirmed_at` 前端读不到，需一个 admin 函数暴露。
- **要不要加策略**：学生不用。学校/中介需新建 admin 函数（如 `admin_email_verified(p_uid)` 读 auth.users）或各自表补 verified 列。
- **建议显示**：学生抽屉 hero 加「邮箱已验证/未验证」徽章（直读 students.email_verified）；学校/中介抽屉同位置，等 admin 函数建好再接。

### 3. AI 预检结果
- **数据在哪**：**不在 documents（该表无任何 jsonb 列，已确认）**。在**独立表 `ai_precheck_shadow` 和 `ai_precheck_draft`**。
- **管理员读得到吗**：需再探这两表的列与策略（本轮只确认了表存在）。
- **要不要加策略**：很可能要加 admin 只读策略（多数新表默认无 admin 策略，同 JOB-09/10/12 的模式）。
- **建议显示**：先探清两表结构 + 关联键（按 doc_id / student_id?），再在材料行显示学生上传时的预检警告。**第三轮先补一条结构探针**。

### 4. 分享 offer 记录
- **数据在哪**：函数 `create_offer_share` / `list_my_shares` 写/读表 **`media_shares`**（列：token/offer_id/sharer_uid/sharer_role/scope/expires_at/revoked_at/open_count/last_opened_at）。
- **管理员读得到吗**：**读不到**——`media_shares` 上一条策略都没有（RLS 下默认拒）。
- **要不要加策略**：**要**——加一条 `admin_read_media_shares`（is_admin 门），同 JOB-09 写法。
- **建议显示**：学生抽屉加「分享记录」段，列 scope/创建时间/打开次数/是否已撤销。

### 5. 所在地变更申请
- **数据在哪**：函数 `request_location_change` 写表 **`location_change_requests`**（列：student_id/from_in_china/to_in_china/doc_id/declared_valid_until/created_at/applied_at/cancelled_at）。
- **管理员读得到吗**：**读不到**——只有 `lcr_self_select`（学生读自己）一条策略，无 admin。
- **要不要加策略**：**要**——加 `admin_read_location_change_requests`（is_admin 门）。
- **建议显示**：学生抽屉加「所在地变更申请」段，列 从→到 / 时间 / 是否已生效。

### 6. 中介委托邀请明细 + 代付款
- **数据在哪**：`named_invites`（列含 partner_id/email/display_name/passport_no/intents/status/claimed_by…）+ `payment_orders`（lane='agent' 且 payer_uid=该中介）。
- **管理员读得到吗**：**都读得到**——`named_invites` 有 `named_invites_admin_select`；`payment_orders` 有 `po_admin_all`（ALL）。**策略够，无需新增**。
- **要不要加策略**：不用。
- **建议显示**：中介抽屉加「委托邀请」段（读 named_invites where partner_id）+「代付的款」段（读 payment_orders where lane='agent' and payer_uid=中介uid）。纯前端可做。

### 7. 学生是否申请过当学生中介
- **数据在哪**：`partners` 表 `kind='student_agent'` 的行（实测 **6 行**）；学生与中介**同 uid**（students.id == partners.id）。
- **管理员读得到吗**：**读得到**——`partners` 有 `partners_admin_select`。
- **要不要加策略**：不用。
- **建议显示**：学生抽屉加标记「该学生已申请成为学生中介」+ 一键跳合作伙伴页（openPartnerDetail(该学生uuid)）。前端 join 即可。

## 小结（给第三轮排期用）
| # | 项 | 现状 | 要加策略吗 | 前端可直接做吗 |
|---|---|---|---|---|
| 1 | 材料闸门 | 三表可派生 | 否 | ✅ 派生 |
| 2 | 邮箱验证 | 学生列可读/校中介需函数 | 学生否·校中介是 | 学生✅ / 校中介待函数 |
| 3 | AI 预检 | 在 ai_precheck_* 表 | 很可能是(待探结构) | 待第三轮先探结构 |
| 4 | 分享记录 | media_shares 无 admin 策略 | **是** | 待加策略 |
| 5 | 所在地变更 | location_change_requests 无 admin 策略 | **是** | 待加策略 |
| 6 | 委托邀请+代付 | 策略齐 | 否 | ✅ 直接做 |
| 7 | 学生中介标记 | 策略齐 | 否 | ✅ 直接做 |

**未动代码。** 探针文件在 `jobs/JOB-2-14/`。请据此开第三轮：能直接做的（1/6/7）先做；4/5 各配一条 admin 只读策略迁移；2 的校/中介半边配一个 admin 邮箱函数；3 先补结构探针。
