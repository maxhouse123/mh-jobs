# JOB-15-00 · 开工检查 + 第十四轮落地复核 + 遗留 8 项探针（全只读）

生成于 2026-09-19 · 零写库 · 未下载任何线上原件

## 做了什么
- 复制 QUEUE-15.md 到 jobs/（md5 `86ac794edf6ae1d85f174770cb7647e4` 与卡一致，8 包）。
- `git -C ~/mh-jobs pull` = Already up to date；逐个核五端基线 md5（命令 `md5 -q`），全部与 SUMMARY-14 一致。
- `~/mh-verify` Playwright 自测：`node _selftest15.cjs` → `PW_SELFTEST MH-SELFTEST-OK`（无头 chromium 可用）。
- 只读探针全部走 `bash jobs/db-run.sh`：probe-schema / probe-a / probe-net / probe-de / probe-f / probe-ghj / probe-g2f / probe-dangling。
- 云端跨域 `curl -si -X OPTIONS`；两只 EF 源码 grep + md5。

## 结果是什么（数字带出处）

### 开工基线核对（全过）
| 端 | 版本 | 现测 md5 | SUMMARY-14 | 命中 |
|---|---|---|---|---|
| admin | v211 | 30b6e79d8ca22f4ecf07a40e345025b8 | 同 | ✅ |
| student | v393 | d3f266183cb86e854bbc316172dfb579 | 同 | ✅ |
| partner | v159 | 1fd5d1ae424510b968199b35223fbfec | 同 | ✅ |
| school | v232 | 26bf7160eb07c83bf53e0b37065084c3 | 同 | ✅ |
| reviewer | v37 | c32bbcb96e027c7780d818191900f7c8 | 同 | ✅ |
- EF 现测 md5：precheck-ai `da52fad0…`（=v20）、watermark-doc `8c98e64c…`（=v32），均与线上现役一致。
- 0204 约束（probe-a A0）：`reviewer_access_log` CHECK 已含 `'served_video_signed'` ✅。
- `platform_flags.wm_render_v` = **2**（2026-09-18 14:34 置）✅。

### 第十四轮落地复核（probe-a）
- **precheck-ai v20 生效**：cron `precheck-ai-shadow` 近 24h 跑 **288 次全 succeeded**；`ai_precheck_shadow` 近 24h 仅 **+2 行**（均 verdict=fail），`too_large` **0**；近 24h 预检碰过的件 size_kb 之和 = **1674 KB（约 1.6 MB）**。→ **止血生效**：从「1.5 GB/天」降到近乎为零（每 5 分钟只碰极少数出错件、不再整份重下）。
- net._http_response 近 6h 全量 status：200×54、500×11、546×59、null×39（546/500 = 水印现场渲染的内存/超时失败，主因见结论 d）。
- **watermark-doc v32 + 重灌**：`wm_cache_index` 按 status×render_v：cached·v1 **25**（全是 PDF）、cached·v2 **72**、gave_up·v1 **2**、unsupported·v1 **6**；近 24h 变 render_v=2 的 **72** 行；**仍 cached 且 render_v=1 的「图片件」= 0 件**；`wm-cache-backfill` 近 24h 跑 288 次全 succeeded，最近一次 2026-09-18 15:40。→ **图片件重灌已 100% 完成**（无图片件停在 v1）。
- **14-06 大图**：5 张 jpg 现状 size_kb = big size 2.jpg 837/837、image.jpg 767、image.jpg 718、IMG_5208.jpg 1378，**全部 <1500 KB**；4 张 wm=cached·v2，1 张（657ff94d image.jpg 718KB，最老件）wm 从未缓存(null)。本机备份 `img-originals-20260919/` 存 5 张原件（big_size_2 = 6.3 MB）；`jobs/JOB-14-06/apply.log` = **0 字节**（未写日志）。→ **大图问题已消解**：DB 里 size_kb 已是压缩后的小值（原件 6.3MB→837KB），无需再压；list.json 里 jpg>1500KB 已为 0。
- **管理端失败件**：`admin_wm_cache_failures()` 经 db-run（无管理员 JWT）返回 **[]**——函数内 `is_admin(auth.uid())` 闸在无登录态时判空。真实底层失败件 = gave_up·2 + unsupported·6（见上 wm 索引）。以管理员上下文看真结果留 15-03。

### 遗留 8 项事实
- **(c) 跨域**：`curl OPTIONS` → **HTTP 405 `{"error":"method_not_allowed"}`，响应头无 `access-control-allow-origin`**。源码顺序（grep）：`Deno.serve` L142 → **L143 `if(req.method!=="POST") return json({...},405)`** → L144 x-precheck-key 校验；`json()` L138-139 只带 `content-type`、**无任何 CORS 头**。→ OPTIONS 在最前面就被 405 挡死。
- **(d) 259f456a**：是 **670 KB 的 jpg 银行流水**（file_name `178953718457….jpg`，doc_type=bank），属**测试学生 A**（43668ae4，is_test=true）。`wm_cache_index` **无行**；`reviewer_doc_queue` 无行；wm-cache 桶无对象；`reviewer_assignments` 唯一一条 09-17 已 **revoked**；近 7 天 `reviewer_access_log` outcome=served×4（每次现场渲染）。
- **(e) f7fdc407**：是 **.docx**（`APPLICATION 2024 BUSINESS ADMINISTRATION.docx`，34 KB，doc_type 误标 photo）。`wm_cache_index` **有行且 status=unsupported**（last_error "type not supported"，render_v=1，object_name null）；`reviewer_doc_queue` 无行（无有效派单）。
- **(f) 管理端 403 清单**：管理端直读 37 张表/视图逐张查授权，**嫌疑 = 恰 4 张**：`ai_precheck_draft` / `application_drafts` / `media_shares` / `task_candidates`。四张均 RLS=on、**已各有 `admin_read_<表>` 读策略**，但 **authenticated 缺 SELECT 授权**（GRANT 层，先于 RLS 判定）。以管理员 uid `88bd171a…` 逐张 `select count(*)` → **四张全 "permission denied for table X"**（probe-g2f DO 块实证）。→ 修法只需 `grant select … to authenticated`（策略已在）。
- **(g) 学生端悬空行**：学生 A 的两张 `mh10-test-2mb.jpg`/`mh10-test-8mb.jpg` **已于 09-17 软删**（deleted_at 非空，对象已不在）→ 命名目标已处理。学生 A 现有 15 条活行**对象全部存在**。另发现 **4 条真·悬空活行**（deleted_at 空、storage 对象缺失），全属**另一测试学生 836845a0**（is_test）：无犯罪.jpg / 20260515105839.pdf / 高中学历.jpg / 签名.jpg。
- **(h) 中介端邀请列表**：partner1（1aad823a）`named_invites` 共 10 条（sent 6 / claimed 1 / opened 1 / expired 1 / revoked 1），最近两条均 status=sent、有 email、resend_count=0。源码：列表容器 `dlInviteList` 由 `_dlRenderList()` 渲染、数据来自 `_dlLoadInvites()`（`from('named_invites')…eq('partner_id',uid)`）；**但整块只在 `_dlOn()`（委托开关开）且页面存在 `.partner-add-student-btn` 时才被 `_dlDecorateDom` 插入**（L12896-12904），挂在邀请按钮之后、非首页常驻区块。
- **(i) 审核端放大组件**：reviewer v37 `openPreview` 图片只有**一个显示落点**（L2853 `<img id=_v37img>`），其后 L2855 已 `MHViewer.attach`；三级回落（直读/signed_url/现场渲染）都汇入这同一个 `_wmFetch().then(res)` 处理器，故**源码层三条路都挂了工具条**。school v232（L22902-22903）、admin v211（L6684）同样各只一个图片落点、均已 attach。`assets/mh-viewer-v1.js` 的 `attach` 对未加载完的 img 用 `addEventListener('load')` 兜底（L74），不惧时序。
- **(j) 巡检基线**：正常一天 `precheck-ai-shadow` = 288 次（每 5 分钟）、`wm-cache-backfill` 190~227 次、`reviewer_access_log` served 活跃日 ~100-150、`ai_precheck_shadow` 新增 2~15、net._http_response 仅近 6h（按 status 200/500/546/null 抄一次）。

## 五段结论
1. **跨域(c)**：确诊——OPTIONS 请求在 L143「非 POST 即 405」处被挡，且 `json()` 不带任何 CORS 头，浏览器预检拿不到 `access-control-allow-origin` → 正式 POST 发不出。15-01 在最前面加 OPTIONS→204 分支 + 全响应统一带 CORS 头即解。
2. **259f456a(d)**：**属探针三选一的 ①「索引没它 / 不是回填候选」**（wm_cache_index 无行、队列无行、wm-cache 桶无对象；它是 670KB jpg，非内存/页数问题）。**不是「必须停」的第三种情况**。15-06 a① 给它补一条 pending 索引行让 backfill 缓存它即可。附注：该件派单 09-17 已 revoke、且属测试学生 A。
3. **f7fdc407(e)**：是 .docx，索引已 unsupported。15-02 的「扩展名短路」（docx→直接显示「打不开·文件类型不支持」+退回、不预取不发请求）会稳妥覆盖它，与索引状态无关。
4. **403 清单(f)**：**恰 4 张表** ai_precheck_draft / application_drafts / media_shares / task_candidates，病根统一 = 缺 authenticated 的表级 SELECT GRANT（策略已在）。0205 只补 grant、不新建策略。
5. **邀请列表(h) 与放大组件(i) 缺在哪**：
   - 邀请列表**存在但非常驻**——只在委托开关开且锚点按钮在时才插入、挂在按钮后，开关一关或锚点未渲染就「找不到」。15-04 = 改常驻 + 固定位置（紧接「我推荐的学生」后）+ 0 条态 + 倒计时，复用 `_dlLoadInvites`，不造第二套。
   - 放大组件**源码层三端三条路都已挂 MHViewer**、组件对时序有兜底；业主在 高中学历.jpg 看不到工具条，最可能是该件恰是 (g) 里的**悬空行**（对象缺失→走 catch 错误分支、根本没显示图片，自然无工具条），属 15-06 territory。15-02 仍按卡把图片落点统一进 `_v38ShowImage` 作纵深加固，并新增扩展名短路（这条对 f7fdc407 是确定修复）。

## 业主要做什么
- 本包只读，**无需任何操作**。
- 知会两点供后续验收参考：①第十四轮止血已坐实（预检下载从 1.5GB/天 → 近 24h 仅 1.6MB）；②图片件水印重灌已 100%（无图片件停在旧清晰度）。
- 附带发现（非本包处置）：测试学生 836845a0 有 4 条对象缺失的活行——因 QUEUE 15-06 边界「不碰其它学生的行」，本轮不动，列入第十六轮清理候选。
