# JOB-14-04 · 放大缩小 + 水印件 2200/82（mh-viewer-v1.js + 三端 + EF + 重灌 SQL）

## 做了什么
- 新增自托管组件 `assets/mh-viewer-v1.js`（零依赖，6059 字节 ≤6KB）：`MHViewer.attach(容器, 图片, {lang})`——滚轮/双指缩放 0.5～8 倍（以鼠标/手指为中心）、按住拖动、双击在「适应窗口 ⇄ 1:1」间切换、右上角工具条 ＋ － 1:1 ⤢、快捷键 + - 0；保留禁右键/禁拖拽。文案中英俄法四语内置。
- 装到**三端的图片查看**：审核端 `openPreview` 图片分支、学校端 `openCloudDocPreview` 图片分支、管理端 `_v162AdmDocRender` 图片分支（管理端顺手去掉旧的 CSS 切换按钮，改用组件工具条）。**PDF 预览不动**（走浏览器阅读器）。
- **云函数 watermark-doc（改源码未部署）**：图片水印渲染从 1600/75（预渲染）与默认 2000/80（现场）统一升到 **2200/82**（放大不糊），成功写 `render_v=2`。
- **重灌开关 SQL**（CC 只交付不执行）：`rerender-flag.sql`（把 wm_render_v 置 2 → backfill 逐跳重渲染旧版件）、`rerender-off.sql`（置回 1）。
- 管理端 v211 顺带：失败件原因人话表加两条（`unsupported`→「文件类型不支持（Word/视频）」、`too_large`→「图片文件过大」）。

## 结果是什么（命令带出处）
- 组件：`wc -c` = 6059 字节（≤6KB）；`node --check` 通过。
- **EF 2200/82**（`grep -cF`）：`2200` 3 处、`, 82)` 2 处；括号配平 BALANCED ok；**最终 v32 md5 = `8c98e64c398e982b84ae7917244e325b`**，未部署。
- **两份重灌 SQL**：`DBRUN_GATE_ONLY=1 bash jobs/db-run.sh` → 两份都 `GATE OK (dry, 未连库)`（含 begin/commit、无 drop/truncate），**未执行**。
- **闸3**（`node jobs/JOB-14-04/gate3-viewer.mjs`）：`GATE3-PASS`——
  - 组件单测：工具条 **4 个按钮**、缩放后 transform 含 scale、放大到上限 ≤8、缩小到下限 ≥0.5、双击「适应⇄1:1」来回切换正确。
  - 三端无头加载 **0 真实报错**、`window.MHViewer` 都挂上。
- 版本四件套：文件名 admin v211 / school v232 / reviewer v37；三端 [VER] + 头注；index 引用 **学校 4 处 / 管理 3 处 / 审核 3 处**（审核 v37 在 14-01 已切）；changelog admin/school/edge-functions 各加行。

## 业主要做什么
1. 部署 watermark-doc（最终 v32，md5 8c98e64c…）后，新渲染的水印件就是 2200/82（放大清楚）。
2. 想把**已有**的旧版水印件也升清晰：部署新 EF 后，在部署卡里跑 `jobs/JOB-14-04/rerender-flag.sql`（打开重灌开关），让后台 cron 逐跳重渲染；重渲染完可跑 `rerender-off.sql` 关掉。**注意：这会重新下载原件渲染，会用流量——建议先做完 14-06 存量大图压缩、且在流量额度恢复后再开。**
3. 推送后：三端打开一张图片材料 → 右上角有 ＋ － 1:1 ⤢ 工具条，能滚轮/双指放大看清成绩单数字。验收并入验收表第 4、5 项。
