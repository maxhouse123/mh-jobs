JOB-3-06  结果：全闸通过（迁移已跑 + 管理端 v197 → v198）

## 做了什么（一句白话）
学校抽屉、中介抽屉的 hero 各加一个「邮箱已验证 / 未验证」徽章。学生的邮箱验证第三轮 JOB-3-03 已做（直读 students 表）；学校和中介的验证状态在 auth.users 表里前端读不到，所以建了个管理员专用函数 admin_email_verified 去读，打开抽屉时调一次。

## 迁移（我经 db-run.sh 跑的，跑前跑后差值）
- 新增函数 admin_email_verified(uuid) —— **函数数 0 → 1**；SECURITY DEFINER + is_admin 门 + 读 auth.users.email_confirmed_at；revoke public/anon/authenticated 后 grant authenticated。
- 自检代理：db-run 以直连超级用户跑（无 admin 登录态、走不了 is_admin 门），故直接数 auth.users 里已验证邮箱账号 = **57 个**，证明底层数据在、函数在 admin 上下文会返回这些非空值。
- 迁移 `jobs/JOB-3-06/migrate.sql`；回滚 `jobs/JOB-3-06/rollback.sql`

## 业主要看什么
学校抽屉、中介抽屉的 hero 有「邮箱已验证 / 未验证」徽章（需你登录管理员在线上打开抽屉看真值）。

## 十道闸（逐条）
- 闸1 基底 md5：`e32b190d820b677194d537290bed2918`（=v197）✓
- 闸2 锚点唯一：7 处各 =1 ✓
- 闸3 补丁自检：_v306FillEmailBadge 定义、admin_email_verified rpc、学校/中介徽章位+填充、[VER] v198 各命中 ✓
- 闸4 产物 md5：`76e88477cf5a3894f7cd812ff1cc3f2f`（v198）
- 闸5 语法：1 块 0 报错；[VER] 探针断言 admin=v198 ✓
- 闸6 消失检查：812→815（+3，无消失）✓
- 闸7 四语：管理端中英；新增文案（邮箱已验证/未验证）中英齐全 ✓
- 闸8 真点击：Playwright 桩 admin_email_verified —— 返回时间戳→「邮箱已验证」；返回 null→「邮箱未验证」。反向样本 v197：函数未定义 → FAIL（有分辨力）✓
- 闸9 视觉：复用 badge green/gray，零新颜色/类/字号 ✓
- 闸10 线上：不推主仓库，跳过

基底/产物 md5：e32b190d820b677194d537290bed2918 → 76e88477cf5a3894f7cd812ff1cc3f2f
迁移跑前跑后：admin_email_verified 函数 0→1；auth.users 已验证账号 57
index.html 改了几处：3（v197→v198）
提交哈希：主仓库本地提交，未推送
偏离或疑点：真徽章值需线上管理员登录看（psql 无 auth.uid()，is_admin 门会拒，函数在 admin 上下文才返真值）；本地已用桩验证徽章渲染逻辑。
