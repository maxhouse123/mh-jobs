# 第五轮队列 · 总回执（2026-09-15）· 收官

> 大白话总账。**主仓库全程未推**——本轮 2 个本地提交（admin v202 + 三端登录门）等你验收后自己挂代理推；日志仓库 `~/mh-jobs` 已实时推送全部回执。
> 本轮第一次动**学生端 / 中介端 / 学校端登录门**和只读探了**审核端**，全程守双盲、守红线。

## 1. 五包结果

| 包 | 结果 | 一句话 |
|---|---|---|
| JOB-5-01 | ⏸ 只探未改（缺密码）| 五端登录页**未登录态 0 个 4xx**；那个 422 是**登录成功后**的预取请求，未登录抓不到、无密码不可复现。要一个测试账号密码才能定位。**不是漏做，是缺前置条件。** |
| JOB-5-02 | ✅ 全闸过 | 管理端手机 390px 学生页搜索行横向溢出 → 该行加 `flex-wrap:wrap`（admin v202）|
| JOB-5-03 | ✅ 已在（无需新版）| 学校端「忘记密码」**早在 v225 就补全了**（链接+重置框+recovery 落地页全在，Playwright 验证可用），无需重复做，**没造无意义的 v229**（v229 归了 JOB-5-04）|
| JOB-5-04 | ✅ 全闸过 | 手机登录门：邮箱防大写 + 密码「眼睛」显隐（student v380 / partner v148 / school v229）|
| JOB-5-05 | ✅ 只探不改（队列硬规矩）| 老 docx 盘点：**5 个学生件，4 个还需审**，入口已关，审核端 415 且无下载，给了三路代价 |

## 2. JOB-5-01 定位到的那个 422（诚实交代）

**没定位到具体是哪个请求**——因为它**在登录成功、拿到会话之后**才发（总检验说的"登录时"= 登录动作完成后那一下）。我按队列规矩「没有密码就只做未登录页面的抓取」照做了，探到两条硬事实：
1. 五端登录页**未登录态一个 4xx 都没有**（含 0 个 422）。
2. 登录页在登录**之前根本不向 Supabase 发任何请求**（只加载本地 vendor/supabase.js）。

**要你给一样东西**：任一端的测试账号密码（stuA / schx / reviewer1 / 管理员任选其一）。拿到我就真登一次抓到那个 422 → 看是哪个 RPC/REST、传了什么、服务端拒的理由 → 改传参或删掉没用的预取 → 再登断言 0 个 4xx。

## 3. JOB-5-04 查出的根因（铁律19：先读源码）

7-28 移动巡检卡的"邮箱变大写登不进"，三样都查了：
- **不是 CSS**：登录门 `text-transform:uppercase` **0 处**（只在与登录无关的邀请码框有）。
- **不是 JS**：全文 `toUpperCase` 只出现在邀请码/证件识别，**没一处动邮箱或密码的值**。
- **真凶=手机键盘首字母自动大写**：输入框缺 `autocapitalize="none"`，安卓键盘默认大写首字母，打出来 `Maxhouseapp@…` 跟注册的小写对不上 → 登不进。

各端情况不同，分别处理：学生端邮箱前版已修（这次补 `inputmode=email`+密码属性+眼睛）；**中介端邮箱完全没修过（这次才补上，真有大写病）**；学校端本无大写病（只补 autocorrect/inputmode+眼睛）。密码"眼睛"三端原来都没有，这次新做（纯 emoji 👁/🙈，无新颜色/类/文案，不动翻译包）。闸8（Pixel UA+390 宽）三端全过。

## 4. JOB-5-05 的数字

- `documents` 里 **.docx 共 5 个**（0 个 .doc、0 软删），5 个不同学生，上传 2026-07-07~07-24（老件）。
- 审核状态：**无裁决 4 / 已退 1 / 已通过 0** → **真正还需审的 = 4 个**。
- `school_documents` 3 + `partner_documents` 1（学校/中介自己的证明件，走管理端不走审核端，与 415 无关）。
- **入口已关**：学生端 v380、中介端学生材料槽都不收 Word 了 → 不会再有新 Word 进审核端。
- 审核端 v31：Word → 水印函数 415「不支持预览」，弹窗**无下载按钮**（下载是为水印双盲特意去掉的）→ 看不了也下不了。
- 三路代价：①转 PDF 回填（需改 4 行真实数据→需你授权）②给 doc 加下载（破双盲，不建议）③收紧白名单（不进审核端的口，不解决问题）。**建议**：让这 4 个学生重传 PDF（口已只收 PDF），最省事不碰红线。

## 5. 五端最终版本 + md5

| 端 | 版本 | md5 | 本轮 |
|---|---|---|---|
| **管理端** | **v202** | `bc8d2a4d7a2886351f750268e7021107` | JOB-5-02 |
| **学生端** | **v380** | `8dc87aff4826888c0609279962521041` | JOB-5-04 |
| **中介端** | **v148** | `5cbbc6d9d23a86a7c9c4ca8887a3deee` | JOB-5-04 |
| **学校端** | **v229** | `4126050798a6c4f607f7ef3f4a906164` | JOB-5-04 |
| 审核端 | v31 | `78998c83d4616ff28668aa4b91018a99` | 仅只读探(JOB-5-05) |

链式基底（本轮起点→终点）：admin `f22e05(v201)→bc8d2a(v202)`；student `fcc25c(v379)→8dc87a(v380)`；partner `d311af(v147)→5cbbc6(v148)`；school `745c91(v228)→412605(v229)`。四端 index.html 引用均已指向新版（学生 4 处、其余各 4 处，实测无残留旧号）。

## 6. 本地待推提交（2 个，主仓库未推）

- `ccf9e97` admin v202：JOB-5-02 修 390px 学生页搜索行横向溢出
- `c222be8` JOB-5-04：手机登录门 student v380 / partner v148 / school v229

（JOB-5-01/03/05 无代码改动：01 缺密码只探、03 早已实现、05 队列规定只探。）

## 7. 迁移

**本轮零迁移**。JOB-5-05 只跑了两个只读 SELECT 探针（走 db-run.sh），没建表、没改数据、没碰 .env。

---
**下一步在你**：
1. 本地开 admin v202 / student v380 / partner v148 / school v229 验收（手机登录门重点看邮箱不大写、密码眼睛能点、用 stuA 真登一次）。
2. 满意就挂代理把本轮 2 个本地提交推上主仓库。
3. **给我一个测试账号密码**，我好把 JOB-5-01 的 422 收尾。
**主仓库本轮全部未推。**
