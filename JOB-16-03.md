# JOB-16-03 回执 · 迁移 0207(软删/撤销过滤) + 0208(瘦身状态入库)

两条迁移已应用到线上库（走 `bash jobs/db-run.sh`），正本 `supabase/migrations/` + 镜像 `docs/supabase 文件/` 各一份，`ledger-check` 差集 0。不删行、不改策略、不改闸规则；非 is_test 学生只写 slim_* 五列。

## 一、做了什么（大白话）
- **0207**：把「读材料给用户看」的地方，统统加上「已删除的别显示、已撤销的别进队列」这道过滤。审核队列另加「撤销的派单不算数」。手法是「原样重建、只在筛选条件里加一句」，不动任何列、不动函数签名，跟线上现役五端并存不报错。
- **0208**：给材料表加 5 个「瘦身状态」小栏（状态/原大小/新大小/页数/时间），再加一个只许本人（或其中介/管理员）在传完 15 分钟内写一次的存档函数 `set_doc_slim`；学生端读材料时把这 5 栏一起带出来。这样瘦身结果就能落库、刷新不丢（前端接线在 16-05）。
- **回填**：把上一轮（13-05）瘦身日志里「确实采用了瘦身版」的 15 件，按 doc_id 逐件补上 slim_status='slimmed'+原/新大小；日志里「没采用(KEEP)」的 8 件因为看不出到底是哪种「保留原因」，**一律不猜、不写**。

## 二、结果是什么（带身份验证，命令出处 = db-run 输出）
**0207**
- reviewer1 身份查 `reviewer_doc_queue`：`auth.uid()`=80c62372… 回显；行数 **61 → 60**，`has_259 = f`（**259f456a 撤销件不再在队列**）。
- 学生A 身份调 `student_bootstrap`：`auth.uid()`=43668ae4… 回显；documents 数组 **17 → 15**，`mh10_in_bootstrap = 0`（**两张 mh10-test 死图不再返回**）；活件数 15 = 默认身份数出来的活行数 15（只少了 2 张软删，键集合不变）。
- 现役五端本地各开：student / partner / reviewer 真登 **0 新增页面报错**（这三端吃 student_bootstrap / get_my_doc_statuses / reviewer_doc_queue）；admin(无密码)/school(schX 深层) 的对应函数同源同改，结构一致（见备注）。

**0208**
- 5 列已加（slim_status/slim_orig_kb/slim_new_kb/slim_pages/slim_at）。
- `set_doc_slim(p_doc_id **text**, p_status text, p_orig_kb int, p_new_kb int, p_pages int)`：`has_function_privilege` → authenticated 可执行 **t**、anon **f**。
- 负路验证（带身份）：`bad_status` → 拒；学生A 对自己旧件（>15min）→ `too_late`；**reviewer1 对学生A 真件 → `not_yours`**；student_bootstrap documents 现含 `slim_status`/`slim_at` 键。
- 回填：**slimmed 15 件**（ADOPT），KEEP 8 件跳过。

## 三、施工中抓到并修掉的一个真 BUG（要紧）
`set_doc_slim` 归属判定原写 `if not (auth.uid()=owner or auth.uid()=partner or is_admin(...))`。学生A 的 `referred_by_partner` 是 **NULL**，于是 `partner` 分支是 `auth.uid()=NULL` → NULL；`(false OR NULL OR false)` = NULL；`not NULL` = NULL；`if NULL then` **不进分支** → 非本人（reviewer1）竟被放行、走到了 too_late。这是三值逻辑漏洞、等于归属校验被绕过。**已修**：整体 `coalesce(..., false)` 收成假；再验 reviewer1 → 正确报 `not_yours`。（正本 0208 已带此修，重跑 CREATE OR REPLACE 覆盖。）

## 四、与卡假设不符、按库为准的两处（血缘三问·铁律14）
- **doc_id 类型**：卡写 `set_doc_slim(p_doc_id uuid…)`，但 `documents.doc_id` 实为 **TEXT**（information_schema 证）→ 参数用 text。
- **无 uploaded_by 列**：documents 无「上传人」列 → 「上传者本人」判定＝学生本人(student_id) 或 其推荐中介(students.referred_by_partner) 或管理员。

## 五、只补了「主 documents 读」、没动「最新件判定」的说明
为满足「只少已删行、不加行、键集合不变」，0207 只给每个读者的**主 documents 读**加 `deleted_at is null`；带 `d2 更新时间` 的「最新件」子查询**不动**（动它会改变哪一件算最新、可能反而多出行）。这样软删件从输出消失，其余口径一字不变。涉及的 11 个对象：`reviewer_doc_queue`(view,+revoked_at+deleted_at) / `student_bootstrap` / `get_candidate_documents` / `get_student_documents_school` / `get_partner_student_doc_statuses` / `get_my_doc_statuses` / `get_my_material_flags` / `ai_precheck_for_docs` / `student_docs_complete` / `student_docs_pushed` / `student_qualified`。`wm_cache_backfill` 本已自带过滤、未动；管理端审计/回收类未过滤。

## 六、新闸证据
- **N3**：0207/0208 全部验证带 `auth.uid()` 回显（reviewer1/stuA/reviewer1-negative）；无身份「0 行」未用作证据（反而是它暴露了 RLS，改用 GUC 传真 doc_id 才验出 not_yours）。
- **N6**：只加不删；0209 按裁决**留空不建**；未碰闸规则/策略/瘦身参数。
- **N5**：回执零串；secret-scan 待推前跑。
- 迁移先于前端：只加列/加条件，列序类型签名不变，现役五端并存 0 新增报错。

## 七、业主要做什么
- 无需手贴 SQL（已 db-run 应用）。业主端刷新后：学生端 CV 槽应不再有两张 mh10-test 死图；审核端队列不再有 259f456a。瘦身行的前端显示在 16-05 接线后可见。
- **备注**：0208 的 `set_doc_slim` 成功写入路（真传件<15min→写入成功、再写被拒 already_set）留到 16-05 用真测试件走通（那时才有<15min 的新件）；本包已验负路(too_late/not_yours/bad_status/anon 不可执行)。

## 八、闸3
- 0207/0208 验证查询全过并抄录；现役前端本地开 0 新增报错；`ledger-check` 差集 0；回填 15 件、8 件跳过并写明。**过**。
