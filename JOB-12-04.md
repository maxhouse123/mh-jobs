# JOB-12-04 · 清桶重灌函数（**只建不跑**）

结果：**已创建 + 权限闸通过；绝未调用**（迁移 0197）

## 大白话（业主看这段）
造了一个"清桶重灌"函数 `wm_cache_purge`：等你部署了 12-02 的瘦身版 EF 之后，用它把桶里的**旧大件**逐个清掉（每次最多 50 个），清掉后每 5 分钟的定时任务会按**新尺寸（1600/75）自动重灌**成小件。
- 它只发"清理"指令（由 EF 用正常方式删文件），**不直接删数据库里的对象行**（那样会留孤儿文件）。
- 暗号**不进任何文件**（运行时从现役触发器里现读）。
- 只有后台/管理员能调，普通登录用户不能。
- **本轮只创建、没有运行它**（必须停）——清桶必须等你部署完新 EF 之后，由我给你一句话触发；否则现在跑，旧函数会按旧参数、不写索引地重灌一遍，白折腾。

## 断言表
| 断言 | 期望 | 实测 | 过 |
|---|---|---|---|
| 函数 wm_cache_purge 存在 | 1 | 1 | ✅ |
| anon 可执行 | 否 | f | ✅ |
| authenticated 可执行 | 否 | f | ✅ |
| public 可执行 | 否 | f | ✅ |
| **本轮是否调用** | 否 | **未调用**（桶对象数不因本包变） | ✅ |

## 触发说明（业主部署新 EF 后）
业主部署 watermark-doc（12-02）后，叫我一句话，我会：`select public.wm_cache_purge(50);` 反复跑到桶空，再看 0195 的 cron 按新参数重灌（桶数先降到 0、再涨回来、但每个都变小）。**在你部署前我绝不跑它**（否则按旧参数重灌）。

## 归档 / rollback
- `supabase/migrations/0197_wm_cache_purge.sql`（secret-free：暗号运行时读 catalog）
- `supabase/migrations/0197_wm_cache_purge_rollback.sql`（drop function；手动跑）

## 边界
- 不碰 0195 的回填函数与 cron；不动 EF、不动三个 fanout 函数；只建函数，绝不调用。

## 动过的文件
- DB：新增函数 wm_cache_purge（新增型）
- 归档 supabase/migrations/0197_*；jobs/JOB-12-04/*
