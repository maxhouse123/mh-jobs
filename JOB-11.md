# JOB-11 · 管理端补管理能力 —— 设计（必须停）
**结果：只做设计、不动代码。完整设计见 `jobs/JOB-11/design.md`。**
六件管理端做不到的事（删错件/替换通知书/撤销派单/解锁材料槽/停用学校/改学校档案）各自的服务端函数、动哪张表、审计、回滚，已在 design.md 逐条列出（全走 SECURITY DEFINER RPC + admin_events 审计 + is_admin 门禁 + 软删优先）。
## 等你拍板三个问题（答完我再开施工卡）
1. 删材料**软删**（标记消失、文件留、可复原）还是**硬删**（彻底抹）？（建议软删）
2. 撤销派单后材料**回可派池**还是**标已撤销终态**？
3. 停用学校后已发 offer **冻结**还是**照常走完**？
