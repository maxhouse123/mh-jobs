# JOB-17-03 · C：中介端 v164 —— Copy link 点了有看得见的反应

## 一、做了什么
病根（QUEUE-17 §先读这一段 C / 业主 09-21）：中介端 Sent invites 与邀请生成框的「Copy link / 复制」点了「像没反应」。
真页面量出的病根（本包 gate3 v163 真登复现，见下）：
- 旧 `_dlCopy`（v163 L13337）成功路只把**按钮字**改「已复制」，无提示条；而中介端列表挂了 body 级 MutationObserver（L13395 `host=document.body`, subtree），改按钮字这一下 DOM 变动**立刻触发清单重画**，按钮字被复原成「复制链接」——所以 1.5s 时屏幕上什么都看不到。
- 旧码用 `try{ navigator.clipboard.writeText(text).then(...) }catch`，**只有同步 catch、没有 `.then` 的拒绝分支**。剪贴板权限被拒时 `writeText` 返回**已拒绝的 Promise**，同步 try 抓不到，`.then` 成功回调不触发 → 一条未捕获拒绝 + 屏幕零反应。

改法（只改三处，纯前端，零 DB/云函数）：
1. **`_dlCopy(text, btn)` 重写**（partner v164）：
   - 成功 → 本端 `toast('链接已复制','success')`（fixed 独立元素，挂 document.body，**不随清单重画消失**，自寿 2.6s）+ 按钮字改「已复制」；改字时把 `_dlState.muting` 置真并**保持到提示条同寿命（2600ms）后自然复原**，挡住 body 观察器被 toast 增删自触的重画（否则按钮字瞬间被复原）。
   - `writeText` 被拒（`.then` 的第二个回调）**或**同步抛错 → `textarea + document.execCommand('copy')` 备用；备用成功同样弹提示条。
   - 备用也失败 → `_dlCopyManual(text)`：复用本端 **`showConfirm`**（新增 `hideCancel` 单按钮选项）显示**可选中**的链接（`user-select:all`）+「没能自动复制，请选中下面的链接手动复制」；showConfirm 缺席才退回 `window.prompt`。
2. **`showConfirm` 补 `hideCancel` 可选项**（L3446 一行）：`cancelBtn.style.display = opts.hideCancel ? 'none' : ''`。默认空串 = 照旧显示，**不改任何现有调用**。
3. **新文案四语**：`dl.copiedToast`（链接已复制 / Link copied / Ссылка скопирована / Lien copié）、`dl.copyManual`（没能自动复制，请选中下面的链接手动复制 / Couldn't copy automatically — please select and copy the link below / …ru / …fr），en/zh 落 `REP_I18N` 二次 assign、ru/fr 落 `_DL_X`；另把已有的 `dl.copied`（已复制/Copied）补进 `_DL_X` 的 ru/fr（原缺，切俄/法时按钮字会露键名）。
- 列表 Copy link（`data-dl-copy` → L13110）与邀请生成框「复制」（`#dlCopy` → L13335）**同走** `_dlCopy`，一改两处齐。
- 四件套：文件名 v164 / [VER] 探针 v163→v164（并留 `[VER-prev163]`）/ index.html 重定向 4 处 / docs/changelog 追一行。旧 v163 一字未动。

## 二、结果是什么（真页面先红后绿，partner1 真登，chromium，`jobs/JOB-17-03/gate/`）
gate 脚本 `copylink-gate.mjs`：本地静态服务真页面、partner1 **真表单登录**（scaffold `login()`，无会话注入）、**真点** `locator.click()`（非 evaluate/dispatch）；判据只认屏幕可见文字/网络计数。全量 JSON：`jobs/JOB-17-03/gate/out/{v163,v164}.json`。

| 场景 | 旧 v163 | 新 v164 |
|---|---|---|
| ① 给权限点 Copy link，1.5s | **RED**：提示条 (none)；按钮 1.5s 已被重画复原「复制链接」 | **GREEN**：提示条「链接已复制」+ 按钮「已复制」；剪贴板 = 该邀请链接(151 字, clipHasLink=true) |
| ② 拒绝剪贴板(writeText→NotAllowedError) | **RED**：屏幕零反应(visibleResult=false) + 1 条未捕获拒绝(=病本身) | **GREEN**：走 execCommand 备用成功 → 同样弹提示条 |
| ②b 备用也失败(execCommand→false) | —（旧无此路） | **GREEN**：showConfirm 单按钮弹窗(cancelHidden=true, confirmLabel「关闭"), 含可选中链接(hasLink=true) |
| ③ 点后清单条数与按钮 | — | 行 10→10、Copy 键 7→7、Revoke 键 7→7 **stable**（过期/已撤销行本就无动作按钮，故按**计数**比而非每行） |
| ④ 四语提示条 | — | zh 链接已复制 / en Link copied / ru Ссылка скопирована / fr Lien copié **逐语命中**(allOk=true) |
| 页面报错 | grant 0 / deny **1**(未捕获拒绝) | grant 0 / deny 0 / manual 0 |

- 本机截图 5 张：`~/mh-verify/shots/JOB-16/17-03/`（脚本 shot() 目录名固定 16）。公开目录只放 `~/mh-jobs/shots/17/17-03/INDEX.md`，**不公开图片**（列表/弹窗含真实测试邀请 token，按 §0.6 记「本机有图」）。

## 三、业主要做什么
- 本包**先提交未推**（主仓库本地 commit，未 push；推送由收官后卡1135 做）。
- **要业主亲眼看的**（进 SUMMARY 清单）：中介端登录 → Sent invites → 点任一「Copy link」：屏幕中上方弹「链接已复制」提示条、按钮短暂变「已复制」，粘贴出来是该邀请链接；把浏览器剪贴板权限设为「阻止」再点，仍有可见反应（提示条或一个只显链接的小窗）；点完后清单和每条的重发/复制/撤销按钮都还在；切 EN/РУ/FR 提示条跟着变语言。

## 四、新闸证据（N1–N6）
- **N1**：静态差集 v163→v164 `removed=0`（ids/handlers/functions），added 仅 `_dlCopyManual/_ok/_fallback`（`jobs/JOB-17-03/inv/`）；**动态**（partner1 真登落地页可见交互件）v163→v164 `68→68, removed 0 / added 0`（`dyn-n1.mjs`）。③ 另证列表按钮种类与计数不变。
- **N2**：旧 v163 真页面先红（① 无提示条、② 零反应+1 未捕获拒绝），新 v164 后绿；均真登真点，非片段非零件。红的样子与业主说的「像没反应」一致。
- **N3**：本包纯前端、无按登录人过滤的库查询；partner1 身份由 scaffold 真表单登录取得（读 accounts.json，密码不落任何输出）。
- **N4**：`_dlCopy` 三条分支（成功 / writeText 拒绝→execCommand 备用 / 备用也失败→手动弹窗）逐条真页面验（ALL-GREEN，见 ②/②b）。
- **N5**：`secret-scan` 对 `~/mh-jobs`（含本回执与 shots/17）与主仓库改动文件 → `SECRET-SCAN-CLEAN`（脚本只记 clipLen/布尔，从不落原始链接/token）。
- **N6**：只按图纸改 `_dlCopy`/`showConfirm`/i18n，未加新活；`showConfirm` 的 `hideCancel` 是单按钮弹窗所必需且默认不改现有行为。

## 五、动过的槽 / 测试写库
- **本包零材料上传、零测试派单、零 §0.11 批次占用、零 DB 写**：只在本地服务的真页面上点 Copy link（浏览器内动作），未调任何写库 RPC；剪贴板拒绝用 initScript 注入 `writeText→reject`（等价真权限拒绝，触发 `.catch` 备用路），execCommand 失败用 `document.execCommand=()=>false` 注入。无残留测试件，无需收尾软删。

下一包 = 17-04
