# JOB-10-06 · 中介端上传失败给「重试」（partner v155 → v156）

结果：**全闸通过（已 commit，未推送主仓库）**

## 大白话（业主看这段）
中介端（合作伙伴端）以前上传材料失败时，那张红色的失败卡上**没有「重试」按钮**——学生端有，中介端漏了。学生只能把整个流程重来。这轮给中介端补上了：现在上传失败，红卡上会出现一个「重试」按钮，点一下，**还是那张卡**回到「上传中 0%」重新传，传成功就变 100%，不会多冒出一张新卡。

一共三条上传线都补齐了：
1. **推荐向导里边填边传的暂存**（_wzStageFiles）
2. **代学生提交材料**（_matCommit）
3. **代学生换版/重传**（_s5CommitPending）

做法是照搬学生端第九轮 9-01 已经验证过的「线程化重试」：失败时把「重传这一份的整套动作」打包成一个回调交给卡片，点重试就再跑一遍这套动作、复用同一张卡。**上传的回落机制（签名URL→XHR→普通upload 三层回落）和大文件断点续传（TUS）层一个字节都没动**；提交按钮本来就是真 disabled，所以没加别的禁用态。

其实那张卡的模块（_v384）**本来就支持**传重试回调，只是这三处当初写成了 `.fail(null)`（不给回调=不出按钮）。这轮就是把这三处接上线。

## 业主要看什么
- 打开中介端 → 推荐一个学生 / 进某学生工作台 → 传材料时若遇网络失败，红卡上应出现「重试」按钮；点它，同一张卡重新跑进度，成功变 100%。
- 四种语言下按钮文案：中「重试」/ 英 Retry / 俄 Повторить / 法 Réessayer（模块自带，未新增词条）。

## 十道闸
| 闸 | 卡什么 | 结果 |
|---|---|---|
| 1 | 基底 md5 = v155 | ✅ 5156d336fe13c5c70575d9f72b80e231 |
| 2 | 锚点唯一 | ✅ 三处锚点各唯一命中（Edit 精确匹配，改前 .fail(null)=3） |
| 3 | 改后关键串核对 | ✅ .fail(null)=0；.fail(_wzCommit)/(_s5cRetry)/(_matRetry) 各就位；三闭包各定义 1 次 |
| 4 | 产物 md5 | ✅ a82f67fb371ac60ae3bddcf1675b950a |
| 5 | 语法全量 | ✅ 1 脚本块 0 报错 |
| 6 | 功能消失=0 | ✅ v155/v156 各 778 特征，消失 0 |
| 7 | 四语逐键 | ✅ 未新增用户文案；retry 标签 en/zh/ru/fr 齐（_v384._i18n.retry，复用） |
| 8 | 真点击（jsdom 驱真 _v384） | ✅ `tests/job10-06-partner.mjs` 28/28：三条线各「失败→红卡+重试按钮→点重试→同一张卡(卡数=1)→100%+完成」 |
| 9 | 视觉一致 | ✅ .v384-retry 类定义 v155/v156 各 1（复用非新增）；hex 颜色分布逐项一致=0 新增；无新字号档 |
| 10 | 线上版本 | ⏭ 本轮不推送主仓库 → 跳过（收官由业主推 v156） |

**反向样本**：同判据在 v155 上跑 —— .fail(null)=3、三闭包全无 → 全部不成立（闸有分辨力，已实测 PASS）。

## 三层自验（铁律11）
- ① UI 对照：纯逻辑接线，无新增 UI 字段；重试按钮+卡片是 _v384 既有组件（复用，遵铁律10 不自造）；无裸 i18n 键；重试按钮 onclick 由模块内定义（真回调，非死按钮）。
- ② 无头走查：jsdom 载入 v156 **真实 _v384 模块**（花括号配平抽取，非重写），用「失败一次再成功」的 upload 桩，按三条线的重试闭包形态各驱一遍，DOM 断言全过。红线：只读本地文件、window.sb/upload 全为受控桩、不碰生产不连库。
- ③ 自验报告：本文件 + tests/job10-06-partner.mjs（入仓）。

## 边界遵守
- 只动 `_v384` 三处失败卡调用 + 各自新增的重试闭包；**回落红线（createSignedUploadUrl+XHR→.upload()）一字未动**（闸8 静态断言在）；**TUS `upload/resumable` 层未动**；未加 aria-disabled。
- 版本 bump 四件套齐：①文件名 v156 ②/③[VER] 探针 v156（partner 头注自 v108 起并入 [VER] 行）④index.html 重定向 4 处 v155→v156（v155 残留 0）。

## 已知局限（诚实记录，与学生端 9-01 同）
- 三条线的「失败计数/汇总提示 toast」在**首轮**即已弹出；若用户**稍后**点重试成功，那条汇总数字不会回改（但重试闭包会 insert 建档 + 刷新/重拉，补传件会真实出现）。这是与学生端 9-01 一致的既有取舍，非本轮新增缺陷。

## 动过的文件（git add 点名）
- 新增 partner-portal/maxhouse-partner-portal-v156.html
- 改 partner-portal/index.html（4 处 v155→v156）
- 新增 tests/job10-06-partner.mjs（闸8，入仓）
- 新增 jobs/JOB-10-06/report.md

提交哈希：见 commit（未推送主仓库，收官时列入待推清单）。
