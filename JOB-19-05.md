# JOB-19-05 回执 · 历史存储孤儿：只查证 · 出清单（不删）

## 一句话
按 JOB-19-00 探针⑥的查证结果，把唯一一个存储孤儿的**完整路径 / 大小 / 上传时间 / 判定依据**出成清单（`jobs/JOB-19-05/orphans.md`），并给出「怎么确认删对了」的一条**只读** SQL（`jobs/JOB-19-05/confirm-deletion.sql`）。**CC 不删、不动任何存储**；删除由业主手动执行。本包**零代码改、零 DB 写、零部署**，仅出文档。主仓库 commit 一笔待收官统一推。

## 孤儿（共 1 个）
| 桶 | 完整路径 | 大小 | 上传时间(UTC) | documents(含软删) | 判定 |
|-----|----------|------|---------------|-------------------|------|
| `student-documents` | `43668ae4-96b6-4518-9725-0b1ade089e92/recommendation/1789916981387_j0sl_r16-online-synth.png` | 220489 字节(约 215 KB) | 2026-09-20 15:09:43 | 0 行(live 0 / 含软删 0) | 孤儿 |

**为什么是孤儿**：文件真实躺在 `storage.objects`，但 `public.documents`（含软删 `deleted_at is not null`）里没有任何一行 `file_path` 等于此路径——无业务记录引用，属纯存储残留（第十六轮 online-walk 合成推荐信件上传后未走正常入库/清理链；SUMMARY-18 建议包已点名）。删它不影响任何真实档案或软删可恢复数据。

## 查证口径（承 19-00 探针⑥）
- 探针文件 `jobs/JOB-19-00/probes/probe6-orphans.sql`（只读，`begin…rollback`）已在 19-00 执行并确证：stuA 前缀下含 `online-synth`/`MH17`/`MH18`/`synthetic` 关键词的对象**只有这 1 个**，且 documents(含软删) 全等匹配 0 行。
- 本轮孤儿治理**只针对测试生成的合成件**（上述关键词范围），**不碰任何真实用户文件**——范围刻意收窄，杜绝误删真档。
- 本包**未再连库**（避免无谓 DB 触碰、不碰 `.env`）：19-00 同轮已查证，19-05 按其结果出清单，符合本包「只查证、出清单」的定位。

## 怎么确认删对了（只读，业主删后运行）
`jobs/JOB-19-05/confirm-deletion.sql`：
```sql
select count(*) as remaining_rows
from storage.objects
where bucket_id = 'student-documents'
  and name = '43668ae4-96b6-4518-9725-0b1ade089e92/recommendation/1789916981387_j0sl_r16-online-synth.png';
-- 期望 remaining_rows = 0
```
手删两法（业主任选）：①控制台 Storage → 桶 `student-documents` → `…/recommendation/` → 勾该文件 Delete；②`select storage.delete_object('student-documents','43668ae4-…/recommendation/1789916981387_j0sl_r16-online-synth.png');`。

## 业主要做什么
- 手删上表那 1 个孤儿（二法任选），删后跑 `confirm-deletion.sql` 见 `remaining_rows=0`。
- SUMMARY-19 会把此路径单列进「要业主手删的存储路径」。

## 新闸证据 N1–N7
- **N1**：本包无代码改（纯文档），不适用按钮/id/函数差集。
- **N2**：孤儿本身即 19-00 已坐实的旧残留；本包只登记不施工。
- **N3**：判定证据来自 19-00 超级用户 ground truth（`storage.objects` × `documents` 全等比对，含软删）；无身份 0 行未用作证据。
- **N4**：不适用（无云函数/分支）。
- **N5**：本包无截图；确认 SQL 与清单不含 token/key/PII（仅一条存储路径 + 合成件名）。
- **N6**：只新增 `jobs/JOB-19-05/{orphans.md,confirm-deletion.sql}` 与本回执；任何端代码、查看器、云函数、存储对象**一行/一字节未动**。
- **N7**：本包无「给人看的提示件」，N7 不适用。

## 测试写库收尾
本包**零 DB 写、零上传、零登录**；无测试件需清理。

## 给 19-06 的交接
- 孤儿清单已就绪（1 个，见上）；SUMMARY-19「要业主手删的存储路径」照抄该路径。
- 19-06 = 收官：`jobs/JOB-19-06/online-walk.sh`+`.mjs`（从 JOB-18-06 复制后改，旧文件不动）新增 ①schX 线上中介端登录被拒/页面无机构内容/会话空（partner v167 未上线时红属预期，回执写明）②中介端 partner1 若有现成 Q1 提醒场景则量卡片样式（有才判）；开头轮询五端 `[VER]` 最多 10 分钟；只在本地对现役线上跑一遍如实记录；调试期不推送，最后一次干净后推。写 SUMMARY-19（§0.8：三端新版本+md5 / 迁移清单 / watermark-doc v34 待部署 / 每包一句话 / 红→绿截图名 / 未放行清单 / 要业主亲眼看的 / 要业主手删的存储路径 / 第二十轮建议包 / 顶格单行 `PUSH_OK=`）。
- **迁移**：19-05 无迁移。下一空号仍 0213。
- 下一包 = 19-06
