# JOB-9-03 · TUS 大文件真断点续传验证（手机·只验不改）— 回执【未完成·跳过】

**一句话结论**：手机线一开始是通的，也确认了几件好事（手机连上、远程控制台通、stuA 已登录、断点续传库能加载、8MB 上传确实走了 `/upload/resumable` 断点续传通道）。但**这一包最终没能跑完**：先是发现「一上来就按 300KB/s 限速」时，第一片 6MB 的创建请求在 2 分钟内都没传完、到不了第二片（PATCH），没法在正确时机断网；随后我第一次跑的脚本在「断网状态下截图」那步卡死崩了，**把手机 Chrome 的网络留在了「模拟断网」的卡死状态**，之后我几种办法都只能部分恢复，越弄手机 Chrome 越不稳（网页的 Service Worker 被拖坏、开了太多标签页、最后远程控制台也连不上了）。按队列规矩「手机线不通 → 该包写回执跳过，不影响其他包」，**9-03 跳过**，后面 9-04～9-08 不受影响照常做。

**需要业主做一件事**：把手机上的 Chrome**彻底关掉重开**（从最近任务里划掉，或设置里强制停止），让它清掉那个卡死的「模拟断网」状态、并让网页重新注册一个干净的 Service Worker。重开后 stuA 的应用就恢复正常了。之后 9-03 可以重跑，或业主自己手动传一个 >6MB 文件、传到一半关一下 WiFi 再开，肉眼看它是否「接着传」而不是「从头传」。

## 自检（8-00 那套）逐项
| 项 | 结果 |
|---|---|
| adb devices / connect | ✅ 192.168.1.50:5555 device |
| 屏幕唤醒(WAKEUP) | ✅ 能唤醒(但会自动再睡, 用 WAKEUP 心跳顶着) |
| forward + connectOverCDP | ✅ 初期通(后期 Chrome 变卡, 超时) |
| stuA 登录态 | ✅ maxhouseapp+stua@gmail.com (uid 43668ae4…) |
| TUS 库可用 | ✅ `tusReady=true`, `tus.Upload` 在 |
| 单文件上限常量 | `_sizeCapMB=10`(MB) → 8MB 未超上限, 按卡用 8MB |
| 8MB 走断点续传通道 | ✅ 抓到 `POST https://…supabase.co/storage/v1/upload/resumable` |

## 三条断言（卡要求）—— 均**未取到实测值**
| 断言 | 实测 | 状态 |
|---|---|---|
| ① 有 `/upload/resumable` 请求 | 抓到 POST(诊断跑中出现) | 🟡 部分(通道确认走 resumable, 但完整序列未取全) |
| ② 断网后 HEAD 且 Upload-Offset≥6291456, 随后 PATCH 且 Upload-Offset≥6291456 | **未取到** | ❌ 未完成 |
| ③ 最终完成 + DB 出现该文件 + 无红卡 | **未取到**(测试期上传均失败/离线) | ❌ 未完成 |

## 具体卡在哪（两个真实障碍）
1. **300KB/s 全程限速下, 6MB 创建请求 2 分钟内不提交**：TUS 第一片(6MB)走「创建即带数据」的 POST；在 300KB/s 下这个 6MB POST 反复重试、始终没提交成功，到不了第二片(PATCH)。所以「监听第一个 PATCH 再断网」的时机根本没出现。这本身是一条值得记的观察——真断点续传只对「已提交的 6MB 之后」的部分生效，而重限速会卡在第一片提交。(改进思路：先常速让 6MB 创建 POST 提交、拿到 offset=6291456, 再对随后的 2MB PATCH 限速并断网——留下轮真机复验用。)
2. **首个完整脚本崩溃 → 手机 Chrome 网络卡在「模拟断网」**：第一次跑的脚本在「断网状态截图」处触发 Playwright 30s 字体等待超时崩溃，崩溃点在「恢复网络」之前，于是 CDP 的 `offline:true` 没被还原。之后逐 target 清 offline、撤/重挂 forward、注销损坏 SW 等只能部分恢复(一度 raw fetch 到 supabase 拿到 401=通), 但网页 Service Worker 已被拖坏(拦截并失败所有请求), 注销 SW 后 reload 又报 DNS 解析失败, 手机 Chrome 越来越不稳, 最后连 connectOverCDP 都超时。**我不能强制重启手机 Chrome**(队列只准 devices/connect/forward/screencap/WAKEUP/am start, 不含 force-stop), 故只能留给业主重启。

## 数据红线 / 清理
- **只碰 stuA 的 bank 槽**, 四类闸门槽(passport/education/transcript/photo)一字节没碰。
- 测试文件命名一律 `mh9-*`(mh9-test-8mb / mh9-diag / mh9-base / mh9-raw)。**这些上传全部失败(离线)**——`handleUploadCloud` 只有 storage 上传成功后才写 documents 行, 上传失败即不写、且失败时会删掉已传的 storage 对象, **故几乎不可能产生孤儿文件**；期间能连库的几次清理复核 bank 活文件命中 `mh9-*` = 0。**但因收尾时网络已不稳, 未能做最终确认。**
- **请业主重启手机 Chrome 后, 顺手看一眼 stuA 的 bank 槽是否就是原来那 2 个文件**(应是)。若见到任何 `mh9-` 开头的文件, 删掉即可(那是本次测试残留, 非真实材料)。
- 网络条件: 已尽力还原(多次 offline:false + 撤/重挂 forward), 但以 Chrome 重启为准。
- 截图: 因离线态截图超时崩溃, **未产出可信的 9-03-1/9-03-2 截图**。
- 未改任何代码/任何端 HTML（本包本就是「只验不改」）。

## 对其他包的影响
无。9-04～9-08 都是本机改 EF 源码 / 加 eruda 等, 不依赖手机, 照常进行。
