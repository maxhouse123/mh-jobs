# JOB-6-06 回执 · 学生中介 90 天冷却（2026-09-16）

> 大白话版。做完了：被中介委托过的学生，在委托关系结束后的 90 天内，不能申请成为「学生中介」（防中介借学生号刷配额）。这道闸装在数据库里（硬拦），学生真去申请时，前端会用四语弹一句「委托关系结束后 90 天内暂不能申请，还剩 N 天」。**主仓库已提交没推。**

## 为什么
二期清单第七项：被委托的学生在绑定结束后 90 天内不得申请成为学生中介，防中介借学生号刷配额。

## 数据库（迁移 0190，已实跑成功）
- 学生申请学生中介走的 RPC = `submit_student_agent_application`（有 3 参 + 9 参两个重载，前端用 9 参；**两个都加了**冷却检查防绕过）。
- 在函数开头加一段：查 `student_delegates` 里 `student_id = 本人` 的最近一条 `revoked_at`（委托关系结束时间），**90 天内 → 抛 `cooling_period:剩余天数`**。
- 其余一字没动（照原文 create or replace）。自检：两重载各含 cooling_period 恰 1 次；**假 uid 查询不报错**（无委托史→返 null→放行）。
- 现状：库里目前 0 条已结束的委托（`revoked_at` 非空行 = 0），所以规则暂时不会误伤任何人，装好待用。

## 前端（学生端 v381 → v382）
- 申请学生中介的入口，拿到服务端的 `cooling_period:N` 时，**不显原始报错**，改显四语友好提示：
  - 中文：「委托关系结束后 90 天内暂不能申请成为学生中介，还剩 N 天。」
  - 英/俄/法各一句（内联，**没新增翻译词条，翻译包一个字没动**）。
- 不是冷却的其它错误 → 照旧显示。

## 自验（三层全过）
- **迁移层**：两重载各加冷却检查（cooling_hits=1）；假 uid 不报错；零删除。
- **前端**：申请入口 error 分支正则抓 `cooling_period:N`；headless 验证 N=45→中文含"45 天"、N=12→英文、非冷却错误→走原路；v382 零解析报错。
- 报告全文 `docs/self-test-report-JOB-6-06.md`；脚本 `~/mh-verify/gate8_job6_06.mjs`。

## 版本 / 提交 / 迁移
- 学生端 **v382**（旧 v381 保留）；index 4 处 + [VER] + changelog 均改。
- 迁移 **0190_student_agent_cooldown.sql** 已跑 + 落盘 `supabase/migrations/`。
- 主仓库：student v382 + index + changelog + 0190 + 报告，**已 commit 未 push**。

## 验收
真触发需要一个「委托关系刚结束不到 90 天」的学生账号去点申请学生中介（现库暂无这种账号）。规则已在数据层强制，前端提示也就位，留你日后真数据验收。
