JOB-2-11  结果：全闸通过（学校端 v225 → v226）

## 做了什么（一句白话）
把 JOB-2-10 建好的层级链，学校端这一半接通：
1. 名额（每档给多少）改成**读云端 `tier_quota` 配置表**（试用5/标准20/高级100/旗舰500），管理员改了配置学校端就跟着变。
2. 层级读 `schools.tier` 真值；去掉「不认识就当标准」那个兜底（迁移后值域已统一四档，兜底会把错误盖住）——改成回落 trial（与迁移「空→trial」一致）。**结果：名额跟着层级走，不再全是 20。**
3. 提额申请从"本地假流程"（QR- 随机号 + 10 秒假审核）**改成写真表 `school_quota_requests`**——学校提交后管理端能收到。
4. 名额/申请记录页**读真表里自己的申请状态**。

## 业主要看什么
1. 学校端看到的层级与管理端一致；名额跟着层级变（高级=100、旗舰=500，不再全 20）。
2. 提交提额申请后，管理端 Quota 审批台「学校提额申请」分区能看到（JOB-2-10 已建好那个分区）。
3. 申请记录页显示真实申请（不再是 10 秒假审核）。

## 十道闸（逐条）
- 闸1 基底 md5：`23928ee8f55294a017d62375e6e48143`（=v225）✓
- 闸2 锚点唯一：7 处各 =1 ✓
- 闸3 补丁自检：loadTierQuota/loadSchoolQuotaRequests 定义、tier_quota 查询、school_quota_requests insert+select、cloudTier 回落 trial、scheduleMockQuotaReview 调用归 0、QR- 随机号归 0、首访一次性拉取、错误 toast ru/fr 各命中 ✓
- 闸4 产物 md5：`ce8c2a9a624a4a5cdac7c6b514fc9451`（v226）
- 闸5 语法：2 脚本块 0 报错 ✓
- 闸6 消失检查：845→847（+2，无消失；scheduleMockQuotaReview/applyQuotaRequestDecision 函数体保留只断消费）✓
- 闸7 四语：学校端中英俄法四语。本卡唯一新增用户文案 = 提交失败 toast，已四语内联（zh/en/ru/fr，撇号用 ’）；成功 toast 复用现有 4 语 key；层级显示词沿用现有 labelZh/labelEn（zh/en，ru/fr 回落 en 系原有行为，未新增）；零裸键 ✓
- 闸8 真点击：Playwright 桩 —— 高级层级 getOfferAllocated=100（非 standard 20）；loadTierQuota 用桩 tier_quota 把 advanced 校准到 150；loadSchoolQuotaRequests 把云行映射进 state.quotaRequests（id/额度/状态/理由）；submitQuotaRequest 走 insert('school_quota_requests', {school_id,requested_quota:300,status:pending})。反向样本 v225：loadTierQuota 未定义、submit 仍 QR- → GATE8 FAIL（有分辨力）✓
- 闸9 视觉：零新增写死颜色/CSS 类/字号（纯逻辑 + 复用现有渲染）✓
- 闸10 线上：不推主仓库，跳过

基底 md5 / 产物 md5：23928ee8f55294a017d62375e6e48143 → ce8c2a9a624a4a5cdac7c6b514fc9451
index.html 改了几处：见下（school-portal/index.html v225→v226）
提交哈希：主仓库本地提交，未推送
偏离或疑点：
- 提额「批准」只改申请状态、不自动加名额（与 JOB-2-10 admin_decide 一致：真加名额靠管理员升层级 admin_set_school_tier，学校端下次载入 schools.tier 反映）。故 loadSchoolQuotaRequests 里 decidedAmount 一律 0（不伪造增额）。
- ru/fr 的层级显示词沿用英文 labelEn（OFFER_QUOTA_TIERS 本就无 ru/fr 标签，属既有，未在本卡新增/改动）。
