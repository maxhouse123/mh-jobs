# JOB-20-04 回执 · 脚本卫生 + 走查脚本对齐

CC 交付。第二十轮 20-04。**不改前端、不部署、不推主仓库**（等收官卡1153）。本包为 shell 工装 + 走查脚本，无 portal 前端改动，故铁律11 三层自验（前端）不适用；按 §20-04 闸3 自证如下。

## 一句话
交付 `jobs/JOB-20-04/shell-lint.sh`（§0.6 三查，自身合规）→ 扫出 `jobs/JOB-19-03/deploy-watermark.sh` 第 **29/58** 行红（`$B：`/`$CODE，`），改为 `${B}：`/`${CODE}，` 后全绿；`jobs/JOB-20-04/online-walk.sh`+`.mjs` 从 19-06 复制、卡片选择器改真类名 `#_v167CardStack ._v167-card`，单探现役线上 **绿8/红0/N4**。

## shell-lint.sh 三查
- **查一**：裸 `$var` 紧贴非 ASCII 字节（perl `/\$[A-Za-z_]\w*[\x80-\xFF]/`）→ 报行号（bash 3.2 吃字节，应 `${var}`）。
- **查二**：正文（shebang 首行豁免）含 zsh 历史展开会误触的叹号（叹号紧贴非空白/非`=`/非`(`）或反引号 → 报行号。**关键**：linter 自身用 perl `\x21`/`\x60` 表示叹号/反引号（正文无字面量），描述用中文词，故扫自身为 0；`if ! cmd`（叹号+空格）不误报，`#!/bin/bash` shebang 豁免。
- **查三**：`bash -n` 或 `zsh -n` 不过 → 报。
- 自证：`bash -n`/`zsh -n` 均过；linter 扫自身 = **绿 0 命中**。

## 闸3 · 旧红新绿（证据 `jobs/JOB-20-04/lint-sweep.log`）
| 文件 | 改前 | 改后 |
|---|---|---|
| `jobs/JOB-19-03/deploy-watermark.sh` | **红 · 命中 2**（行29 `实得 $B：$(...)`、行58 `实得 $CODE，头=`） | **绿 0**（`${B}：`/`${CODE}，`；`"$B"` 判据等号内不动；`bash -n`/`zsh -n` 过） |
| `jobs/JOB-19-06/online-walk.sh` | 绿 0 | 绿 0（原件未动） |
| `jobs/JOB-20-01/deploy-watermark.sh` | 绿 0 | 绿 0（20-01 §0.6 已合规，复核确认） |
| `jobs/JOB-20-04/shell-lint.sh` | — | 绿 0（自身合规） |
| `jobs/JOB-20-04/online-walk.sh` | — | 绿 0 |
- 最终整轮 sweep：`LINT 全绿 (0 命中)`（exit 0）。

## online-walk（从 19-06 复制后改，19-06 原件一字未动）
- **卡片选择器**（§20-04 唯一功能改点）：`.v167-soft-card, #r2SoftToast, [class*="soft-card"], [class*="softWarn"]` → **`#_v167CardStack ._v167-card, #_v167CardStack`**（partner v168 未重命名该类，grep 实证 v167/v168 均用 `_v167CardStack`/`_v167-card`，故 v167 名对两版皆真）。
- **输出路径**（为不覆盖 19-06 回执/截图，必要改）：PKG `19-06→20-04`；回执 `~/mh-jobs/JOB-20-04-online.md`；截图 `~/mh-jobs/shots/20/walk`；`shots/JOB-20`；md 标题/首段改第二十轮口径（去掉 19-06「现役=第十八轮」旧叙述——现役=第十九轮已上线，本包不覆盖第二十轮未推送新版）。**其余逻辑一字未动**（顶部代码注释保留 18-06/19-06 血缘记录，非用户可见输出）。
- **单探现役线上**（`MH_WALK_NO_WAIT=1 MH_WALK_NO_PUSH=1`，只读、不推、不写库；ANON_KEY 用 env.mjs 公开公钥；直连现役线上）：**绿8/红0/N/A4**（`~/mh-jobs/JOB-20-04-online.md`）：
  - ① [VER]：student v398 / partner v167 / school v234 三端直读绿；admin/reviewer CF Access 302 → N/A（挡）。
  - ② schX 真登中介端 → 被拒（locked=true / session=false / 无机构内容 / 见拒绝文案）绿；无 bootstrap 请求绿；0 报错绿。
  - ③ partner1 真登 → console `[VER]=v167` 绿；0 报错绿；Q1 提醒卡片 **N/A**（登入后无现成告警场景才不量——§规矩「有才判」，不主动触发上传避免写库）；公开截图 **N/A**（零信任 PII 红线不产出）。
  - 说明：卡片选择器新值本次未被触发（无现成 Q1 场景），代码已改并 grep 实证；真正量到样式须待有告警场景，如实记 N/A。

## 新闸
- **N1**：本包无 portal 版本升级（纯 jobs 工装），三端前端一字未改。
- **N3 带身份**：online-walk 用 partner1/schX 真登现役线上（只读观察），账号只从 `~/mh-verify/accounts.json` 读；ANON_KEY 为公开公钥；token/密码不打印；公开截图零产出。
- **N5 secret-scan**：`jobs/JOB-20-04/*.sh`、`*.mjs`、`~/mh-jobs/JOB-20-04-online.md`、`shots/20/walk/INDEX.md` 无硬编码 token/key/私钥，SECRET-SCAN-CLEAN。
- **N7**：本包无「给人看的新提示」UI，不涉。

## 交付物
- 主仓库（本地 commit、未 push）：`jobs/JOB-20-04/shell-lint.sh`、`jobs/JOB-20-04/online-walk.sh`、`jobs/JOB-20-04/online-walk.mjs`、`jobs/JOB-19-03/deploy-watermark.sh`（29/58 修复）。`.log`（walk-run/lint-sweep）本机 gitignore。
- mh-jobs（随本回执 push）：`JOB-20-04.md`、`JOB-20-04-online.md`、`shots/20/walk/INDEX.md`。

## 要业主亲眼看的（收官汇总用）
- 无新视觉件。工装类：如需复核，业主可跑 `bash /Volumes/Dev/MAXHOUSE/jobs/JOB-20-04/shell-lint.sh <任意.sh>`。

## 交接
- **下一包 = 20-05 收官**（写 `~/mh-jobs/SUMMARY-20.md`：单独顶格一行 `PUSH_OK=yes/no`；五端新版本+md5、watermark-doc v35「待部署」；每包一句话；旧红→新绿截图名；未放行清单；待业主手贴 SQL 清单；「要业主亲眼看的」清单；「第二十一轮建议包」含冷启动结论/保温方案）。
- 主仓库源码本地 commit、**未 push**（等收官业主卡1153）。
